(() => {
  var __create = Object.create;
  var __defProp = Object.defineProperty;
  var __getOwnPropDesc = Object.getOwnPropertyDescriptor;
  var __getOwnPropNames = Object.getOwnPropertyNames;
  var __getProtoOf = Object.getPrototypeOf;
  var __hasOwnProp = Object.prototype.hasOwnProperty;
  var __esm = (fn, res) => function __init() {
    return fn && (res = (0, fn[__getOwnPropNames(fn)[0]])(fn = 0)), res;
  };
  var __commonJS = (cb, mod) => function __require() {
    return mod || (0, cb[__getOwnPropNames(cb)[0]])((mod = { exports: {} }).exports, mod), mod.exports;
  };
  var __export = (target, all) => {
    for (var name in all)
      __defProp(target, name, { get: all[name], enumerable: true });
  };
  var __copyProps = (to, from, except, desc) => {
    if (from && typeof from === "object" || typeof from === "function") {
      for (let key of __getOwnPropNames(from))
        if (!__hasOwnProp.call(to, key) && key !== except)
          __defProp(to, key, { get: () => from[key], enumerable: !(desc = __getOwnPropDesc(from, key)) || desc.enumerable });
    }
    return to;
  };
  var __toESM = (mod, isNodeMode, target) => (target = mod != null ? __create(__getProtoOf(mod)) : {}, __copyProps(
    // If the importer is in node compatibility mode or this is not an ESM
    // file that has been converted to a CommonJS file using a Babel-
    // compatible transform (i.e. "__esModule" has not been set), then set
    // "default" to the CommonJS "module.exports" for node compatibility.
    isNodeMode || !mod || !mod.__esModule ? __defProp(target, "default", { value: mod, enumerable: true }) : target,
    mod
  ));

  // node_modules/@lit/reactive-element/css-tag.js
  var t, e, s, o, n, r, i, S, c;
  var init_css_tag = __esm({
    "node_modules/@lit/reactive-element/css-tag.js"() {
      t = globalThis;
      e = t.ShadowRoot && (void 0 === t.ShadyCSS || t.ShadyCSS.nativeShadow) && "adoptedStyleSheets" in Document.prototype && "replace" in CSSStyleSheet.prototype;
      s = /* @__PURE__ */ Symbol();
      o = /* @__PURE__ */ new WeakMap();
      n = class {
        constructor(t6, e9, o10) {
          if (this._$cssResult$ = true, o10 !== s) throw Error("CSSResult is not constructable. Use `unsafeCSS` or `css` instead.");
          this.cssText = t6, this.t = e9;
        }
        get styleSheet() {
          let t6 = this.o;
          const s6 = this.t;
          if (e && void 0 === t6) {
            const e9 = void 0 !== s6 && 1 === s6.length;
            e9 && (t6 = o.get(s6)), void 0 === t6 && ((this.o = t6 = new CSSStyleSheet()).replaceSync(this.cssText), e9 && o.set(s6, t6));
          }
          return t6;
        }
        toString() {
          return this.cssText;
        }
      };
      r = (t6) => new n("string" == typeof t6 ? t6 : t6 + "", void 0, s);
      i = (t6, ...e9) => {
        const o10 = 1 === t6.length ? t6[0] : e9.reduce((e10, s6, o11) => e10 + ((t7) => {
          if (true === t7._$cssResult$) return t7.cssText;
          if ("number" == typeof t7) return t7;
          throw Error("Value passed to 'css' function must be a 'css' function result: " + t7 + ". Use 'unsafeCSS' to pass non-literal values, but take care to ensure page security.");
        })(s6) + t6[o11 + 1], t6[0]);
        return new n(o10, t6, s);
      };
      S = (s6, o10) => {
        if (e) s6.adoptedStyleSheets = o10.map((t6) => t6 instanceof CSSStyleSheet ? t6 : t6.styleSheet);
        else for (const e9 of o10) {
          const o11 = document.createElement("style"), n7 = t.litNonce;
          void 0 !== n7 && o11.setAttribute("nonce", n7), o11.textContent = e9.cssText, s6.appendChild(o11);
        }
      };
      c = e ? (t6) => t6 : (t6) => t6 instanceof CSSStyleSheet ? ((t7) => {
        let e9 = "";
        for (const s6 of t7.cssRules) e9 += s6.cssText;
        return r(e9);
      })(t6) : t6;
    }
  });

  // node_modules/@lit/reactive-element/reactive-element.js
  var i2, e2, h, r2, o2, n2, a, c2, l, p, d, u, f, b, y;
  var init_reactive_element = __esm({
    "node_modules/@lit/reactive-element/reactive-element.js"() {
      init_css_tag();
      init_css_tag();
      ({ is: i2, defineProperty: e2, getOwnPropertyDescriptor: h, getOwnPropertyNames: r2, getOwnPropertySymbols: o2, getPrototypeOf: n2 } = Object);
      a = globalThis;
      c2 = a.trustedTypes;
      l = c2 ? c2.emptyScript : "";
      p = a.reactiveElementPolyfillSupport;
      d = (t6, s6) => t6;
      u = { toAttribute(t6, s6) {
        switch (s6) {
          case Boolean:
            t6 = t6 ? l : null;
            break;
          case Object:
          case Array:
            t6 = null == t6 ? t6 : JSON.stringify(t6);
        }
        return t6;
      }, fromAttribute(t6, s6) {
        let i8 = t6;
        switch (s6) {
          case Boolean:
            i8 = null !== t6;
            break;
          case Number:
            i8 = null === t6 ? null : Number(t6);
            break;
          case Object:
          case Array:
            try {
              i8 = JSON.parse(t6);
            } catch (t7) {
              i8 = null;
            }
        }
        return i8;
      } };
      f = (t6, s6) => !i2(t6, s6);
      b = { attribute: true, type: String, converter: u, reflect: false, useDefault: false, hasChanged: f };
      Symbol.metadata ??= /* @__PURE__ */ Symbol("metadata"), a.litPropertyMetadata ??= /* @__PURE__ */ new WeakMap();
      y = class extends HTMLElement {
        static addInitializer(t6) {
          this._$Ei(), (this.l ??= []).push(t6);
        }
        static get observedAttributes() {
          return this.finalize(), this._$Eh && [...this._$Eh.keys()];
        }
        static createProperty(t6, s6 = b) {
          if (s6.state && (s6.attribute = false), this._$Ei(), this.prototype.hasOwnProperty(t6) && ((s6 = Object.create(s6)).wrapped = true), this.elementProperties.set(t6, s6), !s6.noAccessor) {
            const i8 = /* @__PURE__ */ Symbol(), h4 = this.getPropertyDescriptor(t6, i8, s6);
            void 0 !== h4 && e2(this.prototype, t6, h4);
          }
        }
        static getPropertyDescriptor(t6, s6, i8) {
          const { get: e9, set: r6 } = h(this.prototype, t6) ?? { get() {
            return this[s6];
          }, set(t7) {
            this[s6] = t7;
          } };
          return { get: e9, set(s7) {
            const h4 = e9?.call(this);
            r6?.call(this, s7), this.requestUpdate(t6, h4, i8);
          }, configurable: true, enumerable: true };
        }
        static getPropertyOptions(t6) {
          return this.elementProperties.get(t6) ?? b;
        }
        static _$Ei() {
          if (this.hasOwnProperty(d("elementProperties"))) return;
          const t6 = n2(this);
          t6.finalize(), void 0 !== t6.l && (this.l = [...t6.l]), this.elementProperties = new Map(t6.elementProperties);
        }
        static finalize() {
          if (this.hasOwnProperty(d("finalized"))) return;
          if (this.finalized = true, this._$Ei(), this.hasOwnProperty(d("properties"))) {
            const t7 = this.properties, s6 = [...r2(t7), ...o2(t7)];
            for (const i8 of s6) this.createProperty(i8, t7[i8]);
          }
          const t6 = this[Symbol.metadata];
          if (null !== t6) {
            const s6 = litPropertyMetadata.get(t6);
            if (void 0 !== s6) for (const [t7, i8] of s6) this.elementProperties.set(t7, i8);
          }
          this._$Eh = /* @__PURE__ */ new Map();
          for (const [t7, s6] of this.elementProperties) {
            const i8 = this._$Eu(t7, s6);
            void 0 !== i8 && this._$Eh.set(i8, t7);
          }
          this.elementStyles = this.finalizeStyles(this.styles);
        }
        static finalizeStyles(s6) {
          const i8 = [];
          if (Array.isArray(s6)) {
            const e9 = new Set(s6.flat(1 / 0).reverse());
            for (const s7 of e9) i8.unshift(c(s7));
          } else void 0 !== s6 && i8.push(c(s6));
          return i8;
        }
        static _$Eu(t6, s6) {
          const i8 = s6.attribute;
          return false === i8 ? void 0 : "string" == typeof i8 ? i8 : "string" == typeof t6 ? t6.toLowerCase() : void 0;
        }
        constructor() {
          super(), this._$Ep = void 0, this.isUpdatePending = false, this.hasUpdated = false, this._$Em = null, this._$Ev();
        }
        _$Ev() {
          this._$ES = new Promise((t6) => this.enableUpdating = t6), this._$AL = /* @__PURE__ */ new Map(), this._$E_(), this.requestUpdate(), this.constructor.l?.forEach((t6) => t6(this));
        }
        addController(t6) {
          (this._$EO ??= /* @__PURE__ */ new Set()).add(t6), void 0 !== this.renderRoot && this.isConnected && t6.hostConnected?.();
        }
        removeController(t6) {
          this._$EO?.delete(t6);
        }
        _$E_() {
          const t6 = /* @__PURE__ */ new Map(), s6 = this.constructor.elementProperties;
          for (const i8 of s6.keys()) this.hasOwnProperty(i8) && (t6.set(i8, this[i8]), delete this[i8]);
          t6.size > 0 && (this._$Ep = t6);
        }
        createRenderRoot() {
          const t6 = this.shadowRoot ?? this.attachShadow(this.constructor.shadowRootOptions);
          return S(t6, this.constructor.elementStyles), t6;
        }
        connectedCallback() {
          this.renderRoot ??= this.createRenderRoot(), this.enableUpdating(true), this._$EO?.forEach((t6) => t6.hostConnected?.());
        }
        enableUpdating(t6) {
        }
        disconnectedCallback() {
          this._$EO?.forEach((t6) => t6.hostDisconnected?.());
        }
        attributeChangedCallback(t6, s6, i8) {
          this._$AK(t6, i8);
        }
        _$ET(t6, s6) {
          const i8 = this.constructor.elementProperties.get(t6), e9 = this.constructor._$Eu(t6, i8);
          if (void 0 !== e9 && true === i8.reflect) {
            const h4 = (void 0 !== i8.converter?.toAttribute ? i8.converter : u).toAttribute(s6, i8.type);
            this._$Em = t6, null == h4 ? this.removeAttribute(e9) : this.setAttribute(e9, h4), this._$Em = null;
          }
        }
        _$AK(t6, s6) {
          const i8 = this.constructor, e9 = i8._$Eh.get(t6);
          if (void 0 !== e9 && this._$Em !== e9) {
            const t7 = i8.getPropertyOptions(e9), h4 = "function" == typeof t7.converter ? { fromAttribute: t7.converter } : void 0 !== t7.converter?.fromAttribute ? t7.converter : u;
            this._$Em = e9;
            const r6 = h4.fromAttribute(s6, t7.type);
            this[e9] = r6 ?? this._$Ej?.get(e9) ?? r6, this._$Em = null;
          }
        }
        requestUpdate(t6, s6, i8, e9 = false, h4) {
          if (void 0 !== t6) {
            const r6 = this.constructor;
            if (false === e9 && (h4 = this[t6]), i8 ??= r6.getPropertyOptions(t6), !((i8.hasChanged ?? f)(h4, s6) || i8.useDefault && i8.reflect && h4 === this._$Ej?.get(t6) && !this.hasAttribute(r6._$Eu(t6, i8)))) return;
            this.C(t6, s6, i8);
          }
          false === this.isUpdatePending && (this._$ES = this._$EP());
        }
        C(t6, s6, { useDefault: i8, reflect: e9, wrapped: h4 }, r6) {
          i8 && !(this._$Ej ??= /* @__PURE__ */ new Map()).has(t6) && (this._$Ej.set(t6, r6 ?? s6 ?? this[t6]), true !== h4 || void 0 !== r6) || (this._$AL.has(t6) || (this.hasUpdated || i8 || (s6 = void 0), this._$AL.set(t6, s6)), true === e9 && this._$Em !== t6 && (this._$Eq ??= /* @__PURE__ */ new Set()).add(t6));
        }
        async _$EP() {
          this.isUpdatePending = true;
          try {
            await this._$ES;
          } catch (t7) {
            Promise.reject(t7);
          }
          const t6 = this.scheduleUpdate();
          return null != t6 && await t6, !this.isUpdatePending;
        }
        scheduleUpdate() {
          return this.performUpdate();
        }
        performUpdate() {
          if (!this.isUpdatePending) return;
          if (!this.hasUpdated) {
            if (this.renderRoot ??= this.createRenderRoot(), this._$Ep) {
              for (const [t8, s7] of this._$Ep) this[t8] = s7;
              this._$Ep = void 0;
            }
            const t7 = this.constructor.elementProperties;
            if (t7.size > 0) for (const [s7, i8] of t7) {
              const { wrapped: t8 } = i8, e9 = this[s7];
              true !== t8 || this._$AL.has(s7) || void 0 === e9 || this.C(s7, void 0, i8, e9);
            }
          }
          let t6 = false;
          const s6 = this._$AL;
          try {
            t6 = this.shouldUpdate(s6), t6 ? (this.willUpdate(s6), this._$EO?.forEach((t7) => t7.hostUpdate?.()), this.update(s6)) : this._$EM();
          } catch (s7) {
            throw t6 = false, this._$EM(), s7;
          }
          t6 && this._$AE(s6);
        }
        willUpdate(t6) {
        }
        _$AE(t6) {
          this._$EO?.forEach((t7) => t7.hostUpdated?.()), this.hasUpdated || (this.hasUpdated = true, this.firstUpdated(t6)), this.updated(t6);
        }
        _$EM() {
          this._$AL = /* @__PURE__ */ new Map(), this.isUpdatePending = false;
        }
        get updateComplete() {
          return this.getUpdateComplete();
        }
        getUpdateComplete() {
          return this._$ES;
        }
        shouldUpdate(t6) {
          return true;
        }
        update(t6) {
          this._$Eq &&= this._$Eq.forEach((t7) => this._$ET(t7, this[t7])), this._$EM();
        }
        updated(t6) {
        }
        firstUpdated(t6) {
        }
      };
      y.elementStyles = [], y.shadowRootOptions = { mode: "open" }, y[d("elementProperties")] = /* @__PURE__ */ new Map(), y[d("finalized")] = /* @__PURE__ */ new Map(), p?.({ ReactiveElement: y }), (a.reactiveElementVersions ??= []).push("2.1.2");
    }
  });

  // node_modules/lit-html/lit-html.js
  function V(t6, i8) {
    if (!u2(t6) || !t6.hasOwnProperty("raw")) throw Error("invalid template strings array");
    return void 0 !== e3 ? e3.createHTML(i8) : i8;
  }
  function M(t6, i8, s6 = t6, e9) {
    if (i8 === E) return i8;
    let h4 = void 0 !== e9 ? s6._$Co?.[e9] : s6._$Cl;
    const o10 = a2(i8) ? void 0 : i8._$litDirective$;
    return h4?.constructor !== o10 && (h4?._$AO?.(false), void 0 === o10 ? h4 = void 0 : (h4 = new o10(t6), h4._$AT(t6, s6, e9)), void 0 !== e9 ? (s6._$Co ??= [])[e9] = h4 : s6._$Cl = h4), void 0 !== h4 && (i8 = M(t6, h4._$AS(t6, i8.values), h4, e9)), i8;
  }
  var t2, i3, s2, e3, h2, o3, n3, r3, l2, c3, a2, u2, d2, f2, v, _, m, p2, g, $, y2, x, b2, w, T, E, A, C, P, N, S2, R, k, H, I, L, z, Z, j, B, D;
  var init_lit_html = __esm({
    "node_modules/lit-html/lit-html.js"() {
      t2 = globalThis;
      i3 = (t6) => t6;
      s2 = t2.trustedTypes;
      e3 = s2 ? s2.createPolicy("lit-html", { createHTML: (t6) => t6 }) : void 0;
      h2 = "$lit$";
      o3 = `lit$${Math.random().toFixed(9).slice(2)}$`;
      n3 = "?" + o3;
      r3 = `<${n3}>`;
      l2 = document;
      c3 = () => l2.createComment("");
      a2 = (t6) => null === t6 || "object" != typeof t6 && "function" != typeof t6;
      u2 = Array.isArray;
      d2 = (t6) => u2(t6) || "function" == typeof t6?.[Symbol.iterator];
      f2 = "[ 	\n\f\r]";
      v = /<(?:(!--|\/[^a-zA-Z])|(\/?[a-zA-Z][^>\s]*)|(\/?$))/g;
      _ = /-->/g;
      m = />/g;
      p2 = RegExp(`>|${f2}(?:([^\\s"'>=/]+)(${f2}*=${f2}*(?:[^ 	
\f\r"'\`<>=]|("|')|))|$)`, "g");
      g = /'/g;
      $ = /"/g;
      y2 = /^(?:script|style|textarea|title)$/i;
      x = (t6) => (i8, ...s6) => ({ _$litType$: t6, strings: i8, values: s6 });
      b2 = x(1);
      w = x(2);
      T = x(3);
      E = /* @__PURE__ */ Symbol.for("lit-noChange");
      A = /* @__PURE__ */ Symbol.for("lit-nothing");
      C = /* @__PURE__ */ new WeakMap();
      P = l2.createTreeWalker(l2, 129);
      N = (t6, i8) => {
        const s6 = t6.length - 1, e9 = [];
        let n7, l4 = 2 === i8 ? "<svg>" : 3 === i8 ? "<math>" : "", c6 = v;
        for (let i9 = 0; i9 < s6; i9++) {
          const s7 = t6[i9];
          let a4, u6, d3 = -1, f3 = 0;
          for (; f3 < s7.length && (c6.lastIndex = f3, u6 = c6.exec(s7), null !== u6); ) f3 = c6.lastIndex, c6 === v ? "!--" === u6[1] ? c6 = _ : void 0 !== u6[1] ? c6 = m : void 0 !== u6[2] ? (y2.test(u6[2]) && (n7 = RegExp("</" + u6[2], "g")), c6 = p2) : void 0 !== u6[3] && (c6 = p2) : c6 === p2 ? ">" === u6[0] ? (c6 = n7 ?? v, d3 = -1) : void 0 === u6[1] ? d3 = -2 : (d3 = c6.lastIndex - u6[2].length, a4 = u6[1], c6 = void 0 === u6[3] ? p2 : '"' === u6[3] ? $ : g) : c6 === $ || c6 === g ? c6 = p2 : c6 === _ || c6 === m ? c6 = v : (c6 = p2, n7 = void 0);
          const x2 = c6 === p2 && t6[i9 + 1].startsWith("/>") ? " " : "";
          l4 += c6 === v ? s7 + r3 : d3 >= 0 ? (e9.push(a4), s7.slice(0, d3) + h2 + s7.slice(d3) + o3 + x2) : s7 + o3 + (-2 === d3 ? i9 : x2);
        }
        return [V(t6, l4 + (t6[s6] || "<?>") + (2 === i8 ? "</svg>" : 3 === i8 ? "</math>" : "")), e9];
      };
      S2 = class _S {
        constructor({ strings: t6, _$litType$: i8 }, e9) {
          let r6;
          this.parts = [];
          let l4 = 0, a4 = 0;
          const u6 = t6.length - 1, d3 = this.parts, [f3, v3] = N(t6, i8);
          if (this.el = _S.createElement(f3, e9), P.currentNode = this.el.content, 2 === i8 || 3 === i8) {
            const t7 = this.el.content.firstChild;
            t7.replaceWith(...t7.childNodes);
          }
          for (; null !== (r6 = P.nextNode()) && d3.length < u6; ) {
            if (1 === r6.nodeType) {
              if (r6.hasAttributes()) for (const t7 of r6.getAttributeNames()) if (t7.endsWith(h2)) {
                const i9 = v3[a4++], s6 = r6.getAttribute(t7).split(o3), e10 = /([.?@])?(.*)/.exec(i9);
                d3.push({ type: 1, index: l4, name: e10[2], strings: s6, ctor: "." === e10[1] ? I : "?" === e10[1] ? L : "@" === e10[1] ? z : H }), r6.removeAttribute(t7);
              } else t7.startsWith(o3) && (d3.push({ type: 6, index: l4 }), r6.removeAttribute(t7));
              if (y2.test(r6.tagName)) {
                const t7 = r6.textContent.split(o3), i9 = t7.length - 1;
                if (i9 > 0) {
                  r6.textContent = s2 ? s2.emptyScript : "";
                  for (let s6 = 0; s6 < i9; s6++) r6.append(t7[s6], c3()), P.nextNode(), d3.push({ type: 2, index: ++l4 });
                  r6.append(t7[i9], c3());
                }
              }
            } else if (8 === r6.nodeType) if (r6.data === n3) d3.push({ type: 2, index: l4 });
            else {
              let t7 = -1;
              for (; -1 !== (t7 = r6.data.indexOf(o3, t7 + 1)); ) d3.push({ type: 7, index: l4 }), t7 += o3.length - 1;
            }
            l4++;
          }
        }
        static createElement(t6, i8) {
          const s6 = l2.createElement("template");
          return s6.innerHTML = t6, s6;
        }
      };
      R = class {
        constructor(t6, i8) {
          this._$AV = [], this._$AN = void 0, this._$AD = t6, this._$AM = i8;
        }
        get parentNode() {
          return this._$AM.parentNode;
        }
        get _$AU() {
          return this._$AM._$AU;
        }
        u(t6) {
          const { el: { content: i8 }, parts: s6 } = this._$AD, e9 = (t6?.creationScope ?? l2).importNode(i8, true);
          P.currentNode = e9;
          let h4 = P.nextNode(), o10 = 0, n7 = 0, r6 = s6[0];
          for (; void 0 !== r6; ) {
            if (o10 === r6.index) {
              let i9;
              2 === r6.type ? i9 = new k(h4, h4.nextSibling, this, t6) : 1 === r6.type ? i9 = new r6.ctor(h4, r6.name, r6.strings, this, t6) : 6 === r6.type && (i9 = new Z(h4, this, t6)), this._$AV.push(i9), r6 = s6[++n7];
            }
            o10 !== r6?.index && (h4 = P.nextNode(), o10++);
          }
          return P.currentNode = l2, e9;
        }
        p(t6) {
          let i8 = 0;
          for (const s6 of this._$AV) void 0 !== s6 && (void 0 !== s6.strings ? (s6._$AI(t6, s6, i8), i8 += s6.strings.length - 2) : s6._$AI(t6[i8])), i8++;
        }
      };
      k = class _k {
        get _$AU() {
          return this._$AM?._$AU ?? this._$Cv;
        }
        constructor(t6, i8, s6, e9) {
          this.type = 2, this._$AH = A, this._$AN = void 0, this._$AA = t6, this._$AB = i8, this._$AM = s6, this.options = e9, this._$Cv = e9?.isConnected ?? true;
        }
        get parentNode() {
          let t6 = this._$AA.parentNode;
          const i8 = this._$AM;
          return void 0 !== i8 && 11 === t6?.nodeType && (t6 = i8.parentNode), t6;
        }
        get startNode() {
          return this._$AA;
        }
        get endNode() {
          return this._$AB;
        }
        _$AI(t6, i8 = this) {
          t6 = M(this, t6, i8), a2(t6) ? t6 === A || null == t6 || "" === t6 ? (this._$AH !== A && this._$AR(), this._$AH = A) : t6 !== this._$AH && t6 !== E && this._(t6) : void 0 !== t6._$litType$ ? this.$(t6) : void 0 !== t6.nodeType ? this.T(t6) : d2(t6) ? this.k(t6) : this._(t6);
        }
        O(t6) {
          return this._$AA.parentNode.insertBefore(t6, this._$AB);
        }
        T(t6) {
          this._$AH !== t6 && (this._$AR(), this._$AH = this.O(t6));
        }
        _(t6) {
          this._$AH !== A && a2(this._$AH) ? this._$AA.nextSibling.data = t6 : this.T(l2.createTextNode(t6)), this._$AH = t6;
        }
        $(t6) {
          const { values: i8, _$litType$: s6 } = t6, e9 = "number" == typeof s6 ? this._$AC(t6) : (void 0 === s6.el && (s6.el = S2.createElement(V(s6.h, s6.h[0]), this.options)), s6);
          if (this._$AH?._$AD === e9) this._$AH.p(i8);
          else {
            const t7 = new R(e9, this), s7 = t7.u(this.options);
            t7.p(i8), this.T(s7), this._$AH = t7;
          }
        }
        _$AC(t6) {
          let i8 = C.get(t6.strings);
          return void 0 === i8 && C.set(t6.strings, i8 = new S2(t6)), i8;
        }
        k(t6) {
          u2(this._$AH) || (this._$AH = [], this._$AR());
          const i8 = this._$AH;
          let s6, e9 = 0;
          for (const h4 of t6) e9 === i8.length ? i8.push(s6 = new _k(this.O(c3()), this.O(c3()), this, this.options)) : s6 = i8[e9], s6._$AI(h4), e9++;
          e9 < i8.length && (this._$AR(s6 && s6._$AB.nextSibling, e9), i8.length = e9);
        }
        _$AR(t6 = this._$AA.nextSibling, s6) {
          for (this._$AP?.(false, true, s6); t6 !== this._$AB; ) {
            const s7 = i3(t6).nextSibling;
            i3(t6).remove(), t6 = s7;
          }
        }
        setConnected(t6) {
          void 0 === this._$AM && (this._$Cv = t6, this._$AP?.(t6));
        }
      };
      H = class {
        get tagName() {
          return this.element.tagName;
        }
        get _$AU() {
          return this._$AM._$AU;
        }
        constructor(t6, i8, s6, e9, h4) {
          this.type = 1, this._$AH = A, this._$AN = void 0, this.element = t6, this.name = i8, this._$AM = e9, this.options = h4, s6.length > 2 || "" !== s6[0] || "" !== s6[1] ? (this._$AH = Array(s6.length - 1).fill(new String()), this.strings = s6) : this._$AH = A;
        }
        _$AI(t6, i8 = this, s6, e9) {
          const h4 = this.strings;
          let o10 = false;
          if (void 0 === h4) t6 = M(this, t6, i8, 0), o10 = !a2(t6) || t6 !== this._$AH && t6 !== E, o10 && (this._$AH = t6);
          else {
            const e10 = t6;
            let n7, r6;
            for (t6 = h4[0], n7 = 0; n7 < h4.length - 1; n7++) r6 = M(this, e10[s6 + n7], i8, n7), r6 === E && (r6 = this._$AH[n7]), o10 ||= !a2(r6) || r6 !== this._$AH[n7], r6 === A ? t6 = A : t6 !== A && (t6 += (r6 ?? "") + h4[n7 + 1]), this._$AH[n7] = r6;
          }
          o10 && !e9 && this.j(t6);
        }
        j(t6) {
          t6 === A ? this.element.removeAttribute(this.name) : this.element.setAttribute(this.name, t6 ?? "");
        }
      };
      I = class extends H {
        constructor() {
          super(...arguments), this.type = 3;
        }
        j(t6) {
          this.element[this.name] = t6 === A ? void 0 : t6;
        }
      };
      L = class extends H {
        constructor() {
          super(...arguments), this.type = 4;
        }
        j(t6) {
          this.element.toggleAttribute(this.name, !!t6 && t6 !== A);
        }
      };
      z = class extends H {
        constructor(t6, i8, s6, e9, h4) {
          super(t6, i8, s6, e9, h4), this.type = 5;
        }
        _$AI(t6, i8 = this) {
          if ((t6 = M(this, t6, i8, 0) ?? A) === E) return;
          const s6 = this._$AH, e9 = t6 === A && s6 !== A || t6.capture !== s6.capture || t6.once !== s6.once || t6.passive !== s6.passive, h4 = t6 !== A && (s6 === A || e9);
          e9 && this.element.removeEventListener(this.name, this, s6), h4 && this.element.addEventListener(this.name, this, t6), this._$AH = t6;
        }
        handleEvent(t6) {
          "function" == typeof this._$AH ? this._$AH.call(this.options?.host ?? this.element, t6) : this._$AH.handleEvent(t6);
        }
      };
      Z = class {
        constructor(t6, i8, s6) {
          this.element = t6, this.type = 6, this._$AN = void 0, this._$AM = i8, this.options = s6;
        }
        get _$AU() {
          return this._$AM._$AU;
        }
        _$AI(t6) {
          M(this, t6);
        }
      };
      j = { M: h2, P: o3, A: n3, C: 1, L: N, R, D: d2, V: M, I: k, H, N: L, U: z, B: I, F: Z };
      B = t2.litHtmlPolyfillSupport;
      B?.(S2, k), (t2.litHtmlVersions ??= []).push("3.3.2");
      D = (t6, i8, s6) => {
        const e9 = s6?.renderBefore ?? i8;
        let h4 = e9._$litPart$;
        if (void 0 === h4) {
          const t7 = s6?.renderBefore ?? null;
          e9._$litPart$ = h4 = new k(i8.insertBefore(c3(), t7), t7, void 0, s6 ?? {});
        }
        return h4._$AI(t6), h4;
      };
    }
  });

  // node_modules/lit-element/lit-element.js
  var s3, i4, o4;
  var init_lit_element = __esm({
    "node_modules/lit-element/lit-element.js"() {
      init_reactive_element();
      init_reactive_element();
      init_lit_html();
      init_lit_html();
      s3 = globalThis;
      i4 = class extends y {
        constructor() {
          super(...arguments), this.renderOptions = { host: this }, this._$Do = void 0;
        }
        createRenderRoot() {
          const t6 = super.createRenderRoot();
          return this.renderOptions.renderBefore ??= t6.firstChild, t6;
        }
        update(t6) {
          const r6 = this.render();
          this.hasUpdated || (this.renderOptions.isConnected = this.isConnected), super.update(t6), this._$Do = D(r6, this.renderRoot, this.renderOptions);
        }
        connectedCallback() {
          super.connectedCallback(), this._$Do?.setConnected(true);
        }
        disconnectedCallback() {
          super.disconnectedCallback(), this._$Do?.setConnected(false);
        }
        render() {
          return E;
        }
      };
      i4._$litElement$ = true, i4["finalized"] = true, s3.litElementHydrateSupport?.({ LitElement: i4 });
      o4 = s3.litElementPolyfillSupport;
      o4?.({ LitElement: i4 });
      (s3.litElementVersions ??= []).push("4.2.2");
    }
  });

  // node_modules/lit-html/is-server.js
  var init_is_server = __esm({
    "node_modules/lit-html/is-server.js"() {
    }
  });

  // node_modules/lit/index.js
  var init_lit = __esm({
    "node_modules/lit/index.js"() {
      init_reactive_element();
      init_lit_html();
      init_lit_element();
      init_is_server();
    }
  });

  // node_modules/@lit/reactive-element/decorators/custom-element.js
  var t3;
  var init_custom_element = __esm({
    "node_modules/@lit/reactive-element/decorators/custom-element.js"() {
      t3 = (t6) => (e9, o10) => {
        void 0 !== o10 ? o10.addInitializer(() => {
          customElements.define(t6, e9);
        }) : customElements.define(t6, e9);
      };
    }
  });

  // node_modules/@lit/reactive-element/decorators/property.js
  function n4(t6) {
    return (e9, o10) => "object" == typeof o10 ? r4(t6, e9, o10) : ((t7, e10, o11) => {
      const r6 = e10.hasOwnProperty(o11);
      return e10.constructor.createProperty(o11, t7), r6 ? Object.getOwnPropertyDescriptor(e10, o11) : void 0;
    })(t6, e9, o10);
  }
  var o5, r4;
  var init_property = __esm({
    "node_modules/@lit/reactive-element/decorators/property.js"() {
      init_reactive_element();
      o5 = { attribute: true, type: String, converter: u, reflect: false, hasChanged: f };
      r4 = (t6 = o5, e9, r6) => {
        const { kind: n7, metadata: i8 } = r6;
        let s6 = globalThis.litPropertyMetadata.get(i8);
        if (void 0 === s6 && globalThis.litPropertyMetadata.set(i8, s6 = /* @__PURE__ */ new Map()), "setter" === n7 && ((t6 = Object.create(t6)).wrapped = true), s6.set(r6.name, t6), "accessor" === n7) {
          const { name: o10 } = r6;
          return { set(r7) {
            const n8 = e9.get.call(this);
            e9.set.call(this, r7), this.requestUpdate(o10, n8, t6, true, r7);
          }, init(e10) {
            return void 0 !== e10 && this.C(o10, void 0, t6, e10), e10;
          } };
        }
        if ("setter" === n7) {
          const { name: o10 } = r6;
          return function(r7) {
            const n8 = this[o10];
            e9.call(this, r7), this.requestUpdate(o10, n8, t6, true, r7);
          };
        }
        throw Error("Unsupported decorator location: " + n7);
      };
    }
  });

  // node_modules/@lit/reactive-element/decorators/state.js
  function r5(r6) {
    return n4({ ...r6, state: true, attribute: false });
  }
  var init_state = __esm({
    "node_modules/@lit/reactive-element/decorators/state.js"() {
      init_property();
    }
  });

  // node_modules/@lit/reactive-element/decorators/event-options.js
  var init_event_options = __esm({
    "node_modules/@lit/reactive-element/decorators/event-options.js"() {
    }
  });

  // node_modules/@lit/reactive-element/decorators/base.js
  var e4;
  var init_base = __esm({
    "node_modules/@lit/reactive-element/decorators/base.js"() {
      e4 = (e9, t6, c6) => (c6.configurable = true, c6.enumerable = true, Reflect.decorate && "object" != typeof t6 && Object.defineProperty(e9, t6, c6), c6);
    }
  });

  // node_modules/@lit/reactive-element/decorators/query.js
  function e5(e9, r6) {
    return (n7, s6, i8) => {
      const o10 = (t6) => t6.renderRoot?.querySelector(e9) ?? null;
      if (r6) {
        const { get: e10, set: r7 } = "object" == typeof s6 ? n7 : i8 ?? /* @__PURE__ */ (() => {
          const t6 = /* @__PURE__ */ Symbol();
          return { get() {
            return this[t6];
          }, set(e11) {
            this[t6] = e11;
          } };
        })();
        return e4(n7, s6, { get() {
          let t6 = e10.call(this);
          return void 0 === t6 && (t6 = o10(this), (null !== t6 || this.hasUpdated) && r7.call(this, t6)), t6;
        } });
      }
      return e4(n7, s6, { get() {
        return o10(this);
      } });
    };
  }
  var init_query = __esm({
    "node_modules/@lit/reactive-element/decorators/query.js"() {
      init_base();
    }
  });

  // node_modules/@lit/reactive-element/decorators/query-all.js
  var init_query_all = __esm({
    "node_modules/@lit/reactive-element/decorators/query-all.js"() {
      init_base();
    }
  });

  // node_modules/@lit/reactive-element/decorators/query-async.js
  var init_query_async = __esm({
    "node_modules/@lit/reactive-element/decorators/query-async.js"() {
      init_base();
    }
  });

  // node_modules/@lit/reactive-element/decorators/query-assigned-elements.js
  var init_query_assigned_elements = __esm({
    "node_modules/@lit/reactive-element/decorators/query-assigned-elements.js"() {
      init_base();
    }
  });

  // node_modules/@lit/reactive-element/decorators/query-assigned-nodes.js
  var init_query_assigned_nodes = __esm({
    "node_modules/@lit/reactive-element/decorators/query-assigned-nodes.js"() {
      init_base();
    }
  });

  // node_modules/lit/decorators.js
  var init_decorators = __esm({
    "node_modules/lit/decorators.js"() {
      init_custom_element();
      init_property();
      init_state();
      init_event_options();
      init_query();
      init_query_all();
      init_query_async();
      init_query_assigned_elements();
      init_query_assigned_nodes();
    }
  });

  // node_modules/@nldd/design-system/dist/assets/styles/slotted-reset.js
  var slottedReset, inheritedTextReset;
  var init_slotted_reset = __esm({
    "node_modules/@nldd/design-system/dist/assets/styles/slotted-reset.js"() {
      init_lit();
      slottedReset = i`
	all: revert !important;
`;
      inheritedTextReset = i`
	text-align: start !important;
	letter-spacing: normal !important;
	word-spacing: normal !important;
	text-transform: none !important;
`;
    }
  });

  // node_modules/@nldd/design-system/dist/components/actions/button/button.styles.js
  var buttonStyles;
  var init_button_styles = __esm({
    "node_modules/@nldd/design-system/dist/components/actions/button/button.styles.js"() {
      init_lit();
      init_slotted_reset();
      buttonStyles = i`


	/* # Host */

	:host {
		--_corner-radius: var(--semantics-controls-md-corner-radius);
		--_width: auto;
		--_min-size: var(--semantics-controls-md-min-size);
		--_block-padding: var(--semantics-controls-md-block-padding);
		--_inline-padding: calc((var(--_min-size) - var(--_icon-size)) / 2);
		--_gap: var(--semantics-buttons-md-gap);
		--_font: var(--semantics-buttons-md-primary-text-font);
		--_icon-size: var(--semantics-buttons-md-icon-size);
		--_disclosure-icon-size: var(--primitives-space-20);
		--_supporting-font: var(--semantics-buttons-md-supporting-text-font);
		--_background-color: var(--semantics-buttons-neutral-tinted-background-color);
		--_primary-content-color: var(--semantics-buttons-neutral-tinted-primary-content-color);
		--_secondary-content-color: var(--semantics-buttons-neutral-tinted-secondary-content-color);
		--_highlight-border-color: var(--semantics-buttons-neutral-tinted-highlight-border-color);
		--_is-hovered-background-color: var(--semantics-buttons-neutral-tinted-is-hovered-background-color);
		--_is-hovered-primary-content-color: var(--semantics-buttons-neutral-tinted-is-hovered-primary-content-color);
		--_is-hovered-secondary-content-color: var(--semantics-buttons-neutral-tinted-is-hovered-secondary-content-color);
		--_is-hovered-highlight-border-color: var(--semantics-buttons-neutral-tinted-is-hovered-highlight-border-color);
		--_is-active-background-color: var(--semantics-buttons-neutral-tinted-is-active-background-color);
		--_is-active-primary-content-color: var(--semantics-buttons-neutral-tinted-is-active-primary-content-color);
		--_is-active-secondary-content-color: var(--semantics-buttons-neutral-tinted-is-active-secondary-content-color);
		--_is-active-highlight-border-color: var(--semantics-buttons-neutral-tinted-is-active-highlight-border-color);

		${inheritedTextReset}
		display: inline-block;
		position: relative;
		max-width: 100%;
		user-select: none;
		-webkit-tap-highlight-color: transparent;
	}

	:host([size="xs"]) {
		--_corner-radius: var(--semantics-controls-xs-corner-radius);
		--_min-size: var(--semantics-controls-xs-min-size);
		--_block-padding: var(--semantics-controls-xs-block-padding);
		--_gap: var(--semantics-buttons-xs-gap);
		--_font: var(--semantics-buttons-xs-primary-text-font);
		--_icon-size: var(--semantics-buttons-xs-icon-size);
		--_disclosure-icon-size: var(--primitives-space-16);
		--_supporting-font: var(--semantics-buttons-xs-supporting-text-font);
	}

	:host([size="sm"]) {
		--_corner-radius: var(--semantics-controls-sm-corner-radius);
		--_min-size: var(--semantics-controls-sm-min-size);
		--_block-padding: var(--semantics-controls-sm-block-padding);
		--_gap: var(--semantics-buttons-sm-gap);
		--_font: var(--semantics-buttons-sm-primary-text-font);
		--_icon-size: var(--semantics-buttons-sm-icon-size);
		--_disclosure-icon-size: var(--primitives-space-18);
		--_supporting-font: var(--semantics-buttons-sm-supporting-text-font);
	}

	:host([size="lg"]) {
		--_corner-radius: var(--semantics-controls-lg-corner-radius);
		--_min-size: var(--semantics-controls-lg-min-size);
		--_block-padding: var(--semantics-controls-lg-block-padding);
		--_gap: var(--semantics-buttons-lg-gap);
		--_font: var(--semantics-buttons-lg-primary-text-font);
		--_icon-size: var(--semantics-buttons-lg-icon-size);
		--_disclosure-icon-size: var(--primitives-space-24);
		--_supporting-font: var(--semantics-buttons-lg-supporting-text-font);
	}

	:host([variant="neutral-base"]) {
		--_background-color: var(--semantics-buttons-neutral-base-background-color);
		--_primary-content-color: var(--semantics-buttons-neutral-base-primary-content-color);
		--_secondary-content-color: var(--semantics-buttons-neutral-base-secondary-content-color);
		--_highlight-border-color: var(--semantics-buttons-neutral-base-highlight-border-color);
		--_is-hovered-background-color: var(--semantics-buttons-neutral-base-is-hovered-background-color);
		--_is-hovered-primary-content-color: var(--semantics-buttons-neutral-base-is-hovered-primary-content-color);
		--_is-hovered-secondary-content-color: var(--semantics-buttons-neutral-base-is-hovered-secondary-content-color);
		--_is-hovered-highlight-border-color: var(--semantics-buttons-neutral-base-is-hovered-highlight-border-color);
		--_is-active-background-color: var(--semantics-buttons-neutral-base-is-active-background-color);
		--_is-active-primary-content-color: var(--semantics-buttons-neutral-base-is-active-primary-content-color);
		--_is-active-secondary-content-color: var(--semantics-buttons-neutral-base-is-active-secondary-content-color);
		--_is-active-highlight-border-color: var(--semantics-buttons-neutral-base-is-active-highlight-border-color);
	}

	:host([variant="neutral-transparent"]) {
		--_background-color: transparent;
		--_primary-content-color: var(--semantics-buttons-neutral-transparent-primary-content-color);
		--_secondary-content-color: var(--semantics-buttons-neutral-transparent-secondary-content-color);
		--_highlight-border-color: transparent;
		--_is-hovered-background-color: transparent;
		--_is-hovered-primary-content-color: var(--semantics-buttons-neutral-transparent-is-hovered-primary-content-color);
		--_is-hovered-secondary-content-color: var(--semantics-buttons-neutral-transparent-is-hovered-secondary-content-color);
		--_is-hovered-highlight-border-color: transparent;
		--_is-active-background-color: transparent;
		--_is-active-primary-content-color: var(--semantics-buttons-neutral-transparent-is-active-primary-content-color);
		--_is-active-secondary-content-color: var(--semantics-buttons-neutral-transparent-is-active-secondary-content-color);
		--_is-active-highlight-border-color: transparent;
	}

	:host([variant="accent-filled"]),
	:host([variant="primary"]) {
		--_background-color: var(--semantics-buttons-accent-filled-background-color);
		--_primary-content-color: var(--semantics-buttons-accent-filled-primary-content-color);
		--_secondary-content-color: var(--semantics-buttons-accent-filled-secondary-content-color);
		--_highlight-border-color: var(--semantics-buttons-accent-filled-highlight-border-color);
		--_is-hovered-background-color: var(--semantics-buttons-accent-filled-is-hovered-background-color);
		--_is-hovered-primary-content-color: var(--semantics-buttons-accent-filled-is-hovered-primary-content-color);
		--_is-hovered-secondary-content-color: var(--semantics-buttons-accent-filled-is-hovered-secondary-content-color);
		--_is-hovered-highlight-border-color: var(--semantics-buttons-accent-filled-is-hovered-highlight-border-color);
		--_is-active-background-color: var(--semantics-buttons-accent-filled-is-active-background-color);
		--_is-active-primary-content-color: var(--semantics-buttons-accent-filled-is-active-primary-content-color);
		--_is-active-secondary-content-color: var(--semantics-buttons-accent-filled-is-active-secondary-content-color);
		--_is-active-highlight-border-color: var(--semantics-buttons-accent-filled-is-active-highlight-border-color);
	}

	:host([variant="accent-transparent"]) {
		--_background-color: transparent;
		--_primary-content-color: var(--semantics-buttons-accent-transparent-primary-content-color);
		--_secondary-content-color: var(--semantics-buttons-accent-transparent-secondary-content-color);
		--_highlight-border-color: transparent;
		--_is-hovered-background-color: transparent;
		--_is-hovered-primary-content-color: var(--semantics-buttons-accent-transparent-is-hovered-primary-content-color);
		--_is-hovered-secondary-content-color: var(--semantics-buttons-accent-transparent-is-hovered-secondary-content-color);
		--_is-hovered-highlight-border-color: transparent;
		--_is-active-background-color: transparent;
		--_is-active-primary-content-color: var(--semantics-buttons-accent-transparent-is-active-primary-content-color);
		--_is-active-secondary-content-color: var(--semantics-buttons-accent-transparent-is-active-secondary-content-color);
		--_is-active-highlight-border-color: transparent;
	}

	:host([variant="critical-tinted"]),
	:host([variant="destructive"]) {
		--_background-color: var(--semantics-buttons-critical-tinted-background-color);
		--_primary-content-color: var(--semantics-buttons-critical-tinted-primary-content-color);
		--_secondary-content-color: var(--semantics-buttons-critical-tinted-secondary-content-color);
		--_highlight-border-color: var(--semantics-buttons-critical-tinted-highlight-border-color);
		--_is-hovered-background-color: var(--semantics-buttons-critical-tinted-is-hovered-background-color);
		--_is-hovered-primary-content-color: var(--semantics-buttons-critical-tinted-is-hovered-primary-content-color);
		--_is-hovered-secondary-content-color: var(--semantics-buttons-critical-tinted-is-hovered-secondary-content-color);
		--_is-hovered-highlight-border-color: var(--semantics-buttons-critical-tinted-is-hovered-highlight-border-color);
		--_is-active-background-color: var(--semantics-buttons-critical-tinted-is-active-background-color);
		--_is-active-primary-content-color: var(--semantics-buttons-critical-tinted-is-active-primary-content-color);
		--_is-active-secondary-content-color: var(--semantics-buttons-critical-tinted-is-active-secondary-content-color);
		--_is-active-highlight-border-color: var(--semantics-buttons-critical-tinted-is-active-highlight-border-color);
	}

	:host([variant="critical-transparent"]) {
		--_background-color: transparent;
		--_primary-content-color: var(--semantics-buttons-critical-transparent-primary-content-color);
		--_secondary-content-color: var(--semantics-buttons-critical-transparent-secondary-content-color);
		--_highlight-border-color: transparent;
		--_is-hovered-background-color: transparent;
		--_is-hovered-primary-content-color: var(--semantics-buttons-critical-transparent-is-hovered-primary-content-color);
		--_is-hovered-secondary-content-color: var(--semantics-buttons-critical-transparent-is-hovered-secondary-content-color);
		--_is-hovered-highlight-border-color: transparent;
		--_is-active-background-color: transparent;
		--_is-active-primary-content-color: var(--semantics-buttons-critical-transparent-is-active-primary-content-color);
		--_is-active-secondary-content-color: var(--semantics-buttons-critical-transparent-is-active-secondary-content-color);
		--_is-active-highlight-border-color: transparent;
	}

	/* ## Expanded — default (incl. unknown variant) */

	:host([expanded]) {
		--_background-color: var(--semantics-buttons-neutral-tinted-is-expanded-background-color);
		--_primary-content-color: var(--semantics-buttons-neutral-tinted-is-expanded-primary-content-color);
		--_secondary-content-color: var(--semantics-buttons-neutral-tinted-is-expanded-secondary-content-color);
		--_highlight-border-color: var(--semantics-buttons-neutral-tinted-is-expanded-highlight-border-color);
		--_is-hovered-background-color: var(--semantics-buttons-neutral-tinted-is-expanded-is-hovered-background-color);
		--_is-hovered-primary-content-color: var(--semantics-buttons-neutral-tinted-is-expanded-is-hovered-primary-content-color);
		--_is-hovered-secondary-content-color: var(--semantics-buttons-neutral-tinted-is-expanded-is-hovered-secondary-content-color);
		--_is-hovered-highlight-border-color: var(--semantics-buttons-neutral-tinted-is-expanded-is-hovered-highlight-border-color);
		--_is-active-background-color: var(--semantics-buttons-neutral-tinted-is-expanded-is-active-background-color);
		--_is-active-primary-content-color: var(--semantics-buttons-neutral-tinted-is-expanded-is-active-primary-content-color);
		--_is-active-secondary-content-color: var(--semantics-buttons-neutral-tinted-is-expanded-is-active-secondary-content-color);
		--_is-active-highlight-border-color: var(--semantics-buttons-neutral-tinted-is-expanded-is-active-highlight-border-color);
	}

	:host([expanded][variant="neutral-base"]) {
		--_background-color: var(--semantics-buttons-neutral-base-is-expanded-background-color);
		--_primary-content-color: var(--semantics-buttons-neutral-base-is-expanded-primary-content-color);
		--_secondary-content-color: var(--semantics-buttons-neutral-base-is-expanded-secondary-content-color);
		--_highlight-border-color: var(--semantics-buttons-neutral-base-is-expanded-highlight-border-color);
		--_is-hovered-background-color: var(--semantics-buttons-neutral-base-is-expanded-is-hovered-background-color);
		--_is-hovered-primary-content-color: var(--semantics-buttons-neutral-base-is-expanded-is-hovered-primary-content-color);
		--_is-hovered-secondary-content-color: var(--semantics-buttons-neutral-base-is-expanded-is-hovered-secondary-content-color);
		--_is-hovered-highlight-border-color: var(--semantics-buttons-neutral-base-is-expanded-is-hovered-highlight-border-color);
		--_is-active-background-color: var(--semantics-buttons-neutral-base-is-expanded-is-active-background-color);
		--_is-active-primary-content-color: var(--semantics-buttons-neutral-base-is-expanded-is-active-primary-content-color);
		--_is-active-secondary-content-color: var(--semantics-buttons-neutral-base-is-expanded-is-active-secondary-content-color);
		--_is-active-highlight-border-color: var(--semantics-buttons-neutral-base-is-expanded-is-active-highlight-border-color);
	}

	:host([expanded][variant="neutral-transparent"]) {
		--_background-color: transparent;
		--_primary-content-color: var(--semantics-buttons-neutral-transparent-primary-content-color);
		--_secondary-content-color: var(--semantics-buttons-neutral-transparent-secondary-content-color);
		--_highlight-border-color: transparent;
		--_is-hovered-background-color: transparent;
		--_is-hovered-primary-content-color: var(--semantics-buttons-neutral-transparent-is-hovered-primary-content-color);
		--_is-hovered-secondary-content-color: var(--semantics-buttons-neutral-transparent-is-hovered-secondary-content-color);
		--_is-hovered-highlight-border-color: transparent;
		--_is-active-background-color: transparent;
		--_is-active-primary-content-color: var(--semantics-buttons-neutral-transparent-is-active-primary-content-color);
		--_is-active-secondary-content-color: var(--semantics-buttons-neutral-transparent-is-active-secondary-content-color);
		--_is-active-highlight-border-color: transparent;
	}

	:host([expanded][variant="accent-filled"]),
	:host([expanded][variant="primary"]) {
		--_background-color: var(--semantics-buttons-accent-filled-is-expanded-background-color);
		--_primary-content-color: var(--semantics-buttons-accent-filled-is-expanded-primary-content-color);
		--_secondary-content-color: var(--semantics-buttons-accent-filled-is-expanded-secondary-content-color);
		--_highlight-border-color: var(--semantics-buttons-accent-filled-is-expanded-highlight-border-color);
		--_is-hovered-background-color: var(--semantics-buttons-accent-filled-is-expanded-is-hovered-background-color);
		--_is-hovered-primary-content-color: var(--semantics-buttons-accent-filled-is-expanded-is-hovered-primary-content-color);
		--_is-hovered-secondary-content-color: var(--semantics-buttons-accent-filled-is-expanded-is-hovered-secondary-content-color);
		--_is-hovered-highlight-border-color: var(--semantics-buttons-accent-filled-is-expanded-is-hovered-highlight-border-color);
		--_is-active-background-color: var(--semantics-buttons-accent-filled-is-expanded-is-active-background-color);
		--_is-active-primary-content-color: var(--semantics-buttons-accent-filled-is-expanded-is-active-primary-content-color);
		--_is-active-secondary-content-color: var(--semantics-buttons-accent-filled-is-expanded-is-active-secondary-content-color);
		--_is-active-highlight-border-color: var(--semantics-buttons-accent-filled-is-expanded-is-active-highlight-border-color);
	}

	:host([expanded][variant="accent-transparent"]) {
		--_background-color: transparent;
		--_primary-content-color: var(--semantics-buttons-accent-transparent-primary-content-color);
		--_secondary-content-color: var(--semantics-buttons-accent-transparent-secondary-content-color);
		--_highlight-border-color: transparent;
		--_is-hovered-background-color: transparent;
		--_is-hovered-primary-content-color: var(--semantics-buttons-accent-transparent-is-hovered-primary-content-color);
		--_is-hovered-secondary-content-color: var(--semantics-buttons-accent-transparent-is-hovered-secondary-content-color);
		--_is-hovered-highlight-border-color: transparent;
		--_is-active-background-color: transparent;
		--_is-active-primary-content-color: var(--semantics-buttons-accent-transparent-is-active-primary-content-color);
		--_is-active-secondary-content-color: var(--semantics-buttons-accent-transparent-is-active-secondary-content-color);
		--_is-active-highlight-border-color: transparent;
	}

	:host([expanded][variant="critical-tinted"]),
	:host([expanded][variant="destructive"]) {
		--_background-color: var(--semantics-buttons-critical-tinted-is-expanded-background-color);
		--_primary-content-color: var(--semantics-buttons-critical-tinted-is-expanded-primary-content-color);
		--_secondary-content-color: var(--semantics-buttons-critical-tinted-is-expanded-secondary-content-color);
		--_highlight-border-color: var(--semantics-buttons-critical-tinted-is-expanded-highlight-border-color);
		--_is-hovered-background-color: var(--semantics-buttons-critical-tinted-is-expanded-is-hovered-background-color);
		--_is-hovered-primary-content-color: var(--semantics-buttons-critical-tinted-is-expanded-is-hovered-primary-content-color);
		--_is-hovered-secondary-content-color: var(--semantics-buttons-critical-tinted-is-expanded-is-hovered-secondary-content-color);
		--_is-hovered-highlight-border-color: var(--semantics-buttons-critical-tinted-is-expanded-is-hovered-highlight-border-color);
		--_is-active-background-color: var(--semantics-buttons-critical-tinted-is-expanded-is-active-background-color);
		--_is-active-primary-content-color: var(--semantics-buttons-critical-tinted-is-expanded-is-active-primary-content-color);
		--_is-active-secondary-content-color: var(--semantics-buttons-critical-tinted-is-expanded-is-active-secondary-content-color);
		--_is-active-highlight-border-color: var(--semantics-buttons-critical-tinted-is-expanded-is-active-highlight-border-color);
	}

	:host([expanded][variant="critical-transparent"]) {
		--_background-color: transparent;
		--_primary-content-color: var(--semantics-buttons-critical-transparent-primary-content-color);
		--_secondary-content-color: var(--semantics-buttons-critical-transparent-secondary-content-color);
		--_highlight-border-color: transparent;
		--_is-hovered-background-color: transparent;
		--_is-hovered-primary-content-color: var(--semantics-buttons-critical-transparent-is-hovered-primary-content-color);
		--_is-hovered-secondary-content-color: var(--semantics-buttons-critical-transparent-is-hovered-secondary-content-color);
		--_is-hovered-highlight-border-color: transparent;
		--_is-active-background-color: transparent;
		--_is-active-primary-content-color: var(--semantics-buttons-critical-transparent-is-active-primary-content-color);
		--_is-active-secondary-content-color: var(--semantics-buttons-critical-transparent-is-active-secondary-content-color);
		--_is-active-highlight-border-color: transparent;
	}

	:host([width="full"]) {
		display: block;
		width: 100%;
		flex-grow: 1;
	}

	:host([horizontal-alignment="left"]) .button {
		justify-content: flex-start;
		text-align: left;
	}

	:host([horizontal-alignment="right"]) .button {
		justify-content: flex-end;
		text-align: right;
	}

	:host([hidden]) {
		display: none;
	}

	:host([disabled]) {
		opacity: var(--primitives-opacity-disabled);
		pointer-events: none;
	}

	:host([no-highlight-border]),
	:host([no-highlight-border][expanded]) {
		--_highlight-border-color: transparent;
		--_is-hovered-highlight-border-color: transparent;
		--_is-active-highlight-border-color: transparent;
	}


	/* # Block */

	.button {
		box-sizing: border-box;
		display: inline-flex;
		position: relative;
		margin: 0;
		border: none;
		border-radius: var(--_corner-radius);
		background: none;
		background-color: var(--_background-color);
		box-shadow: inset 0 0 0 var(--primitives-border-width-thin) var(--_highlight-border-color);
		width: var(--_width);
		min-width: var(--_min-size);
		max-width: 100%;
		min-height: var(--_min-size);
		padding: var(--_block-padding) var(--_inline-padding);
		gap: var(--_gap);
		align-items: center;
		justify-content: center;
		color: var(--_primary-content-color);
		font: var(--_font);
		text-align: center;
		text-decoration: none;
		text-wrap: pretty;
		transition:
			background-color var(--primitives-transition-duration-fast) var(--primitives-transition-easing-default),
			color var(--primitives-transition-duration-fast) var(--primitives-transition-easing-default)
		;
		appearance: none;
	}

	a.button {
		cursor: var(--semantics-controls-link-cursor);
	}

	.button:hover {
		@media (hover: hover) {
			background-color: var(--_is-hovered-background-color);
			color: var(--_is-hovered-primary-content-color);
			--_highlight-border-color: var(--_is-hovered-highlight-border-color);
		}
	}

	.button:active {
		background-color: var(--_is-active-background-color);
		color: var(--_is-active-primary-content-color);
		--_highlight-border-color: var(--_is-active-highlight-border-color);
	}

	/* Loading keeps the control focusable (not disabled); activation is blocked in JS. */
	:host([loading]) .button {
		cursor: default;
	}

	.button:focus-visible {
		outline: var(--semantics-focus-ring-outline);
		outline-offset: var(--semantics-focus-ring-outline-offset);
		box-shadow: var(--semantics-focus-ring-box-shadow), inset 0 0 0 var(--primitives-border-width-thin) var(--_highlight-border-color);
	}

	.button:focus:not(:focus-visible) {
		outline: none;
	}

	@media (prefers-reduced-motion: reduce) {
		.button,
		.button__content {
			transition: none;
		}
	}


	/* # Elements */

	.button__content {
		display: inline-flex;
		max-width: 100%;
		min-width: 0;
		align-items: center;
		gap: 0;
		transition: opacity var(--primitives-transition-duration-slow) var(--primitives-transition-easing-default);
	}

	/* Loading crossfades the content out (opacity, not visibility, so the button
	   keeps its accessible name) while the indicator fades in. The content stays
	   laid out, so the button keeps its width. */
	:host([loading]) .button__content {
		opacity: 0;
	}

	:host([loading]) .button__disclosure-icon {
		opacity: 0;
	}

	/* Wrapper overlaid on the control, positioned against the host (which is
	   position:relative). It lives outside the <button>/<a> so the indicator's
	   role="status" live region announces loading without joining the button's
	   accessible name. The activity-indicator inside fills it and centres its
	   circle, which inherits the content color via currentColor. */
	.button__activity-indicator {
		position: absolute;
		inset: 0;
		color: var(--_primary-content-color);
	}

	::slotted(nldd-icon) {
		display: none;
	}

	.button__text {
		min-width: 0;
	}

	:host([single-line]) .button__text {
		overflow: hidden;
		text-overflow: ellipsis;
		white-space: nowrap;
	}

	.button__text-area {
		display: flex;
		flex-direction: column;
		min-width: 0;
		padding-inline: var(--_gap);
	}

	:host([size="xs"]) .button__text-area,
	:host([size="sm"]) .button__text-area {
		flex-direction: row;
		align-items: baseline;
		gap: var(--_gap);
	}

	.button__supporting-text {
		min-width: 0;
		color: var(--_secondary-content-color);
		font: var(--_supporting-font);
	}

	.button:hover .button__supporting-text {
		@media (hover: hover) {
			color: var(--_is-hovered-secondary-content-color);
		}
	}

	.button:active .button__supporting-text {
		color: var(--_is-active-secondary-content-color);
	}

	:host([single-line]) .button__supporting-text {
		overflow: hidden;
		text-overflow: ellipsis;
		white-space: nowrap;
	}

	:host([size="md"]) .button.has-supporting-text {
		--_font: var(--semantics-buttons-sm-primary-text-font);
		--_icon-size: var(--semantics-buttons-md-with-supporting-text-icon-size);
		--_inline-padding: calc((var(--_min-size) - var(--_icon-size)) / 2);
		--_block-padding: var(--primitives-space-4);
	}

	:host([size="lg"]) .button.has-supporting-text {
		--_font: var(--semantics-buttons-md-primary-text-font);
		--_icon-size: var(--semantics-buttons-lg-with-supporting-text-icon-size);
		--_inline-padding: calc((var(--_min-size) - var(--_icon-size)) / 2);
		--_block-padding: var(--primitives-space-4);
	}

	.button__start-icon,
	.button__end-icon {
		display: flex;
		width: var(--_icon-size);
		height: var(--_icon-size);
		flex-shrink: 0;
		align-items: center;
		justify-content: center;
	}

	:host([expandable]) .button {
		padding-inline-end: calc(var(--_inline-padding) + var(--_gap) + var(--_disclosure-icon-size));
	}

	.button__disclosure-icon {
		display: block;
		position: absolute;
		top: 50%;
		inset-inline-end: var(--_inline-padding);
		width: var(--_disclosure-icon-size);
		height: var(--_disclosure-icon-size);
		transform: translateY(-50%);
	}
`;
    }
  });

  // node_modules/@nldd/design-system/dist/components/actions/button/button.template.js
  function renderContent(component) {
    return b2`
		<span class="button__content">
			${component.startIcon ? b2`
				<span class="button__start-icon">
					<nldd-icon name=${component.startIcon}></nldd-icon>
				</span>
			` : b2`<slot name="start-icon"></slot>`}
			<span class="button__text-area">
				<span class="button__text">${component.text ? component.text : b2`<slot name="text"></slot>`}</span>
				${component.supportingText ? b2`<span class="button__supporting-text">${component.supportingText}</span>` : A}
			</span>
			${component.endIcon ? b2`
				<span class="button__end-icon">
					<nldd-icon name=${component.endIcon}></nldd-icon>
				</span>
			` : b2`<slot name="end-icon"></slot>`}
		</span>
		${component.expandable ? b2`
			<nldd-icon class="button__disclosure-icon"
				name="chevron-down-small"
			></nldd-icon>
		` : A}
	`;
  }
  function template(helpers) {
    const content = renderContent(this);
    const buttonClass = [
      "button",
      this.supportingText ? "has-supporting-text" : ""
    ].filter(Boolean).join(" ");
    const isDisclosure = this.expandable || !!this.popupType;
    const ariaExpanded = isDisclosure ? String(this.expanded) : this.expanded ? "true" : A;
    const ariaLabel = this.accessibleLabel || (this.supportingText ? [this.text, this.supportingText].filter(Boolean).join(", ") : A);
    const ariaBusy = this.loading ? "true" : A;
    const loadingIndicator = this.loading ? b2`
			<div class="button__activity-indicator">
				<nldd-activity-indicator
					timing="instant"
					size=${this.size === "xs" ? "16" : this.size === "sm" ? "20" : this.size === "lg" ? "28" : "24"}
				></nldd-activity-indicator>
			</div>
		` : A;
    if (this.href) {
      const resolvedRel = this._resolvedRel();
      return b2`
			<a class=${buttonClass}
				href=${this.href}
				target=${this.target || A}
				rel=${resolvedRel || A}
				aria-disabled=${this.disabled ? "true" : A}
				aria-label=${ariaLabel}
				aria-haspopup=${this.popupType || A}
				aria-expanded=${ariaExpanded}
				aria-busy=${ariaBusy}
				@click=${helpers.handleClick}
			>
				${content}
			</a>
			${loadingIndicator}
		`;
    }
    return b2`
		<button class=${buttonClass}
			type=${this.type}
			?disabled=${this.disabled}
			aria-disabled=${this.disabled ? "true" : A}
			aria-label=${ariaLabel}
			aria-haspopup=${this.popupType || A}
			aria-expanded=${ariaExpanded}
			aria-busy=${ariaBusy}
			popovertarget=${this.popovertarget || A}
			.popoverTargetElement=${this.popoverTargetElement}
			.popoverTargetAction=${this.popoverTargetAction}
			@click=${helpers.handleClick}
		>
			${content}
		</button>
		${loadingIndicator}
	`;
  }
  var init_button_template = __esm({
    "node_modules/@nldd/design-system/dist/components/actions/button/button.template.js"() {
      init_lit();
    }
  });

  // node_modules/@nldd/design-system/dist/components/content/icon/icon.styles.js
  var iconStyles;
  var init_icon_styles = __esm({
    "node_modules/@nldd/design-system/dist/components/content/icon/icon.styles.js"() {
      init_lit();
      iconStyles = i`


	/* # Host
	 *
	 * Default size = fill parent (existing behaviour). Setting [size]
	 * pins width/height to a fixed spacer value; setting [color] picks
	 * either a functional semantic colour or a descriptive rijkskleur. */

	:host {
		--_size: 100%;
		--_color: inherit;

		display: inline-flex;
		width: var(--_size);
		align-items: center;
		color: var(--_color);
	}

	:host([hidden]) {
		display: none;
	}


	/* # Size — spacer-aligned, 16+ */

	:host([size="16"]) { --_size: var(--primitives-space-16); }
	:host([size="20"]) { --_size: var(--primitives-space-20); }
	:host([size="24"]) { --_size: var(--primitives-space-24); }
	:host([size="28"]) { --_size: var(--primitives-space-28); }
	:host([size="32"]) { --_size: var(--primitives-space-32); }
	:host([size="40"]) { --_size: var(--primitives-space-40); }
	:host([size="44"]) { --_size: var(--primitives-space-44); }
	:host([size="48"]) { --_size: var(--primitives-space-48); }
	:host([size="56"]) { --_size: var(--primitives-space-56); }
	:host([size="64"]) { --_size: var(--primitives-space-64); }
	:host([size="80"]) { --_size: var(--primitives-space-80); }
	:host([size="96"]) { --_size: var(--primitives-space-96); }


	/* # Color — functional */

	:host([color="primary-content"])   { --_color: var(--semantics-content-color); }
	:host([color="secondary-content"]) { --_color: var(--semantics-content-secondary-color); }
	:host([color="accent"])            { --_color: var(--semantics-content-accent-color); }
	:host([color="critical"])          { --_color: var(--semantics-content-critical-color); }
	:host([color="warning"])           { --_color: var(--semantics-content-warning-color); }
	:host([color="success"])           { --_color: var(--semantics-content-success-color); }


	/* # Color — rijkskleuren */

	:host([color="lintblauw"])   { --_color: var(--components-icon-color-lintblauw-color); }
	:host([color="donkerblauw"]) { --_color: var(--components-icon-color-donkerblauw-color); }
	:host([color="hemelblauw"])  { --_color: var(--components-icon-color-hemelblauw-color); }
	:host([color="lichtblauw"])  { --_color: var(--components-icon-color-lichtblauw-color); }
	:host([color="paars"])       { --_color: var(--components-icon-color-paars-color); }
	:host([color="violet"])      { --_color: var(--components-icon-color-violet-color); }
	:host([color="robijnrood"])  { --_color: var(--components-icon-color-robijnrood-color); }
	:host([color="roze"])        { --_color: var(--components-icon-color-roze-color); }
	:host([color="rood"])        { --_color: var(--components-icon-color-rood-color); }
	:host([color="oranje"])      { --_color: var(--components-icon-color-oranje-color); }
	:host([color="donkergeel"])  { --_color: var(--components-icon-color-donkergeel-color); }
	:host([color="geel"])        { --_color: var(--components-icon-color-geel-color); }
	:host([color="donkerbruin"]) { --_color: var(--components-icon-color-donkerbruin-color); }
	:host([color="bruin"])       { --_color: var(--components-icon-color-bruin-color); }
	:host([color="donkergroen"]) { --_color: var(--components-icon-color-donkergroen-color); }
	:host([color="groen"])       { --_color: var(--components-icon-color-groen-color); }
	:host([color="mosgroen"])    { --_color: var(--components-icon-color-mosgroen-color); }
	:host([color="mintgroen"])   { --_color: var(--components-icon-color-mintgroen-color); }


	/* # Elements */

	svg {
		display: block;
		width: 100%;
	}
`;
    }
  });

  // node_modules/lit-html/directive.js
  var t4, e6, i5;
  var init_directive = __esm({
    "node_modules/lit-html/directive.js"() {
      t4 = { ATTRIBUTE: 1, CHILD: 2, PROPERTY: 3, BOOLEAN_ATTRIBUTE: 4, EVENT: 5, ELEMENT: 6 };
      e6 = (t6) => (...e9) => ({ _$litDirective$: t6, values: e9 });
      i5 = class {
        constructor(t6) {
        }
        get _$AU() {
          return this._$AM._$AU;
        }
        _$AT(t6, e9, i8) {
          this._$Ct = t6, this._$AM = e9, this._$Ci = i8;
        }
        _$AS(t6, e9) {
          return this.update(t6, e9);
        }
        update(t6, e9) {
          return this.render(...e9);
        }
      };
    }
  });

  // node_modules/lit-html/directives/unsafe-html.js
  var e7, o6;
  var init_unsafe_html = __esm({
    "node_modules/lit-html/directives/unsafe-html.js"() {
      init_lit_html();
      init_directive();
      e7 = class extends i5 {
        constructor(i8) {
          if (super(i8), this.it = A, i8.type !== t4.CHILD) throw Error(this.constructor.directiveName + "() can only be used in child bindings");
        }
        render(r6) {
          if (r6 === A || null == r6) return this._t = void 0, this.it = r6;
          if (r6 === E) return r6;
          if ("string" != typeof r6) throw Error(this.constructor.directiveName + "() called with a non-string value");
          if (r6 === this.it) return this._t;
          this.it = r6;
          const s6 = [r6];
          return s6.raw = s6, this._t = { _$litType$: this.constructor.resultType, strings: s6, values: [] };
        }
      };
      e7.directiveName = "unsafeHTML", e7.resultType = 1;
      o6 = e6(e7);
    }
  });

  // node_modules/lit/directives/unsafe-html.js
  var init_unsafe_html2 = __esm({
    "node_modules/lit/directives/unsafe-html.js"() {
      init_unsafe_html();
    }
  });

  // node_modules/@nldd/design-system/dist/components/content/icon/icon.template.js
  function template2(iconSvg) {
    if (!iconSvg) {
      return b2`<svg xmlns="http://www.w3.org/2000/svg" viewBox="0 0 24 24"></svg>`;
    }
    return b2`${o6(iconSvg)}`;
  }
  var init_icon_template = __esm({
    "node_modules/@nldd/design-system/dist/components/content/icon/icon.template.js"() {
      init_lit();
      init_unsafe_html2();
    }
  });

  // node_modules/@nldd/design-system/dist/components/content/icon/icon-aliases.js
  var aliases;
  var init_icon_aliases = __esm({
    "node_modules/@nldd/design-system/dist/components/content/icon/icon-aliases.js"() {
      aliases = {
        // apartment-building
        "office": "apartment-building",
        "building": "apartment-building",
        // arrow-2-counter-clockwise
        "refresh": "arrow-2-counter-clockwise",
        "reload": "arrow-2-counter-clockwise",
        "sync": "arrow-2-counter-clockwise",
        // arrow-down-in-bucket
        "download": "arrow-down-in-bucket",
        // arrow-up-arrow-down
        "sort": "arrow-up-arrow-down",
        // arrow-u-turn-backward
        "undo": "arrow-u-turn-backward",
        // arrow-u-turn-forward
        "redo": "arrow-u-turn-forward",
        // bell
        "notification": "bell",
        "notifications": "bell",
        "alarm": "bell",
        // book
        "guide": "book",
        "read": "book",
        // bookmark-filled
        "bookmarked": "bookmark-filled",
        // books-vertical
        "books": "books-vertical",
        // brackets-ellipsis
        "embed": "brackets-ellipsis",
        // business-suitcase
        "work": "business-suitcase",
        // calendar-event
        "calendar": "calendar-event",
        "event": "calendar-event",
        // certificate
        "license": "certificate",
        "diploma": "certificate",
        // chart-x-y-axis-line
        "analytics": "chart-x-y-axis-line",
        "chart-line": "chart-x-y-axis-line",
        "graph": "chart-x-y-axis-line",
        // check-list
        "tasks": "check-list",
        "todos": "check-list",
        "checklist": "check-list",
        "backlog": "check-list",
        // check-mark
        "checked": "check-mark",
        // check-mark-circle
        "success": "check-mark-circle",
        "valid": "check-mark-circle",
        // check-mark-extra-small
        "checked-extra-small": "check-mark-extra-small",
        // check-mark-small
        "checked-small": "check-mark-small",
        // chevron-left
        "back": "chevron-left",
        // chevron-left-forward-slash-chevron-right
        "code": "chevron-left-forward-slash-chevron-right",
        // chevron-right
        "forward": "chevron-right",
        // circle-dashed
        "icon-placeholder": "circle-dashed",
        // clipboard-rectangle
        "paste": "clipboard-rectangle",
        // clock
        "time": "clock",
        "now": "clock",
        // clock-arrow-clockwise
        "future": "clock-arrow-clockwise",
        // clock-arrow-counter-clockwise
        "history": "clock-arrow-counter-clockwise",
        // cloud-arrow-down
        "download-from-cloud": "cloud-arrow-down",
        // cloud-arrow-up
        "backup-in-cloud": "cloud-arrow-up",
        "deploy": "cloud-arrow-up",
        "upload-to-cloud": "cloud-arrow-up",
        // cylinder-split
        "database": "cylinder-split",
        // cylinder-split-slash
        "database-disabled": "cylinder-split-slash",
        "database-unavailable": "cylinder-split-slash",
        // ellipsis
        "more": "ellipsis",
        // envelope
        "email": "envelope",
        "mail": "envelope",
        // exclamation-circle
        "error": "exclamation-circle",
        "invalid": "exclamation-circle",
        // exclamation-triangle-filled
        "alert": "exclamation-triangle-filled",
        "warning": "exclamation-triangle-filled",
        // eye
        "show": "eye",
        "visible": "eye",
        // eye-slash
        "hidden": "eye-slash",
        "hide": "eye-slash",
        // eyeglasses
        "reading-list": "eyeglasses",
        // face-frowning
        "sad": "face-frowning",
        "frowning": "face-frowning",
        // face-smiling
        "happy": "face-smiling",
        "smiling": "face-smiling",
        // face-smiling-badge-plus
        "add-emoji": "face-smiling-badge-plus",
        // file-text
        "document": "file-text",
        "file": "file-text",
        // flag-filled
        "flagged": "flag-filled",
        // folder
        "directory": "folder",
        // folder-stack
        "directories": "folder-stack",
        // gear
        "global-settings": "gear",
        "settings": "gear",
        // globe
        "languages": "globe",
        // heart-filled
        "favorite": "heart-filled",
        "love": "heart-filled",
        // house
        "home": "house",
        // info-circle
        "info": "info-circle",
        "information": "info-circle",
        // lightbulb
        "idea": "lightbulb",
        // link
        "url": "link",
        "hyperlink": "link",
        // list
        "menu": "list",
        // list-arrow-down
        "sort-descending": "list-arrow-down",
        // list-arrow-up
        "sort-ascending": "list-arrow-up",
        // list-decreasing-lines
        "filter": "list-decreasing-lines",
        // lock-closed
        "lock": "lock-closed",
        "locked": "lock-closed",
        "secure": "lock-closed",
        // lock-open
        "unlocked": "lock-open",
        "unsecure": "lock-open",
        // scissor
        "cut": "scissor",
        // ship-wheel
        "k8s": "ship-wheel",
        "kubernetes": "ship-wheel",
        // magnifier
        "search": "magnifier",
        // message-rectangle-text
        "annotation": "message-rectangle-text",
        "comment": "message-rectangle-text",
        // minus
        "remove": "minus",
        // minus-small
        "remove-small": "minus-small",
        // minus-extra-small
        "remove-extra-small": "minus-extra-small",
        // moon
        "dark-mode": "moon",
        "night": "moon",
        // paper-plane
        "send": "paper-plane",
        // paperclip
        "attachment": "paperclip",
        "attach": "paperclip",
        // pencil
        "write": "pencil",
        // photo
        "image": "photo",
        // photo-slash
        "broken-image": "photo-slash",
        // pencil-on-square
        "edit": "pencil-on-square",
        // person
        "user": "person",
        // person-2
        "group": "person-2",
        "team": "person-2",
        "users": "person-2",
        // person-badge-gear
        "user-admin": "person-badge-gear",
        "user-settings": "person-badge-gear",
        // person-circle
        "account": "person-circle",
        "profile": "person-circle",
        // plus
        "add": "plus",
        // plus-small
        "add-small": "plus-small",
        // puzzle-piece
        "extension": "puzzle-piece",
        "module": "puzzle-piece",
        "plugin": "puzzle-piece",
        // question-mark-circle
        "help": "question-mark-circle",
        "question": "question-mark-circle",
        // shield-check-mark
        "security": "shield-check-mark",
        "verified": "shield-check-mark",
        // slash-circle
        "blocked": "slash-circle",
        "forbidden": "slash-circle",
        // sparkles
        "ai": "sparkles",
        "magic": "sparkles",
        // star
        "rating": "star",
        // star-filled
        "rated": "star-filled",
        // starburst-filled
        "new": "starburst-filled",
        "promotion": "starburst-filled",
        // arrow-right-out-bucket
        "logout": "arrow-right-out-bucket",
        "exit": "arrow-right-out-bucket",
        // arrow-right-in-bucket
        "login": "arrow-right-in-bucket",
        // square-arrow-right-top
        "external-link": "square-arrow-right-top",
        // square-arrow-up
        "share": "square-arrow-up",
        "export": "square-arrow-up",
        // square-on-square
        "copy": "square-on-square",
        // square-plus-on-square
        "duplicate": "square-plus-on-square",
        // rectangle-stack
        "stack": "rectangle-stack",
        // sun
        "day": "sun",
        "light-mode": "sun",
        // table-cells-badge-arrow-down
        "download-table": "table-cells-badge-arrow-down",
        // tag
        "label": "tag",
        "category": "tag",
        // terminal
        "cli": "terminal",
        "console": "terminal",
        // text-quote
        "blockquote": "text-quote",
        // timer
        "countdown": "timer",
        // trash
        "delete": "trash",
        // point-bottom-left-to-point-top-right-s-curve-path
        "path": "point-bottom-left-to-point-top-right-s-curve-path",
        "traject": "point-bottom-left-to-point-top-right-s-curve-path",
        // pipeline-corner-2
        "pipeline": "pipeline-corner-2",
        // pencil-ruler
        "design": "pencil-ruler",
        // globe-rack-server
        "dns": "globe-rack-server",
        // file-box
        "archive": "file-box",
        // cylinder-2-big-small-split
        "coins": "cylinder-2-big-small-split",
        // blocks-9
        "building-blocks": "blocks-9",
        // leaf
        "sustainability": "leaf",
        // desk-with-screen
        "workplace": "desk-with-screen",
        // accessibility
        "a11y": "accessibility",
        // binoculars
        "explore": "binoculars",
        "discover": "binoculars",
        // diamond
        "gem": "diamond",
        "quality": "diamond",
        // radar
        "monitoring": "radar",
        // seal-check-mark
        "certified": "seal-check-mark",
        // shield
        "protection": "shield",
        // shopping-cart
        "cart": "shopping-cart",
        // table-cells
        "table": "table-cells",
        // square-and-arrow-down
        "save": "square-and-arrow-down",
        "import": "square-and-arrow-down",
        // pipeline-machine-gear
        "pipeline-runner": "pipeline-machine-gear",
        // hand
        "privacy": "hand"
      };
    }
  });

  // node_modules/@nldd/design-system/dist/components/content/icon/icon-registry.js
  var iconRegistry;
  var init_icon_registry = __esm({
    "node_modules/@nldd/design-system/dist/components/content/icon/icon-registry.js"() {
      iconRegistry = /* @__PURE__ */ new Map([
        ["accessibility", '<svg xmlns="http://www.w3.org/2000/svg" viewBox="0 0 24 24">\n	<path fill="currentColor" d="M12 2a10 10 0 1 1 0 20 10 10 0 0 1 0-20m0 2a8 8 0 1 0 0 16 8 8 0 0 0 0-16m4.56 4.13a.75.75 0 0 1 .39 1.45l-3.45.92v1.84a5 5 0 0 0 .17 1.3l1.05 3.92a.75.75 0 0 1-1.45.39l-.98-3.69a.3.3 0 0 0-.58 0l-.99 3.69a.75.75 0 0 1-1.46-.32l.01-.07 1.06-3.94a5 5 0 0 0 .17-1.3V10.5l-3.45-.92a.75.75 0 0 1 .4-1.45l2.13.57a9.4 9.4 0 0 0 4.84 0zM12 5.5a1.5 1.5 0 1 1 0 3 1.5 1.5 0 0 1 0-3"/>\n</svg>'],
        ["apartment-building", '<svg xmlns="http://www.w3.org/2000/svg" viewBox="0 0 24 24">\n	<path fill="currentColor" d="M11 16H9v-2h2zM15 16h-2v-2h2zM11 12H9v-2h2zM15 12h-2v-2h2zM11 8H9V6h2zM15 8h-2V6h2z"/>\n	<path fill="currentColor" d="M16 2a3 3 0 0 1 3 3v17h-6v-2h-2v2H5V5a3 3 0 0 1 3-3zM8 4a1 1 0 0 0-1 1v15h2v-2h6v2h2V5a1 1 0 0 0-1-1z"/>\n</svg>'],
        ["app", '<svg xmlns="http://www.w3.org/2000/svg" viewBox="0 0 24 24">\n	<path fill="currentColor" d="M10.43 2.22a1 1 0 0 1 1.37.37l7.5 12.99.2.1c2.06 1.12 1.43 4.76 1.5 5.85-.9-.6-4.37-1.88-4.32-4.22l.02-.23L9.2 4.09a1 1 0 0 1 .37-1.37zm1.07 7.86-5.95 10.3-2.04.55-.55-2.05L9.7 7.21l.08-.12zM3.46 16H2v-3h3.2zm11.46 0H9.24l1.73-3h2.22zM22 16h-.76a3.4 3.4 0 0 0-1.22-1.18L18.96 13H22z"/>\n</svg>'],
        ["arrow-2-counter-clockwise", '<svg xmlns="http://www.w3.org/2000/svg" viewBox="0 0 24 24">\n	<path fill="currentColor" d="M4 12a8 8 0 0 0 14.93 4H16v-2h6v6h-2v-2a9.98 9.98 0 0 1-18-6zM12 2a10 10 0 0 1 10 10h-2A8 8 0 0 0 5.07 8H8v2H2V4h2v2a10 10 0 0 1 8-4"/>\n</svg>'],
        ["arrow-down", '<svg xmlns="http://www.w3.org/2000/svg" viewBox="0 0 24 24">\n	<path fill="currentColor" d="m4 12 8 8 8-8-1.42-1.41L13 16.172V3h-2v13.172L5.42 10.59z"/>\n</svg>'],
        ["arrow-down-in-bucket", '<svg xmlns="http://www.w3.org/2000/svg" viewBox="0 0 24 24">\n	<path fill="currentColor" d="M13 3v10.586l4.293-4.293 1.414 1.414L12 17.414l-6.707-6.707 1.414-1.414L11 13.586V3zM5 16v3a1 1 0 0 0 1 1h12a1 1 0 0 0 1-1v-3h2v3a3 3 0 0 1-3 3H6a3 3 0 0 1-3-3v-3z"/>\n</svg>'],
        ["arrow-left", '<svg xmlns="http://www.w3.org/2000/svg" viewBox="0 0 24 24">\n	<path fill="currentColor" d="m12 4-8 8 8 8 1.41-1.42L7.828 13H21v-2H7.828l5.582-5.58z"/>\n</svg>'],
        ["arrow-left-right", '<svg xmlns="http://www.w3.org/2000/svg" viewBox="0 0 24 24">\n	<path fill="currentColor" d="m20.41 17-5.7-5.7-1.42 1.4 3.3 3.3H4v2h12.59l-3.3 3.3 1.42 1.4zM20 6H7.41l3.3-3.3-1.42-1.4L3.6 7l5.7 5.7 1.42-1.4L7.4 8H20z"/>\n</svg>'],
        ["arrow-right", '<svg xmlns="http://www.w3.org/2000/svg" viewBox="0 0 24 24">\n	<path fill="currentColor" d="m12 4 8 8-8 8-1.41-1.42L16.172 13H3v-2h13.172L10.59 5.42z"/>\n</svg>'],
        ["arrow-right-in-bucket", '<svg xmlns="http://www.w3.org/2000/svg" viewBox="0 0 24 24">\n	<path fill="currentColor" d="M18 3a3 3 0 0 1 3 3v12a3 3 0 0 1-3 3h-6v-2h6a1 1 0 0 0 1-1V6a1 1 0 0 0-1-1h-6V3zm-3 9-5 5-1.41-1.42L11.17 13H3v-2h8.17L8.6 8.42 10 7z"/>\n</svg>'],
        ["arrow-right-out-bucket", '<svg xmlns="http://www.w3.org/2000/svg" viewBox="0 0 24 24">\n	<path fill="currentColor" d="M12 5H6a1 1 0 0 0-1 1v12a1 1 0 0 0 1 1h6v2H6a3 3 0 0 1-3-3V6a3 3 0 0 1 3-3h6zm9 7-5 5-1.41-1.42L17.17 13H9v-2h8.17L14.6 8.42 16 7z"/>\n</svg>'],
        ["arrow-u-turn-backward", '<svg xmlns="http://www.w3.org/2000/svg" viewBox="0 0 24 24">\n	<path fill="currentColor" d="M9.4 4.4 5.82 8H16a6 6 0 0 1 0 12h-4v-2h4a4 4 0 0 0 0-8H5.82l3.58 3.6L8 15 2 9l6-6z"/>\n</svg>'],
        ["arrow-u-turn-forward", '<svg xmlns="http://www.w3.org/2000/svg" viewBox="0 0 24 24">\n	<path fill="currentColor" d="M14.6 4.4 18.18 8H8a6 6 0 0 0 0 12h4v-2H8a4 4 0 0 1 0-8h10.18l-3.58 3.6L16 15l6-6-6-6z"/>\n</svg>'],
        ["arrow-up", '<svg xmlns="http://www.w3.org/2000/svg" viewBox="0 0 24 24">\n	<path fill="currentColor" d="m4 12 8-8 8 8-1.42 1.41L13 7.828V21h-2V7.828L5.42 13.41z"/>\n</svg>'],
        ["arrow-up-arrow-down", '<svg xmlns="http://www.w3.org/2000/svg" viewBox="0 0 24 24">\n	<path fill="currentColor" d="m7 3.586 5.707 5.707-1.414 1.414L8 7.414V20H6V7.414l-3.293 3.293-1.414-1.414zM18 4v12.586l3.293-3.293 1.414 1.414L17 20.414l-5.707-5.707 1.414-1.414L16 16.586V4z"/>\n</svg>'],
        ["bell", '<svg xmlns="http://www.w3.org/2000/svg" viewBox="0 0 24 24">\n	<path fill="currentColor" d="M12 2a4 4 0 0 1 3.92 3.2A7 7 0 0 1 19 11v2a3 3 0 0 0 2.02 2.84c.52.18.98.6.98 1.16a1 1 0 0 1-1 1h-5a4 4 0 0 1-8 0H3a1 1 0 0 1-1-1c0-.55.46-.98.98-1.16A3 3 0 0 0 5 13v-2a7 7 0 0 1 3.08-5.8A4 4 0 0 1 12 2m-2 16a2 2 0 1 0 4 0zm2-14a2.02 2.02 0 0 0-1.96 2.4A5 5 0 0 0 7 11v2c0 1.13-.37 2.16-1 3h12c-.63-.84-1-1.87-1-3v-2a5 5 0 0 0-3.04-4.6A2.03 2.03 0 0 0 12 4"/>\n</svg>'],
        ["binoculars", '<svg xmlns="http://www.w3.org/2000/svg" viewBox="0 0 24 24">\n	<path fill="currentColor" d="M16 3a2 2 0 0 1 2 2v.23a4 4 0 0 1 2.6 3.06l1.3 7.27A4.5 4.5 0 0 1 17.5 21a4.5 4.5 0 0 1-4.47-4H13a1 1 0 1 0-2 0h-.03a4.5 4.5 0 1 1-8.88-1.42L3.41 8.3A4 4 0 0 1 6 5.24V5a2 2 0 1 1 4 0v.77a3 3 0 0 1 .9 1.44 3 3 0 0 1 2.2 0 3 3 0 0 1 .9-1.44V5c0-1.1.9-2 2-2M6.5 14a2.5 2.5 0 1 0 0 5 2.5 2.5 0 0 0 0-5m11 0a2.5 2.5 0 1 0 0 5 2.5 2.5 0 0 0 0-5M12 9a1 1 0 0 0-1 1v4.17a3 3 0 0 1 2 0V10a1 1 0 0 0-1-1M7.34 7a2 2 0 0 0-1.96 1.65l-.67 3.72a4.5 4.5 0 0 1 4.29.39V8a1 1 0 0 0-1-1zM16 7a1 1 0 0 0-1 1v4.76a4.5 4.5 0 0 1 4.3-.39l-.68-3.72A2 2 0 0 0 16.66 7z"/>\n</svg>'],
        ["blocks-9", '<svg xmlns="http://www.w3.org/2000/svg" viewBox="0 0 24 24">\n	<path fill="currentColor" d="M8.1 8c.5.06.9.48.9 1v3h7.1c.5.06.9.48.9 1v8.1a1 1 0 0 1-.9.9H4a1 1 0 0 1-1-.9V9a1 1 0 0 1 1-1zM5 20h2v-2H5zm4 0h2v-2H9zm4 0h2v-2h-2zm-8-4h2v-2H5zm4 0h2v-2H9zm4 0h2v-2h-2zm3.3-10.03a1 1 0 0 1 1.22-.71l3.86 1.03.1.04a1 1 0 0 1 .61 1.19l-1.04 3.86-.03.1a1 1 0 0 1-1.09.63l-.1-.02-3.86-1.04a1 1 0 0 1-.73-1.12l.02-.1zM5 12h2v-2H5zm12.45-2.62 1.93.52.52-1.93-1.93-.52zm-4.52-8.14a1 1 0 0 1 1.12.73l1.04 3.86.02.1a1 1 0 0 1-.63 1.1l-.1.02-3.86 1.04a1 1 0 0 1-1.2-.61l-.03-.1-1.03-3.86a1 1 0 0 1 .7-1.23l3.87-1.03zm-2.48 2.73.52 1.93 1.93-.52-.52-1.93z"/>\n</svg>'],
        ["bold", '<svg xmlns="http://www.w3.org/2000/svg" viewBox="0 0 24 24">\n	<path fill="currentColor" d="M12 2a6 6 0 0 1 4.9 9.45A5.98 5.98 0 0 1 13 22H5V2zM9 18h4a2 2 0 1 0 0-4H9zm0-8h3a2 2 0 1 0 0-4H9z"/>\n</svg>'],
        ["book", '<svg xmlns="http://www.w3.org/2000/svg" viewBox="0 0 24 24">\n	<path fill="currentColor" d="M16.5 3C19.54 3 22 4.8 22 7v13.07c0 .7-1.08 1.07-1.65.66a6.6 6.6 0 0 0-7.61-.08 1.3 1.3 0 0 1-1.48 0 6.5 6.5 0 0 0-7.62.07c-.57.42-1.64.05-1.64-.65V7c0-2.2 2.46-4 5.5-4 1.86 0 3.5.67 4.5 1.7A6.3 6.3 0 0 1 16.5 3m-9 2C6.4 5 4 5.5 4 7v11.2c2.2-1 4.8-1 7 0V6.54A4.6 4.6 0 0 0 7.5 5m9 0c-1.43 0-2.54.55-3.5 1.54V18.2c2.2-1 4.8-1 7 0V7c0-1.5-2.4-2-3.5-2"/>\n</svg>'],
        ["book-batch-play", '<svg xmlns="http://www.w3.org/2000/svg" viewBox="0 0 24 24">\n	<path fill="currentColor" d="M18 13a5 5 0 1 1 0 10 5 5 0 0 1 0-10m-1.5 7.5 4.25-2.5-4.25-2.5z"/>\n	<path fill="currentColor" d="M16.5 2C19.54 2 22 3.8 22 6v7.53a6 6 0 0 0-2-1.19V6c0-1.5-2.4-2-3.5-2-1.43 0-2.54.55-3.5 1.54v9.14a6 6 0 0 0-.71 5.17 1.3 1.3 0 0 1-1.03-.2 6.5 6.5 0 0 0-7.62.07c-.57.42-1.64.05-1.64-.65V6c0-2.2 2.46-4 5.5-4 1.86 0 3.5.67 4.5 1.7A6.3 6.3 0 0 1 16.5 2m-9 2C6.4 4 4 4.5 4 6v11.2c2.2-1 4.8-1 7 0V5.55A4.6 4.6 0 0 0 7.5 4"/>\n</svg>'],
        ["bookmark", '<svg xmlns="http://www.w3.org/2000/svg" viewBox="0 0 24 24">\n	<path fill="currentColor" d="M17 5a1 1 0 0 0-1-1H8a1 1 0 0 0-1 1v13.65l3.05-2.61.21-.17a3 3 0 0 1 3.7.17L17 18.65zm2 15.83-.01.15a1 1 0 0 1-1.52.7l-.12-.1-4.7-4.02a1 1 0 0 0-1.3 0l-4.7 4.03a1 1 0 0 1-1.64-.6L5 20.82V5a3 3 0 0 1 3-3h8.15A3 3 0 0 1 19 5z"/>\n</svg>'],
        ["bookmark-filled", '<svg xmlns="http://www.w3.org/2000/svg" viewBox="0 0 24 24">\n	<path fill="currentColor" d="M5 5a3 3 0 0 1 3-3h8a3 3 0 0 1 3 3v15.83a1 1 0 0 1-1.65.76l-4.7-4.03a1 1 0 0 0-1.3 0l-4.7 4.03A1 1 0 0 1 5 20.83z"/>\n</svg>'],
        ["books-vertical", '<svg xmlns="http://www.w3.org/2000/svg" viewBox="0 0 24 24">\n	<path fill="currentColor" d="M6.5 17c.28 0 .5.22.5.5v1a.5.5 0 0 1-.5.5h-1a.5.5 0 0 1-.5-.5v-1c0-.28.22-.5.5-.5z"/>\n	<path fill="currentColor" d="M13 1a2 2 0 0 1 2 2v2.92q.2-.07.4-.1l1.97-.36a2 2 0 0 1 2.31 1.63l2.13 12.05a2.07 2.07 0 0 1-1.62 2.32l-1.97.35a2.07 2.07 0 0 1-2.32-1.62l-.9-5.12V20a2 2 0 0 1-2 2H4a2 2 0 0 1-2-2V7c0-1.1.9-2 2-2h4V3c0-1.1.9-2 2-2zM4 20h4V7H4zm6 0h3V3h-3zm5.75-12.22 2.12 12.06 1.97-.35-2.13-12.06z"/>\n</svg>'],
        ["brackets-ellipsis", '<svg xmlns="http://www.w3.org/2000/svg" viewBox="0 0 24 24">\n	<path fill="currentColor" d="M7 6H4v12h3v2H2V4h5zM22 20h-5v-2h3V6h-3V4h5z"/>\n	<path fill="currentColor" d="M7.5 13a1.5 1.5 0 1 1 0 3 1.5 1.5 0 0 1 0-3M12 13a1.5 1.5 0 1 1 0 3 1.5 1.5 0 0 1 0-3M16.5 13a1.5 1.5 0 1 1 0 3 1.5 1.5 0 0 1 0-3"/>\n</svg>'],
        ["brick-wall", '<svg xmlns="http://www.w3.org/2000/svg" viewBox="0 0 24 24">\n	<path fill="currentColor" d="M22 13h-5.5v2H22v2h-2v2h2v2H2v-2h2v-2H2v-2h5.5v-2H2v-2h2V9H2V7h5.5V5H2V3h20v2h-5.5v2H22v2h-2v2h2zM9.5 15h5v-2h-5zM6 19h5v-2H6zm7 0h5v-2h-5zM9.5 7h5V5h-5zm1.5 4V9H6v2zm7 0V9h-5v2z"/>\n</svg>'],
        ["bullet-list", '<svg xmlns="http://www.w3.org/2000/svg" viewBox="0 0 24 24">\n	<path fill="currentColor" d="M4.5 16.5a1.5 1.5 0 1 1 0 3 1.5 1.5 0 0 1 0-3M22 19H9v-2h13zM4.5 10.5a1.5 1.5 0 1 1 0 3 1.5 1.5 0 0 1 0-3M22 13H9v-2h13zM4.5 4.5a1.5 1.5 0 1 1 0 3 1.5 1.5 0 0 1 0-3M22 7H9V5h13z"/>\n</svg>'],
        ["business-suitcase", '<svg xmlns="http://www.w3.org/2000/svg" viewBox="0 0 24 24">\n	<path fill="currentColor" d="M14 2c1.66 0 3 1.34 3 3v1h2a3 3 0 0 1 3 3v9c0 1.66-1.34 3-3 3H5c-1.66 0-3-1.34-3-3V9a3 3 0 0 1 3-3h2V5a3 3 0 0 1 3-3zM5 8a1 1 0 0 0-1 1v9a1 1 0 0 0 1 1h14a1 1 0 0 0 1-1V9a1 1 0 0 0-1-1zm5-4a1 1 0 0 0-1 1v1h6V5a1 1 0 0 0-1-1z"/>\n</svg>'],
        ["calendar-event", '<svg xmlns="http://www.w3.org/2000/svg" viewBox="0 0 24 24">\n	<path fill="currentColor" d="M8 2a1 1 0 0 1 1 1v1h6V3a1 1 0 1 1 2 0v1h1a3 3 0 0 1 3 3v12a3 3 0 0 1-3 3H6a3 3 0 0 1-3-3V7a3 3 0 0 1 3-3h1V3a1 1 0 0 1 1-1M7 6H6a1 1 0 0 0-1 1v3h14V7a1 1 0 0 0-1-1h-1v1a1 1 0 1 1-2 0V6H9v1a1 1 0 0 1-2 0zm12 6H5v7a1 1 0 0 0 1 1h12a1 1 0 0 0 1-1zM7 14h4v4H7z"/>\n</svg>'],
        ["caret-down", '<svg xmlns="http://www.w3.org/2000/svg" viewBox="0 0 24 24">\n	<path fill="currentColor" d="m2 8 10 10L22 8z"/>\n</svg>'],
        ["caret-down-extra-small", '<svg xmlns="http://www.w3.org/2000/svg" viewBox="0 0 24 24">\n	<path fill="currentColor" d="m6 10 6 6 6-6z"/>\n</svg>'],
        ["caret-down-small", '<svg xmlns="http://www.w3.org/2000/svg" viewBox="0 0 24 24">\n	<path fill="currentColor" d="m4 9 8 8 8-8z"/>\n</svg>'],
        ["caret-left", '<svg xmlns="http://www.w3.org/2000/svg" viewBox="0 0 24 24">\n	<path fill="currentColor" d="m16 21.7-10-10 10-10z"/>\n</svg>'],
        ["caret-left-extra-small", '<svg xmlns="http://www.w3.org/2000/svg" viewBox="0 0 24 24">\n	<path fill="currentColor" d="m14 18-6-6 6-6z"/>\n</svg>'],
        ["caret-left-small", '<svg xmlns="http://www.w3.org/2000/svg" viewBox="0 0 24 24">\n	<path fill="currentColor" d="m15 20-8-8 8-8z"/>\n</svg>'],
        ["caret-right", '<svg xmlns="http://www.w3.org/2000/svg" viewBox="0 0 24 24">\n	<path fill="currentColor" d="m8 1.7 10 10-10 10z"/>\n</svg>'],
        ["caret-right-extra-small", '<svg xmlns="http://www.w3.org/2000/svg" viewBox="0 0 24 24">\n	<path fill="currentColor" d="m10 6 6 6-6 6z"/>\n</svg>'],
        ["caret-right-small", '<svg xmlns="http://www.w3.org/2000/svg" viewBox="0 0 24 24">\n	<path fill="currentColor" d="m9 4 8 8-8 8z"/>\n</svg>'],
        ["caret-up", '<svg xmlns="http://www.w3.org/2000/svg" viewBox="0 0 24 24">\n	<path fill="currentColor" d="M2 16 12 6l10 10z"/>\n</svg>'],
        ["caret-up-extra-small", '<svg xmlns="http://www.w3.org/2000/svg" viewBox="0 0 24 24">\n	<path fill="currentColor" d="m6 14 6-6 6 6z"/>\n</svg>'],
        ["caret-up-small", '<svg xmlns="http://www.w3.org/2000/svg" viewBox="0 0 24 24">\n	<path fill="currentColor" d="m4 15 8-8 8 8z"/>\n</svg>'],
        ["centralized-network", '<svg xmlns="http://www.w3.org/2000/svg" viewBox="0 0 24 24">\n	<path fill="currentColor" d="M12 1a2 2 0 0 1 .68 3.88l.49 5.12c.78.04 1.5.3 2.1.7l2.97-2.75a2 2 0 1 1 .68.73l-2.9 2.7q.78.9.95 2.14l2.2.3a1.5 1.5 0 1 1-.14.99l-2.07-.28q-.17 1.18-.9 2.05l1.96 2.5a1.5 1.5 0 1 1-.79.62l-1.91-2.44a4 4 0 0 1-3.35.6l-.52 1.48a1.5 1.5 0 1 1-.95-.34l.54-1.51A4 4 0 0 1 9.2 15.2l-2.2.69.01.1a2 2 0 1 1-.3-1.06l2.3-.72a4 4 0 0 1 .66-2.41L6.3 9.5q-.53.48-1.3.5a2 2 0 1 1 1.88-1.32l3.43 2.36q.78-.71 1.86-.95l-.49-5.12A2 2 0 0 1 12 1m1 11a2 2 0 1 0 0 4 2 2 0 0 0 0-4"/>\n</svg>'],
        ["certificate", '<svg xmlns="http://www.w3.org/2000/svg" viewBox="0 0 24 24">\n	<path fill="currentColor" d="M15 11a3 3 0 0 1 2 5.23V21l-2-1-2 1v-4.77A2.99 2.99 0 0 1 15 11m0 2a1 1 0 1 0 0 2 1 1 0 0 0 0-2"/>\n	<path fill="currentColor" d="M19 3a3 3 0 0 1 3 3v10c0 1.67-1.33 3-3 3h-1v-2h1a1 1 0 0 0 1-1V6a1 1 0 0 0-1-1H5a1 1 0 0 0-1 1v10a1 1 0 0 0 1 1h7v2H5c-1.67 0-3-1.33-3-3V6a3 3 0 0 1 3-3z"/>\n	<path fill="currentColor" d="M10 13H6v-2h4zM18 9H6V7h12z"/>\n</svg>'],
        ["chart-x-y-axis-line", '<svg xmlns="http://www.w3.org/2000/svg" viewBox="0 0 24 24">\n	<path fill="currentColor" d="M4 19h18v2H2V3h2z"/>\n	<path fill="currentColor" d="M20.7 7.7 14 14.42l-3-3-3.3 3.3-1.4-1.42L11 8.6l3 3 5.3-5.3z"/>\n</svg>'],
        ["check-circle-filled", '<svg xmlns="http://www.w3.org/2000/svg" viewBox="0 0 24 24">\n	<path fill="currentColor" d="M12 2a10 10 0 1 1 0 20 10 10 0 0 1 0-20m-1.3 11.3L8.42 11 7 12.41l3.7 3.71 6.71-6.7L16 8z"/>\n</svg>'],
        ["check-list", '<svg xmlns="http://www.w3.org/2000/svg" viewBox="0 0 24 24">\n	<path fill="currentColor" d="M5 14a3 3 0 1 1 0 6 3 3 0 0 1 0-6m0 1.5a1.5 1.5 0 1 0 0 3 1.5 1.5 0 0 0 0-3"/>\n	<path fill="currentColor" d="M16 20h-6v-2h6zM22 16H10v-2h12zM18 10h-8V8h8zM8.56 5 4.5 9.06 1.94 6.5 3 5.44l1.5 1.5 3-3zM22 6H10V4h12z"/>\n</svg>'],
        ["check-mark", '<svg xmlns="http://www.w3.org/2000/svg" viewBox="0 0 24 24">\n	<path fill="currentColor" d="M21.707 6.707 10 18.414l-6.707-6.707 1.414-1.414L10 15.586 20.293 5.293z"/>\n</svg>'],
        ["check-mark-circle", '<svg xmlns="http://www.w3.org/2000/svg" viewBox="0 0 24 24">\n	<path fill="currentColor" d="M12 4a8 8 0 1 0 0 16 8 8 0 0 0 0-16M2 12C2 6.477 6.477 2 12 2s10 4.477 10 10-4.477 10-10 10S2 17.523 2 12m14.707-2.113L11 15.594l-3.707-3.707 1.414-1.415L11 12.765l4.293-4.293z"/>\n</svg>'],
        ["check-mark-extra-small", '<svg xmlns="http://www.w3.org/2000/svg" viewBox="0 0 24 24">\n	<path fill="currentColor" d="m17.664 8.707-7.707 7.707-3.707-3.707 1.414-1.414 2.293 2.293 6.293-6.293z"/>\n</svg>'],
        ["check-mark-small", '<svg xmlns="http://www.w3.org/2000/svg" viewBox="0 0 24 24">\n	<path fill="currentColor" d="m19.204 7.707-9.707 9.707-4.707-4.707 1.414-1.414 3.293 3.293 8.293-8.293z"/>\n</svg>'],
        ["chevron-double-left", '<svg xmlns="http://www.w3.org/2000/svg" viewBox="0 0 24 24">\n	<path fill="currentColor" d="M14 3.4 5.429 12 14 20.6 12.6 22l-10-10.001L12.6 2z"/>\n	<path fill="currentColor" d="M22 3.4 13.429 12 22 20.6 20.6 22l-10-10.001L20.6 2z"/>\n</svg>'],
        ["chevron-double-left-extra-small", '<svg xmlns="http://www.w3.org/2000/svg" viewBox="0 0 24 24">\n	<path fill="currentColor" d="M12 7.4 7.429 12 12 16.6 10.6 18l-6-6.001L10.6 6z"/>\n	<path fill="currentColor" d="M18 7.4 13.429 12 18 16.6 16.6 18l-6-6.001L16.6 6z"/>\n</svg>'],
        ["chevron-double-left-small", '<svg xmlns="http://www.w3.org/2000/svg" viewBox="0 0 24 24">\n	<path fill="currentColor" d="M14 5.4 7.429 12 14 18.6 12.6 20l-8-8.001L12.6 4z"/>\n	<path fill="currentColor" d="M21 5.4 14.429 12 21 18.6 19.6 20l-8-8.001L19.6 4z"/>\n</svg>'],
        ["chevron-double-right", '<svg xmlns="http://www.w3.org/2000/svg" viewBox="0 0 24 24">\n	<path fill="currentColor" d="M11.172 11.7 2.6 3.1 4 1.7l10 10-10 10-1.4-1.4z"/>\n	<path fill="currentColor" d="M19.172 11.7 10.6 3.1 12 1.7l10 10-10 10-1.4-1.4z"/>\n</svg>'],
        ["chevron-double-right-extra-small", '<svg xmlns="http://www.w3.org/2000/svg" viewBox="0 0 24 24">\n	<path fill="currentColor" d="M16.172 12 11.6 7.4 13 6l6 6-6 6-1.4-1.4z"/>\n	<path fill="currentColor" d="M10.172 12 5.6 7.4 7 6l6 6-6 6-1.4-1.4z"/>\n</svg>'],
        ["chevron-double-right-small", '<svg xmlns="http://www.w3.org/2000/svg" viewBox="0 0 24 24">\n	<path fill="currentColor" d="M18.172 12 11.6 5.4 13 4l8 8-8 8-1.4-1.4z"/>\n	<path fill="currentColor" d="M11.172 12 4.6 5.4 6 4l8 8-8 8-1.4-1.4z"/>\n</svg>'],
        ["chevron-down", '<svg xmlns="http://www.w3.org/2000/svg" viewBox="0 0 24 24">\n	<path fill="currentColor" d="M12 15.172 3.4 6.6 2 8l10 10L22 8l-1.4-1.4z"/>\n</svg>'],
        ["chevron-down-extra-small", '<svg xmlns="http://www.w3.org/2000/svg" viewBox="0 0 24 24">\n	<path fill="currentColor" d="M12 13.172 7.4 8.6 6 10l6 6 6-6-1.4-1.4z"/>\n</svg>'],
        ["chevron-down-small", '<svg xmlns="http://www.w3.org/2000/svg" viewBox="0 0 24 24">\n	<path fill="currentColor" d="M12 14.172 5.4 7.6 4 9l8 8 8-8-1.4-1.4z"/>\n</svg>'],
        ["chevron-left", '<svg xmlns="http://www.w3.org/2000/svg" viewBox="0 0 24 24">\n	<path fill="currentColor" d="m8.828 11.7 8.572 8.6-1.4 1.4-10-10 10-10 1.4 1.4z"/>\n</svg>'],
        ["chevron-left-chevron-right", '<svg xmlns="http://www.w3.org/2000/svg" viewBox="0 0 24 24">\n	<path fill="currentColor" d="M18.17 12 13.6 7.4 15 6l6 6-6 6-1.4-1.4zM5.83 12l4.57-4.6L9 6l-6 6 6 6 1.4-1.4z"/>\n</svg>'],
        ["chevron-left-extra-small", '<svg xmlns="http://www.w3.org/2000/svg" viewBox="0 0 24 24">\n	<path fill="currentColor" d="m10.828 12 4.572 4.6L14 18l-6-6 6-6 1.4 1.4z"/>\n</svg>'],
        ["chevron-left-forward-slash-chevron-right", '<svg xmlns="http://www.w3.org/2000/svg" viewBox="0 0 24 24">\n	<path fill="currentColor" d="M15.04 4.53 10.9 20l-1.94-.52L13.1 4zM8.4 7.4 3.83 12l4.57 4.6L7 18l-6-6 6-6zM23 12l-6 6-1.4-1.4 4.57-4.6-4.57-4.6L17 6z"/>\n</svg>'],
        ["chevron-left-small", '<svg xmlns="http://www.w3.org/2000/svg" viewBox="0 0 24 24">\n	<path fill="currentColor" d="m9.828 12 6.572 6.6L15 20l-8-8 8-8 1.4 1.4z"/>\n</svg>'],
        ["chevron-right", '<svg xmlns="http://www.w3.org/2000/svg" viewBox="0 0 24 24">\n	<path fill="currentColor" d="M15.172 11.7 6.6 3.1 8 1.7l10 10-10 10-1.4-1.4z"/>\n</svg>'],
        ["chevron-right-extra-small", '<svg xmlns="http://www.w3.org/2000/svg" viewBox="0 0 24 24">\n	<path fill="currentColor" d="M13.172 12 8.6 7.4 10 6l6 6-6 6-1.4-1.4z"/>\n</svg>'],
        ["chevron-right-small", '<svg xmlns="http://www.w3.org/2000/svg" viewBox="0 0 24 24">\n	<path fill="currentColor" d="M14.172 12 7.6 5.4 9 4l8 8-8 8-1.4-1.4z"/>\n</svg>'],
        ["chevron-up", '<svg xmlns="http://www.w3.org/2000/svg" viewBox="0 0 24 24">\n	<path fill="currentColor" d="M12 8.828 3.4 17.4 2 16 12 6l10 10-1.4 1.4z"/>\n</svg>'],
        ["chevron-up-chevron-down", '<svg xmlns="http://www.w3.org/2000/svg" viewBox="0 0 24 24">\n	<path fill="currentColor" d="M12 5.828 7.4 10.4 6 9l6-6 6 6-1.4 1.4zm0 12.344L7.4 13.6 6 15l6 6 6-6-1.4-1.4z"/>\n</svg>'],
        ["chevron-up-extra-small", '<svg xmlns="http://www.w3.org/2000/svg" viewBox="0 0 24 24">\n	<path fill="currentColor" d="M12 10.828 7.4 15.4 6 14l6-6 6 6-1.4 1.4z"/>\n</svg>'],
        ["chevron-up-small", '<svg xmlns="http://www.w3.org/2000/svg" viewBox="0 0 24 24">\n	<path fill="currentColor" d="M12 9.828 5.4 16.4 4 15l8-8 8 8-1.4 1.4z"/>\n</svg>'],
        ["circle-dashed", '<svg xmlns="http://www.w3.org/2000/svg" viewBox="0 0 24 24">\n	<path\n		fill="currentColor"\n		d="M14.59 21.66a10.05 10.05 0 0 1-5.18 0l.52-1.93a8.05 8.05 0 0 0 4.14 0zM5.07 16A8.05 8.05 0 0 0 8 18.93l-1 1.73A10.05 10.05 0 0 1 3.34 17zm15.59 1A10.04 10.04 0 0 1 17 20.66l-1-1.73A8.05 8.05 0 0 0 18.93 16zM4.27 9.93A8 8 0 0 0 4 12a8 8 0 0 0 .27 2.07l-1.93.52a10 10 0 0 1 0-5.18zm17.39-.52a10 10 0 0 1 0 5.18l-.96-.26-.97-.26a8.06 8.06 0 0 0 0-4.14zM8 5.07A8.05 8.05 0 0 0 5.07 8L3.34 7A10.05 10.05 0 0 1 7 3.34zm9-1.73A10.05 10.05 0 0 1 20.66 7l-1.73 1A8.05 8.05 0 0 0 16 5.07zM12 2c.9 0 1.76.12 2.59.34l-.26.96-.26.97A8 8 0 0 0 12 4a8 8 0 0 0-2.07.27L9.4 2.34C10.24 2.12 11.11 2 12 2"\n	/>\n</svg>'],
        ["circle-filled", '<svg xmlns="http://www.w3.org/2000/svg" viewBox="0 0 24 24">\n	<path fill="currentColor" d="M2 12c0 5.51 4.49 10 10 10s10-4.49 10-10S17.51 2 12 2 2 6.49 2 12"/>\n</svg>'],
        ["circle-filled-extra-small", '<svg xmlns="http://www.w3.org/2000/svg" viewBox="0 0 24 24">\n	<path fill="currentColor" d="M8 12c0 2.204 1.796 4 4 4s4-1.796 4-4-1.796-4-4-4-4 1.796-4 4"/>\n</svg>'],
        ["circle-filled-small", '<svg xmlns="http://www.w3.org/2000/svg" viewBox="0 0 24 24">\n	<path fill="currentColor" d="M6 12c0 3.306 2.694 6 6 6s6-2.694 6-6-2.694-6-6-6a6.01 6.01 0 0 0-6 6"/>\n</svg>'],
        ["clipboard", '<svg xmlns="http://www.w3.org/2000/svg" viewBox="0 0 24 24">\n	<path fill="currentColor" d="M15 2a1 1 0 0 1 1 1h1a3 3 0 0 1 3 3v13a3 3 0 0 1-3 3H7a3 3 0 0 1-3-3V6a3 3 0 0 1 3-3h1a1 1 0 0 1 1-1zM7 5a1 1 0 0 0-1 1v13a1 1 0 0 0 1 1h10a1 1 0 0 0 1-1V6a1 1 0 0 0-1-1h-1v2c0 .55-.42 1-1 1H9a1 1 0 0 1-1-1V5zm3 1h4V4h-4z"/>\n</svg>'],
        ["clipboard-rectangle", '<svg xmlns="http://www.w3.org/2000/svg" viewBox="0 0 24 24">\n	<path fill="currentColor" d="M18 10a3 3 0 0 1 3 3v7a3 3 0 0 1-3 3h-5a3 3 0 0 1-3-3v-7a3 3 0 0 1 3-3zm-5 2a1 1 0 0 0-1 1v7a1 1 0 0 0 1 1h5a1 1 0 0 0 1-1v-7a1 1 0 0 0-1-1z"/>\n	<path fill="currentColor" d="M14 2a1 1 0 0 1 1 1h1a3 3 0 0 1 3 3v3.13c-.65-.17-1.33-.13-2-.13V6a1 1 0 0 0-1-1h-1v2c0 .55-.42 1-1 1H8a1 1 0 0 1-1-1V5H6a1 1 0 0 0-1 1v13a1 1 0 0 0 1 1h3a4 4 0 0 0 .54 2H6a3 3 0 0 1-3-3V6a3 3 0 0 1 3-3h1a1 1 0 0 1 1-1zM9 6h4V4H9z"/>\n</svg>'],
        ["clock", '<svg xmlns="http://www.w3.org/2000/svg" viewBox="0 0 24 24">\n	<path fill="currentColor" d="m13 11.59 3.24 3.24-1.41 1.41-3.54-3.53A1 1 0 0 1 11 12V6h2z"/>\n	<path fill="currentColor" d="M12 2a10 10 0 1 1 0 20 10 10 0 0 1 0-20m0 2a8 8 0 1 0 0 16 8 8 0 0 0 0-16"/>\n</svg>'],
        ["clock-arrow-clockwise", '<svg xmlns="http://www.w3.org/2000/svg" viewBox="0 0 24 24">\n	<path fill="currentColor" d="M21 8h-6V6h2.29a8 8 0 1 0 1.64 10l1.73 1a10 10 0 0 1-4.83 4.24c-2.06.8-4.26.95-6.42.42A10 10 0 1 1 19 4.86V2h2z"/>\n	<path fill="currentColor" d="m13 11.59 3.24 3.24-1.41 1.41-3.54-3.53A1 1 0 0 1 11 12V6h2z"/>\n</svg>'],
        ["clock-arrow-counter-clockwise", '<svg xmlns="http://www.w3.org/2000/svg" viewBox="0 0 24 24">\n	<path fill="currentColor" d="M5 4.86A10 10 0 1 1 3.34 17l1.73-1A8 8 0 1 0 6.71 6H9v2H3V2h2z"/>\n	<path fill="currentColor" d="m13 11.59 3.24 3.24-1.41 1.41-3.54-3.53A1 1 0 0 1 11 12V6h2z"/>\n</svg>'],
        ["cloud", '<svg xmlns="http://www.w3.org/2000/svg" viewBox="0 0 24 24">\n	<path fill="currentColor" d="M1 13.5a5.5 5.5 0 0 1 4.64-5.43 7 7 0 0 1 13.29 1.95A4.5 4.5 0 0 1 18.5 19v-2a2.5 2.5 0 0 0 .24-4.99l-1.57-.15-.22-1.56a5 5 0 0 0-9.5-1.4l-.44.98-1.06.16A3.5 3.5 0 0 0 6.5 17v2A5.5 5.5 0 0 1 1 13.5M18.5 17v2h-12v-2z"/>\n</svg>'],
        ["cloud-arrow-down", '<svg xmlns="http://www.w3.org/2000/svg" viewBox="0 0 24 24">\n	<path fill="currentColor" d="m13 19.172 1.58-1.582L16 19l-4 4-4-4 1.42-1.41L11 19.172V10h2z"/>\n	<path fill="currentColor" d="M12 2a7 7 0 0 1 6.93 6.02A4.5 4.5 0 0 1 18.5 17H15v-2h3.5a2.5 2.5 0 0 0 .241-4.988l-1.573-.15-.219-1.564a5 5 0 0 0-9.49-1.393l-.45.972-1.057.165A3.502 3.502 0 0 0 6.5 15H9v2H6.5a5.5 5.5 0 0 1-.856-10.934A7 7 0 0 1 12 2"/>\n</svg>'],
        ["cloud-arrow-up", '<svg xmlns="http://www.w3.org/2000/svg" viewBox="0 0 24 24">\n	<path fill="currentColor" d="m16 12-1.42 1.41L13 11.828V21h-2v-9.172L9.42 13.41 8 12l4-4z"/>\n	<path fill="currentColor" d="M12 2a7 7 0 0 1 6.93 6.02A4.5 4.5 0 0 1 18.5 17H15v-2h3.5a2.5 2.5 0 0 0 .241-4.988l-1.573-.15-.219-1.564a5 5 0 0 0-9.49-1.393l-.45.972-1.057.165A3.502 3.502 0 0 0 6.5 15H9v2H6.5a5.5 5.5 0 0 1-.856-10.934A7 7 0 0 1 12 2"/>\n</svg>'],
        ["cylinder-2-big-small-split", '<svg xmlns="http://www.w3.org/2000/svg" viewBox="0 0 24 24">\n	<path fill="currentColor" d="M8 3c2.76 0 5 1.34 5 3v1.6c.84-.38 1.87-.6 3-.6 2.76 0 5 1.34 5 3v8c0 1.66-2.24 3-5 3a6.5 6.5 0 0 1-4-1.2A6.5 6.5 0 0 1 8 21c-2.76 0-5-1.34-5-3V6c0-1.66 2.24-3 5-3m3 13.4c-.84.38-1.87.6-3 .6a7 7 0 0 1-3-.6V18c0 .55 1.34 1 3 1s3-.45 3-1zm8 0c-.84.38-1.87.6-3 .6a7 7 0 0 1-3-.6V18c0 .55 1.34 1 3 1s3-.45 3-1zm-8-4c-.84.38-1.87.6-3 .6a7 7 0 0 1-3-.6V14c0 .55 1.34 1 3 1s3-.45 3-1zm8 0c-.84.38-1.87.6-3 .6a7 7 0 0 1-3-.6V14c0 .55 1.34 1 3 1s3-.45 3-1zm-8-4c-.84.38-1.87.6-3 .6a7 7 0 0 1-3-.6V10c0 .55 1.34 1 3 1s3-.45 3-1zm5 .6c-1.08 0-1.96.27-2.5.6-.32.18-.44.33-.48.4.04.07.16.22.47.4.55.33 1.43.6 2.51.6s1.96-.27 2.5-.6c.32-.18.44-.33.48-.4a1.3 1.3 0 0 0-.47-.4A5 5 0 0 0 16 9M8 5c-1.08 0-1.96.27-2.5.6-.32.18-.44.33-.48.4.04.07.16.22.47.4.55.33 1.43.6 2.51.6s1.96-.27 2.5-.6c.32-.18.44-.33.48-.4a1.3 1.3 0 0 0-.47-.4A5 5 0 0 0 8 5"/>\n</svg>'],
        ["cylinder-split", '<svg xmlns="http://www.w3.org/2000/svg" viewBox="0 0 24 24">\n	<path fill="currentColor" d="M20 18c0 2.2-3.58 4-8 4s-8-1.8-8-4V6c0-2.2 3.58-4 8-4s8 1.8 8 4zm-13.84.27c.18.21.52.5 1.08.77A11 11 0 0 0 12 20a11 11 0 0 0 4.76-.96q.82-.44 1.08-.77c.06-.07.16-.18.16-.27v-3.36A12.5 12.5 0 0 1 12 16c-2.39 0-4.53-.53-6-1.36V18c0 .1.1.2.16.27M18 8.64A12.5 12.5 0 0 1 12 10c-2.39 0-4.53-.53-6-1.36V12c0 .1.1.2.16.27.18.21.52.5 1.08.77A11 11 0 0 0 12 14a11 11 0 0 0 4.76-.96q.82-.44 1.08-.77c.06-.07.16-.18.16-.27zM12 4a11 11 0 0 0-4.76.96q-.83.44-1.08.77C6.1 5.8 6 5.91 6 6s.1.2.16.27c.18.21.52.5 1.08.77A11 11 0 0 0 12 8a11 11 0 0 0 4.76-.96q.82-.44 1.08-.77c.06-.07.16-.18.16-.27s-.1-.2-.16-.27c-.18-.21-.52-.5-1.08-.77A11 11 0 0 0 12 4"/>\n</svg>'],
        ["cylinder-split-badge-lock", '<svg xmlns="http://www.w3.org/2000/svg" viewBox="0 0 24 24">\n	<path fill="currentColor" d="M18 13a5 5 0 1 1 0 10 5 5 0 0 1 0-10M11 2c4.42 0 8 1.8 8 4v6.08a6 6 0 0 0-2.03 0L17 12V8.64A12.5 12.5 0 0 1 11 10c-2.39 0-4.53-.53-6-1.36V12c0 .1.1.2.16.27.18.21.52.5 1.08.77A11 11 0 0 0 11 14q1.55 0 2.81-.3a6 6 0 0 0-1.45 2.24Q11.7 16 11 16c-2.39 0-4.53-.53-6-1.36V18c0 .1.1.2.16.27.18.21.52.5 1.08.77A11 11 0 0 0 11 20q.69 0 1.32-.06.37 1.05 1.05 1.88Q12.24 22 11 22c-4.42 0-8-1.8-8-4V6c0-2.2 3.58-4 8-4m7 12.75c-.97 0-1.75.78-1.75 1.75v.75H16a.5.5 0 0 0-.5.5v2.5c0 .28.22.5.5.5h4a.5.5 0 0 0 .5-.5v-2.5a.5.5 0 0 0-.5-.5h-.25v-.75c0-.9-.69-1.65-1.57-1.74zm0 1c.41 0 .75.34.75.75v.75h-1.5v-.75c0-.41.34-.75.75-.75M11 4a11 11 0 0 0-4.76.96q-.83.44-1.08.77C5.1 5.8 5 5.91 5 6s.1.2.16.27c.18.21.52.5 1.08.77A11 11 0 0 0 11 8a11 11 0 0 0 4.76-.96q.83-.44 1.08-.77c.06-.07.16-.18.16-.27s-.1-.2-.16-.27c-.18-.21-.52-.5-1.08-.77A11 11 0 0 0 11 4"/>\n</svg>'],
        ["cylinder-split-slash", '<svg xmlns="http://www.w3.org/2000/svg" viewBox="0 0 24 24">\n	<path fill="currentColor" d="M22 20.59 20.6 22 2 3.41 3.41 2zM6 8.83V12c0 .1.1.2.16.27.18.21.52.5 1.08.77.95.48 2.3.84 3.9.93l1.99 1.99Q12.57 16 12 16c-2.39 0-4.53-.53-6-1.36V18c0 .1.1.2.16.27.18.21.52.5 1.08.77A11 11 0 0 0 12 20c1.76 0 3.28-.32 4.38-.79l1.5 1.5A13 13 0 0 1 12 22c-4.42 0-8-1.8-8-4V6.83zM12 2c4.42 0 8 1.8 8 4v11.17l-2-2v-.53l-.35.19-1.51-1.52q.33-.12.62-.27.82-.44 1.08-.77c.06-.07.16-.18.16-.27V8.64c-1.3.74-3.13 1.23-5.2 1.34l-2.03-2.03Q11.37 8 12 8a11 11 0 0 0 4.76-.96q.82-.44 1.08-.77c.06-.07.16-.18.16-.27s-.1-.2-.16-.27c-.18-.21-.52-.5-1.08-.77A11 11 0 0 0 12 4c-1.76 0-3.28.32-4.38.79l-1.5-1.5A13 13 0 0 1 12 2"/>\n</svg>'],
        ["desk-with-screen", '<svg xmlns="http://www.w3.org/2000/svg" viewBox="0 0 24 24">\n	<path fill="currentColor" d="M21 11a1 1 0 0 1 1 1v8.1a1 1 0 0 1-.9.9h-5.2a1 1 0 0 1-.9-.9V13H5v7.1a1 1 0 0 1-2 0V12a1 1 0 0 1 1-1h5V9H7a1 1 0 0 1-1-.9V4a1 1 0 0 1 1-1h6a1 1 0 0 1 1 1v4.1a1 1 0 0 1-1 .9h-2v2zm-4 8h3v-2h-3zm0-4h3v-2h-3zM8 7h4V5H8z"/>\n</svg>'],
        ["diamond", '<svg xmlns="http://www.w3.org/2000/svg" viewBox="0 0 24 24">\n	<path fill="currentColor" d="M22 10L12 22L2 10L6 3H18L22 10ZM10.0635 16.5527L7.8916 11H5.43652L10.0635 16.5527ZM13.9355 16.5518L18.5625 11H16.1084L13.9355 16.5518ZM12 16.0117L13.9609 11H10.0391L12 16.0117ZM16.2139 9H19.125L16.8398 5H15.0713L16.2139 9ZM9.86523 9H14.1348L12.9912 5H11.0088L9.86523 9ZM4.87402 9H7.78613L8.92871 5H7.16016L4.87402 9Z"/>\n</svg>'],
        ["dismiss", '<svg xmlns="http://www.w3.org/2000/svg" viewBox="0 0 24 24">\n	<path fill="currentColor" d="m19.3 3.3 1.4 1.4-7.29 7.3 7.3 7.3-1.42 1.4L12 13.42l-7.3 7.3-1.4-1.42L10.58 12l-7.3-7.3 1.42-1.4L12 10.58z"/>\n</svg>'],
        ["dismiss-circle", '<svg xmlns="http://www.w3.org/2000/svg" viewBox="0 0 24 24">\n	<path fill="currentColor" d="m12 10.59 2.8-2.8 1.4 1.42L13.42 12l2.8 2.8-1.42 1.4L12 13.42l-2.8 2.8-1.4-1.42L10.58 12l-2.8-2.79 1.42-1.42z"/>\n	<path fill="currentColor" d="M12 2a10 10 0 1 1 0 20 10 10 0 0 1 0-20m0 2a8 8 0 1 0 0 16 8 8 0 0 0 0-16"/>\n</svg>'],
        ["dismiss-circle-filled", '<svg xmlns="http://www.w3.org/2000/svg" viewBox="0 0 24 24">\n	<path fill="currentColor" d="M12 2a10 10 0 1 1 0 20 10 10 0 0 1 0-20m0 8.59-3.3-3.3-1.4 1.42L10.58 12l-3.3 3.3 1.42 1.4L12 13.42l3.3 3.3 1.4-1.42L13.42 12l3.3-3.3-1.42-1.4z"/>\n</svg>'],
        ["dismiss-extra-small", '<svg xmlns="http://www.w3.org/2000/svg" viewBox="0 0 24 24">\n	<path fill="currentColor" d="m15.3 7.3 1.4 1.4-3.29 3.3 3.3 3.3-1.42 1.4L12 13.42l-3.3 3.3-1.4-1.42L10.58 12l-3.3-3.3 1.42-1.4L12 10.58z"/>\n</svg>'],
        ["dismiss-small", '<svg xmlns="http://www.w3.org/2000/svg" viewBox="0 0 24 24">\n	<path fill="currentColor" d="m17.3 5.3 1.4 1.4-5.29 5.3 5.3 5.3-1.42 1.4L12 13.42l-5.3 5.3-1.4-1.42L10.58 12l-5.3-5.3 1.42-1.4L12 10.58z"/>\n</svg>'],
        ["ellipsis", '<svg xmlns="http://www.w3.org/2000/svg" viewBox="0 0 24 24">\n	<path fill="currentColor" d="M5 10a2 2 0 1 1 0 4 2 2 0 0 1 0-4m7 0a2 2 0 1 1 0 4 2 2 0 0 1 0-4m7 0a2 2 0 1 1 0 4 2 2 0 0 1 0-4"/>\n</svg>'],
        ["envelope", '<svg xmlns="http://www.w3.org/2000/svg" viewBox="0 0 24 24">\n	<path fill="currentColor" d="M19 4a3 3 0 0 1 3 3v10c0 1.66-1.34 3-3 3H5c-1.66 0-3-1.34-3-3V7a3 3 0 0 1 3-3zm-5.5 9.562a3 3 0 0 1-3 0L4 9.81V17a1 1 0 0 0 1 1h14a1 1 0 0 0 1-1V9.81zM5 6a1 1 0 0 0-1 1v.5l7.5 4.33a1 1 0 0 0 1 0L20 7.5V7a1 1 0 0 0-1-1z"/>\n</svg>'],
        ["euro-sign", '<svg xmlns="http://www.w3.org/2000/svg" viewBox="0 0 24 24">\n	<path fill="currentColor" d="M10.25 2.76a7.3 7.3 0 0 1 4.9-.57 8.2 8.2 0 0 1 4.36 2.74L18.1 6.59a6.3 6.3 0 0 0-3.32-2.09 5.6 5.6 0 0 0-3.76.44l-.22.11a7 7 0 0 0-2.7 2.7L7.98 8A8 8 0 0 0 7.52 9H14v2H7.06a9 9 0 0 0 0 2H14v2H7.52q.24.65.58 1.25A7 7 0 0 0 11 19.06a5.6 5.6 0 0 0 3.52.49l.24-.05a6.3 6.3 0 0 0 3.32-2.1l1.42 1.67-.23.26a8 8 0 0 1-4.12 2.48 7.3 7.3 0 0 1-4.62-.44l-.3-.13a9 9 0 0 1-3.63-3.38l-.18-.3Q5.75 16.36 5.4 15H3v-2h2.04a12 12 0 0 1 0-2H3V9h2.4q.31-1.18.86-2.24l.17-.32a9 9 0 0 1 3.82-3.68"/>\n</svg>'],
        ["exclamation-circle", '<svg xmlns="http://www.w3.org/2000/svg" viewBox="0 0 24 24">\n	<path fill="currentColor" d="M12 15a1.25 1.25 0 1 1 0 2.5 1.25 1.25 0 0 1 0-2.5m1-1.5h-2v-7h2z"/>\n	<path fill="currentColor" d="M12 2c5.523 0 10 4.477 10 10s-4.477 10-10 10S2 17.523 2 12 6.477 2 12 2m0 2a8 8 0 1 0 0 16 8 8 0 0 0 0-16"/>\n</svg>'],
        ["exclamation-circle-filled", '<svg xmlns="http://www.w3.org/2000/svg" viewBox="0 0 24 24">\n	<path fill="currentColor" d="M12 2a10 10 0 1 1 0 20 10 10 0 0 1 0-20m0 13.5a1.25 1.25 0 1 0 0 2.5 1.25 1.25 0 0 0 0-2.5M11 6v8h2V6z"/>\n</svg>'],
        ["exclamation-triangle", '<svg xmlns="http://www.w3.org/2000/svg" viewBox="0 0 24 24">\n	<path fill="currentColor" d="M12 14a1.25 1.25 0 1 1 0 2.5 1.25 1.25 0 0 1 0-2.5m1-1.5h-2v-5h2z"/>\n	<path fill="currentColor" d="M11.99 1.968a3 3 0 0 1 2.62 1.538l7.095 12.242q.035.06.062.126a3 3 0 0 1-2.76 4.125H5a3.02 3.02 0 0 1-2.538-1.387 3 3 0 0 1-.248-2.739 1 1 0 0 1 .06-.125L9.37 3.506a3 3 0 0 1 2.62-1.538m0 2c-.373 0-.705.21-.884.533L4.046 16.68c-.097.286-.06.602.104.86a.99.99 0 0 0 .85.459h13.996a1 1 0 0 0 .938-1.32L12.875 4.502a1.02 1.02 0 0 0-.885-.533"/>\n</svg>'],
        ["exclamation-triangle-filled", '<svg xmlns="http://www.w3.org/2000/svg" viewBox="0 0 24 24">\n	<path fill="currentColor" d="M11.99 1.968a3 3 0 0 1 2.62 1.538l7.095 12.242q.035.06.062.126a3 3 0 0 1-2.76 4.125H5a3.02 3.02 0 0 1-2.538-1.387 3 3 0 0 1-.248-2.739 1 1 0 0 1 .06-.125L9.37 3.506a3 3 0 0 1 2.62-1.538M12 14a1.25 1.25 0 1 0 0 2.5 1.25 1.25 0 0 0 0-2.5m-1-7v5h2V7z"/>\n</svg>'],
        ["eye", '<svg xmlns="http://www.w3.org/2000/svg" viewBox="0 0 24 24">\n	<path fill="currentColor" d="M12 4.5c5.012 0 9.26 3.108 11 7.5-1.74 4.392-5.988 7.5-10.999 7.5S2.739 16.392 1 12c1.74-4.392 5.988-7.5 11-7.5m0 2c-3.878 0-7.188 2.239-8.8 5.5 1.612 3.261 4.923 5.5 8.801 5.5 3.877 0 7.187-2.239 8.799-5.5-1.613-3.261-4.923-5.5-8.8-5.5m0 1a4.5 4.5 0 1 1 0 9 4.5 4.5 0 0 1 0-9m0 2a2.5 2.5 0 1 0 0 5 2.5 2.5 0 0 0 0-5"/>\n</svg>'],
        ["eye-slash", '<svg xmlns="http://www.w3.org/2000/svg" viewBox="0 0 24 24">\n	<path fill="currentColor" d="m21.004 19.59-1.414 1.414L3 4.414 4.414 3zM5.837 8.665C4.717 9.563 3.843 10.7 3.2 12c1.612 3.261 4.923 5.5 8.801 5.5.82 0 1.617-.102 2.38-.291l1.603 1.604a11.8 11.8 0 0 1-3.983.687C6.989 19.5 2.739 16.392 1 12a11.8 11.8 0 0 1 3.416-4.756zM12 4.5c5.012 0 9.26 3.108 11 7.5a11.8 11.8 0 0 1-3.416 4.756l-1.422-1.422c1.12-.898 1.995-2.034 2.638-3.334-1.613-3.261-4.923-5.5-8.8-5.5-.821 0-1.619.1-2.382.29L8.015 5.187A11.8 11.8 0 0 1 12 4.5"/>\n	<path fill="currentColor" d="M9.527 12.356a2.5 2.5 0 0 0 2.117 2.116l1.791 1.792a4.5 4.5 0 0 1-5.7-5.7zM12 7.5a4.5 4.5 0 0 1 4.264 5.935l-1.792-1.791a2.5 2.5 0 0 0-2.117-2.117l-1.792-1.792A4.5 4.5 0 0 1 12 7.5"/>\n</svg>'],
        ["eyeglasses", '<svg xmlns="http://www.w3.org/2000/svg" viewBox="0 0 24 24">\n	<path fill="currentColor" d="M18 6a5 5 0 0 1 4.45 3H24v2h-1.02l.02.5c0 3.04-2.24 5.5-5 5.5s-5-2.46-5-5.5l.01-.4a6 6 0 0 0-2.03 0l.02.4c0 3.04-2.24 5.5-5 5.5s-5-2.46-5-5.5q0-.26.02-.5H0V9h1.55C2.37 7.22 4.05 6 6 6c2 0 3.71 1.28 4.52 3.14a8 8 0 0 1 2.96 0A5 5 0 0 1 18 6M6 8c-1.48 0-3 1.38-3 3.5S4.52 15 6 15s3-1.38 3-3.5S7.48 8 6 8m12 0c-1.48 0-3 1.38-3 3.5s1.52 3.5 3 3.5 3-1.38 3-3.5S19.48 8 18 8"/>\n</svg>'],
        ["face-frowning", '<svg xmlns="http://www.w3.org/2000/svg" viewBox="0 0 24 24">\n	<path fill="currentColor" d="M12 2a10 10 0 1 1 0 20 10 10 0 0 1 0-20m0 2a8 8 0 1 0 0 16 8 8 0 0 0 0-16m0 8.87c1.78 0 3.5.81 4.63 2.18.35.43.18 1.04-.3 1.32s-1.08.1-1.47-.3a4 4 0 0 0-2.86-1.2c-1.07 0-2.12.45-2.86 1.2-.39.4-1 .58-1.47.3-.48-.28-.65-.9-.3-1.32A6 6 0 0 1 12 12.87M9.5 8a1.5 1.5 0 1 1 0 3 1.5 1.5 0 0 1 0-3m5 0a1.5 1.5 0 1 1 0 3 1.5 1.5 0 0 1 0-3"/>\n</svg>'],
        ["face-smiling", '<svg xmlns="http://www.w3.org/2000/svg" viewBox="0 0 24 24">\n	<path fill="currentColor" d="M14.86 13.8c.39-.4 1-.58 1.47-.3.48.28.65.9.3 1.32A6 6 0 0 1 12 17c-1.77 0-3.5-.82-4.63-2.18-.35-.43-.18-1.04.3-1.32s1.08-.1 1.47.3c.74.75 1.8 1.2 2.86 1.2 1.07 0 2.12-.45 2.86-1.2M9.5 8a1.5 1.5 0 1 1 0 3 1.5 1.5 0 0 1 0-3M14.5 8a1.5 1.5 0 1 1 0 3 1.5 1.5 0 0 1 0-3"/>\n	<path fill="currentColor" d="M12 2a10 10 0 1 1 0 20 10 10 0 0 1 0-20m0 2a8 8 0 1 0 0 16 8 8 0 0 0 0-16"/>\n</svg>'],
        ["face-smiling-badge-plus", '<svg xmlns="http://www.w3.org/2000/svg" viewBox="0 0 24 24">\n	<path fill="currentColor" d="M18 13a5 5 0 1 1 0 10 5 5 0 0 1 0-10M12 2a10 10 0 0 1 9.9 11.44 6 6 0 0 0-1.91-1.1L20 12a8 8 0 1 0-8 8l.34-.01q.38 1.06 1.1 1.9-.7.1-1.44.11a10 10 0 0 1 0-20m5.25 15.25H15v1.5h2.25V21h1.5v-2.25H21v-1.5h-2.25V15h-1.5zM7.67 13.5c.48-.28 1.08-.1 1.47.3a4 4 0 0 0 3.72 1.1 6 6 0 0 0-.77 2.1H12c-1.77 0-3.5-.82-4.63-2.18-.35-.43-.18-1.04.3-1.32M9.5 8a1.5 1.5 0 1 1 0 3 1.5 1.5 0 0 1 0-3m5 0a1.5 1.5 0 1 1 0 3 1.5 1.5 0 0 1 0-3"/>\n</svg>'],
        ["file-box", '<svg xmlns="http://www.w3.org/2000/svg" viewBox="0 0 24 24">\n	<path fill="currentColor" d="M15 12a1 1 0 0 1-1 1h-4a1 1 0 1 1 0-2h4a1 1 0 0 1 1 1"/>\n	<path fill="currentColor" d="M19 3a3 3 0 0 1 3 3v2c0 .56-.44 1-1 1v9c0 1.67-1.33 3-3 3H6c-1.67 0-3-1.33-3-3V9c-.56 0-1-.44-1-1V6a3 3 0 0 1 3-3zM5 18a1 1 0 0 0 1 1h12a1 1 0 0 0 1-1V9H5zM5 5a1 1 0 0 0-1 1v1h16V6a1 1 0 0 0-1-1z"/>\n</svg>'],
        ["file-text", '<svg xmlns="http://www.w3.org/2000/svg" viewBox="0 0 24 24">\n	<path fill="currentColor" d="M7 4a1 1 0 0 0-1 1v14a1 1 0 0 0 1 1h10a1 1 0 0 0 1-1V9h-3a2 2 0 0 1-2-2V4zm8 1.414L16.586 7H15zM4 5a3 3 0 0 1 3-3h7a1 1 0 0 1 .707.293l5 5A1 1 0 0 1 20 8v11a3 3 0 0 1-3 3H7a3 3 0 0 1-3-3zm4 3h3v2H8zm0 4h8v2H8zm0 4h8v2H8z"/>\n</svg>'],
        ["file-text-batch-check-mark", '<svg xmlns="http://www.w3.org/2000/svg" viewBox="0 0 24 24">\n	<path fill="currentColor" d="M18 13a5 5 0 1 1 0 10 5 5 0 0 1 0-10m-.75 5.44-1.47-1.47-1.06 1.06 2.53 2.53 4.03-4.03-1.06-1.06z"/>\n	<path fill="currentColor" d="M13 2a1 1 0 0 1 .7.3l5 5a1 1 0 0 1 .3.7v4.08a6 6 0 0 0-2 0V9h-3a2 2 0 0 1-2-2V4H6a1 1 0 0 0-1 1v14a1 1 0 0 0 1 1h6.34a6 6 0 0 0 1.19 2H6a3 3 0 0 1-3-3V5a3 3 0 0 1 3-3zm1 5h1.59L14 5.41z"/>\n	<path fill="currentColor" d="M12.34 16q-.34.95-.34 2H7v-2zM15 12.8q-.83.49-1.47 1.2H7v-2h8zM10 10H7V8h3z"/>\n</svg>'],
        ["file-text-pencil", '<svg xmlns="http://www.w3.org/2000/svg" viewBox="0 0 24 24">\n	<path fill="currentColor" d="M14 2a1 1 0 0 1 .7.3l1.68 1.67-3.35 3.35Q13 7.17 13 7V4H7a1 1 0 0 0-1 1v14a1 1 0 0 0 1 1h10a1 1 0 0 0 1-1v-5.34l2-2V19a3 3 0 0 1-3 3H7a3 3 0 0 1-3-3V5a3 3 0 0 1 3-3z"/>\n	<path fill="currentColor" d="M17.17 4.59a2 2 0 0 1 2.83 0L21.42 6a2 2 0 0 1 0 2.83L12.25 18H8v-4.24zm-7.17 10V16h1.42l5.75-5.76-1.4-1.41zm7.17-7.18 1.42 1.42L20 7.4 18.6 6z"/>\n	<path fill="currentColor" d="M16 18h-2.34l2-2H16zm-4-9.65L10.35 10H8V8h4z"/>\n</svg>'],
        ["flag", '<svg xmlns="http://www.w3.org/2000/svg" viewBox="0 0 24 24">\n	<path fill="currentColor" fill-rule="evenodd" d="m5 3.67 2.04-.34a8 8 0 0 1 3.84.3l2.24.74a8 8 0 0 0 3.84.3L21 4v11l-4.04.67a8 8 0 0 1-3.38-.16l-2.7-.88a8 8 0 0 0-3.84-.3L5 14.67V22H3V2h2zm5.25 1.86c-1.74-.59-3.5-.13-5.25.16v6.95l1.71-.29a10 10 0 0 1 4.8.38l2.24.74a6 6 0 0 0 2.88.23l2.37-.4V6.36l-1.71.29a10 10 0 0 1-4.8-.38z" clip-rule="evenodd"/>\n</svg>'],
        ["flag-filled", '<svg xmlns="http://www.w3.org/2000/svg" viewBox="0 0 24 24">\n	<path fill="currentColor" d="m5 3.67 2.04-.34c2.15-.36 4.07.37 6.08 1.04a8 8 0 0 0 3.84.3L21 4v11l-4.04.67a8 8 0 0 1-3.84-.3c-2-.67-3.93-1.4-6.08-1.04L5 14.67V22H3V2h2z"/>\n</svg>'],
        ["folder", '<svg xmlns="http://www.w3.org/2000/svg" viewBox="0 0 24 24">\n	<path fill="currentColor" d="M2 17V6a3 3 0 0 1 3-3h3.76a3 3 0 0 1 2.12.88L13 6h6a3 3 0 0 1 3 3v8a3 3 0 0 1-3 3v-2a1 1 0 0 0 1-1V9a1 1 0 0 0-.9-1h-6.93l-2.7-2.7a1 1 0 0 0-.71-.3H5a1 1 0 0 0-1 1v11a1 1 0 0 0 1 1v2a3 3 0 0 1-3-3m17 1v2H5v-2z"/>\n</svg>'],
        ["folder-stack", '<svg xmlns="http://www.w3.org/2000/svg" viewBox="0 0 24 24">\n	<path fill="currentColor" d="M4 18a1 1 0 0 0 1 1h13a2 2 0 0 1-2 2H5a3 3 0 0 1-3-3V9c0-1.11.9-2 2-2z"/>\n	<path fill="currentColor" d="M10.76 3a3 3 0 0 1 2.12.88L15 6h4a3 3 0 0 1 3 3v6a3 3 0 0 1-3 3H8a3 3 0 0 1-3-3V6a3 3 0 0 1 3-3zM8 5a1 1 0 0 0-1 1v9a1 1 0 0 0 1 1h11a1 1 0 0 0 1-1V9a1 1 0 0 0-.9-1h-4.93l-2.7-2.7a1 1 0 0 0-.71-.3z"/>\n</svg>'],
        ["foundation", '<svg xmlns="http://www.w3.org/2000/svg" viewBox="0 0 24 24">\n	<path fill="currentColor" d="m21 8.42-1.16 1.63-.84-.6V14h3v2h-3v3h1v2h-4v-2h1v-3h-4v3h1v2h-4v-2h1v-3H7v3h1v2H4v-2h1v-3H2v-2h3V9.45l-.85.6L3 8.44 12 2zm-14-.4V14h2V9h6v5h2V8.02l-5-3.56zM11 14h2v-3h-2z"/>\n</svg>'],
        ["gear", '<svg xmlns="http://www.w3.org/2000/svg" viewBox="0 0 24 24">\n	<path fill="currentColor" d="M12 8a4 4 0 1 1 0 8 4 4 0 0 1 0-8m0 2a2 2 0 1 0 0 4 2 2 0 0 0 0-4"/>\n	<path fill="currentColor" d="M13.19 2a2 2 0 0 1 1.991 1.8l.09.9a9 9 0 0 1 1.416.818l.824-.372a2 2 0 0 1 2.555.824l1.19 2.06a2 2 0 0 1-.564 2.624l-.734.527c.055.536.056 1.09 0 1.637l.734.528c.833.6 1.078 1.734.564 2.624l-1.294 2.22a2 2 0 0 1-2.274.734l-1.001-.443a8 8 0 0 1-1.416.819l-.118 1.088a2 2 0 0 1-1.772 1.603H10.62a2 2 0 0 1-1.8-1.792l-.09-.9a8 8 0 0 1-1.416-.818l-1.001.443a2 2 0 0 1-2.275-.734l-1.293-2.22a2 2 0 0 1 .415-2.506l.882-.646a8 8 0 0 1 0-1.637l-.732-.527A2 2 0 0 1 2.658 8.2l1.278-2.23a2 2 0 0 1 2.555-.824l.823.372a8 8 0 0 1 1.416-.819l.09-.898a2 2 0 0 1 1.8-1.792zm-2.38 2.002-.207 2.049-1.055.472q-.566.256-1.061.615l-.939.68-1.88-.848-1.191 2.06 1.672 1.201C6.089 10.818 6 11.41 6 12s.088 1.18.148 1.766l-1.671 1.203h-.001l1.19 2.061 1.88-.85.94.681q.37.27.783.48l1.333.607.207 2.05V20h2.38l.206-2.05 1.055-.475a6 6 0 0 0 1.062-.614l.94-.68 1.88.85 1.19-2.061-1.672-1.203c.06-.586.15-1.177.15-1.767s-.09-1.182-.15-1.769l1.673-1.2-1.19-2.061h-.003l-1.88.846-.938-.678a6 6 0 0 0-1.062-.614l-1.055-.474L13.19 4h-2.38z"/>\n</svg>'],
        ["globe", '<svg xmlns="http://www.w3.org/2000/svg" viewBox="0 0 24 24">\n	<path fill="currentColor" d="M12 2c5.5 0 10 4.5 10 10s-4.5 10-10 10S2 17.5 2 12 6.5 2 12 2m-1 13.52q-1.07.03-2.07.13c.35 1.38.93 2.98 2.07 3.91zm2 4.04c1.14-.93 1.72-2.53 2.07-3.91q-1-.1-2.07-.13zm-6.06-3.64q-.9.17-1.69.38a8 8 0 0 0 2.8 2.66 13 13 0 0 1-1.11-3.04m10.12 0a13 13 0 0 1-1.11 3.04 8 8 0 0 0 2.8-2.66q-.79-.21-1.7-.38M4.39 9.53a8 8 0 0 0 0 4.94q1.02-.3 2.21-.52a19 19 0 0 1 0-3.9 22 22 0 0 1-2.21-.52m15.22 0q-1.02.3-2.22.52c.14 1.26.14 2.64 0 3.9q1.2.21 2.22.52c.52-1.6.52-3.34 0-4.94m-11.02.8a17 17 0 0 0 0 3.34q1.16-.12 2.41-.15v-3.04q-1.25-.03-2.41-.16m6.82 0q-1.16.12-2.41.15v3.04q1.25.03 2.41.15c.11-1.07.11-2.27 0-3.35M11 4.42c-1.14.94-1.72 2.54-2.07 3.92q.99.1 2.07.13zm2 4.05q1.07-.04 2.07-.13c-.35-1.38-.93-2.98-2.07-3.92zM8.05 5.04a8 8 0 0 0-2.8 2.66q.8.22 1.69.37a13 13 0 0 1 1.11-3.03m7.9 0q.7 1.33 1.1 3.03.91-.16 1.7-.37a8 8 0 0 0-2.8-2.66"/>\n</svg>'],
        ["globe-rack-server", '<svg xmlns="http://www.w3.org/2000/svg" viewBox="0 0 24 24">\n	<path fill="currentColor" d="M6.5 10.5a1.5 1.5 0 1 1 0 3 1.5 1.5 0 0 1 0-3"/>\n	<path fill="currentColor" d="M12 2c4 0 7.48 2.39 9.07 5.81A2 2 0 0 1 22 9.5v5a2 2 0 0 1-.93 1.69A10 10 0 0 1 12 22c-4 0-7.48-2.39-9.07-5.81A2 2 0 0 1 2 14.7V9.5a2 2 0 0 1 .93-1.69A10 10 0 0 1 12 2M9.17 16.5A6.4 6.4 0 0 0 11 19.56V16.5zM13 19.56a6.4 6.4 0 0 0 1.83-3.06H13zM5.4 16.5a8 8 0 0 0 2.65 2.46q-.57-1.1-.96-2.46zm11.5 0q-.37 1.36-.95 2.46a8 8 0 0 0 2.66-2.46zM4 14.5h16v-5H4zm4.05-9.46A8 8 0 0 0 5.4 7.5h1.7q.38-1.36.96-2.46m2.95-.6A6.4 6.4 0 0 0 9.17 7.5H11zm2 3.06h1.83A6.4 6.4 0 0 0 13 4.43zm2.95-2.46q.57 1.1.96 2.46h1.7c-.69-1-1.6-1.85-2.66-2.46"/>\n</svg>'],
        ["hand", '<svg xmlns="http://www.w3.org/2000/svg" viewBox="0 0 24 24">\n	<path fill="currentColor" d="M12.5 1c.89 0 1.67.47 2.11 1.17A2.5 2.5 0 0 1 18 4.5v.05A2.5 2.5 0 0 1 21 7v7a8 8 0 0 1-14.9 4.04l-3.4-5.87A2.5 2.5 0 0 1 7 9.6V4.5a2.5 2.5 0 0 1 3.39-2.33A2.5 2.5 0 0 1 12.5 1m0 2a.5.5 0 0 0-.5.5V10a1 1 0 1 1-2 0V4.5a.5.5 0 0 0-1 0v8.84a1 1 0 0 1-1.89.46l-1.8-3.13-.07-.09a.5.5 0 0 0-.85.5l.05.09 3.38 5.85A6 6 0 0 0 19 14V7a.5.5 0 0 0-1 0v4a1 1 0 1 1-2 0V4.5a.5.5 0 0 0-1 0V10a1 1 0 1 1-2 0V3.5a.5.5 0 0 0-.5-.5"/>\n</svg>'],
        ["handshake", '<svg xmlns="http://www.w3.org/2000/svg" viewBox="0 0 24 24">\n	<path fill="currentColor" d="M5.99 3.67a4 4 0 0 1 5.66 0l.09.1a4 4 0 0 1 4.86.6l4.24 4.25.14.15a4 4 0 0 1 0 5.36l-.14.15L14.12 21l-.19.17a2.5 2.5 0 0 1-3.15 0l-.2-.17q-.6-.62-.7-1.42a2.5 2.5 0 0 1-2.12-2.12 2.5 2.5 0 0 1-1.23-.54l-.19-.17c-.4-.4-.63-.9-.7-1.41a2.5 2.5 0 0 1-1.23-.54l-.19-.17a2.5 2.5 0 0 1 0-3.53l5.3-5.3 3.19 3.17a.5.5 0 1 0 .7-.7l-3.18-3.18a2 2 0 0 0-2.83 0L4.58 7.9a2 2 0 0 0-.52 1.93l-.54.55q-.55.55-.8 1.24a4 4 0 0 1 .44-5.13zm-.35 8.84a.5.5 0 1 0 .7.7l2.48-2.47a1 1 0 1 1 1.41 1.42l-2.47 2.47a.5.5 0 1 0 .7.7l2.48-2.47a1 1 0 1 1 1.41 1.42l-2.47 2.47a.5.5 0 0 0 .7.71l2.48-2.47a1 1 0 0 1 1.42 1.4L12 18.88a.5.5 0 1 0 .7.71l6.72-6.72a2 2 0 0 0 0-2.82L15.18 5.8a2 2 0 0 0-1.93-.51l1.58 1.57.17.2c.8.98.74 2.42-.17 3.34l-.2.17a2.5 2.5 0 0 1-3.15 0l-.19-.17-1.76-1.77z"/>\n</svg>'],
        ["heart", '<svg xmlns="http://www.w3.org/2000/svg" viewBox="0 0 24 24">\n	<path fill="currentColor" d="M12 5.216a5.858 5.858 0 1 1 8.284 8.284v.003L12 21.787l-8.284-8.284c-2.297-2.297-2.216-5.979 0-8.284C6 2.916 9.684 3.026 11.999 5.217zm6.87 1.414a3.86 3.86 0 0 0-5.456 0L12 8.045l-1.414-1.413a3.858 3.858 0 0 0-5.455 5.457l6.87 6.87 6.87-6.873a3.86 3.86 0 0 0 0-5.456"/>\n</svg>'],
        ["heart-filled", '<svg xmlns="http://www.w3.org/2000/svg" viewBox="0 0 24 24">\n	<path fill="currentColor" d="M3.716 5.218a5.86 5.86 0 0 1 8.283 0L12 5.215a5.858 5.858 0 0 1 8.284 8.284l-.001.001.001.002L12 21.787l-8.284-8.284a5.86 5.86 0 0 1 0-8.285"/>\n</svg>'],
        ["house", '<svg xmlns="http://www.w3.org/2000/svg" viewBox="0 0 24 24">\n	<path fill="currentColor" d="m12 2 10.707 10.707-1.414 1.414L20 12.828V21h-7v-6h-2v6H4v-8.172l-1.293 1.293-1.414-1.414zm-6 8.828V19h3v-6h6v6h3v-8.172l-6-6z"/>\n</svg>'],
        ["inbox", '<svg xmlns="http://www.w3.org/2000/svg" viewBox="0 0 24 24">\n	<path fill="currentColor" d="M21 6v12c0 1.66-1.34 3-3 3H6c-1.66 0-3-1.34-3-3V6a3 3 0 0 1 3-3h12c1.66 0 3 1.34 3 3M5 18a1 1 0 0 0 1 1h12a1 1 0 0 0 1-1v-4h-3.126a4.002 4.002 0 0 1-7.748 0H5zM6 5a1 1 0 0 0-1 1v6h5v1c0 1.083.92 2 2 2s2-.918 2-2v-1h5V6a1 1 0 0 0-1-1z"/>\n</svg>'],
        ["info-circle", '<svg xmlns="http://www.w3.org/2000/svg" viewBox="0 0 24 24">\n	<path fill="currentColor" d="M13 17.5h-2v-7h2zm-1-11A1.25 1.25 0 1 1 12 9a1.25 1.25 0 0 1 0-2.5"/>\n	<path fill="currentColor" d="M12 2c5.523 0 10 4.477 10 10s-4.477 10-10 10S2 17.523 2 12 6.477 2 12 2m0 2a8 8 0 1 0 0 16 8 8 0 0 0 0-16"/>\n</svg>'],
        ["info-circle-filled", '<svg xmlns="http://www.w3.org/2000/svg" viewBox="0 0 24 24">\n	<path fill="currentColor" d="M12 2a10 10 0 1 1 0 20 10 10 0 0 1 0-20m-1 16h2v-8h-2zm1-12a1.25 1.25 0 1 0 0 2.5A1.25 1.25 0 0 0 12 6"/>\n</svg>'],
        ["italic", '<svg xmlns="http://www.w3.org/2000/svg" viewBox="0 0 24 24">\n	<path fill="currentColor" d="M18 4h-2.821l-4.287 16H14v2H6v-2h2.821l4.287-16H10V2h8z"/>\n</svg>'],
        ["key", '<svg xmlns="http://www.w3.org/2000/svg" viewBox="0 0 24 24">\n	<path fill="currentColor" d="M12 5a1.5 1.5 0 1 1 0 3 1.5 1.5 0 0 1 0-3"/>\n	<path fill="currentColor" d="M12 1a6 6 0 0 1 6 6l-.01.41A6 6 0 0 1 15 12.2v.3l2.5 2.5-2 2 1.5 1.5-4.5 4.5L9 20v-7.8a6 6 0 0 1-2.99-4.8L6 7a6 6 0 0 1 6-6m0 2a4 4 0 0 0-2 7.46l1 .58v8.04l1.4 1.2 1.77-1.78-1.5-1.5 2-2L13 13.33v-2.29l1-.58A4 4 0 0 0 12 3"/>\n</svg>'],
        ["leaf", '<svg xmlns="http://www.w3.org/2000/svg" viewBox="0 0 24 24">\n	<path fill="currentColor" d="M20.73 3c0 5.66.3 11.03-2.83 14.15a8 8 0 0 1-10.98.31 11 11 0 0 0-1.44 3.5 1 1 0 0 1-1.96-.37c.28-1.5.99-3.12 2.04-4.7A8 8 0 0 1 6.59 5.84C9.7 2.7 15.07 3 20.73 3m-2 2c-2.1 0-4.02 0-5.76.23-2.24.3-3.85.9-4.97 2.02a6 6 0 0 0-1.14 6.9 19 19 0 0 1 5.38-4.45 1 1 0 0 1 .95 1.76c-1.83 1-3.64 2.53-5.09 4.33l-.02.02a6 6 0 0 0 8.4-.07c1.12-1.12 1.73-2.73 2.02-4.97.23-1.74.24-3.65.24-5.77"/>\n</svg>'],
        ["lightbulb", '<svg xmlns="http://www.w3.org/2000/svg" viewBox="0 0 24 24">\n	<path fill="currentColor" d="M15 20a2 2 0 0 1-2 2h-2a2 2 0 0 1-2-2zm0-2a.5.5 0 0 1 0 1H9a.5.5 0 0 1 0-1zM12 2a7 7 0 0 1 4.6 12.27c-.36.32-.6.76-.6 1.25 0 .82-.66 1.48-1.48 1.48H9.48C8.66 17 8 16.36 8 15.52c0-.43-.19-.82-.48-1.13l-.13-.12A7 7 0 0 1 12 2m0 2a5 5 0 0 0-3.3 8.76c.6.53 1.12 1.3 1.26 2.24h4.08c.14-.94.65-1.71 1.25-2.24A5 5 0 0 0 12 4"/>\n</svg>'],
        ["link", '<svg xmlns="http://www.w3.org/2000/svg" viewBox="0 0 24 24">\n	<path fill="currentColor" d="M14.47 9.53a5 5 0 0 1 0 7.07l-3.36 3.35a5 5 0 0 1-7.06-7.06l3.13-3.14c-.18.9-.15 1.85.09 2.74l-1.63 1.63a3 3 0 0 0 4.24 4.24l3.18-3.18a3 3 0 0 0-1.8-5.1 2 2 0 0 1 .39-.55l1.12-1.12q.94.36 1.7 1.12"/>\n	<path fill="currentColor" d="M19.78 4.22a5 5 0 0 1 0 7.07l-2.96 2.96c.18-.9.15-1.85-.09-2.74l1.63-1.63a3 3 0 1 0-4.24-4.24l-3.18 3.18a3 3 0 0 0 1.8 5.1 2 2 0 0 1-.39.56l-1.12 1.11a4.98 4.98 0 0 1-1.88-8l3.36-3.37a5 5 0 0 1 7.07 0"/>\n</svg>'],
        ["list", '<svg xmlns="http://www.w3.org/2000/svg" viewBox="0 0 24 24">\n	<path fill="currentColor" d="M2 5h20v2H2zm0 6h20v2H2zm0 6h20v2H2z"/>\n</svg>'],
        ["list-arrow-down", '<svg xmlns="http://www.w3.org/2000/svg" viewBox="0 0 24 24">\n	<path fill="currentColor" d="M3 5h11v2H3zm16 0v10.586l2.293-2.293 1.414 1.414L18 19.414l-4.707-4.707 1.414-1.414L17 15.586V5zM3 11h9v2H3zm0 6h9v2H3z"/>\n</svg>'],
        ["list-arrow-up", '<svg xmlns="http://www.w3.org/2000/svg" viewBox="0 0 24 24">\n	<path fill="currentColor" d="M3 5h9v2H3zm15-.414 4.707 4.707-1.414 1.414L19 8.414V19h-2V8.414l-2.293 2.293-1.414-1.414zM3 11h9v2H3zm0 6h11v2H3z"/>\n</svg>'],
        ["list-decreasing-lines", '<svg xmlns="http://www.w3.org/2000/svg" viewBox="0 0 24 24">\n	<path fill="currentColor" d="M2 5h20v2H2zm6 12h8v2H8zm-3-6h14v2H5z"/>\n</svg>'],
        ["lock-closed", '<svg xmlns="http://www.w3.org/2000/svg" viewBox="0 0 24 24">\n	<path fill="currentColor" d="M13 17.5h-2v-5h2z"/>\n	<path fill="currentColor" d="M17 9a3 3 0 0 1 3 3v6a3 3 0 0 1-3 3H6.85A3 3 0 0 1 4 18v-6a3 3 0 0 1 3-3V7a5 5 0 0 1 10 0zM9 9h6V7a3 3 0 1 0-6 0zm-2 2a1 1 0 0 0-1 1v6a1 1 0 0 0 1 1h10a1 1 0 0 0 1-1v-6a1 1 0 0 0-1-1z"/>\n</svg>'],
        ["lock-open", '<svg xmlns="http://www.w3.org/2000/svg" viewBox="0 0 24 24">\n	<path fill="currentColor" d="M14.5 17h-5v-2h5z"/>\n	<path fill="currentColor" d="M17 7h-2V6a3 3 0 1 0-6 0v4h8a3 3 0 0 1 3 3v6a3 3 0 0 1-3 3H6.85A3 3 0 0 1 4 19v-6a3 3 0 0 1 3-3V6a5 5 0 0 1 10 0zM7 12a1 1 0 0 0-1 1v6a1 1 0 0 0 1 1h10a1 1 0 0 0 1-1v-6a1 1 0 0 0-1-1z"/>\n</svg>'],
        ["magnifier", '<svg xmlns="http://www.w3.org/2000/svg" viewBox="0 0 24 24">\n	<path fill="currentColor" d="M2 10c0 4.4 3.6 8 8 8a7.95 7.95 0 0 0 4.9-1.7l5.7 5.7 1.4-1.4-5.7-5.7A7.97 7.97 0 0 0 18 10c0-4.4-3.6-8-8-8s-8 3.6-8 8m2 0c0-3.3 2.7-6 6-6s6 2.7 6 6-2.7 6-6 6-6-2.7-6-6"/>\n</svg>'],
        ["message-rectangle-text", '<svg xmlns="http://www.w3.org/2000/svg" viewBox="0 0 24 24">\n	<path fill="currentColor" d="M15 13H6v-2h9zm3-4H6V7h12z"/>\n	<path fill="currentColor" d="M19 3a3 3 0 0 1 3 3v9a3 3 0 0 1-3 3h-5l-4 4-4-4H5a3 3 0 0 1-3-3V6a3 3 0 0 1 3-3zM5 5a1 1 0 0 0-1 1v9a1 1 0 0 0 1 1h1.83L10 19.17 13.17 16H19a1 1 0 0 0 1-1V6a1 1 0 0 0-1-1z"/>\n</svg>'],
        ["minus", '<svg xmlns="http://www.w3.org/2000/svg" viewBox="0 0 24 24">\n	<path fill="currentColor" d="M2 11h20v2H2z"/>\n</svg>'],
        ["minus-extra-small", '<svg xmlns="http://www.w3.org/2000/svg" viewBox="0 0 24 24">\n	<path fill="currentColor" d="M6 11h12v2H6z"/>\n</svg>'],
        ["minus-small", '<svg xmlns="http://www.w3.org/2000/svg" viewBox="0 0 24 24">\n	<path fill="currentColor" d="M4 11h16v2H4z"/>\n</svg>'],
        ["moon", '<svg xmlns="http://www.w3.org/2000/svg" viewBox="0 0 24 24">\n	<path fill="currentColor" d="M10.47 2.12a8.99 8.99 0 0 0 11.41 11.4 10 10 0 1 1-11.4-11.4M8 5.07A8 8 0 1 0 18.93 16 11 11 0 0 1 8 5.07"/>\n</svg>'],
        ["numbered-list", '<svg xmlns="http://www.w3.org/2000/svg" viewBox="0 0 24 24">\n	<path fill="currentColor" d="M5.43 14a2 2 0 0 1 1.3 3.4l-1.1 1.1H7.5V20h-4v-1.5l2.16-2.16a.5.5 0 0 0 .14-.4.5.5 0 0 0-.47-.44.5.5 0 0 0-.51.37l-1.45-.39A2 2 0 0 1 5.43 14M22 19H10v-2h12zM22 13H10v-2h12zM6.5 10H5V5.5H3.5V4h3zM22 7H10V5h12z"/>\n</svg>'],
        ["paper-plane", '<svg xmlns="http://www.w3.org/2000/svg" viewBox="0 0 24 24">\n	<path fill="currentColor" d="M19.7 2.74c.96-.31 1.87.6 1.56 1.57l-5.73 17.94c-.4.98-1.8 1.03-2.27.07L10.2 14.5a1.3 1.3 0 0 0-.7-.7l-7.72-3.03c-1.1-.42-1.04-2 .08-2.35zm-7.77 10.75q.08.15.13.29l2.25 5.74 3.94-12.36zm-7.45-3.8 5.74 2.25.3.13 6.32-6.32z"/>\n</svg>'],
        ["paperclip", '<svg xmlns="http://www.w3.org/2000/svg" viewBox="0 0 24 24">\n	<path fill="currentColor" d="M8.04 15.21a.75.75 0 1 0 1.06 1.06l8-8a2.5 2.5 0 0 0-3.53-3.54l-8 8a4.25 4.25 0 1 0 6 6.02l8.01-8 1.42 1.4-8 8.01a6.2 6.2 0 0 1-8.85 0 6.2 6.2 0 0 1 0-8.84l8-8a4.5 4.5 0 1 1 6.37 6.36l-8 8a2.75 2.75 0 1 1-3.9-3.88l8.01-8 1.42 1.4z"/>\n</svg>'],
        ["pencil", '<svg xmlns="http://www.w3.org/2000/svg" viewBox="0 0 24 24">\n	<path fill="currentColor" d="M21.418 4a2 2 0 0 1 0 2.828L7.246 21H3.003v-4.243L17.175 2.586a2 2 0 0 1 2.828 0zM15.76 6.828 5.003 17.586V19h1.414L17.175 8.243zm1.414-1.414 1.414 1.414 1.414-1.414L18.59 4z"/>\n</svg>'],
        ["pencil-on-square", '<svg xmlns="http://www.w3.org/2000/svg" viewBox="0 0 24 24">\n	<path fill="currentColor" d="M10.932 6H5a1 1 0 0 0-1 1v12a1 1 0 0 0 1 1h12a1 1 0 0 0 1-1v-5.927l2-2V19c0 1.66-1.34 3-3 3H5c-1.66 0-3-1.34-3-3V7a3 3 0 0 1 3-3h7.93z"/>\n	<path fill="currentColor" d="M17.175 2.586a2 2 0 0 1 2.829 0L21.418 4a2 2 0 0 1 0 2.828L11.246 17H7.003v-4.242zm-8.172 11V15h1.414l6.758-6.758-1.414-1.414zm8.172-8.172 1.414 1.414 1.415-1.414L18.589 4z"/>\n</svg>'],
        ["pencil-ruler", '<svg xmlns="http://www.w3.org/2000/svg" viewBox="0 0 24 24">\n	<path fill="currentColor" d="M21.9 16.24a1 1 0 0 1 0 1.42l-4.24 4.24a1 1 0 0 1-1.42 0L12 17.66l1.41-1.42 3.54 3.54 2.83-2.83-1.42-1.41-1.41 1.4-1.42-1.4 1.42-1.42-.7-.7 1.4-1.42z"/>\n	<path fill="currentColor" d="M17.17 2.59a2 2 0 0 1 2.83 0L21.42 4a2 2 0 0 1 0 2.83L6.25 22H2v-4.24zM4 18.59V20h1.42L17.17 8.24l-1.41-1.41zM17.17 5.4l1.42 1.42L20 5.4 18.6 4z"/>\n	<path fill="currentColor" d="M6.34 2.1a1 1 0 0 1 1.42 0L12 6.34l-1.41 1.42-.71-.71-1.42 1.41-1.4-1.41 1.4-1.41-1.4-1.42-2.84 2.83 3.54 3.54L6.34 12 2.1 7.76a1 1 0 0 1 0-1.42z"/>\n</svg>'],
        ["person", '<svg xmlns="http://www.w3.org/2000/svg" viewBox="0 0 24 24">\n	<path fill="currentColor" d="M12 4a3 3 0 1 0 0 6 3 3 0 0 0 0-6M7 7a5 5 0 1 1 10 0A5 5 0 0 1 7 7m2 9a3 3 0 0 0-3 3v3H4v-3a5 5 0 0 1 5-5h6a5 5 0 0 1 5 5l-.032 3h-2L18 19a3 3 0 0 0-3-3z"/>\n</svg>'],
        ["person-2", '<svg xmlns="http://www.w3.org/2000/svg" viewBox="0 0 24 24">\n	<path fill="currentColor" d="M10 14c2.2 0 4 1.8 4 4v3h-2v-3a2 2 0 0 0-2-2H6a2 2 0 0 0-2 2v3H2v-3c0-2.2 1.8-4 4-4zM22 17v3h-2v-3a2 2 0 0 0-2-2h-3.93a5 5 0 0 0-1.92-1.55A4 4 0 0 1 14 13h4c2.2 0 4 1.8 4 4"/>\n	<path fill="currentColor" d="M8 4a4 4 0 1 1 0 8 4 4 0 0 1 0-8m0 2a2 2 0 1 0 0 4 2 2 0 0 0 0-4M16 3a4 4 0 1 1-3.2 6.4 5 5 0 0 0-.5-3.94A4 4 0 0 1 16 3m0 2a2 2 0 1 0 0 4 2 2 0 0 0 0-4"/>\n</svg>'],
        ["person-badge-gear", '<svg xmlns="http://www.w3.org/2000/svg" viewBox="0 0 24 24">\n	<path fill="currentColor" d="M18.75 13q.23.02.25.25v.68q.01.19.19.25.35.11.67.28c.1.05.23.04.3-.04l.5-.49c.09-.1.25-.1.34 0L22.07 15c.1.1.1.25 0 .35l-.49.48q-.12.14-.04.3.16.33.28.68.06.18.25.19h.68q.23.02.25.25v1.5q-.02.23-.25.25h-.68a.3.3 0 0 0-.25.19q-.11.35-.28.67c-.05.1-.04.23.04.3l.49.5c.1.09.1.25 0 .34L21 22.07c-.1.1-.25.1-.35 0l-.48-.49a.3.3 0 0 0-.3-.04q-.33.16-.68.28a.3.3 0 0 0-.19.25v.68q-.02.23-.25.25h-1.5a.25.25 0 0 1-.25-.25v-.68a.3.3 0 0 0-.19-.25 4 4 0 0 1-.67-.28.3.3 0 0 0-.3.04l-.5.49c-.09.1-.25.1-.34 0L13.93 21a.25.25 0 0 1 0-.35l.49-.48q.13-.13.04-.3-.16-.33-.28-.68a.3.3 0 0 0-.25-.19h-.68a.25.25 0 0 1-.25-.25v-1.5q.02-.23.25-.25h.68q.2-.01.25-.19.11-.35.28-.67a.3.3 0 0 0-.04-.3l-.49-.5a.25.25 0 0 1 0-.34L15 13.93c.1-.1.25-.1.35 0l.48.49q.14.13.3.04.33-.16.68-.28.18-.06.19-.25v-.68q.02-.23.25-.25zM18 16.5a1.5 1.5 0 1 0 0 3 1.5 1.5 0 0 0 0-3"/>\n	<path fill="currentColor" d="M13.53 14a6 6 0 0 0-1.19 2H9a3 3 0 0 0-3 3v3H4v-3a5 5 0 0 1 5-5z"/>\n	<path fill="currentColor" d="M12 2a5 5 0 1 1 0 10 5 5 0 0 1 0-10m0 2a3 3 0 1 0 0 6 3 3 0 0 0 0-6"/>\n</svg>'],
        ["person-circle", '<svg xmlns="http://www.w3.org/2000/svg" viewBox="0 0 24 24">\n	<path fill="currentColor" d="M12 6a3.25 3.25 0 1 1 0 6.5A3.25 3.25 0 0 1 12 6"/>\n	<path fill="currentColor" d="M12 2c5.523 0 10 4.477 10 10s-4.477 10-10 10S2 17.523 2 12 6.477 2 12 2m0 2a8 8 0 0 0-5.5 13.807V17a3 3 0 0 1 3-3h5a3 3 0 0 1 3 3v.807A8 8 0 0 0 12 4"/>\n</svg>'],
        ["photo", '<svg xmlns="http://www.w3.org/2000/svg" viewBox="0 0 24 24">\n	<path fill="currentColor" d="M9 8a2 2 0 1 1 0 4 2 2 0 0 1 0-4"/>\n	<path fill="currentColor" d="M19 4a3 3 0 0 1 3 3v10c0 1.666-1.33 3-3 3H5c-1.66 0-3-1.334-3-3V7a3 3 0 0 1 3-3zM5 6a1 1 0 0 0-1 1v9l1.94-1.94a1.5 1.5 0 0 1 2.12 0l1.956 1.956 4.617-4.472a1.5 1.5 0 0 1 2.122.034L20 14.932V7a1 1 0 0 0-1-1z"/>\n</svg>'],
        ["photo-slash", '<svg xmlns="http://www.w3.org/2000/svg" viewBox="0 0 24 24">\n	<path fill="currentColor" d="M22 20.59 20.6 22 2 3.41 3.41 2zM4.01 6.84 4 7v9l1.94-1.94a1.5 1.5 0 0 1 2.12 0l1.96 1.96 1.6-1.57L17.18 20H5c-1.67 0-3-1.33-3-3V7q.02-.94.5-1.67zM19 4a3 3 0 0 1 3 3v10q-.02.94-.5 1.66l-7-6.99c.63-.6 1.59-.78 2.25-.1L20 14.94V7a1 1 0 0 0-1-1H8.83l-2-2zm-9.84 8a2.02 2.02 0 0 1-2.15-2.16z"/>\n</svg>'],
        ["pipeline-corner-2", '<svg xmlns="http://www.w3.org/2000/svg" viewBox="0 0 24 24">\n	<path fill="currentColor" d="M13 11V8a3 3 0 0 0-3-3H4v2h5.5c.83 0 1.5.67 1.5 1.5V11zm-2 5a3 3 0 0 0 3 3h6v-2h-5a2 2 0 0 1-2-2v-2h-2zm5-4a1 1 0 0 1-1 1v1.5c0 .28.22.5.5.5H20a1 1 0 1 1 2 0v6a1 1 0 1 1-2 0h-6a5 5 0 0 1-5-5v-3a1 1 0 1 1 0-2V9.5a.5.5 0 0 0-.5-.5H4a1 1 0 0 1-2 0V3a1 1 0 0 1 2 0h6a5 5 0 0 1 5 5v3a1 1 0 0 1 1 1"/>\n</svg>'],
        ["pipeline-machine-gear", '<svg xmlns="http://www.w3.org/2000/svg" viewBox="0 0 24 24">\n	<path fill="currentColor" d="M12.5 8.5q.23.02.25.25v.42q0 .16.18.24l.24.1q.16.07.3-.04l.3-.3c.1-.1.25-.1.35 0l.7.7c.1.1.1.26 0 .36l-.29.3q-.12.13-.04.3l.1.24q.06.17.24.18h.42q.23.02.25.25v1q-.02.23-.25.25h-.42q-.17 0-.24.18l-.1.24q-.07.16.05.3l.29.3c.1.1.1.25 0 .35l-.7.7c-.1.1-.26.1-.36 0l-.3-.29a.3.3 0 0 0-.3-.04l-.24.1a.3.3 0 0 0-.18.24v.42q-.02.23-.25.25h-1a.25.25 0 0 1-.25-.25v-.42q-.02-.17-.18-.24l-.24-.1a.3.3 0 0 0-.3.04l-.3.3c-.1.1-.25.1-.35 0l-.7-.7a.25.25 0 0 1 0-.36l.29-.3q.11-.13.04-.3l-.1-.24a.3.3 0 0 0-.24-.18h-.42a.25.25 0 0 1-.25-.25v-1q.02-.23.25-.25h.42q.16-.02.24-.18l.1-.24a.3.3 0 0 0-.04-.3l-.3-.3a.25.25 0 0 1 0-.35l.7-.7c.1-.1.26-.1.36 0l.3.28q.13.12.3.05l.24-.1q.17-.06.18-.24v-.42q.02-.23.25-.25zM12 11a1 1 0 1 0 0 2 1 1 0 0 0 0-2"/>\n	<path fill="currentColor" d="M22 4a1 1 0 0 1 1 1v6a1 1 0 1 1-2 0h-2.07q.06.5.07 1a7 7 0 0 1-7 7H3a1 1 0 1 1-2 0v-6a1 1 0 1 1 2 0h2.07A7 7 0 0 1 12 5h9a1 1 0 0 1 1-1M3 17h4.1a7 7 0 0 1-1.43-2H3zm9-10a5 5 0 1 0 0 10 5 5 0 0 0 0-10m4.9 0a7 7 0 0 1 1.43 2H21V7z"/>\n</svg>'],
        ["pipeline-valve", '<svg xmlns="http://www.w3.org/2000/svg" viewBox="0 0 24 24">\n	<path fill="currentColor" d="M13 10h1.1c.5.06.9.48.9 1h5a1 1 0 1 1 2 0v6a1 1 0 1 1-2 0h-5a1 1 0 0 1-.9 1H10a1 1 0 0 1-1-1H4a1 1 0 1 1-2 0v-6a1 1 0 1 1 2 0h5a1 1 0 0 1 1-1h1V7H9a1 1 0 0 1 0-2h6a1 1 0 1 1 0 2h-2zm-2 6h2v-4h-2zm-7-1h5v-2H4zm11 0h5v-2h-5z"/>\n</svg>'],
        ["plus", '<svg xmlns="http://www.w3.org/2000/svg" viewBox="0 0 24 24">\n	<path fill="currentColor" d="M13 11h9v2h-9v9h-2v-9H2v-2h9V2h2z"/>\n</svg>'],
        ["plus-small", '<svg xmlns="http://www.w3.org/2000/svg" viewBox="0 0 24 24">\n	<path fill="currentColor" d="M13 11h7v2h-7v7h-2v-7H4v-2h7V4h2z"/>\n</svg>'],
        ["point-bottom-left-to-point-top-right-s-curve-path", '<svg xmlns="http://www.w3.org/2000/svg" viewBox="0 0 24 24">\n	<path fill="currentColor" d="M18 1a4 4 0 1 1-3.87 5H6.5a2.5 2.5 0 0 0 0 5h11a4.5 4.5 0 1 1 0 9H9.87a4 4 0 1 1 0-2h7.63a2.5 2.5 0 0 0 0-5h-11a4.5 4.5 0 1 1 0-9h7.63A4 4 0 0 1 18 1M6 17a2 2 0 1 0 0 4 2 2 0 0 0 0-4M18 3a2 2 0 1 0 0 4 2 2 0 0 0 0-4"/>\n</svg>'],
        ["puzzle-piece", '<svg xmlns="http://www.w3.org/2000/svg" viewBox="0 0 24 24">\n	<path fill="currentColor" d="M11 1a3.5 3.5 0 0 1 3.5 3.5v.75c0 .14.11.25.25.25H18a1 1 0 0 1 1 1v2.75q.02.23.25.25h.25a3.5 3.5 0 1 1 0 7h-.25a.25.25 0 0 0-.25.25v3.75a1 1 0 0 1-1 1h-4.75a.25.25 0 0 1-.25-.25V20a1.5 1.5 0 0 0-3 0v1.25q-.02.23-.25.25H4a1 1 0 0 1-1-1v-5.75c0-.14.11-.25.25-.25H4.5a1.5 1.5 0 0 0 0-3H3.25a.25.25 0 0 1-.25-.25V6.5a1 1 0 0 1 1-1h3.25c.14 0 .25-.11.25-.25V4.5A3.5 3.5 0 0 1 11 1m0 2c-.83 0-1.5.67-1.5 1.5v.75c0 1.24-1 2.25-2.25 2.25H5v2.04a3.5 3.5 0 0 1 0 6.92v3.04h3.04a3.5 3.5 0 0 1 6.92 0H17v-2.75c0-1.24 1-2.25 2.25-2.25h.25a1.5 1.5 0 0 0 0-3h-.25c-1.24 0-2.25-1-2.25-2.25V7.5h-2.25c-1.24 0-2.25-1-2.25-2.25V4.5c0-.83-.67-1.5-1.5-1.5"/>\n</svg>'],
        ["puzzle-piece-filled", '<svg xmlns="http://www.w3.org/2000/svg" viewBox="0 0 24 24">\n	<path fill="currentColor" d="M11 1.5A2.5 2.5 0 0 1 13.5 4v.75c0 .14.11.25.25.25H18a1 1 0 0 1 1 1v3.75q.02.23.25.25H20a2.5 2.5 0 0 1 0 5h-.75a.25.25 0 0 0-.25.25V20a1 1 0 0 1-1 1h-4.25a.25.25 0 0 1-.25-.25V19.5a2.5 2.5 0 0 0-5 0v1.25q-.02.23-.25.25H4a1 1 0 0 1-1-1v-4.25c0-.14.11-.25.25-.25H4.5a2.5 2.5 0 0 0 0-5H3.25a.25.25 0 0 1-.25-.25V6a1 1 0 0 1 1-1h4.25c.14 0 .25-.11.25-.25V4A2.5 2.5 0 0 1 11 1.5"/>\n</svg>'],
        ["question-mark-circle", '<svg xmlns="http://www.w3.org/2000/svg" viewBox="0 0 24 24">\n	<path fill="currentColor" d="M12 15.5a1.25 1.25 0 1 1 0 2.5 1.25 1.25 0 0 1 0-2.5m-.34-9.48a3.5 3.5 0 0 1 2.82 5.95l-1.19 1.2a1 1 0 0 0-.3.7v.65H11v-.64a3 3 0 0 1 .88-2.13l1.19-1.18a1.5 1.5 0 1 0-2.58-1.07H8.5a3.5 3.5 0 0 1 3.16-3.48"/>\n	<path fill="currentColor" d="M12 2a10 10 0 1 1 0 20 10 10 0 0 1 0-20m0 2a8 8 0 1 0 0 16 8 8 0 0 0 0-16"/>\n</svg>'],
        ["radar", '<svg xmlns="http://www.w3.org/2000/svg" viewBox="0 0 24 24">\n	<path fill="currentColor" d="M10.7 2.09a10 10 0 0 1 7.14 1.8L13.74 11q.25.46.26 1a2 2 0 1 1-2-2l1.07-1.85a4 4 0 1 0 2.8 4.84c.14-.54.58-.99 1.13-.99s1 .45.92 1a6 6 0 1 1-3.83-6.62l1.01-1.75A8 8 0 1 0 19.94 13c.07-.55.5-1 1.06-1 .55 0 1 .45.95 1A10 10 0 1 1 10.7 2.09"/>\n</svg>'],
        ["rectangle-stack", '<svg xmlns="http://www.w3.org/2000/svg" viewBox="0 0 24 24">\n	<path fill="currentColor" d="M19 8a3 3 0 0 1 3 3v8a3 3 0 0 1-3 3H5a3 3 0 0 1-3-3v-8a3 3 0 0 1 3-3zM5 10a1 1 0 0 0-1 1v8a1 1 0 0 0 1 1h14a1 1 0 0 0 1-1v-8a1 1 0 0 0-1-1z"/>\n	<path fill="currentColor" d="M18 5a2 2 0 0 1 2 2H4c0-1.1.9-2 2-2zM16 2a2 2 0 0 1 2 2H6c0-1.1.9-2 2-2z"/>\n</svg>'],
        ["scissor", '<svg xmlns="http://www.w3.org/2000/svg" viewBox="0 0 24 24">\n	<path fill="currentColor" d="M2.4 6.14a3 3 0 0 1 5.55 2.05l2.82 1.62 8.75-2.95a2 2 0 0 1 2.44.87L7.95 15.82a3 3 0 1 1-1-1.74L8.83 13a2 2 0 0 1 0-2L6.95 9.92A3 3 0 0 1 2.4 6.14m3.47 9.72a1 1 0 1 0-1.73 1 1 1 0 0 0 1.73-1m4.7-4.61a.75.75 0 1 0 0 1.5.75.75 0 0 0 0-1.5M5.5 6.77a1 1 0 1 0-1 1.74 1 1 0 0 0 1-1.74"/>\n	<path fill="currentColor" d="M21.96 16.27a2 2 0 0 1-2.44.87l-7.5-2.53 3.53-2.04z"/>\n</svg>'],
        ["score-meter", '<svg xmlns="http://www.w3.org/2000/svg" viewBox="0 0 24 24">\n	<path fill="currentColor" d="M5.24 14.81a7 7 0 0 0 1.81 3.14l-2.12 2.12a10 10 0 0 1-2.59-4.48zm13.83-8.88a10 10 0 0 1 0 14.14l-2.12-2.12a7 7 0 0 0 0-9.9zm-3.33 3.75c.44.5.8 1.11 1 1.74l-3.27 1.89a1.5 1.5 0 1 1-1-1.73zM7.05 8.05A7 7 0 0 0 5 13H2a10 10 0 0 1 2.93-7.07zM12 3a10 10 0 0 1 5 1.34l-1.5 2.6a7 7 0 0 0-7 0L7 4.34A10 10 0 0 1 12 3"/>\n</svg>'],
        ["seal-check-mark", '<svg xmlns="http://www.w3.org/2000/svg" viewBox="0 0 24 24">\n	<path fill="currentColor" d="m15.66 7.41-4.7 4.71-2.71-2.7L9.66 8l1.3 1.3L14.25 6z"/>\n	<path fill="currentColor" d="M12 1a8 8 0 0 1 5 14.24V23l-5-2-5 2v-7.76A7.98 7.98 0 0 1 12 1m3 15.42a8 8 0 0 1-6 0v3.62l3-1.2 3 1.2zM12 3a6 6 0 1 0 0 12 6 6 0 0 0 0-12"/>\n</svg>'],
        ["shield", '<svg xmlns="http://www.w3.org/2000/svg" viewBox="0 0 24 24">\n	<path fill="currentColor" d="M20.39 4.83A14 14 0 0 1 12 22 14 14 0 0 1 3.62 4.8L12 2zM5.28 6.35A12 12 0 0 0 12 19.83a12 12 0 0 0 6.73-13.45L12 4.1z"/>\n</svg>'],
        ["shield-check-mark", '<svg xmlns="http://www.w3.org/2000/svg" viewBox="0 0 24 24">\n	<path fill="currentColor" d="m16.41 9.41-5.2 5.21L8 11.42 9.41 10l1.8 1.8L15 8z"/>\n	<path fill="currentColor" d="M20.39 4.83A14 14 0 0 1 12 22 14 14 0 0 1 3.62 4.8L12 2zM5.28 6.35A12 12 0 0 0 12 19.83a12 12 0 0 0 6.73-13.45L12 4.1z"/>\n</svg>'],
        ["shield-lock", '<svg xmlns="http://www.w3.org/2000/svg" viewBox="0 0 24 24">\n	<path fill="currentColor" d="M12 6.5A2.5 2.5 0 0 1 14.5 9v1a1 1 0 0 1 1 1v3a1 1 0 0 1-1 1h-5a1 1 0 0 1-1-1v-3a1 1 0 0 1 1-1V9A2.5 2.5 0 0 1 12 6.5m-.5 7h1v-2h-1zM12 8a1 1 0 0 0-1 1v1h2V9a1 1 0 0 0-1-1"/>\n	<path fill="currentColor" d="M20.39 4.83A14 14 0 0 1 12 22 14 14 0 0 1 3.62 4.8L12 2zM5.28 6.35A12 12 0 0 0 12 19.83a12 12 0 0 0 6.73-13.45L12 4.1z"/>\n</svg>'],
        ["ship-wheel", '<svg xmlns="http://www.w3.org/2000/svg" viewBox="0 0 24 24">\n	<path fill="currentColor" d="M12 2a1 1 0 0 1 1 1v1.063a7.96 7.96 0 0 1 3.903 1.619l.754-.753a1 1 0 0 1 1.414 1.414l-.754.753A7.96 7.96 0 0 1 19.936 11H21a1 1 0 1 1 0 2h-1.064a7.95 7.95 0 0 1-1.619 3.903l.754.754a1 1 0 0 1-1.414 1.414l-.754-.754A7.95 7.95 0 0 1 13 19.936V21a1 1 0 1 1-2 0v-1.064a7.96 7.96 0 0 1-3.904-1.619l-.753.754a1 1 0 0 1-1.414-1.414l.753-.754A7.96 7.96 0 0 1 4.064 13H3a1 1 0 1 1 0-2h1.064a7.96 7.96 0 0 1 1.618-3.904l-.753-.753a1 1 0 0 1 1.414-1.414l.753.753A7.96 7.96 0 0 1 11 4.063V3a1 1 0 0 1 1-1M8.524 16.889A6 6 0 0 0 11 17.915v-3.09a3 3 0 0 1-.291-.12zm4.766-2.185q-.14.068-.29.121v3.09a6 6 0 0 0 2.475-1.026zM6.085 13c.153.911.51 1.752 1.025 2.475l2.185-2.185a3 3 0 0 1-.121-.29zm8.741 0q-.054.15-.122.29l2.185 2.185A6 6 0 0 0 17.915 13zM12 10.5a1.5 1.5 0 1 0 0 3 1.5 1.5 0 0 0 0-3M7.11 8.524A6 6 0 0 0 6.085 11h3.089q.053-.15.12-.291zm7.594 2.185q.069.142.122.291h3.089a6 6 0 0 0-1.026-2.476zM11 6.084A6 6 0 0 0 8.524 7.11l2.185 2.185A3 3 0 0 1 11 9.174zm2 3.09q.15.053.29.12l2.185-2.184A6 6 0 0 0 13 6.084z"/>\n</svg>'],
        ["shopping-cart", '<svg xmlns="http://www.w3.org/2000/svg" viewBox="0 0 24 24">\n	<path fill="currentColor" d="M7 18a2 2 0 1 1 0 4 2 2 0 0 1 0-4m10 0a2 2 0 1 1 0 4 2 2 0 0 1 0-4"/>\n	<path fill="currentColor" d="M3.4 2a3 3 0 0 1 2.82 2h13.04a2 2 0 0 1 1.96 2.42l-.9 4.2A3 3 0 0 1 17.38 13h-9.8l-.16.4-.8 1.6H19v2H6.62a2 2 0 0 1-1.8-2.9l.8-1.6a2 2 0 0 0 .17-1.32l-1.42-6.4A1 1 0 0 0 3.4 4H2V2zm4.35 8.74.04.26h9.6a1 1 0 0 0 .97-.79l.9-4.21H6.7z"/>\n</svg>'],
        ["slash-circle", '<svg xmlns="http://www.w3.org/2000/svg" viewBox="0 0 24 24">\n	<path fill="currentColor" d="M12 2a10 10 0 1 1 0 20 10 10 0 0 1 0-20M5.68 7.1A8 8 0 0 0 16.9 18.32zM12 4c-1.85 0-3.55.63-4.9 1.68L18.32 16.9A8 8 0 0 0 12 4"/>\n</svg>'],
        ["sparkles", '<svg xmlns="http://www.w3.org/2000/svg" viewBox="0 0 24 24">\n	<path fill="currentColor" d="M15.4 10.16c.1-.53.86-.53.97 0l.8 3.67a1 1 0 0 0 .76.77l3.68.8c.52.1.52.86 0 .97l-3.68.79a1 1 0 0 0-.77.77l-.79 3.67c-.11.53-.86.53-.97 0l-.8-3.67a1 1 0 0 0-.76-.77l-3.68-.8c-.64-.22-.64-.98 0-.97l3.68-.79a1 1 0 0 0 .77-.77zM6.1 4.82c.1-.42.7-.42.79 0l.56 2.61c.06.31.3.55.61.62l2.61.56c.42.09.42.69 0 .78l-2.61.56a.8.8 0 0 0-.61.62l-.56 2.6c-.1.43-.7.43-.79 0l-.56-2.6a.8.8 0 0 0-.61-.62L2.32 9.4c-.43-.09-.43-.7 0-.78l2.6-.56a.8.8 0 0 0 .62-.62zM14.2 2.24c.07-.32.52-.32.59 0l.33 1.54a.6.6 0 0 0 .46.46l1.55.34c.31.06.31.52 0 .58l-1.55.34a.6.6 0 0 0-.46.46l-.33 1.54c-.07.32-.52.32-.59 0l-.33-1.54a.6.6 0 0 0-.46-.46l-1.55-.34c-.32-.06-.32-.52 0-.58l1.55-.34a.6.6 0 0 0 .46-.46z"/>\n</svg>'],
        ["square-and-arrow-down", '<svg xmlns="http://www.w3.org/2000/svg" viewBox="0 0 24 24">\n	<path fill="currentColor" d="M9 8H7a1 1 0 0 0-1 1v9a1 1 0 0 0 1 1h10a1 1 0 0 0 1-1V9a1 1 0 0 0-1-1h-2V6h2a3 3 0 0 1 3 3v9a3 3 0 0 1-3 3H7a3 3 0 0 1-3-3V9a3 3 0 0 1 3-3h2z"/>\n	<path fill="currentColor" d="M16.59 12.41 15.17 11 13 13.17V2h-2v11.17L8.83 11l-1.42 1.41L12 17z"/>\n</svg>'],
        ["square-arrow-right-top", '<svg xmlns="http://www.w3.org/2000/svg" viewBox="0 0 24 24">\n	<path fill="currentColor" d="M11.772 8H5a1 1 0 0 0-1 1v10a1 1 0 0 0 1 1h10a1 1 0 0 0 1-1v-6.765l2-2V19c0 1.66-1.34 3-3 3H5c-1.66 0-3-1.34-3-3V9a3 3 0 0 1 3-3h8.772z"/>\n	<path fill="currentColor" d="M22 11h-2V5.407l-9.296 9.296L9.3 13.3 18.6 4H13V2h9z"/>\n</svg>'],
        ["square-arrow-up", '<svg xmlns="http://www.w3.org/2000/svg" viewBox="0 0 24 24">\n	<path fill="currentColor" d="M9 11H7a1 1 0 0 0-1 1v7a1 1 0 0 0 1 1h10a1 1 0 0 0 1-1v-7a1 1 0 0 0-1-1h-2V9h2a3 3 0 0 1 3 3v7a3 3 0 0 1-3 3H7c-1.66 0-3-1.34-3-3v-7a3 3 0 0 1 3-3h2z"/>\n	<path fill="currentColor" d="M16.59 5.59 15.17 7 13 4.828V16h-2V4.828L8.83 7 7.41 5.59 12 1z"/>\n</svg>'],
        ["square-on-square", '<svg xmlns="http://www.w3.org/2000/svg" viewBox="0 0 24 24">\n	<path fill="currentColor" d="M18 5v1h1c1.66 0 3 1.34 3 3v10c0 1.658-1.308 3-3 3H9c-1.66 0-3-1.34-3-3v-1H5c-1.66 0-3-1.34-3-3V5a3 3 0 0 1 3-3h10c1.66 0 3 1.34 3 3M9 8a1 1 0 0 0-1 1v10a1 1 0 0 0 1 1h10a1 1 0 0 0 1-1V9a1 1 0 0 0-1-1zM5 4a1 1 0 0 0-1 1v10a1 1 0 0 0 1 1h1V9a3 3 0 0 1 3-3h7V5a1 1 0 0 0-1-1z"/>\n</svg>'],
        ["square-plus-on-square", '<svg xmlns="http://www.w3.org/2000/svg" viewBox="0 0 24 24">\n	<path fill="currentColor" d="M15 13h3v2h-3v3h-2v-3h-3v-2h3v-3h2z"/>\n	<path fill="currentColor" d="M18 5v1h1c1.66 0 3 1.34 3 3v10c0 1.658-1.308 3-3 3H9c-1.66 0-3-1.34-3-3v-1H5c-1.66 0-3-1.34-3-3V5a3 3 0 0 1 3-3h10c1.66 0 3 1.34 3 3M9 8a1 1 0 0 0-1 1v10a1 1 0 0 0 1 1h10a1 1 0 0 0 1-1V9a1 1 0 0 0-1-1zM5 4a1 1 0 0 0-1 1v10a1 1 0 0 0 1 1h1V9a3 3 0 0 1 3-3h7V5a1 1 0 0 0-1-1z"/>\n</svg>'],
        ["stack-code", '<svg xmlns="http://www.w3.org/2000/svg" viewBox="0 0 24 24">\n	<path fill="currentColor" d="M19 8a3 3 0 0 1 3 3v8a3 3 0 0 1-3 3H5a3 3 0 0 1-3-3v-8a3 3 0 0 1 3-3zM5 10a1 1 0 0 0-1 1v8a1 1 0 0 0 1 1h14a1 1 0 0 0 1-1v-8a1 1 0 0 0-1-1zm8.87 1.2-2.15 8-1.44-.4 2.14-8zm-3.84 1.83L8.06 15l1.97 1.97-1.06 1.06L5.94 15l3.03-3.03zM18.06 15l-3.03 3.03-1.06-1.06L15.94 15l-1.97-1.97 1.06-1.06zM18 5a2 2 0 0 1 2 2H4c0-1.1.9-2 2-2zm-2-3a2 2 0 0 1 2 2H6c0-1.1.9-2 2-2z"/>\n</svg>'],
        ["star", '<svg xmlns="http://www.w3.org/2000/svg" viewBox="0 0 24 24">\n	<path fill="currentColor" d="M11.55 1.8a.5.5 0 0 1 .9 0l2.56 6.14a.5.5 0 0 0 .41.3l6.63.53a.5.5 0 0 1 .28.87l-5.05 4.32a.5.5 0 0 0-.16.49l1.54 6.46c.1.4-.3.72-.66.57l-5.74-3.5a.5.5 0 0 0-.52 0L6 21.48a.5.5 0 0 1-.66-.57l1.54-6.46a.5.5 0 0 0-.16-.49L1.67 9.64a.5.5 0 0 1 .28-.87l6.63-.53a.5.5 0 0 0 .41-.3zm-.71 6.9a2.5 2.5 0 0 1-2.1 1.54l-3.01.24 2.3 1.96a2.5 2.5 0 0 1 .8 2.47l-.7 2.94 2.72-1.66a2.5 2.5 0 0 1 2.45.09l2.58 1.57-.7-2.94c-.22-.9.09-1.86.8-2.47l2.3-1.96-3.02-.24a2.5 2.5 0 0 1-2.1-1.53L12 5.9z"/>\n</svg>'],
        ["star-filled", '<svg xmlns="http://www.w3.org/2000/svg" viewBox="0 0 24 24">\n	<path fill="currentColor" d="M11.55 1.8a.5.5 0 0 1 .9 0l2.56 6.14a.5.5 0 0 0 .41.3l6.63.53a.5.5 0 0 1 .28.87l-5.05 4.32a.5.5 0 0 0-.16.49l1.54 6.46a.5.5 0 0 1-.73.54l-5.67-3.47a.5.5 0 0 0-.52 0l-5.67 3.47a.5.5 0 0 1-.73-.54l1.54-6.46a.5.5 0 0 0-.16-.49L1.67 9.64a.5.5 0 0 1 .28-.87l6.63-.53a.5.5 0 0 0 .41-.3z"/>\n</svg>'],
        ["starburst-filled", '<svg xmlns="http://www.w3.org/2000/svg" viewBox="0 0 24 24">\n	<path fill="currentColor" d="M11.17 1.48a.95.95 0 0 1 1.66 0l.87 1.55c.26.47.85.63 1.31.36l1.53-.91a.95.95 0 0 1 1.44.83l-.03 1.77c0 .54.43.97.97.97l1.77-.03c.75 0 1.21.8.83 1.44l-.9 1.53a.95.95 0 0 0 .35 1.31l1.55.87a.95.95 0 0 1 0 1.66l-1.55.87A.95.95 0 0 0 20.6 15l.91 1.53a.95.95 0 0 1-.83 1.44l-1.77-.03a.95.95 0 0 0-.97.97l.03 1.77a.95.95 0 0 1-1.44.83l-1.53-.9a.95.95 0 0 0-1.31.35l-.87 1.55a.95.95 0 0 1-1.66 0l-.87-1.55A.95.95 0 0 0 9 20.6l-1.53.91a.95.95 0 0 1-1.44-.83l.03-1.77a.95.95 0 0 0-.97-.97l-1.77.03a.95.95 0 0 1-.83-1.44l.9-1.53a.95.95 0 0 0-.35-1.31l-1.55-.87a.95.95 0 0 1 0-1.66l1.55-.87A.95.95 0 0 0 3.4 9l-.91-1.53a.95.95 0 0 1 .83-1.44l1.77.03c.54 0 .97-.43.97-.97l-.03-1.77a.95.95 0 0 1 1.44-.83l1.53.9a.95.95 0 0 0 1.31-.35z"/>\n</svg>'],
        ["sun", '<svg xmlns="http://www.w3.org/2000/svg" viewBox="0 0 24 24">\n	<path fill="currentColor" d="M13 22h-2v-3.07c.65.09 1.35.09 2 0zM6.4 16.2q.6.8 1.4 1.4l-2.16 2.18-1.42-1.42zM19.78 18.36l-1.42 1.42-2.17-2.17q.8-.6 1.42-1.42z"/>\n	<path fill="currentColor" d="M12 7a5 5 0 1 1 0 10 5 5 0 0 1 0-10m0 2a3 3 0 1 0 0 6 3 3 0 0 0 0-6"/>\n	<path fill="currentColor" d="M5.07 11c-.1.65-.1 1.35 0 2H2v-2zM22 13h-3.07c.1-.65.1-1.35 0-2H22zM7.8 6.4Q7 7 6.4 7.8L4.21 5.65l1.42-1.42zM19.78 5.64 17.6 7.8a7 7 0 0 0-1.42-1.42l2.17-2.17zM13 5.07c-.65-.1-1.35-.1-2 0V2h2z"/>\n</svg>'],
        ["table-cells", '<svg xmlns="http://www.w3.org/2000/svg" viewBox="0 0 24 24">\n	<path fill="currentColor" d="M19 3.5a3 3 0 0 1 3 3v11a3 3 0 0 1-2.85 3H4.85A3 3 0 0 1 2 17.65V6.5a3 3 0 0 1 3-3zm-15 12v2a1 1 0 0 0 1 1h6v-3zm9 0v3h6a1 1 0 0 0 1-1v-2zm-9-2h7v-3H4zm9 0h7v-3h-7zm-8-8a1 1 0 0 0-1 1v2h7v-3zm8 3h7v-2a1 1 0 0 0-1-1h-6z"/>\n</svg>'],
        ["table-cells-badge-arrow-down", '<svg xmlns="http://www.w3.org/2000/svg" viewBox="0 0 24 24">\n	<path fill="currentColor" d="M18 13a5 5 0 1 1 0 10 5 5 0 0 1 0-10m-.75 5.38-1.49-1.49-1.06 1.06 3.3 3.3 3.3-3.3-1.06-1.06-1.49 1.49V15h-1.5z"/>\n	<path fill="currentColor" d="M19 2.5a3 3 0 0 1 3 3v8.03a6 6 0 0 0-2-1.19V9.5h-7v3h2.6a6 6 0 0 0-2.47 2H13v.18a6 6 0 0 0-.81 4.82H4.85A3 3 0 0 1 2 16.65V5.5a3 3 0 0 1 3-3zm-15 14a1 1 0 0 0 1 1h6v-3H4zm0-4h7v-3H4zm1-8a1 1 0 0 0-1 1v2h7v-3zm8 3h7v-2a1 1 0 0 0-1-1h-6z"/>\n</svg>'],
        ["tag", '<svg xmlns="http://www.w3.org/2000/svg" viewBox="0 0 24 24">\n	<path fill="currentColor" d="M21.24 5.2a3 3 0 0 1 .74 2.34l-.43 3.88a3 3 0 0 1-.86 1.79l-7.92 7.92a3.04 3.04 0 0 1-4.13.1l-5.88-5.87a3.04 3.04 0 0 1 .1-4.13L10.8 3.3a3 3 0 0 1 1.79-.86l3.88-.43a3 3 0 0 1 2.33.74l.13.12 2.2 2.2zm-3.74-.9a1 1 0 0 0-.82-.3l-3.88.44a1 1 0 0 0-.6.28l-7.92 7.92a1 1 0 0 0 0 1.42l5.66 5.65a1 1 0 0 0 1.41 0l7.92-7.92a1 1 0 0 0 .29-.6l.43-3.88a1 1 0 0 0-.29-.81zm-1.02 3.22a1.5 1.5 0 1 1-2.12 2.12 1.5 1.5 0 0 1 2.12-2.12"/>\n</svg>'],
        ["terminal", '<svg xmlns="http://www.w3.org/2000/svg" viewBox="0 0 24 24">\n	<path fill="currentColor" d="m11.4 12-4 4L6 14.6 8.57 12 6 9.4 7.4 8zM18 16h-6v-2h6z"/>\n	<path fill="currentColor" d="M19 4a3 3 0 0 1 3 3v10a3 3 0 0 1-3 3H5a3 3 0 0 1-3-3V7a3 3 0 0 1 3-3zM5 6a1 1 0 0 0-1 1v10a1 1 0 0 0 1 1h14a1 1 0 0 0 1-1V7a1 1 0 0 0-1-1z"/>\n</svg>'],
        ["text-quote", '<svg xmlns="http://www.w3.org/2000/svg" viewBox="0 0 24 24">\n	<path fill="currentColor" d="M17 20H3v-2h14zM21 15.5H3v-2h18zM7.22 6.1a3 3 0 0 0-1.55 1.02A2 2 0 1 1 3 9a5 5 0 0 1 3.7-4.83zM12.22 6.1a3 3 0 0 0-1.55 1.02A2 2 0 1 1 8 9a5 5 0 0 1 3.7-4.83zM21 11h-6.5V9H21zM21 6.5h-6.5v-2H21z"/>\n</svg>'],
        ["timer", '<svg xmlns="http://www.w3.org/2000/svg" viewBox="0 0 24 24">\n	<path fill="currentColor" d="M12 2a10 10 0 1 1-8.66 5l1.73 1A8 8 0 1 0 13 4.06V7h-2V2z"/>\n	<path fill="currentColor" d="M12.5 11.13a1 1 0 0 1-1 1.74l-5.2-3 1-1.74z"/>\n</svg>'],
        ["trash", '<svg xmlns="http://www.w3.org/2000/svg" viewBox="0 0 24 24">\n	<path fill="currentColor" fill-rule="evenodd" d="M8 4a2 2 0 0 1 2-2h4a2 2 0 0 1 2 2v2h5v2h-1.08L19 19.046A3 3 0 0 1 16 22H8a3 3 0 0 1-3-2.954L4.08 8H3V6h5zM6.087 8l.91 10.917L7 19a1 1 0 0 0 1 1h8a1 1 0 0 0 1-1l.003-.083L17.914 8zM14 6h-4V4h4zm-3 4v8H9v-8zm4 0v8h-2v-8z"/>\n</svg>'],
        ["underlined", '<svg xmlns="http://www.w3.org/2000/svg" viewBox="0 0 24 24">\n	<path fill="currentColor" d="M20 22H4v-2h16zM8 13c0 1.33 1.43 3 4 3s4-1.67 4-3V2h2v11c0 2.76-2.69 5-6 5s-6-2.24-6-5V2h2z"/>\n</svg>']
      ]);
    }
  });

  // node_modules/@nldd/design-system/dist/components/content/icon/icon.js
  var import_meta, __decorate, ICONS, NLDDIcon;
  var init_icon = __esm({
    "node_modules/@nldd/design-system/dist/components/content/icon/icon.js"() {
      init_lit();
      init_decorators();
      init_icon_styles();
      init_icon_template();
      init_icon_aliases();
      init_icon_registry();
      import_meta = {};
      __decorate = function(decorators, target, key, desc) {
        var c6 = arguments.length, r6 = c6 < 3 ? target : desc === null ? desc = Object.getOwnPropertyDescriptor(target, key) : desc, d3;
        if (typeof Reflect === "object" && typeof Reflect.decorate === "function") r6 = Reflect.decorate(decorators, target, key, desc);
        else for (var i8 = decorators.length - 1; i8 >= 0; i8--) if (d3 = decorators[i8]) r6 = (c6 < 3 ? d3(r6) : c6 > 3 ? d3(target, key, r6) : d3(target, key)) || r6;
        return c6 > 3 && r6 && Object.defineProperty(target, key, r6), r6;
      };
      ICONS = [
        ...iconRegistry.keys(),
        ...Object.keys(aliases)
      ].sort();
      NLDDIcon = class NLDDIcon2 extends i4 {
        constructor() {
          super(...arguments);
          this.name = "circle-dashed";
          this.size = "";
          this.color = "";
          this._iconSvg = null;
        }
        connectedCallback() {
          super.connectedCallback();
          if (!this.hasAttribute("aria-hidden")) {
            this.setAttribute("aria-hidden", "true");
          }
          this._iconSvg = this._loadIcon(this.name);
        }
        updated(changedProperties) {
          if (changedProperties.has("name") && this.name) {
            this._iconSvg = this._loadIcon(this.name);
          }
        }
        _loadIcon(name) {
          const resolvedName = aliases[name] ?? name;
          const svg = iconRegistry.get(resolvedName);
          if (svg) {
            return svg;
          }
          if (import_meta.env?.DEV)
            console.warn(`NLDDIcon: icon "${resolvedName}" not found`);
          return null;
        }
        render() {
          return template2(this._iconSvg);
        }
      };
      NLDDIcon.styles = iconStyles;
      __decorate([
        n4({ type: String })
      ], NLDDIcon.prototype, "name", void 0);
      __decorate([
        n4({ type: String, reflect: true })
      ], NLDDIcon.prototype, "size", void 0);
      __decorate([
        n4({ type: String, reflect: true })
      ], NLDDIcon.prototype, "color", void 0);
      __decorate([
        r5()
      ], NLDDIcon.prototype, "_iconSvg", void 0);
      NLDDIcon = __decorate([
        t3("nldd-icon")
      ], NLDDIcon);
    }
  });

  // node_modules/@nldd/design-system/dist/components/status-and-feedback/activity-indicator/activity-indicator.styles.js
  var activityIndicatorStyles;
  var init_activity_indicator_styles = __esm({
    "node_modules/@nldd/design-system/dist/components/status-and-feedback/activity-indicator/activity-indicator.styles.js"() {
      init_lit();
      init_slotted_reset();
      activityIndicatorStyles = i`


	/* # Host */

	:host {
		--_circle-size: var(--primitives-space-32);
		--_track-opacity: 0.25;
		--_stroke-width: 2;
		--_rotation-duration: 0.8s;
		--_fade-duration: var(--primitives-transition-duration-slow);
		--_fade-easing: ease-out;
		--_pulse-duration: 2s;
		--_pulse-easing: ease-in-out;
		--_max-width: var(--primitives-area-240);
		--_gap: var(--primitives-space-4);
		--_text-font: var(--primitives-font-body-sm-regular-flat);

		${inheritedTextReset}
		box-sizing: border-box;
		display: flex;
		flex-grow: 1;
		flex-shrink: 1;
		flex-basis: auto;
		width: 100%;
		height: 100%;
		align-items: center;
		justify-content: center;
	}

	:host([hidden]) {
		display: none;
	}


	/* ## Sizes (icon scale) */

	:host([size="16"]) { --_circle-size: var(--primitives-space-16); }
	:host([size="20"]) { --_circle-size: var(--primitives-space-20); }
	:host([size="24"]) { --_circle-size: var(--primitives-space-24); }
	:host([size="28"]) { --_circle-size: var(--primitives-space-28); }
	:host([size="32"]) { --_circle-size: var(--primitives-space-32); }
	:host([size="40"]) { --_circle-size: var(--primitives-space-40); }
	:host([size="44"]) { --_circle-size: var(--primitives-space-44); }
	:host([size="48"]) { --_circle-size: var(--primitives-space-48); }
	:host([size="56"]) { --_circle-size: var(--primitives-space-56); }
	:host([size="64"]) { --_circle-size: var(--primitives-space-64); }
	:host([size="80"]) { --_circle-size: var(--primitives-space-80); }
	:host([size="96"]) { --_circle-size: var(--primitives-space-96); }


	/* # Block
	   Fades the indicator in once it becomes visible (both timing modes), and
	   stacks the default circle + label as a centred column. width:100% +
	   max-width gives a slotted progress-bar a width to fill; the fixed-size
	   circle and the label stay centred via align-items. */

	.activity-indicator {
		display: flex;
		flex-direction: column;
		width: 100%;
		max-width: var(--_max-width);
		align-items: center;
		gap: var(--_gap);
		animation: activity-indicator-fade-in var(--_fade-duration) var(--_fade-easing);
	}

	@keyframes activity-indicator-fade-in {
		from { opacity: 0; }
	}

	/* display:contents so the default circle + label (or a slotted override)
	   are direct flex items of the block. */
	slot {
		display: contents;
	}


	/* # Elements */

	/* The whole SVG (stroke included) scales with the size, like an icon. */
	.activity-indicator__circle {
		display: block;
		width: var(--_circle-size);
		height: var(--_circle-size);
	}

	.activity-indicator__track {
		opacity: var(--_track-opacity);
		stroke: currentColor;
		stroke-width: var(--_stroke-width);
	}

	/* Rotate only the arc inside the SVG (around the view-box centre via
	   transform-origin) rather than the whole <svg> element — rotating the
	   element visibly wobbles when it sits at a sub-pixel position (next to a
	   label, or overlaid on a button). Mirrors nldd-progress-circle. */
	.activity-indicator__indicator {
		stroke: currentColor;
		stroke-width: var(--_stroke-width);
		stroke-linecap: round;
		stroke-dasharray: 25 100;
		transform-origin: 50% 50%;
		animation: activity-indicator-rotate var(--_rotation-duration) linear infinite;
	}

	@keyframes activity-indicator-rotate {
		to { transform: rotate(360deg); }
	}

	.activity-indicator__text {
		color: currentColor;
		font: var(--_text-font);
		text-align: center;
	}

	/* show-text off (default): the label still renders as the announced
	   content of the role="status" host, but is visually hidden.
	   Standard visually-hidden recipe. */
	.activity-indicator__text--visually-hidden {
		position: absolute;
		width: 1px;
		height: 1px;
		overflow: hidden;
		clip-path: inset(50%);
		white-space: nowrap;
	}


	/* # Accessibility */

	@media (prefers-reduced-motion: reduce) {
		.activity-indicator {
			animation: none;
		}

		.activity-indicator__indicator {
			/* Drop the rotation (vestibular safety); keep the 25 / 100 arc and
			   pulse its opacity instead, mirroring nldd-progress-circle. */
			animation: activity-indicator-pulse var(--_pulse-duration) var(--_pulse-easing) infinite;
		}

		@keyframes activity-indicator-pulse {
			0%, 100% { opacity: 0.3; }
			50% { opacity: 0.7; }
		}
	}
`;
    }
  });

  // node_modules/lit-html/directives/class-map.js
  var e8;
  var init_class_map = __esm({
    "node_modules/lit-html/directives/class-map.js"() {
      init_lit_html();
      init_directive();
      e8 = e6(class extends i5 {
        constructor(t6) {
          if (super(t6), t6.type !== t4.ATTRIBUTE || "class" !== t6.name || t6.strings?.length > 2) throw Error("`classMap()` can only be used in the `class` attribute and must be the only part in the attribute.");
        }
        render(t6) {
          return " " + Object.keys(t6).filter((s6) => t6[s6]).join(" ") + " ";
        }
        update(s6, [i8]) {
          if (void 0 === this.st) {
            this.st = /* @__PURE__ */ new Set(), void 0 !== s6.strings && (this.nt = new Set(s6.strings.join(" ").split(/\s/).filter((t6) => "" !== t6)));
            for (const t6 in i8) i8[t6] && !this.nt?.has(t6) && this.st.add(t6);
            return this.render(i8);
          }
          const r6 = s6.element.classList;
          for (const t6 of this.st) t6 in i8 || (r6.remove(t6), this.st.delete(t6));
          for (const t6 in i8) {
            const s7 = !!i8[t6];
            s7 === this.st.has(t6) || this.nt?.has(t6) || (s7 ? (r6.add(t6), this.st.add(t6)) : (r6.remove(t6), this.st.delete(t6)));
          }
          return E;
        }
      });
    }
  });

  // node_modules/lit/directives/class-map.js
  var init_class_map2 = __esm({
    "node_modules/lit/directives/class-map.js"() {
      init_class_map();
    }
  });

  // node_modules/@nldd/design-system/dist/components/status-and-feedback/activity-indicator/activity-indicator.template.js
  function activityIndicatorTemplate(component) {
    if (!component._visible || component.complete)
      return A;
    const text = component._accessibleName;
    return b2`
		<div class="activity-indicator">
			<slot>
				<svg class="activity-indicator__circle"
					viewBox="0 0 24 24"
					fill="none"
					aria-hidden="true"
					focusable="false"
				>
					<circle class="activity-indicator__track"
						cx="12"
						cy="12"
						r="9"
					></circle>
					<circle class="activity-indicator__indicator"
						cx="12"
						cy="12"
						r="9"
						pathLength="100"
					></circle>
				</svg>
				<span class=${e8({
      "activity-indicator__text": true,
      "activity-indicator__text--visually-hidden": !component.showText
    })}>${text}</span>
			</slot>
		</div>
	`;
  }
  var init_activity_indicator_template = __esm({
    "node_modules/@nldd/design-system/dist/components/status-and-feedback/activity-indicator/activity-indicator.template.js"() {
      init_lit();
      init_class_map2();
    }
  });

  // node_modules/@nldd/design-system/dist/components/status-and-feedback/activity-indicator/activity-indicator.i18n.js
  var nlddActivityIndicatorTranslations;
  var init_activity_indicator_i18n = __esm({
    "node_modules/@nldd/design-system/dist/components/status-and-feedback/activity-indicator/activity-indicator.i18n.js"() {
      nlddActivityIndicatorTranslations = {
        "components.activity-indicator.loading-text": "Laden"
      };
    }
  });

  // node_modules/@nldd/design-system/dist/components/status-and-feedback/activity-indicator/activity-indicator.js
  var __decorate2, DELAY_MS, NLDDActivityIndicator;
  var init_activity_indicator = __esm({
    "node_modules/@nldd/design-system/dist/components/status-and-feedback/activity-indicator/activity-indicator.js"() {
      init_lit();
      init_decorators();
      init_activity_indicator_styles();
      init_activity_indicator_template();
      init_activity_indicator_i18n();
      __decorate2 = function(decorators, target, key, desc) {
        var c6 = arguments.length, r6 = c6 < 3 ? target : desc === null ? desc = Object.getOwnPropertyDescriptor(target, key) : desc, d3;
        if (typeof Reflect === "object" && typeof Reflect.decorate === "function") r6 = Reflect.decorate(decorators, target, key, desc);
        else for (var i8 = decorators.length - 1; i8 >= 0; i8--) if (d3 = decorators[i8]) r6 = (c6 < 3 ? d3(r6) : c6 > 3 ? d3(target, key, r6) : d3(target, key)) || r6;
        return c6 > 3 && r6 && Object.defineProperty(target, key, r6), r6;
      };
      DELAY_MS = 1e3;
      NLDDActivityIndicator = class NLDDActivityIndicator2 extends i4 {
        constructor() {
          super(...arguments);
          this.size = "32";
          this.showText = false;
          this.text = "";
          this.timing = "default";
          this.complete = false;
          this.translations = {};
          this._visible = false;
        }
        _t(key) {
          return this.translations[key] ?? nlddActivityIndicatorTranslations[key];
        }
        /** Accessible name announced while loading. */
        get _accessibleName() {
          return this.text || this._t("components.activity-indicator.loading-text");
        }
        connectedCallback() {
          super.connectedCallback();
          this._syncAria();
          this._visible = this.timing === "instant";
          if (!this._visible && !this.complete)
            this._scheduleDelay();
        }
        willUpdate(changed) {
          if (changed.has("complete")) {
            this._syncAria();
          }
          if (changed.has("timing")) {
            this._clearDelay();
            if (this.timing === "instant") {
              this._visible = true;
            } else if (!this._visible && !this.complete) {
              this._scheduleDelay();
            }
          }
        }
        disconnectedCallback() {
          super.disconnectedCallback();
          this._clearDelay();
        }
        _clearDelay() {
          if (this._delayTimeout) {
            clearTimeout(this._delayTimeout);
            this._delayTimeout = void 0;
          }
        }
        _scheduleDelay() {
          this._delayTimeout = setTimeout(() => {
            this._visible = true;
            this._delayTimeout = void 0;
          }, DELAY_MS);
        }
        /** A polite live region (role="status") while loading; cleared when
         *  complete. The announced text is the visually-hidden label rendered by
         *  the template — not an aria-label, and without aria-busy, since a busy
         *  live region would defer the very announcement we want. */
        _syncAria() {
          if (this.complete) {
            this.removeAttribute("role");
          } else {
            this.setAttribute("role", "status");
          }
        }
        render() {
          return activityIndicatorTemplate(this);
        }
      };
      NLDDActivityIndicator.styles = activityIndicatorStyles;
      __decorate2([
        n4({ type: String, reflect: true })
      ], NLDDActivityIndicator.prototype, "size", void 0);
      __decorate2([
        n4({ type: Boolean, reflect: true, attribute: "show-text" })
      ], NLDDActivityIndicator.prototype, "showText", void 0);
      __decorate2([
        n4({ type: String, reflect: true })
      ], NLDDActivityIndicator.prototype, "text", void 0);
      __decorate2([
        n4({ type: String, reflect: true })
      ], NLDDActivityIndicator.prototype, "timing", void 0);
      __decorate2([
        n4({ type: Boolean, reflect: true })
      ], NLDDActivityIndicator.prototype, "complete", void 0);
      __decorate2([
        n4({ type: Object })
      ], NLDDActivityIndicator.prototype, "translations", void 0);
      __decorate2([
        r5()
      ], NLDDActivityIndicator.prototype, "_visible", void 0);
      NLDDActivityIndicator = __decorate2([
        t3("nldd-activity-indicator")
      ], NLDDActivityIndicator);
    }
  });

  // node_modules/@nldd/design-system/dist/components/actions/button/button.js
  var import_meta2, __decorate3, NLDDButton;
  var init_button = __esm({
    "node_modules/@nldd/design-system/dist/components/actions/button/button.js"() {
      init_lit();
      init_decorators();
      init_button_styles();
      init_button_template();
      init_icon();
      init_activity_indicator();
      import_meta2 = {};
      __decorate3 = function(decorators, target, key, desc) {
        var c6 = arguments.length, r6 = c6 < 3 ? target : desc === null ? desc = Object.getOwnPropertyDescriptor(target, key) : desc, d3;
        if (typeof Reflect === "object" && typeof Reflect.decorate === "function") r6 = Reflect.decorate(decorators, target, key, desc);
        else for (var i8 = decorators.length - 1; i8 >= 0; i8--) if (d3 = decorators[i8]) r6 = (c6 < 3 ? d3(r6) : c6 > 3 ? d3(target, key, r6) : d3(target, key)) || r6;
        return c6 > 3 && r6 && Object.defineProperty(target, key, r6), r6;
      };
      NLDDButton = class NLDDButton2 extends i4 {
        constructor() {
          super(...arguments);
          this._internals = this.attachInternals();
          this.variant = "neutral-tinted";
          this.size = "md";
          this.horizontalAlignment = "";
          this.width = "";
          this.expandable = false;
          this.expanded = false;
          this.type = "button";
          this.popovertarget = void 0;
          this.popoverTargetElement = null;
          this.popoverTargetAction = "toggle";
          this.loading = false;
          this.disabled = false;
          this.text = "";
          this.supportingText = "";
          this.singleLine = false;
          this.noHighlightBorder = false;
          this.startIcon = "";
          this.endIcon = "";
          this.accessibleLabel = "";
          this.href = void 0;
          this.target = void 0;
          this.rel = void 0;
          this._warnedA11y = false;
        }
        updated(changedProperties) {
          if (changedProperties.has("width")) {
            const w2 = this.width;
            const isFull = w2 === "full";
            const isValidLength = !!w2 && !isFull && CSS.supports("width", w2);
            this.style.width = isValidLength ? w2 : "";
            if (isFull || isValidLength) {
              this.style.setProperty("--_width", "100%");
            } else {
              this.style.removeProperty("--_width");
            }
          }
          const isEmpty = !this.text && !this.accessibleLabel;
          if (import_meta2.env?.DEV && isEmpty && !this._warnedA11y) {
            this._warnedA11y = true;
            console.warn(this.supportingText ? "<nldd-button>: supporting-text is set but text is empty, so the accessible name omits the main label. Set the text attribute (or accessible-label) so screen readers announce it." : "<nldd-button>: button has no text or accessible-label. This produces an inaccessible button (WCAG SC 4.1.2).");
          } else if (!isEmpty) {
            this._warnedA11y = false;
          }
        }
        _handleClick(e9) {
          if (this.disabled || this.loading) {
            e9.preventDefault();
            e9.stopPropagation();
            return;
          }
          if (this.href)
            return;
          if (this.type === "submit")
            this._internals.form?.requestSubmit();
          else if (this.type === "reset")
            this._internals.form?.reset();
        }
        /** Resolves the effective rel value for link rendering. */
        _resolvedRel() {
          if (this.rel)
            return this.rel;
          if (this.target === "_blank")
            return "noopener noreferrer";
          return "";
        }
        /**
         * Delegates focus to the inner `<button>` (or `<a>` when `href` is set), so
         * consumers can call `buttonEl.focus()` without reaching into shadow DOM.
         */
        focus(options) {
          this.shadowRoot?.querySelector(".button")?.focus(options);
        }
        render() {
          return template.call(this, {
            handleClick: this._handleClick.bind(this)
          });
        }
      };
      NLDDButton.styles = buttonStyles;
      NLDDButton.formAssociated = true;
      __decorate3([
        n4({ type: String, reflect: true })
      ], NLDDButton.prototype, "variant", void 0);
      __decorate3([
        n4({ type: String, reflect: true })
      ], NLDDButton.prototype, "size", void 0);
      __decorate3([
        n4({ type: String, reflect: true, attribute: "horizontal-alignment" })
      ], NLDDButton.prototype, "horizontalAlignment", void 0);
      __decorate3([
        n4({ type: String, reflect: true })
      ], NLDDButton.prototype, "width", void 0);
      __decorate3([
        n4({ type: Boolean, reflect: true, attribute: "expandable" })
      ], NLDDButton.prototype, "expandable", void 0);
      __decorate3([
        n4({ type: Boolean, reflect: true })
      ], NLDDButton.prototype, "expanded", void 0);
      __decorate3([
        n4({ type: String, reflect: true, attribute: "popup-type" })
      ], NLDDButton.prototype, "popupType", void 0);
      __decorate3([
        n4({ type: String, reflect: true })
      ], NLDDButton.prototype, "type", void 0);
      __decorate3([
        n4({ type: String })
      ], NLDDButton.prototype, "popovertarget", void 0);
      __decorate3([
        n4({ attribute: false })
      ], NLDDButton.prototype, "popoverTargetElement", void 0);
      __decorate3([
        n4({ attribute: false })
      ], NLDDButton.prototype, "popoverTargetAction", void 0);
      __decorate3([
        n4({ type: Boolean, reflect: true })
      ], NLDDButton.prototype, "loading", void 0);
      __decorate3([
        n4({ type: Boolean, reflect: true })
      ], NLDDButton.prototype, "disabled", void 0);
      __decorate3([
        n4({ type: String })
      ], NLDDButton.prototype, "text", void 0);
      __decorate3([
        n4({ type: String, reflect: true, attribute: "supporting-text" })
      ], NLDDButton.prototype, "supportingText", void 0);
      __decorate3([
        n4({ type: Boolean, reflect: true, attribute: "single-line" })
      ], NLDDButton.prototype, "singleLine", void 0);
      __decorate3([
        n4({ type: Boolean, reflect: true, attribute: "no-highlight-border" })
      ], NLDDButton.prototype, "noHighlightBorder", void 0);
      __decorate3([
        n4({ type: String, attribute: "start-icon" })
      ], NLDDButton.prototype, "startIcon", void 0);
      __decorate3([
        n4({ type: String, attribute: "end-icon" })
      ], NLDDButton.prototype, "endIcon", void 0);
      __decorate3([
        n4({ type: String, attribute: "accessible-label" })
      ], NLDDButton.prototype, "accessibleLabel", void 0);
      __decorate3([
        n4({ type: String, reflect: true })
      ], NLDDButton.prototype, "href", void 0);
      __decorate3([
        n4({ type: String })
      ], NLDDButton.prototype, "target", void 0);
      __decorate3([
        n4({ type: String })
      ], NLDDButton.prototype, "rel", void 0);
      NLDDButton = __decorate3([
        t3("nldd-button")
      ], NLDDButton);
    }
  });

  // node_modules/@nldd/design-system/dist/components/actions/icon-button/icon-button.styles.js
  var iconButtonStyles;
  var init_icon_button_styles = __esm({
    "node_modules/@nldd/design-system/dist/components/actions/icon-button/icon-button.styles.js"() {
      init_lit();
      init_slotted_reset();
      iconButtonStyles = i`


	/* # Host */

	:host {
		--_corner-radius: var(--semantics-controls-md-corner-radius);
		--_width: auto;
		--_min-size: var(--semantics-controls-md-min-size);
		--_padding: calc((var(--semantics-controls-md-min-size) - var(--semantics-buttons-md-icon-only-icon-size)) / 2);
		--_icon-size: var(--semantics-buttons-md-icon-only-icon-size);
		--_disclosure-icon-margin-right: calc(var(--primitives-space-2) * -1);
		--_disclosure-icon-size: var(--primitives-space-20);
		--_text-display: none;
		--_text-font: var(--primitives-font-body-xxs-medium-flat);
		--_background-color: var(--semantics-buttons-neutral-tinted-background-color);
		--_primary-content-color: var(--semantics-buttons-neutral-tinted-primary-content-color);
		--_highlight-border-color: var(--semantics-buttons-neutral-tinted-highlight-border-color);
		--_is-hovered-background-color: var(--semantics-buttons-neutral-tinted-is-hovered-background-color);
		--_is-hovered-primary-content-color: var(--semantics-buttons-neutral-tinted-is-hovered-primary-content-color);
		--_is-hovered-highlight-border-color: var(--semantics-buttons-neutral-tinted-is-hovered-highlight-border-color);
		--_is-active-background-color: var(--semantics-buttons-neutral-tinted-is-active-background-color);
		--_is-active-primary-content-color: var(--semantics-buttons-neutral-tinted-is-active-primary-content-color);
		--_is-active-highlight-border-color: var(--semantics-buttons-neutral-tinted-is-active-highlight-border-color);

		${inheritedTextReset}
		display: inline-block;
		position: relative;
		max-width: 100%;
		user-select: none;
		-webkit-tap-highlight-color: transparent;
	}

	:host([size="xs"]) {
		--_corner-radius: var(--semantics-controls-xs-corner-radius);
		--_min-size: var(--semantics-controls-xs-min-size);
		--_padding: calc((var(--semantics-controls-xs-min-size) - var(--semantics-buttons-xs-icon-only-icon-size)) / 2);
		--_icon-size: var(--semantics-buttons-xs-icon-only-icon-size);
		--_disclosure-icon-margin-right: 0;
		--_disclosure-icon-size: var(--primitives-space-16);
	}

	:host([size="sm"]) {
		--_corner-radius: var(--semantics-controls-sm-corner-radius);
		--_min-size: var(--semantics-controls-sm-min-size);
		--_padding: calc((var(--semantics-controls-sm-min-size) - var(--semantics-buttons-sm-icon-only-icon-size)) / 2);
		--_icon-size: var(--semantics-buttons-sm-icon-only-icon-size);
	}

	:host([size="lg"]) {
		--_corner-radius: var(--semantics-controls-lg-corner-radius);
		--_min-size: var(--semantics-controls-lg-min-size);
		--_padding: var(--primitives-space-8);
		--_text-display: block;
	}

	:host([size="lg"][hide-lg-text]) {
		--_padding: calc((var(--semantics-controls-lg-min-size) - var(--semantics-buttons-lg-icon-only-icon-size)) / 2);
		--_icon-size: var(--semantics-buttons-lg-icon-only-icon-size);
		--_text-display: none;
	}

	:host([variant="neutral-base"]) {
		--_background-color: var(--semantics-buttons-neutral-base-background-color);
		--_primary-content-color: var(--semantics-buttons-neutral-base-primary-content-color);
		--_highlight-border-color: var(--semantics-buttons-neutral-base-highlight-border-color);
		--_is-hovered-background-color: var(--semantics-buttons-neutral-base-is-hovered-background-color);
		--_is-hovered-primary-content-color: var(--semantics-buttons-neutral-base-is-hovered-primary-content-color);
		--_is-hovered-highlight-border-color: var(--semantics-buttons-neutral-base-is-hovered-highlight-border-color);
		--_is-active-background-color: var(--semantics-buttons-neutral-base-is-active-background-color);
		--_is-active-primary-content-color: var(--semantics-buttons-neutral-base-is-active-primary-content-color);
		--_is-active-highlight-border-color: var(--semantics-buttons-neutral-base-is-active-highlight-border-color);
	}

	:host([variant="neutral-transparent"]) {
		--_background-color: transparent;
		--_primary-content-color: var(--semantics-buttons-neutral-transparent-primary-content-color);
		--_highlight-border-color: transparent;
		--_is-hovered-background-color: transparent;
		--_is-hovered-primary-content-color: var(--semantics-buttons-neutral-transparent-is-hovered-primary-content-color);
		--_is-hovered-highlight-border-color: transparent;
		--_is-active-background-color: transparent;
		--_is-active-primary-content-color: var(--semantics-buttons-neutral-transparent-is-active-primary-content-color);
		--_is-active-highlight-border-color: transparent;
	}

	:host([variant="accent-filled"]),
	:host([variant="primary"]) {
		--_background-color: var(--semantics-buttons-accent-filled-background-color);
		--_primary-content-color: var(--semantics-buttons-accent-filled-primary-content-color);
		--_highlight-border-color: var(--semantics-buttons-accent-filled-highlight-border-color);
		--_is-hovered-background-color: var(--semantics-buttons-accent-filled-is-hovered-background-color);
		--_is-hovered-primary-content-color: var(--semantics-buttons-accent-filled-is-hovered-primary-content-color);
		--_is-hovered-highlight-border-color: var(--semantics-buttons-accent-filled-is-hovered-highlight-border-color);
		--_is-active-background-color: var(--semantics-buttons-accent-filled-is-active-background-color);
		--_is-active-primary-content-color: var(--semantics-buttons-accent-filled-is-active-primary-content-color);
		--_is-active-highlight-border-color: var(--semantics-buttons-accent-filled-is-active-highlight-border-color);
	}

	:host([variant="accent-transparent"]) {
		--_background-color: transparent;
		--_primary-content-color: var(--semantics-buttons-accent-transparent-primary-content-color);
		--_highlight-border-color: transparent;
		--_is-hovered-background-color: transparent;
		--_is-hovered-primary-content-color: var(--semantics-buttons-accent-transparent-is-hovered-primary-content-color);
		--_is-hovered-highlight-border-color: transparent;
		--_is-active-background-color: transparent;
		--_is-active-primary-content-color: var(--semantics-buttons-accent-transparent-is-active-primary-content-color);
		--_is-active-highlight-border-color: transparent;
	}

	:host([variant="critical-tinted"]),
	:host([variant="destructive"]) {
		--_background-color: var(--semantics-buttons-critical-tinted-background-color);
		--_primary-content-color: var(--semantics-buttons-critical-tinted-primary-content-color);
		--_highlight-border-color: var(--semantics-buttons-critical-tinted-highlight-border-color);
		--_is-hovered-background-color: var(--semantics-buttons-critical-tinted-is-hovered-background-color);
		--_is-hovered-primary-content-color: var(--semantics-buttons-critical-tinted-is-hovered-primary-content-color);
		--_is-hovered-highlight-border-color: var(--semantics-buttons-critical-tinted-is-hovered-highlight-border-color);
		--_is-active-background-color: var(--semantics-buttons-critical-tinted-is-active-background-color);
		--_is-active-primary-content-color: var(--semantics-buttons-critical-tinted-is-active-primary-content-color);
		--_is-active-highlight-border-color: var(--semantics-buttons-critical-tinted-is-active-highlight-border-color);
	}

	:host([variant="critical-transparent"]) {
		--_background-color: transparent;
		--_primary-content-color: var(--semantics-buttons-critical-transparent-primary-content-color);
		--_highlight-border-color: transparent;
		--_is-hovered-background-color: transparent;
		--_is-hovered-primary-content-color: var(--semantics-buttons-critical-transparent-is-hovered-primary-content-color);
		--_is-hovered-highlight-border-color: transparent;
		--_is-active-background-color: transparent;
		--_is-active-primary-content-color: var(--semantics-buttons-critical-transparent-is-active-primary-content-color);
		--_is-active-highlight-border-color: transparent;
	}

	/* ## Expanded — default (incl. unknown variant) */

	:host([expanded]) {
		--_background-color: var(--semantics-buttons-neutral-tinted-is-expanded-background-color);
		--_primary-content-color: var(--semantics-buttons-neutral-tinted-is-expanded-primary-content-color);
		--_highlight-border-color: var(--semantics-buttons-neutral-tinted-is-expanded-highlight-border-color);
		--_is-hovered-background-color: var(--semantics-buttons-neutral-tinted-is-expanded-is-hovered-background-color);
		--_is-hovered-primary-content-color: var(--semantics-buttons-neutral-tinted-is-expanded-is-hovered-primary-content-color);
		--_is-hovered-highlight-border-color: var(--semantics-buttons-neutral-tinted-is-expanded-is-hovered-highlight-border-color);
		--_is-active-background-color: var(--semantics-buttons-neutral-tinted-is-expanded-is-active-background-color);
		--_is-active-primary-content-color: var(--semantics-buttons-neutral-tinted-is-expanded-is-active-primary-content-color);
		--_is-active-highlight-border-color: var(--semantics-buttons-neutral-tinted-is-expanded-is-active-highlight-border-color);
	}

	:host([expanded][variant="neutral-base"]) {
		--_background-color: var(--semantics-buttons-neutral-base-is-expanded-background-color);
		--_primary-content-color: var(--semantics-buttons-neutral-base-is-expanded-primary-content-color);
		--_highlight-border-color: var(--semantics-buttons-neutral-base-is-expanded-highlight-border-color);
		--_is-hovered-background-color: var(--semantics-buttons-neutral-base-is-expanded-is-hovered-background-color);
		--_is-hovered-primary-content-color: var(--semantics-buttons-neutral-base-is-expanded-is-hovered-primary-content-color);
		--_is-hovered-highlight-border-color: var(--semantics-buttons-neutral-base-is-expanded-is-hovered-highlight-border-color);
		--_is-active-background-color: var(--semantics-buttons-neutral-base-is-expanded-is-active-background-color);
		--_is-active-primary-content-color: var(--semantics-buttons-neutral-base-is-expanded-is-active-primary-content-color);
		--_is-active-highlight-border-color: var(--semantics-buttons-neutral-base-is-expanded-is-active-highlight-border-color);
	}

	:host([expanded][variant="neutral-transparent"]) {
		--_background-color: transparent;
		--_primary-content-color: var(--semantics-buttons-neutral-transparent-primary-content-color);
		--_highlight-border-color: transparent;
		--_is-hovered-background-color: transparent;
		--_is-hovered-primary-content-color: var(--semantics-buttons-neutral-transparent-is-hovered-primary-content-color);
		--_is-hovered-highlight-border-color: transparent;
		--_is-active-background-color: transparent;
		--_is-active-primary-content-color: var(--semantics-buttons-neutral-transparent-is-active-primary-content-color);
		--_is-active-highlight-border-color: transparent;
	}

	:host([expanded][variant="accent-filled"]),
	:host([expanded][variant="primary"]) {
		--_background-color: var(--semantics-buttons-accent-filled-is-expanded-background-color);
		--_primary-content-color: var(--semantics-buttons-accent-filled-is-expanded-primary-content-color);
		--_highlight-border-color: var(--semantics-buttons-accent-filled-is-expanded-highlight-border-color);
		--_is-hovered-background-color: var(--semantics-buttons-accent-filled-is-expanded-is-hovered-background-color);
		--_is-hovered-primary-content-color: var(--semantics-buttons-accent-filled-is-expanded-is-hovered-primary-content-color);
		--_is-hovered-highlight-border-color: var(--semantics-buttons-accent-filled-is-expanded-is-hovered-highlight-border-color);
		--_is-active-background-color: var(--semantics-buttons-accent-filled-is-expanded-is-active-background-color);
		--_is-active-primary-content-color: var(--semantics-buttons-accent-filled-is-expanded-is-active-primary-content-color);
		--_is-active-highlight-border-color: var(--semantics-buttons-accent-filled-is-expanded-is-active-highlight-border-color);
	}

	:host([expanded][variant="accent-transparent"]) {
		--_background-color: transparent;
		--_primary-content-color: var(--semantics-buttons-accent-transparent-primary-content-color);
		--_highlight-border-color: transparent;
		--_is-hovered-background-color: transparent;
		--_is-hovered-primary-content-color: var(--semantics-buttons-accent-transparent-is-hovered-primary-content-color);
		--_is-hovered-highlight-border-color: transparent;
		--_is-active-background-color: transparent;
		--_is-active-primary-content-color: var(--semantics-buttons-accent-transparent-is-active-primary-content-color);
		--_is-active-highlight-border-color: transparent;
	}

	:host([expanded][variant="critical-tinted"]),
	:host([expanded][variant="destructive"]) {
		--_background-color: var(--semantics-buttons-critical-tinted-is-expanded-background-color);
		--_primary-content-color: var(--semantics-buttons-critical-tinted-is-expanded-primary-content-color);
		--_highlight-border-color: var(--semantics-buttons-critical-tinted-is-expanded-highlight-border-color);
		--_is-hovered-background-color: var(--semantics-buttons-critical-tinted-is-expanded-is-hovered-background-color);
		--_is-hovered-primary-content-color: var(--semantics-buttons-critical-tinted-is-expanded-is-hovered-primary-content-color);
		--_is-hovered-highlight-border-color: var(--semantics-buttons-critical-tinted-is-expanded-is-hovered-highlight-border-color);
		--_is-active-background-color: var(--semantics-buttons-critical-tinted-is-expanded-is-active-background-color);
		--_is-active-primary-content-color: var(--semantics-buttons-critical-tinted-is-expanded-is-active-primary-content-color);
		--_is-active-highlight-border-color: var(--semantics-buttons-critical-tinted-is-expanded-is-active-highlight-border-color);
	}

	:host([expanded][variant="critical-transparent"]) {
		--_background-color: transparent;
		--_primary-content-color: var(--semantics-buttons-critical-transparent-primary-content-color);
		--_highlight-border-color: transparent;
		--_is-hovered-background-color: transparent;
		--_is-hovered-primary-content-color: var(--semantics-buttons-critical-transparent-is-hovered-primary-content-color);
		--_is-hovered-highlight-border-color: transparent;
		--_is-active-background-color: transparent;
		--_is-active-primary-content-color: var(--semantics-buttons-critical-transparent-is-active-primary-content-color);
		--_is-active-highlight-border-color: transparent;
	}

	:host([width="full"]) {
		display: block;
		width: 100%;
		flex-grow: 1;
	}

	:host([hidden]) {
		display: none;
	}

	:host([disabled]) {
		opacity: var(--primitives-opacity-disabled);
		pointer-events: none;
	}

	:host([no-highlight-border]),
	:host([no-highlight-border][expanded]) {
		--_highlight-border-color: transparent;
		--_is-hovered-highlight-border-color: transparent;
		--_is-active-highlight-border-color: transparent;
	}


	/* # Block */

	.icon-button {
		box-sizing: border-box;
		display: inline-flex;
		margin: 0;
		border: none;
		border-radius: var(--_corner-radius);
		background: none;
		background-color: var(--_background-color);
		box-shadow: inset 0 0 0 var(--primitives-border-width-thin) var(--_highlight-border-color);
		width: var(--_width);
		min-width: var(--_min-size);
		height: var(--_min-size);
		min-height: var(--_min-size);
		padding: var(--_padding);
		flex-direction: column;
		align-items: center;
		justify-content: center;
		color: var(--_primary-content-color);
		font: inherit;
		text-decoration: none;
		transition:
			background-color var(--primitives-transition-duration-fast) var(--primitives-transition-easing-default),
			color var(--primitives-transition-duration-fast) var(--primitives-transition-easing-default)
		;
		appearance: none;
	}

	a.icon-button {
		cursor: var(--semantics-controls-link-cursor);
	}

	@media (prefers-reduced-motion: reduce) {
		.icon-button,
		.icon-button__icon-area,
		.icon-button__text {
			transition: none;
		}
	}

	.icon-button:focus-visible {
		outline: var(--semantics-focus-ring-outline);
		outline-offset: var(--semantics-focus-ring-outline-offset);
		box-shadow: var(--semantics-focus-ring-box-shadow), inset 0 0 0 var(--primitives-border-width-thin) var(--_highlight-border-color);
	}

	.icon-button:focus:not(:focus-visible) {
		outline: none;
	}

	@media (hover: hover) {
		.icon-button:hover {
			background-color: var(--_is-hovered-background-color);
			color: var(--_is-hovered-primary-content-color);
			--_highlight-border-color: var(--_is-hovered-highlight-border-color);
		}
	}

	.icon-button:active {
		background-color: var(--_is-active-background-color);
		color: var(--_is-active-primary-content-color);
		--_highlight-border-color: var(--_is-active-highlight-border-color);
	}

	/* Loading keeps the control focusable (not disabled); activation is blocked in JS. */
	:host([loading]) .icon-button {
		cursor: default;
	}


	/* # Elements */

	.icon-button__icon-area {
		display: inline-flex;
		flex-direction: row;
		align-items: center;
		justify-content: center;
		transition: opacity var(--primitives-transition-duration-slow) var(--primitives-transition-easing-default);
	}

	/* Loading crossfades the content out (opacity, not visibility, so the control
	   keeps its accessible name) while the indicator fades in. The content stays
	   laid out, so the control keeps its size. */
	:host([loading]) .icon-button__icon-area {
		opacity: 0;
	}

	.icon-button__icon {
		display: flex;
		width: var(--_icon-size);
		height: var(--_icon-size);
		flex-shrink: 0;
		align-items: center;
		justify-content: center;
	}

	.icon-button__disclosure-icon {
		display: flex;
		margin-right: var(--_disclosure-icon-margin-right);
		width: var(--_disclosure-icon-size);
		height: var(--_disclosure-icon-size);
		flex-shrink: 0;
	}

	.icon-button__text {
		display: var(--_text-display);
		text-align: center;
		color: inherit;
		font: var(--_text-font);
		white-space: nowrap;
		transition: opacity var(--primitives-transition-duration-slow) var(--primitives-transition-easing-default);
	}

	:host([loading]) .icon-button__text {
		opacity: 0;
	}

	/* Wrapper overlaid on the control, positioned against the host (which is
	   position:relative). It lives outside the <button>/<a> and the tooltip
	   wrapper so the indicator's role="status" live region works reliably. The
	   activity-indicator inside fills it and centres its circle, which inherits
	   the content color via currentColor. */
	.icon-button__activity-indicator {
		position: absolute;
		inset: 0;
		color: var(--_primary-content-color);
	}
`;
    }
  });

  // node_modules/@floating-ui/utils/dist/floating-ui.utils.mjs
  function clamp(start, value, end) {
    return max(start, min(value, end));
  }
  function evaluate(value, param) {
    return typeof value === "function" ? value(param) : value;
  }
  function getSide(placement) {
    return placement.split("-")[0];
  }
  function getAlignment(placement) {
    return placement.split("-")[1];
  }
  function getOppositeAxis(axis) {
    return axis === "x" ? "y" : "x";
  }
  function getAxisLength(axis) {
    return axis === "y" ? "height" : "width";
  }
  function getSideAxis(placement) {
    const firstChar = placement[0];
    return firstChar === "t" || firstChar === "b" ? "y" : "x";
  }
  function getAlignmentAxis(placement) {
    return getOppositeAxis(getSideAxis(placement));
  }
  function getAlignmentSides(placement, rects, rtl) {
    if (rtl === void 0) {
      rtl = false;
    }
    const alignment = getAlignment(placement);
    const alignmentAxis = getAlignmentAxis(placement);
    const length = getAxisLength(alignmentAxis);
    let mainAlignmentSide = alignmentAxis === "x" ? alignment === (rtl ? "end" : "start") ? "right" : "left" : alignment === "start" ? "bottom" : "top";
    if (rects.reference[length] > rects.floating[length]) {
      mainAlignmentSide = getOppositePlacement(mainAlignmentSide);
    }
    return [mainAlignmentSide, getOppositePlacement(mainAlignmentSide)];
  }
  function getExpandedPlacements(placement) {
    const oppositePlacement = getOppositePlacement(placement);
    return [getOppositeAlignmentPlacement(placement), oppositePlacement, getOppositeAlignmentPlacement(oppositePlacement)];
  }
  function getOppositeAlignmentPlacement(placement) {
    return placement.includes("start") ? placement.replace("start", "end") : placement.replace("end", "start");
  }
  function getSideList(side, isStart, rtl) {
    switch (side) {
      case "top":
      case "bottom":
        if (rtl) return isStart ? rlPlacement : lrPlacement;
        return isStart ? lrPlacement : rlPlacement;
      case "left":
      case "right":
        return isStart ? tbPlacement : btPlacement;
      default:
        return [];
    }
  }
  function getOppositeAxisPlacements(placement, flipAlignment, direction, rtl) {
    const alignment = getAlignment(placement);
    let list = getSideList(getSide(placement), direction === "start", rtl);
    if (alignment) {
      list = list.map((side) => side + "-" + alignment);
      if (flipAlignment) {
        list = list.concat(list.map(getOppositeAlignmentPlacement));
      }
    }
    return list;
  }
  function getOppositePlacement(placement) {
    const side = getSide(placement);
    return oppositeSideMap[side] + placement.slice(side.length);
  }
  function expandPaddingObject(padding) {
    return {
      top: 0,
      right: 0,
      bottom: 0,
      left: 0,
      ...padding
    };
  }
  function getPaddingObject(padding) {
    return typeof padding !== "number" ? expandPaddingObject(padding) : {
      top: padding,
      right: padding,
      bottom: padding,
      left: padding
    };
  }
  function rectToClientRect(rect) {
    const {
      x: x2,
      y: y3,
      width,
      height
    } = rect;
    return {
      width,
      height,
      top: y3,
      left: x2,
      right: x2 + width,
      bottom: y3 + height,
      x: x2,
      y: y3
    };
  }
  var min, max, round, floor, createCoords, oppositeSideMap, lrPlacement, rlPlacement, tbPlacement, btPlacement;
  var init_floating_ui_utils = __esm({
    "node_modules/@floating-ui/utils/dist/floating-ui.utils.mjs"() {
      min = Math.min;
      max = Math.max;
      round = Math.round;
      floor = Math.floor;
      createCoords = (v3) => ({
        x: v3,
        y: v3
      });
      oppositeSideMap = {
        left: "right",
        right: "left",
        bottom: "top",
        top: "bottom"
      };
      lrPlacement = ["left", "right"];
      rlPlacement = ["right", "left"];
      tbPlacement = ["top", "bottom"];
      btPlacement = ["bottom", "top"];
    }
  });

  // node_modules/@floating-ui/core/dist/floating-ui.core.mjs
  function computeCoordsFromPlacement(_ref, placement, rtl) {
    let {
      reference,
      floating
    } = _ref;
    const sideAxis = getSideAxis(placement);
    const alignmentAxis = getAlignmentAxis(placement);
    const alignLength = getAxisLength(alignmentAxis);
    const side = getSide(placement);
    const isVertical = sideAxis === "y";
    const commonX = reference.x + reference.width / 2 - floating.width / 2;
    const commonY = reference.y + reference.height / 2 - floating.height / 2;
    const commonAlign = reference[alignLength] / 2 - floating[alignLength] / 2;
    let coords;
    switch (side) {
      case "top":
        coords = {
          x: commonX,
          y: reference.y - floating.height
        };
        break;
      case "bottom":
        coords = {
          x: commonX,
          y: reference.y + reference.height
        };
        break;
      case "right":
        coords = {
          x: reference.x + reference.width,
          y: commonY
        };
        break;
      case "left":
        coords = {
          x: reference.x - floating.width,
          y: commonY
        };
        break;
      default:
        coords = {
          x: reference.x,
          y: reference.y
        };
    }
    switch (getAlignment(placement)) {
      case "start":
        coords[alignmentAxis] -= commonAlign * (rtl && isVertical ? -1 : 1);
        break;
      case "end":
        coords[alignmentAxis] += commonAlign * (rtl && isVertical ? -1 : 1);
        break;
    }
    return coords;
  }
  async function detectOverflow(state, options) {
    var _await$platform$isEle;
    if (options === void 0) {
      options = {};
    }
    const {
      x: x2,
      y: y3,
      platform: platform2,
      rects,
      elements,
      strategy
    } = state;
    const {
      boundary = "clippingAncestors",
      rootBoundary = "viewport",
      elementContext = "floating",
      altBoundary = false,
      padding = 0
    } = evaluate(options, state);
    const paddingObject = getPaddingObject(padding);
    const altContext = elementContext === "floating" ? "reference" : "floating";
    const element = elements[altBoundary ? altContext : elementContext];
    const clippingClientRect = rectToClientRect(await platform2.getClippingRect({
      element: ((_await$platform$isEle = await (platform2.isElement == null ? void 0 : platform2.isElement(element))) != null ? _await$platform$isEle : true) ? element : element.contextElement || await (platform2.getDocumentElement == null ? void 0 : platform2.getDocumentElement(elements.floating)),
      boundary,
      rootBoundary,
      strategy
    }));
    const rect = elementContext === "floating" ? {
      x: x2,
      y: y3,
      width: rects.floating.width,
      height: rects.floating.height
    } : rects.reference;
    const offsetParent = await (platform2.getOffsetParent == null ? void 0 : platform2.getOffsetParent(elements.floating));
    const offsetScale = await (platform2.isElement == null ? void 0 : platform2.isElement(offsetParent)) ? await (platform2.getScale == null ? void 0 : platform2.getScale(offsetParent)) || {
      x: 1,
      y: 1
    } : {
      x: 1,
      y: 1
    };
    const elementClientRect = rectToClientRect(platform2.convertOffsetParentRelativeRectToViewportRelativeRect ? await platform2.convertOffsetParentRelativeRectToViewportRelativeRect({
      elements,
      rect,
      offsetParent,
      strategy
    }) : rect);
    return {
      top: (clippingClientRect.top - elementClientRect.top + paddingObject.top) / offsetScale.y,
      bottom: (elementClientRect.bottom - clippingClientRect.bottom + paddingObject.bottom) / offsetScale.y,
      left: (clippingClientRect.left - elementClientRect.left + paddingObject.left) / offsetScale.x,
      right: (elementClientRect.right - clippingClientRect.right + paddingObject.right) / offsetScale.x
    };
  }
  async function convertValueToCoords(state, options) {
    const {
      placement,
      platform: platform2,
      elements
    } = state;
    const rtl = await (platform2.isRTL == null ? void 0 : platform2.isRTL(elements.floating));
    const side = getSide(placement);
    const alignment = getAlignment(placement);
    const isVertical = getSideAxis(placement) === "y";
    const mainAxisMulti = originSides.has(side) ? -1 : 1;
    const crossAxisMulti = rtl && isVertical ? -1 : 1;
    const rawValue = evaluate(options, state);
    let {
      mainAxis,
      crossAxis,
      alignmentAxis
    } = typeof rawValue === "number" ? {
      mainAxis: rawValue,
      crossAxis: 0,
      alignmentAxis: null
    } : {
      mainAxis: rawValue.mainAxis || 0,
      crossAxis: rawValue.crossAxis || 0,
      alignmentAxis: rawValue.alignmentAxis
    };
    if (alignment && typeof alignmentAxis === "number") {
      crossAxis = alignment === "end" ? alignmentAxis * -1 : alignmentAxis;
    }
    return isVertical ? {
      x: crossAxis * crossAxisMulti,
      y: mainAxis * mainAxisMulti
    } : {
      x: mainAxis * mainAxisMulti,
      y: crossAxis * crossAxisMulti
    };
  }
  var MAX_RESET_COUNT, computePosition, flip, originSides, offset, shift, size;
  var init_floating_ui_core = __esm({
    "node_modules/@floating-ui/core/dist/floating-ui.core.mjs"() {
      init_floating_ui_utils();
      init_floating_ui_utils();
      MAX_RESET_COUNT = 50;
      computePosition = async (reference, floating, config) => {
        const {
          placement = "bottom",
          strategy = "absolute",
          middleware = [],
          platform: platform2
        } = config;
        const platformWithDetectOverflow = platform2.detectOverflow ? platform2 : {
          ...platform2,
          detectOverflow
        };
        const rtl = await (platform2.isRTL == null ? void 0 : platform2.isRTL(floating));
        let rects = await platform2.getElementRects({
          reference,
          floating,
          strategy
        });
        let {
          x: x2,
          y: y3
        } = computeCoordsFromPlacement(rects, placement, rtl);
        let statefulPlacement = placement;
        let resetCount = 0;
        const middlewareData = {};
        for (let i8 = 0; i8 < middleware.length; i8++) {
          const currentMiddleware = middleware[i8];
          if (!currentMiddleware) {
            continue;
          }
          const {
            name,
            fn
          } = currentMiddleware;
          const {
            x: nextX,
            y: nextY,
            data,
            reset
          } = await fn({
            x: x2,
            y: y3,
            initialPlacement: placement,
            placement: statefulPlacement,
            strategy,
            middlewareData,
            rects,
            platform: platformWithDetectOverflow,
            elements: {
              reference,
              floating
            }
          });
          x2 = nextX != null ? nextX : x2;
          y3 = nextY != null ? nextY : y3;
          middlewareData[name] = {
            ...middlewareData[name],
            ...data
          };
          if (reset && resetCount < MAX_RESET_COUNT) {
            resetCount++;
            if (typeof reset === "object") {
              if (reset.placement) {
                statefulPlacement = reset.placement;
              }
              if (reset.rects) {
                rects = reset.rects === true ? await platform2.getElementRects({
                  reference,
                  floating,
                  strategy
                }) : reset.rects;
              }
              ({
                x: x2,
                y: y3
              } = computeCoordsFromPlacement(rects, statefulPlacement, rtl));
            }
            i8 = -1;
          }
        }
        return {
          x: x2,
          y: y3,
          placement: statefulPlacement,
          strategy,
          middlewareData
        };
      };
      flip = function(options) {
        if (options === void 0) {
          options = {};
        }
        return {
          name: "flip",
          options,
          async fn(state) {
            var _middlewareData$arrow, _middlewareData$flip;
            const {
              placement,
              middlewareData,
              rects,
              initialPlacement,
              platform: platform2,
              elements
            } = state;
            const {
              mainAxis: checkMainAxis = true,
              crossAxis: checkCrossAxis = true,
              fallbackPlacements: specifiedFallbackPlacements,
              fallbackStrategy = "bestFit",
              fallbackAxisSideDirection = "none",
              flipAlignment = true,
              ...detectOverflowOptions
            } = evaluate(options, state);
            if ((_middlewareData$arrow = middlewareData.arrow) != null && _middlewareData$arrow.alignmentOffset) {
              return {};
            }
            const side = getSide(placement);
            const initialSideAxis = getSideAxis(initialPlacement);
            const isBasePlacement = getSide(initialPlacement) === initialPlacement;
            const rtl = await (platform2.isRTL == null ? void 0 : platform2.isRTL(elements.floating));
            const fallbackPlacements = specifiedFallbackPlacements || (isBasePlacement || !flipAlignment ? [getOppositePlacement(initialPlacement)] : getExpandedPlacements(initialPlacement));
            const hasFallbackAxisSideDirection = fallbackAxisSideDirection !== "none";
            if (!specifiedFallbackPlacements && hasFallbackAxisSideDirection) {
              fallbackPlacements.push(...getOppositeAxisPlacements(initialPlacement, flipAlignment, fallbackAxisSideDirection, rtl));
            }
            const placements2 = [initialPlacement, ...fallbackPlacements];
            const overflow = await platform2.detectOverflow(state, detectOverflowOptions);
            const overflows = [];
            let overflowsData = ((_middlewareData$flip = middlewareData.flip) == null ? void 0 : _middlewareData$flip.overflows) || [];
            if (checkMainAxis) {
              overflows.push(overflow[side]);
            }
            if (checkCrossAxis) {
              const sides2 = getAlignmentSides(placement, rects, rtl);
              overflows.push(overflow[sides2[0]], overflow[sides2[1]]);
            }
            overflowsData = [...overflowsData, {
              placement,
              overflows
            }];
            if (!overflows.every((side2) => side2 <= 0)) {
              var _middlewareData$flip2, _overflowsData$filter;
              const nextIndex = (((_middlewareData$flip2 = middlewareData.flip) == null ? void 0 : _middlewareData$flip2.index) || 0) + 1;
              const nextPlacement = placements2[nextIndex];
              if (nextPlacement) {
                const ignoreCrossAxisOverflow = checkCrossAxis === "alignment" ? initialSideAxis !== getSideAxis(nextPlacement) : false;
                if (!ignoreCrossAxisOverflow || // We leave the current main axis only if every placement on that axis
                // overflows the main axis.
                overflowsData.every((d3) => getSideAxis(d3.placement) === initialSideAxis ? d3.overflows[0] > 0 : true)) {
                  return {
                    data: {
                      index: nextIndex,
                      overflows: overflowsData
                    },
                    reset: {
                      placement: nextPlacement
                    }
                  };
                }
              }
              let resetPlacement = (_overflowsData$filter = overflowsData.filter((d3) => d3.overflows[0] <= 0).sort((a4, b3) => a4.overflows[1] - b3.overflows[1])[0]) == null ? void 0 : _overflowsData$filter.placement;
              if (!resetPlacement) {
                switch (fallbackStrategy) {
                  case "bestFit": {
                    var _overflowsData$filter2;
                    const placement2 = (_overflowsData$filter2 = overflowsData.filter((d3) => {
                      if (hasFallbackAxisSideDirection) {
                        const currentSideAxis = getSideAxis(d3.placement);
                        return currentSideAxis === initialSideAxis || // Create a bias to the `y` side axis due to horizontal
                        // reading directions favoring greater width.
                        currentSideAxis === "y";
                      }
                      return true;
                    }).map((d3) => [d3.placement, d3.overflows.filter((overflow2) => overflow2 > 0).reduce((acc, overflow2) => acc + overflow2, 0)]).sort((a4, b3) => a4[1] - b3[1])[0]) == null ? void 0 : _overflowsData$filter2[0];
                    if (placement2) {
                      resetPlacement = placement2;
                    }
                    break;
                  }
                  case "initialPlacement":
                    resetPlacement = initialPlacement;
                    break;
                }
              }
              if (placement !== resetPlacement) {
                return {
                  reset: {
                    placement: resetPlacement
                  }
                };
              }
            }
            return {};
          }
        };
      };
      originSides = /* @__PURE__ */ new Set(["left", "top"]);
      offset = function(options) {
        if (options === void 0) {
          options = 0;
        }
        return {
          name: "offset",
          options,
          async fn(state) {
            var _middlewareData$offse, _middlewareData$arrow;
            const {
              x: x2,
              y: y3,
              placement,
              middlewareData
            } = state;
            const diffCoords = await convertValueToCoords(state, options);
            if (placement === ((_middlewareData$offse = middlewareData.offset) == null ? void 0 : _middlewareData$offse.placement) && (_middlewareData$arrow = middlewareData.arrow) != null && _middlewareData$arrow.alignmentOffset) {
              return {};
            }
            return {
              x: x2 + diffCoords.x,
              y: y3 + diffCoords.y,
              data: {
                ...diffCoords,
                placement
              }
            };
          }
        };
      };
      shift = function(options) {
        if (options === void 0) {
          options = {};
        }
        return {
          name: "shift",
          options,
          async fn(state) {
            const {
              x: x2,
              y: y3,
              placement,
              platform: platform2
            } = state;
            const {
              mainAxis: checkMainAxis = true,
              crossAxis: checkCrossAxis = false,
              limiter = {
                fn: (_ref) => {
                  let {
                    x: x3,
                    y: y4
                  } = _ref;
                  return {
                    x: x3,
                    y: y4
                  };
                }
              },
              ...detectOverflowOptions
            } = evaluate(options, state);
            const coords = {
              x: x2,
              y: y3
            };
            const overflow = await platform2.detectOverflow(state, detectOverflowOptions);
            const crossAxis = getSideAxis(getSide(placement));
            const mainAxis = getOppositeAxis(crossAxis);
            let mainAxisCoord = coords[mainAxis];
            let crossAxisCoord = coords[crossAxis];
            if (checkMainAxis) {
              const minSide = mainAxis === "y" ? "top" : "left";
              const maxSide = mainAxis === "y" ? "bottom" : "right";
              const min2 = mainAxisCoord + overflow[minSide];
              const max2 = mainAxisCoord - overflow[maxSide];
              mainAxisCoord = clamp(min2, mainAxisCoord, max2);
            }
            if (checkCrossAxis) {
              const minSide = crossAxis === "y" ? "top" : "left";
              const maxSide = crossAxis === "y" ? "bottom" : "right";
              const min2 = crossAxisCoord + overflow[minSide];
              const max2 = crossAxisCoord - overflow[maxSide];
              crossAxisCoord = clamp(min2, crossAxisCoord, max2);
            }
            const limitedCoords = limiter.fn({
              ...state,
              [mainAxis]: mainAxisCoord,
              [crossAxis]: crossAxisCoord
            });
            return {
              ...limitedCoords,
              data: {
                x: limitedCoords.x - x2,
                y: limitedCoords.y - y3,
                enabled: {
                  [mainAxis]: checkMainAxis,
                  [crossAxis]: checkCrossAxis
                }
              }
            };
          }
        };
      };
      size = function(options) {
        if (options === void 0) {
          options = {};
        }
        return {
          name: "size",
          options,
          async fn(state) {
            var _state$middlewareData, _state$middlewareData2;
            const {
              placement,
              rects,
              platform: platform2,
              elements
            } = state;
            const {
              apply = () => {
              },
              ...detectOverflowOptions
            } = evaluate(options, state);
            const overflow = await platform2.detectOverflow(state, detectOverflowOptions);
            const side = getSide(placement);
            const alignment = getAlignment(placement);
            const isYAxis = getSideAxis(placement) === "y";
            const {
              width,
              height
            } = rects.floating;
            let heightSide;
            let widthSide;
            if (side === "top" || side === "bottom") {
              heightSide = side;
              widthSide = alignment === (await (platform2.isRTL == null ? void 0 : platform2.isRTL(elements.floating)) ? "start" : "end") ? "left" : "right";
            } else {
              widthSide = side;
              heightSide = alignment === "end" ? "top" : "bottom";
            }
            const maximumClippingHeight = height - overflow.top - overflow.bottom;
            const maximumClippingWidth = width - overflow.left - overflow.right;
            const overflowAvailableHeight = min(height - overflow[heightSide], maximumClippingHeight);
            const overflowAvailableWidth = min(width - overflow[widthSide], maximumClippingWidth);
            const noShift = !state.middlewareData.shift;
            let availableHeight = overflowAvailableHeight;
            let availableWidth = overflowAvailableWidth;
            if ((_state$middlewareData = state.middlewareData.shift) != null && _state$middlewareData.enabled.x) {
              availableWidth = maximumClippingWidth;
            }
            if ((_state$middlewareData2 = state.middlewareData.shift) != null && _state$middlewareData2.enabled.y) {
              availableHeight = maximumClippingHeight;
            }
            if (noShift && !alignment) {
              const xMin = max(overflow.left, 0);
              const xMax = max(overflow.right, 0);
              const yMin = max(overflow.top, 0);
              const yMax = max(overflow.bottom, 0);
              if (isYAxis) {
                availableWidth = width - 2 * (xMin !== 0 || xMax !== 0 ? xMin + xMax : max(overflow.left, overflow.right));
              } else {
                availableHeight = height - 2 * (yMin !== 0 || yMax !== 0 ? yMin + yMax : max(overflow.top, overflow.bottom));
              }
            }
            await apply({
              ...state,
              availableWidth,
              availableHeight
            });
            const nextDimensions = await platform2.getDimensions(elements.floating);
            if (width !== nextDimensions.width || height !== nextDimensions.height) {
              return {
                reset: {
                  rects: true
                }
              };
            }
            return {};
          }
        };
      };
    }
  });

  // node_modules/@floating-ui/utils/dist/floating-ui.utils.dom.mjs
  function hasWindow() {
    return typeof window !== "undefined";
  }
  function getNodeName(node) {
    if (isNode(node)) {
      return (node.nodeName || "").toLowerCase();
    }
    return "#document";
  }
  function getWindow(node) {
    var _node$ownerDocument;
    return (node == null || (_node$ownerDocument = node.ownerDocument) == null ? void 0 : _node$ownerDocument.defaultView) || window;
  }
  function getDocumentElement(node) {
    var _ref;
    return (_ref = (isNode(node) ? node.ownerDocument : node.document) || window.document) == null ? void 0 : _ref.documentElement;
  }
  function isNode(value) {
    if (!hasWindow()) {
      return false;
    }
    return value instanceof Node || value instanceof getWindow(value).Node;
  }
  function isElement(value) {
    if (!hasWindow()) {
      return false;
    }
    return value instanceof Element || value instanceof getWindow(value).Element;
  }
  function isHTMLElement(value) {
    if (!hasWindow()) {
      return false;
    }
    return value instanceof HTMLElement || value instanceof getWindow(value).HTMLElement;
  }
  function isShadowRoot(value) {
    if (!hasWindow() || typeof ShadowRoot === "undefined") {
      return false;
    }
    return value instanceof ShadowRoot || value instanceof getWindow(value).ShadowRoot;
  }
  function isOverflowElement(element) {
    const {
      overflow,
      overflowX,
      overflowY,
      display
    } = getComputedStyle2(element);
    return /auto|scroll|overlay|hidden|clip/.test(overflow + overflowY + overflowX) && display !== "inline" && display !== "contents";
  }
  function isTableElement(element) {
    return /^(table|td|th)$/.test(getNodeName(element));
  }
  function isTopLayer(element) {
    try {
      if (element.matches(":popover-open")) {
        return true;
      }
    } catch (_e) {
    }
    try {
      return element.matches(":modal");
    } catch (_e) {
      return false;
    }
  }
  function isContainingBlock(elementOrCss) {
    const css = isElement(elementOrCss) ? getComputedStyle2(elementOrCss) : elementOrCss;
    return isNotNone(css.transform) || isNotNone(css.translate) || isNotNone(css.scale) || isNotNone(css.rotate) || isNotNone(css.perspective) || !isWebKit() && (isNotNone(css.backdropFilter) || isNotNone(css.filter)) || willChangeRe.test(css.willChange || "") || containRe.test(css.contain || "");
  }
  function getContainingBlock(element) {
    let currentNode = getParentNode(element);
    while (isHTMLElement(currentNode) && !isLastTraversableNode(currentNode)) {
      if (isContainingBlock(currentNode)) {
        return currentNode;
      } else if (isTopLayer(currentNode)) {
        return null;
      }
      currentNode = getParentNode(currentNode);
    }
    return null;
  }
  function isWebKit() {
    if (isWebKitValue == null) {
      isWebKitValue = typeof CSS !== "undefined" && CSS.supports && CSS.supports("-webkit-backdrop-filter", "none");
    }
    return isWebKitValue;
  }
  function isLastTraversableNode(node) {
    return /^(html|body|#document)$/.test(getNodeName(node));
  }
  function getComputedStyle2(element) {
    return getWindow(element).getComputedStyle(element);
  }
  function getNodeScroll(element) {
    if (isElement(element)) {
      return {
        scrollLeft: element.scrollLeft,
        scrollTop: element.scrollTop
      };
    }
    return {
      scrollLeft: element.scrollX,
      scrollTop: element.scrollY
    };
  }
  function getParentNode(node) {
    if (getNodeName(node) === "html") {
      return node;
    }
    const result = (
      // Step into the shadow DOM of the parent of a slotted node.
      node.assignedSlot || // DOM Element detected.
      node.parentNode || // ShadowRoot detected.
      isShadowRoot(node) && node.host || // Fallback.
      getDocumentElement(node)
    );
    return isShadowRoot(result) ? result.host : result;
  }
  function getNearestOverflowAncestor(node) {
    const parentNode = getParentNode(node);
    if (isLastTraversableNode(parentNode)) {
      return node.ownerDocument ? node.ownerDocument.body : node.body;
    }
    if (isHTMLElement(parentNode) && isOverflowElement(parentNode)) {
      return parentNode;
    }
    return getNearestOverflowAncestor(parentNode);
  }
  function getOverflowAncestors(node, list, traverseIframes) {
    var _node$ownerDocument2;
    if (list === void 0) {
      list = [];
    }
    if (traverseIframes === void 0) {
      traverseIframes = true;
    }
    const scrollableAncestor = getNearestOverflowAncestor(node);
    const isBody = scrollableAncestor === ((_node$ownerDocument2 = node.ownerDocument) == null ? void 0 : _node$ownerDocument2.body);
    const win = getWindow(scrollableAncestor);
    if (isBody) {
      const frameElement = getFrameElement(win);
      return list.concat(win, win.visualViewport || [], isOverflowElement(scrollableAncestor) ? scrollableAncestor : [], frameElement && traverseIframes ? getOverflowAncestors(frameElement) : []);
    } else {
      return list.concat(scrollableAncestor, getOverflowAncestors(scrollableAncestor, [], traverseIframes));
    }
  }
  function getFrameElement(win) {
    return win.parent && Object.getPrototypeOf(win.parent) ? win.frameElement : null;
  }
  var willChangeRe, containRe, isNotNone, isWebKitValue;
  var init_floating_ui_utils_dom = __esm({
    "node_modules/@floating-ui/utils/dist/floating-ui.utils.dom.mjs"() {
      willChangeRe = /transform|translate|scale|rotate|perspective|filter/;
      containRe = /paint|layout|strict|content/;
      isNotNone = (value) => !!value && value !== "none";
    }
  });

  // node_modules/@floating-ui/dom/dist/floating-ui.dom.mjs
  function getCssDimensions(element) {
    const css = getComputedStyle2(element);
    let width = parseFloat(css.width) || 0;
    let height = parseFloat(css.height) || 0;
    const hasOffset = isHTMLElement(element);
    const offsetWidth = hasOffset ? element.offsetWidth : width;
    const offsetHeight = hasOffset ? element.offsetHeight : height;
    const shouldFallback = round(width) !== offsetWidth || round(height) !== offsetHeight;
    if (shouldFallback) {
      width = offsetWidth;
      height = offsetHeight;
    }
    return {
      width,
      height,
      $: shouldFallback
    };
  }
  function unwrapElement(element) {
    return !isElement(element) ? element.contextElement : element;
  }
  function getScale(element) {
    const domElement = unwrapElement(element);
    if (!isHTMLElement(domElement)) {
      return createCoords(1);
    }
    const rect = domElement.getBoundingClientRect();
    const {
      width,
      height,
      $: $3
    } = getCssDimensions(domElement);
    let x2 = ($3 ? round(rect.width) : rect.width) / width;
    let y3 = ($3 ? round(rect.height) : rect.height) / height;
    if (!x2 || !Number.isFinite(x2)) {
      x2 = 1;
    }
    if (!y3 || !Number.isFinite(y3)) {
      y3 = 1;
    }
    return {
      x: x2,
      y: y3
    };
  }
  function getVisualOffsets(element) {
    const win = getWindow(element);
    if (!isWebKit() || !win.visualViewport) {
      return noOffsets;
    }
    return {
      x: win.visualViewport.offsetLeft,
      y: win.visualViewport.offsetTop
    };
  }
  function shouldAddVisualOffsets(element, isFixed, floatingOffsetParent) {
    if (isFixed === void 0) {
      isFixed = false;
    }
    if (!floatingOffsetParent || isFixed && floatingOffsetParent !== getWindow(element)) {
      return false;
    }
    return isFixed;
  }
  function getBoundingClientRect(element, includeScale, isFixedStrategy, offsetParent) {
    if (includeScale === void 0) {
      includeScale = false;
    }
    if (isFixedStrategy === void 0) {
      isFixedStrategy = false;
    }
    const clientRect = element.getBoundingClientRect();
    const domElement = unwrapElement(element);
    let scale = createCoords(1);
    if (includeScale) {
      if (offsetParent) {
        if (isElement(offsetParent)) {
          scale = getScale(offsetParent);
        }
      } else {
        scale = getScale(element);
      }
    }
    const visualOffsets = shouldAddVisualOffsets(domElement, isFixedStrategy, offsetParent) ? getVisualOffsets(domElement) : createCoords(0);
    let x2 = (clientRect.left + visualOffsets.x) / scale.x;
    let y3 = (clientRect.top + visualOffsets.y) / scale.y;
    let width = clientRect.width / scale.x;
    let height = clientRect.height / scale.y;
    if (domElement) {
      const win = getWindow(domElement);
      const offsetWin = offsetParent && isElement(offsetParent) ? getWindow(offsetParent) : offsetParent;
      let currentWin = win;
      let currentIFrame = getFrameElement(currentWin);
      while (currentIFrame && offsetParent && offsetWin !== currentWin) {
        const iframeScale = getScale(currentIFrame);
        const iframeRect = currentIFrame.getBoundingClientRect();
        const css = getComputedStyle2(currentIFrame);
        const left = iframeRect.left + (currentIFrame.clientLeft + parseFloat(css.paddingLeft)) * iframeScale.x;
        const top = iframeRect.top + (currentIFrame.clientTop + parseFloat(css.paddingTop)) * iframeScale.y;
        x2 *= iframeScale.x;
        y3 *= iframeScale.y;
        width *= iframeScale.x;
        height *= iframeScale.y;
        x2 += left;
        y3 += top;
        currentWin = getWindow(currentIFrame);
        currentIFrame = getFrameElement(currentWin);
      }
    }
    return rectToClientRect({
      width,
      height,
      x: x2,
      y: y3
    });
  }
  function getWindowScrollBarX(element, rect) {
    const leftScroll = getNodeScroll(element).scrollLeft;
    if (!rect) {
      return getBoundingClientRect(getDocumentElement(element)).left + leftScroll;
    }
    return rect.left + leftScroll;
  }
  function getHTMLOffset(documentElement, scroll) {
    const htmlRect = documentElement.getBoundingClientRect();
    const x2 = htmlRect.left + scroll.scrollLeft - getWindowScrollBarX(documentElement, htmlRect);
    const y3 = htmlRect.top + scroll.scrollTop;
    return {
      x: x2,
      y: y3
    };
  }
  function convertOffsetParentRelativeRectToViewportRelativeRect(_ref) {
    let {
      elements,
      rect,
      offsetParent,
      strategy
    } = _ref;
    const isFixed = strategy === "fixed";
    const documentElement = getDocumentElement(offsetParent);
    const topLayer = elements ? isTopLayer(elements.floating) : false;
    if (offsetParent === documentElement || topLayer && isFixed) {
      return rect;
    }
    let scroll = {
      scrollLeft: 0,
      scrollTop: 0
    };
    let scale = createCoords(1);
    const offsets = createCoords(0);
    const isOffsetParentAnElement = isHTMLElement(offsetParent);
    if (isOffsetParentAnElement || !isOffsetParentAnElement && !isFixed) {
      if (getNodeName(offsetParent) !== "body" || isOverflowElement(documentElement)) {
        scroll = getNodeScroll(offsetParent);
      }
      if (isOffsetParentAnElement) {
        const offsetRect = getBoundingClientRect(offsetParent);
        scale = getScale(offsetParent);
        offsets.x = offsetRect.x + offsetParent.clientLeft;
        offsets.y = offsetRect.y + offsetParent.clientTop;
      }
    }
    const htmlOffset = documentElement && !isOffsetParentAnElement && !isFixed ? getHTMLOffset(documentElement, scroll) : createCoords(0);
    return {
      width: rect.width * scale.x,
      height: rect.height * scale.y,
      x: rect.x * scale.x - scroll.scrollLeft * scale.x + offsets.x + htmlOffset.x,
      y: rect.y * scale.y - scroll.scrollTop * scale.y + offsets.y + htmlOffset.y
    };
  }
  function getClientRects(element) {
    return Array.from(element.getClientRects());
  }
  function getDocumentRect(element) {
    const html = getDocumentElement(element);
    const scroll = getNodeScroll(element);
    const body = element.ownerDocument.body;
    const width = max(html.scrollWidth, html.clientWidth, body.scrollWidth, body.clientWidth);
    const height = max(html.scrollHeight, html.clientHeight, body.scrollHeight, body.clientHeight);
    let x2 = -scroll.scrollLeft + getWindowScrollBarX(element);
    const y3 = -scroll.scrollTop;
    if (getComputedStyle2(body).direction === "rtl") {
      x2 += max(html.clientWidth, body.clientWidth) - width;
    }
    return {
      width,
      height,
      x: x2,
      y: y3
    };
  }
  function getViewportRect(element, strategy) {
    const win = getWindow(element);
    const html = getDocumentElement(element);
    const visualViewport = win.visualViewport;
    let width = html.clientWidth;
    let height = html.clientHeight;
    let x2 = 0;
    let y3 = 0;
    if (visualViewport) {
      width = visualViewport.width;
      height = visualViewport.height;
      const visualViewportBased = isWebKit();
      if (!visualViewportBased || visualViewportBased && strategy === "fixed") {
        x2 = visualViewport.offsetLeft;
        y3 = visualViewport.offsetTop;
      }
    }
    const windowScrollbarX = getWindowScrollBarX(html);
    if (windowScrollbarX <= 0) {
      const doc = html.ownerDocument;
      const body = doc.body;
      const bodyStyles = getComputedStyle(body);
      const bodyMarginInline = doc.compatMode === "CSS1Compat" ? parseFloat(bodyStyles.marginLeft) + parseFloat(bodyStyles.marginRight) || 0 : 0;
      const clippingStableScrollbarWidth = Math.abs(html.clientWidth - body.clientWidth - bodyMarginInline);
      if (clippingStableScrollbarWidth <= SCROLLBAR_MAX) {
        width -= clippingStableScrollbarWidth;
      }
    } else if (windowScrollbarX <= SCROLLBAR_MAX) {
      width += windowScrollbarX;
    }
    return {
      width,
      height,
      x: x2,
      y: y3
    };
  }
  function getInnerBoundingClientRect(element, strategy) {
    const clientRect = getBoundingClientRect(element, true, strategy === "fixed");
    const top = clientRect.top + element.clientTop;
    const left = clientRect.left + element.clientLeft;
    const scale = isHTMLElement(element) ? getScale(element) : createCoords(1);
    const width = element.clientWidth * scale.x;
    const height = element.clientHeight * scale.y;
    const x2 = left * scale.x;
    const y3 = top * scale.y;
    return {
      width,
      height,
      x: x2,
      y: y3
    };
  }
  function getClientRectFromClippingAncestor(element, clippingAncestor, strategy) {
    let rect;
    if (clippingAncestor === "viewport") {
      rect = getViewportRect(element, strategy);
    } else if (clippingAncestor === "document") {
      rect = getDocumentRect(getDocumentElement(element));
    } else if (isElement(clippingAncestor)) {
      rect = getInnerBoundingClientRect(clippingAncestor, strategy);
    } else {
      const visualOffsets = getVisualOffsets(element);
      rect = {
        x: clippingAncestor.x - visualOffsets.x,
        y: clippingAncestor.y - visualOffsets.y,
        width: clippingAncestor.width,
        height: clippingAncestor.height
      };
    }
    return rectToClientRect(rect);
  }
  function hasFixedPositionAncestor(element, stopNode) {
    const parentNode = getParentNode(element);
    if (parentNode === stopNode || !isElement(parentNode) || isLastTraversableNode(parentNode)) {
      return false;
    }
    return getComputedStyle2(parentNode).position === "fixed" || hasFixedPositionAncestor(parentNode, stopNode);
  }
  function getClippingElementAncestors(element, cache) {
    const cachedResult = cache.get(element);
    if (cachedResult) {
      return cachedResult;
    }
    let result = getOverflowAncestors(element, [], false).filter((el) => isElement(el) && getNodeName(el) !== "body");
    let currentContainingBlockComputedStyle = null;
    const elementIsFixed = getComputedStyle2(element).position === "fixed";
    let currentNode = elementIsFixed ? getParentNode(element) : element;
    while (isElement(currentNode) && !isLastTraversableNode(currentNode)) {
      const computedStyle = getComputedStyle2(currentNode);
      const currentNodeIsContaining = isContainingBlock(currentNode);
      if (!currentNodeIsContaining && computedStyle.position === "fixed") {
        currentContainingBlockComputedStyle = null;
      }
      const shouldDropCurrentNode = elementIsFixed ? !currentNodeIsContaining && !currentContainingBlockComputedStyle : !currentNodeIsContaining && computedStyle.position === "static" && !!currentContainingBlockComputedStyle && (currentContainingBlockComputedStyle.position === "absolute" || currentContainingBlockComputedStyle.position === "fixed") || isOverflowElement(currentNode) && !currentNodeIsContaining && hasFixedPositionAncestor(element, currentNode);
      if (shouldDropCurrentNode) {
        result = result.filter((ancestor) => ancestor !== currentNode);
      } else {
        currentContainingBlockComputedStyle = computedStyle;
      }
      currentNode = getParentNode(currentNode);
    }
    cache.set(element, result);
    return result;
  }
  function getClippingRect(_ref) {
    let {
      element,
      boundary,
      rootBoundary,
      strategy
    } = _ref;
    const elementClippingAncestors = boundary === "clippingAncestors" ? isTopLayer(element) ? [] : getClippingElementAncestors(element, this._c) : [].concat(boundary);
    const clippingAncestors = [...elementClippingAncestors, rootBoundary];
    const firstRect = getClientRectFromClippingAncestor(element, clippingAncestors[0], strategy);
    let top = firstRect.top;
    let right = firstRect.right;
    let bottom = firstRect.bottom;
    let left = firstRect.left;
    for (let i8 = 1; i8 < clippingAncestors.length; i8++) {
      const rect = getClientRectFromClippingAncestor(element, clippingAncestors[i8], strategy);
      top = max(rect.top, top);
      right = min(rect.right, right);
      bottom = min(rect.bottom, bottom);
      left = max(rect.left, left);
    }
    return {
      width: right - left,
      height: bottom - top,
      x: left,
      y: top
    };
  }
  function getDimensions(element) {
    const {
      width,
      height
    } = getCssDimensions(element);
    return {
      width,
      height
    };
  }
  function getRectRelativeToOffsetParent(element, offsetParent, strategy) {
    const isOffsetParentAnElement = isHTMLElement(offsetParent);
    const documentElement = getDocumentElement(offsetParent);
    const isFixed = strategy === "fixed";
    const rect = getBoundingClientRect(element, true, isFixed, offsetParent);
    let scroll = {
      scrollLeft: 0,
      scrollTop: 0
    };
    const offsets = createCoords(0);
    function setLeftRTLScrollbarOffset() {
      offsets.x = getWindowScrollBarX(documentElement);
    }
    if (isOffsetParentAnElement || !isOffsetParentAnElement && !isFixed) {
      if (getNodeName(offsetParent) !== "body" || isOverflowElement(documentElement)) {
        scroll = getNodeScroll(offsetParent);
      }
      if (isOffsetParentAnElement) {
        const offsetRect = getBoundingClientRect(offsetParent, true, isFixed, offsetParent);
        offsets.x = offsetRect.x + offsetParent.clientLeft;
        offsets.y = offsetRect.y + offsetParent.clientTop;
      } else if (documentElement) {
        setLeftRTLScrollbarOffset();
      }
    }
    if (isFixed && !isOffsetParentAnElement && documentElement) {
      setLeftRTLScrollbarOffset();
    }
    const htmlOffset = documentElement && !isOffsetParentAnElement && !isFixed ? getHTMLOffset(documentElement, scroll) : createCoords(0);
    const x2 = rect.left + scroll.scrollLeft - offsets.x - htmlOffset.x;
    const y3 = rect.top + scroll.scrollTop - offsets.y - htmlOffset.y;
    return {
      x: x2,
      y: y3,
      width: rect.width,
      height: rect.height
    };
  }
  function isStaticPositioned(element) {
    return getComputedStyle2(element).position === "static";
  }
  function getTrueOffsetParent(element, polyfill) {
    if (!isHTMLElement(element) || getComputedStyle2(element).position === "fixed") {
      return null;
    }
    if (polyfill) {
      return polyfill(element);
    }
    let rawOffsetParent = element.offsetParent;
    if (getDocumentElement(element) === rawOffsetParent) {
      rawOffsetParent = rawOffsetParent.ownerDocument.body;
    }
    return rawOffsetParent;
  }
  function getOffsetParent(element, polyfill) {
    const win = getWindow(element);
    if (isTopLayer(element)) {
      return win;
    }
    if (!isHTMLElement(element)) {
      let svgOffsetParent = getParentNode(element);
      while (svgOffsetParent && !isLastTraversableNode(svgOffsetParent)) {
        if (isElement(svgOffsetParent) && !isStaticPositioned(svgOffsetParent)) {
          return svgOffsetParent;
        }
        svgOffsetParent = getParentNode(svgOffsetParent);
      }
      return win;
    }
    let offsetParent = getTrueOffsetParent(element, polyfill);
    while (offsetParent && isTableElement(offsetParent) && isStaticPositioned(offsetParent)) {
      offsetParent = getTrueOffsetParent(offsetParent, polyfill);
    }
    if (offsetParent && isLastTraversableNode(offsetParent) && isStaticPositioned(offsetParent) && !isContainingBlock(offsetParent)) {
      return win;
    }
    return offsetParent || getContainingBlock(element) || win;
  }
  function isRTL(element) {
    return getComputedStyle2(element).direction === "rtl";
  }
  function rectsAreEqual(a4, b3) {
    return a4.x === b3.x && a4.y === b3.y && a4.width === b3.width && a4.height === b3.height;
  }
  function observeMove(element, onMove) {
    let io = null;
    let timeoutId;
    const root = getDocumentElement(element);
    function cleanup() {
      var _io;
      clearTimeout(timeoutId);
      (_io = io) == null || _io.disconnect();
      io = null;
    }
    function refresh(skip, threshold) {
      if (skip === void 0) {
        skip = false;
      }
      if (threshold === void 0) {
        threshold = 1;
      }
      cleanup();
      const elementRectForRootMargin = element.getBoundingClientRect();
      const {
        left,
        top,
        width,
        height
      } = elementRectForRootMargin;
      if (!skip) {
        onMove();
      }
      if (!width || !height) {
        return;
      }
      const insetTop = floor(top);
      const insetRight = floor(root.clientWidth - (left + width));
      const insetBottom = floor(root.clientHeight - (top + height));
      const insetLeft = floor(left);
      const rootMargin = -insetTop + "px " + -insetRight + "px " + -insetBottom + "px " + -insetLeft + "px";
      const options = {
        rootMargin,
        threshold: max(0, min(1, threshold)) || 1
      };
      let isFirstUpdate = true;
      function handleObserve(entries) {
        const ratio = entries[0].intersectionRatio;
        if (ratio !== threshold) {
          if (!isFirstUpdate) {
            return refresh();
          }
          if (!ratio) {
            timeoutId = setTimeout(() => {
              refresh(false, 1e-7);
            }, 1e3);
          } else {
            refresh(false, ratio);
          }
        }
        if (ratio === 1 && !rectsAreEqual(elementRectForRootMargin, element.getBoundingClientRect())) {
          refresh();
        }
        isFirstUpdate = false;
      }
      try {
        io = new IntersectionObserver(handleObserve, {
          ...options,
          // Handle <iframe>s
          root: root.ownerDocument
        });
      } catch (_e) {
        io = new IntersectionObserver(handleObserve, options);
      }
      io.observe(element);
    }
    refresh(true);
    return cleanup;
  }
  function autoUpdate(reference, floating, update, options) {
    if (options === void 0) {
      options = {};
    }
    const {
      ancestorScroll = true,
      ancestorResize = true,
      elementResize = typeof ResizeObserver === "function",
      layoutShift = typeof IntersectionObserver === "function",
      animationFrame = false
    } = options;
    const referenceEl = unwrapElement(reference);
    const ancestors = ancestorScroll || ancestorResize ? [...referenceEl ? getOverflowAncestors(referenceEl) : [], ...floating ? getOverflowAncestors(floating) : []] : [];
    ancestors.forEach((ancestor) => {
      ancestorScroll && ancestor.addEventListener("scroll", update, {
        passive: true
      });
      ancestorResize && ancestor.addEventListener("resize", update);
    });
    const cleanupIo = referenceEl && layoutShift ? observeMove(referenceEl, update) : null;
    let reobserveFrame = -1;
    let resizeObserver = null;
    if (elementResize) {
      resizeObserver = new ResizeObserver((_ref) => {
        let [firstEntry] = _ref;
        if (firstEntry && firstEntry.target === referenceEl && resizeObserver && floating) {
          resizeObserver.unobserve(floating);
          cancelAnimationFrame(reobserveFrame);
          reobserveFrame = requestAnimationFrame(() => {
            var _resizeObserver;
            (_resizeObserver = resizeObserver) == null || _resizeObserver.observe(floating);
          });
        }
        update();
      });
      if (referenceEl && !animationFrame) {
        resizeObserver.observe(referenceEl);
      }
      if (floating) {
        resizeObserver.observe(floating);
      }
    }
    let frameId;
    let prevRefRect = animationFrame ? getBoundingClientRect(reference) : null;
    if (animationFrame) {
      frameLoop();
    }
    function frameLoop() {
      const nextRefRect = getBoundingClientRect(reference);
      if (prevRefRect && !rectsAreEqual(prevRefRect, nextRefRect)) {
        update();
      }
      prevRefRect = nextRefRect;
      frameId = requestAnimationFrame(frameLoop);
    }
    update();
    return () => {
      var _resizeObserver2;
      ancestors.forEach((ancestor) => {
        ancestorScroll && ancestor.removeEventListener("scroll", update);
        ancestorResize && ancestor.removeEventListener("resize", update);
      });
      cleanupIo == null || cleanupIo();
      (_resizeObserver2 = resizeObserver) == null || _resizeObserver2.disconnect();
      resizeObserver = null;
      if (animationFrame) {
        cancelAnimationFrame(frameId);
      }
    };
  }
  var noOffsets, SCROLLBAR_MAX, getElementRects, platform, offset2, shift2, flip2, size2, computePosition2;
  var init_floating_ui_dom = __esm({
    "node_modules/@floating-ui/dom/dist/floating-ui.dom.mjs"() {
      init_floating_ui_core();
      init_floating_ui_utils();
      init_floating_ui_utils_dom();
      noOffsets = /* @__PURE__ */ createCoords(0);
      SCROLLBAR_MAX = 25;
      getElementRects = async function(data) {
        const getOffsetParentFn = this.getOffsetParent || getOffsetParent;
        const getDimensionsFn = this.getDimensions;
        const floatingDimensions = await getDimensionsFn(data.floating);
        return {
          reference: getRectRelativeToOffsetParent(data.reference, await getOffsetParentFn(data.floating), data.strategy),
          floating: {
            x: 0,
            y: 0,
            width: floatingDimensions.width,
            height: floatingDimensions.height
          }
        };
      };
      platform = {
        convertOffsetParentRelativeRectToViewportRelativeRect,
        getDocumentElement,
        getClippingRect,
        getOffsetParent,
        getElementRects,
        getClientRects,
        getDimensions,
        getScale,
        isElement,
        isRTL
      };
      offset2 = offset;
      shift2 = shift;
      flip2 = flip;
      size2 = size;
      computePosition2 = (reference, floating, options) => {
        const cache = /* @__PURE__ */ new Map();
        const mergedOptions = {
          platform,
          ...options
        };
        const platformWithCache = {
          ...mergedOptions.platform,
          _c: cache
        };
        return computePosition(reference, floating, {
          ...mergedOptions,
          platform: platformWithCache
        });
      };
    }
  });

  // node_modules/@nldd/design-system/dist/components/content/tooltip/tooltip.styles.js
  var tooltipStyles;
  var init_tooltip_styles = __esm({
    "node_modules/@nldd/design-system/dist/components/content/tooltip/tooltip.styles.js"() {
      init_lit();
      init_slotted_reset();
      tooltipStyles = i`


	/* # Host */

	:host {
		--_hide-duration: var(--primitives-transition-duration-fast);
		--_show-duration: var(--primitives-transition-duration-fast);
		--_max-width: var(--primitives-area-280);
		--_z-index: 10000;
		--_show-delay: 700ms;
		--_hide-delay: 50; /* unitless ms, read by JavaScript */
		--_offset: 4; /* px, unitless — read by JS */
		--_shift-padding: 8; /* px, unitless — read by JS */

		${inheritedTextReset}
		display: contents;
	}

	:host([hidden]) {
		display: none;
	}


	/* # Block */

	.tooltip {
		position: fixed;
		opacity: 0;
		margin: 0;
		border: none;
		background: none;
		padding: 0;
		transition:
			opacity var(--_hide-duration) ease,
			display var(--_hide-duration) allow-discrete,
			overlay var(--_hide-duration) allow-discrete;
	}

	.tooltip:popover-open {
		opacity: 1;
		transition:
			opacity var(--_show-duration) ease,
			display var(--_show-duration) allow-discrete,
			overlay var(--_show-duration) allow-discrete;
	}

	@starting-style {
		.tooltip:popover-open {
			opacity: 0;
		}
	}

	@media (prefers-reduced-motion: reduce) {
		.tooltip,
		.tooltip.is-visible,
		.tooltip.is-focus-visible {
			transition: none;
		}
	}


	/* ## Tooltip body */

	.tooltip__body {
		border-radius: var(--primitives-corner-radius-xs);
		box-shadow: var(--components-tooltip-box-shadow);
		background-color: var(--components-tooltip-background-color);
		width: max-content;
		max-width: var(--_max-width);
		padding-block: var(--primitives-space-4);
		padding-inline: var(--primitives-space-8);
		color: var(--components-tooltip-content-color);
		font: var(--primitives-font-body-xs-regular-tight);
		overflow-wrap: break-word;
	}

	@media (forced-colors: active) {
		.tooltip__body {
			border: 1px solid CanvasText;
		}
	}
`;
    }
  });

  // node_modules/@nldd/design-system/dist/components/content/tooltip/tooltip.template.js
  function tooltipTemplate(component) {
    return b2`
		<slot
			@mouseenter=${component._handleTriggerEnter}
			@mouseleave=${component._handleTriggerLeave}
			@focusin=${component._handleFocusIn}
			@focusout=${component._handleFocusOut}
		></slot>
		<div class="tooltip"
			popover="manual"
			aria-hidden="true"
			@mouseenter=${component._handleTooltipEnter}
			@mouseleave=${component._handleTooltipLeave}
		>
			<div class="tooltip__body">${component.text}</div>
		</div>
	`;
  }
  var init_tooltip_template = __esm({
    "node_modules/@nldd/design-system/dist/components/content/tooltip/tooltip.template.js"() {
      init_lit();
    }
  });

  // node_modules/@nldd/design-system/dist/utilities/input-modality.js
  function init() {
    if (initialized)
      return;
    initialized = true;
    controller = new AbortController();
    const { signal } = controller;
    document.addEventListener("keydown", (e9) => {
      if (["Tab", "ArrowUp", "ArrowDown", "ArrowLeft", "ArrowRight", "Enter", " ", "Escape"].includes(e9.key)) {
        modality = "keyboard";
      }
    }, { signal });
    document.addEventListener("pointerdown", (e9) => {
      modality = e9.pointerType === "touch" ? "touch" : "mouse";
    }, { signal });
  }
  function getInputModality() {
    init();
    return modality;
  }
  function isKeyboardMode() {
    return getInputModality() === "keyboard";
  }
  function isPointerMode() {
    const m3 = getInputModality();
    return m3 === "mouse" || m3 === "touch";
  }
  function isTouchMode() {
    return getInputModality() === "touch";
  }
  var modality, initialized, controller;
  var init_input_modality = __esm({
    "node_modules/@nldd/design-system/dist/utilities/input-modality.js"() {
      modality = "mouse";
      initialized = false;
      controller = null;
    }
  });

  // node_modules/@nldd/design-system/dist/components/content/tooltip/tooltip.js
  var __decorate4, tooltipCounter, coarsePointerQuery, DEFAULT_SHOW_DELAY_MS, DEFAULT_HIDE_DELAY_MS, NLDDTooltip;
  var init_tooltip = __esm({
    "node_modules/@nldd/design-system/dist/components/content/tooltip/tooltip.js"() {
      init_lit();
      init_decorators();
      init_floating_ui_dom();
      init_tooltip_styles();
      init_tooltip_template();
      init_input_modality();
      __decorate4 = function(decorators, target, key, desc) {
        var c6 = arguments.length, r6 = c6 < 3 ? target : desc === null ? desc = Object.getOwnPropertyDescriptor(target, key) : desc, d3;
        if (typeof Reflect === "object" && typeof Reflect.decorate === "function") r6 = Reflect.decorate(decorators, target, key, desc);
        else for (var i8 = decorators.length - 1; i8 >= 0; i8--) if (d3 = decorators[i8]) r6 = (c6 < 3 ? d3(r6) : c6 > 3 ? d3(target, key, r6) : d3(target, key)) || r6;
        return c6 > 3 && r6 && Object.defineProperty(target, key, r6), r6;
      };
      tooltipCounter = 0;
      coarsePointerQuery = matchMedia("(pointer: coarse)");
      DEFAULT_SHOW_DELAY_MS = 700;
      DEFAULT_HIDE_DELAY_MS = 50;
      NLDDTooltip = class NLDDTooltip2 extends i4 {
        constructor() {
          super(...arguments);
          this.text = "";
          this.open = false;
          this.placement = "bottom";
          this.timing = "default";
          this._visible = false;
          this._tooltipId = `nldd-tooltip-${++tooltipCounter}`;
          this._hideTimeout = null;
          this._showTimeout = null;
          this._descriptionEl = null;
          this._currentTrigger = null;
          this._boundSlotChange = () => this._syncAriaDescribedBy();
          this._positionVersion = 0;
          this._handleKeyDown = (e9) => {
            if (e9.key !== "Escape" || !this._visible)
              return;
            if (this.open) {
              this.dispatchEvent(new CustomEvent("nldd-tooltip-dismiss", { bubbles: true, composed: true }));
              e9.preventDefault();
              return;
            }
            this._visible = false;
          };
        }
        get _effectivePlacement() {
          return this.placement;
        }
        connectedCallback() {
          super.connectedCallback();
          this.addEventListener("keydown", this._handleKeyDown);
          if (this.hasUpdated) {
            this.shadowRoot?.querySelector("slot")?.addEventListener("slotchange", this._boundSlotChange);
          }
        }
        firstUpdated() {
          this.shadowRoot?.querySelector("slot")?.addEventListener("slotchange", this._boundSlotChange);
        }
        updated(changed) {
          if (changed.has("open")) {
            if (this.open) {
              if (this._hideTimeout) {
                clearTimeout(this._hideTimeout);
                this._hideTimeout = null;
              }
              if (this._showTimeout) {
                clearTimeout(this._showTimeout);
                this._showTimeout = null;
              }
              this._visible = true;
            } else if (this._visible) {
              this._visible = false;
            }
          }
          if (changed.has("_visible")) {
            const tooltip = this._getTooltipElement();
            if (tooltip) {
              if (this._visible) {
                if (!tooltip.matches(":popover-open"))
                  tooltip.showPopover();
                this._updatePosition();
              } else if (tooltip.matches(":popover-open")) {
                tooltip.hidePopover();
              }
            }
          }
          if (changed.has("text") || changed.has("timing")) {
            this._syncAriaDescribedBy();
          }
          if (changed.has("text") && this._visible) {
            this._updatePosition();
          }
          if (changed.has("timing") && this.timing === "never") {
            if (this._showTimeout) {
              clearTimeout(this._showTimeout);
              this._showTimeout = null;
            }
            if (this._visible)
              this._visible = false;
          }
        }
        _syncAriaDescribedBy() {
          const trigger = this._getTriggerElement();
          if (this._currentTrigger && this._currentTrigger !== trigger) {
            this._currentTrigger.removeAttribute("aria-describedby");
          }
          this._currentTrigger = trigger;
          if (!trigger)
            return;
          if (trigger.getRootNode() !== document) {
            return;
          }
          if (this.text && this.timing !== "never") {
            if (!this._descriptionEl) {
              this._descriptionEl = document.createElement("span");
              this._descriptionEl.id = this._tooltipId;
              Object.assign(this._descriptionEl.style, {
                position: "absolute",
                width: "1px",
                height: "1px",
                overflow: "hidden",
                clipPath: "inset(50%)",
                whiteSpace: "nowrap"
              });
              document.body.appendChild(this._descriptionEl);
            }
            this._descriptionEl.textContent = this.text;
            trigger.setAttribute("aria-describedby", this._tooltipId);
          } else {
            trigger.removeAttribute("aria-describedby");
            this._descriptionEl?.remove();
            this._descriptionEl = null;
          }
        }
        _getTriggerElement() {
          const slot = this.shadowRoot?.querySelector("slot");
          const assigned = slot?.assignedElements({ flatten: true });
          return assigned?.[0] ?? null;
        }
        _getTooltipElement() {
          return this.shadowRoot?.querySelector(".tooltip") ?? null;
        }
        _handleTriggerEnter() {
          if (this.timing === "never")
            return;
          if (!this.text)
            return;
          if (coarsePointerQuery.matches)
            return;
          if (this._hideTimeout) {
            clearTimeout(this._hideTimeout);
            this._hideTimeout = null;
          }
          if (this._showTimeout)
            clearTimeout(this._showTimeout);
          if (this.timing === "instant") {
            this._showTimeout = null;
            this._visible = true;
            return;
          }
          const parsedShow = parseInt(getComputedStyle(this).getPropertyValue("--_show-delay"), 10);
          const showDelay = Number.isFinite(parsedShow) ? parsedShow : DEFAULT_SHOW_DELAY_MS;
          this._showTimeout = setTimeout(() => {
            this._showTimeout = null;
            this._visible = true;
          }, showDelay);
        }
        _handleFocusIn() {
          if (this.timing === "never")
            return;
          if (!this.text)
            return;
          if (isTouchMode())
            return;
          if (this._hideTimeout) {
            clearTimeout(this._hideTimeout);
            this._hideTimeout = null;
          }
          if (this._showTimeout) {
            clearTimeout(this._showTimeout);
            this._showTimeout = null;
          }
          this._visible = true;
        }
        _handleTriggerLeave() {
          if (this._showTimeout) {
            clearTimeout(this._showTimeout);
            this._showTimeout = null;
          }
          if (this.open)
            return;
          if (this._hideTimeout) {
            clearTimeout(this._hideTimeout);
          }
          const parsedHide = parseInt(getComputedStyle(this).getPropertyValue("--_hide-delay"), 10);
          const hideDelay = Number.isFinite(parsedHide) ? parsedHide : DEFAULT_HIDE_DELAY_MS;
          this._hideTimeout = setTimeout(() => {
            this._visible = false;
            this._hideTimeout = null;
          }, hideDelay);
        }
        /** Focus guard checks one shadow root level deep for composite triggers. */
        _handleFocusOut(e9) {
          const slot = this.shadowRoot?.querySelector("slot");
          const assigned = slot?.assignedElements({ flatten: true }) ?? [];
          const related = e9.relatedTarget;
          const stillInside = related && assigned.some((el) => el.contains(related) || el.shadowRoot?.contains(related));
          if (!stillInside)
            this._handleTriggerLeave();
        }
        _handleTooltipEnter() {
          if (this._hideTimeout) {
            clearTimeout(this._hideTimeout);
            this._hideTimeout = null;
          }
        }
        _handleTooltipLeave() {
          this._handleTriggerLeave();
        }
        async _updatePosition() {
          const version = ++this._positionVersion;
          const trigger = this._getTriggerElement();
          const tooltip = this._getTooltipElement();
          if (!trigger || !tooltip)
            return;
          if (document.fonts?.status !== "loaded") {
            await document.fonts?.ready;
            if (version !== this._positionVersion)
              return;
          }
          const styles = getComputedStyle(this);
          const { x: x2, y: y3 } = await computePosition2(trigger, tooltip, {
            placement: this._effectivePlacement,
            strategy: "fixed",
            middleware: [
              offset2(parseInt(styles.getPropertyValue("--_offset"), 10)),
              flip2(),
              shift2({ padding: parseInt(styles.getPropertyValue("--_shift-padding"), 10) })
            ]
          });
          if (version !== this._positionVersion)
            return;
          tooltip.style.left = `${x2}px`;
          tooltip.style.top = `${y3}px`;
        }
        disconnectedCallback() {
          super.disconnectedCallback();
          this.removeEventListener("keydown", this._handleKeyDown);
          this.shadowRoot?.querySelector("slot")?.removeEventListener("slotchange", this._boundSlotChange);
          if (this._hideTimeout) {
            clearTimeout(this._hideTimeout);
            this._hideTimeout = null;
          }
          if (this._showTimeout) {
            clearTimeout(this._showTimeout);
            this._showTimeout = null;
          }
          this._currentTrigger?.removeAttribute("aria-describedby");
          this._currentTrigger = null;
          this._descriptionEl?.remove();
          this._descriptionEl = null;
        }
        render() {
          return tooltipTemplate(this);
        }
      };
      NLDDTooltip.styles = tooltipStyles;
      __decorate4([
        n4({ type: String, reflect: true })
      ], NLDDTooltip.prototype, "text", void 0);
      __decorate4([
        n4({ type: Boolean, reflect: true })
      ], NLDDTooltip.prototype, "open", void 0);
      __decorate4([
        n4({ type: String, reflect: true })
      ], NLDDTooltip.prototype, "placement", void 0);
      __decorate4([
        n4({ type: String, reflect: true })
      ], NLDDTooltip.prototype, "timing", void 0);
      __decorate4([
        r5()
      ], NLDDTooltip.prototype, "_visible", void 0);
      NLDDTooltip = __decorate4([
        t3("nldd-tooltip")
      ], NLDDTooltip);
    }
  });

  // node_modules/@nldd/design-system/dist/components/actions/icon-button/icon-button.template.js
  function renderContent2(component) {
    return b2`
		<span class="icon-button__icon-area">
			<span class="icon-button__icon">
				${component.icon ? b2`<nldd-icon name=${component.icon}></nldd-icon>` : b2`<slot name="icon" @slotchange=${component._onIconSlotChange}>
							<nldd-icon name="icon-placeholder"></nldd-icon>
						</slot>`}
			</span>
			${component.expandable ? b2`
				<span class="icon-button__disclosure-icon">
					<nldd-icon name="chevron-down-small"></nldd-icon>
				</span>
			` : A}
		</span>
		${component.text ? b2`
			<span class="icon-button__text">${component.text}</span>
		` : ""}
	`;
  }
  function template3() {
    const label = this.accessibleLabel || this.text || A;
    const content = renderContent2(this);
    const tooltipText = this.accessibleLabel || (this.size !== "lg" || this.hideLgText ? this.text : "");
    const isDisclosure = this.expandable || !!this.popupType;
    const ariaExpanded = isDisclosure ? String(this.expanded) : this.expanded ? "true" : A;
    const ariaBusy = this.loading ? "true" : A;
    const loadingSize = this.size === "xs" ? "16" : this.size === "sm" ? "20" : this.size === "lg" ? "28" : "24";
    const loadingIndicator = this.loading ? b2`
			<div class="icon-button__activity-indicator">
				<nldd-activity-indicator
					timing="instant"
					size=${loadingSize}
				></nldd-activity-indicator>
			</div>
		` : A;
    const renderButton = () => {
      if (this.href) {
        const resolvedRel = this._resolvedRel();
        return b2`
				<a class="icon-button"
					href=${this.href}
					target=${this.target || A}
					rel=${resolvedRel || A}
					aria-disabled=${this.disabled ? "true" : A}
					aria-label=${label}
					aria-haspopup=${this.popupType || A}
					aria-expanded=${ariaExpanded}
					aria-busy=${ariaBusy}
					@click=${this._handleClick}
				>
					${content}
				</a>
			`;
      }
      return b2`
			<button class="icon-button"
				type=${this.type}
				?disabled=${this.disabled}
				aria-disabled=${this.disabled ? "true" : A}
				aria-label=${label}
				aria-haspopup=${this.popupType || A}
				aria-expanded=${ariaExpanded}
				aria-busy=${ariaBusy}
				popovertarget=${this.popovertarget || A}
				.popoverTargetElement=${this.popoverTargetElement}
				.popoverTargetAction=${this.popoverTargetAction}
				@click=${this._handleClick}
			>
				${content}
			</button>
		`;
    };
    const control = tooltipText && this.tooltipTiming !== "never" ? b2`
			<nldd-tooltip text=${tooltipText} timing=${this.tooltipTiming}>
				${renderButton()}
			</nldd-tooltip>
		` : renderButton();
    return b2`${control}${loadingIndicator}`;
  }
  var init_icon_button_template = __esm({
    "node_modules/@nldd/design-system/dist/components/actions/icon-button/icon-button.template.js"() {
      init_lit();
      init_tooltip();
    }
  });

  // node_modules/@nldd/design-system/dist/components/actions/icon-button/icon-button.js
  var import_meta3, __decorate5, NLDDIconButton;
  var init_icon_button = __esm({
    "node_modules/@nldd/design-system/dist/components/actions/icon-button/icon-button.js"() {
      init_lit();
      init_decorators();
      init_icon_button_styles();
      init_icon_button_template();
      init_icon();
      init_activity_indicator();
      import_meta3 = {};
      __decorate5 = function(decorators, target, key, desc) {
        var c6 = arguments.length, r6 = c6 < 3 ? target : desc === null ? desc = Object.getOwnPropertyDescriptor(target, key) : desc, d3;
        if (typeof Reflect === "object" && typeof Reflect.decorate === "function") r6 = Reflect.decorate(decorators, target, key, desc);
        else for (var i8 = decorators.length - 1; i8 >= 0; i8--) if (d3 = decorators[i8]) r6 = (c6 < 3 ? d3(r6) : c6 > 3 ? d3(target, key, r6) : d3(target, key)) || r6;
        return c6 > 3 && r6 && Object.defineProperty(target, key, r6), r6;
      };
      NLDDIconButton = class NLDDIconButton2 extends i4 {
        constructor() {
          super(...arguments);
          this._internals = this.attachInternals();
          this.variant = "neutral-tinted";
          this.size = "md";
          this.hideLgText = false;
          this.loading = false;
          this.disabled = false;
          this.noHighlightBorder = false;
          this.type = "button";
          this.expandable = false;
          this.expanded = false;
          this.width = "";
          this.popovertarget = void 0;
          this.popoverTargetElement = null;
          this.popoverTargetAction = "toggle";
          this.text = "";
          this.icon = "";
          this.accessibleLabel = "";
          this.tooltipTiming = "default";
          this.href = void 0;
          this.target = void 0;
          this.rel = void 0;
          this._warnedA11y = false;
        }
        /** Whether an icon is present via attribute or slot. */
        get _hasIcon() {
          if (this.icon)
            return true;
          const slot = this.shadowRoot?.querySelector('slot[name="icon"]');
          return (slot?.assignedElements().length ?? 0) > 0;
        }
        updated(changedProperties) {
          if (changedProperties.has("width")) {
            const w2 = this.width;
            const isFull = w2 === "full";
            const isValidLength = !!w2 && !isFull && CSS.supports("width", w2);
            this.style.width = isValidLength ? w2 : "";
            if (isFull || isValidLength) {
              this.style.setProperty("--_width", "100%");
            } else {
              this.style.removeProperty("--_width");
            }
          }
          const inaccessible = this._hasIcon && !this.text && !this.accessibleLabel;
          if (import_meta3.env?.DEV && inaccessible && !this._warnedA11y) {
            this._warnedA11y = true;
            console.warn("<nldd-icon-button>: icon is set without text or accessible-label. This produces an inaccessible button (WCAG SC 4.1.2). Add a text or accessible-label attribute.");
          } else if (!inaccessible) {
            this._warnedA11y = false;
          }
        }
        /** Resolves the effective rel value for link rendering. */
        _resolvedRel() {
          if (this.rel)
            return this.rel;
          if (this.target === "_blank")
            return "noopener noreferrer";
          return "";
        }
        _handleClick(e9) {
          if (this.disabled || this.loading) {
            e9.preventDefault();
            e9.stopPropagation();
            return;
          }
          if (this.href)
            return;
          if (this.type === "submit")
            this._internals.form?.requestSubmit();
          else if (this.type === "reset")
            this._internals.form?.reset();
        }
        /**
         * Delegates focus to the inner `<button>` (or `<a>` when `href` is set), so
         * consumers can call `iconButtonEl.focus()` without reaching into shadow DOM.
         */
        focus(options) {
          this.shadowRoot?.querySelector(".icon-button")?.focus(options);
        }
        // Stable reference (not an inline arrow) so Lit does not re-add the slot
        // listener every render; mirrors the other @slotchange handlers.
        _onIconSlotChange() {
          this.requestUpdate();
        }
        render() {
          return template3.call(this);
        }
      };
      NLDDIconButton.styles = iconButtonStyles;
      NLDDIconButton.formAssociated = true;
      __decorate5([
        n4({ type: String, reflect: true })
      ], NLDDIconButton.prototype, "variant", void 0);
      __decorate5([
        n4({ type: String, reflect: true })
      ], NLDDIconButton.prototype, "size", void 0);
      __decorate5([
        n4({ type: Boolean, reflect: true, attribute: "hide-lg-text" })
      ], NLDDIconButton.prototype, "hideLgText", void 0);
      __decorate5([
        n4({ type: Boolean, reflect: true })
      ], NLDDIconButton.prototype, "loading", void 0);
      __decorate5([
        n4({ type: Boolean, reflect: true })
      ], NLDDIconButton.prototype, "disabled", void 0);
      __decorate5([
        n4({ type: Boolean, reflect: true, attribute: "no-highlight-border" })
      ], NLDDIconButton.prototype, "noHighlightBorder", void 0);
      __decorate5([
        n4({ type: String, reflect: true })
      ], NLDDIconButton.prototype, "type", void 0);
      __decorate5([
        n4({ type: Boolean, reflect: true, attribute: "expandable" })
      ], NLDDIconButton.prototype, "expandable", void 0);
      __decorate5([
        n4({ type: Boolean, reflect: true })
      ], NLDDIconButton.prototype, "expanded", void 0);
      __decorate5([
        n4({ type: String, reflect: true, attribute: "popup-type" })
      ], NLDDIconButton.prototype, "popupType", void 0);
      __decorate5([
        n4({ type: String, reflect: true })
      ], NLDDIconButton.prototype, "width", void 0);
      __decorate5([
        n4({ type: String })
      ], NLDDIconButton.prototype, "popovertarget", void 0);
      __decorate5([
        n4({ attribute: false })
      ], NLDDIconButton.prototype, "popoverTargetElement", void 0);
      __decorate5([
        n4({ attribute: false })
      ], NLDDIconButton.prototype, "popoverTargetAction", void 0);
      __decorate5([
        n4({ type: String })
      ], NLDDIconButton.prototype, "text", void 0);
      __decorate5([
        n4({ type: String })
      ], NLDDIconButton.prototype, "icon", void 0);
      __decorate5([
        n4({ type: String, attribute: "accessible-label" })
      ], NLDDIconButton.prototype, "accessibleLabel", void 0);
      __decorate5([
        n4({ type: String, reflect: true, attribute: "tooltip-timing" })
      ], NLDDIconButton.prototype, "tooltipTiming", void 0);
      __decorate5([
        n4({ type: String, reflect: true })
      ], NLDDIconButton.prototype, "href", void 0);
      __decorate5([
        n4({ type: String })
      ], NLDDIconButton.prototype, "target", void 0);
      __decorate5([
        n4({ type: String })
      ], NLDDIconButton.prototype, "rel", void 0);
      NLDDIconButton = __decorate5([
        t3("nldd-icon-button")
      ], NLDDIconButton);
    }
  });

  // node_modules/@nldd/design-system/dist/assets/styles/breakpoints.js
  var breakpoints;
  var init_breakpoints = __esm({
    "node_modules/@nldd/design-system/dist/assets/styles/breakpoints.js"() {
      breakpoints = {
        smMin: "320px",
        smMax: "640px",
        mdMin: "641px",
        mdMax: "1007px",
        lgMin: "1008px"
      };
    }
  });

  // node_modules/@nldd/design-system/dist/utilities/visibility-mixin.js
  function resolveHideBelow(value) {
    if (value === "md")
      return breakpoints.smMax;
    if (value === "lg")
      return breakpoints.mdMax;
    if (value === "sm")
      return null;
    return value;
  }
  function resolveHideAbove(value) {
    if (value === "sm")
      return breakpoints.mdMin;
    if (value === "md")
      return breakpoints.lgMin;
    if (value === "lg")
      return null;
    return value;
  }
  function VisibilityMixin(Base, containerName) {
    class WithVisibility extends Base {
      updated(changed) {
        super.updated(changed);
        if (changed.has("hideBelow") || changed.has("hideAbove")) {
          this._updateVisibilityStyle();
        }
      }
      _updateVisibilityStyle() {
        if (!this.shadowRoot)
          return;
        const containerPrefix = containerName ? `${containerName} ` : "";
        const rules = [];
        const below = this.hideBelow ? resolveHideBelow(this.hideBelow) : null;
        if (below) {
          rules.push(`:host { @container ${containerPrefix}(max-width: ${below}) { display: none !important; } }`);
        }
        const above = this.hideAbove ? resolveHideAbove(this.hideAbove) : null;
        if (above) {
          rules.push(`:host { @container ${containerPrefix}(min-width: ${above}) { display: none !important; } }`);
        }
        if (rules.length === 0) {
          this._visibilityStyle?.remove();
          this._visibilityStyle = void 0;
          return;
        }
        if (!this._visibilityStyle) {
          this._visibilityStyle = document.createElement("style");
          this.shadowRoot.appendChild(this._visibilityStyle);
        }
        this._visibilityStyle.textContent = rules.join("\n");
      }
    }
    __decorate6([
      n4({ type: String, reflect: true, attribute: "hide-below" })
    ], WithVisibility.prototype, "hideBelow", void 0);
    __decorate6([
      n4({ type: String, reflect: true, attribute: "hide-above" })
    ], WithVisibility.prototype, "hideAbove", void 0);
    return WithVisibility;
  }
  var __decorate6;
  var init_visibility_mixin = __esm({
    "node_modules/@nldd/design-system/dist/utilities/visibility-mixin.js"() {
      init_decorators();
      init_breakpoints();
      __decorate6 = function(decorators, target, key, desc) {
        var c6 = arguments.length, r6 = c6 < 3 ? target : desc === null ? desc = Object.getOwnPropertyDescriptor(target, key) : desc, d3;
        if (typeof Reflect === "object" && typeof Reflect.decorate === "function") r6 = Reflect.decorate(decorators, target, key, desc);
        else for (var i8 = decorators.length - 1; i8 >= 0; i8--) if (d3 = decorators[i8]) r6 = (c6 < 3 ? d3(r6) : c6 > 3 ? d3(target, key, r6) : d3(target, key)) || r6;
        return c6 > 3 && r6 && Object.defineProperty(target, key, r6), r6;
      };
    }
  });

  // node_modules/@nldd/design-system/dist/components/lists-and-tables/cells/spacer-cell/spacer-cell.styles.js
  var spacerCellStyles;
  var init_spacer_cell_styles = __esm({
    "node_modules/@nldd/design-system/dist/components/lists-and-tables/cells/spacer-cell/spacer-cell.styles.js"() {
      init_lit();
      spacerCellStyles = i`


	/* # Host */

	:host {
		--_width: var(--primitives-space-16);

		display: block;
		width: var(--_width);
		flex-grow: 0;
		flex-shrink: 0;
	}

	:host([hidden]) {
		display: none;
	}


	/* # Size */

	:host([size="2"])  { --_width: var(--primitives-space-2); }
	:host([size="4"])  { --_width: var(--primitives-space-4); }
	:host([size="6"])  { --_width: var(--primitives-space-6); }
	:host([size="8"])  { --_width: var(--primitives-space-8); }
	:host([size="10"]) { --_width: var(--primitives-space-10); }
	:host([size="12"]) { --_width: var(--primitives-space-12); }
	:host([size="20"]) { --_width: var(--primitives-space-20); }
	:host([size="24"]) { --_width: var(--primitives-space-24); }
	:host([size="28"]) { --_width: var(--primitives-space-28); }
	:host([size="32"]) { --_width: var(--primitives-space-32); }
	:host([size="40"]) { --_width: var(--primitives-space-40); }
	:host([size="44"]) { --_width: var(--primitives-space-44); }
	:host([size="48"]) { --_width: var(--primitives-space-48); }
	:host([size="56"]) { --_width: var(--primitives-space-56); }
	:host([size="64"]) { --_width: var(--primitives-space-64); }
	:host([size="80"]) { --_width: var(--primitives-space-80); }
	:host([size="96"]) { --_width: var(--primitives-space-96); }

	:host([size="flexible"]) {
		--_width: auto;
		flex-grow: 1;
		flex-shrink: 1;
		flex-basis: 0;
	}
`;
    }
  });

  // node_modules/@nldd/design-system/dist/components/lists-and-tables/cells/spacer-cell/spacer-cell.js
  var __decorate8, NLDDSpacerCell;
  var init_spacer_cell = __esm({
    "node_modules/@nldd/design-system/dist/components/lists-and-tables/cells/spacer-cell/spacer-cell.js"() {
      init_lit();
      init_decorators();
      init_spacer_cell_styles();
      init_visibility_mixin();
      __decorate8 = function(decorators, target, key, desc) {
        var c6 = arguments.length, r6 = c6 < 3 ? target : desc === null ? desc = Object.getOwnPropertyDescriptor(target, key) : desc, d3;
        if (typeof Reflect === "object" && typeof Reflect.decorate === "function") r6 = Reflect.decorate(decorators, target, key, desc);
        else for (var i8 = decorators.length - 1; i8 >= 0; i8--) if (d3 = decorators[i8]) r6 = (c6 < 3 ? d3(r6) : c6 > 3 ? d3(target, key, r6) : d3(target, key)) || r6;
        return c6 > 3 && r6 && Object.defineProperty(target, key, r6), r6;
      };
      NLDDSpacerCell = class NLDDSpacerCell2 extends VisibilityMixin(i4, "cells-container") {
        constructor() {
          super(...arguments);
          this.size = "16";
        }
        render() {
          return null;
        }
      };
      NLDDSpacerCell.styles = spacerCellStyles;
      __decorate8([
        n4({ type: String, reflect: true })
      ], NLDDSpacerCell.prototype, "size", void 0);
      NLDDSpacerCell = __decorate8([
        t3("nldd-spacer-cell")
      ], NLDDSpacerCell);
    }
  });

  // node_modules/@nldd/design-system/dist/components/lists-and-tables/cells/text-cell/text-cell.styles.js
  var textCellStyles;
  var init_text_cell_styles = __esm({
    "node_modules/@nldd/design-system/dist/components/lists-and-tables/cells/text-cell/text-cell.styles.js"() {
      init_lit();
      init_slotted_reset();
      textCellStyles = i`


	/* # Host */

	:host {
		--_width: auto;
		--_min-width: 0;
		--_max-width: none;
		--_min-height: 0;
		--_secondary-color: var(--context-cell-content-color, var(--semantics-content-secondary-color));
		--_secondary-font: var(--primitives-font-body-xs-regular-tight);
		--_text-align: start;
		--_text-color: var(--context-cell-content-color, var(--semantics-content-color));
		--_text-font: var(--primitives-font-body-md-regular-tight);

		${inheritedTextReset}
		display: flex;
		width: var(--_width);
		min-width: var(--_min-width);
		max-width: var(--_max-width);
		min-height: var(--_min-height);
		flex-direction: column;
		justify-content: center;
	}

	:host([hidden]) {
		display: none;
	}


	/* # Width */

	:host([width="full"]),
	:host(:not([width])),
	:host([width=""]) {
		flex-grow: 1;
		flex-shrink: 1;
		flex-basis: 0;
	}

	:host([width="fit-content"]) {
		width: fit-content;
		flex-grow: 0;
		flex-shrink: 0;
		flex-basis: auto;
	}

	:host([width]:not([width="full"]):not([width="fit-content"]):not([width=""])) {
		flex-shrink: 0;
	}

	:host([max-width]) {
		flex-basis: var(--_max-width);
	}


	/* # Vertical alignment */

	/* "center" (default) stretches to the full row height then centers content;
	   use vertical-alignment="top" for strict top without a minimum height */

	:host([vertical-alignment="center"]),
	:host(:not([vertical-alignment])) {
		align-self: stretch;
	}

	:host([vertical-alignment="top"]) {
		align-self: flex-start;
	}

	:host([vertical-alignment="bottom"]) {
		align-self: flex-end;
	}


	/* # Horizontal alignment */

	:host([horizontal-alignment="left"]),
	:host(:not([horizontal-alignment])) {
		align-items: flex-start;
	}

	:host([horizontal-alignment="center"]) {
		--_text-align: center;
		align-items: center;
	}

	:host([horizontal-alignment="right"]) {
		--_text-align: right;
		align-items: flex-end;
	}


	/* # Size */

	:host([size="sm"]) {
		--_secondary-font: var(--primitives-font-body-xxs-regular-tight);
		--_text-font: var(--primitives-font-body-sm-regular-tight);
	}


	/* # Color */

	:host([color="secondary"]) {
		--_text-color: var(--context-cell-content-secondary-color, var(--semantics-content-secondary-color));
	}

	:host([color="accent"]) {
		--_secondary-color: var(--context-cell-content-accent-color, var(--semantics-content-accent-color));
		--_text-color: var(--context-cell-content-accent-color, var(--semantics-content-accent-color));
	}

	:host([color="success"]) {
		--_secondary-color: var(--context-cell-content-success-color, var(--semantics-content-success-color));
		--_text-color: var(--context-cell-content-success-color, var(--semantics-content-success-color));
	}

	:host([color="warning"]) {
		--_secondary-color: var(--context-cell-content-warning-color, var(--semantics-content-warning-color));
		--_text-color: var(--context-cell-content-warning-color, var(--semantics-content-warning-color));
	}

	:host([color="critical"]) {
		--_secondary-color: var(--context-cell-content-critical-color, var(--semantics-content-critical-color));
		--_text-color: var(--context-cell-content-critical-color, var(--semantics-content-critical-color));
	}


	/* # Elements */

	.text-cell__overline {
		margin: 0;
		min-width: 0;
		align-self: stretch;
		text-align: var(--_text-align);
		color: var(--_secondary-color);
		font: var(--_secondary-font);
		overflow-wrap: anywhere;
	}

	.text-cell__text {
		margin: 0;
		min-width: 0;
		align-self: stretch;
		text-align: var(--_text-align);
		color: var(--_text-color);
		font: var(--_text-font);
		overflow-wrap: anywhere;
		text-wrap: pretty;
	}

	@media (forced-colors: active) {
		.text-cell__text {
			forced-color-adjust: none;
		}
	}

	.text-cell__supporting-text {
		margin: 0;
		min-width: 0;
		align-self: stretch;
		text-align: var(--_text-align);
		color: var(--_secondary-color);
		font: var(--_secondary-font);
		overflow-wrap: anywhere;
	}
`;
    }
  });

  // node_modules/@nldd/design-system/dist/utilities/render-marked.js
  function renderBold(text) {
    if (!text.includes("**"))
      return text;
    const parts = text.split(/\*\*(.+?)\*\*/g);
    return b2`${parts.map((part, i8) => i8 % 2 === 1 ? b2`<b>${part}</b>` : part)}`;
  }
  function renderQueryMark(text, query, mode = "predictive") {
    if (!text)
      return A;
    const q = query.trim();
    if (!q)
      return renderBold(text);
    const qLower = q.toLowerCase();
    const textLower = text.toLowerCase();
    if (!textLower.includes(qLower))
      return renderBold(text);
    const segments = [];
    let cursor = 0;
    while (cursor < text.length) {
      const idx = textLower.indexOf(qLower, cursor);
      if (idx === -1) {
        segments.push({ text: text.slice(cursor), bold: mode === "predictive" });
        break;
      }
      if (idx > cursor) {
        segments.push({ text: text.slice(cursor, idx), bold: mode === "predictive" });
      }
      segments.push({ text: text.slice(idx, idx + q.length), bold: mode === "match" });
      cursor = idx + q.length;
    }
    return b2`${segments.map((s6) => s6.bold ? b2`<b>${s6.text}</b>` : s6.text)}`;
  }
  var init_render_marked = __esm({
    "node_modules/@nldd/design-system/dist/utilities/render-marked.js"() {
      init_lit();
    }
  });

  // node_modules/@nldd/design-system/dist/components/lists-and-tables/cells/text-cell/text-cell.template.js
  function template6() {
    return b2`
		<p
			class="text-cell__overline"
			?hidden=${!this.overline && !this._hasOverlineSlotted}
		>
			${this.overline && !this._hasOverlineSlotted ? renderQueryMark(this.overline, this.query, this.queryMarkMode) : A}
			<slot name="overline" @slotchange=${this._onSlotChange}></slot>
		</p>
		<p
			class="text-cell__text"
			?hidden=${!this.text && !this._hasDefaultSlotted}
		>
			${this.text && !this._hasDefaultSlotted ? renderQueryMark(this.text, this.query, this.queryMarkMode) : A}
			<slot @slotchange=${this._onSlotChange}></slot>
		</p>
		<p
			class="text-cell__supporting-text"
			?hidden=${!this.supportingText && !this._hasSupportingTextSlotted}
		>
			${this.supportingText && !this._hasSupportingTextSlotted ? renderQueryMark(this.supportingText, this.query, this.queryMarkMode) : A}
			<slot name="supporting-text" @slotchange=${this._onSlotChange}></slot>
		</p>
	`;
  }
  var init_text_cell_template = __esm({
    "node_modules/@nldd/design-system/dist/components/lists-and-tables/cells/text-cell/text-cell.template.js"() {
      init_lit();
      init_render_marked();
    }
  });

  // node_modules/@nldd/design-system/dist/components/lists-and-tables/cells/text-cell/text-cell.js
  var text_cell_exports = {};
  __export(text_cell_exports, {
    NLDDTextCell: () => NLDDTextCell
  });
  var __decorate9, NLDDTextCell;
  var init_text_cell = __esm({
    "node_modules/@nldd/design-system/dist/components/lists-and-tables/cells/text-cell/text-cell.js"() {
      init_lit();
      init_decorators();
      init_text_cell_styles();
      init_text_cell_template();
      init_visibility_mixin();
      __decorate9 = function(decorators, target, key, desc) {
        var c6 = arguments.length, r6 = c6 < 3 ? target : desc === null ? desc = Object.getOwnPropertyDescriptor(target, key) : desc, d3;
        if (typeof Reflect === "object" && typeof Reflect.decorate === "function") r6 = Reflect.decorate(decorators, target, key, desc);
        else for (var i8 = decorators.length - 1; i8 >= 0; i8--) if (d3 = decorators[i8]) r6 = (c6 < 3 ? d3(r6) : c6 > 3 ? d3(target, key, r6) : d3(target, key)) || r6;
        return c6 > 3 && r6 && Object.defineProperty(target, key, r6), r6;
      };
      NLDDTextCell = class NLDDTextCell2 extends VisibilityMixin(i4, "cells-container") {
        constructor() {
          super(...arguments);
          this.size = "md";
          this.color = "default";
          this.width = "full";
          this.horizontalAlignment = "left";
          this.verticalAlignment = "center";
          this.text = "";
          this.overline = "";
          this.supportingText = "";
          this.query = "";
          this.queryMarkMode = "predictive";
          this._hasOverlineSlotted = false;
          this._hasDefaultSlotted = false;
          this._hasSupportingTextSlotted = false;
          this._onSlotChange = (e9) => {
            const slot = e9.target;
            const hasContent = slot.assignedNodes().some((node) => {
              if (node.nodeType === Node.TEXT_NODE) {
                return (node.textContent ?? "").trim().length > 0;
              }
              return node.nodeType === Node.ELEMENT_NODE;
            });
            switch (slot.name) {
              case "overline":
                this._hasOverlineSlotted = hasContent;
                break;
              case "supporting-text":
                this._hasSupportingTextSlotted = hasContent;
                break;
              default:
                this._hasDefaultSlotted = hasContent;
                break;
            }
          };
        }
        updated(changed) {
          super.updated(changed);
          if (changed.has("width") || changed.has("minWidth") || changed.has("maxWidth") || changed.has("minHeight")) {
            this._applyDimensionStyles();
          }
        }
        _applyDimensionStyles() {
          const w2 = this.width;
          const widthIsKeyword = w2 === "full" || w2 === "fit-content";
          const widthIsValidLength = !!w2 && !widthIsKeyword && CSS.supports("width", w2);
          if (widthIsValidLength) {
            this.style.setProperty("--_width", w2);
          } else {
            this.style.removeProperty("--_width");
          }
          if (w2 && !widthIsKeyword && !widthIsValidLength) {
            this.width = "";
          }
          if (this.minWidth) {
            this.style.setProperty("--_min-width", this.minWidth);
          } else {
            this.style.removeProperty("--_min-width");
          }
          if (this.maxWidth) {
            this.style.setProperty("--_max-width", this.maxWidth);
          } else {
            this.style.removeProperty("--_max-width");
          }
          if (this.minHeight) {
            this.style.setProperty("--_min-height", this.minHeight);
          } else {
            this.style.removeProperty("--_min-height");
          }
        }
        render() {
          return template6.call(this);
        }
      };
      NLDDTextCell.styles = [textCellStyles];
      __decorate9([
        n4({ type: String, reflect: true })
      ], NLDDTextCell.prototype, "size", void 0);
      __decorate9([
        n4({ type: String, reflect: true })
      ], NLDDTextCell.prototype, "color", void 0);
      __decorate9([
        n4({ type: String, reflect: true })
      ], NLDDTextCell.prototype, "width", void 0);
      __decorate9([
        n4({ type: String, reflect: true, attribute: "min-width" })
      ], NLDDTextCell.prototype, "minWidth", void 0);
      __decorate9([
        n4({ type: String, reflect: true, attribute: "max-width" })
      ], NLDDTextCell.prototype, "maxWidth", void 0);
      __decorate9([
        n4({ type: String, reflect: true, attribute: "min-height" })
      ], NLDDTextCell.prototype, "minHeight", void 0);
      __decorate9([
        n4({ type: String, reflect: true, attribute: "horizontal-alignment" })
      ], NLDDTextCell.prototype, "horizontalAlignment", void 0);
      __decorate9([
        n4({ type: String, reflect: true, attribute: "vertical-alignment" })
      ], NLDDTextCell.prototype, "verticalAlignment", void 0);
      __decorate9([
        n4({ type: String })
      ], NLDDTextCell.prototype, "text", void 0);
      __decorate9([
        n4({ type: String })
      ], NLDDTextCell.prototype, "overline", void 0);
      __decorate9([
        n4({ type: String, attribute: "supporting-text" })
      ], NLDDTextCell.prototype, "supportingText", void 0);
      __decorate9([
        n4({ type: String })
      ], NLDDTextCell.prototype, "query", void 0);
      __decorate9([
        n4({ type: String, attribute: "query-mark-mode" })
      ], NLDDTextCell.prototype, "queryMarkMode", void 0);
      __decorate9([
        r5()
      ], NLDDTextCell.prototype, "_hasOverlineSlotted", void 0);
      __decorate9([
        r5()
      ], NLDDTextCell.prototype, "_hasDefaultSlotted", void 0);
      __decorate9([
        r5()
      ], NLDDTextCell.prototype, "_hasSupportingTextSlotted", void 0);
      NLDDTextCell = __decorate9([
        t3("nldd-text-cell")
      ], NLDDTextCell);
    }
  });

  // node_modules/@nldd/design-system/dist/components/status-and-feedback/inline-dialog/inline-dialog.styles.js
  var inlineDialogStyles;
  var init_inline_dialog_styles = __esm({
    "node_modules/@nldd/design-system/dist/components/status-and-feedback/inline-dialog/inline-dialog.styles.js"() {
      init_lit();
      init_slotted_reset();
      inlineDialogStyles = i`


	/* # Host */

	:host {
		--_icon-size: var(--primitives-space-40);
		--_icon-color: var(--components-inline-dialog-icon-color);
		--_text-font: var(--primitives-font-body-md-bold-tight);
		--_supporting-text-font: var(--primitives-font-body-sm-regular-tight);

		${inheritedTextReset}
		display: flex;
		flex-grow: 1;
		align-items: center;
		justify-content: center;
	}

	:host([size="lg"]) {
		--_icon-size: var(--primitives-space-48);
		--_text-font: var(--primitives-font-body-lg-bold-tight);
		--_supporting-text-font: var(--primitives-font-body-md-regular-tight);
	}

	:host([variant="alert"]) {
		--_icon-color: var(--components-inline-dialog-icon-warning-color);
	}

	:host([variant="success"]) {
		--_icon-color: var(--components-inline-dialog-icon-success-color);
	}

	:host([icon-color="secondary"]) {
		--_icon-color: var(--components-inline-dialog-icon-secondary-color);
	}

	:host([icon-color="accent"]) {
		--_icon-color: var(--components-inline-dialog-icon-accent-color);
	}

	:host([icon-color="critical"]) {
		--_icon-color: var(--components-inline-dialog-icon-critical-color);
	}

	:host([icon-color="warning"]) {
		--_icon-color: var(--components-inline-dialog-icon-warning-color);
	}

	:host([icon-color="success"]) {
		--_icon-color: var(--components-inline-dialog-icon-success-color);
	}

	:host([hidden]) {
		display: none;
	}


	/* # Elements */

	.inline-dialog__body {
		box-sizing: border-box;
		display: flex;
		max-width: var(--primitives-area-480);
		flex-direction: column;
		flex-grow: 1;
		align-items: center;
	}

	.inline-dialog__main {
		display: flex;
		width: 100%;
		flex-direction: column;
		align-items: center;
		gap: var(--primitives-space-2);
	}

	.inline-dialog__icon {
		display: flex;
		width: var(--_icon-size);
		height: var(--_icon-size);
		flex-shrink: 0;
		align-items: center;
		justify-content: center;
		color: var(--_icon-color);
	}

	.inline-dialog__text {
		margin: 0;
		text-align: center;
		color: var(--semantics-content-color);
		font: var(--_text-font);
		text-wrap: pretty;
	}

	.inline-dialog__text:focus-visible {
		outline: none;
		box-shadow: none;
	}

	.inline-dialog__supporting-text {
		margin: 0;
		text-align: center;
		color: var(--semantics-content-color);
		font: var(--_supporting-text-font);
		text-wrap: pretty;
	}

	.inline-dialog__content {
		width: 100%;
	}

	.inline-dialog__content[hidden] {
		display: none;
	}

	.inline-dialog__footer {
		width: 100%;
		padding-top: var(--primitives-space-16);
	}

	.inline-dialog__footer[hidden] {
		display: none;
	}
`;
    }
  });

  // node_modules/@nldd/design-system/dist/components/status-and-feedback/inline-dialog/inline-dialog.template.js
  function inlineDialogTemplate(component) {
    return b2`
		<div class="inline-dialog__body">
			<div class="inline-dialog__main">
				${component._resolvedIconName ? b2`
					<div class="inline-dialog__icon">
						<nldd-icon name=${component._resolvedIconName}></nldd-icon>
					</div>
				` : A}
				${component.text ? b2`
					${component.headingLevel === 1 ? b2`<h1 class="inline-dialog__text">${component.text}</h1>` : component.headingLevel === 2 ? b2`<h2 class="inline-dialog__text">${component.text}</h2>` : component.headingLevel === 3 ? b2`<h3 class="inline-dialog__text">${component.text}</h3>` : component.headingLevel === 4 ? b2`<h4 class="inline-dialog__text">${component.text}</h4>` : component.headingLevel === 5 ? b2`<h5 class="inline-dialog__text">${component.text}</h5>` : component.headingLevel === 6 ? b2`<h6 class="inline-dialog__text">${component.text}</h6>` : b2`<p class="inline-dialog__text">${component.text}</p>`}
				` : A}
				${component.supportingText ? b2`
					<p class="inline-dialog__supporting-text">${component.supportingText}</p>
				` : A}
				<div class="inline-dialog__content" ?hidden=${!component._hasContent}>
					<slot></slot>
				</div>
			</div>
			<div class="inline-dialog__footer" ?hidden=${!component._hasActions}>
				<nldd-button-group orientation="vertical">
					<slot name="actions"></slot>
				</nldd-button-group>
			</div>
		</div>
	`;
  }
  var init_inline_dialog_template = __esm({
    "node_modules/@nldd/design-system/dist/components/status-and-feedback/inline-dialog/inline-dialog.template.js"() {
      init_lit();
    }
  });

  // node_modules/@nldd/design-system/dist/components/actions/button-group/button-group.styles.js
  var buttonGroupStyles;
  var init_button_group_styles = __esm({
    "node_modules/@nldd/design-system/dist/components/actions/button-group/button-group.styles.js"() {
      init_lit();
      buttonGroupStyles = i`


	/* # Host */

	:host {
		--_width: 100%;
		--_flex-direction: column;
		--_flex-wrap: nowrap;
		--_gap: var(--components-button-group-md-gap);

		display: flex;
		width: 100%;
		justify-content: flex-start;
	}

	:host([size="sm"]) {
		--_gap: var(--components-button-group-sm-gap);
	}

	/* ## Horizontal */

	:host([orientation="horizontal"]) {
		--_width: auto;
		--_flex-direction: row;
		--_flex-wrap: wrap;
	}

	:host([hidden]) {
		display: none;
	}

	::slotted([hidden]) {
		display: none !important;
	}


	/* # Block */

	.button-group {
		display: flex;
		width: var(--_width);
		flex-direction: var(--_flex-direction);
		flex-wrap: var(--_flex-wrap);
		gap: var(--_gap);
	}
`;
    }
  });

  // node_modules/@nldd/design-system/dist/components/actions/button-group/button-group.template.js
  function template7() {
    return b2`
	<div class="button-group">
		<slot @slotchange=${this.handleSlotChange}></slot>
	</div>
	`;
  }
  var init_button_group_template = __esm({
    "node_modules/@nldd/design-system/dist/components/actions/button-group/button-group.template.js"() {
      init_lit();
    }
  });

  // node_modules/@nldd/design-system/dist/components/actions/button-group/button-group.js
  var import_meta4, __decorate10, NLDDButtonGroup;
  var init_button_group = __esm({
    "node_modules/@nldd/design-system/dist/components/actions/button-group/button-group.js"() {
      init_lit();
      init_decorators();
      init_button_group_styles();
      init_button_group_template();
      import_meta4 = {};
      __decorate10 = function(decorators, target, key, desc) {
        var c6 = arguments.length, r6 = c6 < 3 ? target : desc === null ? desc = Object.getOwnPropertyDescriptor(target, key) : desc, d3;
        if (typeof Reflect === "object" && typeof Reflect.decorate === "function") r6 = Reflect.decorate(decorators, target, key, desc);
        else for (var i8 = decorators.length - 1; i8 >= 0; i8--) if (d3 = decorators[i8]) r6 = (c6 < 3 ? d3(r6) : c6 > 3 ? d3(target, key, r6) : d3(target, key)) || r6;
        return c6 > 3 && r6 && Object.defineProperty(target, key, r6), r6;
      };
      NLDDButtonGroup = class NLDDButtonGroup2 extends i4 {
        constructor() {
          super(...arguments);
          this.size = "md";
          this.orientation = "vertical";
        }
        handleSlotChange() {
          const assigned = this._slot.assignedElements({ flatten: true }).filter((el) => el instanceof HTMLElement);
          assigned.forEach((el, index) => {
            if (index >= 3) {
              el.setAttribute("hidden", "");
              if (import_meta4.env?.DEV)
                console.warn("nldd-button-group: Only 3 buttons are allowed. Extra buttons will be hidden.");
            }
            if (this.orientation === "vertical") {
              el.setAttribute("width", "full");
            } else {
              el.removeAttribute("width");
            }
            el.setAttribute("size", this.size);
          });
        }
        updated(changedProperties) {
          if (changedProperties.has("orientation") || changedProperties.has("size")) {
            this.handleSlotChange();
          }
        }
        render() {
          return template7.call(this);
        }
      };
      NLDDButtonGroup.styles = buttonGroupStyles;
      __decorate10([
        n4({ type: String, reflect: true })
      ], NLDDButtonGroup.prototype, "size", void 0);
      __decorate10([
        n4({ type: String, reflect: true })
      ], NLDDButtonGroup.prototype, "orientation", void 0);
      __decorate10([
        e5("slot")
      ], NLDDButtonGroup.prototype, "_slot", void 0);
      NLDDButtonGroup = __decorate10([
        t3("nldd-button-group")
      ], NLDDButtonGroup);
    }
  });

  // node_modules/@nldd/design-system/dist/components/status-and-feedback/inline-dialog/inline-dialog.js
  var __decorate11, NLDDInlineDialog;
  var init_inline_dialog = __esm({
    "node_modules/@nldd/design-system/dist/components/status-and-feedback/inline-dialog/inline-dialog.js"() {
      init_lit();
      init_decorators();
      init_inline_dialog_styles();
      init_inline_dialog_template();
      init_icon();
      init_button_group();
      __decorate11 = function(decorators, target, key, desc) {
        var c6 = arguments.length, r6 = c6 < 3 ? target : desc === null ? desc = Object.getOwnPropertyDescriptor(target, key) : desc, d3;
        if (typeof Reflect === "object" && typeof Reflect.decorate === "function") r6 = Reflect.decorate(decorators, target, key, desc);
        else for (var i8 = decorators.length - 1; i8 >= 0; i8--) if (d3 = decorators[i8]) r6 = (c6 < 3 ? d3(r6) : c6 > 3 ? d3(target, key, r6) : d3(target, key)) || r6;
        return c6 > 3 && r6 && Object.defineProperty(target, key, r6), r6;
      };
      NLDDInlineDialog = class NLDDInlineDialog2 extends i4 {
        constructor() {
          super(...arguments);
          this.variant = "";
          this.size = "md";
          this.icon = "";
          this.iconColor = "";
          this.text = "";
          this.supportingText = "";
          this.headingLevel = null;
          this._hasContent = false;
          this._hasActions = false;
        }
        get _resolvedIconName() {
          if (this.variant === "alert")
            return "alert";
          if (this.variant === "success")
            return "success";
          if (this.icon)
            return this.icon;
          return "";
        }
        firstUpdated() {
          const contentSlot = this.shadowRoot?.querySelector("slot:not([name])");
          const actionsSlot = this.shadowRoot?.querySelector('slot[name="actions"]');
          const hasMeaningfulContent = (slot) => {
            const nodes = slot?.assignedNodes({ flatten: true }) ?? [];
            return nodes.some((n7) => n7.nodeType === Node.ELEMENT_NODE || n7.nodeType === Node.TEXT_NODE && (n7.textContent?.trim() ?? "") !== "");
          };
          const syncContent = () => {
            this._hasContent = hasMeaningfulContent(contentSlot);
          };
          const syncActions = () => {
            this._hasActions = hasMeaningfulContent(actionsSlot);
          };
          contentSlot?.addEventListener("slotchange", syncContent);
          actionsSlot?.addEventListener("slotchange", syncActions);
          syncContent();
          syncActions();
        }
        render() {
          return inlineDialogTemplate(this);
        }
      };
      NLDDInlineDialog.styles = inlineDialogStyles;
      __decorate11([
        n4({ type: String, reflect: true })
      ], NLDDInlineDialog.prototype, "variant", void 0);
      __decorate11([
        n4({ type: String, reflect: true })
      ], NLDDInlineDialog.prototype, "size", void 0);
      __decorate11([
        n4({ type: String, reflect: true })
      ], NLDDInlineDialog.prototype, "icon", void 0);
      __decorate11([
        n4({ type: String, reflect: true, attribute: "icon-color" })
      ], NLDDInlineDialog.prototype, "iconColor", void 0);
      __decorate11([
        n4({ type: String, reflect: true })
      ], NLDDInlineDialog.prototype, "text", void 0);
      __decorate11([
        n4({ type: String, reflect: true, attribute: "supporting-text" })
      ], NLDDInlineDialog.prototype, "supportingText", void 0);
      __decorate11([
        n4({ type: Number, reflect: true, attribute: "heading-level" })
      ], NLDDInlineDialog.prototype, "headingLevel", void 0);
      __decorate11([
        r5()
      ], NLDDInlineDialog.prototype, "_hasContent", void 0);
      __decorate11([
        r5()
      ], NLDDInlineDialog.prototype, "_hasActions", void 0);
      NLDDInlineDialog = __decorate11([
        t3("nldd-inline-dialog")
      ], NLDDInlineDialog);
    }
  });

  // node_modules/lit-html/directives/if-defined.js
  var o7;
  var init_if_defined = __esm({
    "node_modules/lit-html/directives/if-defined.js"() {
      init_lit_html();
      o7 = (o10) => o10 ?? A;
    }
  });

  // node_modules/lit/directives/if-defined.js
  var init_if_defined2 = __esm({
    "node_modules/lit/directives/if-defined.js"() {
      init_if_defined();
    }
  });

  // node_modules/prismjs/prism.js
  var require_prism = __commonJS({
    "node_modules/prismjs/prism.js"(exports, module) {
      var _self = typeof window !== "undefined" ? window : typeof WorkerGlobalScope !== "undefined" && self instanceof WorkerGlobalScope ? self : {};
      var Prism3 = (function(_self2) {
        var lang = /(?:^|\s)lang(?:uage)?-([\w-]+)(?=\s|$)/i;
        var uniqueId = 0;
        var plainTextGrammar = {};
        var _2 = {
          /**
           * By default, Prism will attempt to highlight all code elements (by calling {@link Prism.highlightAll}) on the
           * current page after the page finished loading. This might be a problem if e.g. you wanted to asynchronously load
           * additional languages or plugins yourself.
           *
           * By setting this value to `true`, Prism will not automatically highlight all code elements on the page.
           *
           * You obviously have to change this value before the automatic highlighting started. To do this, you can add an
           * empty Prism object into the global scope before loading the Prism script like this:
           *
           * ```js
           * window.Prism = window.Prism || {};
           * Prism.manual = true;
           * // add a new <script> to load Prism's script
           * ```
           *
           * @default false
           * @type {boolean}
           * @memberof Prism
           * @public
           */
          manual: _self2.Prism && _self2.Prism.manual,
          /**
           * By default, if Prism is in a web worker, it assumes that it is in a worker it created itself, so it uses
           * `addEventListener` to communicate with its parent instance. However, if you're using Prism manually in your
           * own worker, you don't want it to do this.
           *
           * By setting this value to `true`, Prism will not add its own listeners to the worker.
           *
           * You obviously have to change this value before Prism executes. To do this, you can add an
           * empty Prism object into the global scope before loading the Prism script like this:
           *
           * ```js
           * window.Prism = window.Prism || {};
           * Prism.disableWorkerMessageHandler = true;
           * // Load Prism's script
           * ```
           *
           * @default false
           * @type {boolean}
           * @memberof Prism
           * @public
           */
          disableWorkerMessageHandler: _self2.Prism && _self2.Prism.disableWorkerMessageHandler,
          /**
           * A namespace for utility methods.
           *
           * All function in this namespace that are not explicitly marked as _public_ are for __internal use only__ and may
           * change or disappear at any time.
           *
           * @namespace
           * @memberof Prism
           */
          util: {
            encode: function encode(tokens) {
              if (tokens instanceof Token) {
                return new Token(tokens.type, encode(tokens.content), tokens.alias);
              } else if (Array.isArray(tokens)) {
                return tokens.map(encode);
              } else {
                return tokens.replace(/&/g, "&amp;").replace(/</g, "&lt;").replace(/\u00a0/g, " ");
              }
            },
            /**
             * Returns the name of the type of the given value.
             *
             * @param {any} o
             * @returns {string}
             * @example
             * type(null)      === 'Null'
             * type(undefined) === 'Undefined'
             * type(123)       === 'Number'
             * type('foo')     === 'String'
             * type(true)      === 'Boolean'
             * type([1, 2])    === 'Array'
             * type({})        === 'Object'
             * type(String)    === 'Function'
             * type(/abc+/)    === 'RegExp'
             */
            type: function(o10) {
              return Object.prototype.toString.call(o10).slice(8, -1);
            },
            /**
             * Returns a unique number for the given object. Later calls will still return the same number.
             *
             * @param {Object} obj
             * @returns {number}
             */
            objId: function(obj) {
              if (!obj["__id"]) {
                Object.defineProperty(obj, "__id", { value: ++uniqueId });
              }
              return obj["__id"];
            },
            /**
             * Creates a deep clone of the given object.
             *
             * The main intended use of this function is to clone language definitions.
             *
             * @param {T} o
             * @param {Record<number, any>} [visited]
             * @returns {T}
             * @template T
             */
            clone: function deepClone(o10, visited) {
              visited = visited || {};
              var clone;
              var id;
              switch (_2.util.type(o10)) {
                case "Object":
                  id = _2.util.objId(o10);
                  if (visited[id]) {
                    return visited[id];
                  }
                  clone = /** @type {Record<string, any>} */
                  {};
                  visited[id] = clone;
                  for (var key in o10) {
                    if (o10.hasOwnProperty(key)) {
                      clone[key] = deepClone(o10[key], visited);
                    }
                  }
                  return (
                    /** @type {any} */
                    clone
                  );
                case "Array":
                  id = _2.util.objId(o10);
                  if (visited[id]) {
                    return visited[id];
                  }
                  clone = [];
                  visited[id] = clone;
                  /** @type {Array} */
                  /** @type {any} */
                  o10.forEach(function(v3, i8) {
                    clone[i8] = deepClone(v3, visited);
                  });
                  return (
                    /** @type {any} */
                    clone
                  );
                default:
                  return o10;
              }
            },
            /**
             * Returns the Prism language of the given element set by a `language-xxxx` or `lang-xxxx` class.
             *
             * If no language is set for the element or the element is `null` or `undefined`, `none` will be returned.
             *
             * @param {Element} element
             * @returns {string}
             */
            getLanguage: function(element) {
              while (element) {
                var m3 = lang.exec(element.className);
                if (m3) {
                  return m3[1].toLowerCase();
                }
                element = element.parentElement;
              }
              return "none";
            },
            /**
             * Sets the Prism `language-xxxx` class of the given element.
             *
             * @param {Element} element
             * @param {string} language
             * @returns {void}
             */
            setLanguage: function(element, language) {
              element.className = element.className.replace(RegExp(lang, "gi"), "");
              element.classList.add("language-" + language);
            },
            /**
             * Returns the script element that is currently executing.
             *
             * This does __not__ work for line script element.
             *
             * @returns {HTMLScriptElement | null}
             */
            currentScript: function() {
              if (typeof document === "undefined") {
                return null;
              }
              if (document.currentScript && document.currentScript.tagName === "SCRIPT" && 1 < 2) {
                return (
                  /** @type {any} */
                  document.currentScript
                );
              }
              try {
                throw new Error();
              } catch (err) {
                var src = (/at [^(\r\n]*\((.*):[^:]+:[^:]+\)$/i.exec(err.stack) || [])[1];
                if (src) {
                  var scripts = document.getElementsByTagName("script");
                  for (var i8 in scripts) {
                    if (scripts[i8].src == src) {
                      return scripts[i8];
                    }
                  }
                }
                return null;
              }
            },
            /**
             * Returns whether a given class is active for `element`.
             *
             * The class can be activated if `element` or one of its ancestors has the given class and it can be deactivated
             * if `element` or one of its ancestors has the negated version of the given class. The _negated version_ of the
             * given class is just the given class with a `no-` prefix.
             *
             * Whether the class is active is determined by the closest ancestor of `element` (where `element` itself is
             * closest ancestor) that has the given class or the negated version of it. If neither `element` nor any of its
             * ancestors have the given class or the negated version of it, then the default activation will be returned.
             *
             * In the paradoxical situation where the closest ancestor contains __both__ the given class and the negated
             * version of it, the class is considered active.
             *
             * @param {Element} element
             * @param {string} className
             * @param {boolean} [defaultActivation=false]
             * @returns {boolean}
             */
            isActive: function(element, className, defaultActivation) {
              var no = "no-" + className;
              while (element) {
                var classList = element.classList;
                if (classList.contains(className)) {
                  return true;
                }
                if (classList.contains(no)) {
                  return false;
                }
                element = element.parentElement;
              }
              return !!defaultActivation;
            }
          },
          /**
           * This namespace contains all currently loaded languages and the some helper functions to create and modify languages.
           *
           * @namespace
           * @memberof Prism
           * @public
           */
          languages: {
            /**
             * The grammar for plain, unformatted text.
             */
            plain: plainTextGrammar,
            plaintext: plainTextGrammar,
            text: plainTextGrammar,
            txt: plainTextGrammar,
            /**
             * Creates a deep copy of the language with the given id and appends the given tokens.
             *
             * If a token in `redef` also appears in the copied language, then the existing token in the copied language
             * will be overwritten at its original position.
             *
             * ## Best practices
             *
             * Since the position of overwriting tokens (token in `redef` that overwrite tokens in the copied language)
             * doesn't matter, they can technically be in any order. However, this can be confusing to others that trying to
             * understand the language definition because, normally, the order of tokens matters in Prism grammars.
             *
             * Therefore, it is encouraged to order overwriting tokens according to the positions of the overwritten tokens.
             * Furthermore, all non-overwriting tokens should be placed after the overwriting ones.
             *
             * @param {string} id The id of the language to extend. This has to be a key in `Prism.languages`.
             * @param {Grammar} redef The new tokens to append.
             * @returns {Grammar} The new language created.
             * @public
             * @example
             * Prism.languages['css-with-colors'] = Prism.languages.extend('css', {
             *     // Prism.languages.css already has a 'comment' token, so this token will overwrite CSS' 'comment' token
             *     // at its original position
             *     'comment': { ... },
             *     // CSS doesn't have a 'color' token, so this token will be appended
             *     'color': /\b(?:red|green|blue)\b/
             * });
             */
            extend: function(id, redef) {
              var lang2 = _2.util.clone(_2.languages[id]);
              for (var key in redef) {
                lang2[key] = redef[key];
              }
              return lang2;
            },
            /**
             * Inserts tokens _before_ another token in a language definition or any other grammar.
             *
             * ## Usage
             *
             * This helper method makes it easy to modify existing languages. For example, the CSS language definition
             * not only defines CSS highlighting for CSS documents, but also needs to define highlighting for CSS embedded
             * in HTML through `<style>` elements. To do this, it needs to modify `Prism.languages.markup` and add the
             * appropriate tokens. However, `Prism.languages.markup` is a regular JavaScript object literal, so if you do
             * this:
             *
             * ```js
             * Prism.languages.markup.style = {
             *     // token
             * };
             * ```
             *
             * then the `style` token will be added (and processed) at the end. `insertBefore` allows you to insert tokens
             * before existing tokens. For the CSS example above, you would use it like this:
             *
             * ```js
             * Prism.languages.insertBefore('markup', 'cdata', {
             *     'style': {
             *         // token
             *     }
             * });
             * ```
             *
             * ## Special cases
             *
             * If the grammars of `inside` and `insert` have tokens with the same name, the tokens in `inside`'s grammar
             * will be ignored.
             *
             * This behavior can be used to insert tokens after `before`:
             *
             * ```js
             * Prism.languages.insertBefore('markup', 'comment', {
             *     'comment': Prism.languages.markup.comment,
             *     // tokens after 'comment'
             * });
             * ```
             *
             * ## Limitations
             *
             * The main problem `insertBefore` has to solve is iteration order. Since ES2015, the iteration order for object
             * properties is guaranteed to be the insertion order (except for integer keys) but some browsers behave
             * differently when keys are deleted and re-inserted. So `insertBefore` can't be implemented by temporarily
             * deleting properties which is necessary to insert at arbitrary positions.
             *
             * To solve this problem, `insertBefore` doesn't actually insert the given tokens into the target object.
             * Instead, it will create a new object and replace all references to the target object with the new one. This
             * can be done without temporarily deleting properties, so the iteration order is well-defined.
             *
             * However, only references that can be reached from `Prism.languages` or `insert` will be replaced. I.e. if
             * you hold the target object in a variable, then the value of the variable will not change.
             *
             * ```js
             * var oldMarkup = Prism.languages.markup;
             * var newMarkup = Prism.languages.insertBefore('markup', 'comment', { ... });
             *
             * assert(oldMarkup !== Prism.languages.markup);
             * assert(newMarkup === Prism.languages.markup);
             * ```
             *
             * @param {string} inside The property of `root` (e.g. a language id in `Prism.languages`) that contains the
             * object to be modified.
             * @param {string} before The key to insert before.
             * @param {Grammar} insert An object containing the key-value pairs to be inserted.
             * @param {Object<string, any>} [root] The object containing `inside`, i.e. the object that contains the
             * object to be modified.
             *
             * Defaults to `Prism.languages`.
             * @returns {Grammar} The new grammar object.
             * @public
             */
            insertBefore: function(inside, before, insert, root) {
              root = root || /** @type {any} */
              _2.languages;
              var grammar = root[inside];
              var ret = {};
              for (var token in grammar) {
                if (grammar.hasOwnProperty(token)) {
                  if (token == before) {
                    for (var newToken in insert) {
                      if (insert.hasOwnProperty(newToken)) {
                        ret[newToken] = insert[newToken];
                      }
                    }
                  }
                  if (!insert.hasOwnProperty(token)) {
                    ret[token] = grammar[token];
                  }
                }
              }
              var old = root[inside];
              root[inside] = ret;
              _2.languages.DFS(_2.languages, function(key, value) {
                if (value === old && key != inside) {
                  this[key] = ret;
                }
              });
              return ret;
            },
            // Traverse a language definition with Depth First Search
            DFS: function DFS(o10, callback, type, visited) {
              visited = visited || {};
              var objId = _2.util.objId;
              for (var i8 in o10) {
                if (o10.hasOwnProperty(i8)) {
                  callback.call(o10, i8, o10[i8], type || i8);
                  var property = o10[i8];
                  var propertyType = _2.util.type(property);
                  if (propertyType === "Object" && !visited[objId(property)]) {
                    visited[objId(property)] = true;
                    DFS(property, callback, null, visited);
                  } else if (propertyType === "Array" && !visited[objId(property)]) {
                    visited[objId(property)] = true;
                    DFS(property, callback, i8, visited);
                  }
                }
              }
            }
          },
          plugins: {},
          /**
           * This is the most high-level function in Prism’s API.
           * It fetches all the elements that have a `.language-xxxx` class and then calls {@link Prism.highlightElement} on
           * each one of them.
           *
           * This is equivalent to `Prism.highlightAllUnder(document, async, callback)`.
           *
           * @param {boolean} [async=false] Same as in {@link Prism.highlightAllUnder}.
           * @param {HighlightCallback} [callback] Same as in {@link Prism.highlightAllUnder}.
           * @memberof Prism
           * @public
           */
          highlightAll: function(async, callback) {
            _2.highlightAllUnder(document, async, callback);
          },
          /**
           * Fetches all the descendants of `container` that have a `.language-xxxx` class and then calls
           * {@link Prism.highlightElement} on each one of them.
           *
           * The following hooks will be run:
           * 1. `before-highlightall`
           * 2. `before-all-elements-highlight`
           * 3. All hooks of {@link Prism.highlightElement} for each element.
           *
           * @param {ParentNode} container The root element, whose descendants that have a `.language-xxxx` class will be highlighted.
           * @param {boolean} [async=false] Whether each element is to be highlighted asynchronously using Web Workers.
           * @param {HighlightCallback} [callback] An optional callback to be invoked on each element after its highlighting is done.
           * @memberof Prism
           * @public
           */
          highlightAllUnder: function(container, async, callback) {
            var env = {
              callback,
              container,
              selector: 'code[class*="language-"], [class*="language-"] code, code[class*="lang-"], [class*="lang-"] code'
            };
            _2.hooks.run("before-highlightall", env);
            env.elements = Array.prototype.slice.apply(env.container.querySelectorAll(env.selector));
            _2.hooks.run("before-all-elements-highlight", env);
            for (var i8 = 0, element; element = env.elements[i8++]; ) {
              _2.highlightElement(element, async === true, env.callback);
            }
          },
          /**
           * Highlights the code inside a single element.
           *
           * The following hooks will be run:
           * 1. `before-sanity-check`
           * 2. `before-highlight`
           * 3. All hooks of {@link Prism.highlight}. These hooks will be run by an asynchronous worker if `async` is `true`.
           * 4. `before-insert`
           * 5. `after-highlight`
           * 6. `complete`
           *
           * Some the above hooks will be skipped if the element doesn't contain any text or there is no grammar loaded for
           * the element's language.
           *
           * @param {Element} element The element containing the code.
           * It must have a class of `language-xxxx` to be processed, where `xxxx` is a valid language identifier.
           * @param {boolean} [async=false] Whether the element is to be highlighted asynchronously using Web Workers
           * to improve performance and avoid blocking the UI when highlighting very large chunks of code. This option is
           * [disabled by default](https://prismjs.com/faq.html#why-is-asynchronous-highlighting-disabled-by-default).
           *
           * Note: All language definitions required to highlight the code must be included in the main `prism.js` file for
           * asynchronous highlighting to work. You can build your own bundle on the
           * [Download page](https://prismjs.com/download.html).
           * @param {HighlightCallback} [callback] An optional callback to be invoked after the highlighting is done.
           * Mostly useful when `async` is `true`, since in that case, the highlighting is done asynchronously.
           * @memberof Prism
           * @public
           */
          highlightElement: function(element, async, callback) {
            var language = _2.util.getLanguage(element);
            var grammar = _2.languages[language];
            _2.util.setLanguage(element, language);
            var parent = element.parentElement;
            if (parent && parent.nodeName.toLowerCase() === "pre") {
              _2.util.setLanguage(parent, language);
            }
            var code = element.textContent;
            var env = {
              element,
              language,
              grammar,
              code
            };
            function insertHighlightedCode(highlightedCode) {
              env.highlightedCode = highlightedCode;
              _2.hooks.run("before-insert", env);
              env.element.innerHTML = env.highlightedCode;
              _2.hooks.run("after-highlight", env);
              _2.hooks.run("complete", env);
              callback && callback.call(env.element);
            }
            _2.hooks.run("before-sanity-check", env);
            parent = env.element.parentElement;
            if (parent && parent.nodeName.toLowerCase() === "pre" && !parent.hasAttribute("tabindex")) {
              parent.setAttribute("tabindex", "0");
            }
            if (!env.code) {
              _2.hooks.run("complete", env);
              callback && callback.call(env.element);
              return;
            }
            _2.hooks.run("before-highlight", env);
            if (!env.grammar) {
              insertHighlightedCode(_2.util.encode(env.code));
              return;
            }
            if (async && _self2.Worker) {
              var worker = new Worker(_2.filename);
              worker.onmessage = function(evt) {
                insertHighlightedCode(evt.data);
              };
              worker.postMessage(JSON.stringify({
                language: env.language,
                code: env.code,
                immediateClose: true
              }));
            } else {
              insertHighlightedCode(_2.highlight(env.code, env.grammar, env.language));
            }
          },
          /**
           * Low-level function, only use if you know what you’re doing. It accepts a string of text as input
           * and the language definitions to use, and returns a string with the HTML produced.
           *
           * The following hooks will be run:
           * 1. `before-tokenize`
           * 2. `after-tokenize`
           * 3. `wrap`: On each {@link Token}.
           *
           * @param {string} text A string with the code to be highlighted.
           * @param {Grammar} grammar An object containing the tokens to use.
           *
           * Usually a language definition like `Prism.languages.markup`.
           * @param {string} language The name of the language definition passed to `grammar`.
           * @returns {string} The highlighted HTML.
           * @memberof Prism
           * @public
           * @example
           * Prism.highlight('var foo = true;', Prism.languages.javascript, 'javascript');
           */
          highlight: function(text, grammar, language) {
            var env = {
              code: text,
              grammar,
              language
            };
            _2.hooks.run("before-tokenize", env);
            if (!env.grammar) {
              throw new Error('The language "' + env.language + '" has no grammar.');
            }
            env.tokens = _2.tokenize(env.code, env.grammar);
            _2.hooks.run("after-tokenize", env);
            return Token.stringify(_2.util.encode(env.tokens), env.language);
          },
          /**
           * This is the heart of Prism, and the most low-level function you can use. It accepts a string of text as input
           * and the language definitions to use, and returns an array with the tokenized code.
           *
           * When the language definition includes nested tokens, the function is called recursively on each of these tokens.
           *
           * This method could be useful in other contexts as well, as a very crude parser.
           *
           * @param {string} text A string with the code to be highlighted.
           * @param {Grammar} grammar An object containing the tokens to use.
           *
           * Usually a language definition like `Prism.languages.markup`.
           * @returns {TokenStream} An array of strings and tokens, a token stream.
           * @memberof Prism
           * @public
           * @example
           * let code = `var foo = 0;`;
           * let tokens = Prism.tokenize(code, Prism.languages.javascript);
           * tokens.forEach(token => {
           *     if (token instanceof Prism.Token && token.type === 'number') {
           *         console.log(`Found numeric literal: ${token.content}`);
           *     }
           * });
           */
          tokenize: function(text, grammar) {
            var rest = grammar.rest;
            if (rest) {
              for (var token in rest) {
                grammar[token] = rest[token];
              }
              delete grammar.rest;
            }
            var tokenList = new LinkedList();
            addAfter(tokenList, tokenList.head, text);
            matchGrammar(text, tokenList, grammar, tokenList.head, 0);
            return toArray(tokenList);
          },
          /**
           * @namespace
           * @memberof Prism
           * @public
           */
          hooks: {
            all: {},
            /**
             * Adds the given callback to the list of callbacks for the given hook.
             *
             * The callback will be invoked when the hook it is registered for is run.
             * Hooks are usually directly run by a highlight function but you can also run hooks yourself.
             *
             * One callback function can be registered to multiple hooks and the same hook multiple times.
             *
             * @param {string} name The name of the hook.
             * @param {HookCallback} callback The callback function which is given environment variables.
             * @public
             */
            add: function(name, callback) {
              var hooks = _2.hooks.all;
              hooks[name] = hooks[name] || [];
              hooks[name].push(callback);
            },
            /**
             * Runs a hook invoking all registered callbacks with the given environment variables.
             *
             * Callbacks will be invoked synchronously and in the order in which they were registered.
             *
             * @param {string} name The name of the hook.
             * @param {Object<string, any>} env The environment variables of the hook passed to all callbacks registered.
             * @public
             */
            run: function(name, env) {
              var callbacks = _2.hooks.all[name];
              if (!callbacks || !callbacks.length) {
                return;
              }
              for (var i8 = 0, callback; callback = callbacks[i8++]; ) {
                callback(env);
              }
            }
          },
          Token
        };
        _self2.Prism = _2;
        function Token(type, content, alias, matchedStr) {
          this.type = type;
          this.content = content;
          this.alias = alias;
          this.length = (matchedStr || "").length | 0;
        }
        Token.stringify = function stringify(o10, language) {
          if (typeof o10 == "string") {
            return o10;
          }
          if (Array.isArray(o10)) {
            var s6 = "";
            o10.forEach(function(e9) {
              s6 += stringify(e9, language);
            });
            return s6;
          }
          var env = {
            type: o10.type,
            content: stringify(o10.content, language),
            tag: "span",
            classes: ["token", o10.type],
            attributes: {},
            language
          };
          var aliases2 = o10.alias;
          if (aliases2) {
            if (Array.isArray(aliases2)) {
              Array.prototype.push.apply(env.classes, aliases2);
            } else {
              env.classes.push(aliases2);
            }
          }
          _2.hooks.run("wrap", env);
          var attributes = "";
          for (var name in env.attributes) {
            attributes += " " + name + '="' + (env.attributes[name] || "").replace(/"/g, "&quot;") + '"';
          }
          return "<" + env.tag + ' class="' + env.classes.join(" ") + '"' + attributes + ">" + env.content + "</" + env.tag + ">";
        };
        function matchPattern(pattern, pos, text, lookbehind) {
          pattern.lastIndex = pos;
          var match = pattern.exec(text);
          if (match && lookbehind && match[1]) {
            var lookbehindLength = match[1].length;
            match.index += lookbehindLength;
            match[0] = match[0].slice(lookbehindLength);
          }
          return match;
        }
        function matchGrammar(text, tokenList, grammar, startNode, startPos, rematch) {
          for (var token in grammar) {
            if (!grammar.hasOwnProperty(token) || !grammar[token]) {
              continue;
            }
            var patterns = grammar[token];
            patterns = Array.isArray(patterns) ? patterns : [patterns];
            for (var j2 = 0; j2 < patterns.length; ++j2) {
              if (rematch && rematch.cause == token + "," + j2) {
                return;
              }
              var patternObj = patterns[j2];
              var inside = patternObj.inside;
              var lookbehind = !!patternObj.lookbehind;
              var greedy = !!patternObj.greedy;
              var alias = patternObj.alias;
              if (greedy && !patternObj.pattern.global) {
                var flags = patternObj.pattern.toString().match(/[imsuy]*$/)[0];
                patternObj.pattern = RegExp(patternObj.pattern.source, flags + "g");
              }
              var pattern = patternObj.pattern || patternObj;
              for (var currentNode = startNode.next, pos = startPos; currentNode !== tokenList.tail; pos += currentNode.value.length, currentNode = currentNode.next) {
                if (rematch && pos >= rematch.reach) {
                  break;
                }
                var str = currentNode.value;
                if (tokenList.length > text.length) {
                  return;
                }
                if (str instanceof Token) {
                  continue;
                }
                var removeCount = 1;
                var match;
                if (greedy) {
                  match = matchPattern(pattern, pos, text, lookbehind);
                  if (!match || match.index >= text.length) {
                    break;
                  }
                  var from = match.index;
                  var to = match.index + match[0].length;
                  var p4 = pos;
                  p4 += currentNode.value.length;
                  while (from >= p4) {
                    currentNode = currentNode.next;
                    p4 += currentNode.value.length;
                  }
                  p4 -= currentNode.value.length;
                  pos = p4;
                  if (currentNode.value instanceof Token) {
                    continue;
                  }
                  for (var k2 = currentNode; k2 !== tokenList.tail && (p4 < to || typeof k2.value === "string"); k2 = k2.next) {
                    removeCount++;
                    p4 += k2.value.length;
                  }
                  removeCount--;
                  str = text.slice(pos, p4);
                  match.index -= pos;
                } else {
                  match = matchPattern(pattern, 0, str, lookbehind);
                  if (!match) {
                    continue;
                  }
                }
                var from = match.index;
                var matchStr = match[0];
                var before = str.slice(0, from);
                var after = str.slice(from + matchStr.length);
                var reach = pos + str.length;
                if (rematch && reach > rematch.reach) {
                  rematch.reach = reach;
                }
                var removeFrom = currentNode.prev;
                if (before) {
                  removeFrom = addAfter(tokenList, removeFrom, before);
                  pos += before.length;
                }
                removeRange(tokenList, removeFrom, removeCount);
                var wrapped = new Token(token, inside ? _2.tokenize(matchStr, inside) : matchStr, alias, matchStr);
                currentNode = addAfter(tokenList, removeFrom, wrapped);
                if (after) {
                  addAfter(tokenList, currentNode, after);
                }
                if (removeCount > 1) {
                  var nestedRematch = {
                    cause: token + "," + j2,
                    reach
                  };
                  matchGrammar(text, tokenList, grammar, currentNode.prev, pos, nestedRematch);
                  if (rematch && nestedRematch.reach > rematch.reach) {
                    rematch.reach = nestedRematch.reach;
                  }
                }
              }
            }
          }
        }
        function LinkedList() {
          var head = { value: null, prev: null, next: null };
          var tail = { value: null, prev: head, next: null };
          head.next = tail;
          this.head = head;
          this.tail = tail;
          this.length = 0;
        }
        function addAfter(list, node, value) {
          var next = node.next;
          var newNode = { value, prev: node, next };
          node.next = newNode;
          next.prev = newNode;
          list.length++;
          return newNode;
        }
        function removeRange(list, node, count) {
          var next = node.next;
          for (var i8 = 0; i8 < count && next !== list.tail; i8++) {
            next = next.next;
          }
          node.next = next;
          next.prev = node;
          list.length -= i8;
        }
        function toArray(list) {
          var array = [];
          var node = list.head.next;
          while (node !== list.tail) {
            array.push(node.value);
            node = node.next;
          }
          return array;
        }
        if (!_self2.document) {
          if (!_self2.addEventListener) {
            return _2;
          }
          if (!_2.disableWorkerMessageHandler) {
            _self2.addEventListener("message", function(evt) {
              var message = JSON.parse(evt.data);
              var lang2 = message.language;
              var code = message.code;
              var immediateClose = message.immediateClose;
              _self2.postMessage(_2.highlight(code, _2.languages[lang2], lang2));
              if (immediateClose) {
                _self2.close();
              }
            }, false);
          }
          return _2;
        }
        var script = _2.util.currentScript();
        if (script) {
          _2.filename = script.src;
          if (script.hasAttribute("data-manual")) {
            _2.manual = true;
          }
        }
        function highlightAutomaticallyCallback() {
          if (!_2.manual) {
            _2.highlightAll();
          }
        }
        if (!_2.manual) {
          var readyState = document.readyState;
          if (readyState === "loading" || readyState === "interactive" && script && script.defer) {
            document.addEventListener("DOMContentLoaded", highlightAutomaticallyCallback);
          } else {
            if (window.requestAnimationFrame) {
              window.requestAnimationFrame(highlightAutomaticallyCallback);
            } else {
              window.setTimeout(highlightAutomaticallyCallback, 16);
            }
          }
        }
        return _2;
      })(_self);
      if (typeof module !== "undefined" && module.exports) {
        module.exports = Prism3;
      }
      if (typeof global !== "undefined") {
        global.Prism = Prism3;
      }
      Prism3.languages.markup = {
        "comment": {
          pattern: /<!--(?:(?!<!--)[\s\S])*?-->/,
          greedy: true
        },
        "prolog": {
          pattern: /<\?[\s\S]+?\?>/,
          greedy: true
        },
        "doctype": {
          // https://www.w3.org/TR/xml/#NT-doctypedecl
          pattern: /<!DOCTYPE(?:[^>"'[\]]|"[^"]*"|'[^']*')+(?:\[(?:[^<"'\]]|"[^"]*"|'[^']*'|<(?!!--)|<!--(?:[^-]|-(?!->))*-->)*\]\s*)?>/i,
          greedy: true,
          inside: {
            "internal-subset": {
              pattern: /(^[^\[]*\[)[\s\S]+(?=\]>$)/,
              lookbehind: true,
              greedy: true,
              inside: null
              // see below
            },
            "string": {
              pattern: /"[^"]*"|'[^']*'/,
              greedy: true
            },
            "punctuation": /^<!|>$|[[\]]/,
            "doctype-tag": /^DOCTYPE/i,
            "name": /[^\s<>'"]+/
          }
        },
        "cdata": {
          pattern: /<!\[CDATA\[[\s\S]*?\]\]>/i,
          greedy: true
        },
        "tag": {
          pattern: /<\/?(?!\d)[^\s>\/=$<%]+(?:\s(?:\s*[^\s>\/=]+(?:\s*=\s*(?:"[^"]*"|'[^']*'|[^\s'">=]+(?=[\s>]))|(?=[\s/>])))+)?\s*\/?>/,
          greedy: true,
          inside: {
            "tag": {
              pattern: /^<\/?[^\s>\/]+/,
              inside: {
                "punctuation": /^<\/?/,
                "namespace": /^[^\s>\/:]+:/
              }
            },
            "special-attr": [],
            "attr-value": {
              pattern: /=\s*(?:"[^"]*"|'[^']*'|[^\s'">=]+)/,
              inside: {
                "punctuation": [
                  {
                    pattern: /^=/,
                    alias: "attr-equals"
                  },
                  {
                    pattern: /^(\s*)["']|["']$/,
                    lookbehind: true
                  }
                ]
              }
            },
            "punctuation": /\/?>/,
            "attr-name": {
              pattern: /[^\s>\/]+/,
              inside: {
                "namespace": /^[^\s>\/:]+:/
              }
            }
          }
        },
        "entity": [
          {
            pattern: /&[\da-z]{1,8};/i,
            alias: "named-entity"
          },
          /&#x?[\da-f]{1,8};/i
        ]
      };
      Prism3.languages.markup["tag"].inside["attr-value"].inside["entity"] = Prism3.languages.markup["entity"];
      Prism3.languages.markup["doctype"].inside["internal-subset"].inside = Prism3.languages.markup;
      Prism3.hooks.add("wrap", function(env) {
        if (env.type === "entity") {
          env.attributes["title"] = env.content.replace(/&amp;/, "&");
        }
      });
      Object.defineProperty(Prism3.languages.markup.tag, "addInlined", {
        /**
         * Adds an inlined language to markup.
         *
         * An example of an inlined language is CSS with `<style>` tags.
         *
         * @param {string} tagName The name of the tag that contains the inlined language. This name will be treated as
         * case insensitive.
         * @param {string} lang The language key.
         * @example
         * addInlined('style', 'css');
         */
        value: function addInlined(tagName, lang) {
          var includedCdataInside = {};
          includedCdataInside["language-" + lang] = {
            pattern: /(^<!\[CDATA\[)[\s\S]+?(?=\]\]>$)/i,
            lookbehind: true,
            inside: Prism3.languages[lang]
          };
          includedCdataInside["cdata"] = /^<!\[CDATA\[|\]\]>$/i;
          var inside = {
            "included-cdata": {
              pattern: /<!\[CDATA\[[\s\S]*?\]\]>/i,
              inside: includedCdataInside
            }
          };
          inside["language-" + lang] = {
            pattern: /[\s\S]+/,
            inside: Prism3.languages[lang]
          };
          var def = {};
          def[tagName] = {
            pattern: RegExp(/(<__[^>]*>)(?:<!\[CDATA\[(?:[^\]]|\](?!\]>))*\]\]>|(?!<!\[CDATA\[)[\s\S])*?(?=<\/__>)/.source.replace(/__/g, function() {
              return tagName;
            }), "i"),
            lookbehind: true,
            greedy: true,
            inside
          };
          Prism3.languages.insertBefore("markup", "cdata", def);
        }
      });
      Object.defineProperty(Prism3.languages.markup.tag, "addAttribute", {
        /**
         * Adds an pattern to highlight languages embedded in HTML attributes.
         *
         * An example of an inlined language is CSS with `style` attributes.
         *
         * @param {string} attrName The name of the tag that contains the inlined language. This name will be treated as
         * case insensitive.
         * @param {string} lang The language key.
         * @example
         * addAttribute('style', 'css');
         */
        value: function(attrName, lang) {
          Prism3.languages.markup.tag.inside["special-attr"].push({
            pattern: RegExp(
              /(^|["'\s])/.source + "(?:" + attrName + ")" + /\s*=\s*(?:"[^"]*"|'[^']*'|[^\s'">=]+(?=[\s>]))/.source,
              "i"
            ),
            lookbehind: true,
            inside: {
              "attr-name": /^[^\s=]+/,
              "attr-value": {
                pattern: /=[\s\S]+/,
                inside: {
                  "value": {
                    pattern: /(^=\s*(["']|(?!["'])))\S[\s\S]*(?=\2$)/,
                    lookbehind: true,
                    alias: [lang, "language-" + lang],
                    inside: Prism3.languages[lang]
                  },
                  "punctuation": [
                    {
                      pattern: /^=/,
                      alias: "attr-equals"
                    },
                    /"|'/
                  ]
                }
              }
            }
          });
        }
      });
      Prism3.languages.html = Prism3.languages.markup;
      Prism3.languages.mathml = Prism3.languages.markup;
      Prism3.languages.svg = Prism3.languages.markup;
      Prism3.languages.xml = Prism3.languages.extend("markup", {});
      Prism3.languages.ssml = Prism3.languages.xml;
      Prism3.languages.atom = Prism3.languages.xml;
      Prism3.languages.rss = Prism3.languages.xml;
      (function(Prism4) {
        var string = /(?:"(?:\\(?:\r\n|[\s\S])|[^"\\\r\n])*"|'(?:\\(?:\r\n|[\s\S])|[^'\\\r\n])*')/;
        Prism4.languages.css = {
          "comment": /\/\*[\s\S]*?\*\//,
          "atrule": {
            pattern: RegExp("@[\\w-](?:" + /[^;{\s"']|\s+(?!\s)/.source + "|" + string.source + ")*?" + /(?:;|(?=\s*\{))/.source),
            inside: {
              "rule": /^@[\w-]+/,
              "selector-function-argument": {
                pattern: /(\bselector\s*\(\s*(?![\s)]))(?:[^()\s]|\s+(?![\s)])|\((?:[^()]|\([^()]*\))*\))+(?=\s*\))/,
                lookbehind: true,
                alias: "selector"
              },
              "keyword": {
                pattern: /(^|[^\w-])(?:and|not|only|or)(?![\w-])/,
                lookbehind: true
              }
              // See rest below
            }
          },
          "url": {
            // https://drafts.csswg.org/css-values-3/#urls
            pattern: RegExp("\\burl\\((?:" + string.source + "|" + /(?:[^\\\r\n()"']|\\[\s\S])*/.source + ")\\)", "i"),
            greedy: true,
            inside: {
              "function": /^url/i,
              "punctuation": /^\(|\)$/,
              "string": {
                pattern: RegExp("^" + string.source + "$"),
                alias: "url"
              }
            }
          },
          "selector": {
            pattern: RegExp(`(^|[{}\\s])[^{}\\s](?:[^{};"'\\s]|\\s+(?![\\s{])|` + string.source + ")*(?=\\s*\\{)"),
            lookbehind: true
          },
          "string": {
            pattern: string,
            greedy: true
          },
          "property": {
            pattern: /(^|[^-\w\xA0-\uFFFF])(?!\s)[-_a-z\xA0-\uFFFF](?:(?!\s)[-\w\xA0-\uFFFF])*(?=\s*:)/i,
            lookbehind: true
          },
          "important": /!important\b/i,
          "function": {
            pattern: /(^|[^-a-z0-9])[-a-z0-9]+(?=\()/i,
            lookbehind: true
          },
          "punctuation": /[(){};:,]/
        };
        Prism4.languages.css["atrule"].inside.rest = Prism4.languages.css;
        var markup = Prism4.languages.markup;
        if (markup) {
          markup.tag.addInlined("style", "css");
          markup.tag.addAttribute("style", "css");
        }
      })(Prism3);
      Prism3.languages.clike = {
        "comment": [
          {
            pattern: /(^|[^\\])\/\*[\s\S]*?(?:\*\/|$)/,
            lookbehind: true,
            greedy: true
          },
          {
            pattern: /(^|[^\\:])\/\/.*/,
            lookbehind: true,
            greedy: true
          }
        ],
        "string": {
          pattern: /(["'])(?:\\(?:\r\n|[\s\S])|(?!\1)[^\\\r\n])*\1/,
          greedy: true
        },
        "class-name": {
          pattern: /(\b(?:class|extends|implements|instanceof|interface|new|trait)\s+|\bcatch\s+\()[\w.\\]+/i,
          lookbehind: true,
          inside: {
            "punctuation": /[.\\]/
          }
        },
        "keyword": /\b(?:break|catch|continue|do|else|finally|for|function|if|in|instanceof|new|null|return|throw|try|while)\b/,
        "boolean": /\b(?:false|true)\b/,
        "function": /\b\w+(?=\()/,
        "number": /\b0x[\da-f]+\b|(?:\b\d+(?:\.\d*)?|\B\.\d+)(?:e[+-]?\d+)?/i,
        "operator": /[<>]=?|[!=]=?=?|--?|\+\+?|&&?|\|\|?|[?*/~^%]/,
        "punctuation": /[{}[\];(),.:]/
      };
      Prism3.languages.javascript = Prism3.languages.extend("clike", {
        "class-name": [
          Prism3.languages.clike["class-name"],
          {
            pattern: /(^|[^$\w\xA0-\uFFFF])(?!\s)[_$A-Z\xA0-\uFFFF](?:(?!\s)[$\w\xA0-\uFFFF])*(?=\.(?:constructor|prototype))/,
            lookbehind: true
          }
        ],
        "keyword": [
          {
            pattern: /((?:^|\})\s*)catch\b/,
            lookbehind: true
          },
          {
            pattern: /(^|[^.]|\.\.\.\s*)\b(?:as|assert(?=\s*\{)|async(?=\s*(?:function\b|\(|[$\w\xA0-\uFFFF]|$))|await|break|case|class|const|continue|debugger|default|delete|do|else|enum|export|extends|finally(?=\s*(?:\{|$))|for|from(?=\s*(?:['"]|$))|function|(?:get|set)(?=\s*(?:[#\[$\w\xA0-\uFFFF]|$))|if|implements|import|in|instanceof|interface|let|new|null|of|package|private|protected|public|return|static|super|switch|this|throw|try|typeof|undefined|var|void|while|with|yield)\b/,
            lookbehind: true
          }
        ],
        // Allow for all non-ASCII characters (See http://stackoverflow.com/a/2008444)
        "function": /#?(?!\s)[_$a-zA-Z\xA0-\uFFFF](?:(?!\s)[$\w\xA0-\uFFFF])*(?=\s*(?:\.\s*(?:apply|bind|call)\s*)?\()/,
        "number": {
          pattern: RegExp(
            /(^|[^\w$])/.source + "(?:" + // constant
            (/NaN|Infinity/.source + "|" + // binary integer
            /0[bB][01]+(?:_[01]+)*n?/.source + "|" + // octal integer
            /0[oO][0-7]+(?:_[0-7]+)*n?/.source + "|" + // hexadecimal integer
            /0[xX][\dA-Fa-f]+(?:_[\dA-Fa-f]+)*n?/.source + "|" + // decimal bigint
            /\d+(?:_\d+)*n/.source + "|" + // decimal number (integer or float) but no bigint
            /(?:\d+(?:_\d+)*(?:\.(?:\d+(?:_\d+)*)?)?|\.\d+(?:_\d+)*)(?:[Ee][+-]?\d+(?:_\d+)*)?/.source) + ")" + /(?![\w$])/.source
          ),
          lookbehind: true
        },
        "operator": /--|\+\+|\*\*=?|=>|&&=?|\|\|=?|[!=]==|<<=?|>>>?=?|[-+*/%&|^!=<>]=?|\.{3}|\?\?=?|\?\.?|[~:]/
      });
      Prism3.languages.javascript["class-name"][0].pattern = /(\b(?:class|extends|implements|instanceof|interface|new)\s+)[\w.\\]+/;
      Prism3.languages.insertBefore("javascript", "keyword", {
        "regex": {
          pattern: RegExp(
            // lookbehind
            // eslint-disable-next-line regexp/no-dupe-characters-character-class
            /((?:^|[^$\w\xA0-\uFFFF."'\])\s]|\b(?:return|yield))\s*)/.source + // Regex pattern:
            // There are 2 regex patterns here. The RegExp set notation proposal added support for nested character
            // classes if the `v` flag is present. Unfortunately, nested CCs are both context-free and incompatible
            // with the only syntax, so we have to define 2 different regex patterns.
            /\//.source + "(?:" + /(?:\[(?:[^\]\\\r\n]|\\.)*\]|\\.|[^/\\\[\r\n])+\/[dgimyus]{0,7}/.source + "|" + // `v` flag syntax. This supports 3 levels of nested character classes.
            /(?:\[(?:[^[\]\\\r\n]|\\.|\[(?:[^[\]\\\r\n]|\\.|\[(?:[^[\]\\\r\n]|\\.)*\])*\])*\]|\\.|[^/\\\[\r\n])+\/[dgimyus]{0,7}v[dgimyus]{0,7}/.source + ")" + // lookahead
            /(?=(?:\s|\/\*(?:[^*]|\*(?!\/))*\*\/)*(?:$|[\r\n,.;:})\]]|\/\/))/.source
          ),
          lookbehind: true,
          greedy: true,
          inside: {
            "regex-source": {
              pattern: /^(\/)[\s\S]+(?=\/[a-z]*$)/,
              lookbehind: true,
              alias: "language-regex",
              inside: Prism3.languages.regex
            },
            "regex-delimiter": /^\/|\/$/,
            "regex-flags": /^[a-z]+$/
          }
        },
        // This must be declared before keyword because we use "function" inside the look-forward
        "function-variable": {
          pattern: /#?(?!\s)[_$a-zA-Z\xA0-\uFFFF](?:(?!\s)[$\w\xA0-\uFFFF])*(?=\s*[=:]\s*(?:async\s*)?(?:\bfunction\b|(?:\((?:[^()]|\([^()]*\))*\)|(?!\s)[_$a-zA-Z\xA0-\uFFFF](?:(?!\s)[$\w\xA0-\uFFFF])*)\s*=>))/,
          alias: "function"
        },
        "parameter": [
          {
            pattern: /(function(?:\s+(?!\s)[_$a-zA-Z\xA0-\uFFFF](?:(?!\s)[$\w\xA0-\uFFFF])*)?\s*\(\s*)(?!\s)(?:[^()\s]|\s+(?![\s)])|\([^()]*\))+(?=\s*\))/,
            lookbehind: true,
            inside: Prism3.languages.javascript
          },
          {
            pattern: /(^|[^$\w\xA0-\uFFFF])(?!\s)[_$a-z\xA0-\uFFFF](?:(?!\s)[$\w\xA0-\uFFFF])*(?=\s*=>)/i,
            lookbehind: true,
            inside: Prism3.languages.javascript
          },
          {
            pattern: /(\(\s*)(?!\s)(?:[^()\s]|\s+(?![\s)])|\([^()]*\))+(?=\s*\)\s*=>)/,
            lookbehind: true,
            inside: Prism3.languages.javascript
          },
          {
            pattern: /((?:\b|\s|^)(?!(?:as|async|await|break|case|catch|class|const|continue|debugger|default|delete|do|else|enum|export|extends|finally|for|from|function|get|if|implements|import|in|instanceof|interface|let|new|null|of|package|private|protected|public|return|set|static|super|switch|this|throw|try|typeof|undefined|var|void|while|with|yield)(?![$\w\xA0-\uFFFF]))(?:(?!\s)[_$a-zA-Z\xA0-\uFFFF](?:(?!\s)[$\w\xA0-\uFFFF])*\s*)\(\s*|\]\s*\(\s*)(?!\s)(?:[^()\s]|\s+(?![\s)])|\([^()]*\))+(?=\s*\)\s*\{)/,
            lookbehind: true,
            inside: Prism3.languages.javascript
          }
        ],
        "constant": /\b[A-Z](?:[A-Z_]|\dx?)*\b/
      });
      Prism3.languages.insertBefore("javascript", "string", {
        "hashbang": {
          pattern: /^#!.*/,
          greedy: true,
          alias: "comment"
        },
        "template-string": {
          pattern: /`(?:\\[\s\S]|\$\{(?:[^{}]|\{(?:[^{}]|\{[^}]*\})*\})+\}|(?!\$\{)[^\\`])*`/,
          greedy: true,
          inside: {
            "template-punctuation": {
              pattern: /^`|`$/,
              alias: "string"
            },
            "interpolation": {
              pattern: /((?:^|[^\\])(?:\\{2})*)\$\{(?:[^{}]|\{(?:[^{}]|\{[^}]*\})*\})+\}/,
              lookbehind: true,
              inside: {
                "interpolation-punctuation": {
                  pattern: /^\$\{|\}$/,
                  alias: "punctuation"
                },
                rest: Prism3.languages.javascript
              }
            },
            "string": /[\s\S]+/
          }
        },
        "string-property": {
          pattern: /((?:^|[,{])[ \t]*)(["'])(?:\\(?:\r\n|[\s\S])|(?!\2)[^\\\r\n])*\2(?=\s*:)/m,
          lookbehind: true,
          greedy: true,
          alias: "property"
        }
      });
      Prism3.languages.insertBefore("javascript", "operator", {
        "literal-property": {
          pattern: /((?:^|[,{])[ \t]*)(?!\s)[_$a-zA-Z\xA0-\uFFFF](?:(?!\s)[$\w\xA0-\uFFFF])*(?=\s*:)/m,
          lookbehind: true,
          alias: "property"
        }
      });
      if (Prism3.languages.markup) {
        Prism3.languages.markup.tag.addInlined("script", "javascript");
        Prism3.languages.markup.tag.addAttribute(
          /on(?:abort|blur|change|click|composition(?:end|start|update)|dblclick|error|focus(?:in|out)?|key(?:down|up)|load|mouse(?:down|enter|leave|move|out|over|up)|reset|resize|scroll|select|slotchange|submit|unload|wheel)/.source,
          "javascript"
        );
      }
      Prism3.languages.js = Prism3.languages.javascript;
      (function() {
        if (typeof Prism3 === "undefined" || typeof document === "undefined") {
          return;
        }
        if (!Element.prototype.matches) {
          Element.prototype.matches = Element.prototype.msMatchesSelector || Element.prototype.webkitMatchesSelector;
        }
        var LOADING_MESSAGE = "Loading\u2026";
        var FAILURE_MESSAGE = function(status, message) {
          return "\u2716 Error " + status + " while fetching file: " + message;
        };
        var FAILURE_EMPTY_MESSAGE = "\u2716 Error: File does not exist or is empty";
        var EXTENSIONS = {
          "js": "javascript",
          "py": "python",
          "rb": "ruby",
          "ps1": "powershell",
          "psm1": "powershell",
          "sh": "bash",
          "bat": "batch",
          "h": "c",
          "tex": "latex"
        };
        var STATUS_ATTR = "data-src-status";
        var STATUS_LOADING = "loading";
        var STATUS_LOADED = "loaded";
        var STATUS_FAILED = "failed";
        var SELECTOR = "pre[data-src]:not([" + STATUS_ATTR + '="' + STATUS_LOADED + '"]):not([' + STATUS_ATTR + '="' + STATUS_LOADING + '"])';
        function loadFile(src, success, error) {
          var xhr = new XMLHttpRequest();
          xhr.open("GET", src, true);
          xhr.onreadystatechange = function() {
            if (xhr.readyState == 4) {
              if (xhr.status < 400 && xhr.responseText) {
                success(xhr.responseText);
              } else {
                if (xhr.status >= 400) {
                  error(FAILURE_MESSAGE(xhr.status, xhr.statusText));
                } else {
                  error(FAILURE_EMPTY_MESSAGE);
                }
              }
            }
          };
          xhr.send(null);
        }
        function parseRange(range) {
          var m3 = /^\s*(\d+)\s*(?:(,)\s*(?:(\d+)\s*)?)?$/.exec(range || "");
          if (m3) {
            var start = Number(m3[1]);
            var comma = m3[2];
            var end = m3[3];
            if (!comma) {
              return [start, start];
            }
            if (!end) {
              return [start, void 0];
            }
            return [start, Number(end)];
          }
          return void 0;
        }
        Prism3.hooks.add("before-highlightall", function(env) {
          env.selector += ", " + SELECTOR;
        });
        Prism3.hooks.add("before-sanity-check", function(env) {
          var pre = (
            /** @type {HTMLPreElement} */
            env.element
          );
          if (pre.matches(SELECTOR)) {
            env.code = "";
            pre.setAttribute(STATUS_ATTR, STATUS_LOADING);
            var code = pre.appendChild(document.createElement("CODE"));
            code.textContent = LOADING_MESSAGE;
            var src = pre.getAttribute("data-src");
            var language = env.language;
            if (language === "none") {
              var extension = (/\.(\w+)$/.exec(src) || [, "none"])[1];
              language = EXTENSIONS[extension] || extension;
            }
            Prism3.util.setLanguage(code, language);
            Prism3.util.setLanguage(pre, language);
            var autoloader = Prism3.plugins.autoloader;
            if (autoloader) {
              autoloader.loadLanguages(language);
            }
            loadFile(
              src,
              function(text) {
                pre.setAttribute(STATUS_ATTR, STATUS_LOADED);
                var range = parseRange(pre.getAttribute("data-range"));
                if (range) {
                  var lines = text.split(/\r\n?|\n/g);
                  var start = range[0];
                  var end = range[1] == null ? lines.length : range[1];
                  if (start < 0) {
                    start += lines.length;
                  }
                  start = Math.max(0, Math.min(start - 1, lines.length));
                  if (end < 0) {
                    end += lines.length;
                  }
                  end = Math.max(0, Math.min(end, lines.length));
                  text = lines.slice(start, end).join("\n");
                  if (!pre.hasAttribute("data-start")) {
                    pre.setAttribute("data-start", String(start + 1));
                  }
                }
                code.textContent = text;
                Prism3.highlightElement(code);
              },
              function(error) {
                pre.setAttribute(STATUS_ATTR, STATUS_FAILED);
                code.textContent = error;
              }
            );
          }
        });
        Prism3.plugins.fileHighlight = {
          /**
           * Executes the File Highlight plugin for all matching `pre` elements under the given container.
           *
           * Note: Elements which are already loaded or currently loading will not be touched by this method.
           *
           * @param {ParentNode} [container=document]
           */
          highlight: function highlight(container) {
            var elements = (container || document).querySelectorAll(SELECTOR);
            for (var i8 = 0, element; element = elements[i8++]; ) {
              Prism3.highlightElement(element);
            }
          }
        };
        var logged = false;
        Prism3.fileHighlight = function() {
          if (!logged) {
            console.warn("Prism.fileHighlight is deprecated. Use `Prism.plugins.fileHighlight.highlight` instead.");
            logged = true;
          }
          Prism3.plugins.fileHighlight.highlight.apply(this, arguments);
        };
      })();
    }
  });

  // node_modules/@nldd/design-system/dist/utilities/color-scheme-repaint.js
  function init2() {
    if (observer)
      return;
    observer = new MutationObserver((mutations) => {
      for (const m3 of mutations) {
        if (m3.attributeName === "data-scheme") {
          for (const l4 of listeners)
            l4();
          return;
        }
      }
    });
    observer.observe(document.documentElement, {
      attributes: true,
      attributeFilter: ["data-scheme"]
    });
  }
  function onColorSchemeChange(callback) {
    init2();
    listeners.add(callback);
    return () => {
      listeners.delete(callback);
      if (listeners.size === 0) {
        observer?.disconnect();
        observer = null;
      }
    };
  }
  function forceScrollLayerRepaint(el) {
    const x2 = el.scrollLeft;
    const y3 = el.scrollTop;
    const prevDisplay = el.style.display;
    el.style.display = "none";
    void el.offsetHeight;
    el.style.display = prevDisplay;
    el.scrollLeft = x2;
    el.scrollTop = y3;
  }
  var listeners, observer;
  var init_color_scheme_repaint = __esm({
    "node_modules/@nldd/design-system/dist/utilities/color-scheme-repaint.js"() {
      listeners = /* @__PURE__ */ new Set();
      observer = null;
    }
  });

  // node_modules/prismjs/components/prism-yaml.js
  var require_prism_yaml = __commonJS({
    "node_modules/prismjs/components/prism-yaml.js"() {
      (function(Prism3) {
        var anchorOrAlias = /[*&][^\s[\]{},]+/;
        var tag = /!(?:<[\w\-%#;/?:@&=+$,.!~*'()[\]]+>|(?:[a-zA-Z\d-]*!)?[\w\-%#;/?:@&=+$.~*'()]+)?/;
        var properties = "(?:" + tag.source + "(?:[ 	]+" + anchorOrAlias.source + ")?|" + anchorOrAlias.source + "(?:[ 	]+" + tag.source + ")?)";
        var plainKey = /(?:[^\s\x00-\x08\x0e-\x1f!"#%&'*,\-:>?@[\]`{|}\x7f-\x84\x86-\x9f\ud800-\udfff\ufffe\uffff]|[?:-]<PLAIN>)(?:[ \t]*(?:(?![#:])<PLAIN>|:<PLAIN>))*/.source.replace(/<PLAIN>/g, function() {
          return /[^\s\x00-\x08\x0e-\x1f,[\]{}\x7f-\x84\x86-\x9f\ud800-\udfff\ufffe\uffff]/.source;
        });
        var string = /"(?:[^"\\\r\n]|\\.)*"|'(?:[^'\\\r\n]|\\.)*'/.source;
        function createValuePattern(value, flags) {
          flags = (flags || "").replace(/m/g, "") + "m";
          var pattern = /([:\-,[{]\s*(?:\s<<prop>>[ \t]+)?)(?:<<value>>)(?=[ \t]*(?:$|,|\]|\}|(?:[\r\n]\s*)?#))/.source.replace(/<<prop>>/g, function() {
            return properties;
          }).replace(/<<value>>/g, function() {
            return value;
          });
          return RegExp(pattern, flags);
        }
        Prism3.languages.yaml = {
          "scalar": {
            pattern: RegExp(/([\-:]\s*(?:\s<<prop>>[ \t]+)?[|>])[ \t]*(?:((?:\r?\n|\r)[ \t]+)\S[^\r\n]*(?:\2[^\r\n]+)*)/.source.replace(/<<prop>>/g, function() {
              return properties;
            })),
            lookbehind: true,
            alias: "string"
          },
          "comment": /#.*/,
          "key": {
            pattern: RegExp(/((?:^|[:\-,[{\r\n?])[ \t]*(?:<<prop>>[ \t]+)?)<<key>>(?=\s*:\s)/.source.replace(/<<prop>>/g, function() {
              return properties;
            }).replace(/<<key>>/g, function() {
              return "(?:" + plainKey + "|" + string + ")";
            })),
            lookbehind: true,
            greedy: true,
            alias: "atrule"
          },
          "directive": {
            pattern: /(^[ \t]*)%.+/m,
            lookbehind: true,
            alias: "important"
          },
          "datetime": {
            pattern: createValuePattern(/\d{4}-\d\d?-\d\d?(?:[tT]|[ \t]+)\d\d?:\d{2}:\d{2}(?:\.\d*)?(?:[ \t]*(?:Z|[-+]\d\d?(?::\d{2})?))?|\d{4}-\d{2}-\d{2}|\d\d?:\d{2}(?::\d{2}(?:\.\d*)?)?/.source),
            lookbehind: true,
            alias: "number"
          },
          "boolean": {
            pattern: createValuePattern(/false|true/.source, "i"),
            lookbehind: true,
            alias: "important"
          },
          "null": {
            pattern: createValuePattern(/null|~/.source, "i"),
            lookbehind: true,
            alias: "important"
          },
          "string": {
            pattern: createValuePattern(string),
            lookbehind: true,
            greedy: true
          },
          "number": {
            pattern: createValuePattern(/[+-]?(?:0x[\da-f]+|0o[0-7]+|(?:\d+(?:\.\d*)?|\.\d+)(?:e[+-]?\d+)?|\.inf|\.nan)/.source, "i"),
            lookbehind: true
          },
          "tag": tag,
          "important": anchorOrAlias,
          "punctuation": /---|[:[\]{}\-,|>?]|\.\.\./
        };
        Prism3.languages.yml = Prism3.languages.yaml;
      })(Prism);
    }
  });

  // node_modules/prismjs/components/prism-json.js
  var require_prism_json = __commonJS({
    "node_modules/prismjs/components/prism-json.js"() {
      Prism.languages.json = {
        "property": {
          pattern: /(^|[^\\])"(?:\\.|[^\\"\r\n])*"(?=\s*:)/,
          lookbehind: true,
          greedy: true
        },
        "string": {
          pattern: /(^|[^\\])"(?:\\.|[^\\"\r\n])*"(?!\s*:)/,
          lookbehind: true,
          greedy: true
        },
        "comment": {
          pattern: /\/\/.*|\/\*[\s\S]*?(?:\*\/|$)/,
          greedy: true
        },
        "number": /-?\b\d+(?:\.\d+)?(?:e[+-]?\d+)?\b/i,
        "punctuation": /[{}[\],]/,
        "operator": /:/,
        "boolean": /\b(?:false|true)\b/,
        "null": {
          pattern: /\bnull\b/,
          alias: "keyword"
        }
      };
      Prism.languages.webmanifest = Prism.languages.json;
    }
  });

  // node_modules/prismjs/components/prism-javascript.js
  var require_prism_javascript = __commonJS({
    "node_modules/prismjs/components/prism-javascript.js"() {
      Prism.languages.javascript = Prism.languages.extend("clike", {
        "class-name": [
          Prism.languages.clike["class-name"],
          {
            pattern: /(^|[^$\w\xA0-\uFFFF])(?!\s)[_$A-Z\xA0-\uFFFF](?:(?!\s)[$\w\xA0-\uFFFF])*(?=\.(?:constructor|prototype))/,
            lookbehind: true
          }
        ],
        "keyword": [
          {
            pattern: /((?:^|\})\s*)catch\b/,
            lookbehind: true
          },
          {
            pattern: /(^|[^.]|\.\.\.\s*)\b(?:as|assert(?=\s*\{)|async(?=\s*(?:function\b|\(|[$\w\xA0-\uFFFF]|$))|await|break|case|class|const|continue|debugger|default|delete|do|else|enum|export|extends|finally(?=\s*(?:\{|$))|for|from(?=\s*(?:['"]|$))|function|(?:get|set)(?=\s*(?:[#\[$\w\xA0-\uFFFF]|$))|if|implements|import|in|instanceof|interface|let|new|null|of|package|private|protected|public|return|static|super|switch|this|throw|try|typeof|undefined|var|void|while|with|yield)\b/,
            lookbehind: true
          }
        ],
        // Allow for all non-ASCII characters (See http://stackoverflow.com/a/2008444)
        "function": /#?(?!\s)[_$a-zA-Z\xA0-\uFFFF](?:(?!\s)[$\w\xA0-\uFFFF])*(?=\s*(?:\.\s*(?:apply|bind|call)\s*)?\()/,
        "number": {
          pattern: RegExp(
            /(^|[^\w$])/.source + "(?:" + // constant
            (/NaN|Infinity/.source + "|" + // binary integer
            /0[bB][01]+(?:_[01]+)*n?/.source + "|" + // octal integer
            /0[oO][0-7]+(?:_[0-7]+)*n?/.source + "|" + // hexadecimal integer
            /0[xX][\dA-Fa-f]+(?:_[\dA-Fa-f]+)*n?/.source + "|" + // decimal bigint
            /\d+(?:_\d+)*n/.source + "|" + // decimal number (integer or float) but no bigint
            /(?:\d+(?:_\d+)*(?:\.(?:\d+(?:_\d+)*)?)?|\.\d+(?:_\d+)*)(?:[Ee][+-]?\d+(?:_\d+)*)?/.source) + ")" + /(?![\w$])/.source
          ),
          lookbehind: true
        },
        "operator": /--|\+\+|\*\*=?|=>|&&=?|\|\|=?|[!=]==|<<=?|>>>?=?|[-+*/%&|^!=<>]=?|\.{3}|\?\?=?|\?\.?|[~:]/
      });
      Prism.languages.javascript["class-name"][0].pattern = /(\b(?:class|extends|implements|instanceof|interface|new)\s+)[\w.\\]+/;
      Prism.languages.insertBefore("javascript", "keyword", {
        "regex": {
          pattern: RegExp(
            // lookbehind
            // eslint-disable-next-line regexp/no-dupe-characters-character-class
            /((?:^|[^$\w\xA0-\uFFFF."'\])\s]|\b(?:return|yield))\s*)/.source + // Regex pattern:
            // There are 2 regex patterns here. The RegExp set notation proposal added support for nested character
            // classes if the `v` flag is present. Unfortunately, nested CCs are both context-free and incompatible
            // with the only syntax, so we have to define 2 different regex patterns.
            /\//.source + "(?:" + /(?:\[(?:[^\]\\\r\n]|\\.)*\]|\\.|[^/\\\[\r\n])+\/[dgimyus]{0,7}/.source + "|" + // `v` flag syntax. This supports 3 levels of nested character classes.
            /(?:\[(?:[^[\]\\\r\n]|\\.|\[(?:[^[\]\\\r\n]|\\.|\[(?:[^[\]\\\r\n]|\\.)*\])*\])*\]|\\.|[^/\\\[\r\n])+\/[dgimyus]{0,7}v[dgimyus]{0,7}/.source + ")" + // lookahead
            /(?=(?:\s|\/\*(?:[^*]|\*(?!\/))*\*\/)*(?:$|[\r\n,.;:})\]]|\/\/))/.source
          ),
          lookbehind: true,
          greedy: true,
          inside: {
            "regex-source": {
              pattern: /^(\/)[\s\S]+(?=\/[a-z]*$)/,
              lookbehind: true,
              alias: "language-regex",
              inside: Prism.languages.regex
            },
            "regex-delimiter": /^\/|\/$/,
            "regex-flags": /^[a-z]+$/
          }
        },
        // This must be declared before keyword because we use "function" inside the look-forward
        "function-variable": {
          pattern: /#?(?!\s)[_$a-zA-Z\xA0-\uFFFF](?:(?!\s)[$\w\xA0-\uFFFF])*(?=\s*[=:]\s*(?:async\s*)?(?:\bfunction\b|(?:\((?:[^()]|\([^()]*\))*\)|(?!\s)[_$a-zA-Z\xA0-\uFFFF](?:(?!\s)[$\w\xA0-\uFFFF])*)\s*=>))/,
          alias: "function"
        },
        "parameter": [
          {
            pattern: /(function(?:\s+(?!\s)[_$a-zA-Z\xA0-\uFFFF](?:(?!\s)[$\w\xA0-\uFFFF])*)?\s*\(\s*)(?!\s)(?:[^()\s]|\s+(?![\s)])|\([^()]*\))+(?=\s*\))/,
            lookbehind: true,
            inside: Prism.languages.javascript
          },
          {
            pattern: /(^|[^$\w\xA0-\uFFFF])(?!\s)[_$a-z\xA0-\uFFFF](?:(?!\s)[$\w\xA0-\uFFFF])*(?=\s*=>)/i,
            lookbehind: true,
            inside: Prism.languages.javascript
          },
          {
            pattern: /(\(\s*)(?!\s)(?:[^()\s]|\s+(?![\s)])|\([^()]*\))+(?=\s*\)\s*=>)/,
            lookbehind: true,
            inside: Prism.languages.javascript
          },
          {
            pattern: /((?:\b|\s|^)(?!(?:as|async|await|break|case|catch|class|const|continue|debugger|default|delete|do|else|enum|export|extends|finally|for|from|function|get|if|implements|import|in|instanceof|interface|let|new|null|of|package|private|protected|public|return|set|static|super|switch|this|throw|try|typeof|undefined|var|void|while|with|yield)(?![$\w\xA0-\uFFFF]))(?:(?!\s)[_$a-zA-Z\xA0-\uFFFF](?:(?!\s)[$\w\xA0-\uFFFF])*\s*)\(\s*|\]\s*\(\s*)(?!\s)(?:[^()\s]|\s+(?![\s)])|\([^()]*\))+(?=\s*\)\s*\{)/,
            lookbehind: true,
            inside: Prism.languages.javascript
          }
        ],
        "constant": /\b[A-Z](?:[A-Z_]|\dx?)*\b/
      });
      Prism.languages.insertBefore("javascript", "string", {
        "hashbang": {
          pattern: /^#!.*/,
          greedy: true,
          alias: "comment"
        },
        "template-string": {
          pattern: /`(?:\\[\s\S]|\$\{(?:[^{}]|\{(?:[^{}]|\{[^}]*\})*\})+\}|(?!\$\{)[^\\`])*`/,
          greedy: true,
          inside: {
            "template-punctuation": {
              pattern: /^`|`$/,
              alias: "string"
            },
            "interpolation": {
              pattern: /((?:^|[^\\])(?:\\{2})*)\$\{(?:[^{}]|\{(?:[^{}]|\{[^}]*\})*\})+\}/,
              lookbehind: true,
              inside: {
                "interpolation-punctuation": {
                  pattern: /^\$\{|\}$/,
                  alias: "punctuation"
                },
                rest: Prism.languages.javascript
              }
            },
            "string": /[\s\S]+/
          }
        },
        "string-property": {
          pattern: /((?:^|[,{])[ \t]*)(["'])(?:\\(?:\r\n|[\s\S])|(?!\2)[^\\\r\n])*\2(?=\s*:)/m,
          lookbehind: true,
          greedy: true,
          alias: "property"
        }
      });
      Prism.languages.insertBefore("javascript", "operator", {
        "literal-property": {
          pattern: /((?:^|[,{])[ \t]*)(?!\s)[_$a-zA-Z\xA0-\uFFFF](?:(?!\s)[$\w\xA0-\uFFFF])*(?=\s*:)/m,
          lookbehind: true,
          alias: "property"
        }
      });
      if (Prism.languages.markup) {
        Prism.languages.markup.tag.addInlined("script", "javascript");
        Prism.languages.markup.tag.addAttribute(
          /on(?:abort|blur|change|click|composition(?:end|start|update)|dblclick|error|focus(?:in|out)?|key(?:down|up)|load|mouse(?:down|enter|leave|move|out|over|up)|reset|resize|scroll|select|slotchange|submit|unload|wheel)/.source,
          "javascript"
        );
      }
      Prism.languages.js = Prism.languages.javascript;
    }
  });

  // node_modules/prismjs/components/prism-typescript.js
  var require_prism_typescript = __commonJS({
    "node_modules/prismjs/components/prism-typescript.js"() {
      (function(Prism3) {
        Prism3.languages.typescript = Prism3.languages.extend("javascript", {
          "class-name": {
            pattern: /(\b(?:class|extends|implements|instanceof|interface|new|type)\s+)(?!keyof\b)(?!\s)[_$a-zA-Z\xA0-\uFFFF](?:(?!\s)[$\w\xA0-\uFFFF])*(?:\s*<(?:[^<>]|<(?:[^<>]|<[^<>]*>)*>)*>)?/,
            lookbehind: true,
            greedy: true,
            inside: null
            // see below
          },
          "builtin": /\b(?:Array|Function|Promise|any|boolean|console|never|number|string|symbol|unknown)\b/
        });
        Prism3.languages.typescript.keyword.push(
          /\b(?:abstract|declare|is|keyof|readonly|require)\b/,
          // keywords that have to be followed by an identifier
          /\b(?:asserts|infer|interface|module|namespace|type)\b(?=\s*(?:[{_$a-zA-Z\xA0-\uFFFF]|$))/,
          // This is for `import type *, {}`
          /\btype\b(?=\s*(?:[\{*]|$))/
        );
        delete Prism3.languages.typescript["parameter"];
        delete Prism3.languages.typescript["literal-property"];
        var typeInside = Prism3.languages.extend("typescript", {});
        delete typeInside["class-name"];
        Prism3.languages.typescript["class-name"].inside = typeInside;
        Prism3.languages.insertBefore("typescript", "function", {
          "decorator": {
            pattern: /@[$\w\xA0-\uFFFF]+/,
            inside: {
              "at": {
                pattern: /^@/,
                alias: "operator"
              },
              "function": /^[\s\S]+/
            }
          },
          "generic-function": {
            // e.g. foo<T extends "bar" | "baz">( ...
            pattern: /#?(?!\s)[_$a-zA-Z\xA0-\uFFFF](?:(?!\s)[$\w\xA0-\uFFFF])*\s*<(?:[^<>]|<(?:[^<>]|<[^<>]*>)*>)*>(?=\s*\()/,
            greedy: true,
            inside: {
              "function": /^#?(?!\s)[_$a-zA-Z\xA0-\uFFFF](?:(?!\s)[$\w\xA0-\uFFFF])*/,
              "generic": {
                pattern: /<[\s\S]+/,
                // everything after the first <
                alias: "class-name",
                inside: typeInside
              }
            }
          }
        });
        Prism3.languages.ts = Prism3.languages.typescript;
      })(Prism);
    }
  });

  // node_modules/prismjs/components/prism-css.js
  var require_prism_css = __commonJS({
    "node_modules/prismjs/components/prism-css.js"() {
      (function(Prism3) {
        var string = /(?:"(?:\\(?:\r\n|[\s\S])|[^"\\\r\n])*"|'(?:\\(?:\r\n|[\s\S])|[^'\\\r\n])*')/;
        Prism3.languages.css = {
          "comment": /\/\*[\s\S]*?\*\//,
          "atrule": {
            pattern: RegExp("@[\\w-](?:" + /[^;{\s"']|\s+(?!\s)/.source + "|" + string.source + ")*?" + /(?:;|(?=\s*\{))/.source),
            inside: {
              "rule": /^@[\w-]+/,
              "selector-function-argument": {
                pattern: /(\bselector\s*\(\s*(?![\s)]))(?:[^()\s]|\s+(?![\s)])|\((?:[^()]|\([^()]*\))*\))+(?=\s*\))/,
                lookbehind: true,
                alias: "selector"
              },
              "keyword": {
                pattern: /(^|[^\w-])(?:and|not|only|or)(?![\w-])/,
                lookbehind: true
              }
              // See rest below
            }
          },
          "url": {
            // https://drafts.csswg.org/css-values-3/#urls
            pattern: RegExp("\\burl\\((?:" + string.source + "|" + /(?:[^\\\r\n()"']|\\[\s\S])*/.source + ")\\)", "i"),
            greedy: true,
            inside: {
              "function": /^url/i,
              "punctuation": /^\(|\)$/,
              "string": {
                pattern: RegExp("^" + string.source + "$"),
                alias: "url"
              }
            }
          },
          "selector": {
            pattern: RegExp(`(^|[{}\\s])[^{}\\s](?:[^{};"'\\s]|\\s+(?![\\s{])|` + string.source + ")*(?=\\s*\\{)"),
            lookbehind: true
          },
          "string": {
            pattern: string,
            greedy: true
          },
          "property": {
            pattern: /(^|[^-\w\xA0-\uFFFF])(?!\s)[-_a-z\xA0-\uFFFF](?:(?!\s)[-\w\xA0-\uFFFF])*(?=\s*:)/i,
            lookbehind: true
          },
          "important": /!important\b/i,
          "function": {
            pattern: /(^|[^-a-z0-9])[-a-z0-9]+(?=\()/i,
            lookbehind: true
          },
          "punctuation": /[(){};:,]/
        };
        Prism3.languages.css["atrule"].inside.rest = Prism3.languages.css;
        var markup = Prism3.languages.markup;
        if (markup) {
          markup.tag.addInlined("style", "css");
          markup.tag.addAttribute("style", "css");
        }
      })(Prism);
    }
  });

  // node_modules/prismjs/components/prism-markup.js
  var require_prism_markup = __commonJS({
    "node_modules/prismjs/components/prism-markup.js"() {
      Prism.languages.markup = {
        "comment": {
          pattern: /<!--(?:(?!<!--)[\s\S])*?-->/,
          greedy: true
        },
        "prolog": {
          pattern: /<\?[\s\S]+?\?>/,
          greedy: true
        },
        "doctype": {
          // https://www.w3.org/TR/xml/#NT-doctypedecl
          pattern: /<!DOCTYPE(?:[^>"'[\]]|"[^"]*"|'[^']*')+(?:\[(?:[^<"'\]]|"[^"]*"|'[^']*'|<(?!!--)|<!--(?:[^-]|-(?!->))*-->)*\]\s*)?>/i,
          greedy: true,
          inside: {
            "internal-subset": {
              pattern: /(^[^\[]*\[)[\s\S]+(?=\]>$)/,
              lookbehind: true,
              greedy: true,
              inside: null
              // see below
            },
            "string": {
              pattern: /"[^"]*"|'[^']*'/,
              greedy: true
            },
            "punctuation": /^<!|>$|[[\]]/,
            "doctype-tag": /^DOCTYPE/i,
            "name": /[^\s<>'"]+/
          }
        },
        "cdata": {
          pattern: /<!\[CDATA\[[\s\S]*?\]\]>/i,
          greedy: true
        },
        "tag": {
          pattern: /<\/?(?!\d)[^\s>\/=$<%]+(?:\s(?:\s*[^\s>\/=]+(?:\s*=\s*(?:"[^"]*"|'[^']*'|[^\s'">=]+(?=[\s>]))|(?=[\s/>])))+)?\s*\/?>/,
          greedy: true,
          inside: {
            "tag": {
              pattern: /^<\/?[^\s>\/]+/,
              inside: {
                "punctuation": /^<\/?/,
                "namespace": /^[^\s>\/:]+:/
              }
            },
            "special-attr": [],
            "attr-value": {
              pattern: /=\s*(?:"[^"]*"|'[^']*'|[^\s'">=]+)/,
              inside: {
                "punctuation": [
                  {
                    pattern: /^=/,
                    alias: "attr-equals"
                  },
                  {
                    pattern: /^(\s*)["']|["']$/,
                    lookbehind: true
                  }
                ]
              }
            },
            "punctuation": /\/?>/,
            "attr-name": {
              pattern: /[^\s>\/]+/,
              inside: {
                "namespace": /^[^\s>\/:]+:/
              }
            }
          }
        },
        "entity": [
          {
            pattern: /&[\da-z]{1,8};/i,
            alias: "named-entity"
          },
          /&#x?[\da-f]{1,8};/i
        ]
      };
      Prism.languages.markup["tag"].inside["attr-value"].inside["entity"] = Prism.languages.markup["entity"];
      Prism.languages.markup["doctype"].inside["internal-subset"].inside = Prism.languages.markup;
      Prism.hooks.add("wrap", function(env) {
        if (env.type === "entity") {
          env.attributes["title"] = env.content.replace(/&amp;/, "&");
        }
      });
      Object.defineProperty(Prism.languages.markup.tag, "addInlined", {
        /**
         * Adds an inlined language to markup.
         *
         * An example of an inlined language is CSS with `<style>` tags.
         *
         * @param {string} tagName The name of the tag that contains the inlined language. This name will be treated as
         * case insensitive.
         * @param {string} lang The language key.
         * @example
         * addInlined('style', 'css');
         */
        value: function addInlined(tagName, lang) {
          var includedCdataInside = {};
          includedCdataInside["language-" + lang] = {
            pattern: /(^<!\[CDATA\[)[\s\S]+?(?=\]\]>$)/i,
            lookbehind: true,
            inside: Prism.languages[lang]
          };
          includedCdataInside["cdata"] = /^<!\[CDATA\[|\]\]>$/i;
          var inside = {
            "included-cdata": {
              pattern: /<!\[CDATA\[[\s\S]*?\]\]>/i,
              inside: includedCdataInside
            }
          };
          inside["language-" + lang] = {
            pattern: /[\s\S]+/,
            inside: Prism.languages[lang]
          };
          var def = {};
          def[tagName] = {
            pattern: RegExp(/(<__[^>]*>)(?:<!\[CDATA\[(?:[^\]]|\](?!\]>))*\]\]>|(?!<!\[CDATA\[)[\s\S])*?(?=<\/__>)/.source.replace(/__/g, function() {
              return tagName;
            }), "i"),
            lookbehind: true,
            greedy: true,
            inside
          };
          Prism.languages.insertBefore("markup", "cdata", def);
        }
      });
      Object.defineProperty(Prism.languages.markup.tag, "addAttribute", {
        /**
         * Adds an pattern to highlight languages embedded in HTML attributes.
         *
         * An example of an inlined language is CSS with `style` attributes.
         *
         * @param {string} attrName The name of the tag that contains the inlined language. This name will be treated as
         * case insensitive.
         * @param {string} lang The language key.
         * @example
         * addAttribute('style', 'css');
         */
        value: function(attrName, lang) {
          Prism.languages.markup.tag.inside["special-attr"].push({
            pattern: RegExp(
              /(^|["'\s])/.source + "(?:" + attrName + ")" + /\s*=\s*(?:"[^"]*"|'[^']*'|[^\s'">=]+(?=[\s>]))/.source,
              "i"
            ),
            lookbehind: true,
            inside: {
              "attr-name": /^[^\s=]+/,
              "attr-value": {
                pattern: /=[\s\S]+/,
                inside: {
                  "value": {
                    pattern: /(^=\s*(["']|(?!["'])))\S[\s\S]*(?=\2$)/,
                    lookbehind: true,
                    alias: [lang, "language-" + lang],
                    inside: Prism.languages[lang]
                  },
                  "punctuation": [
                    {
                      pattern: /^=/,
                      alias: "attr-equals"
                    },
                    /"|'/
                  ]
                }
              }
            }
          });
        }
      });
      Prism.languages.html = Prism.languages.markup;
      Prism.languages.mathml = Prism.languages.markup;
      Prism.languages.svg = Prism.languages.markup;
      Prism.languages.xml = Prism.languages.extend("markup", {});
      Prism.languages.ssml = Prism.languages.xml;
      Prism.languages.atom = Prism.languages.xml;
      Prism.languages.rss = Prism.languages.xml;
    }
  });

  // node_modules/prismjs/components/prism-bash.js
  var require_prism_bash = __commonJS({
    "node_modules/prismjs/components/prism-bash.js"() {
      (function(Prism3) {
        var envVars = "\\b(?:BASH|BASHOPTS|BASH_ALIASES|BASH_ARGC|BASH_ARGV|BASH_CMDS|BASH_COMPLETION_COMPAT_DIR|BASH_LINENO|BASH_REMATCH|BASH_SOURCE|BASH_VERSINFO|BASH_VERSION|COLORTERM|COLUMNS|COMP_WORDBREAKS|DBUS_SESSION_BUS_ADDRESS|DEFAULTS_PATH|DESKTOP_SESSION|DIRSTACK|DISPLAY|EUID|GDMSESSION|GDM_LANG|GNOME_KEYRING_CONTROL|GNOME_KEYRING_PID|GPG_AGENT_INFO|GROUPS|HISTCONTROL|HISTFILE|HISTFILESIZE|HISTSIZE|HOME|HOSTNAME|HOSTTYPE|IFS|INSTANCE|JOB|LANG|LANGUAGE|LC_ADDRESS|LC_ALL|LC_IDENTIFICATION|LC_MEASUREMENT|LC_MONETARY|LC_NAME|LC_NUMERIC|LC_PAPER|LC_TELEPHONE|LC_TIME|LESSCLOSE|LESSOPEN|LINES|LOGNAME|LS_COLORS|MACHTYPE|MAILCHECK|MANDATORY_PATH|NO_AT_BRIDGE|OLDPWD|OPTERR|OPTIND|ORBIT_SOCKETDIR|OSTYPE|PAPERSIZE|PATH|PIPESTATUS|PPID|PS1|PS2|PS3|PS4|PWD|RANDOM|REPLY|SECONDS|SELINUX_INIT|SESSION|SESSIONTYPE|SESSION_MANAGER|SHELL|SHELLOPTS|SHLVL|SSH_AUTH_SOCK|TERM|UID|UPSTART_EVENTS|UPSTART_INSTANCE|UPSTART_JOB|UPSTART_SESSION|USER|WINDOWID|XAUTHORITY|XDG_CONFIG_DIRS|XDG_CURRENT_DESKTOP|XDG_DATA_DIRS|XDG_GREETER_DATA_DIR|XDG_MENU_PREFIX|XDG_RUNTIME_DIR|XDG_SEAT|XDG_SEAT_PATH|XDG_SESSION_DESKTOP|XDG_SESSION_ID|XDG_SESSION_PATH|XDG_SESSION_TYPE|XDG_VTNR|XMODIFIERS)\\b";
        var commandAfterHeredoc = {
          pattern: /(^(["']?)\w+\2)[ \t]+\S.*/,
          lookbehind: true,
          alias: "punctuation",
          // this looks reasonably well in all themes
          inside: null
          // see below
        };
        var insideString = {
          "bash": commandAfterHeredoc,
          "environment": {
            pattern: RegExp("\\$" + envVars),
            alias: "constant"
          },
          "variable": [
            // [0]: Arithmetic Environment
            {
              pattern: /\$?\(\([\s\S]+?\)\)/,
              greedy: true,
              inside: {
                // If there is a $ sign at the beginning highlight $(( and )) as variable
                "variable": [
                  {
                    pattern: /(^\$\(\([\s\S]+)\)\)/,
                    lookbehind: true
                  },
                  /^\$\(\(/
                ],
                "number": /\b0x[\dA-Fa-f]+\b|(?:\b\d+(?:\.\d*)?|\B\.\d+)(?:[Ee]-?\d+)?/,
                // Operators according to https://www.gnu.org/software/bash/manual/bashref.html#Shell-Arithmetic
                "operator": /--|\+\+|\*\*=?|<<=?|>>=?|&&|\|\||[=!+\-*/%<>^&|]=?|[?~:]/,
                // If there is no $ sign at the beginning highlight (( and )) as punctuation
                "punctuation": /\(\(?|\)\)?|,|;/
              }
            },
            // [1]: Command Substitution
            {
              pattern: /\$\((?:\([^)]+\)|[^()])+\)|`[^`]+`/,
              greedy: true,
              inside: {
                "variable": /^\$\(|^`|\)$|`$/
              }
            },
            // [2]: Brace expansion
            {
              pattern: /\$\{[^}]+\}/,
              greedy: true,
              inside: {
                "operator": /:[-=?+]?|[!\/]|##?|%%?|\^\^?|,,?/,
                "punctuation": /[\[\]]/,
                "environment": {
                  pattern: RegExp("(\\{)" + envVars),
                  lookbehind: true,
                  alias: "constant"
                }
              }
            },
            /\$(?:\w+|[#?*!@$])/
          ],
          // Escape sequences from echo and printf's manuals, and escaped quotes.
          "entity": /\\(?:[abceEfnrtv\\"]|O?[0-7]{1,3}|U[0-9a-fA-F]{8}|u[0-9a-fA-F]{4}|x[0-9a-fA-F]{1,2})/
        };
        Prism3.languages.bash = {
          "shebang": {
            pattern: /^#!\s*\/.*/,
            alias: "important"
          },
          "comment": {
            pattern: /(^|[^"{\\$])#.*/,
            lookbehind: true
          },
          "function-name": [
            // a) function foo {
            // b) foo() {
            // c) function foo() {
            // but not “foo {”
            {
              // a) and c)
              pattern: /(\bfunction\s+)[\w-]+(?=(?:\s*\(?:\s*\))?\s*\{)/,
              lookbehind: true,
              alias: "function"
            },
            {
              // b)
              pattern: /\b[\w-]+(?=\s*\(\s*\)\s*\{)/,
              alias: "function"
            }
          ],
          // Highlight variable names as variables in for and select beginnings.
          "for-or-select": {
            pattern: /(\b(?:for|select)\s+)\w+(?=\s+in\s)/,
            alias: "variable",
            lookbehind: true
          },
          // Highlight variable names as variables in the left-hand part
          // of assignments (“=” and “+=”).
          "assign-left": {
            pattern: /(^|[\s;|&]|[<>]\()\w+(?:\.\w+)*(?=\+?=)/,
            inside: {
              "environment": {
                pattern: RegExp("(^|[\\s;|&]|[<>]\\()" + envVars),
                lookbehind: true,
                alias: "constant"
              }
            },
            alias: "variable",
            lookbehind: true
          },
          // Highlight parameter names as variables
          "parameter": {
            pattern: /(^|\s)-{1,2}(?:\w+:[+-]?)?\w+(?:\.\w+)*(?=[=\s]|$)/,
            alias: "variable",
            lookbehind: true
          },
          "string": [
            // Support for Here-documents https://en.wikipedia.org/wiki/Here_document
            {
              pattern: /((?:^|[^<])<<-?\s*)(\w+)\s[\s\S]*?(?:\r?\n|\r)\2/,
              lookbehind: true,
              greedy: true,
              inside: insideString
            },
            // Here-document with quotes around the tag
            // → No expansion (so no “inside”).
            {
              pattern: /((?:^|[^<])<<-?\s*)(["'])(\w+)\2\s[\s\S]*?(?:\r?\n|\r)\3/,
              lookbehind: true,
              greedy: true,
              inside: {
                "bash": commandAfterHeredoc
              }
            },
            // “Normal” string
            {
              // https://www.gnu.org/software/bash/manual/html_node/Double-Quotes.html
              pattern: /(^|[^\\](?:\\\\)*)"(?:\\[\s\S]|\$\([^)]+\)|\$(?!\()|`[^`]+`|[^"\\`$])*"/,
              lookbehind: true,
              greedy: true,
              inside: insideString
            },
            {
              // https://www.gnu.org/software/bash/manual/html_node/Single-Quotes.html
              pattern: /(^|[^$\\])'[^']*'/,
              lookbehind: true,
              greedy: true
            },
            {
              // https://www.gnu.org/software/bash/manual/html_node/ANSI_002dC-Quoting.html
              pattern: /\$'(?:[^'\\]|\\[\s\S])*'/,
              greedy: true,
              inside: {
                "entity": insideString.entity
              }
            }
          ],
          "environment": {
            pattern: RegExp("\\$?" + envVars),
            alias: "constant"
          },
          "variable": insideString.variable,
          "function": {
            pattern: /(^|[\s;|&]|[<>]\()(?:add|apropos|apt|apt-cache|apt-get|aptitude|aspell|automysqlbackup|awk|basename|bash|bc|bconsole|bg|bzip2|cal|cargo|cat|cfdisk|chgrp|chkconfig|chmod|chown|chroot|cksum|clear|cmp|column|comm|composer|cp|cron|crontab|csplit|curl|cut|date|dc|dd|ddrescue|debootstrap|df|diff|diff3|dig|dir|dircolors|dirname|dirs|dmesg|docker|docker-compose|du|egrep|eject|env|ethtool|expand|expect|expr|fdformat|fdisk|fg|fgrep|file|find|fmt|fold|format|free|fsck|ftp|fuser|gawk|git|gparted|grep|groupadd|groupdel|groupmod|groups|grub-mkconfig|gzip|halt|head|hg|history|host|hostname|htop|iconv|id|ifconfig|ifdown|ifup|import|install|ip|java|jobs|join|kill|killall|less|link|ln|locate|logname|logrotate|look|lpc|lpr|lprint|lprintd|lprintq|lprm|ls|lsof|lynx|make|man|mc|mdadm|mkconfig|mkdir|mke2fs|mkfifo|mkfs|mkisofs|mknod|mkswap|mmv|more|most|mount|mtools|mtr|mutt|mv|nano|nc|netstat|nice|nl|node|nohup|notify-send|npm|nslookup|op|open|parted|passwd|paste|pathchk|ping|pkill|pnpm|podman|podman-compose|popd|pr|printcap|printenv|ps|pushd|pv|quota|quotacheck|quotactl|ram|rar|rcp|reboot|remsync|rename|renice|rev|rm|rmdir|rpm|rsync|scp|screen|sdiff|sed|sendmail|seq|service|sftp|sh|shellcheck|shuf|shutdown|sleep|slocate|sort|split|ssh|stat|strace|su|sudo|sum|suspend|swapon|sync|sysctl|tac|tail|tar|tee|time|timeout|top|touch|tr|traceroute|tsort|tty|umount|uname|unexpand|uniq|units|unrar|unshar|unzip|update-grub|uptime|useradd|userdel|usermod|users|uudecode|uuencode|v|vcpkg|vdir|vi|vim|virsh|vmstat|wait|watch|wc|wget|whereis|which|who|whoami|write|xargs|xdg-open|yarn|yes|zenity|zip|zsh|zypper)(?=$|[)\s;|&])/,
            lookbehind: true
          },
          "keyword": {
            pattern: /(^|[\s;|&]|[<>]\()(?:case|do|done|elif|else|esac|fi|for|function|if|in|select|then|until|while)(?=$|[)\s;|&])/,
            lookbehind: true
          },
          // https://www.gnu.org/software/bash/manual/html_node/Shell-Builtin-Commands.html
          "builtin": {
            pattern: /(^|[\s;|&]|[<>]\()(?:\.|:|alias|bind|break|builtin|caller|cd|command|continue|declare|echo|enable|eval|exec|exit|export|getopts|hash|help|let|local|logout|mapfile|printf|pwd|read|readarray|readonly|return|set|shift|shopt|source|test|times|trap|type|typeset|ulimit|umask|unalias|unset)(?=$|[)\s;|&])/,
            lookbehind: true,
            // Alias added to make those easier to distinguish from strings.
            alias: "class-name"
          },
          "boolean": {
            pattern: /(^|[\s;|&]|[<>]\()(?:false|true)(?=$|[)\s;|&])/,
            lookbehind: true
          },
          "file-descriptor": {
            pattern: /\B&\d\b/,
            alias: "important"
          },
          "operator": {
            // Lots of redirections here, but not just that.
            pattern: /\d?<>|>\||\+=|=[=~]?|!=?|<<[<-]?|[&\d]?>>|\d[<>]&?|[<>][&=]?|&[>&]?|\|[&|]?/,
            inside: {
              "file-descriptor": {
                pattern: /^\d/,
                alias: "important"
              }
            }
          },
          "punctuation": /\$?\(\(?|\)\)?|\.\.|[{}[\];\\]/,
          "number": {
            pattern: /(^|\s)(?:[1-9]\d*|0)(?:[.,]\d+)?\b/,
            lookbehind: true
          }
        };
        commandAfterHeredoc.inside = Prism3.languages.bash;
        var toBeCopied = [
          "comment",
          "function-name",
          "for-or-select",
          "assign-left",
          "parameter",
          "string",
          "environment",
          "function",
          "keyword",
          "builtin",
          "boolean",
          "file-descriptor",
          "operator",
          "punctuation",
          "number"
        ];
        var inside = insideString.variable[1].inside;
        for (var i8 = 0; i8 < toBeCopied.length; i8++) {
          inside[toBeCopied[i8]] = Prism3.languages.bash[toBeCopied[i8]];
        }
        Prism3.languages.sh = Prism3.languages.bash;
        Prism3.languages.shell = Prism3.languages.bash;
      })(Prism);
    }
  });

  // node_modules/prismjs/components/prism-markdown.js
  var require_prism_markdown = __commonJS({
    "node_modules/prismjs/components/prism-markdown.js"() {
      (function(Prism3) {
        var inner = /(?:\\.|[^\\\n\r]|(?:\n|\r\n?)(?![\r\n]))/.source;
        function createInline(pattern) {
          pattern = pattern.replace(/<inner>/g, function() {
            return inner;
          });
          return RegExp(/((?:^|[^\\])(?:\\{2})*)/.source + "(?:" + pattern + ")");
        }
        var tableCell = /(?:\\.|``(?:[^`\r\n]|`(?!`))+``|`[^`\r\n]+`|[^\\|\r\n`])+/.source;
        var tableRow = /\|?__(?:\|__)+\|?(?:(?:\n|\r\n?)|(?![\s\S]))/.source.replace(/__/g, function() {
          return tableCell;
        });
        var tableLine = /\|?[ \t]*:?-{3,}:?[ \t]*(?:\|[ \t]*:?-{3,}:?[ \t]*)+\|?(?:\n|\r\n?)/.source;
        Prism3.languages.markdown = Prism3.languages.extend("markup", {});
        Prism3.languages.insertBefore("markdown", "prolog", {
          "front-matter-block": {
            pattern: /(^(?:\s*[\r\n])?)---(?!.)[\s\S]*?[\r\n]---(?!.)/,
            lookbehind: true,
            greedy: true,
            inside: {
              "punctuation": /^---|---$/,
              "front-matter": {
                pattern: /\S+(?:\s+\S+)*/,
                alias: ["yaml", "language-yaml"],
                inside: Prism3.languages.yaml
              }
            }
          },
          "blockquote": {
            // > ...
            pattern: /^>(?:[\t ]*>)*/m,
            alias: "punctuation"
          },
          "table": {
            pattern: RegExp("^" + tableRow + tableLine + "(?:" + tableRow + ")*", "m"),
            inside: {
              "table-data-rows": {
                pattern: RegExp("^(" + tableRow + tableLine + ")(?:" + tableRow + ")*$"),
                lookbehind: true,
                inside: {
                  "table-data": {
                    pattern: RegExp(tableCell),
                    inside: Prism3.languages.markdown
                  },
                  "punctuation": /\|/
                }
              },
              "table-line": {
                pattern: RegExp("^(" + tableRow + ")" + tableLine + "$"),
                lookbehind: true,
                inside: {
                  "punctuation": /\||:?-{3,}:?/
                }
              },
              "table-header-row": {
                pattern: RegExp("^" + tableRow + "$"),
                inside: {
                  "table-header": {
                    pattern: RegExp(tableCell),
                    alias: "important",
                    inside: Prism3.languages.markdown
                  },
                  "punctuation": /\|/
                }
              }
            }
          },
          "code": [
            {
              // Prefixed by 4 spaces or 1 tab and preceded by an empty line
              pattern: /((?:^|\n)[ \t]*\n|(?:^|\r\n?)[ \t]*\r\n?)(?: {4}|\t).+(?:(?:\n|\r\n?)(?: {4}|\t).+)*/,
              lookbehind: true,
              alias: "keyword"
            },
            {
              // ```optional language
              // code block
              // ```
              pattern: /^```[\s\S]*?^```$/m,
              greedy: true,
              inside: {
                "code-block": {
                  pattern: /^(```.*(?:\n|\r\n?))[\s\S]+?(?=(?:\n|\r\n?)^```$)/m,
                  lookbehind: true
                },
                "code-language": {
                  pattern: /^(```).+/,
                  lookbehind: true
                },
                "punctuation": /```/
              }
            }
          ],
          "title": [
            {
              // title 1
              // =======
              // title 2
              // -------
              pattern: /\S.*(?:\n|\r\n?)(?:==+|--+)(?=[ \t]*$)/m,
              alias: "important",
              inside: {
                punctuation: /==+$|--+$/
              }
            },
            {
              // # title 1
              // ###### title 6
              pattern: /(^\s*)#.+/m,
              lookbehind: true,
              alias: "important",
              inside: {
                punctuation: /^#+|#+$/
              }
            }
          ],
          "hr": {
            // ***
            // ---
            // * * *
            // -----------
            pattern: /(^\s*)([*-])(?:[\t ]*\2){2,}(?=\s*$)/m,
            lookbehind: true,
            alias: "punctuation"
          },
          "list": {
            // * item
            // + item
            // - item
            // 1. item
            pattern: /(^\s*)(?:[*+-]|\d+\.)(?=[\t ].)/m,
            lookbehind: true,
            alias: "punctuation"
          },
          "url-reference": {
            // [id]: http://example.com "Optional title"
            // [id]: http://example.com 'Optional title'
            // [id]: http://example.com (Optional title)
            // [id]: <http://example.com> "Optional title"
            pattern: /!?\[[^\]]+\]:[\t ]+(?:\S+|<(?:\\.|[^>\\])+>)(?:[\t ]+(?:"(?:\\.|[^"\\])*"|'(?:\\.|[^'\\])*'|\((?:\\.|[^)\\])*\)))?/,
            inside: {
              "variable": {
                pattern: /^(!?\[)[^\]]+/,
                lookbehind: true
              },
              "string": /(?:"(?:\\.|[^"\\])*"|'(?:\\.|[^'\\])*'|\((?:\\.|[^)\\])*\))$/,
              "punctuation": /^[\[\]!:]|[<>]/
            },
            alias: "url"
          },
          "bold": {
            // **strong**
            // __strong__
            // allow one nested instance of italic text using the same delimiter
            pattern: createInline(/\b__(?:(?!_)<inner>|_(?:(?!_)<inner>)+_)+__\b|\*\*(?:(?!\*)<inner>|\*(?:(?!\*)<inner>)+\*)+\*\*/.source),
            lookbehind: true,
            greedy: true,
            inside: {
              "content": {
                pattern: /(^..)[\s\S]+(?=..$)/,
                lookbehind: true,
                inside: {}
                // see below
              },
              "punctuation": /\*\*|__/
            }
          },
          "italic": {
            // *em*
            // _em_
            // allow one nested instance of bold text using the same delimiter
            pattern: createInline(/\b_(?:(?!_)<inner>|__(?:(?!_)<inner>)+__)+_\b|\*(?:(?!\*)<inner>|\*\*(?:(?!\*)<inner>)+\*\*)+\*/.source),
            lookbehind: true,
            greedy: true,
            inside: {
              "content": {
                pattern: /(^.)[\s\S]+(?=.$)/,
                lookbehind: true,
                inside: {}
                // see below
              },
              "punctuation": /[*_]/
            }
          },
          "strike": {
            // ~~strike through~~
            // ~strike~
            // eslint-disable-next-line regexp/strict
            pattern: createInline(/(~~?)(?:(?!~)<inner>)+\2/.source),
            lookbehind: true,
            greedy: true,
            inside: {
              "content": {
                pattern: /(^~~?)[\s\S]+(?=\1$)/,
                lookbehind: true,
                inside: {}
                // see below
              },
              "punctuation": /~~?/
            }
          },
          "code-snippet": {
            // `code`
            // ``code``
            pattern: /(^|[^\\`])(?:``[^`\r\n]+(?:`[^`\r\n]+)*``(?!`)|`[^`\r\n]+`(?!`))/,
            lookbehind: true,
            greedy: true,
            alias: ["code", "keyword"]
          },
          "url": {
            // [example](http://example.com "Optional title")
            // [example][id]
            // [example] [id]
            pattern: createInline(/!?\[(?:(?!\])<inner>)+\](?:\([^\s)]+(?:[\t ]+"(?:\\.|[^"\\])*")?\)|[ \t]?\[(?:(?!\])<inner>)+\])/.source),
            lookbehind: true,
            greedy: true,
            inside: {
              "operator": /^!/,
              "content": {
                pattern: /(^\[)[^\]]+(?=\])/,
                lookbehind: true,
                inside: {}
                // see below
              },
              "variable": {
                pattern: /(^\][ \t]?\[)[^\]]+(?=\]$)/,
                lookbehind: true
              },
              "url": {
                pattern: /(^\]\()[^\s)]+/,
                lookbehind: true
              },
              "string": {
                pattern: /(^[ \t]+)"(?:\\.|[^"\\])*"(?=\)$)/,
                lookbehind: true
              }
            }
          }
        });
        ["url", "bold", "italic", "strike"].forEach(function(token) {
          ["url", "bold", "italic", "strike", "code-snippet"].forEach(function(inside) {
            if (token !== inside) {
              Prism3.languages.markdown[token].inside.content.inside[inside] = Prism3.languages.markdown[inside];
            }
          });
        });
        Prism3.hooks.add("after-tokenize", function(env) {
          if (env.language !== "markdown" && env.language !== "md") {
            return;
          }
          function walkTokens(tokens) {
            if (!tokens || typeof tokens === "string") {
              return;
            }
            for (var i8 = 0, l4 = tokens.length; i8 < l4; i8++) {
              var token = tokens[i8];
              if (token.type !== "code") {
                walkTokens(token.content);
                continue;
              }
              var codeLang = token.content[1];
              var codeBlock = token.content[3];
              if (codeLang && codeBlock && codeLang.type === "code-language" && codeBlock.type === "code-block" && typeof codeLang.content === "string") {
                var lang = codeLang.content.replace(/\b#/g, "sharp").replace(/\b\+\+/g, "pp");
                lang = (/[a-z][\w-]*/i.exec(lang) || [""])[0].toLowerCase();
                var alias = "language-" + lang;
                if (!codeBlock.alias) {
                  codeBlock.alias = [alias];
                } else if (typeof codeBlock.alias === "string") {
                  codeBlock.alias = [codeBlock.alias, alias];
                } else {
                  codeBlock.alias.push(alias);
                }
              }
            }
          }
          walkTokens(env.tokens);
        });
        Prism3.hooks.add("wrap", function(env) {
          if (env.type !== "code-block") {
            return;
          }
          var codeLang = "";
          for (var i8 = 0, l4 = env.classes.length; i8 < l4; i8++) {
            var cls = env.classes[i8];
            var match = /language-(.+)/.exec(cls);
            if (match) {
              codeLang = match[1];
              break;
            }
          }
          var grammar = Prism3.languages[codeLang];
          if (!grammar) {
            if (codeLang && codeLang !== "none" && Prism3.plugins.autoloader) {
              var id = "md-" + (/* @__PURE__ */ new Date()).valueOf() + "-" + Math.floor(Math.random() * 1e16);
              env.attributes["id"] = id;
              Prism3.plugins.autoloader.loadLanguages(codeLang, function() {
                var ele = document.getElementById(id);
                if (ele) {
                  ele.innerHTML = Prism3.highlight(ele.textContent, Prism3.languages[codeLang], codeLang);
                }
              });
            }
          } else {
            env.content = Prism3.highlight(textContent(env.content), grammar, codeLang);
          }
        });
        var tagPattern = RegExp(Prism3.languages.markup.tag.pattern.source, "gi");
        var KNOWN_ENTITY_NAMES = {
          "amp": "&",
          "lt": "<",
          "gt": ">",
          "quot": '"'
        };
        var fromCodePoint = String.fromCodePoint || String.fromCharCode;
        function textContent(html) {
          var text = html.replace(tagPattern, "");
          text = text.replace(/&(\w{1,8}|#x?[\da-f]{1,8});/gi, function(m3, code) {
            code = code.toLowerCase();
            if (code[0] === "#") {
              var value;
              if (code[1] === "x") {
                value = parseInt(code.slice(2), 16);
              } else {
                value = Number(code.slice(1));
              }
              return fromCodePoint(value);
            } else {
              var known = KNOWN_ENTITY_NAMES[code];
              if (known) {
                return known;
              }
              return m3;
            }
          });
          return text;
        }
        Prism3.languages.md = Prism3.languages.markdown;
      })(Prism);
    }
  });

  // node_modules/prismjs/components/prism-rust.js
  var require_prism_rust = __commonJS({
    "node_modules/prismjs/components/prism-rust.js"() {
      (function(Prism3) {
        var multilineComment = /\/\*(?:[^*/]|\*(?!\/)|\/(?!\*)|<self>)*\*\//.source;
        for (var i8 = 0; i8 < 2; i8++) {
          multilineComment = multilineComment.replace(/<self>/g, function() {
            return multilineComment;
          });
        }
        multilineComment = multilineComment.replace(/<self>/g, function() {
          return /[^\s\S]/.source;
        });
        Prism3.languages.rust = {
          "comment": [
            {
              pattern: RegExp(/(^|[^\\])/.source + multilineComment),
              lookbehind: true,
              greedy: true
            },
            {
              pattern: /(^|[^\\:])\/\/.*/,
              lookbehind: true,
              greedy: true
            }
          ],
          "string": {
            pattern: /b?"(?:\\[\s\S]|[^\\"])*"|b?r(#*)"(?:[^"]|"(?!\1))*"\1/,
            greedy: true
          },
          "char": {
            pattern: /b?'(?:\\(?:x[0-7][\da-fA-F]|u\{(?:[\da-fA-F]_*){1,6}\}|.)|[^\\\r\n\t'])'/,
            greedy: true
          },
          "attribute": {
            pattern: /#!?\[(?:[^\[\]"]|"(?:\\[\s\S]|[^\\"])*")*\]/,
            greedy: true,
            alias: "attr-name",
            inside: {
              "string": null
              // see below
            }
          },
          // Closure params should not be confused with bitwise OR |
          "closure-params": {
            pattern: /([=(,:]\s*|\bmove\s*)\|[^|]*\||\|[^|]*\|(?=\s*(?:\{|->))/,
            lookbehind: true,
            greedy: true,
            inside: {
              "closure-punctuation": {
                pattern: /^\||\|$/,
                alias: "punctuation"
              },
              rest: null
              // see below
            }
          },
          "lifetime-annotation": {
            pattern: /'\w+/,
            alias: "symbol"
          },
          "fragment-specifier": {
            pattern: /(\$\w+:)[a-z]+/,
            lookbehind: true,
            alias: "punctuation"
          },
          "variable": /\$\w+/,
          "function-definition": {
            pattern: /(\bfn\s+)\w+/,
            lookbehind: true,
            alias: "function"
          },
          "type-definition": {
            pattern: /(\b(?:enum|struct|trait|type|union)\s+)\w+/,
            lookbehind: true,
            alias: "class-name"
          },
          "module-declaration": [
            {
              pattern: /(\b(?:crate|mod)\s+)[a-z][a-z_\d]*/,
              lookbehind: true,
              alias: "namespace"
            },
            {
              pattern: /(\b(?:crate|self|super)\s*)::\s*[a-z][a-z_\d]*\b(?:\s*::(?:\s*[a-z][a-z_\d]*\s*::)*)?/,
              lookbehind: true,
              alias: "namespace",
              inside: {
                "punctuation": /::/
              }
            }
          ],
          "keyword": [
            // https://github.com/rust-lang/reference/blob/master/src/keywords.md
            /\b(?:Self|abstract|as|async|await|become|box|break|const|continue|crate|do|dyn|else|enum|extern|final|fn|for|if|impl|in|let|loop|macro|match|mod|move|mut|override|priv|pub|ref|return|self|static|struct|super|trait|try|type|typeof|union|unsafe|unsized|use|virtual|where|while|yield)\b/,
            // primitives and str
            // https://doc.rust-lang.org/stable/rust-by-example/primitives.html
            /\b(?:bool|char|f(?:32|64)|[ui](?:8|16|32|64|128|size)|str)\b/
          ],
          // functions can technically start with an upper-case letter, but this will introduce a lot of false positives
          // and Rust's naming conventions recommend snake_case anyway.
          // https://doc.rust-lang.org/1.0.0/style/style/naming/README.html
          "function": /\b[a-z_]\w*(?=\s*(?:::\s*<|\())/,
          "macro": {
            pattern: /\b\w+!/,
            alias: "property"
          },
          "constant": /\b[A-Z_][A-Z_\d]+\b/,
          "class-name": /\b[A-Z]\w*\b/,
          "namespace": {
            pattern: /(?:\b[a-z][a-z_\d]*\s*::\s*)*\b[a-z][a-z_\d]*\s*::(?!\s*<)/,
            inside: {
              "punctuation": /::/
            }
          },
          // Hex, oct, bin, dec numbers with visual separators and type suffix
          "number": /\b(?:0x[\dA-Fa-f](?:_?[\dA-Fa-f])*|0o[0-7](?:_?[0-7])*|0b[01](?:_?[01])*|(?:(?:\d(?:_?\d)*)?\.)?\d(?:_?\d)*(?:[Ee][+-]?\d+)?)(?:_?(?:f32|f64|[iu](?:8|16|32|64|size)?))?\b/,
          "boolean": /\b(?:false|true)\b/,
          "punctuation": /->|\.\.=|\.{1,3}|::|[{}[\];(),:]/,
          "operator": /[-+*\/%!^]=?|=[=>]?|&[&=]?|\|[|=]?|<<?=?|>>?=?|[@?]/
        };
        Prism3.languages.rust["closure-params"].inside.rest = Prism3.languages.rust;
        Prism3.languages.rust["attribute"].inside["string"] = Prism3.languages.rust["string"];
      })(Prism);
    }
  });

  // node_modules/prismjs/components/prism-gherkin.js
  var require_prism_gherkin = __commonJS({
    "node_modules/prismjs/components/prism-gherkin.js"() {
      (function(Prism3) {
        var tableRow = /(?:\r?\n|\r)[ \t]*\|.+\|(?:(?!\|).)*/.source;
        Prism3.languages.gherkin = {
          "pystring": {
            pattern: /("""|''')[\s\S]+?\1/,
            alias: "string"
          },
          "comment": {
            pattern: /(^[ \t]*)#.*/m,
            lookbehind: true
          },
          "tag": {
            pattern: /(^[ \t]*)@\S*/m,
            lookbehind: true
          },
          "feature": {
            pattern: /((?:^|\r?\n|\r)[ \t]*)(?:Ability|Ahoy matey!|Arwedd|Aspekt|Besigheid Behoefte|Business Need|Caracteristica|Característica|Egenskab|Egenskap|Eiginleiki|Feature|Fīča|Fitur|Fonctionnalité|Fonksyonalite|Funcionalidade|Funcionalitat|Functionalitate|Funcţionalitate|Funcționalitate|Functionaliteit|Fungsi|Funkcia|Funkcija|Funkcionalitāte|Funkcionalnost|Funkcja|Funksie|Funktionalität|Funktionalitéit|Funzionalità|Hwaet|Hwæt|Jellemző|Karakteristik|Lastnost|Mak|Mogucnost|laH|Mogućnost|Moznosti|Možnosti|OH HAI|Omadus|Ominaisuus|Osobina|Özellik|Potrzeba biznesowa|perbogh|poQbogh malja'|Požadavek|Požiadavka|Pretty much|Qap|Qu'meH 'ut|Savybė|Tính năng|Trajto|Vermoë|Vlastnosť|Właściwość|Značilnost|Δυνατότητα|Λειτουργία|Могућност|Мөмкинлек|Особина|Свойство|Үзенчәлеклелек|Функционал|Функционалност|Функция|Функціонал|תכונה|خاصية|خصوصیت|صلاحیت|کاروبار کی ضرورت|وِیژگی|रूप लेख|ਖਾਸੀਅਤ|ਨਕਸ਼ ਨੁਹਾਰ|ਮੁਹਾਂਦਰਾ|గుణము|ಹೆಚ್ಚಳ|ความต้องการทางธุรกิจ|ความสามารถ|โครงหลัก|기능|フィーチャ|功能|機能):(?:[^:\r\n]+(?:\r?\n|\r|$))*/,
            lookbehind: true,
            inside: {
              "important": {
                pattern: /(:)[^\r\n]+/,
                lookbehind: true
              },
              "keyword": /[^:\r\n]+:/
            }
          },
          "scenario": {
            pattern: /(^[ \t]*)(?:Abstract Scenario|Abstrakt Scenario|Achtergrond|Aer|Ær|Agtergrond|All y'all|Antecedentes|Antecedents|Atburðarás|Atburðarásir|Awww, look mate|B4|Background|Baggrund|Bakgrund|Bakgrunn|Bakgrunnur|Beispiele|Beispiller|Bối cảnh|Cefndir|Cenario|Cenário|Cenario de Fundo|Cenário de Fundo|Cenarios|Cenários|Contesto|Context|Contexte|Contexto|Conto|Contoh|Contone|Dæmi|Dasar|Dead men tell no tales|Delineacao do Cenario|Delineação do Cenário|Dis is what went down|Dữ liệu|Dyagram Senaryo|Dyagram senaryo|Egzanp|Ejemplos|Eksempler|Ekzemploj|Enghreifftiau|Esbozo do escenario|Escenari|Escenario|Esempi|Esquema de l'escenari|Esquema del escenario|Esquema do Cenario|Esquema do Cenário|EXAMPLZ|Examples|Exempel|Exemple|Exemples|Exemplos|First off|Fono|Forgatókönyv|Forgatókönyv vázlat|Fundo|Geçmiş|Grundlage|Hannergrond|ghantoH|Háttér|Heave to|Istorik|Juhtumid|Keadaan|Khung kịch bản|Khung tình huống|Kịch bản|Koncept|Konsep skenario|Kontèks|Kontekst|Kontekstas|Konteksts|Kontext|Konturo de la scenaro|Latar Belakang|lut chovnatlh|lut|lutmey|Lýsing Atburðarásar|Lýsing Dæma|MISHUN SRSLY|MISHUN|Menggariskan Senario|mo'|Náčrt Scenára|Náčrt Scénáře|Náčrt Scenáru|Oris scenarija|Örnekler|Osnova|Osnova Scenára|Osnova scénáře|Osnutek|Ozadje|Paraugs|Pavyzdžiai|Példák|Piemēri|Plan du scénario|Plan du Scénario|Plan Senaryo|Plan senaryo|Plang vum Szenario|Pozadí|Pozadie|Pozadina|Príklady|Příklady|Primer|Primeri|Primjeri|Przykłady|Raamstsenaarium|Reckon it's like|Rerefons|Scenár|Scénář|Scenarie|Scenarij|Scenarijai|Scenarijaus šablonas|Scenariji|Scenārijs|Scenārijs pēc parauga|Scenarijus|Scenario|Scénario|Scenario Amlinellol|Scenario Outline|Scenario Template|Scenariomal|Scenariomall|Scenarios|Scenariu|Scenariusz|Scenaro|Schema dello scenario|Se ðe|Se the|Se þe|Senario|Senaryo Deskripsyon|Senaryo deskripsyon|Senaryo|Senaryo taslağı|Shiver me timbers|Situācija|Situai|Situasie Uiteensetting|Situasie|Skenario konsep|Skenario|Skica|Structura scenariu|Structură scenariu|Struktura scenarija|Stsenaarium|Swa hwaer swa|Swa|Swa hwær swa|Szablon scenariusza|Szenario|Szenariogrundriss|Tapaukset|Tapaus|Tapausaihio|Taust|Tausta|Template Keadaan|Template Senario|Template Situai|The thing of it is|Tình huống|Variantai|Voorbeelde|Voorbeelden|Wharrimean is|Yo-ho-ho|You'll wanna|Założenia|Παραδείγματα|Περιγραφή Σεναρίου|Σενάρια|Σενάριο|Υπόβαθρο|Кереш|Контекст|Концепт|Мисаллар|Мисоллар|Основа|Передумова|Позадина|Предистория|Предыстория|Приклади|Пример|Примери|Примеры|Рамка на сценарий|Скица|Структура сценарија|Структура сценария|Структура сценарію|Сценарий|Сценарий структураси|Сценарийның төзелеше|Сценарији|Сценарио|Сценарій|Тарих|Үрнәкләр|דוגמאות|רקע|תבנית תרחיש|תרחיש|الخلفية|الگوی سناریو|امثلة|پس منظر|زمینه|سناریو|سيناريو|سيناريو مخطط|مثالیں|منظر نامے کا خاکہ|منظرنامہ|نمونه ها|उदाहरण|परिदृश्य|परिदृश्य रूपरेखा|पृष्ठभूमि|ਉਦਾਹਰਨਾਂ|ਪਟਕਥਾ|ਪਟਕਥਾ ਢਾਂਚਾ|ਪਟਕਥਾ ਰੂਪ ਰੇਖਾ|ਪਿਛੋਕੜ|ఉదాహరణలు|కథనం|నేపథ్యం|సన్నివేశం|ಉದಾಹರಣೆಗಳು|ಕಥಾಸಾರಾಂಶ|ವಿವರಣೆ|ಹಿನ್ನೆಲೆ|โครงสร้างของเหตุการณ์|ชุดของตัวอย่าง|ชุดของเหตุการณ์|แนวคิด|สรุปเหตุการณ์|เหตุการณ์|배경|시나리오|시나리오 개요|예|サンプル|シナリオ|シナリオアウトライン|シナリオテンプレ|シナリオテンプレート|テンプレ|例|例子|剧本|剧本大纲|劇本|劇本大綱|场景|场景大纲|場景|場景大綱|背景):[^:\r\n]*/m,
            lookbehind: true,
            inside: {
              "important": {
                pattern: /(:)[^\r\n]*/,
                lookbehind: true
              },
              "keyword": /[^:\r\n]+:/
            }
          },
          "table-body": {
            // Look-behind is used to skip the table head, which has the same format as any table row
            pattern: RegExp("(" + tableRow + ")(?:" + tableRow + ")+"),
            lookbehind: true,
            inside: {
              "outline": {
                pattern: /<[^>]+>/,
                alias: "variable"
              },
              "td": {
                pattern: /\s*[^\s|][^|]*/,
                alias: "string"
              },
              "punctuation": /\|/
            }
          },
          "table-head": {
            pattern: RegExp(tableRow),
            inside: {
              "th": {
                pattern: /\s*[^\s|][^|]*/,
                alias: "variable"
              },
              "punctuation": /\|/
            }
          },
          "atrule": {
            pattern: /(^[ \t]+)(?:'a|'ach|'ej|7|a|A také|A taktiež|A tiež|A zároveň|Aber|Ac|Adott|Akkor|Ak|Aleshores|Ale|Ali|Allora|Alors|Als|Ama|Amennyiben|Amikor|Ampak|an|AN|Ananging|And y'all|And|Angenommen|Anrhegedig a|An|Apabila|Atès|Atesa|Atunci|Avast!|Aye|A|awer|Bagi|Banjur|Bet|Biết|Blimey!|Buh|But at the end of the day I reckon|But y'all|But|BUT|Cal|Când|Cand|Cando|Ce|Cuando|Če|Ða ðe|Ða|Dadas|Dada|Dados|Dado|DaH ghu' bejlu'|dann|Dann|Dano|Dan|Dar|Dat fiind|Data|Date fiind|Date|Dati fiind|Dati|Daţi fiind|Dați fiind|DEN|Dato|De|Den youse gotta|Dengan|Diberi|Diyelim ki|Donada|Donat|Donitaĵo|Do|Dun|Duota|Ðurh|Eeldades|Ef|Eğer ki|Entao|Então|Entón|E|En|Entonces|Epi|És|Etant donnée|Etant donné|Et|Étant données|Étant donnée|Étant donné|Etant données|Etant donnés|Étant donnés|Fakat|Gangway!|Gdy|Gegeben seien|Gegeben sei|Gegeven|Gegewe|ghu' noblu'|Gitt|Given y'all|Given|Givet|Givun|Ha|Cho|I CAN HAZ|In|Ir|It's just unbelievable|I|Ja|Jeśli|Jeżeli|Kad|Kada|Kadar|Kai|Kaj|Když|Keď|Kemudian|Ketika|Khi|Kiedy|Ko|Kuid|Kui|Kun|Lan|latlh|Le sa a|Let go and haul|Le|Lè sa a|Lè|Logo|Lorsqu'<|Lorsque|mä|Maar|Mais|Mając|Ma|Majd|Maka|Manawa|Mas|Men|Menawa|Mutta|Nalika|Nalikaning|Nanging|Når|När|Nato|Nhưng|Niin|Njuk|O zaman|Och|Og|Oletetaan|Ond|Onda|Oraz|Pak|Pero|Però|Podano|Pokiaľ|Pokud|Potem|Potom|Privzeto|Pryd|Quan|Quand|Quando|qaSDI'|Så|Sed|Se|Siis|Sipoze ke|Sipoze Ke|Sipoze|Si|Şi|Și|Soit|Stel|Tada|Tad|Takrat|Tak|Tapi|Ter|Tetapi|Tha the|Tha|Then y'all|Then|Thì|Thurh|Toda|Too right|Un|Und|ugeholl|Và|vaj|Vendar|Ve|wann|Wanneer|WEN|Wenn|When y'all|When|Wtedy|Wun|Y'know|Yeah nah|Yna|Youse know like when|Youse know when youse got|Y|Za predpokladu|Za předpokladu|Zadan|Zadani|Zadano|Zadate|Zadato|Zakładając|Zaradi|Zatati|Þa þe|Þa|Þá|Þegar|Þurh|Αλλά|Δεδομένου|Και|Όταν|Τότε|А також|Агар|Але|Али|Аммо|А|Әгәр|Әйтик|Әмма|Бирок|Ва|Вә|Дадено|Дано|Допустим|Если|Задате|Задати|Задато|И|І|К тому же|Када|Кад|Когато|Когда|Коли|Ләкин|Лекин|Нәтиҗәдә|Нехай|Но|Онда|Припустимо, що|Припустимо|Пусть|Также|Та|Тогда|Тоді|То|Унда|Һәм|Якщо|אבל|אזי|אז|בהינתן|וגם|כאשר|آنگاه|اذاً|اگر|اما|اور|با فرض|بالفرض|بفرض|پھر|تب|ثم|جب|عندما|فرض کیا|لكن|لیکن|متى|هنگامی|و|अगर|और|कदा|किन्तु|चूंकि|जब|तथा|तदा|तब|परन्तु|पर|यदि|ਅਤੇ|ਜਦੋਂ|ਜਿਵੇਂ ਕਿ|ਜੇਕਰ|ਤਦ|ਪਰ|అప్పుడు|ఈ పరిస్థితిలో|కాని|చెప్పబడినది|మరియు|ಆದರೆ|ನಂತರ|ನೀಡಿದ|ಮತ್ತು|ಸ್ಥಿತಿಯನ್ನು|กำหนดให้|ดังนั้น|แต่|เมื่อ|และ|그러면<|그리고<|단<|만약<|만일<|먼저<|조건<|하지만<|かつ<|しかし<|ただし<|ならば<|もし<|並且<|但し<|但是<|假如<|假定<|假設<|假设<|前提<|同时<|同時<|并且<|当<|當<|而且<|那么<|那麼<)(?=[ \t])/m,
            lookbehind: true
          },
          "string": {
            pattern: /"(?:\\.|[^"\\\r\n])*"|'(?:\\.|[^'\\\r\n])*'/,
            inside: {
              "outline": {
                pattern: /<[^>]+>/,
                alias: "variable"
              }
            }
          },
          "outline": {
            pattern: /<[^>]+>/,
            alias: "variable"
          }
        };
      })(Prism);
    }
  });

  // node_modules/prismjs/components/prism-toml.js
  var require_prism_toml = __commonJS({
    "node_modules/prismjs/components/prism-toml.js"() {
      (function(Prism3) {
        var key = /(?:[\w-]+|'[^'\n\r]*'|"(?:\\.|[^\\"\r\n])*")/.source;
        function insertKey(pattern) {
          return pattern.replace(/__/g, function() {
            return key;
          });
        }
        Prism3.languages.toml = {
          "comment": {
            pattern: /#.*/,
            greedy: true
          },
          "table": {
            pattern: RegExp(insertKey(/(^[\t ]*\[\s*(?:\[\s*)?)__(?:\s*\.\s*__)*(?=\s*\])/.source), "m"),
            lookbehind: true,
            greedy: true,
            alias: "class-name"
          },
          "key": {
            pattern: RegExp(insertKey(/(^[\t ]*|[{,]\s*)__(?:\s*\.\s*__)*(?=\s*=)/.source), "m"),
            lookbehind: true,
            greedy: true,
            alias: "property"
          },
          "string": {
            pattern: /"""(?:\\[\s\S]|[^\\])*?"""|'''[\s\S]*?'''|'[^'\n\r]*'|"(?:\\.|[^\\"\r\n])*"/,
            greedy: true
          },
          "date": [
            {
              // Offset Date-Time, Local Date-Time, Local Date
              pattern: /\b\d{4}-\d{2}-\d{2}(?:[T\s]\d{2}:\d{2}:\d{2}(?:\.\d+)?(?:Z|[+-]\d{2}:\d{2})?)?\b/i,
              alias: "number"
            },
            {
              // Local Time
              pattern: /\b\d{2}:\d{2}:\d{2}(?:\.\d+)?\b/,
              alias: "number"
            }
          ],
          "number": /(?:\b0(?:x[\da-zA-Z]+(?:_[\da-zA-Z]+)*|o[0-7]+(?:_[0-7]+)*|b[10]+(?:_[10]+)*))\b|[-+]?\b\d+(?:_\d+)*(?:\.\d+(?:_\d+)*)?(?:[eE][+-]?\d+(?:_\d+)*)?\b|[-+]?\b(?:inf|nan)\b/,
          "boolean": /\b(?:false|true)\b/,
          "punctuation": /[.,=[\]{}]/
        };
      })(Prism);
    }
  });

  // node_modules/prismjs/components/prism-sql.js
  var require_prism_sql = __commonJS({
    "node_modules/prismjs/components/prism-sql.js"() {
      Prism.languages.sql = {
        "comment": {
          pattern: /(^|[^\\])(?:\/\*[\s\S]*?\*\/|(?:--|\/\/|#).*)/,
          lookbehind: true
        },
        "variable": [
          {
            pattern: /@(["'`])(?:\\[\s\S]|(?!\1)[^\\])+\1/,
            greedy: true
          },
          /@[\w.$]+/
        ],
        "string": {
          pattern: /(^|[^@\\])("|')(?:\\[\s\S]|(?!\2)[^\\]|\2\2)*\2/,
          greedy: true,
          lookbehind: true
        },
        "identifier": {
          pattern: /(^|[^@\\])`(?:\\[\s\S]|[^`\\]|``)*`/,
          greedy: true,
          lookbehind: true,
          inside: {
            "punctuation": /^`|`$/
          }
        },
        "function": /\b(?:AVG|COUNT|FIRST|FORMAT|LAST|LCASE|LEN|MAX|MID|MIN|MOD|NOW|ROUND|SUM|UCASE)(?=\s*\()/i,
        // Should we highlight user defined functions too?
        "keyword": /\b(?:ACTION|ADD|AFTER|ALGORITHM|ALL|ALTER|ANALYZE|ANY|APPLY|AS|ASC|AUTHORIZATION|AUTO_INCREMENT|BACKUP|BDB|BEGIN|BERKELEYDB|BIGINT|BINARY|BIT|BLOB|BOOL|BOOLEAN|BREAK|BROWSE|BTREE|BULK|BY|CALL|CASCADED?|CASE|CHAIN|CHAR(?:ACTER|SET)?|CHECK(?:POINT)?|CLOSE|CLUSTERED|COALESCE|COLLATE|COLUMNS?|COMMENT|COMMIT(?:TED)?|COMPUTE|CONNECT|CONSISTENT|CONSTRAINT|CONTAINS(?:TABLE)?|CONTINUE|CONVERT|CREATE|CROSS|CURRENT(?:_DATE|_TIME|_TIMESTAMP|_USER)?|CURSOR|CYCLE|DATA(?:BASES?)?|DATE(?:TIME)?|DAY|DBCC|DEALLOCATE|DEC|DECIMAL|DECLARE|DEFAULT|DEFINER|DELAYED|DELETE|DELIMITERS?|DENY|DESC|DESCRIBE|DETERMINISTIC|DISABLE|DISCARD|DISK|DISTINCT|DISTINCTROW|DISTRIBUTED|DO|DOUBLE|DROP|DUMMY|DUMP(?:FILE)?|DUPLICATE|ELSE(?:IF)?|ENABLE|ENCLOSED|END|ENGINE|ENUM|ERRLVL|ERRORS|ESCAPED?|EXCEPT|EXEC(?:UTE)?|EXISTS|EXIT|EXPLAIN|EXTENDED|FETCH|FIELDS|FILE|FILLFACTOR|FIRST|FIXED|FLOAT|FOLLOWING|FOR(?: EACH ROW)?|FORCE|FOREIGN|FREETEXT(?:TABLE)?|FROM|FULL|FUNCTION|GEOMETRY(?:COLLECTION)?|GLOBAL|GOTO|GRANT|GROUP|HANDLER|HASH|HAVING|HOLDLOCK|HOUR|IDENTITY(?:COL|_INSERT)?|IF|IGNORE|IMPORT|INDEX|INFILE|INNER|INNODB|INOUT|INSERT|INT|INTEGER|INTERSECT|INTERVAL|INTO|INVOKER|ISOLATION|ITERATE|JOIN|KEYS?|KILL|LANGUAGE|LAST|LEAVE|LEFT|LEVEL|LIMIT|LINENO|LINES|LINESTRING|LOAD|LOCAL|LOCK|LONG(?:BLOB|TEXT)|LOOP|MATCH(?:ED)?|MEDIUM(?:BLOB|INT|TEXT)|MERGE|MIDDLEINT|MINUTE|MODE|MODIFIES|MODIFY|MONTH|MULTI(?:LINESTRING|POINT|POLYGON)|NATIONAL|NATURAL|NCHAR|NEXT|NO|NONCLUSTERED|NULLIF|NUMERIC|OFF?|OFFSETS?|ON|OPEN(?:DATASOURCE|QUERY|ROWSET)?|OPTIMIZE|OPTION(?:ALLY)?|ORDER|OUT(?:ER|FILE)?|OVER|PARTIAL|PARTITION|PERCENT|PIVOT|PLAN|POINT|POLYGON|PRECEDING|PRECISION|PREPARE|PREV|PRIMARY|PRINT|PRIVILEGES|PROC(?:EDURE)?|PUBLIC|PURGE|QUICK|RAISERROR|READS?|REAL|RECONFIGURE|REFERENCES|RELEASE|RENAME|REPEAT(?:ABLE)?|REPLACE|REPLICATION|REQUIRE|RESIGNAL|RESTORE|RESTRICT|RETURN(?:ING|S)?|REVOKE|RIGHT|ROLLBACK|ROUTINE|ROW(?:COUNT|GUIDCOL|S)?|RTREE|RULE|SAVE(?:POINT)?|SCHEMA|SECOND|SELECT|SERIAL(?:IZABLE)?|SESSION(?:_USER)?|SET(?:USER)?|SHARE|SHOW|SHUTDOWN|SIMPLE|SMALLINT|SNAPSHOT|SOME|SONAME|SQL|START(?:ING)?|STATISTICS|STATUS|STRIPED|SYSTEM_USER|TABLES?|TABLESPACE|TEMP(?:ORARY|TABLE)?|TERMINATED|TEXT(?:SIZE)?|THEN|TIME(?:STAMP)?|TINY(?:BLOB|INT|TEXT)|TOP?|TRAN(?:SACTIONS?)?|TRIGGER|TRUNCATE|TSEQUAL|TYPES?|UNBOUNDED|UNCOMMITTED|UNDEFINED|UNION|UNIQUE|UNLOCK|UNPIVOT|UNSIGNED|UPDATE(?:TEXT)?|USAGE|USE|USER|USING|VALUES?|VAR(?:BINARY|CHAR|CHARACTER|YING)|VIEW|WAITFOR|WARNINGS|WHEN|WHERE|WHILE|WITH(?: ROLLUP|IN)?|WORK|WRITE(?:TEXT)?|YEAR)\b/i,
        "boolean": /\b(?:FALSE|NULL|TRUE)\b/i,
        "number": /\b0x[\da-f]+\b|\b\d+(?:\.\d*)?|\B\.\d+\b/i,
        "operator": /[-+*\/=%^~]|&&?|\|\|?|!=?|<(?:=>?|<|>)?|>[>=]?|\b(?:AND|BETWEEN|DIV|ILIKE|IN|IS|LIKE|NOT|OR|REGEXP|RLIKE|SOUNDS LIKE|XOR)\b/i,
        "punctuation": /[;[\]()`,.]/
      };
    }
  });

  // node_modules/prismjs/components/prism-python.js
  var require_prism_python = __commonJS({
    "node_modules/prismjs/components/prism-python.js"() {
      Prism.languages.python = {
        "comment": {
          pattern: /(^|[^\\])#.*/,
          lookbehind: true,
          greedy: true
        },
        "string-interpolation": {
          pattern: /(?:f|fr|rf)(?:("""|''')[\s\S]*?\1|("|')(?:\\.|(?!\2)[^\\\r\n])*\2)/i,
          greedy: true,
          inside: {
            "interpolation": {
              // "{" <expression> <optional "!s", "!r", or "!a"> <optional ":" format specifier> "}"
              pattern: /((?:^|[^{])(?:\{\{)*)\{(?!\{)(?:[^{}]|\{(?!\{)(?:[^{}]|\{(?!\{)(?:[^{}])+\})+\})+\}/,
              lookbehind: true,
              inside: {
                "format-spec": {
                  pattern: /(:)[^:(){}]+(?=\}$)/,
                  lookbehind: true
                },
                "conversion-option": {
                  pattern: /![sra](?=[:}]$)/,
                  alias: "punctuation"
                },
                rest: null
              }
            },
            "string": /[\s\S]+/
          }
        },
        "triple-quoted-string": {
          pattern: /(?:[rub]|br|rb)?("""|''')[\s\S]*?\1/i,
          greedy: true,
          alias: "string"
        },
        "string": {
          pattern: /(?:[rub]|br|rb)?("|')(?:\\.|(?!\1)[^\\\r\n])*\1/i,
          greedy: true
        },
        "function": {
          pattern: /((?:^|\s)def[ \t]+)[a-zA-Z_]\w*(?=\s*\()/g,
          lookbehind: true
        },
        "class-name": {
          pattern: /(\bclass\s+)\w+/i,
          lookbehind: true
        },
        "decorator": {
          pattern: /(^[\t ]*)@\w+(?:\.\w+)*/m,
          lookbehind: true,
          alias: ["annotation", "punctuation"],
          inside: {
            "punctuation": /\./
          }
        },
        "keyword": /\b(?:_(?=\s*:)|and|as|assert|async|await|break|case|class|continue|def|del|elif|else|except|exec|finally|for|from|global|if|import|in|is|lambda|match|nonlocal|not|or|pass|print|raise|return|try|while|with|yield)\b/,
        "builtin": /\b(?:__import__|abs|all|any|apply|ascii|basestring|bin|bool|buffer|bytearray|bytes|callable|chr|classmethod|cmp|coerce|compile|complex|delattr|dict|dir|divmod|enumerate|eval|execfile|file|filter|float|format|frozenset|getattr|globals|hasattr|hash|help|hex|id|input|int|intern|isinstance|issubclass|iter|len|list|locals|long|map|max|memoryview|min|next|object|oct|open|ord|pow|property|range|raw_input|reduce|reload|repr|reversed|round|set|setattr|slice|sorted|staticmethod|str|sum|super|tuple|type|unichr|unicode|vars|xrange|zip)\b/,
        "boolean": /\b(?:False|None|True)\b/,
        "number": /\b0(?:b(?:_?[01])+|o(?:_?[0-7])+|x(?:_?[a-f0-9])+)\b|(?:\b\d+(?:_\d+)*(?:\.(?:\d+(?:_\d+)*)?)?|\B\.\d+(?:_\d+)*)(?:e[+-]?\d+(?:_\d+)*)?j?(?!\w)/i,
        "operator": /[-+%=]=?|!=|:=|\*\*?=?|\/\/?=?|<[<=>]?|>[=>]?|[&|^~]/,
        "punctuation": /[{}[\];(),.:]/
      };
      Prism.languages.python["string-interpolation"].inside["interpolation"].inside.rest = Prism.languages.python;
      Prism.languages.py = Prism.languages.python;
    }
  });

  // node_modules/@nldd/design-system/dist/components/layout/page/page.styles.js
  var pageStyles;
  var init_page_styles = __esm({
    "node_modules/@nldd/design-system/dist/components/layout/page/page.styles.js"() {
      init_lit();
      pageStyles = i`


	/* # Host */

	:host {
		--_background-color: var(--context-parent-background-color, var(--semantics-surfaces-base-background-color));

		display: flex;
		background-color: var(--_background-color);
		width: 100%;
		height: 100%;
		overflow-y: auto;
		overflow-x: hidden;
		overscroll-behavior: contain;
		flex-direction: column;
		/* New stacking context so descendant z-index (e.g. list-item
		 * sticky/elevated layers) can't paint over the page's scrollbar. */
		isolation: isolate;
	}

	:host([hidden]) {
		display: none;
	}

	:host([background="base"]) {
		--context-parent-background-color: var(--semantics-surfaces-base-background-color);
		--_background-color: var(--context-parent-background-color);
	}

	:host([background="tinted"]) {
		--context-parent-background-color: var(--semantics-surfaces-tinted-background-color);
		--_background-color: var(--context-parent-background-color);
	}

	/* Overflow hidden prevents content from escaping the scroll wrapper.
	   Overlays inside slotted content should use popover, dialog, or
	   position: fixed to render in the top layer. */
	:host([sticky-header]) {
		position: relative;
		overflow: hidden;
	}


	/* # Block */

	.page {
		display: flex;
		min-height: 0;
		flex-direction: column;
		flex-grow: 1;
	}


	/* # Elements */

	.page__header {
		position: relative;
		flex-shrink: 0;
	}

	:host([sticky-header]) .page__header {
		position: absolute;
		top: 0;
		left: 0;
		right: 0;
		z-index: 1;
		background-color: color-mix(in srgb, var(--_background-color) 95%, transparent);
	}

	:host([sticky-header]) .page__header::after {
		content: '';
		position: absolute;
		top: 100%;
		left: 0;
		right: 0;
		opacity: 0;
		background: linear-gradient(to bottom, color-mix(in srgb, var(--_background-color) 95%, transparent), transparent);
		pointer-events: none;
		height: var(--primitives-space-32);
		transition: opacity var(--primitives-transition-duration-medium) var(--primitives-transition-easing-default);
	}

	:host([sticky-header]) .page__header.is-scrolled::after {
		opacity: 1;
	}

	.page__scroll {
		display: flex;
		min-height: 0;
		flex-direction: column;
		flex-grow: 1;
	}

	:host([sticky-header]) .page__scroll {
		overflow-y: auto;
		overflow-x: hidden;
		overscroll-behavior: contain;
	}

	.page__main {
		display: flex;
		flex-direction: column;
		flex-grow: 1;
	}

	.page__footer {
		position: relative;
		flex-shrink: 0;
	}

	:host([sticky-footer]) .page__footer {
		position: sticky;
		bottom: 0;
		z-index: 1;
		background-color: color-mix(in srgb, var(--_background-color) 95%, transparent);
	}

	:host([sticky-footer]) .page__footer::before {
		content: '';
		position: absolute;
		bottom: 100%;
		left: 0;
		right: 0;
		background: linear-gradient(to top, color-mix(in srgb, var(--_background-color) 95%, transparent), transparent);
		pointer-events: none;
		height: var(--primitives-space-32);
	}
`;
    }
  });

  // node_modules/@nldd/design-system/dist/components/layout/page/page.template.js
  function pageTemplate(component) {
    return b2`
		<div class="page">
			<header class="page__header ${component._scrolled ? "is-scrolled" : ""}">
				<slot name="header"></slot>
			</header>
			<div class="page__scroll">
				<main class="page__main">
					<slot></slot>
				</main>
				<footer class="page__footer">
					<slot name="footer"></slot>
				</footer>
			</div>
		</div>
	`;
  }
  var init_page_template = __esm({
    "node_modules/@nldd/design-system/dist/components/layout/page/page.template.js"() {
      init_lit();
    }
  });

  // node_modules/@nldd/design-system/dist/components/layout/page/page.js
  var page_exports = {};
  __export(page_exports, {
    NLDDPage: () => NLDDPage
  });
  var __decorate52, NLDDPage;
  var init_page = __esm({
    "node_modules/@nldd/design-system/dist/components/layout/page/page.js"() {
      init_lit();
      init_decorators();
      init_page_styles();
      init_page_template();
      __decorate52 = function(decorators, target, key, desc) {
        var c6 = arguments.length, r6 = c6 < 3 ? target : desc === null ? desc = Object.getOwnPropertyDescriptor(target, key) : desc, d3;
        if (typeof Reflect === "object" && typeof Reflect.decorate === "function") r6 = Reflect.decorate(decorators, target, key, desc);
        else for (var i8 = decorators.length - 1; i8 >= 0; i8--) if (d3 = decorators[i8]) r6 = (c6 < 3 ? d3(r6) : c6 > 3 ? d3(target, key, r6) : d3(target, key)) || r6;
        return c6 > 3 && r6 && Object.defineProperty(target, key, r6), r6;
      };
      NLDDPage = class NLDDPage2 extends i4 {
        constructor() {
          super(...arguments);
          this.stickyHeader = false;
          this.stickyFooter = false;
          this.background = "inherit";
          this._scrolled = false;
          this._scrollTarget = null;
          this._headerObserver = null;
          this._mainSlot = null;
          this._onScroll = () => {
            const target = this.stickyHeader ? this._scrollEl : this;
            this._scrolled = target.scrollTop > 0;
          };
          this._updateMainItems = () => {
            const slot = this._mainSlot;
            if (!slot)
              return;
            const items = slot.assignedElements();
            const visible = items.filter((el) => !el.hasAttribute("hidden"));
            const last = visible[visible.length - 1];
            items.forEach((el) => el.classList.toggle("is-last", el === last));
          };
        }
        get scrollTarget() {
          return this.stickyHeader ? this._scrollEl ?? this : this;
        }
        get _headerEl() {
          return this.shadowRoot?.querySelector(".page__header") ?? null;
        }
        get _scrollEl() {
          return this.shadowRoot?.querySelector(".page__scroll") ?? null;
        }
        connectedCallback() {
          super.connectedCallback();
          this.style.containerType = "inline-size";
          this.style.containerName = "layout-container";
          if (this.hasUpdated) {
            this._setupScrollListener();
            if (this.stickyHeader)
              this._setupHeaderObserver();
          }
        }
        disconnectedCallback() {
          super.disconnectedCallback();
          this._teardownScrollListener();
          this._teardownHeaderObserver();
          this._teardownMainSlotListener();
        }
        firstUpdated() {
          this._setupScrollListener();
          if (this.stickyHeader)
            this._setupHeaderObserver();
          this._setupMainSlotListener();
        }
        updated(changed) {
          if (changed.has("stickyHeader")) {
            this._teardownScrollListener();
            this._setupScrollListener();
            if (this.stickyHeader) {
              this._setupHeaderObserver();
            } else {
              this._teardownHeaderObserver();
              if (this._scrollEl)
                this._scrollEl.style.paddingTop = "";
            }
          }
        }
        _setupScrollListener() {
          const target = this.stickyHeader ? this._scrollEl : this;
          if (!target)
            return;
          target.addEventListener("scroll", this._onScroll);
          this._scrollTarget = target;
        }
        _teardownScrollListener() {
          if (this._scrollTarget) {
            this._scrollTarget.removeEventListener("scroll", this._onScroll);
            this._scrollTarget = null;
          }
        }
        _setupHeaderObserver() {
          this._teardownHeaderObserver();
          const header = this._headerEl;
          const scroll = this._scrollEl;
          if (!header || !scroll)
            return;
          this._headerObserver = new ResizeObserver(() => {
            if (scroll.scrollTop > 0)
              return;
            scroll.style.paddingTop = `${header.offsetHeight}px`;
          });
          this._headerObserver.observe(header);
        }
        _teardownHeaderObserver() {
          if (this._headerObserver) {
            this._headerObserver.disconnect();
            this._headerObserver = null;
          }
        }
        // Mark the last visible main-slot child with `is-last`, so section-style
        // components can grow to fill remaining vertical space without coupling
        // to specific tag types or relying on light-DOM `:last-child` (which
        // breaks when slot="footer" siblings are present). Same shape as
        // nldd-list's _updateItems().
        _setupMainSlotListener() {
          this._teardownMainSlotListener();
          const slot = this.shadowRoot?.querySelector("slot:not([name])") ?? null;
          this._mainSlot = slot;
          if (!slot)
            return;
          slot.addEventListener("slotchange", this._updateMainItems);
          this._updateMainItems();
        }
        _teardownMainSlotListener() {
          if (this._mainSlot) {
            this._mainSlot.removeEventListener("slotchange", this._updateMainItems);
            this._mainSlot = null;
          }
        }
        render() {
          return pageTemplate(this);
        }
      };
      NLDDPage.styles = pageStyles;
      __decorate52([
        n4({ type: Boolean, reflect: true, attribute: "sticky-header" })
      ], NLDDPage.prototype, "stickyHeader", void 0);
      __decorate52([
        n4({ type: Boolean, reflect: true, attribute: "sticky-footer" })
      ], NLDDPage.prototype, "stickyFooter", void 0);
      __decorate52([
        n4({ type: String, reflect: true })
      ], NLDDPage.prototype, "background", void 0);
      __decorate52([
        r5()
      ], NLDDPage.prototype, "_scrolled", void 0);
      NLDDPage = __decorate52([
        t3("nldd-page")
      ], NLDDPage);
    }
  });

  // node_modules/@nldd/design-system/dist/utilities/page-section-mixin.js
  function paddingToValue(size3) {
    if (size3 === void 0 || size3 === "")
      return null;
    if (size3 === "0")
      return "0";
    return `var(--primitives-space-${size3})`;
  }
  function PageSectionMixin(Base) {
    class WithPageSection extends Base {
      constructor() {
        super(...arguments);
        this.background = "inherit";
        this.scheme = "inherit";
      }
      connectedCallback() {
        super.connectedCallback();
        this._unsubScheme = onColorSchemeChange(() => {
          if (this.scheme === "inverted")
            this._applyScheme();
        });
      }
      disconnectedCallback() {
        super.disconnectedCallback();
        this._unsubScheme?.();
        this._unsubScheme = void 0;
      }
      updated(changed) {
        super.updated(changed);
        if (changed.has("background"))
          this._applyBackground();
        if (changed.has("scheme"))
          this._applyScheme();
        if (PADDING_KEYS.some((k2) => changed.has(k2)))
          this._applyPadding();
        if (changed.has("height"))
          this._applyHeight();
      }
      // Hide a slot wrapper (header/footer) when the slot has no assigned
      // elements, so empty regions collapse. Shared by every page section.
      _onSlotChange(e9) {
        const slot = e9.target;
        const wrapper = slot.parentElement;
        if (!wrapper)
          return;
        wrapper.hidden = slot.assignedElements().length === 0;
      }
      _applyBackground() {
        if (this.background === "base" || this.background === "tinted") {
          const token = BACKGROUND_TOKEN[this.background];
          this.style.backgroundColor = token;
          this.style.setProperty("--context-parent-background-color", token);
        } else {
          this.style.backgroundColor = "";
          this.style.removeProperty("--context-parent-background-color");
        }
      }
      _applyScheme() {
        if (this.scheme === "light" || this.scheme === "dark") {
          this.style.colorScheme = this.scheme;
        } else if (this.scheme === "inverted") {
          this.style.colorScheme = this._resolveActiveScheme() === "dark" ? "light" : "dark";
        } else {
          this.style.removeProperty("color-scheme");
        }
      }
      /**
       * Best-effort detection of the *actively rendered* scheme around the
       * section, used to invert it.
       *
       * `getComputedStyle(parent).colorScheme` returns the *declared* value
       * (`'light dark'` for "supports both"; `'normal'` for the initial
       * value) — not the one the browser actually paints with. For
       * `'inverted'` we want the painted scheme, so:
       *   1. Prefer the app's explicit override on `:root[data-scheme]`
       *      (the project's site-wide light/dark toggle).
       *   2. Walk up to find an ancestor that pins a single scheme.
       *   3. Otherwise fall back to `prefers-color-scheme` — that's what
       *      `'light dark'` resolves to in practice.
       *
       * Perf: the per-call cost is `getComputedStyle()` for each ancestor
       * up to one that pins a scheme. This only runs when the project
       * does NOT set `:root[data-scheme]` (apps with a global theme
       * toggle, like regelrecht, hit the fast path and never enter the
       * walk). When the fallback is needed the walk is bounded by the
       * DOM depth and only fires on `scheme` prop change or a
       * color-scheme repaint event — not on every render.
       *
       * Cross-shadow caveat: `parentElement` doesn't traverse shadow
       * boundaries, so a `color-scheme` declared on an outer custom
       * element's host won't be picked up by the walk; we then fall
       * through to `prefers-color-scheme`. For reliable `'inverted'`
       * behaviour inside nested shadow trees, set `data-scheme` on
       * `:root` so the fast path resolves the painted scheme directly.
       */
      _resolveActiveScheme() {
        const root = document.documentElement.getAttribute("data-scheme");
        if (root === "light" || root === "dark")
          return root;
        let el = this.parentElement;
        while (el) {
          const declared = getComputedStyle(el).colorScheme;
          const hasLight = declared.includes("light");
          const hasDark = declared.includes("dark");
          if (hasDark && !hasLight)
            return "dark";
          if (hasLight && !hasDark)
            return "light";
          el = el.parentElement;
        }
        return window.matchMedia?.("(prefers-color-scheme: dark)").matches ? "dark" : "light";
      }
      _applyPadding() {
        const self2 = this;
        for (const scope of PADDING_SCOPES) {
          const prop = (suffix) => scope ? `${scope}Padding${suffix}` : `padding${suffix}`;
          const block = self2[prop("Block")];
          const top = self2[prop("Top")];
          const bottom = self2[prop("Bottom")];
          const prefix = scope ? `${scope}-` : "";
          this._setVar(`--_${prefix}padding-top`, paddingToValue(top ?? block));
          this._setVar(`--_${prefix}padding-bottom`, paddingToValue(bottom ?? block));
        }
      }
      _applyHeight() {
        const v3 = this.height;
        if (v3 && typeof CSS !== "undefined" && CSS.supports("min-height", v3)) {
          this.style.minHeight = v3;
        } else {
          this.style.removeProperty("min-height");
        }
      }
      _setVar(name, value) {
        if (value === null)
          this.style.removeProperty(name);
        else
          this.style.setProperty(name, value);
      }
    }
    __decorate53([
      n4({ type: String, reflect: true })
    ], WithPageSection.prototype, "background", void 0);
    __decorate53([
      n4({ type: String, reflect: true })
    ], WithPageSection.prototype, "scheme", void 0);
    __decorate53([
      n4({ type: String, reflect: true, attribute: "padding-block" })
    ], WithPageSection.prototype, "paddingBlock", void 0);
    __decorate53([
      n4({ type: String, reflect: true, attribute: "padding-top" })
    ], WithPageSection.prototype, "paddingTop", void 0);
    __decorate53([
      n4({ type: String, reflect: true, attribute: "padding-bottom" })
    ], WithPageSection.prototype, "paddingBottom", void 0);
    __decorate53([
      n4({ type: String, reflect: true, attribute: "sm-padding-block" })
    ], WithPageSection.prototype, "smPaddingBlock", void 0);
    __decorate53([
      n4({ type: String, reflect: true, attribute: "sm-padding-top" })
    ], WithPageSection.prototype, "smPaddingTop", void 0);
    __decorate53([
      n4({ type: String, reflect: true, attribute: "sm-padding-bottom" })
    ], WithPageSection.prototype, "smPaddingBottom", void 0);
    __decorate53([
      n4({ type: String, reflect: true, attribute: "md-padding-block" })
    ], WithPageSection.prototype, "mdPaddingBlock", void 0);
    __decorate53([
      n4({ type: String, reflect: true, attribute: "md-padding-top" })
    ], WithPageSection.prototype, "mdPaddingTop", void 0);
    __decorate53([
      n4({ type: String, reflect: true, attribute: "md-padding-bottom" })
    ], WithPageSection.prototype, "mdPaddingBottom", void 0);
    __decorate53([
      n4({ type: String, reflect: true, attribute: "lg-padding-block" })
    ], WithPageSection.prototype, "lgPaddingBlock", void 0);
    __decorate53([
      n4({ type: String, reflect: true, attribute: "lg-padding-top" })
    ], WithPageSection.prototype, "lgPaddingTop", void 0);
    __decorate53([
      n4({ type: String, reflect: true, attribute: "lg-padding-bottom" })
    ], WithPageSection.prototype, "lgPaddingBottom", void 0);
    __decorate53([
      n4({ type: String, reflect: true })
    ], WithPageSection.prototype, "height", void 0);
    return WithPageSection;
  }
  var __decorate53, BACKGROUND_TOKEN, PADDING_SCOPES, PADDING_KEYS;
  var init_page_section_mixin = __esm({
    "node_modules/@nldd/design-system/dist/utilities/page-section-mixin.js"() {
      init_decorators();
      init_color_scheme_repaint();
      __decorate53 = function(decorators, target, key, desc) {
        var c6 = arguments.length, r6 = c6 < 3 ? target : desc === null ? desc = Object.getOwnPropertyDescriptor(target, key) : desc, d3;
        if (typeof Reflect === "object" && typeof Reflect.decorate === "function") r6 = Reflect.decorate(decorators, target, key, desc);
        else for (var i8 = decorators.length - 1; i8 >= 0; i8--) if (d3 = decorators[i8]) r6 = (c6 < 3 ? d3(r6) : c6 > 3 ? d3(target, key, r6) : d3(target, key)) || r6;
        return c6 > 3 && r6 && Object.defineProperty(target, key, r6), r6;
      };
      BACKGROUND_TOKEN = {
        base: "var(--semantics-surfaces-base-background-color)",
        tinted: "var(--semantics-surfaces-tinted-background-color)"
      };
      PADDING_SCOPES = ["", "sm", "md", "lg"];
      PADDING_KEYS = [
        "paddingBlock",
        "paddingTop",
        "paddingBottom",
        "smPaddingBlock",
        "smPaddingTop",
        "smPaddingBottom",
        "mdPaddingBlock",
        "mdPaddingTop",
        "mdPaddingBottom",
        "lgPaddingBlock",
        "lgPaddingTop",
        "lgPaddingBottom"
      ];
    }
  });

  // node_modules/@nldd/design-system/dist/components/layout/page-sections/simple-section/simple-section.styles.js
  var smMax4, mdMin6, mdMax3, lgMin3, simpleSectionStyles;
  var init_simple_section_styles = __esm({
    "node_modules/@nldd/design-system/dist/components/layout/page-sections/simple-section/simple-section.styles.js"() {
      init_lit();
      init_breakpoints();
      smMax4 = r(breakpoints.smMax);
      mdMin6 = r(breakpoints.mdMin);
      mdMax3 = r(breakpoints.mdMax);
      lgMin3 = r(breakpoints.lgMin);
      simpleSectionStyles = i`


	/* # Host */

	:host {
		container-type: inline-size;
		/* Block-padding overrides from PageSectionMixin; 'initial' makes the
		   var() in .simple-section fall back to the responsive default until the
		   mixin sets a value inline on the host. */
		--_padding-top: initial;
		--_padding-bottom: initial;
		--_sm-padding-top: initial;
		--_sm-padding-bottom: initial;
		--_md-padding-top: initial;
		--_md-padding-bottom: initial;
		--_lg-padding-top: initial;
		--_lg-padding-bottom: initial;
		--_max-width: var(--semantics-page-sections-body-max-width);

		display: flex;
		width: 100%;
		flex-direction: column;
		align-items: center;
	}

	:host([hidden]) {
		display: none;
	}

	:host(:last-child),
	:host(.is-last) {
		flex-grow: 1;
	}

	:host([width="full"]) {
		--_max-width: none;
	}


	/* # Block */

	.simple-section {
		box-sizing: border-box;
		display: flex;
		width: 100%;
		flex-direction: column;
		flex-grow: 1;
		align-items: center;


		@container (max-width: ${smMax4}) {
			padding-inline: var(--semantics-page-sections-sm-margin-inline);
			padding-top: var(--_sm-padding-top, var(--_padding-top, var(--semantics-page-sections-sm-margin-block)));
			padding-bottom: var(--_sm-padding-bottom, var(--_padding-bottom, var(--semantics-page-sections-sm-margin-block)));
		}

		@container (min-width: ${mdMin6}) and (max-width: ${mdMax3}) {
			padding-inline: var(--semantics-page-sections-md-margin-inline);
			padding-top: var(--_md-padding-top, var(--_padding-top, var(--semantics-page-sections-md-margin-block)));
			padding-bottom: var(--_md-padding-bottom, var(--_padding-bottom, var(--semantics-page-sections-md-margin-block)));
		}

		@container (min-width: ${lgMin3}) {
			padding-inline: var(--semantics-page-sections-lg-margin-inline);
			padding-top: var(--_lg-padding-top, var(--_padding-top, var(--semantics-page-sections-lg-margin-block)));
			padding-bottom: var(--_lg-padding-bottom, var(--_padding-bottom, var(--semantics-page-sections-lg-margin-block)));
		}
	}


	/* # Elements */

	.simple-section__body {
		display: flex;
		width: 100%;
		max-width: var(--_max-width);
		flex-direction: column;
		flex-grow: 1;

		@container (max-width: ${smMax4}) {
			gap: var(--semantics-page-sections-sm-gap);
		}

		@container (min-width: ${mdMin6}) and (max-width: ${mdMax3}) {
			gap: var(--semantics-page-sections-md-gap);
		}

		@container (min-width: ${lgMin3}) {
			gap: var(--semantics-page-sections-lg-gap);
		}
	}

	.simple-section__header[hidden] {
		display: none;
	}

	.simple-section__main {
		display: flex;
		flex-direction: column;
		flex-grow: 1;
	}

	.simple-section__footer[hidden] {
		display: none;
	}
`;
    }
  });

  // node_modules/@nldd/design-system/dist/components/layout/page-sections/simple-section/simple-section.template.js
  function simpleSectionTemplate(component) {
    return b2`
		<section class="simple-section">
			<div class="simple-section__body">
				<header class="simple-section__header" hidden>
					<slot name="header" @slotchange=${component._onSlotChange}></slot>
				</header>
				<div class="simple-section__main">
					<slot></slot>
				</div>
				<footer class="simple-section__footer" hidden>
					<slot name="footer" @slotchange=${component._onSlotChange}></slot>
				</footer>
			</div>
		</section>
	`;
  }
  var init_simple_section_template = __esm({
    "node_modules/@nldd/design-system/dist/components/layout/page-sections/simple-section/simple-section.template.js"() {
      init_lit();
    }
  });

  // node_modules/@nldd/design-system/dist/components/layout/page-sections/simple-section/simple-section.js
  var simple_section_exports = {};
  __export(simple_section_exports, {
    NLDDSimpleSection: () => NLDDSimpleSection
  });
  var __decorate54, NLDDSimpleSection;
  var init_simple_section = __esm({
    "node_modules/@nldd/design-system/dist/components/layout/page-sections/simple-section/simple-section.js"() {
      init_lit();
      init_decorators();
      init_page_section_mixin();
      init_simple_section_styles();
      init_simple_section_template();
      __decorate54 = function(decorators, target, key, desc) {
        var c6 = arguments.length, r6 = c6 < 3 ? target : desc === null ? desc = Object.getOwnPropertyDescriptor(target, key) : desc, d3;
        if (typeof Reflect === "object" && typeof Reflect.decorate === "function") r6 = Reflect.decorate(decorators, target, key, desc);
        else for (var i8 = decorators.length - 1; i8 >= 0; i8--) if (d3 = decorators[i8]) r6 = (c6 < 3 ? d3(r6) : c6 > 3 ? d3(target, key, r6) : d3(target, key)) || r6;
        return c6 > 3 && r6 && Object.defineProperty(target, key, r6), r6;
      };
      NLDDSimpleSection = class NLDDSimpleSection2 extends PageSectionMixin(i4) {
        constructor() {
          super(...arguments);
          this.width = "";
        }
        updated(changedProperties) {
          super.updated(changedProperties);
          if (changedProperties.has("width")) {
            const w2 = this.width;
            if (w2 && w2 !== "full" && CSS.supports("max-width", w2)) {
              this.style.setProperty("--_max-width", w2);
            } else {
              this.style.removeProperty("--_max-width");
            }
          }
        }
        render() {
          return simpleSectionTemplate(this);
        }
      };
      NLDDSimpleSection.styles = simpleSectionStyles;
      __decorate54([
        n4({ type: String, reflect: true })
      ], NLDDSimpleSection.prototype, "width", void 0);
      NLDDSimpleSection = __decorate54([
        t3("nldd-simple-section")
      ], NLDDSimpleSection);
    }
  });

  // node_modules/@nldd/design-system/dist/components/layout/sheet/sheet.styles.js
  var smMax12, mdMin15, lgMin11, sheetStyles;
  var init_sheet_styles = __esm({
    "node_modules/@nldd/design-system/dist/components/layout/sheet/sheet.styles.js"() {
      init_lit();
      init_breakpoints();
      smMax12 = r(breakpoints.smMax);
      mdMin15 = r(breakpoints.mdMin);
      lgMin11 = r(breakpoints.lgMin);
      sheetStyles = i`


	/* # Host */

	:host {
		--_width: initial;
		--_height: initial;

		display: block;
	}


	/* # Keyframes */

	@keyframes sheet-slide-in-right {
		from { transform: translateX(100%); }
		to { transform: translateX(0); }
	}

	@keyframes sheet-slide-out-right {
		from { transform: translateX(0); }
		to { transform: translateX(100%); }
	}

	@keyframes sheet-slide-in-left {
		from { transform: translateX(-100%); }
		to { transform: translateX(0); }
	}

	@keyframes sheet-slide-out-left {
		from { transform: translateX(0); }
		to { transform: translateX(-100%); }
	}

	@keyframes sheet-slide-in-bottom {
		from { transform: translateY(100%); }
		to { transform: translateY(0); }
	}

	@keyframes sheet-slide-out-bottom {
		from { transform: translateY(0); }
		to { transform: translateY(100%); }
	}


	/* # Block */

	.sheet {
		display: flex;
		position: fixed;
		margin: 0;
		outline: none;
		border: none;
		box-shadow: var(--semantics-overlays-box-shadow);
		background: var(--semantics-surfaces-base-background-color);
		overflow: hidden;
		padding: 0;
		flex-direction: column;
	}

	.sheet:focus-visible:not(.is-pointer-focus) {
		outline: var(--semantics-focus-ring-outline);
		outline-offset: var(--semantics-focus-ring-outline-offset);
		box-shadow: var(--semantics-focus-ring-box-shadow), var(--semantics-overlays-box-shadow);
	}

	.sheet:not([open]) {
		display: none;
	}

	.sheet::backdrop {
		background: var(--semantics-overlays-backdrop-color);
	}

	:host([modeless]) .sheet::backdrop {
		background: transparent;
	}

	/* ## Placement: right (default) */

	:host([placement="right"]) .sheet,
	:host(:not([placement])) .sheet {
		inset: var(--semantics-overlays-inset) var(--semantics-overlays-inset) var(--semantics-overlays-inset) auto;
		border-radius: var(--semantics-overlays-corner-radius);
		width: min(var(--_width, var(--semantics-sheets-side-md-width)), calc(100vw - var(--semantics-overlays-inset) * 2));
		height: calc(100dvh - var(--semantics-overlays-inset) * 2);

		@media (min-width: ${lgMin11}) {
			width: min(var(--_width, var(--semantics-sheets-side-lg-width)), calc(100vw - var(--semantics-overlays-inset) * 2));
		}

		&[open] {
			animation: sheet-slide-in-right var(--semantics-sheets-side-animation-duration) var(--primitives-transition-easing-default) both;
		}

		&.is-closing {
			animation: sheet-slide-out-right var(--semantics-sheets-side-animation-duration) var(--primitives-transition-easing-default) both;
		}
	}

	/* ## Placement: left */

	:host([placement="left"]) .sheet {
		inset: var(--semantics-overlays-inset) auto var(--semantics-overlays-inset) var(--semantics-overlays-inset);
		border-radius: var(--semantics-overlays-corner-radius);
		width: min(var(--_width, var(--semantics-sheets-side-md-width)), calc(100vw - var(--semantics-overlays-inset) * 2));
		height: calc(100dvh - var(--semantics-overlays-inset) * 2);

		@media (min-width: ${lgMin11}) {
			width: min(var(--_width, var(--semantics-sheets-side-lg-width)), calc(100vw - var(--semantics-overlays-inset) * 2));
		}

		&[open] {
			animation: sheet-slide-in-left var(--semantics-sheets-side-animation-duration) var(--primitives-transition-easing-default) both;
		}

		&.is-closing {
			animation: sheet-slide-out-left var(--semantics-sheets-side-animation-duration) var(--primitives-transition-easing-default) both;
		}
	}

	/* ## Placement: bottom
	 *
	 * Default height is full (viewport minus top-inset) so visualisation-
	 * heavy bottom sheets fill the available area without the consumer
	 * having to opt in. Consumers that want a content-sized sheet pass
	 * height="fit-content" — max-height already keeps the sheet within
	 * 100dvh - top-inset regardless of the chosen value, so the dismiss-
	 * tap area at the top is preserved even for oversized custom heights. */

	:host([placement="bottom"]) .sheet {
		inset: auto 0 0 0;
		margin-inline: auto;
		border-radius: var(--semantics-overlays-corner-radius) var(--semantics-overlays-corner-radius) 0 0;
		max-width: var(--semantics-page-sections-body-max-width);
		max-height: calc(100dvh - var(--semantics-sheets-bottom-top-inset));
		height: var(--_height, calc(100dvh - var(--semantics-sheets-bottom-top-inset)));

		@media (max-width: ${smMax12}) {
			width: 100%;
			max-width: 100%;
		}

		@media (min-width: ${mdMin15}) {
			width: calc(100% - var(--semantics-sheets-bottom-md-inline-inset));
		}

		@media (min-width: ${lgMin11}) {
			width: calc(100% - var(--semantics-sheets-bottom-lg-inline-inset));
		}

		&[open] {
			animation: sheet-slide-in-bottom var(--semantics-sheets-bottom-animation-duration) var(--primitives-transition-easing-default) both;
		}

		&.is-closing {
			animation: sheet-slide-out-bottom var(--semantics-sheets-bottom-animation-duration) var(--primitives-transition-easing-default) both;
		}
	}

	/* ## Responsive: sm viewport — all placements become bottom sheet
	 *
	 * The same default-full + opt-out-via-height semantics apply when
	 * a side sheet collapses to a bottom sheet on sm. */

	@media (max-width: ${smMax12}) {
		:host([placement="right"]) .sheet,
		:host(:not([placement])) .sheet,
		:host([placement="left"]) .sheet {
			inset: auto 0 0 0;
			border-radius: var(--semantics-overlays-corner-radius) var(--semantics-overlays-corner-radius) 0 0;
			width: 100%;
			max-width: 100%;
			max-height: calc(100dvh - var(--semantics-sheets-bottom-top-inset));
			height: var(--_height, calc(100dvh - var(--semantics-sheets-bottom-top-inset)));

			&[open] {
				animation: sheet-slide-in-bottom var(--semantics-sheets-bottom-animation-duration) var(--primitives-transition-easing-default) both;
			}

			&.is-closing {
				animation: sheet-slide-out-bottom var(--semantics-sheets-bottom-animation-duration) var(--primitives-transition-easing-default) both;
			}
		}
	}

	@media (prefers-reduced-motion: reduce) {
		.sheet[open],
		.sheet.is-closing {
			animation: none;
		}
	}


	/* # Elements */

	.sheet__body {
		display: flex;
		min-height: 0;
		width: 100%;
		flex-direction: column;
		flex-grow: 1;
	}

	::slotted(*) {
		min-height: 0;
		flex-grow: 1;
		flex-shrink: 1;
		flex-basis: 0;
	}
`;
    }
  });

  // node_modules/@nldd/design-system/dist/components/layout/sheet/sheet.template.js
  function sheetTemplate(component) {
    return b2`
		<dialog class="sheet"
			aria-label=${component.accessibleLabel}
			aria-modal=${component.modeless ? A : "true"}
			@click=${component._handleDialogClick}
			@cancel=${component._handleCancel}
		>
			<div class="sheet__body">
				<slot></slot>
			</div>
		</dialog>
	`;
  }
  var init_sheet_template = __esm({
    "node_modules/@nldd/design-system/dist/components/layout/sheet/sheet.template.js"() {
      init_lit();
    }
  });

  // node_modules/@nldd/design-system/dist/components/layout/sheet/sheet.js
  var sheet_exports = {};
  __export(sheet_exports, {
    NLDDSheet: () => NLDDSheet
  });
  var import_meta18, __decorate66, NLDDSheet;
  var init_sheet = __esm({
    "node_modules/@nldd/design-system/dist/components/layout/sheet/sheet.js"() {
      init_lit();
      init_decorators();
      init_sheet_styles();
      init_sheet_template();
      init_input_modality();
      import_meta18 = {};
      __decorate66 = function(decorators, target, key, desc) {
        var c6 = arguments.length, r6 = c6 < 3 ? target : desc === null ? desc = Object.getOwnPropertyDescriptor(target, key) : desc, d3;
        if (typeof Reflect === "object" && typeof Reflect.decorate === "function") r6 = Reflect.decorate(decorators, target, key, desc);
        else for (var i8 = decorators.length - 1; i8 >= 0; i8--) if (d3 = decorators[i8]) r6 = (c6 < 3 ? d3(r6) : c6 > 3 ? d3(target, key, r6) : d3(target, key)) || r6;
        return c6 > 3 && r6 && Object.defineProperty(target, key, r6), r6;
      };
      NLDDSheet = class NLDDSheet2 extends i4 {
        constructor() {
          super(...arguments);
          this.placement = "right";
          this.height = "";
          this.modeless = false;
          this.accessibleLabel = "Venster";
          this.width = "";
          this._hasWarnedLabel = false;
          this._hasWarnedHeight = false;
          this._closing = false;
          this._handleDismiss = () => {
            this.hide();
          };
        }
        updated(changed) {
          if (changed.has("width")) {
            if (this.width) {
              this.style.setProperty("--_width", this.width);
            } else {
              this.style.removeProperty("--_width");
            }
          }
          if (changed.has("height")) {
            const h4 = this.height;
            if (h4 && h4 !== "full" && CSS.supports("height", h4)) {
              this.style.setProperty("--_height", h4);
            } else {
              if (import_meta18.env?.DEV && h4 && h4 !== "full" && !this._hasWarnedHeight) {
                this._hasWarnedHeight = true;
                console.warn(`<nldd-sheet>: Invalid height value "${h4}". Falling back to full height. Use 'full', 'fit-content', or a valid CSS length (e.g. '50dvh', '480px').`);
              }
              this.style.removeProperty("--_height");
            }
          }
        }
        get _dialog() {
          return this.shadowRoot?.querySelector("dialog") ?? null;
        }
        connectedCallback() {
          super.connectedCallback();
          this.style.containerType = "inline-size";
          this.style.containerName = "layout-container";
          this.addEventListener("dismiss", this._handleDismiss);
        }
        disconnectedCallback() {
          super.disconnectedCallback();
          this.removeEventListener("dismiss", this._handleDismiss);
        }
        show() {
          const dialog = this._dialog;
          if (!dialog)
            return;
          if (import_meta18.env?.DEV && this.accessibleLabel === "Dialoogvenster" && !this._hasWarnedLabel) {
            this._hasWarnedLabel = true;
            console.warn('<nldd-sheet>: No accessible-label provided. Screen readers will announce this dialog as "Dialoogvenster". Set accessible-label to a descriptive name matching the dialog title.');
          }
          if (this.modeless) {
            dialog.show();
          } else {
            dialog.showModal();
          }
          this._manageFocus();
          this.dispatchEvent(new CustomEvent("open", { bubbles: true, composed: true }));
        }
        _manageFocus() {
          if (this.querySelector("[autofocus]"))
            return;
          const dialog = this._dialog;
          if (!dialog)
            return;
          dialog.classList.toggle("is-pointer-focus", isPointerMode());
          dialog.focus();
        }
        hide() {
          const dialog = this._dialog;
          if (!dialog || !dialog.open || this._closing)
            return;
          this._closing = true;
          dialog.classList.add("is-closing");
          dialog.addEventListener("animationend", () => {
            dialog.classList.remove("is-closing");
            this._closing = false;
            dialog.close();
            this.dispatchEvent(new CustomEvent("close", { bubbles: true, composed: true }));
          }, { once: true });
          requestAnimationFrame(() => {
            if (this._closing && getComputedStyle(dialog).animationName === "none") {
              dialog.classList.remove("is-closing");
              this._closing = false;
              dialog.close();
              this.dispatchEvent(new CustomEvent("close", { bubbles: true, composed: true }));
            }
          });
        }
        _handleDialogClick(e9) {
          if (this.modeless)
            return;
          if (e9.target === this._dialog) {
            this.hide();
          }
        }
        _handleCancel(e9) {
          e9.preventDefault();
          this.hide();
        }
        render() {
          return sheetTemplate(this);
        }
      };
      NLDDSheet.styles = sheetStyles;
      __decorate66([
        n4({ type: String, reflect: true })
      ], NLDDSheet.prototype, "placement", void 0);
      __decorate66([
        n4({ type: String, reflect: true })
      ], NLDDSheet.prototype, "height", void 0);
      __decorate66([
        n4({ type: Boolean, reflect: true })
      ], NLDDSheet.prototype, "modeless", void 0);
      __decorate66([
        n4({ type: String, attribute: "accessible-label" })
      ], NLDDSheet.prototype, "accessibleLabel", void 0);
      __decorate66([
        n4({ type: String, reflect: true })
      ], NLDDSheet.prototype, "width", void 0);
      NLDDSheet = __decorate66([
        t3("nldd-sheet")
      ], NLDDSheet);
    }
  });

  // node_modules/@nldd/design-system/dist/components/lists-and-tables/list/list.styles.js
  var listStyles;
  var init_list_styles = __esm({
    "node_modules/@nldd/design-system/dist/components/lists-and-tables/list/list.styles.js"() {
      init_lit();
      listStyles = i`


	/* # Host */

	:host {
		--_drag-clone-top: 0px;
		--_drag-clone-left: 0px;
		--_drag-clone-opacity: 0.95;
		--_drag-clone-z-index: 100;
		--_drag-clone-width: 0px;
		--_drag-clone-height: 0px;

		display: block;
		position: relative;
		isolation: isolate;
	}

	:host([no-dividers]) {
		--context-list-divider-display: none;
	}


	/* ## Background + border
	   Only the box variant has a surface. simple is a plain vertical
	   strip with no chrome. variant="box" defaults to the tinted
	   surface; an explicit background="base" picks the base semantic
	   pair (same shape nldd-box uses). */

	:host([variant="box"]) {
		--_background-color: var(--semantics-surfaces-tinted-background-color);
		--_border-color: var(--semantics-surfaces-tinted-border-color);
	}

	:host([background="base"]) {
		--_background-color: var(--semantics-surfaces-base-background-color);
		--_border-color: var(--semantics-surfaces-base-border-color);
	}

	:host([variant="box"]) .list__items {
		border-radius: var(--components-list-corner-radius);
		background-color: var(--_background-color);
		/* Inner box-shadow paints the border ring inside the radius
		   without taking layout space, matching nldd-box / nldd-banner. */
		box-shadow: inset 0 0 0 1px var(--_border-color);
		overflow: hidden;
	}


	/* # Elements */

	.list__body {
		display: flex;
		flex-direction: column;
		gap: var(--primitives-space-8);
	}

	.list__header,
	.list__footer {
		display: contents;
	}

	.list__items {
		display: flex;
		flex-direction: column;
	}

	.list__empty {
		padding: var(--primitives-space-16);
	}

	.list__empty[hidden] {
		display: none;
	}

	::slotted(.nldd-list-drag-placeholder) {
		box-sizing: border-box;
		border-radius: var(--components-list-item-indicator-corner-radius);
		background-color: var(--components-list-drag-placeholder-background-color);
		pointer-events: none;
	}

	.list__drag-clone {
		display: flex;
		position: absolute;
		top: var(--_drag-clone-top);
		left: var(--_drag-clone-left);
		opacity: var(--_drag-clone-opacity);
		z-index: var(--_drag-clone-z-index);
		border-radius: var(--components-list-item-indicator-corner-radius);
		background: var(--semantics-surfaces-base-background-color);
		pointer-events: none;
		width: var(--_drag-clone-width);
		height: var(--_drag-clone-height);
		overflow: hidden;
		flex-direction: row;
		align-items: stretch;
	}

	.list__polite-announcer,
	.list__assertive-announcer {
		position: absolute;
		margin: -1px;
		border: 0;
		width: 1px;
		height: 1px;
		overflow: hidden;
		padding: 0;
		white-space: nowrap;
		clip-path: inset(50%);
	}


	/* # Accessibility
	   forced-colors / Windows High Contrast strips box-shadow, so the
	   inset border on the box variant would disappear. Restore the
	   frame with a real border in that mode — same fallback nldd-box
	   and nldd-banner use. */

	@media (forced-colors: active) {
		:host([variant="box"]) .list__items {
			border: 1px solid CanvasText;
		}
	}
`;
    }
  });

  // node_modules/@nldd/design-system/dist/components/lists-and-tables/list/list.template.js
  var template13;
  var init_list_template = __esm({
    "node_modules/@nldd/design-system/dist/components/lists-and-tables/list/list.template.js"() {
      init_lit();
      init_if_defined2();
      template13 = (itemsLabel, hasHeader, type, isEmpty, emptyText, emptySupportingText) => {
        const isNavigation = type === "navigation";
        const skipItemsLabel = hasHeader || isNavigation;
        const itemsRole = isEmpty ? void 0 : "list";
        return b2`
		<div class="list__body">
			<div class="list__header">
				<slot name="header"></slot>
			</div>
			<div class="list__items"
				role=${o7(itemsRole)}
				aria-label=${o7(skipItemsLabel || isEmpty ? void 0 : itemsLabel)}
			>
				<slot></slot>
				<div class="list__empty" ?hidden=${!isEmpty}>
					<slot name="empty">
						<nldd-inline-dialog
							text=${emptyText}
							supporting-text=${emptySupportingText || A}
						></nldd-inline-dialog>
					</slot>
				</div>
			</div>
			<div class="list__footer">
				<slot name="footer"></slot>
			</div>
		</div>
		<div class="list__polite-announcer"
			role="status"
			aria-live="polite"
			aria-atomic="true"
		></div>
		<div class="list__assertive-announcer"
			role="alert"
			aria-live="assertive"
			aria-atomic="true"
		></div>
	`;
      };
    }
  });

  // node_modules/@nldd/design-system/dist/components/lists-and-tables/list/list.i18n.js
  var nlddListTranslations;
  var init_list_i18n = __esm({
    "node_modules/@nldd/design-system/dist/components/lists-and-tables/list/list.i18n.js"() {
      nlddListTranslations = {
        "components.list.items-label-text": "Lijst",
        "components.list.navigation-label-text": "Navigatie",
        "components.list.empty-text": "Geen items",
        "components.list.reorder-moved-text": "Item verplaatst naar positie {position}.",
        "components.list.reorder-dropped-text": "Item neergezet op positie {position}.",
        "components.list.reorder-no-change-text": "Item neergezet. Positie ongewijzigd.",
        "components.list.reorder-cancelled-text": "Slepen geannuleerd."
      };
    }
  });

  // node_modules/@nldd/design-system/dist/components/lists-and-tables/list/list.js
  var list_exports = {};
  __export(list_exports, {
    NLDDList: () => NLDDList
  });
  var import_meta21, __decorate69, NLDDList;
  var init_list = __esm({
    "node_modules/@nldd/design-system/dist/components/lists-and-tables/list/list.js"() {
      init_lit();
      init_decorators();
      init_list_styles();
      init_list_template();
      init_list_i18n();
      init_inline_dialog();
      import_meta21 = {};
      __decorate69 = function(decorators, target, key, desc) {
        var c6 = arguments.length, r6 = c6 < 3 ? target : desc === null ? desc = Object.getOwnPropertyDescriptor(target, key) : desc, d3;
        if (typeof Reflect === "object" && typeof Reflect.decorate === "function") r6 = Reflect.decorate(decorators, target, key, desc);
        else for (var i8 = decorators.length - 1; i8 >= 0; i8--) if (d3 = decorators[i8]) r6 = (c6 < 3 ? d3(r6) : c6 > 3 ? d3(target, key, r6) : d3(target, key)) || r6;
        return c6 > 3 && r6 && Object.defineProperty(target, key, r6), r6;
      };
      NLDDList = class NLDDList2 extends i4 {
        constructor() {
          super(...arguments);
          this.variant = "simple";
          this.type = "list";
          this.reorderable = false;
          this.noDividers = false;
          this.emptyText = "";
          this.emptySupportingText = "";
          this.translations = {};
          this._mergedTranslations = { ...nlddListTranslations };
          this._hasHeader = false;
          this._isEmpty = false;
          this._draggingEl = null;
          this._draggingFromIndex = -1;
          this._placeholder = null;
          this._currentDropIndex = -1;
          this._pointerId = null;
          this._clone = null;
          this._cloneOffsetY = 0;
          this._listRect = null;
          this._itemsObserver = null;
          this._onPointerDown = (event) => {
            if (!this.reorderable || this.type !== "list")
              return;
            const path = event.composedPath();
            const hasDragHandle = path.some((el) => el instanceof Element && el.hasAttribute("reorderable-only"));
            if (!hasDragHandle)
              return;
            const item = path.find((el) => el instanceof Element && el.tagName.toLowerCase() === "nldd-list-item");
            if (!item)
              return;
            event.preventDefault();
            this._lastPointerY = event.clientY;
            this._startDrag(item, event.clientY);
            const handle = path.find((el) => el instanceof HTMLButtonElement);
            handle?.focus();
            this._pointerId = event.pointerId;
            this.setPointerCapture(event.pointerId);
            this.addEventListener("pointermove", this._onPointerMove);
            this.addEventListener("pointerup", this._onPointerUp);
            this.addEventListener("pointercancel", this._onPointerCancel);
          };
          this._lastPointerY = 0;
          this._onPointerMove = (event) => {
            if (!this._draggingEl || !this._placeholder)
              return;
            if (this._clone) {
              this._listRect = this.getBoundingClientRect();
              this._clone.style.setProperty("--_drag-clone-top", `${event.clientY - this._listRect.top - this._cloneOffsetY}px`);
            }
            const draggingDown = event.clientY >= this._lastPointerY;
            this._lastPointerY = event.clientY;
            const items = this._getItems();
            const nonDragging = items.filter((i8) => i8 !== this._draggingEl);
            const pointerY = event.clientY;
            let toIndex = nonDragging.length;
            for (let i8 = 0; i8 < nonDragging.length; i8++) {
              const inner = nonDragging[i8].shadowRoot?.querySelector(".list-item") ?? nonDragging[i8];
              const rect = inner.getBoundingClientRect();
              const threshold = draggingDown ? rect.top : rect.bottom;
              if (pointerY < threshold) {
                toIndex = i8;
                break;
              }
            }
            this._setDropIndex(toIndex);
          };
          this._onPointerUp = (_event) => {
            this._endDrag();
          };
          this._onPointerCancel = () => {
            this._cancelDrag();
          };
          this._onKeyDown = (event) => {
            if (!this.reorderable || this.type !== "list")
              return;
            if (event.key !== "ArrowUp" && event.key !== "ArrowDown")
              return;
            const path = event.composedPath();
            const handle = path.find((el) => el instanceof HTMLElement && el.hasAttribute("reorderable-only"));
            if (!handle)
              return;
            const item = path.find((el) => el instanceof Element && el.tagName.toLowerCase() === "nldd-list-item");
            if (!item)
              return;
            const items = this._getItems();
            const fromIndex = items.indexOf(item);
            if (fromIndex === -1)
              return;
            const targetIndex = event.key === "ArrowUp" ? fromIndex - 1 : fromIndex + 1;
            if (targetIndex < 0 || targetIndex >= items.length)
              return;
            event.preventDefault();
            const focused = this._getDeepActiveElement() ?? handle;
            this.dispatchEvent(new CustomEvent("nldd-reorder", {
              detail: { fromIndex, toIndex: targetIndex },
              bubbles: true,
              composed: true
            }));
            this._announce(this._t("components.list.reorder-moved-text", { position: targetIndex + 1 }));
            requestAnimationFrame(() => focused.focus());
          };
        }
        // — Lifecycle ————————————————————————————————————————————————————————————
        firstUpdated() {
          const slot = this.shadowRoot?.querySelector("slot:not([name])");
          slot?.addEventListener("slotchange", () => {
            this._updateItems();
            this._updateEmpty();
          });
          this._updateItems();
          this._updateEmpty();
          const headerSlot = this.shadowRoot?.querySelector('slot[name="header"]');
          headerSlot?.addEventListener("slotchange", () => {
            this._hasHeader = headerSlot.assignedElements().length > 0;
          });
          this._itemsObserver = new MutationObserver((mutations) => {
            const relevant = mutations.some((m3) => {
              if (m3.type === "childList")
                return m3.target === this;
              if (m3.type === "attributes" && m3.attributeName === "hidden") {
                return m3.target instanceof Element && m3.target.parentElement === this;
              }
              return false;
            });
            if (!relevant)
              return;
            this._updateItems();
            this._updateEmpty();
          });
          this._itemsObserver.observe(this, {
            childList: true,
            subtree: true,
            attributes: true,
            attributeFilter: ["hidden"]
          });
          this._applyHostType();
        }
        connectedCallback() {
          super.connectedCallback();
          this.style.containerType = "inline-size";
          this.style.containerName = "cells-container";
          this.addEventListener("pointerdown", this._onPointerDown);
          this.addEventListener("keydown", this._onKeyDown);
        }
        disconnectedCallback() {
          super.disconnectedCallback();
          this.removeEventListener("pointerdown", this._onPointerDown);
          this.removeEventListener("keydown", this._onKeyDown);
          this._itemsObserver?.disconnect();
          this._itemsObserver = null;
          this._cancelDrag();
        }
        updated(changed) {
          if (changed.has("reorderable") || changed.has("type")) {
            if (this.reorderable && this.type !== "list" && import_meta21.env?.DEV) {
              console.warn('nldd-list: `reorderable` is only valid when type="list". Ignoring.');
            }
            this._updateItems();
          }
          if (changed.has("translations")) {
            this._mergedTranslations = { ...nlddListTranslations, ...this.translations };
          }
          if (changed.has("type")) {
            this._applyHostType();
          }
        }
        // — Host attribute routing ————————————————————————————————————————————————
        _applyHostType() {
          if (this.type === "navigation") {
            this.setAttribute("role", "navigation");
            if (!this.hasAttribute("aria-label") && !this.hasAttribute("aria-labelledby")) {
              this.setAttribute("aria-label", this._t("components.list.navigation-label-text"));
              this.setAttribute("data-nldd-auto-label", "");
            }
          } else {
            if (this.getAttribute("role") === "navigation") {
              this.removeAttribute("role");
            }
            if (this.hasAttribute("data-nldd-auto-label")) {
              const autoLabel = this._t("components.list.navigation-label-text");
              if (this.getAttribute("aria-label") === autoLabel) {
                this.removeAttribute("aria-label");
              }
              this.removeAttribute("data-nldd-auto-label");
            }
          }
        }
        // — Items ————————————————————————————————————————————————————————————————
        _getItems() {
          const slot = this.shadowRoot?.querySelector("slot:not([name])");
          return (slot?.assignedElements() ?? []).filter((el) => el.tagName.toLowerCase() === "nldd-list-item" && !el.hasAttribute("data-nldd-placeholder"));
        }
        _updateItems() {
          const items = this._getItems();
          const visibleItems = items.filter((item) => !item.hasAttribute("hidden"));
          const lastVisible = visibleItems[visibleItems.length - 1];
          const reorderActive = this.reorderable && this.type === "list";
          items.forEach((item) => {
            item.classList.toggle("is-last", item === lastVisible);
            if (reorderActive) {
              item.setAttribute("reorderable", "");
            } else {
              item.removeAttribute("reorderable");
            }
          });
        }
        _updateEmpty() {
          const items = this._getItems();
          this._isEmpty = items.length === 0 || items.every((item) => item.hasAttribute("hidden"));
        }
        _getDeepActiveElement() {
          let active = document.activeElement;
          while (active?.shadowRoot?.activeElement) {
            active = active.shadowRoot.activeElement;
          }
          return active instanceof HTMLElement ? active : null;
        }
        // — Drag: core ———————————————————————————————————————————————————————————
        _startDrag(item, clientY = 0) {
          const items = this._getItems();
          const fromIndex = items.indexOf(item);
          if (fromIndex === -1)
            return;
          this._draggingEl = item;
          this._draggingFromIndex = fromIndex;
          this._currentDropIndex = fromIndex;
          const inner = item.shadowRoot?.querySelector(".list-item") ?? item;
          const rect = inner.getBoundingClientRect();
          this._placeholder = document.createElement("div");
          this._placeholder.className = "nldd-list-drag-placeholder";
          this._placeholder.setAttribute("aria-hidden", "true");
          this._placeholder.setAttribute("data-nldd-placeholder", "");
          this._placeholder.style.height = `${rect.height}px`;
          item.after(this._placeholder);
          item.classList.add("is-dragging");
          item.classList.add("is-dragging-pointer");
          this._listRect = this.getBoundingClientRect();
          this._cloneOffsetY = clientY - rect.top;
          const hostClone = item.cloneNode(true);
          hostClone.classList.remove("is-dragging");
          hostClone.classList.remove("is-dragging-pointer");
          hostClone.setAttribute("data-nldd-clone", "");
          this._clone = document.createElement("div");
          this._clone.className = "list__drag-clone";
          this._clone.style.setProperty("--_drag-clone-top", `${clientY - this._listRect.top - this._cloneOffsetY}px`);
          this._clone.style.setProperty("--_drag-clone-left", `${rect.left - this._listRect.left}px`);
          this._clone.style.setProperty("--_drag-clone-width", `${rect.width}px`);
          this._clone.style.setProperty("--_drag-clone-height", `${rect.height}px`);
          this._clone.appendChild(hostClone);
          this.renderRoot.appendChild(this._clone);
        }
        /**
         * Places the placeholder so the dragged item will land at position `toIndex`
         * among the non-dragging items (0 = before first, nonDragging.length = after last).
         */
        _setDropIndex(toIndex) {
          if (!this._placeholder || !this._draggingEl)
            return;
          const nonDragging = this._getItems().filter((i8) => i8 !== this._draggingEl);
          const clamped = Math.max(0, Math.min(nonDragging.length, toIndex));
          this._currentDropIndex = clamped;
          this._placeholder.remove();
          if (nonDragging.length === 0) {
            this._draggingEl.after(this._placeholder);
            return;
          }
          if (clamped === 0) {
            nonDragging[0].before(this._placeholder);
          } else {
            nonDragging[clamped - 1].after(this._placeholder);
          }
        }
        _getDropIndex() {
          return this._currentDropIndex;
        }
        _endDrag() {
          if (!this._draggingEl)
            return;
          const fromIndex = this._draggingFromIndex;
          const toIndex = this._getDropIndex();
          const movedItem = this._draggingEl;
          this._cleanupDrag();
          if (fromIndex !== toIndex) {
            this.dispatchEvent(new CustomEvent("nldd-reorder", {
              detail: { fromIndex, toIndex },
              bubbles: true,
              composed: true
            }));
            this._announce(this._t("components.list.reorder-dropped-text", { position: toIndex + 1 }));
            requestAnimationFrame(() => {
              const handle = movedItem.querySelector("[reorderable-only]")?.shadowRoot?.querySelector("button");
              handle?.focus();
            });
          } else {
            this._announce(this._t("components.list.reorder-no-change-text"));
          }
        }
        _cancelDrag() {
          if (!this._draggingEl)
            return;
          this._cleanupDrag();
          this._announce(this._t("components.list.reorder-cancelled-text"));
        }
        _cleanupDrag() {
          this._draggingEl?.classList.remove("is-dragging");
          this._draggingEl?.classList.remove("is-dragging-pointer");
          this._placeholder?.remove();
          this._clone?.remove();
          if (this._pointerId !== null) {
            try {
              this.releasePointerCapture(this._pointerId);
            } catch (e9) {
              if (!(e9 instanceof DOMException))
                throw e9;
            }
            this._pointerId = null;
          }
          this.removeEventListener("pointermove", this._onPointerMove);
          this.removeEventListener("pointerup", this._onPointerUp);
          this.removeEventListener("pointercancel", this._onPointerCancel);
          this._draggingEl = null;
          this._draggingFromIndex = -1;
          this._placeholder = null;
          this._clone = null;
          this._cloneOffsetY = 0;
          this._listRect = null;
          this._currentDropIndex = -1;
        }
        // — i18n ————————————————————————————————————————————————————————————————
        _t(key, vars) {
          let str = this._mergedTranslations[key];
          if (vars) {
            for (const [k2, v3] of Object.entries(vars)) {
              str = str.replace(`{${k2}}`, String(v3));
            }
          }
          return str;
        }
        _announce(message, assertive = false) {
          const selector = assertive ? ".list__assertive-announcer" : ".list__polite-announcer";
          const region = this.shadowRoot?.querySelector(selector);
          if (!region)
            return;
          region.textContent = "";
          requestAnimationFrame(() => requestAnimationFrame(() => {
            region.textContent = message;
          }));
        }
        render() {
          return template13(this._t("components.list.items-label-text"), this._hasHeader, this.type, this._isEmpty, this.emptyText || this._t("components.list.empty-text"), this.emptySupportingText);
        }
      };
      NLDDList.styles = [listStyles];
      __decorate69([
        n4({ reflect: true })
      ], NLDDList.prototype, "variant", void 0);
      __decorate69([
        n4({ reflect: true })
      ], NLDDList.prototype, "background", void 0);
      __decorate69([
        n4({ reflect: true })
      ], NLDDList.prototype, "type", void 0);
      __decorate69([
        n4({ type: Boolean, reflect: true })
      ], NLDDList.prototype, "reorderable", void 0);
      __decorate69([
        n4({ type: Boolean, reflect: true, attribute: "no-dividers" })
      ], NLDDList.prototype, "noDividers", void 0);
      __decorate69([
        n4({ type: String, attribute: "empty-text" })
      ], NLDDList.prototype, "emptyText", void 0);
      __decorate69([
        n4({ type: String, attribute: "empty-supporting-text" })
      ], NLDDList.prototype, "emptySupportingText", void 0);
      __decorate69([
        n4({ type: Object })
      ], NLDDList.prototype, "translations", void 0);
      __decorate69([
        r5()
      ], NLDDList.prototype, "_mergedTranslations", void 0);
      __decorate69([
        r5()
      ], NLDDList.prototype, "_hasHeader", void 0);
      __decorate69([
        r5()
      ], NLDDList.prototype, "_isEmpty", void 0);
      NLDDList = __decorate69([
        t3("nldd-list")
      ], NLDDList);
    }
  });

  // node_modules/@nldd/design-system/dist/components/lists-and-tables/list-item/list-item.styles.js
  var listItemStyles;
  var init_list_item_styles = __esm({
    "node_modules/@nldd/design-system/dist/components/lists-and-tables/list-item/list-item.styles.js"() {
      init_lit();
      listItemStyles = i`


	/* # Host */

	:host {
		--_background-color: transparent;
		--_z-index-content: 0;
		--_z-index-focus: 1;
		--_z-index-indicator: calc(var(--_z-index-content) - 1);
		--_focus-outline-offset: 6px;

		--context-list-item-padding-block: var(--components-list-item-md-padding-block);

		container-type: inline-size;
		display: block;
		width: 100%;
		-webkit-tap-highlight-color: transparent;
	}

	:host([size="sm"]) {
		--context-list-item-padding-block: var(--components-list-item-sm-padding-block);
	}

	:host([hidden]) {
		display: none;
	}

	:host(:focus-within) {
		position: relative;
		z-index: var(--_z-index-focus);
	}

	:host(.is-dragging) {
		opacity: var(--semantics-controls-is-dragging-opacity);
	}

	:host(.is-dragging-pointer) {
		display: none;
	}

	:host(:not([reorderable])) ::slotted([reorderable-only]) {
		display: none;
	}

	:host([reorderable]) ::slotted([reorderable-only]) {
		cursor: grab;
		touch-action: none;
	}

	:host(.is-dragging) ::slotted([reorderable-only]) {
		cursor: grabbing;
	}


	/* # Block */

	.list-item {
		box-sizing: border-box;
		display: flex;
		position: relative;
		width: 100%;
		min-height: var(--semantics-controls-md-min-size);
		flex-direction: row;
		align-items: stretch;
		isolation: isolate;
	}

	:host([size="sm"]) .list-item {
		min-height: var(--semantics-controls-sm-min-size);
	}


	/* # Action */

	.list-item__action {
		display: flex;
		margin: 0;
		outline: none;
		border: none;
		background: none;
		width: 100%;
		padding: 0;
		flex-direction: row;
		align-items: stretch;
		text-align: start;
		color: inherit;
		text-decoration: none;
	}

	a.list-item__action {
		cursor: var(--semantics-controls-link-cursor);
	}


	/* # Indicator */

	/* Non-interactive: items without an inner action get the selection
	   indicator directly on .list-item, driven only by :host([selected]) */

	.list-item:not(:has(.list-item__action))::before {
		content: '';
		display: block;
		position: absolute;
		inset-block: 0;
		inset-inline: min(calc(var(--primitives-space-8) * -1), calc(var(--components-list-item-indicator-corner-radius) * -1));
		z-index: var(--_z-index-indicator);
		border-radius: var(--components-list-item-indicator-corner-radius);
		background-color: var(--_background-color);
		pointer-events: none;
	}

	:host([selected]) {
		--_background-color: var(--components-list-item-is-selected-background-color);
		--context-cell-content-color: var(--components-list-item-is-selected-content-color);
	}

	/* Interactive: items with a link/button inner element; indicator responds
	   to hover and focus on top of the selected state */

	.list-item__action::before {
		content: '';
		display: block;
		position: absolute;
		inset-block: 0;
		inset-inline: min(calc(var(--primitives-space-8) * -1), calc(var(--components-list-item-indicator-corner-radius) * -1));
		z-index: var(--_z-index-indicator);
		border-radius: var(--components-list-item-indicator-corner-radius);
		background-color: var(--_background-color);
		pointer-events: none;
	}

	/* hover only on hover-capable devices: avoids touch-scroll flashing the
	   hover state on the row under the finger */
	@media (hover: hover) {
		.list-item__action:hover {
			--_background-color: var(--components-list-item-is-hovered-background-color);
			--context-cell-content-color: var(--components-list-item-is-hovered-content-color);
		}

		:host([selected]) .list-item__action:hover {
			--_background-color: var(--components-list-item-is-selected-background-color);
			--context-cell-content-color: var(--components-list-item-is-selected-content-color);
		}

		:host([selected]) .list-item__action:focus:hover {
			--_background-color: var(--components-list-item-is-highlighted-background-color);
			--context-cell-content-color: var(--components-list-item-is-highlighted-content-color);
		}
	}

	/* Press feedback — works on touch where :hover doesn't. Same visual as
	   :hover (the gated mouse rule above) so it reads consistently across
	   input modes. */
	.list-item__action:active {
		--_background-color: var(--components-list-item-is-hovered-background-color);
		--context-cell-content-color: var(--components-list-item-is-hovered-content-color);
	}

	:host([selected]) .list-item__action:active {
		--_background-color: var(--components-list-item-is-highlighted-background-color);
		--context-cell-content-color: var(--components-list-item-is-highlighted-content-color);
	}

	:host([selected]) .list-item__action {
		--_background-color: var(--components-list-item-is-selected-background-color);
		--context-cell-content-color: var(--components-list-item-is-selected-content-color);
	}

	:host([selected]) .list-item__action:focus {
		--_background-color: var(--components-list-item-is-highlighted-background-color);
		--context-cell-content-color: var(--components-list-item-is-highlighted-content-color);
	}


	/* # Focus */

	.list-item__action:focus-visible:not(.is-pointer-focus)::after {
		content: '';
		display: block;
		position: absolute;
		outline: var(--semantics-focus-ring-outline);
		outline-offset: var(--semantics-focus-ring-outline-offset);
		box-shadow: var(--semantics-focus-ring-box-shadow);
		pointer-events: none;
	}

	:host(:not(.is-boxed)) .list-item__action:focus-visible:not(.is-pointer-focus)::after {
		inset-block: 0;
		inset-inline: min(calc(var(--primitives-space-8) * -1), calc(var(--components-list-item-indicator-corner-radius) * -1));
		border-radius: var(--components-list-item-indicator-corner-radius);
	}

	:host(.is-boxed) .list-item__action:focus-visible:not(.is-pointer-focus)::after {
		inset: var(--_focus-outline-offset);
	}

	:host(.is-boxed:first-child) .list-item__action:focus-visible:not(.is-pointer-focus)::after {
		border-top-left-radius: calc(var(--components-list-corner-radius) - var(--_focus-outline-offset));
		border-top-right-radius: calc(var(--components-list-corner-radius) - var(--_focus-outline-offset));
	}

	:host(.is-boxed:last-child) .list-item__action:focus-visible:not(.is-pointer-focus)::after {
		border-bottom-left-radius: calc(var(--components-list-corner-radius) - var(--_focus-outline-offset));
		border-bottom-right-radius: calc(var(--components-list-corner-radius) - var(--_focus-outline-offset));
	}


	/* # Start & end area */

	.list-item__start-area,
	.list-item__end-area {
		display: none;
		position: relative;
		z-index: var(--_z-index-content);
		padding-block: var(--context-list-item-padding-block);
		flex-direction: row;
		flex-shrink: 0;
		align-items: center;
	}

	.list-item__start-area.is-visible,
	.list-item__end-area.is-visible {
		display: flex;
	}


	/* # Main area */

	.list-item__main-area {
		display: flex;
		position: relative;
		z-index: var(--_z-index-content);
		min-width: 0;
		padding-block: var(--context-list-item-padding-block);
		flex-direction: row;
		flex-grow: 1;
		align-items: center;
	}


	/* # Divider */

	.list-item__divider {
		display: var(--context-list-divider-display, block);
		position: absolute;
		inset-block-end: 0;
		inset-inline: 0;
		background-color: var(--semantics-dividers-color);
		height: var(--semantics-dividers-thickness);
	}

	:host([selected]) .list-item__divider,
	:host(.is-boxed.is-last) .list-item__divider {
		display: none;
	}
`;
    }
  });

  // node_modules/@nldd/design-system/dist/components/lists-and-tables/list-item/list-item.template.js
  var areas, template14;
  var init_list_item_template = __esm({
    "node_modules/@nldd/design-system/dist/components/lists-and-tables/list-item/list-item.template.js"() {
      init_lit();
      init_class_map2();
      areas = (showStart, showEnd) => b2`
	<div class=${e8({ "list-item__start-area": true, "is-visible": showStart })}>
		<slot name="start">
			<nldd-spacer-cell size="12"></nldd-spacer-cell>
		</slot>
	</div>
	<div class="list-item__main-area">
		<slot></slot>
		<div class="list-item__divider"></div>
	</div>
	<div class=${e8({ "list-item__end-area": true, "is-visible": showEnd })}>
		<slot name="end">
			<nldd-spacer-cell size="12"></nldd-spacer-cell>
		</slot>
	</div>
`;
      template14 = (type, href, showStart, showEnd) => {
        if (href) {
          return b2`<div class="list-item">
			<a class="list-item__action"
				href=${href}
			>${areas(showStart, showEnd)}</a>
		</div>`;
        }
        if (type === "button") {
          return b2`<div class="list-item">
			<button class="list-item__action"
				type="button"
			>${areas(showStart, showEnd)}</button>
		</div>`;
        }
        return b2`<div class="list-item">
		${areas(showStart, showEnd)}
	</div>`;
      };
    }
  });

  // node_modules/@nldd/design-system/dist/components/lists-and-tables/list-item/list-item.js
  var list_item_exports = {};
  __export(list_item_exports, {
    NLDDListItem: () => NLDDListItem
  });
  var import_meta22, __decorate70, NLDDListItem;
  var init_list_item = __esm({
    "node_modules/@nldd/design-system/dist/components/lists-and-tables/list-item/list-item.js"() {
      init_lit();
      init_decorators();
      init_list_item_styles();
      init_list_item_template();
      init_input_modality();
      init_spacer_cell();
      import_meta22 = {};
      __decorate70 = function(decorators, target, key, desc) {
        var c6 = arguments.length, r6 = c6 < 3 ? target : desc === null ? desc = Object.getOwnPropertyDescriptor(target, key) : desc, d3;
        if (typeof Reflect === "object" && typeof Reflect.decorate === "function") r6 = Reflect.decorate(decorators, target, key, desc);
        else for (var i8 = decorators.length - 1; i8 >= 0; i8--) if (d3 = decorators[i8]) r6 = (c6 < 3 ? d3(r6) : c6 > 3 ? d3(target, key, r6) : d3(target, key)) || r6;
        return c6 > 3 && r6 && Object.defineProperty(target, key, r6), r6;
      };
      NLDDListItem = class NLDDListItem2 extends i4 {
        constructor() {
          super(...arguments);
          this.size = "md";
          this.selected = false;
          this.reorderable = false;
          this._showStart = false;
          this._showEnd = false;
          this._parentType = "list";
          this._isBoxed = false;
          this._listObserver = null;
          this._handleClick = () => {
            this._action?.focus();
          };
          this._handleFocusIn = () => {
            this._action?.classList.toggle("is-pointer-focus", isPointerMode());
          };
          this._handleFocusOut = () => {
            this._action?.classList.remove("is-pointer-focus");
          };
        }
        connectedCallback() {
          super.connectedCallback();
          if (this.hasAttribute("data-nldd-clone"))
            return;
          this.setAttribute("role", "listitem");
          this.addEventListener("focusin", this._handleFocusIn);
          this.addEventListener("focusout", this._handleFocusOut);
          this.addEventListener("click", this._handleClick);
        }
        disconnectedCallback() {
          super.disconnectedCallback();
          this._listObserver?.disconnect();
          this._listObserver = null;
          this.removeEventListener("focusin", this._handleFocusIn);
          this.removeEventListener("focusout", this._handleFocusOut);
          this.removeEventListener("click", this._handleClick);
        }
        firstUpdated() {
          if (this.hasAttribute("data-nldd-clone")) {
            this._observeStartSlot();
            this._observeEndSlot();
            return;
          }
          this._syncWithList();
          this._observeStartSlot();
          this._observeEndSlot();
        }
        updated(changed) {
          if (changed.has("selected") || changed.has("type") || changed.has("href") || changed.has("_parentType")) {
            this._updateAriaState();
          }
        }
        /**
         * Syncs the item with the closest parent nldd-list (variant + type).
         * Called once in firstUpdated. If the item is moved to a different nldd-list
         * after first render, the MutationObserver will still watch the original list.
         * This is acceptable as moving items between lists is not a supported use case.
         */
        _syncWithList() {
          const list = this.closest("nldd-list");
          if (!list) {
            if (import_meta22.env?.DEV) {
              console.warn("nldd-list-item: no parent nldd-list found. Variant/type sync will not work if appended into a list after first render.");
            }
            return;
          }
          this._applyVariant(list.variant);
          this._applyParentType(list.type);
          this._listObserver = new MutationObserver(() => {
            this._applyVariant(list.variant);
            this._applyParentType(list.type);
          });
          this._listObserver.observe(list, {
            attributes: true,
            attributeFilter: ["variant", "type"]
          });
        }
        _applyVariant(variant) {
          this._isBoxed = variant === "box";
          this.classList.toggle("is-boxed", this._isBoxed);
          this._updateVisibility();
        }
        _applyParentType(type) {
          this._parentType = type;
        }
        _updateAriaState() {
          this.setAttribute("role", "listitem");
          const action = this.shadowRoot?.querySelector(".list-item__action");
          if (this._parentType === "navigation" && this.selected && action) {
            action.setAttribute("aria-current", "page");
          } else {
            action?.removeAttribute("aria-current");
          }
        }
        _updateVisibility() {
          const startSlot = this.shadowRoot?.querySelector('slot[name="start"]');
          const endSlot = this.shadowRoot?.querySelector('slot[name="end"]');
          this._showStart = this._isBoxed || (startSlot?.assignedElements().length ?? 0) > 0;
          this._showEnd = this._isBoxed || (endSlot?.assignedElements().length ?? 0) > 0;
        }
        _observeStartSlot() {
          const slot = this.shadowRoot?.querySelector('slot[name="start"]');
          slot?.addEventListener("slotchange", () => this._updateVisibility());
        }
        _observeEndSlot() {
          const slot = this.shadowRoot?.querySelector('slot[name="end"]');
          slot?.addEventListener("slotchange", () => this._updateVisibility());
        }
        /**
         * Delegates focus to the inner `.list-item__action` (the button or anchor),
         * so consumers can call `listItemEl.focus()` without reaching into shadow DOM.
         */
        focus(options) {
          this._action?.focus(options);
        }
        render() {
          return template14(this.type, this.href, this._showStart, this._showEnd);
        }
      };
      NLDDListItem.styles = [listItemStyles];
      __decorate70([
        n4({ reflect: true })
      ], NLDDListItem.prototype, "size", void 0);
      __decorate70([
        n4({ type: Boolean, reflect: true })
      ], NLDDListItem.prototype, "selected", void 0);
      __decorate70([
        n4({ reflect: true })
      ], NLDDListItem.prototype, "type", void 0);
      __decorate70([
        n4({ reflect: true })
      ], NLDDListItem.prototype, "href", void 0);
      __decorate70([
        n4({ type: Boolean, reflect: true })
      ], NLDDListItem.prototype, "reorderable", void 0);
      __decorate70([
        r5()
      ], NLDDListItem.prototype, "_showStart", void 0);
      __decorate70([
        r5()
      ], NLDDListItem.prototype, "_showEnd", void 0);
      __decorate70([
        r5()
      ], NLDDListItem.prototype, "_parentType", void 0);
      __decorate70([
        e5(".list-item__action")
      ], NLDDListItem.prototype, "_action", void 0);
      NLDDListItem = __decorate70([
        t3("nldd-list-item")
      ], NLDDListItem);
    }
  });

  // node_modules/@nldd/design-system/dist/components/navigation/top-title-bar/top-title-bar.styles.js
  var topTitleBarStyles;
  var init_top_title_bar_styles = __esm({
    "node_modules/@nldd/design-system/dist/components/navigation/top-title-bar/top-title-bar.styles.js"() {
      init_lit();
      init_slotted_reset();
      topTitleBarStyles = i`


	/* # Host */

	:host {
		${inheritedTextReset}
		display: block;
		width: 100%;
	}

	:host([hidden]) {
		display: none;
	}


	/* # Block */

	.top-title-bar {
		box-sizing: border-box;
		display: flex;
		width: 100%;
		padding-inline: var(--primitives-space-6);
		flex-direction: row;
		align-items: center;
	}


	/* # Elements */

	.top-title-bar__start {
		display: flex;
		min-width: 0;
		flex-direction: row;
		flex-grow: 1;
		flex-shrink: 1;
		flex-basis: 0;
		align-items: center;
	}

	.top-title-bar__end {
		display: flex;
		margin-top: var(--primitives-space-6);
		flex-direction: row;
		flex-grow: 0;
		flex-shrink: 0;
		align-items: center;
	}

	.top-title-bar__end[hidden] {
		display: none;
	}

	.top-title-bar__back-button {
		display: var(--context-back-button-display, flex);
		margin-top: var(--primitives-space-6);
		min-width: 0;
		flex-direction: row;
		align-items: center;
	}

	.top-title-bar__text-back-button {
		display: flex;
		min-width: 0;
	}

	:host(.is-compact) .top-title-bar__text-back-button {
		display: none;
	}

	.top-title-bar__icon-back-button {
		display: none;
	}

	:host(.is-compact) .top-title-bar__icon-back-button {
		display: flex;
	}

	.top-title-bar__divider {
		display: none;
		background-color: var(--components-top-title-bar-divider-color);
		width: var(--semantics-dividers-thickness);
		height: var(--primitives-space-24);
		flex-shrink: 0;
	}

	:host(.is-compact) .top-title-bar__divider {
		display: block;
	}

	.top-title-bar__title-group {
		display: none;
		margin-top: var(--primitives-space-6);
		min-width: 0;
		min-height: var(--semantics-controls-md-min-size);
		overflow: hidden;
		padding-inline: var(--primitives-space-10);
		flex-direction: column;
		flex-grow: 1;
		flex-shrink: 1;
		justify-content: center;
	}

	:host(.is-compact) .top-title-bar__title-group {
		display: flex;
	}

	.top-title-bar__title {
		margin: 0;
		overflow: hidden;
		color: var(--semantics-content-color);
		font: var(--primitives-font-body-lg-medium-flat);
		text-overflow: ellipsis;
		white-space: nowrap;
	}

	.top-title-bar__title:has(+ .top-title-bar__subtitle) {
		font: var(--primitives-font-body-md-medium-flat);
	}

	@media (forced-colors: active) {
		.top-title-bar__title {
			color: CanvasText;
		}
	}

	.top-title-bar__subtitle {
		margin: 0;
		overflow: hidden;
		color: var(--semantics-content-secondary-color);
		font: var(--primitives-font-body-xxs-regular-flat);
		text-overflow: ellipsis;
		white-space: nowrap;
	}

	.top-title-bar__dismiss-button {
		display: var(--context-dismiss-button-display, block);
	}
`;
    }
  });

  // node_modules/@nldd/design-system/dist/components/navigation/top-title-bar/top-title-bar.template.js
  function topTitleBarTemplate(component) {
    const showBack = !!component.backText;
    return b2`
		<div class="top-title-bar">
			<div class="top-title-bar__start">
				${showBack ? b2`
					<div class="top-title-bar__back-button">
						<div class="top-title-bar__text-back-button">
							<nldd-button
								variant="accent-transparent"
								start-icon="chevron-left"
								text=${component.backText}
								href=${component.backHref || A}
								single-line
								@click=${component._handleBack}
							></nldd-button>
						</div>
						<div class="top-title-bar__icon-back-button">
							<nldd-icon-button
								variant="accent-transparent"
								icon="chevron-left"
								text=${component.backText}
								accessible-label=${component.backText || A}
								href=${component.backHref || A}
								@click=${component._handleBack}
							></nldd-icon-button>
						</div>
						<div class="top-title-bar__divider"></div>
					</div>
				` : A}
				<div class="top-title-bar__title-group">
					<h1 class="top-title-bar__title">${component.text}</h1>
					${component.supportingText ? b2`
						<p class="top-title-bar__subtitle">${component.supportingText}</p>
					` : A}
				</div>
			</div>
			<div class="top-title-bar__end" ?hidden=${!component.dismissText && !component._hasToolbarItems}>
				<slot name="toolbar" @slotchange=${component._onToolbarSlotChange}></slot>
				${component.dismissText ? b2`
					<div class="top-title-bar__dismiss-button">
						<nldd-button
							variant="accent-transparent"
							text=${component.dismissText}
							@click=${component._handleDismiss}
						></nldd-button>
					</div>
				` : A}
			</div>
		</div>
	`;
  }
  var init_top_title_bar_template = __esm({
    "node_modules/@nldd/design-system/dist/components/navigation/top-title-bar/top-title-bar.template.js"() {
      init_lit();
      init_button();
      init_icon_button();
    }
  });

  // node_modules/@nldd/design-system/dist/components/navigation/top-title-bar/top-title-bar.js
  var top_title_bar_exports = {};
  __export(top_title_bar_exports, {
    NLDDTopTitleBar: () => NLDDTopTitleBar
  });
  var __decorate83, NLDDTopTitleBar;
  var init_top_title_bar = __esm({
    "node_modules/@nldd/design-system/dist/components/navigation/top-title-bar/top-title-bar.js"() {
      init_lit();
      init_decorators();
      init_top_title_bar_styles();
      init_top_title_bar_template();
      __decorate83 = function(decorators, target, key, desc) {
        var c6 = arguments.length, r6 = c6 < 3 ? target : desc === null ? desc = Object.getOwnPropertyDescriptor(target, key) : desc, d3;
        if (typeof Reflect === "object" && typeof Reflect.decorate === "function") r6 = Reflect.decorate(decorators, target, key, desc);
        else for (var i8 = decorators.length - 1; i8 >= 0; i8--) if (d3 = decorators[i8]) r6 = (c6 < 3 ? d3(r6) : c6 > 3 ? d3(target, key, r6) : d3(target, key)) || r6;
        return c6 > 3 && r6 && Object.defineProperty(target, key, r6), r6;
      };
      NLDDTopTitleBar = class NLDDTopTitleBar2 extends i4 {
        constructor() {
          super(...arguments);
          this.text = "";
          this.supportingText = "";
          this.collapseAnchor = "";
          this.backText = "";
          this.backHref = "";
          this.dismissText = "";
          this._hasToolbarItems = false;
          this._pageElement = null;
          this._anchorElement = null;
          this._activeScrollTarget = null;
          this._scrollTargetStyleObserver = null;
          this._boundOnScroll = this._onScroll.bind(this);
          this._onToolbarSlotChange = (e9) => {
            const slot = e9.target;
            this._hasToolbarItems = slot.assignedElements().length > 0;
          };
        }
        connectedCallback() {
          super.connectedCallback();
          this._connectPage();
          this._connectAnchor();
          this._updateAutoCompact();
        }
        disconnectedCallback() {
          super.disconnectedCallback();
          this._teardownAnchor();
        }
        updated(changed) {
          if (changed.has("collapseAnchor")) {
            this._teardownAnchor();
            if (this.collapseAnchor) {
              this._connectAnchor();
            }
            this._updateAutoCompact();
          } else if (changed.has("text")) {
            this._updateAutoCompact();
          }
        }
        // Without a scroll anchor the bar can't toggle compact state on scroll,
        // so we settle on a static state. With a `text` value we auto-compact
        // (so the title shows in the title-group); without `text` we stay
        // non-compact so the `back-text` button is visible. With an anchor the
        // scroll listener owns the class — this method is a no-op.
        _updateAutoCompact() {
          if (this.collapseAnchor)
            return;
          this.classList.toggle("is-compact", Boolean(this.text));
        }
        _connectPage() {
          let el = this;
          while (el) {
            if (el.tagName.toLowerCase() === "nldd-page") {
              this._pageElement = el;
              return;
            }
            el = el.parentElement ?? (el.getRootNode() instanceof ShadowRoot ? el.getRootNode().host : null);
          }
        }
        _connectAnchor() {
          if (!this.collapseAnchor)
            return;
          const root = this.getRootNode();
          this._anchorElement = root.getElementById?.(this.collapseAnchor) ?? root.querySelector(`#${this.collapseAnchor}`);
          if (!this._anchorElement)
            return;
          const page = this._pageElement;
          this._activeScrollTarget = page ? page.scrollTarget : window;
          this._activeScrollTarget.addEventListener("scroll", this._boundOnScroll, { passive: true });
          if (this._activeScrollTarget instanceof Element) {
            this._scrollTargetStyleObserver = new MutationObserver(() => this._onScroll());
            this._scrollTargetStyleObserver.observe(this._activeScrollTarget, {
              attributes: true,
              attributeFilter: ["style"]
            });
          }
          this.updateComplete.then(() => this._onScroll());
        }
        _teardownAnchor() {
          this._activeScrollTarget?.removeEventListener("scroll", this._boundOnScroll);
          this._activeScrollTarget = null;
          this._anchorElement = null;
          this._scrollTargetStyleObserver?.disconnect();
          this._scrollTargetStyleObserver = null;
        }
        _onScroll() {
          if (!this._anchorElement || !this._pageElement)
            return;
          const pageTop = this._pageElement.getBoundingClientRect().top;
          const anchorTop = this._anchorElement.getBoundingClientRect().top;
          this.classList.toggle("is-compact", anchorTop <= pageTop);
        }
        _handleBack() {
          if (this.backHref)
            return;
          this.dispatchEvent(new CustomEvent("back", { bubbles: true, composed: true }));
        }
        _handleDismiss() {
          this.dispatchEvent(new CustomEvent("dismiss", { bubbles: true, composed: true }));
        }
        render() {
          return topTitleBarTemplate(this);
        }
      };
      NLDDTopTitleBar.styles = topTitleBarStyles;
      __decorate83([
        n4({ type: String })
      ], NLDDTopTitleBar.prototype, "text", void 0);
      __decorate83([
        n4({ type: String, attribute: "supporting-text" })
      ], NLDDTopTitleBar.prototype, "supportingText", void 0);
      __decorate83([
        n4({ type: String, attribute: "collapse-anchor" })
      ], NLDDTopTitleBar.prototype, "collapseAnchor", void 0);
      __decorate83([
        n4({ type: String, attribute: "back-text" })
      ], NLDDTopTitleBar.prototype, "backText", void 0);
      __decorate83([
        n4({ type: String, attribute: "back-href" })
      ], NLDDTopTitleBar.prototype, "backHref", void 0);
      __decorate83([
        n4({ type: String, attribute: "dismiss-text" })
      ], NLDDTopTitleBar.prototype, "dismissText", void 0);
      __decorate83([
        r5()
      ], NLDDTopTitleBar.prototype, "_hasToolbarItems", void 0);
      NLDDTopTitleBar = __decorate83([
        t3("nldd-top-title-bar")
      ], NLDDTopTitleBar);
    }
  });

  // node_modules/@nldd/design-system/dist/components/index.js
  init_button();
  init_icon_button();

  // node_modules/@nldd/design-system/dist/components/actions/split-button/split-button.js
  init_lit();
  init_decorators();

  // node_modules/@nldd/design-system/dist/components/actions/split-button/split-button.styles.js
  init_lit();
  init_slotted_reset();
  var splitButtonStyles = i`


	/* # Host */

	:host {
		--_corner-radius: var(--semantics-controls-md-corner-radius);
		--_background-color: var(--semantics-buttons-neutral-tinted-background-color);
		--_divider-color: var(--semantics-buttons-neutral-tinted-divider-color);
		--_highlight-border-color: var(--semantics-buttons-neutral-tinted-highlight-border-color);
		--_divider-length: var(--semantics-buttons-md-divider-length);
		--_width: auto;

		${inheritedTextReset}
		display: inline-flex;
		isolation: isolate;
		-webkit-tap-highlight-color: transparent;
	}

	:host([size="xs"]) {
		--_corner-radius: var(--semantics-controls-xs-corner-radius);
		--_divider-length: var(--semantics-buttons-xs-divider-length);
	}

	:host([size="sm"]) {
		--_corner-radius: var(--semantics-controls-sm-corner-radius);
		--_divider-length: var(--semantics-buttons-sm-divider-length);
	}

	:host([size="lg"]) {
		--_corner-radius: var(--semantics-controls-lg-corner-radius);
		--_divider-length: var(--semantics-buttons-lg-divider-length);
	}

	/* ## Accent Filled (Primary) */

	:host([variant="accent-filled"]),
	:host([variant="primary"]) {
		--_background-color: var(--semantics-buttons-accent-filled-background-color);
		--_divider-color: var(--semantics-buttons-accent-filled-divider-color);
		--_highlight-border-color: var(--semantics-buttons-accent-filled-highlight-border-color);
	}

	:host([variant="neutral-base"]) {
		--_background-color: var(--semantics-buttons-neutral-base-background-color);
		--_divider-color: var(--semantics-buttons-neutral-base-divider-color);
		--_highlight-border-color: var(--semantics-buttons-neutral-base-highlight-border-color);
	}

	:host([width="full"]) {
		display: block;
		width: 100%;
	}

	:host([hidden]) {
		display: none;
	}

	:host([disabled]) {
		opacity: var(--primitives-opacity-disabled);
		pointer-events: none;
	}

	:host([disabled]) nldd-button,
	:host([disabled]) nldd-icon-button {
		opacity: 1;
	}


	/* # Block */

	.split-button {
		display: inline-flex;
		position: relative;
		width: var(--_width);
		min-width: fit-content;
		border-radius: var(--_corner-radius);
		background-color: var(--_background-color);
		flex-direction: row;
		align-items: center;
	}

	.split-button::after {
		content: '';
		position: absolute;
		inset: 0;
		border-radius: inherit;
		box-shadow: inset 0 0 0 var(--primitives-border-width-thin) var(--_highlight-border-color);
		pointer-events: none;
	}


	/* # Elements */

	.split-button__divider {
		background-color: var(--_divider-color);
		width: 1px;
		height: var(--_divider-length);
		flex-shrink: 0;
	}

	.split-button__popup-button {
		display: flex;
		flex-shrink: 0;
	}

	nldd-button:focus-within,
	nldd-icon-button:focus-within {
		position: relative;
		z-index: 1;
	}
`;

  // node_modules/@nldd/design-system/dist/components/actions/split-button/split-button.template.js
  init_lit();
  function template4() {
    return b2`
		<div class="split-button">
			<nldd-button
				variant=${this.variant}
				size=${this.size}
				text=${this.text}
				start-icon=${this.icon || A}
				horizontal-alignment="left"
				width="full"
				no-highlight-border
				?disabled=${this.disabled}
				@click=${this._handleActionClick}
			></nldd-button>
			<div class="split-button__divider"></div>
			<div class="split-button__popup-button">
				<nldd-icon-button
					variant=${this.variant}
					size=${this.size}
					icon="chevron-down-small"
					text=${this._t("components.split-button.menu-action")}
					tooltip-timing="never"
					no-highlight-border
					hide-lg-text
					?disabled=${this.disabled}
					?expanded=${this._menuIsOpen}
					popup-type="menu"
					@click=${this._handleMenuClick}
				></nldd-icon-button>
			</div>
		</div>
		<slot @slotchange=${this._handleSlotChange}></slot>
	`;
  }

  // node_modules/@nldd/design-system/dist/components/actions/split-button/split-button.i18n.js
  var nlddSplitButtonTranslations = {
    "components.split-button.menu-action": "Meer opties"
  };

  // node_modules/@nldd/design-system/dist/components/actions/split-button/split-button.js
  init_button();
  init_icon_button();

  // node_modules/@nldd/design-system/dist/components/actions/menu/menu.js
  init_lit();
  init_decorators();
  init_floating_ui_dom();

  // node_modules/@nldd/design-system/dist/components/actions/menu/menu.styles.js
  init_lit();
  init_slotted_reset();
  var menuStyles = i`


	/* # Host */

	:host {
		--_viewport-margin: var(--primitives-space-16);
		--_width: var(--primitives-area-280);
		--_max-height: calc(infinity * 1px);
		--_max-items: 9999;
		--_padding: var(--primitives-space-8);
		--_item-size: var(--semantics-controls-md-min-size);
		--_item-background-color: transparent;
		--_item-is-highlighted-background-color: var(--components-menu-item-is-highlighted-background-color);
		--_item-is-highlighted-content-color: var(--components-menu-item-is-highlighted-content-color);

		@media (pointer: fine) {
			--_padding: var(--primitives-space-6);
			--_item-size: var(--semantics-controls-sm-min-size);
		}

		${inheritedTextReset}
		display: block;
		position: absolute;
		margin: 0;
		border: none;
		background: transparent;
		overflow: visible;
		padding: 0;
		user-select: none;
		-webkit-tap-highlight-color: transparent;
	}

	:host(:not(:popover-open)) {
		display: none;
	}

	:host([scroll-active]) {
		--_item-is-highlighted-background-color: transparent;
		--_item-is-highlighted-content-color: initial;
	}


	/* # Block */

	.menu {
		display: flex;
		box-sizing: border-box;
		border-radius: var(--semantics-overlays-corner-radius);
		box-shadow: var(--components-menu-box-shadow);
		background: var(--semantics-surfaces-base-background-color);
		width: var(--_width);
		padding: var(--_padding);
		flex-direction: column;
		max-height: min(var(--_max-height), calc(var(--_max-items) * var(--_item-size) + var(--_padding) * 2));
		outline: none;
		overflow-y: auto;
	}

	.menu:focus-visible:not(.is-pointer-focus) {
		box-shadow: var(--semantics-focus-ring-box-shadow), var(--components-menu-box-shadow);
		outline: var(--semantics-focus-ring-outline);
		outline-offset: var(--semantics-focus-ring-outline-offset);
	}


	/* # Empty */

	.menu__empty {
		padding: var(--primitives-space-8);
	}


	/* # Back button — drill-in mode header */

	.menu__back-button {
		display: flex;
		box-sizing: border-box;
		border: none;
		border-radius: var(--semantics-controls-md-corner-radius);
		background: transparent;
		width: 100%;
		min-height: var(--_item-size);
		padding: var(--primitives-space-8);
		flex-direction: row;
		align-items: center;
		text-align: start;
		appearance: none;
		--context-cell-content-color: var(--semantics-content-secondary-color);

		@media (pointer: fine) {
			border-radius: var(--semantics-controls-sm-corner-radius);
			padding: var(--primitives-space-4) var(--primitives-space-8);
		}
	}

	/* Hover gated to hover-capable pointers: touch's sticky :hover would
	 * otherwise leave the back button highlighted after opening a submenu. */

	@media (hover: hover) {
		.menu__back-button:hover {
			background-color: var(--_item-is-highlighted-background-color);
			--context-cell-content-color: var(--_item-is-highlighted-content-color);
		}
	}

	.menu__back-button:active:hover {
		background-color: var(--_item-is-highlighted-background-color);
		--context-cell-content-color: var(--_item-is-highlighted-content-color);
	}

	.menu__back-button:focus-visible {
		position: relative;
		z-index: 1;
		outline: var(--semantics-focus-ring-outline);
		outline-offset: var(--semantics-focus-ring-outline-offset);
		box-shadow: var(--semantics-focus-ring-box-shadow);
	}

	.menu__back-button-divider {
		margin: var(--primitives-space-4) 0;
		background-color: var(--semantics-dividers-color);
		height: var(--semantics-dividers-thickness);
	}


	/* # Live region — drill-in view-change announcements (WCAG 4.1.3)
	 *
	 * Sibling of .menu so the role="status" doesn't violate menu's
	 * required-owned-children. Visually hidden, kept in the a11y tree. */

	.menu__live-region {
		position: absolute;
		margin: -1px;
		border: 0;
		width: 1px;
		height: 1px;
		overflow: hidden;
		padding: 0;
		white-space: nowrap;
		clip-path: inset(50%);
	}
`;
  var menuItemStyles = i`


	/* # Host */

	:host {
		${inheritedTextReset}
		display: block;
		-webkit-tap-highlight-color: transparent;
	}

	:host([hidden]) {
		display: none;
	}

	/* ## Open submenu opener
	 *
	 * Lighter neutral bg while the cursor is in the submenu — the active
	 * item is in the submenu, the opener just marks the branch. The
	 * highlighted/hover rule below upgrades back to the bold accent when
	 * the cursor returns to the opener. */

	.menu__item[aria-expanded="true"] {
		--_item-background-color: var(--components-menu-item-is-expanded-background-color);
		--context-cell-content-color: var(--components-menu-item-is-expanded-content-color);
		--context-cell-content-secondary-color: var(--components-menu-item-is-expanded-content-color);
	}

	/* ## Highlighted or pressed
	 *
	 * :hover on [aria-expanded="true"] covers the cursor-on-open-opener
	 * case where [highlighted] has been cleared by submenu-open.
	 * :active:hover is the press flash on touch (neutralised during
	 * scroll via --_item-is-highlighted-* on :host([scroll-active])). */

	:host([highlighted]) .menu__item,
	.menu__item[aria-expanded="true"]:hover,
	.menu__item:active:hover {
		--_item-background-color: var(--_item-is-highlighted-background-color);
		--context-cell-content-color: var(--_item-is-highlighted-content-color);
		--context-cell-content-secondary-color: var(--_item-is-highlighted-content-color);
	}

	/* ## Destructive */

	:host([destructive]) {
		--_item-is-highlighted-background-color: var(--components-menu-item-is-destructive-is-highlighted-background-color);
		--_item-is-highlighted-content-color: var(--components-menu-item-is-destructive-is-highlighted-content-color);
		--context-cell-content-color: var(--components-menu-item-is-destructive-content-color);
		--context-cell-content-secondary-color: var(--components-menu-item-is-destructive-content-color);
	}


	/* # Elements */

	.menu__item {
		display: flex;
		box-sizing: border-box;
		border: none;
		border-radius: var(--semantics-controls-md-corner-radius);
		background: var(--_item-background-color);
		width: 100%;
		min-height: var(--_item-size);
		padding: var(--primitives-space-8);
		flex-direction: row;
		align-items: center;
		text-align: start;
		appearance: none;
		@media (pointer: fine) {
			border-radius: var(--semantics-controls-sm-corner-radius);
			padding: var(--primitives-space-4) var(--primitives-space-8);
		}
	}

	.menu__item:focus-visible {
		position: relative;
		z-index: 1;
		outline: var(--semantics-focus-ring-outline);
		outline-offset: var(--semantics-focus-ring-outline-offset);
		box-shadow: var(--semantics-focus-ring-box-shadow);
	}

	:host([disabled]) .menu__item {
		opacity: var(--primitives-opacity-disabled);
		pointer-events: none;
	}


	@media (prefers-reduced-motion: reduce) {
		.menu__item {
			transition: none;
		}
	}


	@media (forced-colors: active) {
		:host([highlighted]) .menu__item,
		.menu__item:hover,
		.menu__item:focus-visible {
			background-color: Highlight;
			color: HighlightText;
		}
	}
`;
  var menuDividerStyles = i`


	/* # Host */

	:host {
		display: block;
		padding: var(--primitives-space-4) 0;
	}

	:host([hidden]) {
		display: none;
	}


	/* # Divider */

	.menu__divider {
		background-color: var(--semantics-dividers-color);
		height: var(--semantics-dividers-thickness);
	}
`;
  var menuGroupStyles = i`


	/* # Host
	 *
	 * Auto top/bottom dividers via border-top/-bottom; suppressed for the
	 * first child (top) and when followed by another group or last child
	 * (bottom — flag set by parent menu via data-no-bottom-divider since
	 * :last-child can't see hidden siblings).
	 *
	 * padding-top > padding-bottom binds the title visually to its items
	 * rather than to whatever sits above. */

	:host {
		${inheritedTextReset}
		display: block;
		margin-top: var(--primitives-space-4);
		margin-bottom: var(--primitives-space-4);
		border-top: var(--semantics-dividers-thickness) solid var(--semantics-dividers-color);
		border-bottom: var(--semantics-dividers-thickness) solid var(--semantics-dividers-color);
		padding-top: var(--primitives-space-6);
		padding-bottom: var(--primitives-space-4);
	}

	:host(:first-child) {
		border-top: none;
		padding-top: var(--primitives-space-2);
	}

	:host([data-no-bottom-divider]) {
		margin-bottom: 0;
		border-bottom: none;
		padding-bottom: var(--primitives-space-2);
	}

	:host([hidden]) {
		display: none;
	}


	/* # Title */

	.menu-group__title {
		margin: 0;
		padding-top: 0;
		padding-right: var(--primitives-space-8);
		padding-bottom: var(--primitives-space-1);
		padding-left: var(--primitives-space-4);
		font: var(--primitives-font-body-sm-regular-tight);
		color: var(--semantics-content-secondary-color);
	}


	/* # Items wrapper
	 *
	 * display: block keeps the role="group" wrapper as a real box so the
	 * a11y tree exposes role + aria-labelledby reliably (display: contents
	 * has historical a11y-tree bugs in older WebKit/Chromium). */

	.menu-group__items {
		display: block;
	}
`;

  // node_modules/@nldd/design-system/dist/components/actions/menu/menu.template.js
  init_lit();
  var menuRoleMap = {
    menu: "menu",
    listbox: "listbox"
  };
  var itemRoleMap = {
    button: { menu: "menuitem", listbox: "option" },
    checkbox: { menu: "menuitemcheckbox", listbox: "option" },
    radio: { menu: "menuitemradio", listbox: "option" }
  };
  function menuTemplate(isEmpty, variant) {
    const menuRole = isEmpty ? A : menuRoleMap[variant];
    const showBack = this._isSubmenu && this._drillInMode && this._parentItem !== null;
    return b2`
		<div class="menu"
			role=${menuRole}
			tabindex="-1"
			@touchstart=${this._handleMenuTouchStart}
			@touchmove=${this._handleMenuTouchMove}
			@touchend=${this._handleMenuTouchEnd}
			@touchcancel=${this._handleMenuTouchEnd}
		>
			${showBack ? b2`
				<button class="menu__back-button"
					type="button"
					aria-label=${this._resolvedBackLabel}
					@click=${this._handleBack}
					@mouseenter=${this._handleBackMouseenter}
				>
					<nldd-icon-cell class="menu__back-icon" size="20" icon="chevron-left"></nldd-icon-cell>
					<nldd-spacer-cell size="6"></nldd-spacer-cell>
					<nldd-text-cell text=${this._parentItem.text}></nldd-text-cell>
				</button>
				<!-- Pure visual divider; role="none" keeps strict ARIA validators
				     quiet (a focusable role="separator" inside a menu would need
				     aria-valuenow et al.; this one is decorative). -->
				<div class="menu__back-button-divider" role="none"></div>
			` : A}
			<slot></slot>
			${isEmpty ? b2`
				<div class="menu__empty">
					<slot name="empty">
						<nldd-inline-dialog
							text=${this._resolvedEmptyText}
							supporting-text=${this.emptySupportingText || A}
						></nldd-inline-dialog>
					</slot>
				</div>
			` : A}
		</div>
		<!-- Drill-in view-change announcer (WCAG 4.1.3). Sibling of .menu so
		     it sits outside role="menu"'s required-children set. Updated via
		     _announce(); empty and inert until a drill-in transition. -->
		<div class="menu__live-region"
			role="status"
			aria-live="polite"
			aria-atomic="true"
		></div>
	`;
  }
  function menuItemTemplate(variant = "menu") {
    const hasCheckState = this.type !== "button" && variant === "menu";
    const role = itemRoleMap[this.type][variant];
    const hasSubmenu = this._hasSubmenu;
    return b2`
		<button class="menu__item"
			type="button"
			role=${role}
			?disabled=${this.disabled}
			aria-checked=${hasCheckState ? String(this.selected) : A}
			aria-selected=${variant === "listbox" ? String(this.selected) : A}
			aria-haspopup=${hasSubmenu ? "menu" : A}
			aria-expanded=${hasSubmenu ? String(this._submenuOpen) : A}
			aria-controls=${hasSubmenu && this._submenuEl?.id ? this._submenuEl.id : A}
			.popoverTargetElement=${this._submenuEl}
			@click=${this._handleClick}
		>
			${hasCheckState ? b2`
				<nldd-icon-cell class="menu__item-check"
					size="24"
					horizontal-alignment="center"
					icon=${this.selected ? "check-mark" : A}
				></nldd-icon-cell>
				<nldd-spacer-cell size="4"></nldd-spacer-cell>
			` : A}
			${this.icon ? b2`
				<nldd-icon-cell class="menu__item-icon" size="20" icon=${this.icon}></nldd-icon-cell>
				<nldd-spacer-cell size="8"></nldd-spacer-cell>
			` : A}
			<nldd-text-cell class="menu__item-text" text=${this.text} query=${this.query} query-mark-mode=${this.queryMarkMode}></nldd-text-cell>
			${this.details ? b2`
				<nldd-spacer-cell size="8"></nldd-spacer-cell>
				<nldd-text-cell class="menu__item-details"
					width="fit-content"
					horizontal-alignment="right"
					color="secondary"
					text=${this.details}
				></nldd-text-cell>
			` : A}
			${hasSubmenu ? b2`
				<nldd-spacer-cell size="6"></nldd-spacer-cell>
				<nldd-icon-cell class="menu__item-submenu-indicator"
					size="20"
					icon="chevron-right"
				></nldd-icon-cell>
			` : A}
		</button>
		<!--
			Project the slotted nldd-menu (the submenu, if any) into the flat
			tree. Without this slot, a light-DOM child nldd-menu sits outside
			any flat tree — and the Popover API uses the flat tree to locate
			a popover's ancestor popover. Without an ancestor, calling
			showPopover() on the submenu would dismiss the parent menu (its
			DOM ancestor) instead of stacking on top of it. The slot itself
			has no visible effect because the submenu is display:none until
			it opens its own popover.
		-->
		<slot></slot>
	`;
  }
  function menuDividerTemplate() {
    return b2`<div class="menu__divider" role="separator"></div>`;
  }
  function menuGroupTemplate(component) {
    return b2`
		<div class="menu-group__title"
			id=${component._titleId}
			aria-hidden="true"
		>${component.text}</div>
		<div class="menu-group__items"
			role="group"
			aria-labelledby=${component._titleId}
		>
			<slot></slot>
		</div>
	`;
  }

  // node_modules/@nldd/design-system/dist/components/actions/menu/menu.i18n.js
  var nlddMenuTranslations = {
    "components.menu.empty-text": "Geen opties beschikbaar",
    "components.menu.back": "Terug",
    "components.menu.submenu-title": "Submenu: {title}",
    "components.menu.submenu-back-action": "Terug naar {title}"
  };

  // node_modules/@nldd/design-system/dist/components/lists-and-tables/cells/icon-cell/icon-cell.js
  init_lit();
  init_decorators();

  // node_modules/@nldd/design-system/dist/components/lists-and-tables/cells/icon-cell/icon-cell.styles.js
  init_lit();
  var iconCellStyles = i`


	/* # Host */

	:host {
		--_size: var(--primitives-space-24);
		--_content-color: var(--context-cell-content-color, var(--semantics-content-color));

		display: flex;
		width: var(--_size);
		flex-direction: column;
		align-items: center;
		color: var(--_content-color);
	}

	:host([hidden]) {
		display: none;
	}

	:host([size="16"]) {
		--_size: var(--primitives-space-16);
	}

	:host([size="20"]) {
		--_size: var(--primitives-space-20);
	}

	:host([size="32"]) {
		--_size: var(--primitives-space-32);
	}

	/* ## Color */

	:host([color="secondary"]) {
		--_content-color: var(--context-cell-content-secondary-color, var(--semantics-content-secondary-color));
	}

	:host([color="accent"]) {
		--_content-color: var(--context-cell-content-accent-color, var(--semantics-content-accent-color));
	}

	:host([color="success"]) {
		--_content-color: var(--context-cell-content-success-color, var(--semantics-content-success-color));
	}

	:host([color="warning"]) {
		--_content-color: var(--context-cell-content-warning-color, var(--semantics-content-warning-color));
	}

	:host([color="critical"]) {
		--_content-color: var(--context-cell-content-critical-color, var(--semantics-content-critical-color));
	}


	/* # Vertical alignment */

	:host([vertical-alignment="center"]),
	:host(:not([vertical-alignment])) {
		align-self: stretch;
		justify-content: center;
	}

	:host([vertical-alignment="top"]) {
		align-self: flex-start;
		justify-content: flex-start;
	}

	:host([vertical-alignment="bottom"]) {
		align-self: flex-end;
		justify-content: flex-end;
	}


	/* # Elements */

	/* No width/height here: a slotted nldd-icon defines its own --_size,
	   which shadows this cell's --_size in ::slotted var() resolution
	   (custom props resolve against the slotted element). The icon sizes
	   itself — width fills the cell, height auto keeps it square. */
	::slotted(*) {
		display: block;
		flex-shrink: 0;
	}
`;

  // node_modules/@nldd/design-system/dist/components/lists-and-tables/cells/icon-cell/icon-cell.template.js
  init_lit();
  function template5() {
    return b2`
		${this.icon ? b2`<nldd-icon name=${this.icon}></nldd-icon>` : b2`<slot></slot>`}
	`;
  }

  // node_modules/@nldd/design-system/dist/components/lists-and-tables/cells/icon-cell/icon-cell.js
  init_visibility_mixin();
  init_icon();
  var __decorate7 = function(decorators, target, key, desc) {
    var c6 = arguments.length, r6 = c6 < 3 ? target : desc === null ? desc = Object.getOwnPropertyDescriptor(target, key) : desc, d3;
    if (typeof Reflect === "object" && typeof Reflect.decorate === "function") r6 = Reflect.decorate(decorators, target, key, desc);
    else for (var i8 = decorators.length - 1; i8 >= 0; i8--) if (d3 = decorators[i8]) r6 = (c6 < 3 ? d3(r6) : c6 > 3 ? d3(target, key, r6) : d3(target, key)) || r6;
    return c6 > 3 && r6 && Object.defineProperty(target, key, r6), r6;
  };
  var NLDDIconCell = class NLDDIconCell2 extends VisibilityMixin(i4, "cells-container") {
    constructor() {
      super(...arguments);
      this.verticalAlignment = "center";
      this.size = "24";
      this.color = "default";
      this.icon = "";
    }
    render() {
      return template5.call(this);
    }
  };
  NLDDIconCell.styles = iconCellStyles;
  __decorate7([
    n4({ type: String, reflect: true, attribute: "vertical-alignment" })
  ], NLDDIconCell.prototype, "verticalAlignment", void 0);
  __decorate7([
    n4({ type: String, reflect: true })
  ], NLDDIconCell.prototype, "size", void 0);
  __decorate7([
    n4({ type: String, reflect: true })
  ], NLDDIconCell.prototype, "color", void 0);
  __decorate7([
    n4({ type: String, reflect: true })
  ], NLDDIconCell.prototype, "icon", void 0);
  NLDDIconCell = __decorate7([
    t3("nldd-icon-cell")
  ], NLDDIconCell);

  // node_modules/@nldd/design-system/dist/components/actions/menu/menu.js
  init_spacer_cell();
  init_text_cell();
  init_icon();
  init_inline_dialog();
  init_input_modality();
  init_breakpoints();
  var __decorate12 = function(decorators, target, key, desc) {
    var c6 = arguments.length, r6 = c6 < 3 ? target : desc === null ? desc = Object.getOwnPropertyDescriptor(target, key) : desc, d3;
    if (typeof Reflect === "object" && typeof Reflect.decorate === "function") r6 = Reflect.decorate(decorators, target, key, desc);
    else for (var i8 = decorators.length - 1; i8 >= 0; i8--) if (d3 = decorators[i8]) r6 = (c6 < 3 ? d3(r6) : c6 > 3 ? d3(target, key, r6) : d3(target, key)) || r6;
    return c6 > 3 && r6 && Object.defineProperty(target, key, r6), r6;
  };
  var NLDDMenuDivider = class extends i4 {
    render() {
      return menuDividerTemplate();
    }
  };
  NLDDMenuDivider.styles = menuDividerStyles;
  if (!customElements.get("nldd-menu-divider")) {
    customElements.define("nldd-menu-divider", NLDDMenuDivider);
  }
  var NLDDMenuGroup = class _NLDDMenuGroup extends i4 {
    constructor() {
      super(...arguments);
      this.text = "";
      this._titleId = `nldd-menu-group-title-${_NLDDMenuGroup._idCounter++}`;
    }
    render() {
      return menuGroupTemplate(this);
    }
  };
  NLDDMenuGroup.styles = menuGroupStyles;
  NLDDMenuGroup._idCounter = 0;
  __decorate12([
    n4({ type: String, reflect: true })
  ], NLDDMenuGroup.prototype, "text", void 0);
  if (!customElements.get("nldd-menu-group")) {
    customElements.define("nldd-menu-group", NLDDMenuGroup);
  }
  var NLDDMenuItem = class _NLDDMenuItem extends i4 {
    constructor() {
      super(...arguments);
      this.text = "";
      this.value = "";
      this.aliases = "";
      this.details = "";
      this.icon = "";
      this.type = "button";
      this.selected = false;
      this.destructive = false;
      this.disabled = false;
      this.query = "";
      this.queryMarkMode = "predictive";
      this.menuVariant = "menu";
      this._submenuOpen = false;
      this._cachedSubmenuEl = null;
      this._submenuMutationObserver = null;
    }
    connectedCallback() {
      super.connectedCallback();
      if (!this.id) {
        this.id = `nldd-menu-item-${_NLDDMenuItem._idCounter++}`;
      }
      this.addEventListener("focusin", (e9) => {
        const realTarget = e9.composedPath()[0];
        if (!realTarget || !this.shadowRoot?.contains(realTarget))
          return;
        this.setAttribute("data-focused", "");
        this.dispatchEvent(new CustomEvent("menu-item-focused", {
          bubbles: true,
          composed: true
        }));
      });
      this.addEventListener("focusout", (e9) => {
        const realTarget = e9.composedPath()[0];
        if (!realTarget || !this.shadowRoot?.contains(realTarget))
          return;
        this.removeAttribute("data-focused");
      });
    }
    focus(options) {
      const focusable = this.shadowRoot?.querySelector("button, a");
      focusable?.focus(options);
    }
    get _submenuEl() {
      return this._cachedSubmenuEl;
    }
    get _hasSubmenu() {
      return this._cachedSubmenuEl !== null;
    }
    firstUpdated() {
      this._cachedSubmenuEl = this.querySelector(":scope > nldd-menu");
      if (this._cachedSubmenuEl !== null) {
        this.requestUpdate();
      }
      this._submenuMutationObserver = new MutationObserver(() => {
        const current = this.querySelector(":scope > nldd-menu");
        if (current && current !== this._cachedSubmenuEl) {
          console.warn("[nldd-menu-item] A nldd-menu child was added after mount. Submenu attachment is resolved once at firstUpdated in v1, so this menu will not be treated as a submenu. Define the nldd-menu child before the item is connected to the DOM.");
        }
      });
      this._submenuMutationObserver.observe(this, { childList: true });
    }
    disconnectedCallback() {
      super.disconnectedCallback();
      this._submenuMutationObserver?.disconnect();
      this._submenuMutationObserver = null;
    }
    _handleClick(event) {
      if (this.disabled)
        return;
      if (this._hasSubmenu) {
        event?.preventDefault();
        if (this._submenuOpen)
          return;
        this.dispatchEvent(new CustomEvent("submenu-open", {
          detail: { submenu: this._submenuEl, item: this },
          bubbles: true,
          composed: false
        }));
        return;
      }
      this.dispatchEvent(new CustomEvent("select", {
        bubbles: true,
        composed: true
      }));
      this.closest("nldd-menu")?.hidePopover?.();
    }
    /** Programmatically select this item. */
    select() {
      this._handleClick();
    }
    render() {
      return menuItemTemplate.call(this, this.menuVariant);
    }
  };
  NLDDMenuItem.styles = menuItemStyles;
  NLDDMenuItem._idCounter = 0;
  __decorate12([
    n4({ type: String, reflect: true })
  ], NLDDMenuItem.prototype, "text", void 0);
  __decorate12([
    n4({ type: String, reflect: true })
  ], NLDDMenuItem.prototype, "value", void 0);
  __decorate12([
    n4({ type: String, reflect: true })
  ], NLDDMenuItem.prototype, "aliases", void 0);
  __decorate12([
    n4({ type: String, reflect: true })
  ], NLDDMenuItem.prototype, "details", void 0);
  __decorate12([
    n4({ type: String, reflect: true })
  ], NLDDMenuItem.prototype, "icon", void 0);
  __decorate12([
    n4({ type: String, reflect: true })
  ], NLDDMenuItem.prototype, "type", void 0);
  __decorate12([
    n4({ type: Boolean, reflect: true })
  ], NLDDMenuItem.prototype, "selected", void 0);
  __decorate12([
    n4({ type: Boolean, reflect: true })
  ], NLDDMenuItem.prototype, "destructive", void 0);
  __decorate12([
    n4({ type: Boolean, reflect: true })
  ], NLDDMenuItem.prototype, "disabled", void 0);
  __decorate12([
    n4({ type: String, reflect: true })
  ], NLDDMenuItem.prototype, "query", void 0);
  __decorate12([
    n4({ type: String, reflect: true, attribute: "query-mark-mode" })
  ], NLDDMenuItem.prototype, "queryMarkMode", void 0);
  __decorate12([
    r5()
  ], NLDDMenuItem.prototype, "menuVariant", void 0);
  __decorate12([
    r5()
  ], NLDDMenuItem.prototype, "_submenuOpen", void 0);
  if (!customElements.get("nldd-menu-item")) {
    customElements.define("nldd-menu-item", NLDDMenuItem);
  }
  var defaultFilterFn = (query, item) => {
    const q = query.toLowerCase();
    const textMatch = item.text.toLowerCase().includes(q);
    const valueMatch = item.value !== "" && item.value.toLowerCase().includes(q);
    const aliasesMatch = item.aliases !== "" && item.aliases.split(" ").some((s6) => s6.toLowerCase().includes(q));
    return textMatch || valueMatch || aliasesMatch;
  };
  var NLDDMenu = class _NLDDMenu extends i4 {
    constructor() {
      super(...arguments);
      this.anchor = "";
      this.anchorElement = null;
      this.placement = "bottom-start";
      this.variant = "menu";
      this.emptyText = "";
      this.emptySupportingText = "";
      this.width = "";
      this.maxItems = 0;
      this.translations = {};
      this.debugSafeTriangle = false;
      this.filterFn = defaultFilterFn;
      this._isEmpty = false;
      this._activeSubmenu = null;
      this._activeSubmenuOpener = null;
      this._activeSubmenuCleanup = null;
      this._drillInHidingForDeeper = false;
      this._openChain = [];
      this._chainRoot = null;
      this._collapseSelf = null;
      this._parentMenu = null;
      this._parentItem = null;
      this._isOpen = false;
      this._collapsedByPointerGesture = false;
      this._cleanupAutoUpdate = null;
      this._announceRaf = 0;
      this._pendingAnnouncement = "";
      this._handleDocumentClick = (event) => {
        if (this.anchorElement)
          return;
        const collapsedByGesture = this._collapsedByPointerGesture;
        this._collapsedByPointerGesture = false;
        const anchorEl = this._getAnchorEl();
        if (!anchorEl)
          return;
        const path = event.composedPath();
        if (!path.includes(anchorEl))
          return;
        if (collapsedByGesture)
          return;
        if (this._drillInMode && (this._isOpen || this._openChain.length > 0)) {
          this._collapseChain();
          return;
        }
        if (this._isOpen) {
          this.hidePopover();
        } else {
          this.showPopover();
        }
      };
      this._hoverOpenTimer = null;
      this._safeTriangleListener = null;
      this._safeTriangleStallTimer = null;
      this._lastCursorPos = null;
      this._safeTriangleApex = null;
      this._safeTriangleSubmenu = null;
      this._movingTowardSubmenu = false;
      this._handleMenuItemMouseenter = (event) => {
        if (isTouchMode())
          return;
        const item = event.target.closest("nldd-menu-item");
        if (!item)
          return;
        this._activateItem(item);
      };
      this._handleMouseleave = () => {
        if (this.variant !== "listbox")
          this._clearHighlight();
        this._cancelHoverOpen();
      };
      this._safeTriangleOverlay = null;
      this._handleMenuItemFocused = (event) => {
        const item = event.target.closest("nldd-menu-item");
        if (!item || item.disabled || item.hasAttribute("hidden"))
          return;
        if (item.closest("nldd-menu") !== this)
          return;
        this._setHighlight(item);
      };
      this._handleSubmenuOpen = (event) => {
        const { item, submenu } = event.detail;
        if (item.closest("nldd-menu") !== this)
          return;
        event.stopPropagation();
        if (this._activeSubmenu === submenu)
          return;
        if (this._activeSubmenu && this._activeSubmenu !== submenu) {
          this._activeSubmenu.hidePopover?.();
        }
        submenu._parentMenu = this;
        submenu._parentItem = item;
        const chainRoot = this._rootMenu;
        submenu._chainRoot = chainRoot;
        if (!chainRoot._openChain.includes(submenu))
          chainRoot._openChain.push(submenu);
        if (this._drillInMode) {
          const root = this._rootMenu;
          submenu.anchorElement = root._getAnchorEl();
          submenu.placement = root.placement;
        } else {
          submenu.anchorElement = item;
          submenu.placement = "right-start";
        }
        this._activeSubmenu = submenu;
        this._activeSubmenuOpener = item;
        item._submenuOpen = true;
        item.removeAttribute("highlighted");
        const wasDrillIn = this._drillInMode;
        const cleanup = (skipReshow) => {
          submenu.removeEventListener("toggle", onToggle);
          if (this._activeSubmenu === submenu) {
            this._activeSubmenu = null;
            this._activeSubmenuOpener = null;
            this._activeSubmenuCleanup = null;
          }
          submenu._parentMenu = null;
          submenu._parentItem = null;
          const ci = chainRoot._openChain.indexOf(submenu);
          if (ci !== -1)
            chainRoot._openChain.splice(ci, 1);
          submenu._collapseSelf = null;
          submenu._chainRoot = null;
          item._submenuOpen = false;
          if (wasDrillIn) {
            if (drillInOriginalParent && drillInOriginalParent.isConnected) {
              drillInOriginalParent.insertBefore(submenu, drillInNextSibling);
            } else {
              submenu.remove();
            }
            if (!skipReshow && this.isConnected && !this.matches(":popover-open")) {
              this.showPopover?.();
            }
            if (!skipReshow && this.isConnected) {
              const dest = this._parentItem?.text;
              this._announce(dest ? this._t("components.menu.submenu-back-action", { title: dest }) : this._t("components.menu.back"));
            }
          }
          if (!item.hasAttribute("data-focused")) {
            item.removeAttribute("highlighted");
          }
          this._stopSafeTriangle();
          this.dispatchEvent(new CustomEvent("submenu-close", {
            detail: { submenu, item },
            bubbles: true,
            composed: false
          }));
        };
        const onToggle = (e9) => {
          const tg = e9;
          if (tg.newState !== "closed")
            return;
          if (submenu._drillInHidingForDeeper) {
            submenu._drillInHidingForDeeper = false;
            return;
          }
          cleanup(false);
        };
        submenu.addEventListener("toggle", onToggle);
        this._activeSubmenuCleanup = cleanup;
        submenu._collapseSelf = () => {
          submenu.removeEventListener("toggle", onToggle);
          submenu.hidePopover?.();
          cleanup(true);
        };
        let drillInOriginalParent = null;
        let drillInNextSibling = null;
        if (wasDrillIn) {
          drillInOriginalParent = submenu.parentElement;
          drillInNextSibling = submenu.nextSibling;
          document.body.appendChild(submenu);
        }
        submenu.showPopover?.();
        if (wasDrillIn) {
          submenu._announce(submenu._t("components.menu.submenu-title", { title: item.text }));
          this._drillInHidingForDeeper = true;
          this.hidePopover?.();
        }
        if (!wasDrillIn) {
          requestAnimationFrame(() => {
            if (this._activeSubmenu === submenu) {
              this._startSafeTriangle(submenu);
            }
          });
        }
      };
      this._handleBack = () => {
        this.hidePopover?.();
      };
      this._handleBackMouseenter = () => {
        this._clearHighlight();
      };
      this._touchStartX = 0;
      this._touchStartY = 0;
      this._handleMenuTouchStart = (event) => {
        const t6 = event.touches[0];
        if (!t6)
          return;
        this._touchStartX = t6.clientX;
        this._touchStartY = t6.clientY;
        this.removeAttribute("scroll-active");
      };
      this._handleMenuTouchMove = (event) => {
        if (this.hasAttribute("scroll-active"))
          return;
        const t6 = event.touches[0];
        if (!t6)
          return;
        const dx = t6.clientX - this._touchStartX;
        const dy = t6.clientY - this._touchStartY;
        if (Math.hypot(dx, dy) > _NLDDMenu._TOUCH_SCROLL_THRESHOLD_PX) {
          this.setAttribute("scroll-active", "");
        }
      };
      this._handleMenuTouchEnd = () => {
        this.removeAttribute("scroll-active");
      };
      this._handleWindowResize = () => {
        this._collapseChain();
      };
      this._collapseChain = () => {
        const root = this._chainRoot ?? this;
        const chain = root._openChain;
        root._openChain = [];
        for (let i8 = chain.length - 1; i8 >= 0; i8--)
          chain[i8]._collapseSelf?.();
        if (root._isOpen)
          root.hidePopover?.();
      };
      this._dragStartItem = null;
      this._handleItemPointerdown = (event) => {
        if (event.button !== 0)
          return;
        const item = event.target?.closest("nldd-menu-item");
        if (!item || item.disabled)
          return;
        this._dragStartItem = item;
        document.addEventListener("pointerup", this._handleDragRelease, { capture: true, once: true });
      };
      this._handleDragRelease = (event) => {
        const startItem = this._dragStartItem;
        this._dragStartItem = null;
        if (!startItem)
          return;
        if (!this._isOpen)
          return;
        const releaseItem = this._menuItemFromPoint(event.clientX, event.clientY);
        if (!releaseItem || releaseItem === startItem || releaseItem.disabled)
          return;
        releaseItem._handleClick();
      };
      this._handleDocumentPointerdown = (event) => {
        if (!this._isOpen)
          return;
        if (!this._isSubmenu) {
          const anchorEl = this._getAnchorEl();
          if (anchorEl && event.composedPath().includes(anchorEl)) {
            this._collapsedByPointerGesture = true;
            return;
          }
        }
        if (!this._drillInMode)
          return;
        if (!this._isSubmenu)
          return;
        const path = event.composedPath();
        let menu = this;
        while (menu) {
          if (path.includes(menu))
            return;
          menu = menu._parentMenu;
        }
        if (event.pointerType === "touch") {
          this._outsideTapStartX = event.clientX;
          this._outsideTapStartY = event.clientY;
          this._teardownOutsideTap();
          this._outsideTapTracking = true;
          document.addEventListener("pointermove", this._outsideTapMove, true);
          document.addEventListener("pointerup", this._outsideTapEnd, true);
          document.addEventListener("pointercancel", this._outsideTapCancel, true);
          return;
        }
        (this._chainRoot ?? this)._collapsedByPointerGesture = true;
        this._collapseChain();
      };
      this._outsideTapStartX = 0;
      this._outsideTapStartY = 0;
      this._outsideTapTracking = false;
      this._outsideTapMove = (e9) => {
        const dx = e9.clientX - this._outsideTapStartX;
        const dy = e9.clientY - this._outsideTapStartY;
        if (Math.hypot(dx, dy) > _NLDDMenu._TOUCH_SCROLL_THRESHOLD_PX) {
          this._teardownOutsideTap();
        }
      };
      this._outsideTapEnd = () => {
        const wasTap = this._outsideTapTracking;
        this._teardownOutsideTap();
        if (wasTap) {
          (this._chainRoot ?? this)._collapsedByPointerGesture = true;
          this._collapseChain();
        }
      };
      this._outsideTapCancel = () => {
        this._teardownOutsideTap();
      };
      this._handleKeydown = (event) => {
        const items = this._getVisibleItems();
        if (items.length === 0)
          return;
        const focusedDescendant = this.querySelector("nldd-menu-item[data-focused]");
        if (focusedDescendant && focusedDescendant.closest("nldd-menu") !== this)
          return;
        const index = this._getFocusedIndex(items);
        switch (event.key) {
          case "ArrowDown": {
            event.preventDefault();
            event.stopPropagation();
            const next = index === -1 ? 0 : index < items.length - 1 ? index + 1 : 0;
            items[next].focus();
            break;
          }
          case "ArrowUp": {
            event.preventDefault();
            event.stopPropagation();
            const prev = index === -1 ? items.length - 1 : index > 0 ? index - 1 : items.length - 1;
            items[prev].focus();
            break;
          }
          case "Home": {
            event.preventDefault();
            event.stopPropagation();
            items[0].focus();
            break;
          }
          case "End": {
            event.preventDefault();
            event.stopPropagation();
            items[items.length - 1].focus();
            break;
          }
          case "ArrowRight": {
            const focused = items[index];
            if (focused?._hasSubmenu) {
              event.preventDefault();
              event.stopPropagation();
              focused._handleClick();
              requestAnimationFrame(() => {
                this._activeSubmenu?._getVisibleItems()[0]?.focus();
              });
            }
            break;
          }
          case "ArrowLeft": {
            if (this._isSubmenu) {
              event.preventDefault();
              event.stopPropagation();
              const parentItem = this._parentItem;
              this.hidePopover();
              parentItem?.focus();
            }
            break;
          }
          case "Escape": {
            event.preventDefault();
            event.stopPropagation();
            this.hidePopover();
            const focusTarget = this._isSubmenu ? this._parentItem : this._getAnchorEl();
            focusTarget?.focus();
            break;
          }
          default: {
            if (event.key.length === 1 && !event.ctrlKey && !event.metaKey && !event.altKey) {
              this._handleTypeahead(event, items, index);
            }
          }
        }
      };
      this._typeaheadBuffer = "";
      this._typeaheadTimer = null;
      this._handleToggle = async (event) => {
        const toggleEvent = event;
        this._isOpen = toggleEvent.newState === "open";
        this._syncAnchorPopupState(this._isOpen);
        if (toggleEvent.newState !== "open") {
          this._cleanupAutoUpdate?.();
          this._cleanupAutoUpdate = null;
          window.removeEventListener("resize", this._handleWindowResize);
          document.removeEventListener("pointerdown", this._handleDocumentPointerdown, true);
          this._teardownOutsideTap();
          this._clearHighlight();
          return;
        }
        this._updateDividerVisibility();
        this._clearHighlight();
        this._updateEmptyState();
        Array.from(this.querySelectorAll("nldd-menu-item")).forEach((item) => {
          item.menuVariant = this.variant;
        });
        await this.reposition();
        const anchorEl = this._getAnchorEl();
        if (anchorEl) {
          this._cleanupAutoUpdate = autoUpdate(anchorEl, this, () => this.reposition());
        }
        window.addEventListener("resize", this._handleWindowResize, { passive: true });
        document.addEventListener("pointerdown", this._handleDocumentPointerdown, true);
        await this.updateComplete;
        if (this.variant !== "listbox") {
          const keyboard = isKeyboardMode();
          const items = this._getVisibleItems();
          if (keyboard && items.length > 0) {
            this._setHighlight(items[0]);
            items[0].focus();
          } else {
            const menu = this.shadowRoot?.querySelector(".menu");
            menu?.classList.toggle("is-pointer-focus", !keyboard);
            menu?.focus();
          }
        }
      };
    }
    // — i18n ——————————————————————————————————————————————————————————————————
    _t(key, vars) {
      let str = this.translations[key] ?? nlddMenuTranslations[key];
      if (vars) {
        for (const [k2, v3] of Object.entries(vars)) {
          str = str.replace(`{${k2}}`, String(v3));
        }
      }
      return str;
    }
    /** Resolved empty text: emptyText attribute takes precedence, then i18n fallback. */
    get _resolvedEmptyText() {
      return this.emptyText || this._t("components.menu.empty-text");
    }
    /** Accessible name for the drill-in back button. Prefixes the parent
     * item's label with the localised "back" word so AT announces "Back:
     * Bestand" instead of an ambiguous "Bestand, button". Only used when
     * this menu is itself a submenu in drill-in mode. */
    get _resolvedBackLabel() {
      const back = this._t("components.menu.back");
      const parent = this._parentItem?.text ?? "";
      return parent ? `${back}: ${parent}` : back;
    }
    /**
     * Write `message` to this menu's polite live region. Clears first, then
     * sets on a double rAF — the empty→text transition is what makes screen
     * readers reliably re-announce unchanged-looking content across engines.
     *
     * Spam control without muting real navigation: a doubled event in the
     * same tick (identical message still pending) is dropped; a *different*
     * message arriving first cancels the stale pending one (rapid navigation
     * announces only the level you land on); once flushed, the same text may
     * be announced again for a later genuine transition (e.g. re-entering a
     * submenu after going back).
     */
    _announce(message) {
      if (!message)
        return;
      if (this._announceRaf && message === this._pendingAnnouncement)
        return;
      const region = this.shadowRoot?.querySelector(".menu__live-region");
      if (!region)
        return;
      if (this._announceRaf)
        cancelAnimationFrame(this._announceRaf);
      this._pendingAnnouncement = message;
      region.textContent = "";
      this._announceRaf = requestAnimationFrame(() => {
        this._announceRaf = requestAnimationFrame(() => {
          this._announceRaf = 0;
          this._pendingAnnouncement = "";
          region.textContent = message;
        });
      });
    }
    // — Lifecycle ——————————————————————————————————————————————————————————————
    updated(changedProperties) {
      if (changedProperties.has("width")) {
        if (this.width) {
          this.style.setProperty("--_width", this.width);
        } else {
          this.style.removeProperty("--_width");
        }
      }
      if (changedProperties.has("maxItems")) {
        if (this.maxItems > 0) {
          this.style.setProperty("--_max-items", String(this.maxItems));
        } else {
          this.style.removeProperty("--_max-items");
        }
      }
      if (changedProperties.has("variant")) {
        Array.from(this.querySelectorAll("nldd-menu-item")).forEach((item) => {
          item.menuVariant = this.variant;
        });
      }
    }
    // — Anchor ————————————————————————————————————————————————————————————————
    _getAnchorEl() {
      if (this.anchorElement)
        return this.anchorElement;
      if (this.anchor)
        return document.getElementById(this.anchor);
      return null;
    }
    /**
     * Pick the drill-in side (above vs below the anchor) purely from the
     * available viewport space around the anchor — never from content
     * size. Content-independent so every level of the chain resolves to
     * the same side and a bigger submenu scrolls internally rather than
     * flipping the whole stack. Keeps the configured alignment suffix
     * (`-start` / `-end`) from `placement`.
     */
    _resolveDrillInPlacement(anchorEl) {
      const base = this.placement || "bottom-start";
      const align = base.includes("-") ? base.slice(base.indexOf("-")) : "";
      const rect = anchorEl.getBoundingClientRect();
      const margin = this._cssPx("--_viewport-margin");
      const spaceBelow = window.innerHeight - rect.bottom - margin;
      const spaceAbove = rect.top - margin;
      const side = spaceBelow >= spaceAbove ? "bottom" : "top";
      return `${side}${align}`;
    }
    /**
     * Reflect this menu's open state on the anchor so an anchor button (or
     * any element exposing an `expanded` IDL property) shows the active
     * "is-expanded" visual while we're open. Skip menu-items as anchors —
     * they manage their own `_submenuOpen` lifecycle and don't have an
     * `expanded` property. Also default `popupType` to 'menu' / 'listbox'
     * (matching this menu's variant) when the anchor supports it and the
     * consumer hasn't already chosen a value, so screen readers announce
     * the popup role without each consumer having to set it manually.
     *
     * Finally, drive the anchor's `popoverTargetAction` so the browser's
     * native invoker action always matches the current state explicitly:
     * `'hide'` while open, `'show'` while closed. We avoid `'toggle'` on
     * purpose — toggle re-evaluates the popover's live state at click time
     * and would re-open the menu if it had just been light-dismissed
     * milliseconds earlier (the source of the "menu briefly closes and
     * reopens" bug). With explicit show/hide, the worst case is a no-op
     * (e.g. `'hide'` on an already-closed menu) — never a spurious open.
     *
     * The invoker association (`popoverTargetElement`) is set elsewhere
     * — persistently on anchor change rather than per-toggle — so the
     * browser's light-dismiss exclusion is in place from the very first
     * click instead of only after the menu has already opened once.
     */
    _syncAnchorPopupState(isOpen) {
      const anchor = this._getAnchorEl();
      if (!anchor)
        return;
      if ("expanded" in anchor)
        anchor.expanded = isOpen;
      if (isOpen && "popupType" in anchor && !anchor.popupType) {
        anchor.popupType = this.variant === "listbox" ? "listbox" : "menu";
      }
      if ("popoverTargetAction" in anchor) {
        anchor.popoverTargetAction = isOpen ? "hide" : "show";
      }
    }
    /**
     * Item-activation logic shared between the native `mouseenter` capture
     * handler and the safe-triangle recovery paths (sideways exit, direction
     * reversal, stall dismissal). Those paths know which item is under the
     * cursor — they don't need to synthesize a MouseEvent just to re-enter
     * the handler. Factoring out the body keeps the entry contracts honest:
     * the recovery sites pass an already-resolved item rather than relying
     * on `event.target` semantics they'd have to fake.
     */
    _activateItem(item) {
      if (item.disabled || item.hasAttribute("hidden"))
        return;
      if (item.closest("nldd-menu") !== this)
        return;
      if (this._movingTowardSubmenu && item !== this._activeSubmenuOpener)
        return;
      this._setHighlight(item);
      if (this._drillInMode)
        return;
      if (this._activeSubmenu && item !== this._activeSubmenuOpener) {
        this._activeSubmenu.hidePopover?.();
      }
      this._cancelHoverOpen();
      if (item._hasSubmenu && this._activeSubmenuOpener !== item) {
        this._hoverOpenTimer = window.setTimeout(() => {
          this._hoverOpenTimer = null;
          item._handleClick();
        }, 150);
      }
    }
    /** Resolve the menu-item under a viewport point and activate it via
     * `_activateItem`. Used by safe-triangle recovery paths whose natural
     * mouseenter has already fired (and bailed) — they need to re-trigger
     * activation without synthesizing a MouseEvent. */
    _activateItemAt(p4) {
      const el = document.elementFromPoint(p4.x, p4.y);
      const item = el?.closest("nldd-menu-item");
      if (item)
        this._activateItem(item);
    }
    /** Start the global mouse tracker that powers the safe triangle. Called
     * by _handleSubmenuOpen when a submenu opens in cascade mode. The
     * listener is a thin orchestrator over the named phase methods below. */
    _startSafeTriangle(submenu) {
      this._stopSafeTriangle();
      this._lastCursorPos = null;
      this._safeTriangleApex = null;
      this._safeTriangleSubmenu = submenu;
      this._movingTowardSubmenu = false;
      this._safeTriangleListener = (e9) => {
        const p4 = { x: e9.clientX, y: e9.clientY };
        const r6 = submenu.getBoundingClientRect();
        if (_NLDDMenu._isPointInMenuTree(p4, submenu)) {
          this._safeTriangleArrived(p4);
          return;
        }
        const openerRect = this._activeSubmenuOpener?.getBoundingClientRect();
        const cursorOnOpener = _NLDDMenu._rectContainsPoint(openerRect, p4);
        const lastOnOpener = _NLDDMenu._rectContainsPoint(openerRect, this._lastCursorPos);
        if (this._safeTriangleApex !== null && cursorOnOpener) {
          this._safeTriangleApex = null;
        }
        const wasMoving = this._movingTowardSubmenu;
        if (this._safeTriangleApex === null) {
          if (cursorOnOpener) {
            this._safeTriangleWaitOnOpener(p4, r6, openerRect);
            return;
          }
          if (openerRect && lastOnOpener && (p4.y > openerRect.bottom || p4.y < openerRect.top)) {
            this._safeTrianglePinApex(p4, openerRect);
          } else {
            this._safeTriangleSidewaysExit(p4, wasMoving);
          }
        } else {
          this._safeTrianglePolygonTest(p4, r6);
        }
        this._lastCursorPos = p4;
        this._safeTriangleSyncOpenerHighlight();
        this._safeTriangleDirectionReversalRecovery(p4, wasMoving);
        this._safeTriangleScheduleStall();
      };
      window.addEventListener("mousemove", this._safeTriangleListener);
    }
    // — Safe-triangle phase methods ————————————————————————————————————————
    /** Cursor entered the submenu (or any nested submenu) — clean up triangle
     * state but leave the window mousemove listener alive. The listener still
     * needs to fire while the cursor is inside the submenu so we can detect a
     * return trip to the opener and re-pin a fresh apex for another exit. */
    _safeTriangleArrived(p4) {
      this._movingTowardSubmenu = false;
      this._lastCursorPos = p4;
      this._safeTriangleApex = null;
      this._activeSubmenuOpener?.removeAttribute("highlighted");
      this._removeSafeTriangleOverlay();
    }
    /** Cursor still on the opener — wait for an exit direction. Sets the
     * "in transit" flag (so brief peer pass-throughs don't activate) and
     * renders a live debug preview that follows the cursor. */
    _safeTriangleWaitOnOpener(p4, r6, openerRect) {
      this._movingTowardSubmenu = true;
      this._lastCursorPos = p4;
      if (this._activeSubmenuOpener && !this._activeSubmenuOpener.hasAttribute("highlighted")) {
        this._setHighlight(this._activeSubmenuOpener);
      }
      if (this.debugSafeTriangle && openerRect) {
        const nearX = p4.x < r6.left ? r6.left : r6.right;
        this._renderSafeTriangleOverlay([
          { x: p4.x - _NLDDMenu._SAFE_TRIANGLE_APEX_X_OFFSET, y: p4.y },
          { x: nearX, y: r6.top },
          { x: nearX, y: r6.bottom }
        ]);
      }
    }
    /** Cursor crossed the opener's top or bottom edge — pin apex there.
     * x is clamped to the opener's range so the wedge starts at the visual
     * edge regardless of how far past the edge this mousemove sample is, then
     * shifted by _SAFE_TRIANGLE_APEX_X_OFFSET to widen the wedge at the
     * cursor side (the 4px headroom that makes brief diagonal wobble forgive). */
    _safeTrianglePinApex(p4, openerRect) {
      const apexX = Math.max(openerRect.left, Math.min(openerRect.right, p4.x)) - _NLDDMenu._SAFE_TRIANGLE_APEX_X_OFFSET;
      const apexY = p4.y > openerRect.bottom ? openerRect.bottom : openerRect.top;
      this._safeTriangleApex = { x: apexX, y: apexY };
      this._movingTowardSubmenu = true;
    }
    /** Cursor exited the opener sideways (left/right) or with no opener
     * history — no triangle protection. Drop the flag and the overlay,
     * and synthesize mouseenter on the element under the cursor so peers
     * activate immediately (the natural mouseenter fired with flag=true
     * during the on-opener phase and got bailed). */
    _safeTriangleSidewaysExit(p4, wasMoving) {
      this._movingTowardSubmenu = false;
      this._lastCursorPos = p4;
      this._removeSafeTriangleOverlay();
      if (wasMoving)
        this._activateItemAt(p4);
    }
    /** Apex pinned — test whether the current cursor position is inside the
     * wedge. Picks the submenu edge facing the cursor (left for right-start,
     * right when floating-ui flipped it). */
    _safeTrianglePolygonTest(p4, r6) {
      const nearX = p4.x < r6.left ? r6.left : r6.right;
      const apex = this._safeTriangleApex;
      const wedge = [
        apex,
        { x: nearX, y: r6.top },
        { x: nearX, y: r6.bottom }
      ];
      this._movingTowardSubmenu = _NLDDMenu._pointInPolygon(p4, wedge);
      if (this.debugSafeTriangle) {
        this._renderSafeTriangleOverlay(wedge);
      }
    }
    /** Sync opener highlight with the in-transit flag. Uses _setHighlight so
     * any peer that briefly got highlighted (e.g. via the synthetic
     * mouseenter on a wedge-edge wobble) is cleared. Skip when the opener
     * already has the attribute to avoid DOM churn each frame. */
    _safeTriangleSyncOpenerHighlight() {
      if (this._activeSubmenuOpener && this._movingTowardSubmenu && !this._activeSubmenuOpener.hasAttribute("highlighted")) {
        this._setHighlight(this._activeSubmenuOpener);
      }
    }
    /** When the in-transit flag flips true → false, the cursor may already
     * be sitting on a peer item whose natural mouseenter fired (and bailed)
     * earlier. mouseenter won't re-fire until the cursor leaves and re-
     * enters, so synthesize one here to activate the peer immediately. */
    _safeTriangleDirectionReversalRecovery(p4, wasMoving) {
      if (!wasMoving || this._movingTowardSubmenu)
        return;
      this._activateItemAt(p4);
    }
    /** Re-arm the stall-dismissal timer. If the cursor sits motionless
     * inside the safe triangle long enough, drop the protection so the
     * element under the cursor can become interactive without nudging. */
    _safeTriangleScheduleStall() {
      if (this._safeTriangleStallTimer !== null) {
        clearTimeout(this._safeTriangleStallTimer);
        this._safeTriangleStallTimer = null;
      }
      if (!this._movingTowardSubmenu)
        return;
      this._safeTriangleStallTimer = window.setTimeout(() => {
        this._safeTriangleStallTimer = null;
        this._movingTowardSubmenu = false;
        this._safeTriangleApex = null;
        if (this._lastCursorPos)
          this._activateItemAt(this._lastCursorPos);
      }, _NLDDMenu._SAFE_TRIANGLE_STALL_DISMISS_MS);
    }
    /** True when the rect contains the point (inclusive). Returns false for
     * a missing rect or point — convenient for the optional-chained call
     * sites in the safe-triangle logic. */
    static _rectContainsPoint(rect, point) {
      return !!rect && !!point && point.x >= rect.left && point.x <= rect.right && point.y >= rect.top && point.y <= rect.bottom;
    }
    _stopSafeTriangle() {
      if (this._safeTriangleListener !== null) {
        window.removeEventListener("mousemove", this._safeTriangleListener);
        this._safeTriangleListener = null;
      }
      if (this._safeTriangleStallTimer !== null) {
        clearTimeout(this._safeTriangleStallTimer);
        this._safeTriangleStallTimer = null;
      }
      this._lastCursorPos = null;
      this._safeTriangleApex = null;
      this._safeTriangleSubmenu = null;
      this._movingTowardSubmenu = false;
      this._removeSafeTriangleOverlay();
    }
    _renderSafeTriangleOverlay(vertices) {
      const NS = "http://www.w3.org/2000/svg";
      if (this._safeTriangleOverlay === null) {
        const wrapper = document.createElement("div");
        wrapper.setAttribute("popover", "manual");
        wrapper.setAttribute("aria-hidden", "true");
        wrapper.style.cssText = "position:fixed;inset:0;width:100vw;height:100vh;pointer-events:none;border:0;padding:0;margin:0;background:transparent;overflow:visible;";
        const svg = document.createElementNS(NS, "svg");
        svg.setAttribute("width", "100%");
        svg.setAttribute("height", "100%");
        const polygon2 = document.createElementNS(NS, "polygon");
        polygon2.setAttribute("fill", "rgba(255, 0, 128, 0.15)");
        polygon2.setAttribute("stroke", "rgba(255, 0, 128, 0.85)");
        polygon2.setAttribute("stroke-width", "1");
        svg.appendChild(polygon2);
        wrapper.appendChild(svg);
        document.body.appendChild(wrapper);
        wrapper.showPopover();
        this._safeTriangleOverlay = wrapper;
      }
      const polygon = this._safeTriangleOverlay.querySelector("polygon");
      polygon.setAttribute("points", vertices.map((v3) => `${v3.x},${v3.y}`).join(" "));
    }
    _removeSafeTriangleOverlay() {
      this._safeTriangleOverlay?.hidePopover?.();
      this._safeTriangleOverlay?.remove();
      this._safeTriangleOverlay = null;
    }
    _cancelHoverOpen() {
      if (this._hoverOpenTimer !== null) {
        clearTimeout(this._hoverOpenTimer);
        this._hoverOpenTimer = null;
      }
    }
    /** Recursively checks whether a point sits inside the given menu OR any of
     * its descendant submenus. Used by the safe-triangle "arrived" check so a
     * cursor that has continued past the immediate submenu into a deeper
     * nested submenu still reads as "in the safe area" — without the recursive
     * walk we'd treat the deeper hover as an exit and re-pin a stale apex. */
    static _isPointInMenuTree(p4, menu) {
      const r6 = menu.getBoundingClientRect();
      if (p4.x >= r6.left && p4.x <= r6.right && p4.y >= r6.top && p4.y <= r6.bottom)
        return true;
      if (menu._activeSubmenu)
        return _NLDDMenu._isPointInMenuTree(p4, menu._activeSubmenu);
      return false;
    }
    /** Convex point-in-polygon test via consistent edge-cross-product sign.
     * Vertices must be ordered (clockwise or counter-clockwise). */
    static _pointInPolygon(p4, vertices) {
      let hasNeg = false;
      let hasPos = false;
      for (let i8 = 0; i8 < vertices.length; i8++) {
        const a4 = vertices[i8];
        const b3 = vertices[(i8 + 1) % vertices.length];
        const cross = (p4.x - b3.x) * (a4.y - b3.y) - (a4.x - b3.x) * (p4.y - b3.y);
        if (cross < 0)
          hasNeg = true;
        if (cross > 0)
          hasPos = true;
        if (hasNeg && hasPos)
          return false;
      }
      return true;
    }
    static _getDrillInModeQuery() {
      if (_NLDDMenu._drillInModeQuery === null) {
        _NLDDMenu._drillInModeQuery = matchMedia(`(pointer: coarse), (max-width: ${breakpoints.smMax})`);
      }
      return _NLDDMenu._drillInModeQuery;
    }
    get _drillInMode() {
      return _NLDDMenu._getDrillInModeQuery().matches;
    }
    /** Walks the parent-menu chain up to the root (the menu that wasn't
     * opened by another menu — has no _parentMenu). */
    get _rootMenu() {
      let m3 = this;
      while (m3._parentMenu)
        m3 = m3._parentMenu;
      return m3;
    }
    /** True when this menu is itself a submenu (was opened by another menu's
     * item). The root menu returns false. */
    get _isSubmenu() {
      return this._parentMenu !== null;
    }
    /**
     * Resolves the menu-item under the pointer, piercing shadow boundaries.
     * `document.elementFromPoint` stops at the outermost shadow host, so a menu
     * nested inside another component's shadow DOM (e.g. nldd-split-button)
     * resolves the release target to that host and never finds the item — which
     * silently disables press-drag-release there. Descending the shadow roots
     * restores it and is a no-op for light-DOM menus.
     */
    _menuItemFromPoint(x2, y3) {
      let root = document;
      const MAX_PIERCE_DEPTH = 20;
      for (let depth = 0; depth < MAX_PIERCE_DEPTH; depth++) {
        const el = root.elementFromPoint(x2, y3);
        if (!el)
          return null;
        const item = el.closest("nldd-menu-item");
        if (item)
          return item;
        if (!el.shadowRoot)
          return null;
        root = el.shadowRoot;
      }
      return null;
    }
    _teardownOutsideTap() {
      this._outsideTapTracking = false;
      document.removeEventListener("pointermove", this._outsideTapMove, true);
      document.removeEventListener("pointerup", this._outsideTapEnd, true);
      document.removeEventListener("pointercancel", this._outsideTapCancel, true);
    }
    connectedCallback() {
      super.connectedCallback();
      if (!this.id) {
        this.id = `nldd-menu-${_NLDDMenu._menuIdCounter++}`;
      }
      if (!this.hasAttribute("popover")) {
        this.setAttribute("popover", "");
      }
      this.addEventListener("toggle", this._handleToggle);
      this.addEventListener("keydown", this._handleKeydown);
      this.addEventListener("mouseenter", this._handleMenuItemMouseenter, true);
      this.addEventListener("mouseleave", this._handleMouseleave);
      this.addEventListener("menu-item-focused", this._handleMenuItemFocused);
      this.addEventListener("submenu-open", this._handleSubmenuOpen);
      this.addEventListener("select", this._collapseChain);
      this.addEventListener("pointerdown", this._handleItemPointerdown);
      document.addEventListener("click", this._handleDocumentClick);
    }
    firstUpdated() {
      this._updateEmptyState();
    }
    disconnectedCallback() {
      super.disconnectedCallback();
      if (this._activeSubmenu) {
        this._activeSubmenu.hidePopover?.();
      }
      this.removeEventListener("toggle", this._handleToggle);
      this.removeEventListener("keydown", this._handleKeydown);
      this.removeEventListener("mouseenter", this._handleMenuItemMouseenter, true);
      this.removeEventListener("mouseleave", this._handleMouseleave);
      this.removeEventListener("menu-item-focused", this._handleMenuItemFocused);
      this.removeEventListener("submenu-open", this._handleSubmenuOpen);
      this.removeEventListener("select", this._collapseChain);
      this.removeEventListener("pointerdown", this._handleItemPointerdown);
      document.removeEventListener("click", this._handleDocumentClick);
      document.removeEventListener("pointerup", this._handleDragRelease, true);
      document.removeEventListener("pointerdown", this._handleDocumentPointerdown, true);
      this._teardownOutsideTap();
      window.removeEventListener("resize", this._handleWindowResize);
      this._cancelHoverOpen();
      this._stopSafeTriangle();
      if (this._typeaheadTimer !== null) {
        clearTimeout(this._typeaheadTimer);
        this._typeaheadTimer = null;
      }
      this._cleanupAutoUpdate?.();
      this._cleanupAutoUpdate = null;
    }
    // — Internal helpers ——————————————————————————————————————————————————————
    _getVisibleItems() {
      return Array.from(this.querySelectorAll("nldd-menu-item:not([hidden]):not([disabled])")).filter((item) => item.closest("nldd-menu") === this);
    }
    _getFocusedIndex(items) {
      return items.findIndex((item) => item.hasAttribute("data-focused"));
    }
    _clearHighlight() {
      Array.from(this.querySelectorAll("nldd-menu-item")).filter((item) => item.closest("nldd-menu") === this).forEach((item) => item.removeAttribute("highlighted"));
    }
    _setHighlight(target) {
      this._clearHighlight();
      const resolved = target ?? this._getVisibleItems()[0] ?? null;
      resolved?.setAttribute("highlighted", "");
    }
    _updateEmptyState() {
      this._isEmpty = this._getVisibleItems().length === 0;
    }
    _updateDividerVisibility() {
      const children = Array.from(this.children);
      children.forEach((el) => {
        const tag = el.tagName.toLowerCase();
        if (tag === "nldd-menu-divider")
          el.removeAttribute("hidden");
        if (tag === "nldd-menu-group")
          el.removeAttribute("data-no-bottom-divider");
      });
      const visible = children.filter((el) => !el.hasAttribute("hidden"));
      visible.forEach((el, index) => {
        if (el.tagName.toLowerCase() !== "nldd-menu-divider")
          return;
        const prevTag = visible[index - 1]?.tagName.toLowerCase();
        const nextTag = visible[index + 1]?.tagName.toLowerCase();
        const isFirst = index === 0;
        const isLast = index === visible.length - 1;
        if (isFirst || isLast || prevTag === "nldd-menu-divider" || prevTag === "nldd-menu-group" || nextTag === "nldd-menu-group") {
          el.setAttribute("hidden", "");
        }
      });
      const remaining = visible.filter((el) => !el.hasAttribute("hidden"));
      remaining.forEach((el, index) => {
        if (el.tagName.toLowerCase() !== "nldd-menu-group")
          return;
        const nextTag = remaining[index + 1]?.tagName.toLowerCase();
        const isLast = index === remaining.length - 1;
        if (isLast || nextTag === "nldd-menu-group") {
          el.setAttribute("data-no-bottom-divider", "");
        }
      });
    }
    // — Public API ————————————————————————————————————————————————————————————
    /**
     * Filter items based on a query string.
     *
     * Matching items are kept visible. Non-matching items are hidden. Matching
     * items receive `query=<query>` so their text-cell bolds the non-typed
     * remainder (predictive completion — the ARIA APG pattern for combobox).
     *
     * When the query is empty, all items are shown and `query` is cleared.
     */
    filter(query) {
      const allItems = Array.from(this.querySelectorAll("nldd-menu-item"));
      allItems.forEach((item) => {
        const matches = !query || this.filterFn(query, item);
        item.toggleAttribute("hidden", !matches);
        item.query = matches && query ? query : "";
      });
      this._updateGroupVisibility();
      this._setHighlight(null);
      this._updateEmptyState();
      this._updateDividerVisibility();
      if (this._isOpen)
        this.reposition();
    }
    /**
     * Hide each nldd-menu-group whose items are all filtered out — a labelled
     * heading above an empty section reads as broken. Runs after filter() has
     * updated individual item visibility.
     */
    _updateGroupVisibility() {
      const groups = this.querySelectorAll("nldd-menu-group");
      groups.forEach((group) => {
        const visibleItems = group.querySelectorAll("nldd-menu-item:not([hidden])");
        group.toggleAttribute("hidden", visibleItems.length === 0);
      });
    }
    /**
     * Move both focus and highlight to the next, previous, or first visible item.
     */
    focusItem(direction) {
      const items = this._getVisibleItems();
      if (items.length === 0)
        return;
      let targetIndex;
      if (direction === "first") {
        targetIndex = 0;
      } else {
        const current = items.findIndex((item) => item.hasAttribute("highlighted") || item.hasAttribute("data-focused"));
        if (direction === "next") {
          targetIndex = current === -1 ? 0 : current < items.length - 1 ? current + 1 : 0;
        } else {
          targetIndex = current === -1 ? items.length - 1 : current > 0 ? current - 1 : items.length - 1;
        }
      }
      items.forEach((item) => item.removeAttribute("highlighted"));
      items[targetIndex].setAttribute("highlighted", "");
      items[targetIndex].focus();
    }
    /**
     * Move the highlight to the next or previous visible item without moving focus.
     * Useful when keyboard navigation should keep focus on the input.
     */
    moveHighlight(direction) {
      const items = this._getVisibleItems();
      if (items.length === 0)
        return;
      const current = items.findIndex((item) => item.hasAttribute("highlighted"));
      let next;
      if (direction === "next") {
        next = current === -1 ? 0 : current < items.length - 1 ? current + 1 : 0;
      } else {
        next = current === -1 ? items.length - 1 : current > 0 ? current - 1 : items.length - 1;
      }
      items.forEach((item) => item.removeAttribute("highlighted"));
      items[next].setAttribute("highlighted", "");
    }
    /** Returns the currently highlighted item, or null if none. */
    getHighlighted() {
      return this.querySelector("nldd-menu-item[highlighted]");
    }
    /** Returns the ID of the currently highlighted item, or empty string if none. */
    getHighlightedId() {
      return this.getHighlighted()?.id ?? "";
    }
    /** Read a px-valued CSS custom property as a number. `parseFloat`
     * keeps fractional pixels; an unset/NaN value falls back to 0. */
    _cssPx(name) {
      const v3 = parseFloat(getComputedStyle(this).getPropertyValue(name));
      return Number.isNaN(v3) ? 0 : v3;
    }
    /** Recalculate position and size relative to the anchor element. */
    async reposition() {
      const anchorEl = this._getAnchorEl();
      if (!anchorEl || !this._isOpen)
        return;
      const viewportMargin = this._cssPx("--_viewport-margin");
      const submenuPadding = this._isSubmenu && !this._drillInMode ? this._cssPx("--_padding") : 0;
      const drillIn = this._drillInMode;
      const effectivePlacement = drillIn ? this._resolveDrillInPlacement(anchorEl) : this.placement;
      const { x: x2, y: y3 } = await computePosition2(anchorEl, this, {
        placement: effectivePlacement,
        middleware: [
          offset2({ mainAxis: 0, alignmentAxis: -submenuPadding }),
          ...drillIn ? [] : [flip2({ padding: viewportMargin })],
          shift2({ padding: viewportMargin }),
          size2({
            padding: viewportMargin,
            apply: ({ availableHeight }) => {
              this.style.setProperty("--_max-height", `${availableHeight}px`);
            }
          })
        ]
      });
      Object.assign(this.style, {
        left: `${x2}px`,
        top: `${y3}px`
      });
    }
    /**
     * ARIA APG typeahead: characters typed within 500ms accumulate into a
     * buffer; the menu jumps to the first item whose text starts with the
     * buffer (case-insensitive).
     *
     * Single-char buffer (first press or after reset) cycles through matches
     * — repeated presses of the same letter step through every item starting
     * with it. Multi-char buffer matches from the currently-focused item, so
     * extending the prefix while the current item still matches keeps focus
     * stable instead of jumping forward.
     */
    _handleTypeahead(event, items, currentIndex) {
      if (this._typeaheadTimer !== null) {
        clearTimeout(this._typeaheadTimer);
      }
      this._typeaheadBuffer += event.key.toLowerCase();
      this._typeaheadTimer = window.setTimeout(() => {
        this._typeaheadBuffer = "";
        this._typeaheadTimer = null;
      }, _NLDDMenu._TYPEAHEAD_RESET_MS);
      const start = this._typeaheadBuffer.length === 1 && currentIndex >= 0 ? currentIndex + 1 : Math.max(0, currentIndex);
      for (let i8 = 0; i8 < items.length; i8++) {
        const idx = (start + i8) % items.length;
        if (items[idx].text.toLowerCase().startsWith(this._typeaheadBuffer)) {
          event.preventDefault();
          event.stopPropagation();
          items[idx].focus();
          return;
        }
      }
    }
    render() {
      return menuTemplate.call(this, this._isEmpty, this.variant);
    }
  };
  NLDDMenu.styles = menuStyles;
  NLDDMenu._SAFE_TRIANGLE_STALL_DISMISS_MS = 500;
  NLDDMenu._SAFE_TRIANGLE_APEX_X_OFFSET = 4;
  NLDDMenu._drillInModeQuery = null;
  NLDDMenu._TOUCH_SCROLL_THRESHOLD_PX = 8;
  NLDDMenu._menuIdCounter = 0;
  NLDDMenu._TYPEAHEAD_RESET_MS = 500;
  __decorate12([
    n4({ type: String, reflect: true })
  ], NLDDMenu.prototype, "anchor", void 0);
  __decorate12([
    n4({ attribute: false })
  ], NLDDMenu.prototype, "anchorElement", void 0);
  __decorate12([
    n4({ type: String, reflect: true })
  ], NLDDMenu.prototype, "placement", void 0);
  __decorate12([
    n4({ type: String, reflect: true })
  ], NLDDMenu.prototype, "variant", void 0);
  __decorate12([
    n4({ type: String, attribute: "empty-text" })
  ], NLDDMenu.prototype, "emptyText", void 0);
  __decorate12([
    n4({ type: String, attribute: "empty-supporting-text" })
  ], NLDDMenu.prototype, "emptySupportingText", void 0);
  __decorate12([
    n4({ type: String, reflect: true })
  ], NLDDMenu.prototype, "width", void 0);
  __decorate12([
    n4({ type: Number, attribute: "max-items" })
  ], NLDDMenu.prototype, "maxItems", void 0);
  __decorate12([
    n4({ type: Object })
  ], NLDDMenu.prototype, "translations", void 0);
  __decorate12([
    n4({ type: Boolean, reflect: true, attribute: "debug-safe-triangle" })
  ], NLDDMenu.prototype, "debugSafeTriangle", void 0);
  __decorate12([
    n4({ attribute: false })
  ], NLDDMenu.prototype, "filterFn", void 0);
  __decorate12([
    r5()
  ], NLDDMenu.prototype, "_isEmpty", void 0);
  __decorate12([
    r5()
  ], NLDDMenu.prototype, "_parentItem", void 0);
  if (!customElements.get("nldd-menu")) {
    customElements.define("nldd-menu", NLDDMenu);
  }

  // node_modules/@nldd/design-system/dist/components/actions/split-button/split-button.js
  var __decorate13 = function(decorators, target, key, desc) {
    var c6 = arguments.length, r6 = c6 < 3 ? target : desc === null ? desc = Object.getOwnPropertyDescriptor(target, key) : desc, d3;
    if (typeof Reflect === "object" && typeof Reflect.decorate === "function") r6 = Reflect.decorate(decorators, target, key, desc);
    else for (var i8 = decorators.length - 1; i8 >= 0; i8--) if (d3 = decorators[i8]) r6 = (c6 < 3 ? d3(r6) : c6 > 3 ? d3(target, key, r6) : d3(target, key)) || r6;
    return c6 > 3 && r6 && Object.defineProperty(target, key, r6), r6;
  };
  var NLDDSplitButton = class NLDDSplitButton2 extends i4 {
    constructor() {
      super(...arguments);
      this.size = "md";
      this.variant = "neutral-tinted";
      this.disabled = false;
      this.width = "";
      this.text = "";
      this.icon = "";
      this.translations = {};
      this._menuIsOpen = false;
      this._menu = null;
      this._menuWasOpenOnPointerdown = false;
      this._handleMenuToggle = (event) => {
        this._menuIsOpen = event.newState === "open";
      };
    }
    // — i18n —————————————————————————————————————————————————————————————————
    _t(key) {
      return this.translations[key] ?? nlddSplitButtonTranslations[key];
    }
    // — Lifecycle ————————————————————————————————————————————————————————————
    firstUpdated() {
      this._popupButtonWrapper?.addEventListener("pointerdown", () => {
        this._menuWasOpenOnPointerdown = this._menu?.matches(":popover-open") ?? false;
      });
      if (this._menu && this._popupButtonWrapper) {
        this._menu.anchorElement = this._popupButtonWrapper;
      }
    }
    disconnectedCallback() {
      super.disconnectedCallback();
      this._menu?.removeEventListener("toggle", this._handleMenuToggle);
    }
    /**
     * Wires the slotted `nldd-menu` to the chevron: anchors it to the popup
     * button and tracks its open state. Re-runs whenever the slotted content
     * changes (menu added, removed or replaced).
     */
    _handleSlotChange(event) {
      const slot = event.target;
      const menu = slot.assignedElements().find((el) => el.matches("nldd-menu")) ?? null;
      if (menu === this._menu)
        return;
      this._menu?.removeEventListener("toggle", this._handleMenuToggle);
      this._menu = menu;
      this._menuIsOpen = false;
      if (menu) {
        if (this._popupButtonWrapper)
          menu.anchorElement = this._popupButtonWrapper;
        menu.addEventListener("toggle", this._handleMenuToggle);
      }
    }
    _handleActionClick(e9) {
      if (this.disabled)
        return;
      e9.stopPropagation();
      this.dispatchEvent(new CustomEvent("action-click", { bubbles: true, composed: true }));
    }
    _handleMenuClick(e9) {
      if (this.disabled)
        return;
      e9.stopPropagation();
      if (this._menu) {
        const wasOpen = this._menuWasOpenOnPointerdown;
        this._menuWasOpenOnPointerdown = false;
        if (wasOpen)
          return;
        this._menu.showPopover();
        return;
      }
      this.dispatchEvent(new CustomEvent("menu-click", { bubbles: true, composed: true }));
    }
    // willUpdate (not updated) so the host width is resolved before the first
    // render rather than one frame after it — otherwise an explicit width
    // briefly flashes at the auto default.
    willUpdate(changedProperties) {
      if (changedProperties.has("width")) {
        const w2 = this.width;
        const isFull = w2 === "full";
        const isValidLength = !!w2 && !isFull && CSS.supports("width", w2);
        this.style.width = isValidLength ? w2 : "";
        if (isFull || isValidLength) {
          this.style.setProperty("--_width", "100%");
        } else {
          this.style.removeProperty("--_width");
        }
      }
    }
    render() {
      return template4.call(this);
    }
  };
  NLDDSplitButton.styles = splitButtonStyles;
  __decorate13([
    n4({ type: String, reflect: true })
  ], NLDDSplitButton.prototype, "size", void 0);
  __decorate13([
    n4({ type: String, reflect: true })
  ], NLDDSplitButton.prototype, "variant", void 0);
  __decorate13([
    n4({ type: Boolean, reflect: true })
  ], NLDDSplitButton.prototype, "disabled", void 0);
  __decorate13([
    n4({ type: String, reflect: true })
  ], NLDDSplitButton.prototype, "width", void 0);
  __decorate13([
    n4({ type: String })
  ], NLDDSplitButton.prototype, "text", void 0);
  __decorate13([
    n4({ type: String })
  ], NLDDSplitButton.prototype, "icon", void 0);
  __decorate13([
    n4({ type: Object })
  ], NLDDSplitButton.prototype, "translations", void 0);
  __decorate13([
    e5(".split-button__popup-button")
  ], NLDDSplitButton.prototype, "_popupButtonWrapper", void 0);
  __decorate13([
    r5()
  ], NLDDSplitButton.prototype, "_menuIsOpen", void 0);
  NLDDSplitButton = __decorate13([
    t3("nldd-split-button")
  ], NLDDSplitButton);

  // node_modules/@nldd/design-system/dist/components/index.js
  init_button_group();

  // node_modules/@nldd/design-system/dist/components/actions/button-bar/button-bar.js
  init_lit();
  init_decorators();

  // node_modules/@nldd/design-system/dist/components/actions/button-bar/button-bar.styles.js
  init_lit();
  var buttonBarStyles = i`


	/* # Host */

	:host {
		--_corner-radius: var(--semantics-controls-md-corner-radius);
		--_background-color: var(--semantics-buttons-neutral-tinted-background-color);
		--_size: var(--semantics-controls-md-min-size);
		--_divider-color: var(--semantics-buttons-neutral-tinted-divider-color);
		--_highlight-border-color: var(--semantics-buttons-neutral-tinted-highlight-border-color);
		--_divider-length: var(--semantics-buttons-md-divider-length);

		display: inline-flex;
		isolation: isolate;
	}

	:host([size="xs"]) {
		--_corner-radius: var(--semantics-controls-xs-corner-radius);
		--_size: var(--semantics-controls-xs-min-size);
		--_divider-length: var(--semantics-buttons-xs-divider-length);
	}

	:host([size="sm"]) {
		--_corner-radius: var(--semantics-controls-sm-corner-radius);
		--_size: var(--semantics-controls-sm-min-size);
		--_divider-length: var(--semantics-buttons-sm-divider-length);
	}

	:host([size="lg"]) {
		--_corner-radius: var(--semantics-controls-lg-corner-radius);
		--_size: var(--semantics-controls-lg-min-size);
		--_divider-length: var(--semantics-buttons-lg-divider-length);
	}

	/* ## Accent Filled (Primary) */

	:host([variant="accent-filled"]),
	:host([variant="primary"]) {
		--_background-color: var(--semantics-buttons-accent-filled-background-color);
		--_divider-color: var(--semantics-buttons-accent-filled-divider-color);
		--_highlight-border-color: var(--semantics-buttons-accent-filled-highlight-border-color);
	}

	:host([variant="neutral-base"]) {
		--_background-color: var(--semantics-buttons-neutral-base-background-color);
		--_divider-color: var(--semantics-buttons-neutral-base-divider-color);
		--_highlight-border-color: var(--semantics-buttons-neutral-base-highlight-border-color);
	}

	:host([hidden]) {
		display: none;
	}

	:host([disabled]) {
		opacity: var(--primitives-opacity-disabled);
		pointer-events: none;
	}

	:host([disabled]) ::slotted(nldd-button),
	:host([disabled]) ::slotted(nldd-icon-button) {
		opacity: 1;
	}


	/* # Block */

	.button-bar {
		display: flex;
		position: relative;
		border-radius: var(--_corner-radius);
		background-color: var(--_background-color);
		height: var(--_size);
		flex-direction: row;
		justify-content: center;
		align-items: center;
	}

	.button-bar::after {
		content: '';
		position: absolute;
		inset: 0;
		border-radius: inherit;
		box-shadow: inset 0 0 0 var(--primitives-border-width-thin) var(--_highlight-border-color);
		pointer-events: none;
	}


	/* # Elements */

	.button-bar__divider {
		display: flex;
		height: var(--_size);
		align-items: center;
		justify-content: center;
		flex-shrink: 0;
	}

	.button-bar__divider-line {
		background-color: var(--_divider-color);
		width: var(--semantics-dividers-thickness);
		height: var(--_divider-length);
	}

	::slotted([data-focused]) {
		position: relative;
		z-index: 1;
	}
`;

  // node_modules/@nldd/design-system/dist/components/actions/button-bar/button-bar.template.js
  init_lit();

  // node_modules/lit-html/directives/repeat.js
  init_lit_html();
  init_directive();

  // node_modules/lit-html/directive-helpers.js
  init_lit_html();
  var { I: t5 } = j;
  var i6 = (o10) => o10;
  var s4 = () => document.createComment("");
  var v2 = (o10, n7, e9) => {
    const l4 = o10._$AA.parentNode, d3 = void 0 === n7 ? o10._$AB : n7._$AA;
    if (void 0 === e9) {
      const i8 = l4.insertBefore(s4(), d3), n8 = l4.insertBefore(s4(), d3);
      e9 = new t5(i8, n8, o10, o10.options);
    } else {
      const t6 = e9._$AB.nextSibling, n8 = e9._$AM, c6 = n8 !== o10;
      if (c6) {
        let t7;
        e9._$AQ?.(o10), e9._$AM = o10, void 0 !== e9._$AP && (t7 = o10._$AU) !== n8._$AU && e9._$AP(t7);
      }
      if (t6 !== d3 || c6) {
        let o11 = e9._$AA;
        for (; o11 !== t6; ) {
          const t7 = i6(o11).nextSibling;
          i6(l4).insertBefore(o11, d3), o11 = t7;
        }
      }
    }
    return e9;
  };
  var u3 = (o10, t6, i8 = o10) => (o10._$AI(t6, i8), o10);
  var m2 = {};
  var p3 = (o10, t6 = m2) => o10._$AH = t6;
  var M2 = (o10) => o10._$AH;
  var h3 = (o10) => {
    o10._$AR(), o10._$AA.remove();
  };

  // node_modules/lit-html/directives/repeat.js
  var u4 = (e9, s6, t6) => {
    const r6 = /* @__PURE__ */ new Map();
    for (let l4 = s6; l4 <= t6; l4++) r6.set(e9[l4], l4);
    return r6;
  };
  var c4 = e6(class extends i5 {
    constructor(e9) {
      if (super(e9), e9.type !== t4.CHILD) throw Error("repeat() can only be used in text expressions");
    }
    dt(e9, s6, t6) {
      let r6;
      void 0 === t6 ? t6 = s6 : void 0 !== s6 && (r6 = s6);
      const l4 = [], o10 = [];
      let i8 = 0;
      for (const s7 of e9) l4[i8] = r6 ? r6(s7, i8) : i8, o10[i8] = t6(s7, i8), i8++;
      return { values: o10, keys: l4 };
    }
    render(e9, s6, t6) {
      return this.dt(e9, s6, t6).values;
    }
    update(s6, [t6, r6, c6]) {
      const d3 = M2(s6), { values: p4, keys: a4 } = this.dt(t6, r6, c6);
      if (!Array.isArray(d3)) return this.ut = a4, p4;
      const h4 = this.ut ??= [], v3 = [];
      let m3, y3, x2 = 0, j2 = d3.length - 1, k2 = 0, w2 = p4.length - 1;
      for (; x2 <= j2 && k2 <= w2; ) if (null === d3[x2]) x2++;
      else if (null === d3[j2]) j2--;
      else if (h4[x2] === a4[k2]) v3[k2] = u3(d3[x2], p4[k2]), x2++, k2++;
      else if (h4[j2] === a4[w2]) v3[w2] = u3(d3[j2], p4[w2]), j2--, w2--;
      else if (h4[x2] === a4[w2]) v3[w2] = u3(d3[x2], p4[w2]), v2(s6, v3[w2 + 1], d3[x2]), x2++, w2--;
      else if (h4[j2] === a4[k2]) v3[k2] = u3(d3[j2], p4[k2]), v2(s6, d3[x2], d3[j2]), j2--, k2++;
      else if (void 0 === m3 && (m3 = u4(a4, k2, w2), y3 = u4(h4, x2, j2)), m3.has(h4[x2])) if (m3.has(h4[j2])) {
        const e9 = y3.get(a4[k2]), t7 = void 0 !== e9 ? d3[e9] : null;
        if (null === t7) {
          const e10 = v2(s6, d3[x2]);
          u3(e10, p4[k2]), v3[k2] = e10;
        } else v3[k2] = u3(t7, p4[k2]), v2(s6, d3[x2], t7), d3[e9] = null;
        k2++;
      } else h3(d3[j2]), j2--;
      else h3(d3[x2]), x2++;
      for (; k2 <= w2; ) {
        const e9 = v2(s6, v3[w2 + 1]);
        u3(e9, p4[k2]), v3[k2++] = e9;
      }
      for (; x2 <= j2; ) {
        const e9 = d3[x2++];
        null !== e9 && h3(e9);
      }
      return this.ut = a4, p3(s6, v3), E;
    }
  });

  // node_modules/@nldd/design-system/dist/components/actions/button-bar/button-bar.template.js
  function template8() {
    return b2`
		<div class="button-bar" part="bar" role="group">
			${c4(this._children, (c6) => c6.id, (c6) => renderChild.call(this, c6))}
		</div>
	`;
  }
  function renderChild(child) {
    if (child.type === "divider") {
      return b2`
			<div class="button-bar__divider">
				<div class="button-bar__divider-line"></div>
			</div>
		`;
    }
    return b2`<slot name="child-${child.id}"></slot>`;
  }

  // node_modules/@nldd/design-system/dist/components/actions/button-bar/button-bar.js
  var __decorate14 = function(decorators, target, key, desc) {
    var c6 = arguments.length, r6 = c6 < 3 ? target : desc === null ? desc = Object.getOwnPropertyDescriptor(target, key) : desc, d3;
    if (typeof Reflect === "object" && typeof Reflect.decorate === "function") r6 = Reflect.decorate(decorators, target, key, desc);
    else for (var i8 = decorators.length - 1; i8 >= 0; i8--) if (d3 = decorators[i8]) r6 = (c6 < 3 ? d3(r6) : c6 > 3 ? d3(target, key, r6) : d3(target, key)) || r6;
    return c6 > 3 && r6 && Object.defineProperty(target, key, r6), r6;
  };
  if (!customElements.get("nldd-button-bar-divider")) {
    customElements.define("nldd-button-bar-divider", class extends HTMLElement {
    });
  }
  var BUTTON_TAGS = ["nldd-button", "nldd-icon-button"];
  var NLDDButtonBar = class NLDDButtonBar2 extends i4 {
    constructor() {
      super(...arguments);
      this.size = "md";
      this.variant = "neutral-tinted";
      this.disabled = false;
      this._children = [];
      this._idCounter = 0;
      this._observer = null;
      this._building = false;
      this._individuallyDisabled = /* @__PURE__ */ new WeakSet();
    }
    connectedCallback() {
      super.connectedCallback();
      this._observer = new MutationObserver(() => this._buildChildren());
      this._observer.observe(this, { childList: true });
      this._buildChildren();
      this.addEventListener("focusin", this._handleFocusIn);
      this.addEventListener("focusout", this._handleFocusOut);
    }
    disconnectedCallback() {
      super.disconnectedCallback();
      this._observer?.disconnect();
      this._observer = null;
      this.removeEventListener("focusin", this._handleFocusIn);
      this.removeEventListener("focusout", this._handleFocusOut);
    }
    updated(changedProperties) {
      if (changedProperties.has("size") || changedProperties.has("_children")) {
        this._propagateSize();
      }
      if (changedProperties.has("variant") || changedProperties.has("_children")) {
        this._propagateVariant();
      }
      if (changedProperties.has("disabled")) {
        this._propagateDisabled();
      } else if (changedProperties.has("_children") && this.disabled) {
        Array.from(this.children).filter((el) => BUTTON_TAGS.includes(el.tagName.toLowerCase())).forEach((el) => el.setAttribute("disabled", ""));
      }
    }
    _handleFocusIn(e9) {
      const target = e9.target;
      if (BUTTON_TAGS.includes(target.tagName.toLowerCase())) {
        target.setAttribute("data-focused", "");
      }
    }
    _handleFocusOut(e9) {
      const target = e9.target;
      if (BUTTON_TAGS.includes(target.tagName.toLowerCase())) {
        target.removeAttribute("data-focused");
      }
    }
    _propagateSize() {
      Array.from(this.children).filter((el) => BUTTON_TAGS.includes(el.tagName.toLowerCase())).forEach((el) => el.setAttribute("size", this.size));
    }
    _propagateVariant() {
      Array.from(this.children).filter((el) => BUTTON_TAGS.includes(el.tagName.toLowerCase())).forEach((el) => el.setAttribute("variant", this.variant));
    }
    _propagateDisabled() {
      const buttons = Array.from(this.children).filter((el) => BUTTON_TAGS.includes(el.tagName.toLowerCase()));
      if (this.disabled) {
        buttons.forEach((el) => {
          if (el.hasAttribute("disabled")) {
            this._individuallyDisabled.add(el);
          }
        });
        buttons.forEach((el) => el.setAttribute("disabled", ""));
      } else {
        buttons.forEach((el) => {
          if (!this._individuallyDisabled.has(el)) {
            el.removeAttribute("disabled");
          }
        });
        this._individuallyDisabled = /* @__PURE__ */ new WeakSet();
      }
    }
    _buildChildren() {
      if (this._building)
        return;
      this._building = true;
      this._idCounter = 0;
      this._children = Array.from(this.children).map((el) => {
        const tag = el.tagName.toLowerCase();
        if (tag === "nldd-button-bar-divider") {
          return { type: "divider", id: this._idCounter++ };
        }
        if (BUTTON_TAGS.includes(tag)) {
          el.setAttribute("size", this.size);
          el.setAttribute("variant", this.variant);
          el.setAttribute("no-highlight-border", "");
        }
        const id = this._idCounter++;
        const slotName = `child-${id}`;
        if (el.getAttribute("slot") !== slotName) {
          el.setAttribute("slot", slotName);
        }
        return { type: "button", element: el, id };
      });
      this._building = false;
    }
    render() {
      return template8.call(this);
    }
  };
  NLDDButtonBar.styles = buttonBarStyles;
  __decorate14([
    n4({ type: String, reflect: true })
  ], NLDDButtonBar.prototype, "size", void 0);
  __decorate14([
    n4({ type: String, reflect: true })
  ], NLDDButtonBar.prototype, "variant", void 0);
  __decorate14([
    n4({ type: Boolean, reflect: true })
  ], NLDDButtonBar.prototype, "disabled", void 0);
  __decorate14([
    r5()
  ], NLDDButtonBar.prototype, "_children", void 0);
  NLDDButtonBar = __decorate14([
    t3("nldd-button-bar")
  ], NLDDButtonBar);

  // node_modules/@nldd/design-system/dist/components/actions/toolbar/toolbar.js
  init_lit();
  init_decorators();

  // node_modules/@nldd/design-system/dist/components/actions/toolbar/toolbar.styles.js
  init_lit();
  init_slotted_reset();
  var toolbarStyles = i`


	/* # Host */

	:host {
		--_gap: var(--components-toolbar-md-gap);
		/* --_width..--_overflow-button-width: measured + set by toolbar.ts; 0px is a valid pre-measurement placeholder */
		--_width: 0px;
		--_start-width: 0px;
		--_center-width: 0px;
		--_end-width: 0px;
		--_overflow-button-width: 0px;
		--_label-margin-top: var(--primitives-space-4);

		${inheritedTextReset}
		box-sizing: border-box;
		display: block;
	}

	:host([size="sm"]) {
		--_gap: var(--components-toolbar-sm-gap);
	}

	:host([size="lg"]) {
		--_gap: var(--components-toolbar-lg-gap);
	}

	:host([hidden]) {
		display: none;
	}


	/* # Block */

	.toolbar {
		display: flex;
		width: 100%;
		flex-direction: row;
		gap: var(--_gap);
		align-items: center;
	}


	/* # Items */

	.toolbar__items {
		display: flex;
		min-width: 0;
		flex-direction: row;
		flex-grow: 1;
		flex-shrink: 1;
		flex-basis: 0;
		gap: var(--_gap);
		align-items: flex-start;
	}


	/* # Spacers */

	.toolbar__flexible-spacer {
		margin-left: calc(-1 * var(--_gap));
		flex-grow: 1;
		flex-shrink: 1;
	}

	.toolbar__center-fill {
		display: flex;
		min-width: 0;
		flex-direction: row;
		flex-grow: 1;
		flex-shrink: 1;
		align-items: flex-start;
		justify-content: center;
	}

	.toolbar__left-spacer {
		margin-right: calc(-1 * var(--_gap));
		min-width: 0;
		flex-grow: 0;
		flex-shrink: 1;
		flex-basis: calc(
			var(--_width) / 2
			- var(--_start-width)
			- var(--_center-width) / 2
			- var(--_gap)
		);
	}

	.toolbar__right-spacer {
		margin-left: calc(-1 * var(--_gap));
		min-width: 0;
		flex-grow: 0;
		flex-shrink: 1;
		flex-basis: calc(
			var(--_width) / 2
			- var(--_end-width)
			- var(--_center-width) / 2
			- var(--_gap)
			- var(--_overflow-button-width)
		);
	}


	/* # Overflow button */

	.toolbar__overflow-button {
		display: inline-flex;
		flex-direction: column;
		flex-grow: 0;
		flex-shrink: 0;
		align-items: center;
	}

	.toolbar__overflow-button.is-hidden {
		display: none;
	}

	.toolbar__overflow-button-label {
		display: none;
		margin-top: var(--_label-margin-top);
		color: var(--semantics-content-color);
		font: var(--primitives-font-body-xs-regular-flat);
		white-space: nowrap;
	}

	:host([show-item-labels]) .toolbar__overflow-button-label {
		display: block;
	}
`;
  var toolbarItemStyles = i`


	/* # Host */

	:host {
		--_item-max-width: none;
		--_item-min-width: 0px;
		--_item-width: auto;
		--_label-margin-top: var(--primitives-space-4);

		display: inline-flex;
		max-width: var(--_item-max-width);
		flex-direction: column;
		flex-grow: 0;
		flex-shrink: 0;
		align-items: center;
	}

	:host([fluid]) {
		min-width: var(--_item-min-width);
		flex-shrink: 1;
		flex-basis: var(--_item-width);
	}

	:host([solo-fluid]) {
		min-width: 0;
		flex-grow: 1;
		flex-shrink: 1;
		flex-basis: 0;
	}

	:host([hidden]) {
		display: none;
	}


	/* # Block */

	.toolbar__item-content {
		display: inline-flex;
		width: 100%;
		align-items: center;
		justify-content: center;
	}

	:host([fluid]) .toolbar__item-content ::slotted(*),
	:host([solo-fluid]) .toolbar__item-content ::slotted(*) {
		width: 100%;
	}


	/* # Elements */

	.toolbar__item-label {
		display: none;
		margin-top: var(--_label-margin-top);
		color: var(--semantics-content-color);
		font: var(--primitives-font-body-xs-regular-flat);
		white-space: nowrap;
	}

	:host([show-item-labels]) .toolbar__item-label {
		display: block;
	}


	/* # Slots */

	slot[name="overflow"] {
		display: none;
	}
`;
  var toolbarTitleStyles = i`


	/* # Host */

	:host {
		--_title-group-min-width: 200px;
		--_title-width: auto;
		--_title-max-width: none;
		--_title-group-height: var(--semantics-controls-md-min-size);
		--_title-font: var(--primitives-font-body-lg-medium-flat);
		--_subtitle-font: var(--primitives-font-body-xs-regular-flat);

		${inheritedTextReset}
		display: inline-flex;
		min-width: var(--_title-group-min-width);
		max-width: var(--_title-max-width);
		height: var(--_title-group-height);
		overflow: hidden;
		flex-direction: column;
		flex-shrink: 1;
		flex-basis: var(--_title-width);
		justify-content: center;
	}

	:host([size="sm"]) {
		--_title-group-height: var(--semantics-controls-sm-min-size);
		--_title-font: var(--primitives-font-body-sm-medium-flat);
		--_subtitle-font: var(--primitives-font-body-xxs-regular-flat);
	}

	:host([size="lg"]) {
		--_title-group-height: var(--semantics-controls-lg-min-size);
	}

	:host([solo-fluid]) {
		min-width: 0;
		flex-grow: 1;
		flex-shrink: 1;
		flex-basis: 0;
	}

	:host([align="center"]) {
		align-items: center;
	}

	:host([align="left"]) {
		align-items: flex-start;
	}

	:host([hidden]) {
		display: none;
	}


	/* # Elements */

	/* text-align lives on the text elements, not :host: the inheritedTextReset on
	   :host locks text-align to start, so a host-level override would need
	   !important. The title/subtitle are shadow elements outside that reset. */
	.toolbar__title {
		margin: 0;
		max-width: 100%;
		overflow: hidden;
		color: var(--semantics-content-color);
		font: var(--_title-font);
		text-overflow: ellipsis;
		white-space: nowrap;
	}

	:host([align="center"]) .toolbar__title {
		text-align: center;
	}

	:host([align="left"]) .toolbar__title {
		text-align: left;
	}

	.toolbar__subtitle {
		margin: 0;
		max-width: 100%;
		overflow: hidden;
		color: var(--semantics-content-secondary-color);
		font: var(--_subtitle-font);
		text-overflow: ellipsis;
		white-space: nowrap;
	}

	:host([align="center"]) .toolbar__subtitle {
		text-align: center;
	}

	:host([align="left"]) .toolbar__subtitle {
		text-align: left;
	}
`;

  // node_modules/@nldd/design-system/dist/components/actions/toolbar/toolbar.template.js
  init_lit();
  init_icon_button();
  function toolbarItemTemplate(component) {
    return b2`
		<div class="toolbar__item-content">
			<slot></slot>
		</div>
		${component.label ? b2`<span class="toolbar__item-label">${component.label}</span>` : A}
		<slot name="overflow"></slot>
	`;
  }
  function toolbarTitleTemplate(component) {
    return b2`
		${component.text ? b2`<p class="toolbar__title">${component.text}</p>` : A}
		${component.supportingText ? b2`<p class="toolbar__subtitle">${component.supportingText}</p>` : A}
	`;
  }
  function template9(component, hasCenterChildren, leftSpacerZero, rightSpacerZero, isSoloFluidItem, hasOverflow, menuOpen, label, centerOnly, t6) {
    return b2`
		<div class="toolbar"
			role="toolbar"
			aria-label=${label || A}
		>
			<div class="toolbar__items">
				<slot name="start"></slot>
				${hasCenterChildren ? b2`
					${centerOnly ? b2`
						<div class="toolbar__center-fill">
							<slot name="center"></slot>
						</div>
					` : b2`
						${leftSpacerZero ? A : b2`<div class="toolbar__left-spacer"></div>`}
						<slot name="center"></slot>
						${rightSpacerZero ? A : b2`<div class="toolbar__right-spacer"></div>`}
					`}
				` : isSoloFluidItem ? A : b2`
					<div class="toolbar__flexible-spacer"></div>
				`}
				<slot name="end"></slot>
			</div>
			<div class="toolbar__overflow-button ${hasOverflow ? "" : "is-hidden"}">
				<nldd-icon-button size=${component.size}
					icon="ellipsis"
					text=${t6("components.toolbar.overflow-action")}
					tooltip-timing="never"
					popup-type="menu"
					?expanded=${menuOpen}
				></nldd-icon-button>
				<span class="toolbar__overflow-button-label">${t6("components.toolbar.overflow-action")}</span>
			</div>
		</div>
	`;
  }

  // node_modules/@nldd/design-system/dist/components/actions/toolbar/toolbar.i18n.js
  var nlddToolbarTranslations = {
    "components.toolbar.overflow-action": "Meer"
  };

  // node_modules/@nldd/design-system/dist/components/actions/toolbar/toolbar.js
  var __decorate15 = function(decorators, target, key, desc) {
    var c6 = arguments.length, r6 = c6 < 3 ? target : desc === null ? desc = Object.getOwnPropertyDescriptor(target, key) : desc, d3;
    if (typeof Reflect === "object" && typeof Reflect.decorate === "function") r6 = Reflect.decorate(decorators, target, key, desc);
    else for (var i8 = decorators.length - 1; i8 >= 0; i8--) if (d3 = decorators[i8]) r6 = (c6 < 3 ? d3(r6) : c6 > 3 ? d3(target, key, r6) : d3(target, key)) || r6;
    return c6 > 3 && r6 && Object.defineProperty(target, key, r6), r6;
  };
  var NLDDToolbarItem = class NLDDToolbarItem2 extends i4 {
    constructor() {
      super(...arguments);
      this.width = "";
      this.minWidth = "";
      this.maxWidth = "";
      this.label = "";
      this.priority = 0;
      this.size = "md";
      this.showItemLabels = false;
    }
    // Layout state — `fluid`, `solo-fluid` and `hidden` — is owned by
    // nldd-toolbar and toggled directly as attributes during measurement
    // (synchronous, so the host box reflows immediately). They are not
    // reactive properties: Lit would reflect them asynchronously, which would
    // desync the attribute from the synchronous getBoundingClientRect reads.
    render() {
      return toolbarItemTemplate(this);
    }
  };
  NLDDToolbarItem.styles = toolbarItemStyles;
  __decorate15([
    n4({ type: String })
  ], NLDDToolbarItem.prototype, "width", void 0);
  __decorate15([
    n4({ type: String, attribute: "min-width" })
  ], NLDDToolbarItem.prototype, "minWidth", void 0);
  __decorate15([
    n4({ type: String, attribute: "max-width" })
  ], NLDDToolbarItem.prototype, "maxWidth", void 0);
  __decorate15([
    n4({ type: String })
  ], NLDDToolbarItem.prototype, "label", void 0);
  __decorate15([
    n4({ type: Number })
  ], NLDDToolbarItem.prototype, "priority", void 0);
  __decorate15([
    n4({ type: String, reflect: true })
  ], NLDDToolbarItem.prototype, "size", void 0);
  __decorate15([
    n4({ type: Boolean, reflect: true, attribute: "show-item-labels" })
  ], NLDDToolbarItem.prototype, "showItemLabels", void 0);
  NLDDToolbarItem = __decorate15([
    t3("nldd-toolbar-item")
  ], NLDDToolbarItem);
  var NLDDToolbarTitle = class NLDDToolbarTitle2 extends i4 {
    constructor() {
      super(...arguments);
      this.text = "";
      this.supportingText = "";
      this.align = "left";
      this.width = "";
      this.minWidth = "";
      this.maxWidth = "";
      this.size = "md";
    }
    // Layout state — `solo-fluid` and `hidden` — is owned by nldd-toolbar and
    // toggled directly as attributes during measurement (synchronous). Not a
    // reactive property for the same reflection-timing reason as the item.
    // willUpdate (not updated) so the size CSS variables are written before the
    // toolbar measures the title with getBoundingClientRect() on the same frame.
    willUpdate(changedProperties) {
      if (changedProperties.has("minWidth"))
        this._reflectSizeVar("--_title-group-min-width", this.minWidth);
      if (changedProperties.has("width"))
        this._reflectSizeVar("--_title-width", this.width);
      if (changedProperties.has("maxWidth"))
        this._reflectSizeVar("--_title-max-width", this.maxWidth);
    }
    _reflectSizeVar(prop, value) {
      if (value) {
        this.style.setProperty(prop, value);
      } else {
        this.style.removeProperty(prop);
      }
    }
    render() {
      return toolbarTitleTemplate(this);
    }
  };
  NLDDToolbarTitle.styles = toolbarTitleStyles;
  __decorate15([
    n4({ type: String })
  ], NLDDToolbarTitle.prototype, "text", void 0);
  __decorate15([
    n4({ type: String, attribute: "supporting-text" })
  ], NLDDToolbarTitle.prototype, "supportingText", void 0);
  __decorate15([
    n4({ type: String, reflect: true })
  ], NLDDToolbarTitle.prototype, "align", void 0);
  __decorate15([
    n4({ type: String })
  ], NLDDToolbarTitle.prototype, "width", void 0);
  __decorate15([
    n4({ type: String, attribute: "min-width" })
  ], NLDDToolbarTitle.prototype, "minWidth", void 0);
  __decorate15([
    n4({ type: String, attribute: "max-width" })
  ], NLDDToolbarTitle.prototype, "maxWidth", void 0);
  __decorate15([
    n4({ type: String, reflect: true })
  ], NLDDToolbarTitle.prototype, "size", void 0);
  NLDDToolbarTitle = __decorate15([
    t3("nldd-toolbar-title")
  ], NLDDToolbarTitle);
  var NLDDToolbar = class NLDDToolbar2 extends i4 {
    constructor() {
      super(...arguments);
      this.size = "md";
      this.showItemLabels = false;
      this.label = "";
      this.translations = {};
      this._menuOpen = false;
      this._startChildren = [];
      this._centerChildren = [];
      this._endChildren = [];
      this._overflowIds = /* @__PURE__ */ new Set();
      this._leftSpacerZero = false;
      this._rightSpacerZero = false;
      this._pinnedOverflowItems = [];
      this._childIds = /* @__PURE__ */ new WeakMap();
      this._idCounter = 0;
      this._itemWidths = /* @__PURE__ */ new Map();
      this._observer = null;
      this._resizeObserver = null;
      this._menu = null;
      this._isMeasuring = false;
      this._isBuilding = false;
      this._hasMeasured = false;
      this._prioritizedItemsCache = null;
    }
    // — i18n —————————————————————————————————————————————————————————————————
    _t(key) {
      return this.translations[key] ?? nlddToolbarTranslations[key];
    }
    _getId(el) {
      if (!this._childIds.has(el)) {
        this._childIds.set(el, this._idCounter++);
      }
      return this._childIds.get(el);
    }
    connectedCallback() {
      super.connectedCallback();
      this._observer = new MutationObserver((mutations) => {
        if (this._isBuilding)
          return;
        const onlyInternalMoves = mutations.every((m3) => {
          if (m3.type === "attributes") {
            const tag = m3.target.tagName.toLowerCase();
            return tag !== "nldd-toolbar-item" && tag !== "nldd-toolbar-title";
          }
          return false;
        });
        if (onlyInternalMoves)
          return;
        this._buildChildren();
      });
      setTimeout(() => this._buildChildren(), 0);
      this._createMenu();
    }
    disconnectedCallback() {
      super.disconnectedCallback();
      this._observer?.disconnect();
      this._observer = null;
      this._resizeObserver?.disconnect();
      this._resizeObserver = null;
      this._menu?.remove();
      this._menu = null;
    }
    firstUpdated() {
      this._resizeObserver = new ResizeObserver(() => {
        if (this._isMeasuring)
          return;
        if (this._menuOpen) {
          this._menu?.hidePopover();
        }
        this._measureAndUpdate();
      });
      this._resizeObserver.observe(this);
      this._syncHosts();
    }
    updated(changedProperties) {
      if (changedProperties.has("size") || changedProperties.has("showItemLabels")) {
        this._syncHosts();
      }
      if (changedProperties.has("_startChildren") || changedProperties.has("_centerChildren") || changedProperties.has("_endChildren")) {
        this.updateComplete.then(() => this._measureAndUpdate());
      }
      if (changedProperties.has("_overflowIds") || changedProperties.has("_pinnedOverflowItems")) {
        this._syncMenuItems();
        this._syncMenuAnchor();
        if (!this._hasMeasured) {
          this.updateComplete.then(() => this._updateAreaVars());
        }
      }
    }
    _createMenu() {
      if (this._menu)
        return;
      const menu = document.createElement("nldd-menu");
      menu.setAttribute("placement", "bottom-end");
      menu.id = `nldd-toolbar-overflow-menu-${this._idCounter++}`;
      menu.addEventListener("toggle", (event) => {
        this._menuOpen = event.newState === "open";
      });
      document.body.appendChild(menu);
      this._menu = menu;
    }
    _syncMenuAnchor() {
      if (!this._menu)
        return;
      const overflowButton = this.shadowRoot?.querySelector(".toolbar__overflow-button nldd-icon-button");
      if (overflowButton) {
        this._menu.anchorElement = overflowButton;
        if ("popoverTargetElement" in overflowButton) {
          overflowButton.popoverTargetElement = this._menu;
        }
        if ("popoverTargetAction" in overflowButton && overflowButton.popoverTargetAction === "toggle") {
          overflowButton.popoverTargetAction = "show";
        }
      }
    }
    /**
     * Syncs overflow menu items by cloning the original `nldd-menu-item` elements.
     * Note: `cloneNode` does not copy event listeners added via `addEventListener`.
     * The `select` event works correctly since it is dispatched by `nldd-menu-item` internally.
     * Consumers should avoid adding extra listeners directly on overflow `nldd-menu-item` elements.
     */
    _syncMenuItems() {
      if (!this._menu)
        return;
      this._menu.innerHTML = "";
      const prioritized = [...this._getPrioritizedItems()].reverse();
      prioritized.forEach((child) => {
        if (!this._overflowIds.has(child.id))
          return;
        if (child.overflowItems.length === 0)
          return;
        child.overflowItems.forEach((el) => {
          const clone = el.cloneNode(true);
          clone.removeAttribute("slot");
          this._menu.appendChild(clone);
        });
      });
      if (this._pinnedOverflowItems.length > 0) {
        this._pinnedOverflowItems.forEach((el) => {
          const clone = el.cloneNode(true);
          clone.removeAttribute("slot");
          this._menu.appendChild(clone);
        });
      }
    }
    /**
     * Syncs declarative state onto the slotted item and title hosts: size,
     * show-item-labels, the base fluid flag and the fluid width custom
     * properties. The parent owns layout decisions; each host owns its own box
     * and rendering. Attributes are set directly (not via reactive properties)
     * so the host box reflows synchronously before the next measurement.
     */
    _syncHosts() {
      this._allHostChildren().forEach((child) => {
        if (child.type === "item") {
          const host = child.element;
          host.setAttribute("size", this.size);
          host.toggleAttribute("show-item-labels", this.showItemLabels);
          host.toggleAttribute("fluid", child.isFluid);
          if (child.isFluid && child.minWidth) {
            host.style.setProperty("--_item-min-width", child.minWidth);
          } else {
            host.style.removeProperty("--_item-min-width");
          }
          if (child.isFluid && child.width) {
            host.style.setProperty("--_item-width", child.width);
          } else {
            host.style.removeProperty("--_item-width");
          }
          if (child.isFluid && child.maxWidth) {
            host.style.setProperty("--_item-max-width", child.maxWidth);
          } else {
            host.style.removeProperty("--_item-max-width");
          }
          Array.from(host.children).forEach((inner) => {
            if (inner.getAttribute("slot") !== "overflow") {
              inner.setAttribute("size", this.size);
            }
          });
        } else if (child.type === "title") {
          const host = child.element;
          host.setAttribute("size", this.size);
        }
      });
    }
    _getPrioritizedItems() {
      if (this._prioritizedItemsCache)
        return this._prioritizedItemsCache;
      const endItems = this._endChildren.filter((c6) => c6.type === "item");
      const startItems = this._startChildren.filter((c6) => c6.type === "item");
      const centerItems = this._centerChildren.filter((c6) => c6.type === "item");
      const result = [
        ...endItems.map((item, index) => ({ item, areaOrder: 0, index })),
        ...centerItems.map((item, index) => ({ item, areaOrder: 1, index })),
        ...startItems.map((item, index) => ({ item, areaOrder: 2, index }))
      ].sort((a4, b3) => {
        if (a4.item.priority !== b3.item.priority)
          return a4.item.priority - b3.item.priority;
        if (a4.areaOrder !== b3.areaOrder)
          return a4.areaOrder - b3.areaOrder;
        return b3.index - a4.index;
      }).map(({ item }) => item);
      this._prioritizedItemsCache = result;
      return result;
    }
    _allHostChildren() {
      return [...this._startChildren, ...this._centerChildren, ...this._endChildren];
    }
    _hostFor(id) {
      const child = this._allHostChildren().find((c6) => c6.id === id);
      return child && (child.type === "item" || child.type === "title") ? child.element : null;
    }
    _measureItemWidths() {
      this._allHostChildren().forEach((child) => {
        if (child.type === "item" || child.type === "title") {
          const host = child.element;
          this._itemWidths.set(child.id, host.getBoundingClientRect().width);
        }
      });
    }
    _computeAreaWidth(children, itemGap) {
      const visible = children.filter((c6) => !this._overflowIds.has(c6.id));
      const gaps = Math.max(0, visible.length - 1) * itemGap;
      const itemsWidth = visible.reduce((sum, child) => {
        if (child.type === "item" || child.type === "title") {
          return sum + (this._itemWidths.get(child.id) ?? 0);
        }
        return sum;
      }, 0);
      return gaps + itemsWidth;
    }
    _computeSpacerZeros(hostWidth, itemGap, overflowButtonWidth, startWidth, centerWidth, endWidth) {
      if (startWidth === 0 && endWidth === 0) {
        return { leftZero: true, rightZero: true };
      }
      const leftSpacer = hostWidth / 2 - startWidth - centerWidth / 2 - itemGap;
      const rightSpacer = hostWidth / 2 - endWidth - centerWidth / 2 - itemGap - overflowButtonWidth;
      return {
        leftZero: leftSpacer <= 0,
        rightZero: rightSpacer <= 0
      };
    }
    _updateAreaVars() {
      const itemsEl = this.shadowRoot?.querySelector(".toolbar__items");
      if (!itemsEl)
        return;
      const hostWidth = this.getBoundingClientRect().width;
      const itemGap = parseFloat(getComputedStyle(itemsEl).gap ?? "0");
      const hostGap = parseFloat(getComputedStyle(this).gap ?? "0");
      const overflowButtonContainerEl = this.shadowRoot?.querySelector(".toolbar__overflow-button");
      const overflowButtonEl = this.shadowRoot?.querySelector(".toolbar__overflow-button nldd-icon-button");
      const overflowButtonWidth = overflowButtonContainerEl && !overflowButtonContainerEl.classList.contains("is-hidden") && overflowButtonEl ? overflowButtonEl.getBoundingClientRect().width + hostGap : 0;
      this.style.setProperty("--_overflow-button-width", `${overflowButtonWidth}px`);
      const startWidth = this._computeAreaWidth(this._startChildren, itemGap);
      const centerWidth = this._computeAreaWidth(this._centerChildren, itemGap);
      const endWidth = this._computeAreaWidth(this._endChildren, itemGap);
      this.style.setProperty("--_start-width", `${startWidth}px`);
      this.style.setProperty("--_center-width", `${centerWidth}px`);
      this.style.setProperty("--_end-width", `${endWidth}px`);
      const { leftZero, rightZero } = this._computeSpacerZeros(hostWidth, itemGap, overflowButtonWidth, startWidth, centerWidth, endWidth);
      if (leftZero !== this._leftSpacerZero)
        this._leftSpacerZero = leftZero;
      if (rightZero !== this._rightSpacerZero)
        this._rightSpacerZero = rightZero;
    }
    _measureAndUpdate() {
      if (this._isMeasuring)
        return;
      const itemsEl = this.shadowRoot?.querySelector(".toolbar__items");
      if (!itemsEl)
        return;
      this._isMeasuring = true;
      const hostWidth = this.getBoundingClientRect().width;
      this.style.setProperty("--_width", `${hostWidth}px`);
      this._measureOverflow(itemsEl);
      this._hasMeasured = true;
      this._isMeasuring = false;
    }
    _measureOverflow(itemsEl) {
      const overflowButtonEl = this.shadowRoot?.querySelector(".toolbar__overflow-button");
      const allChildren = this._allHostChildren();
      const itemChildren = allChildren.filter((c6) => c6.type === "item");
      const titleChildren = allChildren.filter((c6) => c6.type === "title");
      itemChildren.forEach((child) => {
        const host = child.element;
        host.hidden = false;
        if (host.hasAttribute("solo-fluid")) {
          host.removeAttribute("solo-fluid");
          host.toggleAttribute("fluid", child.isFluid);
          if (child.isFluid && child.minWidth) {
            host.style.setProperty("--_item-min-width", child.minWidth);
          }
        }
      });
      titleChildren.forEach((child) => {
        const host = child.element;
        if (host.hasAttribute("solo-fluid")) {
          host.removeAttribute("solo-fluid");
          host.style.removeProperty("min-width");
        }
      });
      if (this._pinnedOverflowItems.length > 0) {
        overflowButtonEl?.classList.remove("is-hidden");
      } else {
        overflowButtonEl?.classList.add("is-hidden");
      }
      void itemsEl.offsetWidth;
      const centerOnly = this._startChildren.length === 0 && this._endChildren.length === 0 && this._centerChildren.length > 0;
      const isOverflowing = () => itemsEl.scrollWidth > itemsEl.clientWidth + 1;
      if (!isOverflowing()) {
        if (this._overflowIds.size > 0) {
          this._overflowIds = /* @__PURE__ */ new Set();
        }
        this._promoteSoloFluid(itemsEl, itemChildren, titleChildren, centerOnly);
        this._measureItemWidths();
        this._updateAreaVars();
        return;
      }
      overflowButtonEl?.classList.remove("is-hidden");
      void itemsEl.offsetWidth;
      const prioritized = this._getPrioritizedItems();
      const newOverflowIds = /* @__PURE__ */ new Set();
      const priorityGroups = groupForOverflow(prioritized);
      for (const group of priorityGroups) {
        if (!isOverflowing())
          break;
        if (group.some((c6) => c6.isFluid)) {
          const groupIds = new Set(group.map((c6) => c6.id));
          const nonFluidVisibleOutside = itemChildren.filter((c6) => {
            const host = c6.element;
            return !host.hidden && !host.hasAttribute("fluid") && !host.hasAttribute("solo-fluid") && !groupIds.has(c6.id);
          });
          if (nonFluidVisibleOutside.length === 0)
            break;
        }
        for (const child of group) {
          newOverflowIds.add(child.id);
          child.element.hidden = true;
        }
        void itemsEl.offsetWidth;
      }
      if (newOverflowIds.size === 0 && this._pinnedOverflowItems.length === 0) {
        overflowButtonEl?.classList.add("is-hidden");
      }
      this._promoteSoloFluid(itemsEl, itemChildren, titleChildren, centerOnly);
      const changed = newOverflowIds.size !== this._overflowIds.size || [...newOverflowIds].some((id) => !this._overflowIds.has(id));
      if (changed) {
        this._overflowIds = newOverflowIds;
      }
      this._measureItemWidths();
      this._updateAreaVars();
    }
    /**
     * Promotes a lone remaining fluid item — or a lone title when no items are
     * visible — to the solo-fluid state so it grows to fill the row. In
     * center-only mode the center-fill wrapper already grows the items, so item
     * promotion is skipped there (matching the previous template behaviour).
     */
    _promoteSoloFluid(itemsEl, itemChildren, titleChildren, centerOnly) {
      const remainingVisible = itemChildren.filter((c6) => !c6.element.hidden);
      if (!centerOnly && remainingVisible.length === 1 && remainingVisible[0].element.hasAttribute("fluid")) {
        const host = remainingVisible[0].element;
        host.removeAttribute("fluid");
        host.setAttribute("solo-fluid", "");
        host.style.removeProperty("--_item-min-width");
        void itemsEl.offsetWidth;
      }
      const remainingTitles = titleChildren.filter((c6) => !c6.element.hidden);
      if (remainingVisible.length === 0 && remainingTitles.length === 1) {
        const host = remainingTitles[0].element;
        host.setAttribute("solo-fluid", "");
        host.style.setProperty("min-width", "0px");
        void itemsEl.offsetWidth;
      }
    }
    _buildChildrenForSlot(slotName) {
      return Array.from(this.children).filter((el) => el.getAttribute("slot") === slotName).map((el) => {
        const tag = el.tagName.toLowerCase();
        if (tag === "nldd-toolbar-title") {
          const id2 = this._getId(el);
          return {
            type: "title",
            element: el,
            minWidth: el.getAttribute("min-width") ?? "200px",
            id: id2
          };
        }
        if (tag === "nldd-toolbar-item") {
          const id2 = this._getId(el);
          const label = el.getAttribute("label") ?? "";
          const priority = parseInt(el.getAttribute("priority") ?? "0", 10);
          const hasPriority = el.hasAttribute("priority");
          const minWidth = el.getAttribute("min-width") ?? "";
          const maxWidth = el.getAttribute("max-width") ?? "";
          const width = el.getAttribute("width") ?? "";
          const isFluid = !!(minWidth || maxWidth || width);
          const overflowItems = Array.from(el.children).filter((child) => {
            const childTag = child.tagName.toLowerCase();
            return childTag === "nldd-menu-item" || childTag === "nldd-menu-divider" || childTag === "nldd-menu-group";
          });
          overflowItems.forEach((child) => child.setAttribute("slot", "overflow"));
          return { type: "item", element: el, label, id: id2, priority, hasPriority, overflowItems, minWidth, maxWidth, width, isFluid };
        }
        const id = this._getId(el);
        return { type: "other", element: el, id };
      });
    }
    _buildPinnedOverflowItems() {
      this._pinnedOverflowItems = Array.from(this.children).filter((el) => {
        const tag = el.tagName.toLowerCase();
        return el.getAttribute("slot") === "overflow" && (tag === "nldd-menu-item" || tag === "nldd-menu-divider" || tag === "nldd-menu-group");
      });
    }
    _buildChildren() {
      if (this._isBuilding)
        return;
      this._isBuilding = true;
      this._observer?.disconnect();
      this._startChildren = this._buildChildrenForSlot("start");
      this._centerChildren = this._buildChildrenForSlot("center");
      this._endChildren = this._buildChildrenForSlot("end");
      this._buildPinnedOverflowItems();
      this._prioritizedItemsCache = null;
      this._syncHosts();
      const itemAttributeFilter = ["label", "priority", "min-width", "max-width", "width", "text", "disabled", "selected", "type"];
      this._observer?.observe(this, { childList: true, attributes: true, subtree: true, attributeFilter: itemAttributeFilter });
      this._isBuilding = false;
    }
    render() {
      const allChildren = this._allHostChildren();
      const visibleNonDivider = allChildren.filter((c6) => !this._overflowIds.has(c6.id));
      const isSoloFluid = visibleNonDivider.length === 1 && (visibleNonDivider[0].type === "title" || visibleNonDivider[0].type === "item" && visibleNonDivider[0].isFluid);
      return template9(this, this._centerChildren.length > 0, this._leftSpacerZero, this._rightSpacerZero, isSoloFluid, this._pinnedOverflowItems.length > 0 || this._overflowIds.size > 0, this._menuOpen, this.label, this._startChildren.length === 0 && this._endChildren.length === 0 && this._centerChildren.length > 0, (key) => this._t(key));
    }
  };
  NLDDToolbar.styles = toolbarStyles;
  __decorate15([
    n4({ type: String, reflect: true })
  ], NLDDToolbar.prototype, "size", void 0);
  __decorate15([
    n4({ type: Boolean, reflect: true, attribute: "show-item-labels" })
  ], NLDDToolbar.prototype, "showItemLabels", void 0);
  __decorate15([
    n4({ type: String, reflect: true })
  ], NLDDToolbar.prototype, "label", void 0);
  __decorate15([
    n4({ type: Object })
  ], NLDDToolbar.prototype, "translations", void 0);
  __decorate15([
    r5()
  ], NLDDToolbar.prototype, "_menuOpen", void 0);
  __decorate15([
    r5()
  ], NLDDToolbar.prototype, "_startChildren", void 0);
  __decorate15([
    r5()
  ], NLDDToolbar.prototype, "_centerChildren", void 0);
  __decorate15([
    r5()
  ], NLDDToolbar.prototype, "_endChildren", void 0);
  __decorate15([
    r5()
  ], NLDDToolbar.prototype, "_overflowIds", void 0);
  __decorate15([
    r5()
  ], NLDDToolbar.prototype, "_leftSpacerZero", void 0);
  __decorate15([
    r5()
  ], NLDDToolbar.prototype, "_rightSpacerZero", void 0);
  __decorate15([
    r5()
  ], NLDDToolbar.prototype, "_pinnedOverflowItems", void 0);
  NLDDToolbar = __decorate15([
    t3("nldd-toolbar")
  ], NLDDToolbar);
  function groupForOverflow(items) {
    const groups = [];
    const byPriority = /* @__PURE__ */ new Map();
    for (const child of items) {
      if (!child.hasPriority) {
        groups.push([child]);
        continue;
      }
      const existing = byPriority.get(child.priority);
      if (existing) {
        existing.push(child);
        continue;
      }
      const group = [child];
      byPriority.set(child.priority, group);
      groups.push(group);
    }
    return groups;
  }

  // node_modules/@nldd/design-system/dist/components/index.js
  init_icon();

  // node_modules/@nldd/design-system/dist/components/content/image/image.js
  init_lit();
  init_decorators();

  // node_modules/@nldd/design-system/dist/components/content/image/image.styles.js
  init_lit();
  init_slotted_reset();
  var imageStyles = i`


	/* # Host */

	:host {
		--_corner-radius: var(--components-image-corner-radius);
		--_background-color: var(--components-image-background-color);
		--_caption-gap: var(--components-image-caption-gap);
		--_caption-color: var(--components-image-caption-color);
		--_caption-font: var(--components-image-caption-font);
		--_credit-color: var(--components-image-credit-color);
		--_credit-font: var(--components-image-credit-font);
		--_error-text-color: var(--components-image-error-text-color);
		--_error-font: var(--components-image-error-font);
		--_error-padding: var(--primitives-space-8);
		--_error-gap: var(--primitives-space-2);
		--_caption-row-gap: var(--primitives-space-8);
		--_object-fit: cover;
		--_object-position: center;
		--_max-width: 100%;

		${inheritedTextReset}
		box-sizing: border-box;
		display: block;
		max-width: var(--_max-width);
	}

	:host([hidden]) {
		display: none;
	}


	/* ## Object-fit variants
	   Driven by the reflected attribute so we can also style slotted img/picture
	   via ::slotted() with the same CSS var. */

	:host([object-fit="contain"]) { --_object-fit: contain; }
	:host([object-fit="fill"]) { --_object-fit: fill; }
	:host([object-fit="scale-down"]) { --_object-fit: scale-down; }
	:host([object-fit="none"]) { --_object-fit: none; }


	/* ## Object-position variants */

	:host([object-position="top"]) { --_object-position: top; }
	:host([object-position="bottom"]) { --_object-position: bottom; }
	:host([object-position="left"]) { --_object-position: left; }
	:host([object-position="right"]) { --_object-position: right; }


	/* ## Shape variants
	   The :host base --_corner-radius is the design-system rounded token, used by
	   shape="rounded". The default shape is "square" (set on the property), which
	   overrides to 0; circle overrides to 50% for avatar-style use (combine with
	   aspect-ratio 1/1 for a true circle). */

	:host([shape="square"]) { --_corner-radius: var(--primitives-corner-radius-none); }
	:host([shape="circle"]) { --_corner-radius: 50%; }


	/* # Figure
	   Only rendered when caption or credit is present. Acts as the layout
	   wrapper holding image + figcaption with a small gap. */

	.image__figure {
		box-sizing: border-box;
		display: flex;
		flex-direction: column;
		margin: 0;
		gap: var(--_caption-gap);
	}


	/* # Media wrapper
	   The box that constrains the image. aspect-ratio is set as an inline style
	   from the component when provided, so we don't need a CSS var. The media is
	   transparent by default; an LQIP paints it while loading, and the error
	   state paints it grey (see "Error state" below). */

	.image__media {
		display: block;
		position: relative;
		overflow: hidden;
		max-width: 100%;
		border-radius: var(--_corner-radius);
	}


	/* # Image element
	   Both the internal img and any slotted img get the same styling so the
	   wrapper behaves identically in both cases. ::slotted() can't reach
	   descendants, so a slotted <picture> works for source switching but
	   consumers must put any extra styling on the inner <img> themselves —
	   object-fit / object-position aren't inheritable into <picture>'s child
	   <img>, so we don't bother targeting <picture> here. */

	.image__img {
		display: block;
		width: 100%;
		height: 100%;
		max-width: 100%;
		object-fit: var(--_object-fit);
		object-position: var(--_object-position);
	}

	::slotted(img) {
		${slottedReset}
		display: block !important;
		width: 100% !important;
		height: 100% !important;
		max-width: 100% !important;
		object-fit: var(--_object-fit) !important;
		object-position: var(--_object-position) !important;
	}

	/* <picture> is a wrapper that picks the right <source>; layout properties
	   like width/height DO apply to it directly even though object-fit doesn't.
	   Size it so the slotted picture fills the media wrapper, then let the
	   consumer style the inner <img> for fit/position. */
	::slotted(picture) {
		${slottedReset}
		display: block !important;
		width: 100% !important;
		height: 100% !important;
		max-width: 100% !important;
	}


	/* # LQIP placeholder
	   Per-cell Oklab LQIP — extends Lean Rada's CSS-only LQIP technique
	   (https://leanrada.com/notes/css-only-lqip/) with one quantized
	   color per cell instead of grayscale-only cells. The component
	   parses lqip="base,c1,c2,c3,c4,c5,c6" and forwards each byte as an
	   inline --context-lqip-* variable (crosses the shadow boundary;
	   hence the --context-* prefix, not --_).
	   Each byte packs 2 bits L + 3 bits a + 3 bits b — the same format
	   Lean uses for his single base color, applied 7 times. The base
	   shows through cell-gradient transparent edges; the six cells carry
	   the per-zone hue. No blend modes are needed because every cell
	   already has its own real color — overlap zones blend naturally
	   through the smooth alpha falloff (stop10/20/30/40).
	   Browser support: depends on CSS mod() (Chrome 113+, Safari 15.4+,
	   Firefox 118+, May 2023 baseline), CSS round(down, ...) (Chrome 111+,
	   Safari 15.4+, Firefox 118+), oklab() (Chrome 111+, Safari 15.4+,
	   Firefox 113+), and the relative-color syntax rgb(from … r g b / α)
	   used in the gradient stops (Chrome 119+, Safari 16.4+, Firefox 128+,
	   mid-2024 baseline). The @supports gate probes all three of the
	   youngest-per-engine capabilities — without the relative-color probe,
	   Firefox 113-127 would pass the gate but render transparent/black
	   stops because rgb(from …) wouldn't parse. Older engines drop the
	   LQIP gradient entirely and fall through to the neutral
	   --_background-color set on .image__media. Degraded but harmless:
	   the placeholder is a "nice to have", not a load-bearing element. */

	@supports (left: mod(1px, 1px)) and (background: oklab(0 0 0)) and (color: rgb(from red r g b)) {

	.image__media--lqip {
		--_lqip-base-ll: mod(round(down, var(--context-lqip-base) / 64), 4);
		--_lqip-base-aaa: mod(round(down, var(--context-lqip-base) / 8), 8);
		--_lqip-base-bbb: mod(var(--context-lqip-base), 8);
		--_lqip-c1-ll: mod(round(down, var(--context-lqip-c1) / 64), 4);
		--_lqip-c1-aaa: mod(round(down, var(--context-lqip-c1) / 8), 8);
		--_lqip-c1-bbb: mod(var(--context-lqip-c1), 8);
		--_lqip-c2-ll: mod(round(down, var(--context-lqip-c2) / 64), 4);
		--_lqip-c2-aaa: mod(round(down, var(--context-lqip-c2) / 8), 8);
		--_lqip-c2-bbb: mod(var(--context-lqip-c2), 8);
		--_lqip-c3-ll: mod(round(down, var(--context-lqip-c3) / 64), 4);
		--_lqip-c3-aaa: mod(round(down, var(--context-lqip-c3) / 8), 8);
		--_lqip-c3-bbb: mod(var(--context-lqip-c3), 8);
		--_lqip-c4-ll: mod(round(down, var(--context-lqip-c4) / 64), 4);
		--_lqip-c4-aaa: mod(round(down, var(--context-lqip-c4) / 8), 8);
		--_lqip-c4-bbb: mod(var(--context-lqip-c4), 8);
		--_lqip-c5-ll: mod(round(down, var(--context-lqip-c5) / 64), 4);
		--_lqip-c5-aaa: mod(round(down, var(--context-lqip-c5) / 8), 8);
		--_lqip-c5-bbb: mod(var(--context-lqip-c5), 8);
		--_lqip-c6-ll: mod(round(down, var(--context-lqip-c6) / 64), 4);
		--_lqip-c6-aaa: mod(round(down, var(--context-lqip-c6) / 8), 8);
		--_lqip-c6-bbb: mod(var(--context-lqip-c6), 8);

		--_lqip-base-clr: oklab(
			calc(var(--_lqip-base-ll) / 3 * 0.6 + 0.2)
			calc(var(--_lqip-base-aaa) / 8 * 0.7 - 0.35)
			calc((var(--_lqip-base-bbb) + 1) / 8 * 0.7 - 0.35)
		);
		--_lqip-c1-clr: oklab(
			calc(var(--_lqip-c1-ll) / 3 * 0.6 + 0.2)
			calc(var(--_lqip-c1-aaa) / 8 * 0.7 - 0.35)
			calc((var(--_lqip-c1-bbb) + 1) / 8 * 0.7 - 0.35)
		);
		--_lqip-c2-clr: oklab(
			calc(var(--_lqip-c2-ll) / 3 * 0.6 + 0.2)
			calc(var(--_lqip-c2-aaa) / 8 * 0.7 - 0.35)
			calc((var(--_lqip-c2-bbb) + 1) / 8 * 0.7 - 0.35)
		);
		--_lqip-c3-clr: oklab(
			calc(var(--_lqip-c3-ll) / 3 * 0.6 + 0.2)
			calc(var(--_lqip-c3-aaa) / 8 * 0.7 - 0.35)
			calc((var(--_lqip-c3-bbb) + 1) / 8 * 0.7 - 0.35)
		);
		--_lqip-c4-clr: oklab(
			calc(var(--_lqip-c4-ll) / 3 * 0.6 + 0.2)
			calc(var(--_lqip-c4-aaa) / 8 * 0.7 - 0.35)
			calc((var(--_lqip-c4-bbb) + 1) / 8 * 0.7 - 0.35)
		);
		--_lqip-c5-clr: oklab(
			calc(var(--_lqip-c5-ll) / 3 * 0.6 + 0.2)
			calc(var(--_lqip-c5-aaa) / 8 * 0.7 - 0.35)
			calc((var(--_lqip-c5-bbb) + 1) / 8 * 0.7 - 0.35)
		);
		--_lqip-c6-clr: oklab(
			calc(var(--_lqip-c6-ll) / 3 * 0.6 + 0.2)
			calc(var(--_lqip-c6-aaa) / 8 * 0.7 - 0.35)
			calc((var(--_lqip-c6-bbb) + 1) / 8 * 0.7 - 0.35)
		);

		--_lqip-stop10: 2%;
		--_lqip-stop20: 8%;
		--_lqip-stop30: 18%;
		--_lqip-stop40: 32%;

		background-color: transparent;
		background-image:
			radial-gradient(50% 75% at 16.67% 25%,
				var(--_lqip-c1-clr),
				rgb(from var(--_lqip-c1-clr) r g b / calc(100% - var(--_lqip-stop10))) 10%,
				rgb(from var(--_lqip-c1-clr) r g b / calc(100% - var(--_lqip-stop20))) 20%,
				rgb(from var(--_lqip-c1-clr) r g b / calc(100% - var(--_lqip-stop30))) 30%,
				rgb(from var(--_lqip-c1-clr) r g b / calc(100% - var(--_lqip-stop40))) 40%,
				rgb(from var(--_lqip-c1-clr) r g b / var(--_lqip-stop40)) 60%,
				rgb(from var(--_lqip-c1-clr) r g b / var(--_lqip-stop30)) 70%,
				rgb(from var(--_lqip-c1-clr) r g b / var(--_lqip-stop20)) 80%,
				rgb(from var(--_lqip-c1-clr) r g b / var(--_lqip-stop10)) 90%,
				transparent),
			radial-gradient(50% 75% at 50% 25%,
				var(--_lqip-c2-clr),
				rgb(from var(--_lqip-c2-clr) r g b / calc(100% - var(--_lqip-stop10))) 10%,
				rgb(from var(--_lqip-c2-clr) r g b / calc(100% - var(--_lqip-stop20))) 20%,
				rgb(from var(--_lqip-c2-clr) r g b / calc(100% - var(--_lqip-stop30))) 30%,
				rgb(from var(--_lqip-c2-clr) r g b / calc(100% - var(--_lqip-stop40))) 40%,
				rgb(from var(--_lqip-c2-clr) r g b / var(--_lqip-stop40)) 60%,
				rgb(from var(--_lqip-c2-clr) r g b / var(--_lqip-stop30)) 70%,
				rgb(from var(--_lqip-c2-clr) r g b / var(--_lqip-stop20)) 80%,
				rgb(from var(--_lqip-c2-clr) r g b / var(--_lqip-stop10)) 90%,
				transparent),
			radial-gradient(50% 75% at 83.33% 25%,
				var(--_lqip-c3-clr),
				rgb(from var(--_lqip-c3-clr) r g b / calc(100% - var(--_lqip-stop10))) 10%,
				rgb(from var(--_lqip-c3-clr) r g b / calc(100% - var(--_lqip-stop20))) 20%,
				rgb(from var(--_lqip-c3-clr) r g b / calc(100% - var(--_lqip-stop30))) 30%,
				rgb(from var(--_lqip-c3-clr) r g b / calc(100% - var(--_lqip-stop40))) 40%,
				rgb(from var(--_lqip-c3-clr) r g b / var(--_lqip-stop40)) 60%,
				rgb(from var(--_lqip-c3-clr) r g b / var(--_lqip-stop30)) 70%,
				rgb(from var(--_lqip-c3-clr) r g b / var(--_lqip-stop20)) 80%,
				rgb(from var(--_lqip-c3-clr) r g b / var(--_lqip-stop10)) 90%,
				transparent),
			radial-gradient(50% 75% at 16.67% 75%,
				var(--_lqip-c4-clr),
				rgb(from var(--_lqip-c4-clr) r g b / calc(100% - var(--_lqip-stop10))) 10%,
				rgb(from var(--_lqip-c4-clr) r g b / calc(100% - var(--_lqip-stop20))) 20%,
				rgb(from var(--_lqip-c4-clr) r g b / calc(100% - var(--_lqip-stop30))) 30%,
				rgb(from var(--_lqip-c4-clr) r g b / calc(100% - var(--_lqip-stop40))) 40%,
				rgb(from var(--_lqip-c4-clr) r g b / var(--_lqip-stop40)) 60%,
				rgb(from var(--_lqip-c4-clr) r g b / var(--_lqip-stop30)) 70%,
				rgb(from var(--_lqip-c4-clr) r g b / var(--_lqip-stop20)) 80%,
				rgb(from var(--_lqip-c4-clr) r g b / var(--_lqip-stop10)) 90%,
				transparent),
			radial-gradient(50% 75% at 50% 75%,
				var(--_lqip-c5-clr),
				rgb(from var(--_lqip-c5-clr) r g b / calc(100% - var(--_lqip-stop10))) 10%,
				rgb(from var(--_lqip-c5-clr) r g b / calc(100% - var(--_lqip-stop20))) 20%,
				rgb(from var(--_lqip-c5-clr) r g b / calc(100% - var(--_lqip-stop30))) 30%,
				rgb(from var(--_lqip-c5-clr) r g b / calc(100% - var(--_lqip-stop40))) 40%,
				rgb(from var(--_lqip-c5-clr) r g b / var(--_lqip-stop40)) 60%,
				rgb(from var(--_lqip-c5-clr) r g b / var(--_lqip-stop30)) 70%,
				rgb(from var(--_lqip-c5-clr) r g b / var(--_lqip-stop20)) 80%,
				rgb(from var(--_lqip-c5-clr) r g b / var(--_lqip-stop10)) 90%,
				transparent),
			radial-gradient(50% 75% at 83.33% 75%,
				var(--_lqip-c6-clr),
				rgb(from var(--_lqip-c6-clr) r g b / calc(100% - var(--_lqip-stop10))) 10%,
				rgb(from var(--_lqip-c6-clr) r g b / calc(100% - var(--_lqip-stop20))) 20%,
				rgb(from var(--_lqip-c6-clr) r g b / calc(100% - var(--_lqip-stop30))) 30%,
				rgb(from var(--_lqip-c6-clr) r g b / calc(100% - var(--_lqip-stop40))) 40%,
				rgb(from var(--_lqip-c6-clr) r g b / var(--_lqip-stop40)) 60%,
				rgb(from var(--_lqip-c6-clr) r g b / var(--_lqip-stop30)) 70%,
				rgb(from var(--_lqip-c6-clr) r g b / var(--_lqip-stop20)) 80%,
				rgb(from var(--_lqip-c6-clr) r g b / var(--_lqip-stop10)) 90%,
				transparent),
			linear-gradient(0deg, var(--_lqip-base-clr), var(--_lqip-base-clr));
	}

	} /* end @supports for LQIP gradient */

	/* Fade-in transitions live OUTSIDE the @supports — they only run when the
	   --lqip class is present and the engine renders the gradient anyway. */

	.image__media--lqip .image__img {
		opacity: 0;
		transition: opacity 300ms ease-out;
	}

	:host([loaded]) .image__media--lqip .image__img {
		opacity: 1;
	}


	/* # Errored image
	   Hide the broken <img> entirely so the browser's native fallback rendering
	   (alt text leaking out + broken-image icon/border) doesn't show through
	   underneath our own error overlay. display:none also drops the element
	   from the accessibility tree, so its alt isn't announced alongside the
	   visible .image__error-text in the overlay (the overlay wrapper itself
	   carries no role/aria-label — the visible text serves AT users too, so a
	   duplicate label there would just announce the alt twice). Box dimensions
	   are preserved by aspect-ratio on .image__media, not by the img itself.
	   Listeners stay attached — only DOM removal detaches them — so the next
	   src change still triggers load/error. */

	:host([errored]) .image__img {
		display: none;
	}


	/* # Live region for dynamic load failures
	   Visually-hidden span sits inside .image__media and announces the
	   translated "image could not be loaded" message + alt text via
	   aria-live="polite" when _imageErrored flips mid-session. The element
	   is always rendered (so AT subscribes from first paint) but the
	   content only flows in on the error transition, which is what fires
	   the announcement. Standard visually-hidden recipe. */

	.image__status {
		position: absolute;
		width: 1px;
		height: 1px;
		overflow: hidden;
		clip-path: inset(50%);
		white-space: nowrap;
	}


	/* # Error state
	   Centred icon + alt-text shown when the image fails to load. The media is
	   painted grey (--_background-color) via :host([errored]) so the icon and
	   label stay legible — no separate card needed. */

	:host([errored]) .image__media {
		background-color: var(--_background-color);
	}

	.image__error {
		position: absolute;
		inset: 0;
		display: flex;
		flex-direction: column;
		align-items: center;
		justify-content: center;
		gap: var(--_error-gap);
		padding: var(--_error-padding);
		color: var(--_error-text-color);
		font: var(--_error-font);
		text-align: center;
	}

	.image__error-text {
		max-width: 100%;
		overflow-wrap: anywhere;
	}


	/* # Caption */

	.image__caption {
		display: flex;
		flex-wrap: wrap;
		gap: var(--_caption-row-gap);
		color: var(--_caption-color);
		font: var(--_caption-font);
		text-wrap: pretty;
	}

	.image__credit {
		color: var(--_credit-color);
		font: var(--_credit-font);
	}


	/* # Accessibility */

	@media (prefers-reduced-motion: reduce) {
		.image__media--lqip .image__img {
			transition: none;
		}
	}

	@media (forced-colors: active) {
		.image__media {
			border: 1px solid CanvasText;
		}
	}
`;

  // node_modules/@nldd/design-system/dist/components/content/image/image.template.js
  init_lit();
  init_class_map2();
  init_if_defined2();

  // node_modules/lit-html/directives/style-map.js
  init_lit_html();
  init_directive();
  var n5 = "important";
  var i7 = " !" + n5;
  var o8 = e6(class extends i5 {
    constructor(t6) {
      if (super(t6), t6.type !== t4.ATTRIBUTE || "style" !== t6.name || t6.strings?.length > 2) throw Error("The `styleMap` directive must be used in the `style` attribute and must be the only part in the attribute.");
    }
    render(t6) {
      return Object.keys(t6).reduce((e9, r6) => {
        const s6 = t6[r6];
        return null == s6 ? e9 : e9 + `${r6 = r6.includes("-") ? r6 : r6.replace(/(?:^(webkit|moz|ms|o)|)(?=[A-Z])/g, "-$&").toLowerCase()}:${s6};`;
      }, "");
    }
    update(e9, [r6]) {
      const { style: s6 } = e9.element;
      if (void 0 === this.ft) return this.ft = new Set(Object.keys(r6)), this.render(r6);
      for (const t6 of this.ft) null == r6[t6] && (this.ft.delete(t6), t6.includes("-") ? s6.removeProperty(t6) : s6[t6] = null);
      for (const t6 in r6) {
        const e10 = r6[t6];
        if (null != e10) {
          this.ft.add(t6);
          const r7 = "string" == typeof e10 && e10.endsWith(i7);
          t6.includes("-") || r7 ? s6.setProperty(t6, r7 ? e10.slice(0, -11) : e10, r7 ? n5 : "") : s6[t6] = e10;
        }
      }
      return E;
    }
  });

  // node_modules/@nldd/design-system/dist/components/content/image/image.template.js
  function imageTemplate(component) {
    const parsedLqip = component._parsedLqip;
    const hasLqip = parsedLqip !== null;
    const mediaStyles = {};
    if (component._cssAspectRatio)
      mediaStyles.aspectRatio = component._cssAspectRatio;
    if (hasLqip) {
      mediaStyles["--context-lqip-base"] = String(parsedLqip.base);
      parsedLqip.cells.forEach((cell, i8) => {
        mediaStyles[`--context-lqip-c${i8 + 1}`] = String(cell);
      });
    }
    const mediaClasses = e8({
      "image__media": true,
      // LQIP only renders while the image is in-flight. Once it errors we
      // fall back to the neutral background so the error card sits on a
      // calm surface instead of a distracting gradient.
      "image__media--lqip": hasLqip && !component._imageErrored
    });
    const fallbackImg = b2`
		<img class="image__img"
			src=${component.src || A}
			alt=${component.decorative ? "" : component.alt}
			aria-hidden=${component.decorative ? "true" : A}
			srcset=${component.srcset || A}
			sizes=${component.sizes || A}
			width=${o7(component._numericWidth)}
			height=${o7(component.height)}
			loading=${component.loading}
			decoding=${component.decoding}
			fetchpriority=${o7(component.fetchPriority)}
			@load=${component._onImageLoad}
			@error=${component._onImageError}
		>
	`;
    const errorOverlay = component._imageErrored ? b2`
		<div class="image__error"
			aria-hidden=${component.decorative ? "true" : A}
		>
			<nldd-icon name="broken-image" size="32" color="secondary-content"></nldd-icon>
			${component.decorative ? A : b2`<span class="image__error-text">${component.alt}</span>`}
		</div>
	` : A;
    const errorBase = component._t("components.image.error-text");
    const errorStatusMessage = component._imageErrored && !component.decorative ? component.alt ? `${errorBase}: ${component.alt}` : errorBase : "";
    const liveRegion = b2`<span class="image__status" aria-live="polite">${errorStatusMessage}</span>`;
    const media = b2`
		<div class=${mediaClasses}
			style=${o8(mediaStyles)}
		>
			<slot>${fallbackImg}</slot>
			${errorOverlay}
			${liveRegion}
		</div>
	`;
    if (!component._hasCaption) {
      return media;
    }
    return b2`
		<figure class="image__figure">
			${media}
			<figcaption class="image__caption">
				<slot name="caption" @slotchange=${component._onCaptionSlotChange}
				>${component.caption}</slot>
				${component.credit ? b2`<span class="image__credit">${component.credit}</span>` : A}
			</figcaption>
		</figure>
	`;
  }

  // node_modules/@nldd/design-system/dist/components/content/image/image.i18n.js
  var nlddImageTranslations = {
    /** Live-region announcement when an image fails to load mid-session.
     *  Combined with the alt text so SR users know which image failed.
     *  Decorative images (alt=" '"', aria-hidden) suppress the announcement. */
    "components.image.error-text": "Afbeelding is niet geladen"
  };

  // node_modules/@nldd/design-system/dist/components/content/image/image.js
  init_icon();
  var import_meta5 = {};
  var __decorate16 = function(decorators, target, key, desc) {
    var c6 = arguments.length, r6 = c6 < 3 ? target : desc === null ? desc = Object.getOwnPropertyDescriptor(target, key) : desc, d3;
    if (typeof Reflect === "object" && typeof Reflect.decorate === "function") r6 = Reflect.decorate(decorators, target, key, desc);
    else for (var i8 = decorators.length - 1; i8 >= 0; i8--) if (d3 = decorators[i8]) r6 = (c6 < 3 ? d3(r6) : c6 > 3 ? d3(target, key, r6) : d3(target, key)) || r6;
    return c6 > 3 && r6 && Object.defineProperty(target, key, r6), r6;
  };
  var NLDDImage = class NLDDImage2 extends i4 {
    constructor() {
      super(...arguments);
      this.src = "";
      this.alt = "";
      this.srcset = "";
      this.sizes = "";
      this.width = "full";
      this.loading = "lazy";
      this.decoding = "async";
      this.aspectRatio = "";
      this.objectFit = "cover";
      this.objectPosition = "center";
      this.shape = "square";
      this.caption = "";
      this.credit = "";
      this.decorative = false;
      this.lqip = "";
      this.translations = {};
      this._parsedLqipValue = null;
      this._hasSlottedCaption = false;
      this._imageLoaded = false;
      this._imageErrored = false;
      this._warnedWidth = false;
      this._warnedAlt = false;
      this._onImageLoad = () => {
        this._imageLoaded = true;
        this._imageErrored = false;
      };
      this._onImageError = () => {
        this._imageErrored = true;
        this._imageLoaded = false;
      };
      this._onCaptionSlotChange = (e9) => {
        this._syncSlottedCaption(e9.target);
      };
    }
    _t(key) {
      return this.translations[key] ?? nlddImageTranslations[key];
    }
    /** Parsed LQIP: 7 numbers in [0, 255] — base + 6 cells — or null when the
     *  attribute is empty or malformed. Memoised against the raw lqip string,
     *  so successive reads return the same object reference until the
     *  attribute changes. */
    get _parsedLqip() {
      if (this._parsedLqipKey === this.lqip)
        return this._parsedLqipValue;
      this._parsedLqipKey = this.lqip;
      if (!this.lqip) {
        this._parsedLqipValue = null;
        return null;
      }
      const parts = this.lqip.split(",").map((s6) => s6.trim());
      if (parts.length !== 7) {
        this._parsedLqipValue = null;
        return null;
      }
      const nums = parts.map((s6) => {
        const n7 = Number(s6);
        return Number.isInteger(n7) && n7 >= 0 && n7 <= 255 ? n7 : NaN;
      });
      this._parsedLqipValue = nums.some(Number.isNaN) ? null : { base: nums[0], cells: nums.slice(1) };
      return this._parsedLqipValue;
    }
    /** Convert "16:9" → "16/9" so the CSS `aspect-ratio` parser accepts it.
     *  Already-slashed values pass through untouched. */
    get _cssAspectRatio() {
      if (!this.aspectRatio)
        return "";
      return this.aspectRatio.replace(":", "/");
    }
    /** Width parsed as a positive number if it isn't 'full'. Undefined when
     *  the host should fill its parent (no max-width and no <img width> hint).
     *  Empty / NaN / non-positive values fall back to undefined.
     *
     *  Sentinel behavior: `width="0"` (and any other non-positive value)
     *  silently behaves as `width="full"`. A zero-width image is meaningless,
     *  so we treat the input as the consumer's mistake rather than a literal
     *  request — a DEV-mode warn flags it during development. */
    get _numericWidth() {
      if (this.width === "full")
        return void 0;
      const n7 = Number(this.width);
      if (Number.isFinite(n7) && n7 > 0)
        return n7;
      if (import_meta5.env?.DEV && this.width !== null && this.width !== void 0 && String(this.width) !== "" && !this._warnedWidth) {
        this._warnedWidth = true;
        console.warn(`<nldd-image>: width="${this.width}" is not a positive number. Falling back to "full".`);
      }
      return void 0;
    }
    get _hasCaption() {
      return !!this.caption || !!this.credit || this._hasSlottedCaption;
    }
    willUpdate(changed) {
      if (changed.has("src")) {
        this._imageLoaded = false;
        this._imageErrored = false;
        this._warnedAlt = false;
      }
    }
    updated(changed) {
      if (changed.has("width")) {
        const n7 = this._numericWidth;
        if (n7 !== void 0) {
          this.style.setProperty("--_max-width", `${n7}px`);
        } else {
          this.style.removeProperty("--_max-width");
        }
      }
      if (import_meta5.env?.DEV) {
        const inaccessible = !this.decorative && !this.alt.trim();
        if (inaccessible && !this._warnedAlt) {
          this._warnedAlt = true;
          console.warn("<nldd-image>: Non-decorative images need a non-empty `alt`. Set `decorative` if the image conveys no information.");
        } else if (!inaccessible) {
          this._warnedAlt = false;
        }
      }
      this.toggleAttribute("loaded", this._imageLoaded);
      this.toggleAttribute("errored", this._imageErrored);
    }
    firstUpdated() {
      const img = this.shadowRoot?.querySelector(".image__img");
      if (img?.complete && img.naturalWidth > 0) {
        this._imageLoaded = true;
      }
      const captionSlot = this.shadowRoot?.querySelector('slot[name="caption"]');
      if (captionSlot)
        this._syncSlottedCaption(captionSlot);
    }
    /** Recompute _hasSlottedCaption from a caption slot's assigned nodes.
     *  Takes the slot directly so both the slotchange handler and the
     *  firstUpdated initial-sync can call it — no synthetic Event needed. */
    _syncSlottedCaption(slot) {
      this._hasSlottedCaption = slot.assignedNodes({ flatten: true }).some((node) => {
        if (node.nodeType === Node.TEXT_NODE) {
          return (node.textContent || "").trim() !== "";
        }
        return true;
      });
    }
    render() {
      return imageTemplate(this);
    }
  };
  NLDDImage.styles = imageStyles;
  __decorate16([
    n4({ type: String, reflect: true })
  ], NLDDImage.prototype, "src", void 0);
  __decorate16([
    n4({ type: String })
  ], NLDDImage.prototype, "alt", void 0);
  __decorate16([
    n4({ type: String })
  ], NLDDImage.prototype, "srcset", void 0);
  __decorate16([
    n4({ type: String })
  ], NLDDImage.prototype, "sizes", void 0);
  __decorate16([
    n4({ type: String, reflect: true })
  ], NLDDImage.prototype, "width", void 0);
  __decorate16([
    n4({ type: Number, reflect: true })
  ], NLDDImage.prototype, "height", void 0);
  __decorate16([
    n4({ type: String })
  ], NLDDImage.prototype, "loading", void 0);
  __decorate16([
    n4({ type: String })
  ], NLDDImage.prototype, "decoding", void 0);
  __decorate16([
    n4({ type: String, attribute: "fetchpriority" })
  ], NLDDImage.prototype, "fetchPriority", void 0);
  __decorate16([
    n4({ type: String, attribute: "aspect-ratio", reflect: true })
  ], NLDDImage.prototype, "aspectRatio", void 0);
  __decorate16([
    n4({ type: String, attribute: "object-fit", reflect: true })
  ], NLDDImage.prototype, "objectFit", void 0);
  __decorate16([
    n4({ type: String, attribute: "object-position", reflect: true })
  ], NLDDImage.prototype, "objectPosition", void 0);
  __decorate16([
    n4({ type: String, reflect: true })
  ], NLDDImage.prototype, "shape", void 0);
  __decorate16([
    n4({ type: String })
  ], NLDDImage.prototype, "caption", void 0);
  __decorate16([
    n4({ type: String })
  ], NLDDImage.prototype, "credit", void 0);
  __decorate16([
    n4({ type: Boolean, reflect: true })
  ], NLDDImage.prototype, "decorative", void 0);
  __decorate16([
    n4({ type: String })
  ], NLDDImage.prototype, "lqip", void 0);
  __decorate16([
    n4({ type: Object })
  ], NLDDImage.prototype, "translations", void 0);
  __decorate16([
    r5()
  ], NLDDImage.prototype, "_hasSlottedCaption", void 0);
  __decorate16([
    r5()
  ], NLDDImage.prototype, "_imageLoaded", void 0);
  __decorate16([
    r5()
  ], NLDDImage.prototype, "_imageErrored", void 0);
  NLDDImage = __decorate16([
    t3("nldd-image")
  ], NLDDImage);

  // node_modules/@nldd/design-system/dist/components/content/rich-text/rich-text.js
  init_lit();
  init_decorators();

  // node_modules/@nldd/design-system/dist/components/content/rich-text/rich-text.i18n.js
  var nlddRichTextTranslations = {
    "components.rich-text.table-scroll-label": "Scrollbare tabel"
  };

  // node_modules/@nldd/design-system/dist/components/content/rich-text/rich-text.js
  var __decorate17 = function(decorators, target, key, desc) {
    var c6 = arguments.length, r6 = c6 < 3 ? target : desc === null ? desc = Object.getOwnPropertyDescriptor(target, key) : desc, d3;
    if (typeof Reflect === "object" && typeof Reflect.decorate === "function") r6 = Reflect.decorate(decorators, target, key, desc);
    else for (var i8 = decorators.length - 1; i8 >= 0; i8--) if (d3 = decorators[i8]) r6 = (c6 < 3 ? d3(r6) : c6 > 3 ? d3(target, key, r6) : d3(target, key)) || r6;
    return c6 > 3 && r6 && Object.defineProperty(target, key, r6), r6;
  };
  var MANAGED_LABEL_ATTR = "data-nldd-managed-label";
  var NLDDRichText = class NLDDRichText2 extends i4 {
    constructor() {
      super(...arguments);
      this.spacing = "snug";
      this.centered = false;
      this.translations = {};
    }
    _t(key) {
      return this.translations[key] ?? nlddRichTextTranslations[key];
    }
    createRenderRoot() {
      return this;
    }
    connectedCallback() {
      super.connectedCallback();
      this._resizeObserver = new ResizeObserver(() => this._syncTables());
      this._resizeObserver.observe(this);
      this._mutationObserver = new MutationObserver(() => this._syncTables());
      this._mutationObserver.observe(this, { childList: true, subtree: true });
      this._syncTables();
    }
    disconnectedCallback() {
      super.disconnectedCallback();
      this._mutationObserver?.disconnect();
      this._mutationObserver = void 0;
      this._resizeObserver?.disconnect();
      this._resizeObserver = void 0;
    }
    _syncTables() {
      for (const table of this.querySelectorAll("table")) {
        if (table.scrollWidth > table.clientWidth) {
          if (table.getAttribute("tabindex") !== "0") {
            table.setAttribute("tabindex", "0");
            if (!table.hasAttribute("aria-label") && !table.hasAttribute("aria-labelledby") && !table.querySelector("caption")) {
              table.setAttribute("aria-label", this._t("components.rich-text.table-scroll-label"));
              table.setAttribute(MANAGED_LABEL_ATTR, "");
            }
          }
        } else if (table.getAttribute("tabindex") === "0") {
          table.removeAttribute("tabindex");
          if (table.hasAttribute(MANAGED_LABEL_ATTR)) {
            table.removeAttribute("aria-label");
            table.removeAttribute(MANAGED_LABEL_ATTR);
          }
        }
      }
    }
  };
  __decorate17([
    n4({ type: String, reflect: true })
  ], NLDDRichText.prototype, "spacing", void 0);
  __decorate17([
    n4({ type: Boolean, reflect: true })
  ], NLDDRichText.prototype, "centered", void 0);
  __decorate17([
    n4({ type: Object })
  ], NLDDRichText.prototype, "translations", void 0);
  NLDDRichText = __decorate17([
    t3("nldd-rich-text")
  ], NLDDRichText);

  // node_modules/@nldd/design-system/dist/components/content/title/title.js
  init_lit();
  init_decorators();

  // node_modules/@nldd/design-system/dist/components/content/title/title.styles.js
  init_lit();
  init_breakpoints();
  init_slotted_reset();
  var smMax = r(breakpoints.smMax);
  var mdMin = r(breakpoints.mdMin);
  var mdMax = r(breakpoints.mdMax);
  var lgMin = r(breakpoints.lgMin);
  var titleStyles = i`


	/* # Host */

	:host {
		@media (max-width: ${smMax}) {
			--_font: var(--primitives-font-display-3-sm);
		}

		@media (min-width: ${mdMin}) and (max-width: ${mdMax}) {
			--_font: var(--primitives-font-display-3-md);
		}

		@media (min-width: ${lgMin}) {
			--_font: var(--primitives-font-display-3-lg);
		}

		@container layout-container (max-width: ${smMax}) {
			--_font: var(--primitives-font-display-3-sm);
		}

		@container layout-container (min-width: ${mdMin}) and (max-width: ${mdMax}) {
			--_font: var(--primitives-font-display-3-md);
		}

		@container layout-container (min-width: ${lgMin}) {
			--_font: var(--primitives-font-display-3-lg);
		}

		${inheritedTextReset}
		display: flex;
	}

	:host([size="1"]) {
		@media (max-width: ${smMax}) {
			--_font: var(--primitives-font-display-1-sm);
		}

		@media (min-width: ${mdMin}) and (max-width: ${mdMax}) {
			--_font: var(--primitives-font-display-1-md);
		}

		@media (min-width: ${lgMin}) {
			--_font: var(--primitives-font-display-1-lg);
		}

		@container layout-container (max-width: ${smMax}) {
			--_font: var(--primitives-font-display-1-sm);
		}

		@container layout-container (min-width: ${mdMin}) and (max-width: ${mdMax}) {
			--_font: var(--primitives-font-display-1-md);
		}

		@container layout-container (min-width: ${lgMin}) {
			--_font: var(--primitives-font-display-1-lg);
		}
	}

	:host([size="2"]) {
		@media (max-width: ${smMax}) {
			--_font: var(--primitives-font-display-2-sm);
		}

		@media (min-width: ${mdMin}) and (max-width: ${mdMax}) {
			--_font: var(--primitives-font-display-2-md);
		}

		@media (min-width: ${lgMin}) {
			--_font: var(--primitives-font-display-2-lg);
		}

		@container layout-container (max-width: ${smMax}) {
			--_font: var(--primitives-font-display-2-sm);
		}

		@container layout-container (min-width: ${mdMin}) and (max-width: ${mdMax}) {
			--_font: var(--primitives-font-display-2-md);
		}

		@container layout-container (min-width: ${lgMin}) {
			--_font: var(--primitives-font-display-2-lg);
		}
	}

	:host([size="4"]) {
		@media (max-width: ${smMax}) {
			--_font: var(--primitives-font-display-4-sm);
		}

		@media (min-width: ${mdMin}) and (max-width: ${mdMax}) {
			--_font: var(--primitives-font-display-4-md);
		}

		@media (min-width: ${lgMin}) {
			--_font: var(--primitives-font-display-4-lg);
		}

		@container layout-container (max-width: ${smMax}) {
			--_font: var(--primitives-font-display-4-sm);
		}

		@container layout-container (min-width: ${mdMin}) and (max-width: ${mdMax}) {
			--_font: var(--primitives-font-display-4-md);
		}

		@container layout-container (min-width: ${lgMin}) {
			--_font: var(--primitives-font-display-4-lg);
		}
	}

	:host([size="5"]) {
		@media (max-width: ${smMax}) {
			--_font: var(--primitives-font-display-5-sm);
		}

		@media (min-width: ${mdMin}) and (max-width: ${mdMax}) {
			--_font: var(--primitives-font-display-5-md);
		}

		@media (min-width: ${lgMin}) {
			--_font: var(--primitives-font-display-5-lg);
		}

		@container layout-container (max-width: ${smMax}) {
			--_font: var(--primitives-font-display-5-sm);
		}

		@container layout-container (min-width: ${mdMin}) and (max-width: ${mdMax}) {
			--_font: var(--primitives-font-display-5-md);
		}

		@container layout-container (min-width: ${lgMin}) {
			--_font: var(--primitives-font-display-5-lg);
		}
	}

	:host([size="6"]) {
		@media (max-width: ${smMax}) {
			--_font: var(--primitives-font-display-6-sm);
		}

		@media (min-width: ${mdMin}) and (max-width: ${mdMax}) {
			--_font: var(--primitives-font-display-6-md);
		}

		@media (min-width: ${lgMin}) {
			--_font: var(--primitives-font-display-6-lg);
		}

		@container layout-container (max-width: ${smMax}) {
			--_font: var(--primitives-font-display-6-sm);
		}

		@container layout-container (min-width: ${mdMin}) and (max-width: ${mdMax}) {
			--_font: var(--primitives-font-display-6-md);
		}

		@container layout-container (min-width: ${lgMin}) {
			--_font: var(--primitives-font-display-6-lg);
		}
	}

	:host([hidden]) {
		display: none;
	}


	/* # Block */

	.title {
		display: flex;
		width: 100%;
		flex-direction: row;
		gap: var(--primitives-space-12);
		align-items: center;
	}


	/* # Elements */

	.title__title-group {
		display: flex;
		min-width: 0;
		flex-direction: column;
		flex-grow: 1;
		flex-shrink: 1;
		flex-basis: 0;
	}

	::slotted([slot="overline"]) {
		${slottedReset}
		${inheritedTextReset}
		margin: 0 !important;
		color: var(--semantics-content-secondary-color) !important;
		font: var(--primitives-font-body-sm-regular-tight) !important;
		overflow-wrap: anywhere !important;
	}

	::slotted(:not([slot])) {
		${slottedReset}
		${inheritedTextReset}
		margin: 0 !important;
		color: var(--semantics-content-color) !important;
		font: var(--_font) !important;
		overflow-wrap: anywhere !important;
		text-wrap: pretty !important;
	}

	::slotted([slot="subtitle"]) {
		${slottedReset}
		${inheritedTextReset}
		margin: 0 !important;
		color: var(--semantics-content-secondary-color) !important;
		font: var(--primitives-font-body-sm-regular-tight) !important;
		overflow-wrap: anywhere !important;
	}

	.title__actions {
		display: flex;
		flex-direction: row;
		flex-shrink: 0;
		align-items: center;
	}
`;

  // node_modules/@nldd/design-system/dist/components/content/title/title.template.js
  init_lit();
  function titleTemplate() {
    return b2`
		<div class="title">
			<div class="title__title-group">
				<slot name="overline"></slot>
				<slot></slot>
				<slot name="subtitle"></slot>
			</div>
			<div class="title__actions">
				<slot name="actions"></slot>
			</div>
		</div>
	`;
  }

  // node_modules/@nldd/design-system/dist/components/content/title/title.js
  var __decorate18 = function(decorators, target, key, desc) {
    var c6 = arguments.length, r6 = c6 < 3 ? target : desc === null ? desc = Object.getOwnPropertyDescriptor(target, key) : desc, d3;
    if (typeof Reflect === "object" && typeof Reflect.decorate === "function") r6 = Reflect.decorate(decorators, target, key, desc);
    else for (var i8 = decorators.length - 1; i8 >= 0; i8--) if (d3 = decorators[i8]) r6 = (c6 < 3 ? d3(r6) : c6 > 3 ? d3(target, key, r6) : d3(target, key)) || r6;
    return c6 > 3 && r6 && Object.defineProperty(target, key, r6), r6;
  };
  var NLDDTitle = class NLDDTitle2 extends i4 {
    constructor() {
      super(...arguments);
      this.size = 3;
    }
    render() {
      return titleTemplate();
    }
  };
  NLDDTitle.styles = titleStyles;
  __decorate18([
    n4({ type: Number, reflect: true })
  ], NLDDTitle.prototype, "size", void 0);
  NLDDTitle = __decorate18([
    t3("nldd-title")
  ], NLDDTitle);

  // node_modules/@nldd/design-system/dist/components/index.js
  init_tooltip();

  // node_modules/@nldd/design-system/dist/components/content/blockquote/blockquote.js
  init_lit();
  init_decorators();

  // node_modules/@nldd/design-system/dist/components/content/blockquote/blockquote.styles.js
  init_lit();
  init_breakpoints();
  init_slotted_reset();
  var smMax2 = r(breakpoints.smMax);
  var blockquoteStyles = i`


	/* # Host */

	:host {
		--_spacing: var(--semantics-blockquotes-md-spacing);
		--_quote-font: var(--semantics-blockquotes-md-quote-font);
		--_attribution-font: var(--semantics-blockquotes-md-attribution-font);

		@media (max-width: ${smMax2}) {
			--_spacing: var(--semantics-blockquotes-sm-spacing);
			--_quote-font: var(--semantics-blockquotes-sm-quote-font);
			--_attribution-font: var(--semantics-blockquotes-sm-attribution-font);
		}

		@container layout-container (max-width: ${smMax2}) {
			--_spacing: var(--semantics-blockquotes-sm-spacing);
			--_quote-font: var(--semantics-blockquotes-sm-quote-font);
			--_attribution-font: var(--semantics-blockquotes-sm-attribution-font);
		}

		${inheritedTextReset}
		display: block;
		max-width: var(--semantics-blockquotes-max-width);
	}

	:host([hidden]) {
		display: none;
	}


	/* # Block */

	.blockquote {
		display: flex;
		margin: 0;
		border-top: var(--semantics-blockquotes-border);
		border-left: var(--semantics-blockquotes-border);
		border-top-left-radius: var(--semantics-blockquotes-corner-radius);
		padding-inline: var(--_spacing);
		padding-top: var(--_spacing);
		padding-bottom: 0;
		flex-direction: column;
		gap: calc(var(--_spacing) / 2);
		color: var(--semantics-content-color);
	}


	/* # Elements */

	.blockquote__quote {
		display: flex;
		flex-direction: column;
		gap: calc(var(--_spacing) / 2);
		font: var(--_quote-font);
	}

	.blockquote__attribution {
		display: block;
		font: var(--_attribution-font);
	}

	.blockquote__attribution::before {
		content: '— ' / '';
	}

	.blockquote__attribution[hidden] {
		display: none;
	}

	::slotted(p) {
		${slottedReset}
		${inheritedTextReset}
		margin: 0 !important;
	}

	.blockquote__attribution::slotted(p) {
		display: inline !important;
	}
`;

  // node_modules/@nldd/design-system/dist/components/content/blockquote/blockquote.template.js
  init_lit();
  function template10(component) {
    return b2`
		<blockquote class="blockquote"
			cite=${component.cite || A}
		>
			<div class="blockquote__quote">
				<slot></slot>
			</div>
			<slot class="blockquote__attribution"
				name="attribution"
				?hidden=${!component._hasAttribution}
				@slotchange=${component._handleAttributionSlotChange}
			></slot>
		</blockquote>
	`;
  }

  // node_modules/@nldd/design-system/dist/components/content/blockquote/blockquote.js
  var __decorate19 = function(decorators, target, key, desc) {
    var c6 = arguments.length, r6 = c6 < 3 ? target : desc === null ? desc = Object.getOwnPropertyDescriptor(target, key) : desc, d3;
    if (typeof Reflect === "object" && typeof Reflect.decorate === "function") r6 = Reflect.decorate(decorators, target, key, desc);
    else for (var i8 = decorators.length - 1; i8 >= 0; i8--) if (d3 = decorators[i8]) r6 = (c6 < 3 ? d3(r6) : c6 > 3 ? d3(target, key, r6) : d3(target, key)) || r6;
    return c6 > 3 && r6 && Object.defineProperty(target, key, r6), r6;
  };
  var NLDDBlockquote = class NLDDBlockquote2 extends i4 {
    constructor() {
      super(...arguments);
      this.cite = void 0;
      this._hasAttribution = false;
      this._handleAttributionSlotChange = (e9) => {
        const slot = e9.target;
        this._hasAttribution = slot.assignedNodes({ flatten: true }).some((n7) => {
          if (n7.nodeType === Node.ELEMENT_NODE)
            return true;
          return (n7.textContent ?? "").trim().length > 0;
        });
      };
    }
    render() {
      return template10(this);
    }
  };
  NLDDBlockquote.styles = blockquoteStyles;
  __decorate19([
    n4({ type: String })
  ], NLDDBlockquote.prototype, "cite", void 0);
  __decorate19([
    r5()
  ], NLDDBlockquote.prototype, "_hasAttribution", void 0);
  NLDDBlockquote = __decorate19([
    t3("nldd-blockquote")
  ], NLDDBlockquote);

  // node_modules/@nldd/design-system/dist/components/content/tag/tag.js
  init_lit();
  init_decorators();

  // node_modules/@nldd/design-system/dist/components/content/tag/tag.styles.js
  init_lit();
  init_slotted_reset();
  var tagStyles = i`


	/* # Host */

	:host {
		--_corner-radius: var(--components-tag-md-corner-radius);
		--_background-color: var(--components-tag-neutral-background-color);
		--_min-height: var(--primitives-space-24);
		--_inline-padding: var(--primitives-space-6);
		--_gap: var(--primitives-space-3);
		--_content-color: var(--components-tag-neutral-content-color);
		--_font: var(--primitives-font-body-sm-medium-flat);
		--_icon-size: var(--primitives-space-16);
		--_icon-offset-correction: var(--primitives-space-1);

		${inheritedTextReset}
		display: inline-flex;
		vertical-align: middle;
	}

	:host([size="sm"]) {
		--_corner-radius: var(--components-tag-sm-corner-radius);
		--_min-height: var(--primitives-space-20);
		--_inline-padding: var(--primitives-space-4);
		--_gap: var(--primitives-space-2);
		--_font: var(--primitives-font-body-xs-medium-flat);
		--_icon-size: var(--primitives-space-14);
	}

	/* ## Color */

	:host([color="accent"]) {
		--_background-color: var(--components-tag-accent-background-color);
		--_content-color: var(--components-tag-accent-content-color);
	}

	:host([color="success"]) {
		--_background-color: var(--components-tag-success-background-color);
		--_content-color: var(--components-tag-success-content-color);
	}

	:host([color="warning"]) {
		--_background-color: var(--components-tag-warning-background-color);
		--_content-color: var(--components-tag-warning-content-color);
	}

	:host([color="critical"]) {
		--_background-color: var(--components-tag-critical-background-color);
		--_content-color: var(--components-tag-critical-content-color);
	}

	/* ### Rijkskleuren */

	:host([color="coolgray"]) {
		--_background-color: var(--components-tag-coolgray-background-color);
		--_content-color: var(--components-tag-coolgray-content-color);
	}

	:host([color="lintblauw"]) {
		--_background-color: var(--components-tag-lintblauw-background-color);
		--_content-color: var(--components-tag-lintblauw-content-color);
	}

	:host([color="donkerblauw"]) {
		--_background-color: var(--components-tag-donkerblauw-background-color);
		--_content-color: var(--components-tag-donkerblauw-content-color);
	}

	:host([color="hemelblauw"]) {
		--_background-color: var(--components-tag-hemelblauw-background-color);
		--_content-color: var(--components-tag-hemelblauw-content-color);
	}

	:host([color="lichtblauw"]) {
		--_background-color: var(--components-tag-lichtblauw-background-color);
		--_content-color: var(--components-tag-lichtblauw-content-color);
	}

	:host([color="paars"]) {
		--_background-color: var(--components-tag-paars-background-color);
		--_content-color: var(--components-tag-paars-content-color);
	}

	:host([color="violet"]) {
		--_background-color: var(--components-tag-violet-background-color);
		--_content-color: var(--components-tag-violet-content-color);
	}

	:host([color="robijnrood"]) {
		--_background-color: var(--components-tag-robijnrood-background-color);
		--_content-color: var(--components-tag-robijnrood-content-color);
	}

	:host([color="roze"]) {
		--_background-color: var(--components-tag-roze-background-color);
		--_content-color: var(--components-tag-roze-content-color);
	}

	:host([color="rood"]) {
		--_background-color: var(--components-tag-rood-background-color);
		--_content-color: var(--components-tag-rood-content-color);
	}

	:host([color="oranje"]) {
		--_background-color: var(--components-tag-oranje-background-color);
		--_content-color: var(--components-tag-oranje-content-color);
	}

	:host([color="donkergeel"]) {
		--_background-color: var(--components-tag-donkergeel-background-color);
		--_content-color: var(--components-tag-donkergeel-content-color);
	}

	:host([color="geel"]) {
		--_background-color: var(--components-tag-geel-background-color);
		--_content-color: var(--components-tag-geel-content-color);
	}

	:host([color="donkerbruin"]) {
		--_background-color: var(--components-tag-donkerbruin-background-color);
		--_content-color: var(--components-tag-donkerbruin-content-color);
	}

	:host([color="bruin"]) {
		--_background-color: var(--components-tag-bruin-background-color);
		--_content-color: var(--components-tag-bruin-content-color);
	}

	:host([color="donkergroen"]) {
		--_background-color: var(--components-tag-donkergroen-background-color);
		--_content-color: var(--components-tag-donkergroen-content-color);
	}

	:host([color="groen"]) {
		--_background-color: var(--components-tag-groen-background-color);
		--_content-color: var(--components-tag-groen-content-color);
	}

	:host([color="mosgroen"]) {
		--_background-color: var(--components-tag-mosgroen-background-color);
		--_content-color: var(--components-tag-mosgroen-content-color);
	}

	:host([color="mintgroen"]) {
		--_background-color: var(--components-tag-mintgroen-background-color);
		--_content-color: var(--components-tag-mintgroen-content-color);
	}

	:host([hidden]) {
		display: none;
	}


	/* # Block */

	.tag {
		box-sizing: border-box;
		display: inline-flex;
		border-radius: var(--_corner-radius);
		background-color: var(--_background-color);
		min-height: var(--_min-height);
		padding: 0 var(--_inline-padding);
		gap: var(--_gap);
		align-items: center;
		color: var(--_content-color);
		font: var(--_font);
		white-space: nowrap;
	}


	/* # Elements */

	.tag__icon {
		display: inline-flex;
		margin-inline: calc((var(--_min-height) - var(--_icon-size)) / 2 - var(--_inline-padding));
		width: var(--_icon-size);
		height: var(--_icon-size);
		flex-shrink: 0;
		align-items: center;
	}

	.tag__icon:has(+ .tag__text) {
		margin-left: calc((var(--_min-height) - var(--_icon-size)) / 2 - var(--_inline-padding) + var(--_icon-offset-correction));
		margin-right: 0;
	}

	.tag__text {
		display: inline-block;
	}
`;

  // node_modules/@nldd/design-system/dist/components/content/tag/tag.template.js
  init_lit();
  function template11(component) {
    const variant = component._effectiveVariant;
    const showIcon = variant === "icon" || variant === "icon-and-text" && component._hasIcon;
    const showText = (variant === "text" || variant === "icon-and-text") && component._hasText;
    const iconOnly = variant === "icon" && component._hasIcon && !!component.accessibleLabel;
    return b2`
		<span class="tag"
			role=${iconOnly ? "img" : A}
			aria-label=${iconOnly ? component.accessibleLabel : A}
		>
			${showIcon ? b2`
				<span class="tag__icon">
					${component.icon ? b2`<nldd-icon name=${component.icon}></nldd-icon>` : b2`<slot name="icon">${variant === "icon" ? b2`<nldd-icon name="icon-placeholder"></nldd-icon>` : A}</slot>`}
				</span>
			` : A}
			${showText ? b2`
				<span class="tag__text">
					${component.text ? component.text : b2`<slot></slot>`}
				</span>
			` : A}
		</span>
	`;
  }

  // node_modules/@nldd/design-system/dist/components/content/tag/tag.js
  init_icon();
  var import_meta6 = {};
  var __decorate20 = function(decorators, target, key, desc) {
    var c6 = arguments.length, r6 = c6 < 3 ? target : desc === null ? desc = Object.getOwnPropertyDescriptor(target, key) : desc, d3;
    if (typeof Reflect === "object" && typeof Reflect.decorate === "function") r6 = Reflect.decorate(decorators, target, key, desc);
    else for (var i8 = decorators.length - 1; i8 >= 0; i8--) if (d3 = decorators[i8]) r6 = (c6 < 3 ? d3(r6) : c6 > 3 ? d3(target, key, r6) : d3(target, key)) || r6;
    return c6 > 3 && r6 && Object.defineProperty(target, key, r6), r6;
  };
  var NLDDTag = class NLDDTag2 extends i4 {
    constructor() {
      super(...arguments);
      this.color = "neutral";
      this.size = "md";
      this.text = "";
      this.icon = "";
      this.variant = "";
      this.accessibleLabel = "";
      this._hasSlotText = false;
      this._hasSlotIcon = false;
    }
    get _hasText() {
      return !!this.text || this._hasSlotText;
    }
    get _hasIcon() {
      return !!this.icon || this._hasSlotIcon;
    }
    get _effectiveVariant() {
      if (this.variant)
        return this.variant;
      if (this._hasIcon && this._hasText)
        return "icon-and-text";
      if (this._hasIcon)
        return "icon";
      return "text";
    }
    connectedCallback() {
      super.connectedCallback();
      this._updateSlotState();
      this._childObserver = new MutationObserver(() => this._updateSlotState());
      this._childObserver.observe(this, { childList: true });
    }
    disconnectedCallback() {
      super.disconnectedCallback();
      this._childObserver?.disconnect();
      this._childObserver = void 0;
    }
    _updateSlotState() {
      let hasText = false;
      let hasIcon = false;
      for (const node of Array.from(this.childNodes)) {
        if (node.nodeType === Node.TEXT_NODE) {
          if (node.textContent?.trim())
            hasText = true;
        } else if (node.nodeType === Node.ELEMENT_NODE) {
          const slotName = node.getAttribute("slot");
          if (slotName === "icon")
            hasIcon = true;
          else if (!slotName)
            hasText = true;
        }
      }
      this._hasSlotText = hasText;
      this._hasSlotIcon = hasIcon;
    }
    updated() {
      if (import_meta6.env?.DEV && this._effectiveVariant === "icon" && !this.accessibleLabel) {
        console.warn("nldd-tag: icon-only tag without an accessible-label has no accessible name. Add accessible-label so screen readers can announce it.", this);
      }
    }
    render() {
      return template11(this);
    }
  };
  NLDDTag.styles = tagStyles;
  __decorate20([
    n4({ type: String, reflect: true })
  ], NLDDTag.prototype, "color", void 0);
  __decorate20([
    n4({ type: String, reflect: true })
  ], NLDDTag.prototype, "size", void 0);
  __decorate20([
    n4({ type: String })
  ], NLDDTag.prototype, "text", void 0);
  __decorate20([
    n4({ type: String })
  ], NLDDTag.prototype, "icon", void 0);
  __decorate20([
    n4({ type: String, reflect: true })
  ], NLDDTag.prototype, "variant", void 0);
  __decorate20([
    n4({ type: String, attribute: "accessible-label" })
  ], NLDDTag.prototype, "accessibleLabel", void 0);
  __decorate20([
    r5()
  ], NLDDTag.prototype, "_hasSlotText", void 0);
  __decorate20([
    r5()
  ], NLDDTag.prototype, "_hasSlotIcon", void 0);
  NLDDTag = __decorate20([
    t3("nldd-tag")
  ], NLDDTag);

  // node_modules/@nldd/design-system/dist/components/content/keyboard-shortcut/keyboard-shortcut.js
  init_lit();
  init_decorators();

  // node_modules/@nldd/design-system/dist/components/content/keyboard-shortcut/keyboard-shortcut.styles.js
  init_lit();
  init_slotted_reset();
  var keyboardShortcutStyles = i`


	/* # Host */

	:host {
		--_size: var(--components-keyboard-shortcut-md-size);
		--_inline-padding: var(--primitives-space-6);
		--_font: var(--primitives-font-body-xs-regular-flat);

		${inheritedTextReset}
		display: inline-flex;
		vertical-align: middle;
	}

	:host([size="sm"]) {
		--_size: var(--components-keyboard-shortcut-sm-size);
		--_inline-padding: var(--primitives-space-4);
		--_font: var(--primitives-font-body-xxs-regular-flat);
	}

	:host([hidden]) {
		display: none;
	}

	@media (any-hover: none) {
		:host(:not([always-visible])) {
			display: none;
		}
	}


	/* # Block */

	.keyboard-shortcut {
		display: inline-flex;
		gap: var(--primitives-space-4);
		align-items: center;
	}


	/* # Elements */

	.keyboard-shortcut__key {
		box-sizing: border-box;
		display: inline-flex;
		border: var(--components-keyboard-shortcut-border-thickness) solid var(--components-keyboard-shortcut-border-color);
		border-radius: var(--components-keyboard-shortcut-corner-radius);
		background-color: var(--components-keyboard-shortcut-background-color);
		min-width: var(--_size);
		height: var(--_size);
		padding: 0 var(--_inline-padding);
		align-items: center;
		justify-content: center;
		color: var(--components-keyboard-shortcut-content-color);
		font: var(--_font);
		white-space: nowrap;
	}

	@media (forced-colors: active) {
		.keyboard-shortcut__key {
			border-color: CanvasText;
			background-color: Canvas;
			color: CanvasText;
		}
	}

	.keyboard-shortcut__separator {
		color: var(--components-keyboard-shortcut-separator-color);
		font: var(--_font);
	}
`;

  // node_modules/@nldd/design-system/dist/components/content/keyboard-shortcut/keyboard-shortcut.template.js
  init_lit();
  function template12(component) {
    const keys = component._parsedKeys;
    if (keys.length === 0) {
      return b2`
			<kbd class="keyboard-shortcut">
				<slot></slot>
			</kbd>
		`;
    }
    return b2`
		<kbd class="keyboard-shortcut">
			${keys.map((key, index) => b2`
				${index > 0 ? b2`<span class="keyboard-shortcut__separator" aria-hidden="true">+</span>` : ""}
				<kbd class="keyboard-shortcut__key">${key}</kbd>
			`)}
		</kbd>
	`;
  }

  // node_modules/@nldd/design-system/dist/utilities/os.js
  var cached = null;
  var override = null;
  function classify(platform2) {
    const p4 = platform2.toLowerCase();
    if (/mac|iphone|ipad|ipod|ios/.test(p4))
      return "mac";
    if (/win/.test(p4))
      return "windows";
    if (/android/.test(p4))
      return "other";
    if (/linux|cros/.test(p4))
      return "linux";
    return "other";
  }
  function detectOS() {
    if (override !== null)
      return override;
    if (cached !== null)
      return cached;
    if (typeof navigator === "undefined") {
      cached = "other";
      return cached;
    }
    const nav = navigator;
    const platform2 = nav.userAgentData?.platform || nav.platform || "";
    cached = classify(platform2);
    return cached;
  }

  // node_modules/@nldd/design-system/dist/components/content/keyboard-shortcut/keyboard-shortcut.js
  var __decorate21 = function(decorators, target, key, desc) {
    var c6 = arguments.length, r6 = c6 < 3 ? target : desc === null ? desc = Object.getOwnPropertyDescriptor(target, key) : desc, d3;
    if (typeof Reflect === "object" && typeof Reflect.decorate === "function") r6 = Reflect.decorate(decorators, target, key, desc);
    else for (var i8 = decorators.length - 1; i8 >= 0; i8--) if (d3 = decorators[i8]) r6 = (c6 < 3 ? d3(r6) : c6 > 3 ? d3(target, key, r6) : d3(target, key)) || r6;
    return c6 > 3 && r6 && Object.defineProperty(target, key, r6), r6;
  };
  var NLDDKeyboardShortcut = class NLDDKeyboardShortcut2 extends i4 {
    constructor() {
      super(...arguments);
      this.keys = "";
      this.macKeys = "";
      this.windowsKeys = "";
      this.linuxKeys = "";
      this.size = "md";
      this.alwaysVisible = false;
      this.debugOS = "";
    }
    get _resolvedKeys() {
      const os = this.debugOS || detectOS();
      if (os === "mac" && this.macKeys)
        return this.macKeys;
      if (os === "windows" && this.windowsKeys)
        return this.windowsKeys;
      if (os === "linux" && this.linuxKeys)
        return this.linuxKeys;
      return this.keys;
    }
    get _parsedKeys() {
      const spec = this._resolvedKeys;
      if (!spec)
        return [];
      const MARKER = "\0";
      return spec.replace(/\+\+\+/g, `+${MARKER}+`).split("+").map((k2) => k2.trim().replace(new RegExp(MARKER, "g"), "+")).filter(Boolean);
    }
    render() {
      return template12(this);
    }
  };
  NLDDKeyboardShortcut.styles = keyboardShortcutStyles;
  __decorate21([
    n4({ type: String })
  ], NLDDKeyboardShortcut.prototype, "keys", void 0);
  __decorate21([
    n4({ type: String, attribute: "mac-keys" })
  ], NLDDKeyboardShortcut.prototype, "macKeys", void 0);
  __decorate21([
    n4({ type: String, attribute: "windows-keys" })
  ], NLDDKeyboardShortcut.prototype, "windowsKeys", void 0);
  __decorate21([
    n4({ type: String, attribute: "linux-keys" })
  ], NLDDKeyboardShortcut.prototype, "linuxKeys", void 0);
  __decorate21([
    n4({ type: String, reflect: true })
  ], NLDDKeyboardShortcut.prototype, "size", void 0);
  __decorate21([
    n4({ type: Boolean, reflect: true, attribute: "always-visible" })
  ], NLDDKeyboardShortcut.prototype, "alwaysVisible", void 0);
  __decorate21([
    n4({ type: String, attribute: "debug-os" })
  ], NLDDKeyboardShortcut.prototype, "debugOS", void 0);
  NLDDKeyboardShortcut = __decorate21([
    t3("nldd-keyboard-shortcut")
  ], NLDDKeyboardShortcut);

  // node_modules/@nldd/design-system/dist/components/content/code-viewer/code-viewer.js
  init_lit();
  init_decorators();
  var import_prismjs = __toESM(require_prism(), 1);

  // node_modules/@nldd/design-system/dist/components/content/code-viewer/code-viewer.styles.js
  init_lit();
  init_slotted_reset();
  var codeViewerStyles = i`


	/* # Host */

	:host {
		--_corner-radius: var(--primitives-corner-radius-lg);
		--_background-color: var(--semantics-surfaces-tinted-background-color);
		--_border-color: var(--semantics-surfaces-tinted-border-color);
		/* Shared shadow value so the default and focus-visible rules can
		   compose with var(...) instead of repeating the inset literal. */
		--_border-shadow: inset 0 0 0 1px var(--_border-color);
		--_block-padding: var(--primitives-space-16);
		--_inline-padding: var(--primitives-space-16);
		--_content-color: var(--semantics-content-color);
		--_font: var(--primitives-font-monospace-sm-regular-snug);
		--_actions-area-padding: var(--primitives-space-8);
		--_actions-area-size: calc(var(--semantics-controls-md-min-size) + var(--_actions-area-padding) * 2);
		--_actions-z-index: 1;

		${inheritedTextReset}
		display: flex;
		position: relative;
		/* Own stacking context so the absolutely-positioned actions button
		   (z-index: var(--_actions-z-index)) stays scoped to the code-viewer
		   and can't paint over other layers on the page. */
		isolation: isolate;
	}

	:host([hidden]) {
		display: none;
	}

	:host([background="base"]) {
		--_background-color: var(--semantics-surfaces-base-background-color);
		--_border-color: var(--semantics-surfaces-base-border-color);
	}

	/* variant="simple" drops the entire frame (no rounded corners, no
	   padding, no fill, no border ring). The host becomes a thin wrapper
	   around the slot content — use for embedding in a consumer-supplied
	   container. Same shape nldd-list uses for its simple variant. */
	:host([variant="simple"]) {
		--_corner-radius: 0;
		--_background-color: transparent;
		--_border-color: transparent;
		--_block-padding: 0;
		--_inline-padding: 0;
	}


	/* # Block */

	.code-viewer {
		box-sizing: border-box;
		display: flex;
		position: relative;
		margin: 0;
		border-radius: var(--_corner-radius);
		/* Inner box-shadow paints the 1px border ring inside the radius
		   without taking layout space — matches nldd-box / nldd-banner.
		   variant="simple" suppresses the ring by setting --_border-color
		   to transparent above. The forced-colors fallback at the bottom
		   of the file restores a real border for Windows High Contrast
		   users. */
		box-shadow: var(--_border-shadow);
		background-color: var(--_background-color);
		overflow-x: auto;
		padding: var(--_block-padding) var(--_inline-padding);
		flex-direction: column;
		flex-grow: 1;
		align-items: start;
		justify-content: center;
		color: var(--_content-color);
		font: var(--_font);
		white-space: pre;
	}

	:host(:not([no-copy])) .code-viewer {
		min-height: var(--_actions-area-size);
		padding-right: var(--_actions-area-size);
	}

	/* variant="simple" + copy-button: pin the button flush to the host's
	   top-right corner (no inset padding) and keep the snippet at least as
	   tall as the button so the layout never clips the action. The reserved
	   right-padding is dropped — bare snippets imply no chrome, so the code
	   may flow under the button if a single line is long enough. */
	:host([variant="simple"]:not([no-copy])) {
		--_actions-area-padding: 0;
	}

	:host([variant="simple"]:not([no-copy])) .code-viewer {
		min-height: var(--_actions-area-size);
		padding-right: 0;
	}

	.code-viewer:focus-visible {
		outline: var(--semantics-focus-ring-outline);
		outline-offset: var(--semantics-focus-ring-outline-offset);
		/* Comma-compose so the outer focus ring layers over the existing
		   inset border ring instead of replacing it. Focus first (outer),
		   border second (inner). */
		box-shadow: var(--semantics-focus-ring-box-shadow), var(--_border-shadow);
	}

	.code-viewer:focus:not(:focus-visible) {
		outline: none;
	}

	:host([wrap]) .code-viewer {
		overflow-x: visible;
		white-space: pre-wrap;
		word-break: break-word;
	}

	/* Lit reflects empty-string props as language=""; exclude that case */
	:host([language]:not([language=""])) slot {
		display: none;
	}

	.code__highlighted {
		color: inherit;
		font: inherit;
	}

	.code-viewer__actions {
		position: absolute;
		top: var(--_actions-area-padding);
		right: var(--_actions-area-padding);
		z-index: var(--_actions-z-index);
	}

	/* Visually-hidden live region announces copy success/failure to
	   screen readers — the static accessible-label on the icon button
	   alone can't convey state changes. */

	.code-viewer__live-region {
		position: absolute;
		width: 1px;
		height: 1px;
		overflow: hidden;
		clip-path: inset(50%);
		white-space: nowrap;
	}


	/* # Tokens */

	.token.comment,
	.token.prolog,
	.token.doctype,
	.token.cdata { color: var(--components-code-viewer-token-comment-color); font-style: italic; }
	.token.punctuation { color: var(--components-code-viewer-token-punctuation-color); }
	.token.namespace { opacity: 0.7; }
	.token.keyword { color: var(--components-code-viewer-token-keyword-color); }
	.token.string, .token.char { color: var(--components-code-viewer-token-string-color); }
	.token.number { color: var(--components-code-viewer-token-number-color); }
	.token.boolean { color: var(--components-code-viewer-token-boolean-color); }
	.token.null { color: var(--components-code-viewer-token-null-color); }
	.token.function { color: var(--components-code-viewer-token-function-color); }
	.token.class-name { color: var(--components-code-viewer-token-class-color); }
	.token.builtin { color: var(--components-code-viewer-token-builtin-color); }
	.token.tag { color: var(--components-code-viewer-token-tag-color); }
	.token.attr-name { color: var(--components-code-viewer-token-attr-name-color); }
	.token.attr-value { color: var(--components-code-viewer-token-attr-value-color); }
	.token.property { color: var(--components-code-viewer-token-property-color); }
	.token.selector { color: var(--components-code-viewer-token-selector-color); }
	.token.atrule { color: var(--components-code-viewer-token-atrule-color); }
	.token.regex { color: var(--components-code-viewer-token-regex-color); }
	.token.url { color: var(--components-code-viewer-token-url-color); }
	.token.operator { color: var(--components-code-viewer-token-operator-color); }
	.token.constant { color: var(--components-code-viewer-token-constant-color); }
	.token.deleted { color: var(--components-code-viewer-token-deleted-color); }
	.token.inserted { color: var(--components-code-viewer-token-inserted-color); }
	.token.important { color: var(--components-code-viewer-token-important-color); font-weight: bold; }
	.token.symbol { color: var(--components-code-viewer-token-symbol-color); }
	.token.entity { color: var(--components-code-viewer-token-entity-color); }
	.token.variable { color: var(--components-code-viewer-token-variable-color); }
	.token.key { color: var(--components-code-viewer-token-property-color); }
	.token.bold { font-weight: bold; }
	.token.italic { font-style: italic; }


	/* # Accessibility
	   forced-colors / Windows High Contrast strips box-shadow, so the
	   inset border ring on .code-viewer would disappear. Restore the
	   frame with a real border in that mode — same fallback nldd-box,
	   nldd-banner, and nldd-list use. variant="simple" keeps no border
	   (the host is a bare wrapper in that mode). */

	@media (forced-colors: active) {
		:host([variant="box"]) .code-viewer {
			border: 1px solid CanvasText;
		}
	}
`;

  // node_modules/@nldd/design-system/dist/components/content/code-viewer/code-viewer.template.js
  init_lit();
  init_if_defined2();
  init_unsafe_html2();
  function codeViewerTemplate(component) {
    const scrollable = component._isScrollable;
    const copyState = component._copyState;
    const tooltipText = copyState === "success" ? component._t("components.code-viewer.copy-success-text") : copyState === "failure" ? component._t("components.code-viewer.copy-failure-text") : component._t("components.code-viewer.copy-action");
    const buttonLabel = tooltipText;
    const liveRegionText = copyState === "success" ? component._t("components.code-viewer.copy-success-text") : copyState === "failure" ? component._t("components.code-viewer.copy-failure-text") : "";
    return b2`<pre
		class="code-viewer"
		tabindex=${o7(scrollable ? "0" : void 0)}
		role=${o7(scrollable ? "region" : void 0)}
		aria-label=${o7(scrollable ? component._t("components.code-viewer.region-label") : void 0)}
	><slot @slotchange=${component._onSlotChange}></slot>${component.language && component._highlightedHtml ? b2`<code class="code__highlighted language-${component.language}">${o6(component._highlightedHtml)}</code>` : ""}</pre>${component.noCopy ? A : b2`
		<div class="code-viewer__actions">
			<div class="code-viewer__copy-button">
				<nldd-tooltip
					text=${tooltipText}
					placement="left"
					?open=${copyState !== "idle"}
					@nldd-tooltip-dismiss=${component._onCopyDismiss}
				>
					<nldd-icon-button
						icon=${copyState === "success" ? "check-mark" : "copy"}
						accessible-label=${buttonLabel}
						tooltip-timing="never"
						size="md"
						@click=${component._onCopyClick}
					></nldd-icon-button>
				</nldd-tooltip>
			</div>
			<div class="code-viewer__live-region" role="status" aria-live="polite">${liveRegionText}</div>
		</div>
	`}`;
  }

  // node_modules/@nldd/design-system/dist/components/content/code-viewer/code-viewer.i18n.js
  var nlddCodeViewerTranslations = {
    "components.code-viewer.region-label": "Code",
    "components.code-viewer.copy-action": "Kopieer",
    "components.code-viewer.copy-success-text": "Gekopieerd",
    "components.code-viewer.copy-failure-text": "Kopi\xEBren mislukt"
  };

  // node_modules/@nldd/design-system/dist/components/content/code-viewer/code-viewer.js
  init_color_scheme_repaint();
  init_icon_button();
  init_tooltip();
  var __decorate22 = function(decorators, target, key, desc) {
    var c6 = arguments.length, r6 = c6 < 3 ? target : desc === null ? desc = Object.getOwnPropertyDescriptor(target, key) : desc, d3;
    if (typeof Reflect === "object" && typeof Reflect.decorate === "function") r6 = Reflect.decorate(decorators, target, key, desc);
    else for (var i8 = decorators.length - 1; i8 >= 0; i8--) if (d3 = decorators[i8]) r6 = (c6 < 3 ? d3(r6) : c6 > 3 ? d3(target, key, r6) : d3(target, key)) || r6;
    return c6 > 3 && r6 && Object.defineProperty(target, key, r6), r6;
  };
  var COPY_FEEDBACK_DURATION_MS = 2e3;
  var GRAMMAR_LOADERS = {
    yaml: () => Promise.resolve().then(() => __toESM(require_prism_yaml(), 1)),
    json: () => Promise.resolve().then(() => __toESM(require_prism_json(), 1)),
    javascript: () => Promise.resolve().then(() => __toESM(require_prism_javascript(), 1)),
    typescript: () => Promise.resolve().then(() => __toESM(require_prism_typescript(), 1)),
    css: () => Promise.resolve().then(() => __toESM(require_prism_css(), 1)),
    html: () => Promise.resolve().then(() => __toESM(require_prism_markup(), 1)),
    xml: () => Promise.resolve().then(() => __toESM(require_prism_markup(), 1)),
    bash: () => Promise.resolve().then(() => __toESM(require_prism_bash(), 1)),
    markdown: () => Promise.resolve().then(() => __toESM(require_prism_markdown(), 1)),
    rust: () => Promise.resolve().then(() => __toESM(require_prism_rust(), 1)),
    gherkin: () => Promise.resolve().then(() => __toESM(require_prism_gherkin(), 1)),
    toml: () => Promise.resolve().then(() => __toESM(require_prism_toml(), 1)),
    sql: () => Promise.resolve().then(() => __toESM(require_prism_sql(), 1)),
    python: () => Promise.resolve().then(() => __toESM(require_prism_python(), 1))
  };
  var grammarLoads = /* @__PURE__ */ new Map();
  function loadGrammar(language) {
    const loader = GRAMMAR_LOADERS[language];
    if (!loader)
      return void 0;
    let pending = grammarLoads.get(language);
    if (!pending) {
      pending = loader().catch((err) => {
        grammarLoads.delete(language);
        throw err;
      });
      grammarLoads.set(language, pending);
    }
    return pending;
  }
  var NLDDCodeViewer = class NLDDCodeViewer2 extends i4 {
    constructor() {
      super(...arguments);
      this.variant = "box";
      this.background = "tinted";
      this.language = "";
      this.noCopy = false;
      this.wrap = false;
      this.translations = {};
      this._highlightedHtml = "";
      this._isScrollable = false;
      this._copyState = "idle";
      this._highlightPending = Promise.resolve();
    }
    render() {
      return codeViewerTemplate(this);
    }
    connectedCallback() {
      super.connectedCallback();
      this._unsubscribeScheme = onColorSchemeChange(() => this._repaintCodeBlock());
    }
    disconnectedCallback() {
      super.disconnectedCallback();
      this._unsubscribeScheme?.();
      this._unsubscribeScheme = void 0;
      this._resizeObserver?.disconnect();
      this._resizeObserver = void 0;
      clearTimeout(this._copyResetTimer);
      this._copyResetTimer = void 0;
    }
    firstUpdated() {
      const pre = this.shadowRoot?.querySelector(".code-viewer");
      if (!pre)
        return;
      this._resizeObserver = new ResizeObserver(() => this._updateScrollable(pre));
      this._resizeObserver.observe(pre);
      this._updateScrollable(pre);
    }
    _onSlotChange(e9) {
      this._refreshHighlight(e9.target);
    }
    updated(changed) {
      if (changed.has("language"))
        this._refreshHighlight();
      if (changed.size > 0 && !(changed.size === 1 && changed.has("_isScrollable"))) {
        const pre = this.shadowRoot?.querySelector(".code-viewer");
        if (pre)
          this._updateScrollable(pre);
      }
    }
    _t(key) {
      return this.translations[key] ?? nlddCodeViewerTranslations[key];
    }
    _updateScrollable(pre) {
      this._isScrollable = !this.wrap && pre.scrollWidth > pre.clientWidth;
    }
    /* Read the raw, unhighlighted slot text. Prism wraps tokens in spans for
     * the visual highlight, but the user clicking "copy" wants what they'd
     * have typed — so go to the assigned light-DOM nodes, not the rendered
     * shadow content. */
    _getRawText() {
      const slot = this.shadowRoot?.querySelector("slot");
      if (!slot)
        return "";
      return slot.assignedNodes({ flatten: true }).map((n7) => n7.textContent ?? "").join("");
    }
    async _onCopyClick() {
      try {
        await navigator.clipboard.writeText(this._getRawText());
        this._copyState = "success";
      } catch {
        this._copyState = "failure";
      }
      clearTimeout(this._copyResetTimer);
      this._copyResetTimer = setTimeout(() => {
        this._copyState = "idle";
      }, COPY_FEEDBACK_DURATION_MS);
    }
    /** Escape on the open feedback tooltip dismisses it early (WCAG 1.4.13:
     *  persistent hover/focus content must be dismissible without moving
     *  focus). nldd-tooltip emits nldd-tooltip-dismiss because the consumer
     *  owns its open lifecycle; we honour it by clearing the feedback state
     *  and cancelling the auto-reset timer. */
    _onCopyDismiss() {
      clearTimeout(this._copyResetTimer);
      this._copyState = "idle";
    }
    _repaintCodeBlock() {
      const block = this.shadowRoot?.querySelector(".code-viewer");
      if (block)
        forceScrollLayerRepaint(block);
    }
    /* Lazy grammar loading is async; surface the in-flight highlight
     * through updateComplete so consumers (and tests) can `await
     * el.updateComplete` and see the final highlighted output. */
    async getUpdateComplete() {
      const result = await super.getUpdateComplete();
      await this._highlightPending;
      return result;
    }
    _refreshHighlight(slot) {
      this._highlightPending = this._runHighlight(slot);
    }
    async _runHighlight(slot) {
      if (!this.language) {
        this._highlightedHtml = "";
        return;
      }
      const language = this.language;
      if (!import_prismjs.default.languages[language]) {
        const pending = loadGrammar(language);
        if (!pending) {
          this._highlightedHtml = "";
          return;
        }
        await pending;
        if (this.language !== language)
          return;
      }
      const grammar = import_prismjs.default.languages[language];
      if (!grammar) {
        this._highlightedHtml = "";
        return;
      }
      const target = slot ?? this.shadowRoot?.querySelector("slot");
      if (!target)
        return;
      const text = target.assignedNodes({ flatten: true }).map((n7) => n7.textContent ?? "").join("");
      this._highlightedHtml = import_prismjs.default.highlight(text, grammar, language);
    }
  };
  NLDDCodeViewer.styles = codeViewerStyles;
  __decorate22([
    n4({ type: String, reflect: true })
  ], NLDDCodeViewer.prototype, "variant", void 0);
  __decorate22([
    n4({ type: String, reflect: true })
  ], NLDDCodeViewer.prototype, "background", void 0);
  __decorate22([
    n4({ type: String, reflect: true })
  ], NLDDCodeViewer.prototype, "language", void 0);
  __decorate22([
    n4({ type: Boolean, reflect: true, attribute: "no-copy" })
  ], NLDDCodeViewer.prototype, "noCopy", void 0);
  __decorate22([
    n4({ type: Boolean, reflect: true })
  ], NLDDCodeViewer.prototype, "wrap", void 0);
  __decorate22([
    n4({ type: Object })
  ], NLDDCodeViewer.prototype, "translations", void 0);
  __decorate22([
    r5()
  ], NLDDCodeViewer.prototype, "_highlightedHtml", void 0);
  __decorate22([
    r5()
  ], NLDDCodeViewer.prototype, "_isScrollable", void 0);
  __decorate22([
    r5()
  ], NLDDCodeViewer.prototype, "_copyState", void 0);
  NLDDCodeViewer = __decorate22([
    t3("nldd-code-viewer")
  ], NLDDCodeViewer);

  // node_modules/@nldd/design-system/dist/components/forms/form/form.js
  var FORWARDED_ATTRIBUTES = [
    "name",
    "action",
    "method",
    "novalidate",
    "enctype",
    "target",
    "autocomplete"
  ];
  var DESCENDANT_SELECTOR = "nldd-form-field, nldd-form-actions";
  var NLDDForm = class extends HTMLElement {
    constructor() {
      super(...arguments);
      this._form = null;
      this._observer = null;
      this._userProvidedForm = false;
    }
    static get observedAttributes() {
      return [...FORWARDED_ATTRIBUTES, "label-alignment"];
    }
    connectedCallback() {
      if (!this._form) {
        const userForm = this.querySelector(":scope > form");
        if (userForm) {
          this._form = userForm;
          this._userProvidedForm = true;
          this._mirrorAttributes();
        } else {
          const form = document.createElement("form");
          this._form = form;
          this._mirrorAttributes();
          const initialChildren = Array.from(this.childNodes);
          for (const node of initialChildren) {
            form.appendChild(node);
          }
          this.appendChild(form);
        }
      }
      if (!this._observer) {
        const form = this._form;
        const userProvided = this._userProvidedForm;
        this._observer = new MutationObserver((mutations) => {
          for (const m3 of mutations) {
            if (m3.target !== this)
              continue;
            if (userProvided)
              continue;
            m3.addedNodes.forEach((node) => {
              if (node === form)
                return;
              form.appendChild(node);
            });
          }
          this._propagateLabelAlignment(this.getAttribute("label-alignment"));
        });
        this._observer.observe(this, { childList: true, subtree: true });
      }
      this._propagateLabelAlignment(this.getAttribute("label-alignment"));
    }
    disconnectedCallback() {
      this._observer?.disconnect();
      this._observer = null;
    }
    attributeChangedCallback(name, _oldVal, newVal) {
      if (name === "label-alignment") {
        this._propagateLabelAlignment(newVal);
        return;
      }
      if (!this._form)
        return;
      if (newVal === null)
        this._form.removeAttribute(name);
      else
        this._form.setAttribute(name, newVal);
    }
    /**
     * Push `label-alignment` to descendant nldd-form-field and nldd-form-actions
     * elements as a separate `form-label-alignment` attribute, defaulting to
     * `'top'` when the form itself has no `label-alignment`. Descendant CSS
     * uses `[label-alignment="X"]` first and falls back to
     * `[form-label-alignment="X"]` when no own value is set, so a consumer's
     * explicit `label-alignment` always wins automatically — without us having
     * to track which elements we previously inherited to.
     *
     * Always-setting (rather than only when the form has its own value) lets
     * downstream selectors assume the attribute is present on every descendant
     * inside an `nldd-form`, which keeps form.css's adjacent-sibling tight-gap
     * rule simple.
     *
     * The two-attribute split also avoids the historical Lit reflect-default
     * vs MutationObserver timing race: we never touch `label-alignment` on
     * the descendant, so a freshly upgraded Lit element reflecting its default
     * doesn't conflict with the form's intent.
     */
    _propagateLabelAlignment(value) {
      const fields = this.querySelectorAll(DESCENDANT_SELECTOR);
      fields.forEach((f3) => f3.setAttribute("form-label-alignment", value ?? "top"));
    }
    _mirrorAttributes() {
      if (!this._form)
        return;
      for (const attr of FORWARDED_ATTRIBUTES) {
        const val = this.getAttribute(attr);
        if (val !== null)
          this._form.setAttribute(attr, val);
      }
    }
    /** The inner <form> element. Useful for `form.checkValidity()` etc. */
    get form() {
      return this._form;
    }
  };
  if (!customElements.get("nldd-form")) {
    customElements.define("nldd-form", NLDDForm);
  }

  // node_modules/@nldd/design-system/dist/components/forms/form-actions/form-actions.js
  init_lit();
  init_decorators();

  // node_modules/@nldd/design-system/dist/components/forms/form-actions/form-actions.styles.js
  init_lit();
  init_breakpoints();
  var mdMin2 = r(breakpoints.mdMin);
  var formActionsStyles = i`


	/* # Host */

	:host {
		display: block;
		container-type: inline-size;
	}

	:host([hidden]) {
		display: none;
	}


	/* # Block */

	.form-actions {
		display: flex;
		flex-direction: column;
	}

	:host([label-alignment="left"]) .form-actions,
	:host([label-alignment="right"]) .form-actions,
	:host(:not([label-alignment])[form-label-alignment="left"]) .form-actions,
	:host(:not([label-alignment])[form-label-alignment="right"]) .form-actions {
		@container (min-width: ${mdMin2}) {
			flex-direction: row;
			gap: var(--semantics-forms-columns-gap);
			align-items: start;
		}
	}

	:host([label-alignment="left"]) .form-actions::before,
	:host([label-alignment="right"]) .form-actions::before,
	:host(:not([label-alignment])[form-label-alignment="left"]) .form-actions::before,
	:host(:not([label-alignment])[form-label-alignment="right"]) .form-actions::before {
		@container (min-width: ${mdMin2}) {
			content: '';
			width: var(--semantics-forms-label-column-width);
			flex-grow: 0;
			flex-shrink: 0;
		}
	}


	/* # Elements */

	.form-actions__main {
		display: flex;
		min-width: 0;
		flex-grow: 1;
		flex-shrink: 1;
		flex-basis: 0;
	}
`;

  // node_modules/@nldd/design-system/dist/components/forms/form-actions/form-actions.template.js
  init_lit();
  function formActionsTemplate() {
    return b2`
		<div class="form-actions">
			<div class="form-actions__main">
				<slot></slot>
			</div>
		</div>
	`;
  }

  // node_modules/@nldd/design-system/dist/components/forms/form-actions/form-actions.js
  var __decorate23 = function(decorators, target, key, desc) {
    var c6 = arguments.length, r6 = c6 < 3 ? target : desc === null ? desc = Object.getOwnPropertyDescriptor(target, key) : desc, d3;
    if (typeof Reflect === "object" && typeof Reflect.decorate === "function") r6 = Reflect.decorate(decorators, target, key, desc);
    else for (var i8 = decorators.length - 1; i8 >= 0; i8--) if (d3 = decorators[i8]) r6 = (c6 < 3 ? d3(r6) : c6 > 3 ? d3(target, key, r6) : d3(target, key)) || r6;
    return c6 > 3 && r6 && Object.defineProperty(target, key, r6), r6;
  };
  var NLDDFormActions = class NLDDFormActions2 extends i4 {
    constructor() {
      super(...arguments);
      this.labelAlignment = void 0;
    }
    render() {
      return formActionsTemplate();
    }
  };
  NLDDFormActions.styles = formActionsStyles;
  __decorate23([
    n4({ type: String, reflect: true, attribute: "label-alignment" })
  ], NLDDFormActions.prototype, "labelAlignment", void 0);
  NLDDFormActions = __decorate23([
    t3("nldd-form-actions")
  ], NLDDFormActions);

  // node_modules/@nldd/design-system/dist/components/forms/form-field/form-field.js
  init_lit();
  init_decorators();

  // node_modules/@nldd/design-system/dist/components/forms/form-field/form-field.styles.js
  init_lit();
  init_breakpoints();
  init_slotted_reset();
  var mdMin3 = r(breakpoints.mdMin);
  var formFieldStyles = i`


	/* # Host */

	:host {
		--_gap: var(--primitives-space-3);

		${inheritedTextReset}
		display: block;
		container-type: inline-size;
	}

	:host([hidden]) {
		display: none;
	}


	/* # Block */

	.form-field {
		display: flex;
		flex-direction: column;
		gap: var(--_gap);
	}

	:host([label-alignment="left"]) .form-field,
	:host([label-alignment="right"]) .form-field,
	:host(:not([label-alignment])[form-label-alignment="left"]) .form-field,
	:host(:not([label-alignment])[form-label-alignment="right"]) .form-field {
		@container (min-width: ${mdMin3}) {
			flex-direction: row;
			gap: var(--semantics-forms-columns-gap);
			align-items: start;
		}
	}


	/* # Header */

	.form-field__header {
		box-sizing: border-box;
		display: flex;
		/* Paints above .form-field__main so a label descender stays readable where it
		   overlaps the input focus ring — this is what lets the top-aligned --_gap stay
		   tight. Reset to auto in the side-by-side layout below (no vertical overlap there). */
		z-index: 1;
		flex-direction: column;
	}

	:host([label-alignment="left"]) .form-field__header,
	:host([label-alignment="right"]) .form-field__header,
	:host(:not([label-alignment])[form-label-alignment="left"]) .form-field__header,
	:host(:not([label-alignment])[form-label-alignment="right"]) .form-field__header {
		@container (min-width: ${mdMin3}) {
			z-index: auto;
			width: var(--semantics-forms-label-column-width);
			min-height: var(--semantics-controls-md-min-size);
			flex-grow: 0;
			flex-shrink: 0;
			justify-content: center;
		}
	}

	:host([label-alignment="right"]) .form-field__header,
	:host(:not([label-alignment])[form-label-alignment="right"]) .form-field__header {
		@container (min-width: ${mdMin3}) {
			align-items: end;
			text-align: right;
		}
	}

	:host([label-alignment="left"]) .form-field__header,
	:host(:not([label-alignment])[form-label-alignment="left"]) .form-field__header {
		@container (min-width: ${mdMin3}) {
			align-items: start;
			text-align: left;
		}
	}

	.form-field__header.is-empty {
		display: none;
	}

	:host([label-alignment="left"]) .form-field__header.is-empty,
	:host([label-alignment="right"]) .form-field__header.is-empty,
	:host(:not([label-alignment])[form-label-alignment="left"]) .form-field__header.is-empty,
	:host(:not([label-alignment])[form-label-alignment="right"]) .form-field__header.is-empty {
		@container (min-width: ${mdMin3}) {
			display: flex;
		}
	}


	/* # Label */

	.form-field__label {
		display: inline-flex;
		gap: var(--primitives-space-4);
		align-items: baseline;
		color: var(--semantics-content-color);
		font: var(--primitives-font-body-md-regular-tight);
		text-wrap: pretty;
	}

	:host([label-alignment="left"]) .form-field__label,
	:host([label-alignment="right"]) .form-field__label,
	:host(:not([label-alignment])[form-label-alignment="left"]) .form-field__label,
	:host(:not([label-alignment])[form-label-alignment="right"]) .form-field__label {
		@container (min-width: ${mdMin3}) {
			display: flex;
			flex-direction: column;
			gap: var(--primitives-space-0);
		}
	}

	:host([label-alignment="right"]) .form-field__label,
	:host(:not([label-alignment])[form-label-alignment="right"]) .form-field__label {
		@container (min-width: ${mdMin3}) {
			align-items: end;
		}
	}

	:host([label-alignment="left"]) .form-field__label,
	:host(:not([label-alignment])[form-label-alignment="left"]) .form-field__label {
		@container (min-width: ${mdMin3}) {
			align-items: start;
		}
	}


	/* # Optional indicator */

	.form-field__optional {
		color: var(--semantics-content-secondary-color);
		font: var(--primitives-font-body-xs-regular-tight);
	}


	/* # Supporting label */

	.form-field__supporting-label {
		color: var(--semantics-content-secondary-color);
		font: var(--primitives-font-body-xs-regular-tight);
		text-wrap: pretty;
	}


	/* # Main */

	.form-field__main {
		display: flex;
		min-width: 0;
		flex-direction: column;
		flex-grow: 1;
		flex-shrink: 1;
		flex-basis: 0;
	}


	/* # Errors */

	.form-field__errors {
		display: flex;
		flex-direction: column;
	}

	:host(.has-errors) .form-field__errors {
		margin-top: var(--primitives-space-2);
	}
`;
  var formFieldHelpTextStyles = i`


	/* # Host */

	:host {
		${inheritedTextReset}
		display: contents;
	}


	/* # Help text */

	.form-field__help-text {
		margin: var(--primitives-space-2) 0 0;
		color: var(--semantics-content-color);
		font: var(--primitives-font-body-sm-regular-tight);
	}


	/* # Links */

	::slotted(a) {
		${slottedReset}
		${inheritedTextReset}
		border-radius: var(--primitives-corner-radius-xxs) !important;
		color: var(--semantics-links-color) !important;
		text-decoration: underline !important;
		text-underline-offset: var(--primitives-space-2) !important;
	}

	@media (hover: hover) {
		::slotted(a:hover) {
			color: var(--semantics-links-is-hovered-color) !important;
		}
	}

	::slotted(a:active) {
		color: var(--semantics-links-is-active-color) !important;
	}

	::slotted(a:focus-visible) {
		outline: var(--semantics-focus-ring-outline) !important;
		outline-offset: var(--semantics-focus-ring-outline-offset) !important;
		box-shadow: var(--semantics-focus-ring-box-shadow) !important;
	}

	::slotted(a:focus:not(:focus-visible)) {
		outline: none !important;
	}
`;
  var formFieldErrorTextStyles = i`


	/* # Host */

	:host {
		${inheritedTextReset}
		display: none;
	}

	:host([invalid]) {
		display: block;
	}


	/* # Error text */

	.form-field__error-text {
		margin: 0;
		color: var(--semantics-content-critical-color);
		font: var(--primitives-font-body-sm-regular-tight);
	}


	/* # Links */

	::slotted(a) {
		${slottedReset}
		${inheritedTextReset}
		border-radius: var(--primitives-corner-radius-xxs) !important;
		color: var(--semantics-links-color) !important;
		text-decoration: underline !important;
		text-underline-offset: var(--primitives-space-2) !important;
	}

	@media (hover: hover) {
		::slotted(a:hover) {
			color: var(--semantics-links-is-hovered-color) !important;
		}
	}

	::slotted(a:active) {
		color: var(--semantics-links-is-active-color) !important;
	}

	::slotted(a:focus-visible) {
		outline: var(--semantics-focus-ring-outline) !important;
		outline-offset: var(--semantics-focus-ring-outline-offset) !important;
		box-shadow: var(--semantics-focus-ring-box-shadow) !important;
	}

	::slotted(a:focus:not(:focus-visible)) {
		outline: none !important;
	}
`;

  // node_modules/@nldd/design-system/dist/components/forms/form-field/form-field.template.js
  init_lit();
  function renderOptional(label) {
    return b2`<span class="form-field__optional">${label}</span>`;
  }
  function formFieldTemplate(component) {
    const hasLabel = Boolean(component.label);
    const hasSupportingLabel = Boolean(component.supportingLabel);
    const isHeaderEmpty = !hasLabel && !hasSupportingLabel;
    const headerEl = b2`
		<div class="form-field__header ${isHeaderEmpty ? "is-empty" : ""}">
			${hasLabel ? b2`
				<label class="form-field__label"
					@click=${(e9) => component._focusInput(e9)}
				>
					${component.label}
					${component.optional ? renderOptional(component.optionalLabel) : A}
				</label>
			` : A}
			${hasSupportingLabel ? b2`
				<span class="form-field__supporting-label">${component.supportingLabel}</span>
			` : A}
		</div>
	`;
    return b2`
		<div class="form-field">
			${headerEl}
			<div class="form-field__main">
				<slot></slot>
				<div class="form-field__errors"
					aria-live="polite"
				>
					<slot name="errors"></slot>
				</div>
				<slot name="help"></slot>
			</div>
		</div>
	`;
  }
  function formFieldHelpTextTemplate(_component) {
    return b2`
		<p class="form-field__help-text">
			<slot></slot>
		</p>
	`;
  }
  function formFieldErrorTextTemplate(_component) {
    return b2`
		<p class="form-field__error-text">
			<slot></slot>
		</p>
	`;
  }

  // node_modules/@nldd/design-system/dist/components/forms/form-field/form-field.js
  var __decorate24 = function(decorators, target, key, desc) {
    var c6 = arguments.length, r6 = c6 < 3 ? target : desc === null ? desc = Object.getOwnPropertyDescriptor(target, key) : desc, d3;
    if (typeof Reflect === "object" && typeof Reflect.decorate === "function") r6 = Reflect.decorate(decorators, target, key, desc);
    else for (var i8 = decorators.length - 1; i8 >= 0; i8--) if (d3 = decorators[i8]) r6 = (c6 < 3 ? d3(r6) : c6 > 3 ? d3(target, key, r6) : d3(target, key)) || r6;
    return c6 > 3 && r6 && Object.defineProperty(target, key, r6), r6;
  };
  var HELPER_TAGS = ["nldd-form-field-help-text", "nldd-form-field-error-text"];
  function generateId() {
    const uuid = crypto.randomUUID?.() ?? Math.random().toString(36).slice(2);
    return `nldd-field-input-${uuid}`;
  }
  var NLDDFormField = class NLDDFormField2 extends i4 {
    constructor() {
      super(...arguments);
      this.labelAlignment = void 0;
      this.label = "";
      this.supportingLabel = "";
      this.optional = false;
      this.optionalLabel = "Optioneel";
      this._childObserver = null;
      this._observer = null;
      this._hasErrors = false;
    }
    render() {
      return formFieldTemplate(this);
    }
    updated(changed) {
      this.classList.toggle("has-errors", this._hasErrors);
      if (changed.has("label")) {
        this._onSlotChange();
      }
    }
    connectedCallback() {
      super.connectedCallback();
      this._childObserver = new MutationObserver(() => this._onSlotChange());
      this._childObserver.observe(this, { childList: true });
    }
    firstUpdated() {
      this._onSlotChange();
    }
    disconnectedCallback() {
      super.disconnectedCallback();
      this._childObserver?.disconnect();
      this._observer?.disconnect();
    }
    /** Called when the label header is clicked — focuses the slotted input. */
    _focusInput(e9) {
      const input = this._findInput();
      if (!input)
        return;
      const tag = input.tagName.toLowerCase();
      const type = input.type?.toLowerCase();
      if (tag !== "input" || type !== "checkbox" && type !== "radio") {
        e9.preventDefault();
      }
      input.focus();
    }
    _onSlotChange() {
      this._observer?.disconnect();
      const input = this._findInput();
      if (!input)
        return;
      const isCustomInput = "inputId" in input;
      if (isCustomInput) {
        const existingId = input.inputId;
        const generatedId = existingId || generateId();
        input.inputId = generatedId;
      } else {
        if (!input.id)
          input.id = generateId();
      }
      if (this.label) {
        if (isCustomInput) {
          input.setAttribute("accessible-label", this.label);
          input.removeAttribute("aria-label");
        } else {
          input.setAttribute("aria-label", this.label);
          input.removeAttribute("accessible-label");
        }
      } else {
        input.removeAttribute("accessible-label");
        input.removeAttribute("aria-label");
      }
      Array.from(this.children).filter((el) => el.tagName.toLowerCase() === "nldd-form-field-help-text").forEach((el) => {
        if (!el.id)
          el.id = generateId();
      });
      this._observer = new MutationObserver(() => this._syncErrorText());
      this._observer.observe(input, {
        attributes: true,
        attributeFilter: ["invalid", "error-message"]
      });
      this._syncErrorText();
    }
    /** First child element that is not a form field helper component. */
    _findInput() {
      return Array.from(this.children).find((el) => !HELPER_TAGS.includes(el.tagName.toLowerCase()));
    }
    /**
     * Reads `invalid` and `error-message` from the input and toggles
     * the `invalid` attribute on the referenced nldd-form-field-error-text elements.
     * Also sets `aria-describedby` on the input to reference visible error texts.
     *
     * Note: this mechanism relies on the slotted input reflecting an `invalid`
     * attribute, which nldd-text-field and nldd-password-field do. Plain native
     * `<input>` elements use constraint validation (validity.valid, the `invalid`
     * event) instead — support for native inputs is a known limitation and
     * tracked as a follow-up.
     */
    _syncErrorText() {
      const input = this._findInput();
      if (!input)
        return;
      const isCustomInput = "inputId" in input;
      const isInvalid = input.hasAttribute("invalid");
      const referencedIds = (input.getAttribute("error-message") ?? "").split(" ").filter(Boolean);
      const allErrorTexts = Array.from(this.children).filter((el) => el.tagName.toLowerCase() === "nldd-form-field-error-text");
      const helpIds = Array.from(this.children).filter((el) => el.tagName.toLowerCase() === "nldd-form-field-help-text" && el.id).map((el) => el.id);
      const visibleErrorIds = [];
      for (const el of allErrorTexts) {
        const shouldShow = isInvalid && referencedIds.includes(el.id);
        el.toggleAttribute("invalid", shouldShow);
        if (shouldShow && el.id)
          visibleErrorIds.push(el.id);
      }
      const describedByIds = [...helpIds, ...visibleErrorIds];
      const describedByValue = describedByIds.join(" ");
      if (isCustomInput) {
        input.errorMessageIds = describedByValue;
      } else {
        if (describedByIds.length > 0) {
          input.setAttribute("aria-describedby", describedByValue);
        } else {
          input.removeAttribute("aria-describedby");
        }
      }
      this._hasErrors = visibleErrorIds.length > 0;
    }
  };
  NLDDFormField.styles = formFieldStyles;
  __decorate24([
    n4({ type: String, attribute: "label-alignment", reflect: true })
  ], NLDDFormField.prototype, "labelAlignment", void 0);
  __decorate24([
    n4({ type: String })
  ], NLDDFormField.prototype, "label", void 0);
  __decorate24([
    n4({ type: String, attribute: "supporting-label" })
  ], NLDDFormField.prototype, "supportingLabel", void 0);
  __decorate24([
    n4({ type: Boolean })
  ], NLDDFormField.prototype, "optional", void 0);
  __decorate24([
    n4({ type: String, attribute: "optional-label" })
  ], NLDDFormField.prototype, "optionalLabel", void 0);
  __decorate24([
    r5()
  ], NLDDFormField.prototype, "_hasErrors", void 0);
  NLDDFormField = __decorate24([
    t3("nldd-form-field")
  ], NLDDFormField);
  var NLDDFormFieldHelpText = class NLDDFormFieldHelpText2 extends i4 {
    connectedCallback() {
      super.connectedCallback();
      this.slot = "help";
    }
    render() {
      return formFieldHelpTextTemplate(this);
    }
  };
  NLDDFormFieldHelpText.styles = formFieldHelpTextStyles;
  NLDDFormFieldHelpText = __decorate24([
    t3("nldd-form-field-help-text")
  ], NLDDFormFieldHelpText);
  var NLDDFormFieldErrorText = class NLDDFormFieldErrorText2 extends i4 {
    constructor() {
      super(...arguments);
      this.invalid = false;
    }
    connectedCallback() {
      super.connectedCallback();
      this.slot = "errors";
    }
    render() {
      return formFieldErrorTextTemplate(this);
    }
  };
  NLDDFormFieldErrorText.styles = formFieldErrorTextStyles;
  __decorate24([
    n4({ type: Boolean, reflect: true })
  ], NLDDFormFieldErrorText.prototype, "invalid", void 0);
  NLDDFormFieldErrorText = __decorate24([
    t3("nldd-form-field-error-text")
  ], NLDDFormFieldErrorText);

  // node_modules/@nldd/design-system/dist/components/forms/form-section/form-section.js
  var import_meta7 = {};
  var FIELDSET_CLASS = "form-section";
  var HEADER_CLASS = "form-section__header";
  var TITLE_CLASS = "form-section__title";
  var SUBTITLE_CLASS = "form-section__subtitle";
  var MAIN_CLASS = "form-section__main";
  var NLDDFormSection = class extends HTMLElement {
    constructor() {
      super(...arguments);
      this._fieldset = null;
      this._legend = null;
      this._main = null;
      this._observer = null;
      this._hasWarnedNoLabel = false;
    }
    static get observedAttributes() {
      return ["text", "supporting-text"];
    }
    get text() {
      return this.getAttribute("text") ?? "";
    }
    set text(value) {
      if (value)
        this.setAttribute("text", value);
      else
        this.removeAttribute("text");
    }
    get supportingText() {
      return this.getAttribute("supporting-text") ?? "";
    }
    set supportingText(value) {
      if (value)
        this.setAttribute("supporting-text", value);
      else
        this.removeAttribute("supporting-text");
    }
    connectedCallback() {
      if (!this._fieldset)
        this._buildStructure();
      this._renderLegend();
      this._warnIfNoLabel();
      if (!this._observer) {
        const main = this._main;
        this._observer = new MutationObserver((mutations) => {
          for (const m3 of mutations) {
            if (m3.target !== this)
              continue;
            m3.addedNodes.forEach((node) => {
              if (node === this._fieldset)
                return;
              main?.appendChild(node);
            });
          }
        });
        this._observer.observe(this, { childList: true });
      }
    }
    disconnectedCallback() {
      this._observer?.disconnect();
      this._observer = null;
    }
    attributeChangedCallback(name) {
      if (name === "text" || name === "supporting-text") {
        if (this._legend) {
          this._renderLegend();
          this._warnIfNoLabel();
        }
      }
    }
    _warnIfNoLabel() {
      if (!import_meta7.env?.DEV)
        return;
      if (this._hasWarnedNoLabel)
        return;
      if (this.text || this.supportingText)
        return;
      this._hasWarnedNoLabel = true;
      console.warn('<nldd-form-section>: No `text` or `supporting-text` provided. The <fieldset> has no accessible name and screen readers may announce it as just "group" (Chrome) or nothing (Firefox). Set `text` for a group label, or document this section as purely visual grouping.');
    }
    _buildStructure() {
      const initialChildren = Array.from(this.childNodes);
      const fieldset = document.createElement("fieldset");
      fieldset.className = FIELDSET_CLASS;
      const legend = document.createElement("legend");
      legend.className = HEADER_CLASS;
      const main = document.createElement("div");
      main.className = MAIN_CLASS;
      fieldset.append(legend, main);
      for (const child of initialChildren) {
        main.appendChild(child);
      }
      this.appendChild(fieldset);
      this._fieldset = fieldset;
      this._legend = legend;
      this._main = main;
    }
    _renderLegend() {
      const legend = this._legend;
      if (!legend)
        return;
      const text = this.text;
      const supportingText = this.supportingText;
      const hasContent = !!text || !!supportingText;
      legend.textContent = "";
      if (text) {
        const titleSpan = document.createElement("span");
        titleSpan.className = TITLE_CLASS;
        titleSpan.textContent = text;
        legend.appendChild(titleSpan);
      }
      if (supportingText) {
        const subtitleSpan = document.createElement("span");
        subtitleSpan.className = SUBTITLE_CLASS;
        subtitleSpan.textContent = supportingText;
        legend.appendChild(subtitleSpan);
      }
      legend.hidden = !hasContent;
    }
  };
  if (!customElements.get("nldd-form-section")) {
    customElements.define("nldd-form-section", NLDDFormSection);
  }

  // node_modules/@nldd/design-system/dist/components/inputs/text-field/text-field.js
  init_lit();
  init_decorators();

  // node_modules/@nldd/design-system/dist/components/inputs/text-field/text-field.styles.js
  init_lit();
  init_slotted_reset();
  var textFieldStyles = i`


	/* # Host */

	:host {
		--_width: 100%;
		--_corner-radius: var(--semantics-controls-md-corner-radius);
		--_background-color: var(--semantics-input-fields-background-color);
		--_min-size: var(--semantics-controls-md-min-size);
		--_inline-padding: var(--semantics-controls-md-inline-padding);
		--_text-font: var(--semantics-input-fields-md-text-font);
		--_validation-icon-size: var(--semantics-input-fields-md-validation-icon-size);

		${inheritedTextReset}
		display: block;
		width: var(--_width);
		max-width: 100%;
		-webkit-tap-highlight-color: transparent;
	}

	:host([hidden]) {
		display: none;
	}

	:host([size="sm"]) {
		--_corner-radius: var(--semantics-controls-sm-corner-radius);
		--_min-size: var(--semantics-controls-sm-min-size);
		--_inline-padding: var(--semantics-controls-sm-inline-padding);
		--_text-font: var(--semantics-input-fields-sm-text-font);
		--_validation-icon-size: var(--semantics-input-fields-sm-validation-icon-size);
	}


	/* # Block */

	.text-field {
		box-sizing: border-box;
		display: flex;
		border: var(--semantics-input-fields-border);
		border-radius: var(--_corner-radius);
		background-color: var(--_background-color);
		min-height: var(--_min-size);
		overflow: hidden;
		padding-left: calc(var(--_inline-padding) - var(--semantics-input-fields-border-thickness));
		flex-direction: row;
		align-items: center;
	}

	:host([valid]) .text-field {
		border-color: var(--semantics-input-fields-is-valid-border-color);
	}

	:host([invalid]) .text-field {
		border-color: var(--semantics-input-fields-is-invalid-border-color);
	}

	:host([readonly]) .text-field {
		--_background-color: var(--semantics-input-fields-is-read-only-background-color);
		border-color: var(--semantics-input-fields-is-read-only-border-color);
	}

	:host([disabled]) .text-field {
		opacity: var(--primitives-opacity-disabled);
	}

	.text-field:has(input:-webkit-autofill),
	.text-field:has(input:autofill) {
		--_background-color: var(--semantics-input-fields-is-autofill-background-color);
	}

	.text-field:focus-within {
		outline: var(--semantics-focus-ring-outline);
		outline-offset: var(--semantics-focus-ring-outline-offset);
		box-shadow: var(--semantics-focus-ring-box-shadow);
	}


	/* # Elements */

	.text-field__input {
		box-sizing: border-box;
		margin: 0;
		outline: none;
		border: none;
		background: transparent;
		min-width: 0;
		min-height: calc(var(--_min-size) - var(--semantics-input-fields-border-thickness) * 2);
		overflow: hidden;
		padding: 0;
		flex-grow: 1;
		color: var(--semantics-content-color);
		font: var(--_text-font);
		appearance: none;
	}

	:host([disabled]) .text-field__input {
		pointer-events: none;
	}

	.text-field__input::placeholder {
		color: var(--semantics-input-fields-placeholder-color);
	}

	.text-field__input:-webkit-autofill,
	.text-field__input:autofill,
	.text-field__input:-webkit-autofill:disabled,
	.text-field__input:autofill:disabled {
		box-shadow: 0 0 0 999px var(--_background-color) inset;
		-webkit-text-fill-color: var(--semantics-input-fields-is-autofill-content-color);
	}

	.text-field__input-fade {
		position: relative;
		width: 0;
		flex-shrink: 0;
		align-self: stretch;
	}

	.text-field__input-fade::after {
		content: '';
		position: absolute;
		top: 0;
		right: 0;
		bottom: 0;
		border-radius: var(--_corner-radius);
		background: linear-gradient(90deg, color-mix(in oklch, var(--_background-color) 0%, transparent) 0%, var(--_background-color) 100%);
		pointer-events: none;
		width: var(--primitives-space-8);
	}

	.text-field__validation-icon-area {
		display: flex;
		width: calc(var(--_min-size) - var(--semantics-input-fields-border-thickness) * 2);
		height: 100%;
		flex-shrink: 0;
		align-items: center;
		justify-content: center;
	}

	:host([valid]) .text-field__validation-icon-area {
		color: var(--semantics-input-fields-is-valid-icon-color);
	}

	:host([invalid]) .text-field__validation-icon-area {
		color: var(--semantics-input-fields-is-invalid-icon-color);
	}

	.text-field__validation-icon {
		width: var(--_validation-icon-size);
		height: var(--_validation-icon-size);
	}
`;

  // node_modules/@nldd/design-system/dist/components/inputs/text-field/text-field.template.js
  init_lit();
  init_icon();
  function renderValidationIcon(component) {
    if (component.invalid) {
      return b2`
			<div class="text-field__validation-icon-area">
				<nldd-icon class="text-field__validation-icon"
					name="invalid"
					aria-hidden="true"
				></nldd-icon>
			</div>
		`;
    }
    if (component.valid) {
      return b2`
			<div class="text-field__validation-icon-area">
				<nldd-icon class="text-field__validation-icon"
					name="valid"
					aria-hidden="true"
				></nldd-icon>
			</div>
		`;
    }
    return A;
  }
  function textFieldTemplate(component) {
    return b2`
		<div class="text-field">
			<input class="text-field__input"
				id=${component.inputId || A}
				type=${component.type}
				.value=${component.value}
				placeholder=${component.placeholder || A}
				?disabled=${component.disabled}
				?readonly=${component.readonly}
				?required=${component.required}
				name=${component.name || A}
				autocomplete=${component.autocomplete || A}
				spellcheck=${component.noSpellcheck ? "false" : "true"}
				aria-label=${component.accessibleLabel || A}
				aria-describedby=${component.errorMessageIds || A}
				aria-invalid=${component.invalid ? "true" : A}
				@input=${component._handleInput}
				@change=${component._handleChange}
			/>
			<div class="text-field__input-fade"></div>
			${renderValidationIcon(component)}
		</div>
	`;
  }

  // node_modules/@nldd/design-system/dist/components/inputs/text-field/text-field.js
  var __decorate25 = function(decorators, target, key, desc) {
    var c6 = arguments.length, r6 = c6 < 3 ? target : desc === null ? desc = Object.getOwnPropertyDescriptor(target, key) : desc, d3;
    if (typeof Reflect === "object" && typeof Reflect.decorate === "function") r6 = Reflect.decorate(decorators, target, key, desc);
    else for (var i8 = decorators.length - 1; i8 >= 0; i8--) if (d3 = decorators[i8]) r6 = (c6 < 3 ? d3(r6) : c6 > 3 ? d3(target, key, r6) : d3(target, key)) || r6;
    return c6 > 3 && r6 && Object.defineProperty(target, key, r6), r6;
  };
  var NLDDTextField = class NLDDTextField2 extends i4 {
    constructor() {
      super(...arguments);
      this._internals = this.attachInternals();
      this._initialValue = "";
      this.size = "md";
      this.value = "";
      this.inputId = "";
      this.placeholder = "";
      this.invalid = false;
      this.valid = false;
      this.disabled = false;
      this.type = "text";
      this.name = "";
      this.readonly = false;
      this.required = false;
      this.autocomplete = "";
      this.accessibleLabel = "";
      this.errorMessageIds = "";
      this.noSpellcheck = false;
      this.width = "";
    }
    firstUpdated() {
      this._initialValue = this.value;
    }
    updated(changed) {
      if (changed.has("width")) {
        const w2 = this.width;
        if (w2 && w2 !== "full" && CSS.supports("width", w2)) {
          this.style.setProperty("--_width", w2);
        } else {
          this.style.removeProperty("--_width");
        }
      }
      if (changed.has("value")) {
        this._internals.setFormValue(this.value);
      }
    }
    formResetCallback() {
      this.value = this._initialValue;
    }
    formDisabledCallback(disabled) {
      this.disabled = disabled;
    }
    formStateRestoreCallback(state) {
      if (typeof state === "string")
        this.value = state;
    }
    _handleInput(e9) {
      e9.stopPropagation();
      const input = e9.target;
      this.value = input.value;
      this.dispatchEvent(new CustomEvent("input", {
        detail: { value: this.value },
        bubbles: true,
        composed: true
      }));
    }
    _handleChange(e9) {
      e9.stopPropagation();
      const input = e9.target;
      this.value = input.value;
      this.dispatchEvent(new CustomEvent("change", {
        detail: { value: this.value },
        bubbles: true,
        composed: true
      }));
    }
    blur() {
      this._input?.blur();
    }
    /**
     * Delegates focus to the inner native `<input>`, so consumers can call
     * `textFieldEl.focus()` without reaching into shadow DOM.
     */
    focus(options) {
      this._input?.focus(options);
    }
    render() {
      return textFieldTemplate(this);
    }
  };
  NLDDTextField.formAssociated = true;
  NLDDTextField.shadowRootOptions = {
    ...i4.shadowRootOptions,
    delegatesFocus: true
  };
  NLDDTextField.styles = textFieldStyles;
  __decorate25([
    n4({ type: String, reflect: true })
  ], NLDDTextField.prototype, "size", void 0);
  __decorate25([
    n4({ type: String })
  ], NLDDTextField.prototype, "value", void 0);
  __decorate25([
    n4({ type: String, attribute: "input-id" })
  ], NLDDTextField.prototype, "inputId", void 0);
  __decorate25([
    n4({ type: String })
  ], NLDDTextField.prototype, "placeholder", void 0);
  __decorate25([
    n4({ type: Boolean, reflect: true })
  ], NLDDTextField.prototype, "invalid", void 0);
  __decorate25([
    n4({ type: Boolean, reflect: true })
  ], NLDDTextField.prototype, "valid", void 0);
  __decorate25([
    n4({ type: Boolean, reflect: true })
  ], NLDDTextField.prototype, "disabled", void 0);
  __decorate25([
    n4({ type: String })
  ], NLDDTextField.prototype, "type", void 0);
  __decorate25([
    n4({ type: String, reflect: true })
  ], NLDDTextField.prototype, "name", void 0);
  __decorate25([
    n4({ type: Boolean, reflect: true })
  ], NLDDTextField.prototype, "readonly", void 0);
  __decorate25([
    n4({ type: Boolean, reflect: true })
  ], NLDDTextField.prototype, "required", void 0);
  __decorate25([
    n4({ type: String })
  ], NLDDTextField.prototype, "autocomplete", void 0);
  __decorate25([
    n4({ type: String, attribute: "accessible-label" })
  ], NLDDTextField.prototype, "accessibleLabel", void 0);
  __decorate25([
    n4({ type: String, attribute: "error-message-ids" })
  ], NLDDTextField.prototype, "errorMessageIds", void 0);
  __decorate25([
    n4({ type: Boolean, reflect: true, attribute: "no-spellcheck" })
  ], NLDDTextField.prototype, "noSpellcheck", void 0);
  __decorate25([
    n4({ type: String, reflect: true })
  ], NLDDTextField.prototype, "width", void 0);
  __decorate25([
    e5(".text-field__input")
  ], NLDDTextField.prototype, "_input", void 0);
  NLDDTextField = __decorate25([
    t3("nldd-text-field")
  ], NLDDTextField);

  // node_modules/@nldd/design-system/dist/components/inputs/multi-line-text-field/multi-line-text-field.js
  init_lit();
  init_decorators();

  // node_modules/@nldd/design-system/dist/components/inputs/multi-line-text-field/multi-line-text-field.styles.js
  init_lit();
  init_slotted_reset();
  var multiLineTextFieldStyles = i`


	/* # Host */

	:host {
		--_width: 100%;
		--_background-color: var(--semantics-input-fields-background-color);
		--_corner-radius: var(--semantics-controls-md-corner-radius);
		--_inline-padding: calc(var(--semantics-controls-md-inline-padding) - var(--semantics-input-fields-border-thickness));
		--_min-size: var(--semantics-controls-md-min-size);
		--_text-font: var(--semantics-input-fields-md-text-font);
		--_icon-area-size: calc(var(--_min-size) - var(--semantics-input-fields-border-thickness) * 2);
		--_validation-icon-size: var(--semantics-input-fields-md-validation-icon-size);

		${inheritedTextReset}
		display: block;
		width: var(--_width);
		max-width: 100%;
		-webkit-tap-highlight-color: transparent;
	}

	:host([hidden]) {
		display: none;
	}

	:host([size="sm"]) {
		--_corner-radius: var(--semantics-controls-sm-corner-radius);
		--_inline-padding: calc(var(--semantics-controls-sm-inline-padding) - var(--semantics-input-fields-border-thickness));
		--_min-size: var(--semantics-controls-sm-min-size);
		--_text-font: var(--semantics-input-fields-sm-text-font);
		--_validation-icon-size: var(--semantics-input-fields-sm-validation-icon-size);
	}


	/* # Block */

	.multi-line-text-field {
		box-sizing: border-box;
		display: block;
		position: relative;
		border: var(--semantics-input-fields-border);
		border-radius: var(--_corner-radius);
		background-color: var(--_background-color);
		overflow: hidden;
	}

	:host([valid]) .multi-line-text-field {
		border-color: var(--semantics-input-fields-is-valid-border-color);
	}

	:host([invalid]) .multi-line-text-field {
		border-color: var(--semantics-input-fields-is-invalid-border-color);
	}

	:host([readonly]) .multi-line-text-field {
		--_background-color: var(--semantics-input-fields-is-read-only-background-color);
		border-color: var(--semantics-input-fields-is-read-only-border-color);
	}

	:host([disabled]) .multi-line-text-field {
		opacity: var(--primitives-opacity-disabled);
	}

	.multi-line-text-field:has(textarea:-webkit-autofill),
	.multi-line-text-field:has(textarea:autofill) {
		--_background-color: var(--semantics-input-fields-is-autofill-background-color);
	}

	.multi-line-text-field:focus-within {
		outline: var(--semantics-focus-ring-outline);
		outline-offset: var(--semantics-focus-ring-outline-offset);
		box-shadow: var(--semantics-focus-ring-box-shadow);
	}


	/* # Elements */

	.multi-line-text-field__input {
		box-sizing: border-box;
		display: block;
		margin: 0;
		outline: none;
		border: none;
		background: transparent;
		width: 100%;
		min-height: calc(var(--_min-size) - var(--semantics-input-fields-border-thickness) * 2);
		padding-block: calc((var(--_min-size) - var(--semantics-input-fields-border-thickness) * 2 - 1lh) / 2);
		padding-inline: var(--_inline-padding);
		color: var(--semantics-content-color);
		font: var(--_text-font);
		appearance: none;
	}

	:host([resize="vertical"]) .multi-line-text-field__input {
		resize: vertical;
	}

	:host([resize="none"]) .multi-line-text-field__input {
		resize: none;
	}

	:host([resize="auto"]) .multi-line-text-field__input {
		resize: none;
		field-sizing: content;
	}

	:host([valid]) .multi-line-text-field__input,
	:host([invalid]) .multi-line-text-field__input {
		padding-inline-end: var(--_icon-area-size);
	}

	:host([disabled]) .multi-line-text-field__input {
		pointer-events: none;
	}

	.multi-line-text-field__input::placeholder {
		color: var(--semantics-input-fields-placeholder-color);
	}

	.multi-line-text-field__input:-webkit-autofill,
	.multi-line-text-field__input:autofill,
	.multi-line-text-field__input:-webkit-autofill:disabled,
	.multi-line-text-field__input:autofill:disabled {
		box-shadow: 0 0 0 999px var(--_background-color) inset;
		-webkit-text-fill-color: var(--semantics-input-fields-is-autofill-content-color);
	}

	.multi-line-text-field__validation-icon-area {
		display: flex;
		position: absolute;
		top: 0;
		right: 0;
		pointer-events: none;
		width: var(--_icon-area-size);
		height: var(--_icon-area-size);
		align-items: center;
		justify-content: center;
	}

	:host([valid]) .multi-line-text-field__validation-icon-area {
		color: var(--semantics-input-fields-is-valid-icon-color);
	}

	:host([invalid]) .multi-line-text-field__validation-icon-area {
		color: var(--semantics-input-fields-is-invalid-icon-color);
	}

	.multi-line-text-field__validation-icon {
		width: var(--_validation-icon-size);
		height: var(--_validation-icon-size);
	}
`;

  // node_modules/@nldd/design-system/dist/components/inputs/multi-line-text-field/multi-line-text-field.template.js
  init_lit();
  init_icon();
  function renderValidationIcon2(component) {
    if (component.invalid) {
      return b2`
			<div class="multi-line-text-field__validation-icon-area">
				<nldd-icon class="multi-line-text-field__validation-icon"
					name="invalid"
					aria-hidden="true"
				></nldd-icon>
			</div>
		`;
    }
    if (component.valid) {
      return b2`
			<div class="multi-line-text-field__validation-icon-area">
				<nldd-icon class="multi-line-text-field__validation-icon"
					name="valid"
					aria-hidden="true"
				></nldd-icon>
			</div>
		`;
    }
    return A;
  }
  function multiLineTextFieldTemplate(component) {
    return b2`
		<div class="multi-line-text-field">
			<textarea class="multi-line-text-field__input"
				id=${component.inputId || A}
				rows=${component.rows}
				.value=${component.value}
				placeholder=${component.placeholder || A}
				?disabled=${component.disabled}
				?readonly=${component.readonly}
				?required=${component.required}
				autocomplete=${component.autocomplete || A}
				spellcheck=${component.noSpellcheck ? "false" : "true"}
				aria-label=${component.accessibleLabel || A}
				aria-describedby=${component.errorMessageIds || A}
				aria-invalid=${component.invalid ? "true" : A}
				@input=${component._handleInput}
				@change=${component._handleChange}
			></textarea>
			${renderValidationIcon2(component)}
		</div>
	`;
  }

  // node_modules/@nldd/design-system/dist/components/inputs/multi-line-text-field/multi-line-text-field.js
  var import_meta8 = {};
  var __decorate26 = function(decorators, target, key, desc) {
    var c6 = arguments.length, r6 = c6 < 3 ? target : desc === null ? desc = Object.getOwnPropertyDescriptor(target, key) : desc, d3;
    if (typeof Reflect === "object" && typeof Reflect.decorate === "function") r6 = Reflect.decorate(decorators, target, key, desc);
    else for (var i8 = decorators.length - 1; i8 >= 0; i8--) if (d3 = decorators[i8]) r6 = (c6 < 3 ? d3(r6) : c6 > 3 ? d3(target, key, r6) : d3(target, key)) || r6;
    return c6 > 3 && r6 && Object.defineProperty(target, key, r6), r6;
  };
  var NLDDMultiLineTextField = class NLDDMultiLineTextField2 extends i4 {
    constructor() {
      super(...arguments);
      this._internals = this.attachInternals();
      this._initialValue = "";
      this.size = "md";
      this.value = "";
      this.inputId = "";
      this.placeholder = "";
      this.invalid = false;
      this.valid = false;
      this.disabled = false;
      this.name = "";
      this.readonly = false;
      this.required = false;
      this.autocomplete = "";
      this.rows = 3;
      this.resize = "vertical";
      this.accessibleLabel = "";
      this.errorMessageIds = "";
      this.noSpellcheck = false;
      this.width = "";
    }
    firstUpdated() {
      this._initialValue = this.value;
      if (import_meta8.env?.DEV && !this.accessibleLabel && !this.inputId) {
        console.warn("<nldd-multi-line-text-field>: No accessible-label or input-id provided. Use nldd-form-field for labeled usage, or set accessible-label for screen reader accessibility.");
      }
    }
    updated(changed) {
      if (changed.has("width")) {
        const w2 = this.width;
        if (w2 && w2 !== "full" && CSS.supports("width", w2)) {
          this.style.setProperty("--_width", w2);
        } else {
          this.style.removeProperty("--_width");
        }
      }
      if (changed.has("resize") && this.resize === "auto" && this._textarea) {
        this._textarea.style.width = "";
        this._textarea.style.height = "";
      }
      if (changed.has("value")) {
        this._internals.setFormValue(this.value);
      }
    }
    formResetCallback() {
      this.value = this._initialValue;
    }
    formDisabledCallback(disabled) {
      this.disabled = disabled;
    }
    formStateRestoreCallback(state) {
      if (typeof state === "string")
        this.value = state;
    }
    _handleInput(e9) {
      e9.stopPropagation();
      const textarea = e9.target;
      this.value = textarea.value;
      this.dispatchEvent(new CustomEvent("input", {
        detail: { value: this.value },
        bubbles: true,
        composed: true
      }));
    }
    _handleChange(e9) {
      e9.stopPropagation();
      const textarea = e9.target;
      this.value = textarea.value;
      this.dispatchEvent(new CustomEvent("change", {
        detail: { value: this.value },
        bubbles: true,
        composed: true
      }));
    }
    render() {
      return multiLineTextFieldTemplate(this);
    }
  };
  NLDDMultiLineTextField.formAssociated = true;
  NLDDMultiLineTextField.shadowRootOptions = {
    ...i4.shadowRootOptions,
    delegatesFocus: true
  };
  NLDDMultiLineTextField.styles = multiLineTextFieldStyles;
  __decorate26([
    n4({ type: String, reflect: true })
  ], NLDDMultiLineTextField.prototype, "size", void 0);
  __decorate26([
    n4({ type: String })
  ], NLDDMultiLineTextField.prototype, "value", void 0);
  __decorate26([
    n4({ type: String, attribute: "input-id" })
  ], NLDDMultiLineTextField.prototype, "inputId", void 0);
  __decorate26([
    n4({ type: String })
  ], NLDDMultiLineTextField.prototype, "placeholder", void 0);
  __decorate26([
    n4({ type: Boolean, reflect: true })
  ], NLDDMultiLineTextField.prototype, "invalid", void 0);
  __decorate26([
    n4({ type: Boolean, reflect: true })
  ], NLDDMultiLineTextField.prototype, "valid", void 0);
  __decorate26([
    n4({ type: Boolean, reflect: true })
  ], NLDDMultiLineTextField.prototype, "disabled", void 0);
  __decorate26([
    n4({ type: String, reflect: true })
  ], NLDDMultiLineTextField.prototype, "name", void 0);
  __decorate26([
    n4({ type: Boolean, reflect: true })
  ], NLDDMultiLineTextField.prototype, "readonly", void 0);
  __decorate26([
    n4({ type: Boolean, reflect: true })
  ], NLDDMultiLineTextField.prototype, "required", void 0);
  __decorate26([
    n4({ type: String })
  ], NLDDMultiLineTextField.prototype, "autocomplete", void 0);
  __decorate26([
    n4({ type: Number })
  ], NLDDMultiLineTextField.prototype, "rows", void 0);
  __decorate26([
    n4({ type: String, reflect: true })
  ], NLDDMultiLineTextField.prototype, "resize", void 0);
  __decorate26([
    n4({ type: String, attribute: "accessible-label" })
  ], NLDDMultiLineTextField.prototype, "accessibleLabel", void 0);
  __decorate26([
    n4({ type: String, attribute: "error-message-ids" })
  ], NLDDMultiLineTextField.prototype, "errorMessageIds", void 0);
  __decorate26([
    n4({ type: Boolean, reflect: true, attribute: "no-spellcheck" })
  ], NLDDMultiLineTextField.prototype, "noSpellcheck", void 0);
  __decorate26([
    n4({ type: String, reflect: true })
  ], NLDDMultiLineTextField.prototype, "width", void 0);
  __decorate26([
    e5(".multi-line-text-field__input")
  ], NLDDMultiLineTextField.prototype, "_textarea", void 0);
  NLDDMultiLineTextField = __decorate26([
    t3("nldd-multi-line-text-field")
  ], NLDDMultiLineTextField);

  // node_modules/@nldd/design-system/dist/components/inputs/code-editor/code-editor.js
  init_lit();
  init_decorators();

  // node_modules/@nldd/design-system/dist/components/inputs/code-editor/code-editor.styles.js
  init_lit();
  init_slotted_reset();
  var codeEditorStyles = i`


	/* # Host */

	:host {
		--_corner-radius: var(--primitives-corner-radius-lg);
		--_background-color: var(--semantics-surfaces-tinted-background-color);
		--_block-padding: var(--primitives-space-16);
		--_inline-padding: var(--primitives-space-16);
		--_content-color: var(--semantics-content-color);
		--_font: var(--primitives-font-monospace-sm-regular-snug);

		/* iOS Safari auto-zooms a focused field rendered under 16px (sm is ~14px).
		   Bump to the 16px md size on touch to prevent it; non-touch keeps the
		   compact size, and pinch-zoom stays available (no maximum-scale hack). */
		@media (pointer: coarse) {
			--_font: var(--primitives-font-monospace-md-regular-snug);
		}

		${inheritedTextReset}
		/* flex column + min-height:0 + flex:1 makes the host a good flex
		   citizen so a fixed-height parent grows the textarea; with no set
		   height it falls back to the rows attribute. */
		display: flex;
		min-height: 0;
		flex-direction: column;
		flex-grow: 1;
		flex-shrink: 1;
		flex-basis: auto;
		-webkit-tap-highlight-color: transparent;
	}

	:host([hidden]) {
		display: none;
	}


	/* # Block */

	.code-editor {
		box-sizing: border-box;
		display: flex;
		position: relative;
		border-radius: var(--_corner-radius);
		background-color: var(--_background-color);
		min-height: 0;
		overflow: hidden;
		flex-direction: column;
		flex-grow: 1;
		flex-shrink: 1;
		flex-basis: auto;
	}

	:host([disabled]) .code-editor {
		opacity: var(--primitives-opacity-disabled);
	}

	.code-editor:focus-within {
		outline: var(--semantics-focus-ring-outline);
		outline-offset: var(--semantics-focus-ring-outline-offset);
		box-shadow: var(--semantics-focus-ring-box-shadow);
	}


	/* # Elements */

	.code-editor__input {
		box-sizing: border-box;
		display: block;
		margin: 0;
		outline: none;
		border: none;
		background: transparent;
		width: 100%;
		min-height: 0;
		padding: var(--_block-padding) var(--_inline-padding);
		flex-grow: 1;
		flex-shrink: 1;
		flex-basis: auto;
		color: var(--_content-color);
		font: var(--_font);
		tab-size: 2;
		white-space: pre;
		overflow-wrap: normal;
		appearance: none;
	}

	:host([wrap]) .code-editor__input {
		white-space: pre-wrap;
		overflow-wrap: break-word;
	}

	:host([resize="vertical"]) .code-editor__input {
		resize: vertical;
	}

	:host([resize="none"]) .code-editor__input {
		resize: none;
	}

	:host([resize="auto"]) .code-editor__input {
		resize: none;
		field-sizing: content;
	}

	:host([disabled]) .code-editor__input {
		pointer-events: none;
	}

	.code-editor__input::placeholder {
		color: var(--semantics-input-fields-placeholder-color);
	}
`;

  // node_modules/@nldd/design-system/dist/components/inputs/code-editor/code-editor.template.js
  init_lit();
  function codeEditorTemplate(component) {
    return b2`
		<div class="code-editor">
			<textarea class="code-editor__input"
				id=${component.inputId || A}
				rows=${component.rows}
				.value=${component.value}
				placeholder=${component.placeholder || A}
				?disabled=${component.disabled}
				?readonly=${component.readonly}
				?required=${component.required}
				spellcheck="false"
				autocomplete="off"
				autocorrect="off"
				autocapitalize="off"
				aria-label=${component.accessibleLabel || A}
				@input=${component._handleInput}
				@change=${component._handleChange}
			></textarea>
		</div>
	`;
  }

  // node_modules/@nldd/design-system/dist/components/inputs/code-editor/code-editor.js
  var __decorate27 = function(decorators, target, key, desc) {
    var c6 = arguments.length, r6 = c6 < 3 ? target : desc === null ? desc = Object.getOwnPropertyDescriptor(target, key) : desc, d3;
    if (typeof Reflect === "object" && typeof Reflect.decorate === "function") r6 = Reflect.decorate(decorators, target, key, desc);
    else for (var i8 = decorators.length - 1; i8 >= 0; i8--) if (d3 = decorators[i8]) r6 = (c6 < 3 ? d3(r6) : c6 > 3 ? d3(target, key, r6) : d3(target, key)) || r6;
    return c6 > 3 && r6 && Object.defineProperty(target, key, r6), r6;
  };
  var NLDDCodeEditor = class NLDDCodeEditor2 extends i4 {
    constructor() {
      super(...arguments);
      this._internals = this.attachInternals();
      this._initialValue = "";
      this.value = "";
      this.inputId = "";
      this.placeholder = "";
      this.disabled = false;
      this.name = "";
      this.readonly = false;
      this.required = false;
      this.wrap = false;
      this.rows = 6;
      this.resize = "vertical";
      this.accessibleLabel = "";
      this._accessibleLabelWarned = false;
    }
    firstUpdated() {
      this._initialValue = this.value;
    }
    updated(changed) {
      if (changed.has("value")) {
        this._internals.setFormValue(this.value);
      }
      this._checkAccessibleLabel();
    }
    /* Ran from updated(), not firstUpdated(): a parent nldd-form-field sets
     * input-id slightly after our first render, so checking earlier triggers
     * a spurious warning for the standard form-field usage. We re-check on
     * every update and warn once if the missing label is still missing. */
    _checkAccessibleLabel() {
      if (this._accessibleLabelWarned)
        return;
      if (this.accessibleLabel || this.inputId)
        return;
      this._accessibleLabelWarned = true;
      console.warn("<nldd-code-editor>: No accessible-label or input-id provided. Use nldd-form-field for labeled usage, or set accessible-label for screen reader accessibility (WCAG SC 4.1.2).");
    }
    formResetCallback() {
      this.value = this._initialValue;
    }
    formDisabledCallback(disabled) {
      this.disabled = disabled;
    }
    formStateRestoreCallback(state) {
      if (typeof state === "string")
        this.value = state;
    }
    _handleInput(e9) {
      e9.stopPropagation();
      const textarea = e9.target;
      this.value = textarea.value;
      this.dispatchEvent(new CustomEvent("input", {
        detail: { value: this.value },
        bubbles: true,
        composed: true
      }));
    }
    _handleChange(e9) {
      e9.stopPropagation();
      const textarea = e9.target;
      this.value = textarea.value;
      this.dispatchEvent(new CustomEvent("change", {
        detail: { value: this.value },
        bubbles: true,
        composed: true
      }));
    }
    render() {
      return codeEditorTemplate(this);
    }
  };
  NLDDCodeEditor.formAssociated = true;
  NLDDCodeEditor.shadowRootOptions = {
    ...i4.shadowRootOptions,
    delegatesFocus: true
  };
  NLDDCodeEditor.styles = codeEditorStyles;
  __decorate27([
    n4({ type: String })
  ], NLDDCodeEditor.prototype, "value", void 0);
  __decorate27([
    n4({ type: String, attribute: "input-id" })
  ], NLDDCodeEditor.prototype, "inputId", void 0);
  __decorate27([
    n4({ type: String })
  ], NLDDCodeEditor.prototype, "placeholder", void 0);
  __decorate27([
    n4({ type: Boolean, reflect: true })
  ], NLDDCodeEditor.prototype, "disabled", void 0);
  __decorate27([
    n4({ type: String, reflect: true })
  ], NLDDCodeEditor.prototype, "name", void 0);
  __decorate27([
    n4({ type: Boolean, reflect: true })
  ], NLDDCodeEditor.prototype, "readonly", void 0);
  __decorate27([
    n4({ type: Boolean, reflect: true })
  ], NLDDCodeEditor.prototype, "required", void 0);
  __decorate27([
    n4({ type: Boolean, reflect: true })
  ], NLDDCodeEditor.prototype, "wrap", void 0);
  __decorate27([
    n4({ type: Number })
  ], NLDDCodeEditor.prototype, "rows", void 0);
  __decorate27([
    n4({ type: String, reflect: true })
  ], NLDDCodeEditor.prototype, "resize", void 0);
  __decorate27([
    n4({ type: String, attribute: "accessible-label" })
  ], NLDDCodeEditor.prototype, "accessibleLabel", void 0);
  __decorate27([
    e5(".code-editor__input")
  ], NLDDCodeEditor.prototype, "_textarea", void 0);
  NLDDCodeEditor = __decorate27([
    t3("nldd-code-editor")
  ], NLDDCodeEditor);

  // node_modules/@nldd/design-system/dist/components/inputs/password-field/password-field.js
  init_lit();
  init_decorators();

  // node_modules/@nldd/design-system/dist/components/inputs/password-field/password-field.styles.js
  init_lit();
  init_slotted_reset();
  var passwordFieldStyles = i`


	/* # Host */

	:host {
		--_width: 100%;
		--_background-color: var(--semantics-input-fields-background-color);
		--_corner-radius: var(--semantics-controls-md-corner-radius);
		--_min-size: var(--semantics-controls-md-min-size);
		--_inline-padding: var(--semantics-controls-md-inline-padding);
		--_text-font: var(--semantics-input-fields-md-text-font);
		--_mask-font: var(--semantics-input-fields-md-mask-font);
		--_validation-icon-size: var(--semantics-input-fields-md-validation-icon-size);
		--_visibility-toggle-padding: calc((var(--_min-size) - var(--semantics-input-fields-border-thickness) * 2 - var(--semantics-controls-sm-min-size)) / 2);
		--_z-index-button-focus: 1;

		${inheritedTextReset}
		display: block;
		width: var(--_width);
		max-width: 100%;
		-webkit-tap-highlight-color: transparent;
	}

	:host([hidden]) {
		display: none;
	}

	:host([size="sm"]) {
		--_corner-radius: var(--semantics-controls-sm-corner-radius);
		--_min-size: var(--semantics-controls-sm-min-size);
		--_inline-padding: var(--semantics-controls-sm-inline-padding);
		--_text-font: var(--semantics-input-fields-sm-text-font);
		--_mask-font: var(--semantics-input-fields-sm-mask-font);
		--_validation-icon-size: var(--semantics-input-fields-sm-validation-icon-size);
		--_visibility-toggle-padding: calc((var(--_min-size) - var(--semantics-input-fields-border-thickness) * 2 - var(--semantics-controls-xs-min-size)) / 2);
	}


	/* # Block */

	.password-field {
		box-sizing: border-box;
		display: flex;
		border: var(--semantics-input-fields-border);
		border-radius: var(--_corner-radius);
		background-color: var(--_background-color);
		min-height: var(--_min-size);
		overflow: hidden;
		padding-left: calc(var(--_inline-padding) - var(--semantics-input-fields-border-thickness));
		flex-direction: row;
		align-items: center;
	}

	:host([valid]) .password-field {
		border-color: var(--semantics-input-fields-is-valid-border-color);
	}

	:host([invalid]) .password-field {
		border-color: var(--semantics-input-fields-is-invalid-border-color);
	}

	:host([readonly]) .password-field {
		--_background-color: var(--semantics-input-fields-is-read-only-background-color);
		border-color: var(--semantics-input-fields-is-read-only-border-color);
	}

	:host([disabled]) .password-field {
		opacity: var(--primitives-opacity-disabled);
	}

	.password-field:has(input:-webkit-autofill),
	.password-field:has(input:autofill) {
		--_background-color: var(--semantics-input-fields-is-autofill-background-color);
	}

	.password-field:focus-within:not(:has(.password-field__visibility-toggle-button:focus-within)) {
		outline: var(--semantics-focus-ring-outline);
		outline-offset: var(--semantics-focus-ring-outline-offset);
		box-shadow: var(--semantics-focus-ring-box-shadow);
	}

	.password-field:has(.password-field__visibility-toggle-button:focus-within) {
		overflow: visible;
	}


	/* # Elements */

	.password-field__input {
		box-sizing: border-box;
		margin: 0;
		outline: none;
		border: none;
		background: transparent;
		min-width: 0;
		min-height: calc(var(--_min-size) - var(--semantics-input-fields-border-thickness) * 2);
		overflow: hidden;
		padding: 0;
		flex-grow: 1;
		color: var(--semantics-content-color);
		font: var(--_text-font);
		appearance: none;
	}

	.password-field__input::placeholder {
		color: var(--semantics-input-fields-placeholder-color);
		font: var(--_text-font);
	}

	.password-field__input.is-masked {
		font: var(--_mask-font);
	}

	:host([disabled]) .password-field__input {
		pointer-events: none;
	}

	.password-field__input:-webkit-autofill,
	.password-field__input:autofill,
	.password-field__input:-webkit-autofill:disabled,
	.password-field__input:autofill:disabled {
		box-shadow: 0 0 0 999px var(--_background-color) inset;
		-webkit-text-fill-color: var(--semantics-input-fields-is-autofill-content-color);
	}

	.password-field__input-fade {
		position: relative;
		width: 0;
		flex-shrink: 0;
		align-self: stretch;
	}

	.password-field__input-fade::after {
		content: '';
		position: absolute;
		top: 0;
		right: 0;
		bottom: 0;
		border-radius: var(--_corner-radius);
		background: linear-gradient(90deg, color-mix(in oklch, var(--_background-color) 0%, transparent) 0%, var(--_background-color) 100%);
		pointer-events: none;
		width: var(--primitives-space-8);
	}

	.password-field__validation-icon-area {
		display: flex;
		width: calc(var(--_min-size) - var(--semantics-input-fields-border-thickness) * 2);
		height: 100%;
		flex-shrink: 0;
		align-items: center;
		justify-content: center;
	}

	:host([valid]) .password-field__validation-icon-area {
		color: var(--semantics-input-fields-is-valid-icon-color);
	}

	:host([invalid]) .password-field__validation-icon-area {
		color: var(--semantics-input-fields-is-invalid-icon-color);
	}

	.password-field__validation-icon {
		width: var(--_validation-icon-size);
		height: var(--_validation-icon-size);
	}

	.password-field__visibility-toggle-button {
		display: flex;
		height: 100%;
		padding-block: var(--_visibility-toggle-padding);
		padding-inline-end: var(--_visibility-toggle-padding);
		flex-shrink: 0;
		align-items: center;
		justify-content: center;
	}

	.password-field__visibility-toggle-button:focus-within {
		position: relative;
		z-index: var(--_z-index-button-focus);
	}
`;

  // node_modules/@nldd/design-system/dist/components/inputs/password-field/password-field.template.js
  init_lit();
  init_icon();
  init_button();
  function renderValidationIcon3(component) {
    if (component.invalid) {
      return b2`
			<div class="password-field__validation-icon-area">
				<nldd-icon class="password-field__validation-icon"
					name="invalid"
					aria-hidden="true"
				></nldd-icon>
			</div>
		`;
    }
    if (component.valid) {
      return b2`
			<div class="password-field__validation-icon-area">
				<nldd-icon class="password-field__validation-icon"
					name="valid"
					aria-hidden="true"
				></nldd-icon>
			</div>
		`;
    }
    return A;
  }
  function renderVisibilityToggle(component) {
    const buttonSize = component.size === "sm" ? "xs" : "sm";
    const label = component.masked ? component.showButtonText : component.hideButtonText;
    const accessibleLabel = component.masked ? component.showButtonAccessibleLabel : component.hideButtonAccessibleLabel;
    return b2`
		<div class="password-field__visibility-toggle-button">
			<nldd-button
				size=${buttonSize}
				type="button"
				text=${label}
				accessible-label=${accessibleLabel}
				?disabled=${component.disabled}
				@click=${component._handleToggle}
				@pointerdown=${(e9) => e9.preventDefault()}
			></nldd-button>
		</div>
	`;
  }
  function passwordFieldTemplate(component) {
    return b2`
		<div class="password-field">
			<input class="password-field__input ${component.masked ? "is-masked" : ""}"
				id=${component.inputId || A}
				type=${component.masked ? "password" : "text"}
				.value=${component.value}
				placeholder=${component.placeholder || A}
				?disabled=${component.disabled}
				?readonly=${component.readonly}
				?required=${component.required}
				name=${component.name || A}
				autocomplete=${component.autocomplete || A}
				spellcheck="false"
				aria-label=${component.accessibleLabel || A}
				aria-describedby=${component.errorMessageIds || A}
				aria-invalid=${component.invalid ? "true" : A}
				@input=${component._handleInput}
				@change=${component._handleChange}
			/>
			<div class="password-field__input-fade"></div>
			${renderValidationIcon3(component)}
			${renderVisibilityToggle(component)}
		</div>
	`;
  }

  // node_modules/@nldd/design-system/dist/components/inputs/password-field/password-field.js
  var __decorate28 = function(decorators, target, key, desc) {
    var c6 = arguments.length, r6 = c6 < 3 ? target : desc === null ? desc = Object.getOwnPropertyDescriptor(target, key) : desc, d3;
    if (typeof Reflect === "object" && typeof Reflect.decorate === "function") r6 = Reflect.decorate(decorators, target, key, desc);
    else for (var i8 = decorators.length - 1; i8 >= 0; i8--) if (d3 = decorators[i8]) r6 = (c6 < 3 ? d3(r6) : c6 > 3 ? d3(target, key, r6) : d3(target, key)) || r6;
    return c6 > 3 && r6 && Object.defineProperty(target, key, r6), r6;
  };
  var NLDDPasswordField = class NLDDPasswordField2 extends i4 {
    constructor() {
      super(...arguments);
      this._internals = this.attachInternals();
      this._initialValue = "";
      this.size = "md";
      this.value = "";
      this.inputId = "";
      this.placeholder = "";
      this.valid = false;
      this.invalid = false;
      this.disabled = false;
      this.masked = true;
      this.showButtonText = "Toon";
      this.hideButtonText = "Verberg";
      this.showButtonAccessibleLabel = "Toon wachtwoord";
      this.hideButtonAccessibleLabel = "Verberg wachtwoord";
      this.readonly = false;
      this.required = false;
      this.name = "";
      this.autocomplete = "";
      this.accessibleLabel = "";
      this.errorMessageIds = "";
      this.width = "";
    }
    firstUpdated() {
      this._initialValue = this.value;
    }
    updated(changed) {
      if (changed.has("width")) {
        const w2 = this.width;
        if (w2 && w2 !== "full" && CSS.supports("width", w2)) {
          this.style.setProperty("--_width", w2);
        } else {
          this.style.removeProperty("--_width");
        }
      }
      if (changed.has("value")) {
        this._internals.setFormValue(this.value);
      }
    }
    formResetCallback() {
      this.value = this._initialValue;
    }
    formDisabledCallback(disabled) {
      this.disabled = disabled;
    }
    formStateRestoreCallback(state) {
      if (typeof state === "string")
        this.value = state;
    }
    _handleInput(e9) {
      e9.stopPropagation();
      const input = e9.target;
      this.value = input.value;
      this.dispatchEvent(new CustomEvent("input", {
        detail: { value: this.value },
        bubbles: true,
        composed: true
      }));
    }
    _handleChange(e9) {
      e9.stopPropagation();
      const input = e9.target;
      this.value = input.value;
      this.dispatchEvent(new CustomEvent("change", {
        detail: { value: this.value },
        bubbles: true,
        composed: true
      }));
    }
    _handleToggle() {
      this.masked = !this.masked;
    }
    focus() {
      this._input?.focus();
    }
    blur() {
      this._input?.blur();
    }
    render() {
      return passwordFieldTemplate(this);
    }
  };
  NLDDPasswordField.formAssociated = true;
  NLDDPasswordField.shadowRootOptions = {
    ...i4.shadowRootOptions,
    delegatesFocus: true
  };
  NLDDPasswordField.styles = passwordFieldStyles;
  __decorate28([
    n4({ type: String, reflect: true })
  ], NLDDPasswordField.prototype, "size", void 0);
  __decorate28([
    n4({ type: String })
  ], NLDDPasswordField.prototype, "value", void 0);
  __decorate28([
    n4({ type: String, attribute: "input-id" })
  ], NLDDPasswordField.prototype, "inputId", void 0);
  __decorate28([
    n4({ type: String })
  ], NLDDPasswordField.prototype, "placeholder", void 0);
  __decorate28([
    n4({ type: Boolean, reflect: true })
  ], NLDDPasswordField.prototype, "valid", void 0);
  __decorate28([
    n4({ type: Boolean, reflect: true })
  ], NLDDPasswordField.prototype, "invalid", void 0);
  __decorate28([
    n4({ type: Boolean, reflect: true })
  ], NLDDPasswordField.prototype, "disabled", void 0);
  __decorate28([
    n4({ type: Boolean, reflect: true })
  ], NLDDPasswordField.prototype, "masked", void 0);
  __decorate28([
    n4({ type: String, attribute: "show-button-text" })
  ], NLDDPasswordField.prototype, "showButtonText", void 0);
  __decorate28([
    n4({ type: String, attribute: "hide-button-text" })
  ], NLDDPasswordField.prototype, "hideButtonText", void 0);
  __decorate28([
    n4({ type: String, attribute: "show-button-accessible-label" })
  ], NLDDPasswordField.prototype, "showButtonAccessibleLabel", void 0);
  __decorate28([
    n4({ type: String, attribute: "hide-button-accessible-label" })
  ], NLDDPasswordField.prototype, "hideButtonAccessibleLabel", void 0);
  __decorate28([
    n4({ type: Boolean, reflect: true })
  ], NLDDPasswordField.prototype, "readonly", void 0);
  __decorate28([
    n4({ type: Boolean, reflect: true })
  ], NLDDPasswordField.prototype, "required", void 0);
  __decorate28([
    n4({ type: String, reflect: true })
  ], NLDDPasswordField.prototype, "name", void 0);
  __decorate28([
    n4({ type: String })
  ], NLDDPasswordField.prototype, "autocomplete", void 0);
  __decorate28([
    n4({ type: String, attribute: "accessible-label" })
  ], NLDDPasswordField.prototype, "accessibleLabel", void 0);
  __decorate28([
    n4({ type: String, attribute: "error-message-ids" })
  ], NLDDPasswordField.prototype, "errorMessageIds", void 0);
  __decorate28([
    n4({ type: String, reflect: true })
  ], NLDDPasswordField.prototype, "width", void 0);
  __decorate28([
    e5(".password-field__input")
  ], NLDDPasswordField.prototype, "_input", void 0);
  NLDDPasswordField = __decorate28([
    t3("nldd-password-field")
  ], NLDDPasswordField);

  // node_modules/@nldd/design-system/dist/components/inputs/search-field/search-field.js
  init_lit();
  init_decorators();

  // node_modules/@nldd/design-system/dist/components/inputs/search-field/search-field.styles.js
  init_lit();
  init_slotted_reset();
  var searchFieldStyles = i`


	/* # Host */

	:host {
		--_width: 100%;
		--_background-color: var(--semantics-input-fields-background-color);
		--_corner-radius: var(--semantics-controls-md-corner-radius);
		--_min-size: var(--semantics-controls-md-min-size);
		--_search-icon-size: var(--primitives-space-24);
		--_text-font: var(--semantics-input-fields-md-text-font);
		--_end-padding-right: calc((var(--_min-size) - var(--semantics-controls-sm-min-size)) / 2 - var(--semantics-input-fields-border-thickness));
		--_end-gap: var(--primitives-space-6);
		--_z-index-button-focus: 1;

		${inheritedTextReset}
		display: block;
		width: var(--_width);
		max-width: 100%;
		min-width: 0;
		-webkit-tap-highlight-color: transparent;
	}

	:host([hidden]) {
		display: none;
	}

	:host([disabled]) {
		opacity: var(--primitives-opacity-disabled);
		pointer-events: none;
	}

	:host([size="sm"]) {
		--_corner-radius: var(--semantics-controls-sm-corner-radius);
		--_min-size: var(--semantics-controls-sm-min-size);
		--_search-icon-size: var(--primitives-space-20);
		--_text-font: var(--semantics-input-fields-sm-text-font);
		--_end-padding-right: calc((var(--_min-size) - var(--semantics-controls-xs-min-size)) / 2 - var(--semantics-input-fields-border-thickness));
		--_end-gap: var(--primitives-space-4);
	}


	/* # Block */

	.search-field {
		box-sizing: border-box;
		display: flex;
		position: relative;
		border: var(--semantics-input-fields-border);
		border-radius: var(--_corner-radius);
		background-color: var(--_background-color);
		width: 100%;
		min-height: var(--_min-size);
		flex-direction: row;
		align-items: center;
	}

	.search-field:has(input:-webkit-autofill),
	.search-field:has(input:autofill) {
		--_background-color: var(--semantics-input-fields-is-autofill-background-color);
	}

	.search-field:has(.search-field__input:focus-visible) {
		outline: var(--semantics-focus-ring-outline);
		outline-offset: var(--semantics-focus-ring-outline-offset);
		box-shadow: var(--semantics-focus-ring-box-shadow);
	}


	/* # Elements */

	.search-field__search-icon {
		display: flex;
		margin-inline: calc((var(--_min-size) - var(--_search-icon-size)) / 2 - var(--semantics-input-fields-border-thickness));
		width: var(--_search-icon-size);
		height: var(--_search-icon-size);
		flex-shrink: 0;
		align-items: center;
		justify-content: center;
		color: var(--semantics-content-secondary-color);
	}

	.search-field__input {
		box-sizing: border-box;
		margin: 0;
		outline: none;
		border: none;
		background: transparent;
		min-width: 0;
		padding: 0;
		flex-grow: 1;
		flex-shrink: 1;
		flex-basis: 0;
		align-self: stretch;
		color: var(--semantics-content-color);
		font: var(--_text-font);
		appearance: none;
	}

	.search-field__input::placeholder {
		color: var(--semantics-input-fields-placeholder-color);
	}

	.search-field__input:-webkit-autofill,
	.search-field__input:autofill,
	.search-field__input:-webkit-autofill:disabled,
	.search-field__input:autofill:disabled {
		box-shadow: 0 0 0 999px var(--_background-color) inset;
		-webkit-text-fill-color: var(--semantics-input-fields-is-autofill-content-color);
	}

	.search-field__input::-webkit-search-cancel-button {
		-webkit-appearance: none;
	}

	.search-field__input-fade {
		position: relative;
		width: 0;
		flex-shrink: 0;
		align-self: stretch;
	}

	.search-field__input-fade::after {
		content: '';
		position: absolute;
		top: 0;
		right: 0;
		bottom: 0;
		border-radius: var(--_corner-radius);
		background: linear-gradient(90deg, color-mix(in oklch, var(--_background-color) 0%, transparent) 0%, var(--_background-color) 100%);
		pointer-events: none;
		width: var(--primitives-space-8);
	}

	.search-field__end {
		display: flex;
		position: relative;
		padding-right: var(--_end-padding-right);
		flex-shrink: 0;
		gap: var(--_end-gap);
		align-items: center;
	}

	.search-field__clear-button:focus-within,
	.search-field__search-button:focus-within {
		position: relative;
		z-index: var(--_z-index-button-focus);
	}
`;

  // node_modules/@nldd/design-system/dist/components/inputs/search-field/search-field.template.js
  init_lit();
  init_icon_button();
  init_button();
  init_icon();
  function searchFieldTemplate(component) {
    const buttonSize = component.size === "sm" ? "xs" : "sm";
    return b2`
		<div class="search-field">
			<div class="search-field__search-icon">
				<nldd-icon name="search"></nldd-icon>
			</div>
			<input class="search-field__input"
				type="search"
				.value=${component.value}
				placeholder=${component.placeholder}
				aria-label=${component.accessibleLabel || component.placeholder || A}
				?disabled=${component.disabled}
				name=${component.name || A}
				spellcheck=${component.noSpellcheck ? "false" : "true"}
				@input=${component._handleInput}
				@change=${component._handleChange}
				@keydown=${component._handleKeydown}
			>
			<div class="search-field__input-fade"></div>
			<div class="search-field__end">
				${component.value ? b2`
					<div class="search-field__clear-button">
						<nldd-icon-button
							variant="neutral-transparent"
							size=${buttonSize}
							icon="dismiss"
							text=${component._t("components.search-field.clear-action")}
							@click=${component._handleClear}
						></nldd-icon-button>
					</div>
				` : A}
				${component.showSearchButton ? b2`
					<div class="search-field__search-button">
						<nldd-button
							variant="neutral-tinted"
							size=${buttonSize}
							text=${component._t("components.search-field.search-action")}
							@click=${component._handleSearch}
						></nldd-button>
					</div>
				` : A}
			</div>
		</div>
	`;
  }

  // node_modules/@nldd/design-system/dist/components/inputs/search-field/search-field.i18n.js
  var nlddSearchFieldTranslations = {
    "components.search-field.clear-action": "Wis zoekopdracht",
    "components.search-field.search-action": "Zoek"
  };

  // node_modules/@nldd/design-system/dist/components/inputs/search-field/search-field.js
  init_icon_button();
  init_button();
  init_icon();
  var __decorate29 = function(decorators, target, key, desc) {
    var c6 = arguments.length, r6 = c6 < 3 ? target : desc === null ? desc = Object.getOwnPropertyDescriptor(target, key) : desc, d3;
    if (typeof Reflect === "object" && typeof Reflect.decorate === "function") r6 = Reflect.decorate(decorators, target, key, desc);
    else for (var i8 = decorators.length - 1; i8 >= 0; i8--) if (d3 = decorators[i8]) r6 = (c6 < 3 ? d3(r6) : c6 > 3 ? d3(target, key, r6) : d3(target, key)) || r6;
    return c6 > 3 && r6 && Object.defineProperty(target, key, r6), r6;
  };
  var NLDDSearchField = class NLDDSearchField2 extends i4 {
    constructor() {
      super(...arguments);
      this._internals = this.attachInternals();
      this._initialValue = "";
      this.value = "";
      this.placeholder = "Zoeken";
      this.accessibleLabel = "";
      this.size = "md";
      this.disabled = false;
      this.name = "";
      this.showSearchButton = false;
      this.translations = {};
      this.noSpellcheck = false;
      this.width = "";
    }
    firstUpdated() {
      this._initialValue = this.value;
    }
    updated(changed) {
      if (changed.has("width")) {
        const w2 = this.width;
        if (w2 && w2 !== "full" && CSS.supports("width", w2)) {
          this.style.setProperty("--_width", w2);
        } else {
          this.style.removeProperty("--_width");
        }
      }
      if (changed.has("value")) {
        this._internals.setFormValue(this.value);
      }
    }
    formResetCallback() {
      this.value = this._initialValue;
    }
    formDisabledCallback(disabled) {
      this.disabled = disabled;
    }
    formStateRestoreCallback(state) {
      if (typeof state === "string")
        this.value = state;
    }
    // — i18n ——————————————————————————————————————————————————————————————————
    _t(key) {
      return this.translations[key] ?? nlddSearchFieldTranslations[key];
    }
    // — Handlers ————————————————————————————————————————————————————————————
    _handleInput(e9) {
      const input = e9.target;
      this.value = input.value;
      this.dispatchEvent(new CustomEvent("input", {
        detail: { value: this.value },
        bubbles: true,
        composed: true
      }));
    }
    _handleChange(e9) {
      const input = e9.target;
      this.value = input.value;
      this.dispatchEvent(new CustomEvent("change", {
        detail: { value: this.value },
        bubbles: true,
        composed: true
      }));
    }
    _handleKeydown(e9) {
      if (e9.key === "Enter") {
        this._dispatchSearch();
      }
    }
    _handleClear() {
      this.value = "";
      this.dispatchEvent(new CustomEvent("input", {
        detail: { value: "" },
        bubbles: true,
        composed: true
      }));
      this.dispatchEvent(new CustomEvent("change", {
        detail: { value: "" },
        bubbles: true,
        composed: true
      }));
      this._input?.focus();
    }
    _handleSearch() {
      this._dispatchSearch();
    }
    _dispatchSearch() {
      this.dispatchEvent(new CustomEvent("search", {
        detail: { value: this.value },
        bubbles: true,
        composed: true
      }));
    }
    /**
     * Delegates focus to the inner native `<input>`, so consumers can call
     * `searchFieldEl.focus()` without reaching into shadow DOM.
     */
    focus(options) {
      this._input?.focus(options);
    }
    render() {
      return searchFieldTemplate(this);
    }
  };
  NLDDSearchField.formAssociated = true;
  NLDDSearchField.styles = searchFieldStyles;
  __decorate29([
    n4({ type: String })
  ], NLDDSearchField.prototype, "value", void 0);
  __decorate29([
    n4({ type: String })
  ], NLDDSearchField.prototype, "placeholder", void 0);
  __decorate29([
    n4({ type: String, attribute: "accessible-label" })
  ], NLDDSearchField.prototype, "accessibleLabel", void 0);
  __decorate29([
    n4({ type: String, reflect: true })
  ], NLDDSearchField.prototype, "size", void 0);
  __decorate29([
    n4({ type: Boolean, reflect: true })
  ], NLDDSearchField.prototype, "disabled", void 0);
  __decorate29([
    n4({ type: String, reflect: true })
  ], NLDDSearchField.prototype, "name", void 0);
  __decorate29([
    n4({ type: Boolean, reflect: true, attribute: "show-search-button" })
  ], NLDDSearchField.prototype, "showSearchButton", void 0);
  __decorate29([
    n4({ type: Object })
  ], NLDDSearchField.prototype, "translations", void 0);
  __decorate29([
    n4({ type: Boolean, reflect: true, attribute: "no-spellcheck" })
  ], NLDDSearchField.prototype, "noSpellcheck", void 0);
  __decorate29([
    n4({ type: String, reflect: true })
  ], NLDDSearchField.prototype, "width", void 0);
  __decorate29([
    e5(".search-field__input")
  ], NLDDSearchField.prototype, "_input", void 0);
  NLDDSearchField = __decorate29([
    t3("nldd-search-field")
  ], NLDDSearchField);

  // node_modules/@nldd/design-system/dist/components/inputs/number-field/number-field.js
  init_lit();
  init_decorators();

  // node_modules/@nldd/design-system/dist/components/inputs/number-field/number-field.styles.js
  init_lit();
  init_slotted_reset();
  var numberFieldStyles = i`


	/* # Host */

	:host {
		--_width: auto;
		--_corner-radius: var(--semantics-controls-md-corner-radius);
		--_min-size: var(--semantics-controls-md-min-size);
		--_spin-button-padding: calc((var(--_min-size) - var(--semantics-controls-sm-min-size)) / 2 - var(--semantics-input-fields-border-thickness));
		--_text-font: var(--semantics-input-fields-md-text-font);
		--_inline-padding: calc(var(--semantics-controls-md-inline-padding) - var(--semantics-input-fields-border-thickness));

		${inheritedTextReset}
		display: inline-block;
		-webkit-tap-highlight-color: transparent;
	}

	:host([hidden]) {
		display: none;
	}

	:host([size="sm"]) {
		--_corner-radius: var(--semantics-controls-sm-corner-radius);
		--_min-size: var(--semantics-controls-sm-min-size);
		--_spin-button-padding: calc((var(--_min-size) - var(--semantics-controls-xs-min-size)) / 2 - var(--semantics-input-fields-border-thickness));
		--_text-font: var(--semantics-input-fields-sm-text-font);
		--_inline-padding: calc(var(--semantics-controls-sm-inline-padding) - var(--semantics-input-fields-border-thickness));
	}

	:host([disabled]) {
		opacity: var(--primitives-opacity-disabled);
		pointer-events: none;
	}

	:host([disabled]) nldd-icon-button {
		opacity: 1;
	}

	:host([width="full"]) {
		display: block;
		width: 100%;
	}

	:host([width]) {
		width: var(--_width);
		max-width: 100%;
	}


	/* # Block */

	.number-field {
		box-sizing: border-box;
		display: inline-flex;
		border: var(--semantics-input-fields-border);
		border-radius: var(--_corner-radius);
		background-color: var(--semantics-input-fields-background-color);
		height: var(--_min-size);
		flex-direction: row;
		align-items: center;
	}

	:host([width="full"]) .number-field,
	:host([width]) .number-field {
		width: 100%;
	}

	.number-field:has(.number-field__input:focus-visible) {
		outline: var(--semantics-focus-ring-outline);
		outline-offset: var(--semantics-focus-ring-outline-offset);
		box-shadow: var(--semantics-focus-ring-box-shadow);
	}


	/* # Elements */

	.number-field__decrement-button,
	.number-field__increment-button {
		display: flex;
		height: 100%;
		align-items: center;
	}

	.number-field__decrement-button {
		padding-left: var(--_spin-button-padding);
	}

	.number-field__increment-button {
		padding-right: var(--_spin-button-padding);
	}

	.number-field__input {
		box-sizing: border-box;
		margin: 0;
		outline: none;
		border: none;
		background: transparent;
		min-width: var(--_min-size);
		padding: 0 var(--primitives-space-6);
		align-self: stretch;
		text-align: center;
		color: var(--semantics-content-color);
		font: var(--_text-font);
		appearance: none;
	}

	.number-field__input[type="number"] {
		-moz-appearance: textfield;
	}

	.number-field__input::-webkit-outer-spin-button,
	.number-field__input::-webkit-inner-spin-button {
		margin: 0;
		-webkit-appearance: none;
	}

	:host([hide-spin-buttons]) .number-field__input {
		min-width: var(--primitives-space-80);
		padding-inline: var(--_inline-padding);
		text-align: left;
	}

	:host([width="full"]) .number-field__input,
	:host([width]) .number-field__input {
		min-width: 0;
		flex-grow: 1;
	}
`;

  // node_modules/@nldd/design-system/dist/components/inputs/number-field/number-field.template.js
  init_lit();
  init_icon_button();
  function numberFieldTemplate(component) {
    const canDecrease = component.value > component.min;
    const canIncrease = component.value < component.max;
    const iconButtonSize = component.size === "sm" ? "xs" : "sm";
    return b2`
		<div class="number-field"
			role="group"
			aria-label=${component._t("components.number-field.to-adjust-value-action")}
		>
			${!component.hideSpinButtons ? b2`
				<div class="number-field__decrement-button">
					<nldd-icon-button
						variant="neutral-tinted"
						size=${iconButtonSize}
						icon="minus"
						text=${component._t("components.number-field.decrement-action")}
						tooltip-timing="never"
						?disabled=${component.disabled || !canDecrease}
						@click=${component._handleDecrease}
					></nldd-icon-button>
				</div>
			` : A}
			<input class="number-field__input"
				type="number"
				inputmode=${Number.isInteger(component.step) ? "numeric" : "decimal"}
				aria-label=${component.accessibleLabel || A}
				.value=${String(component.value)}
				min=${component.min}
				max=${component.max}
				step=${component.step}
				?disabled=${component.disabled}
				name=${component.name || A}
				@input=${component._handleInput}
				@change=${component._handleChange}
			>
			${!component.hideSpinButtons ? b2`
				<div class="number-field__increment-button">
					<nldd-icon-button
						variant="neutral-tinted"
						size=${iconButtonSize}
						icon="plus"
						text=${component._t("components.number-field.increment-action")}
						tooltip-timing="never"
						?disabled=${component.disabled || !canIncrease}
						@click=${component._handleIncrease}
					></nldd-icon-button>
				</div>
			` : A}
		</div>
	`;
  }

  // node_modules/@nldd/design-system/dist/components/inputs/number-field/number-field.i18n.js
  var nlddNumberFieldTranslations = {
    "components.number-field.decrement-action": "Verlaag",
    "components.number-field.increment-action": "Verhoog",
    "components.number-field.to-adjust-value-action": "Waarde aanpassen"
  };

  // node_modules/@nldd/design-system/dist/components/inputs/number-field/number-field.js
  init_icon_button();
  init_icon();
  var import_meta9 = {};
  var __decorate30 = function(decorators, target, key, desc) {
    var c6 = arguments.length, r6 = c6 < 3 ? target : desc === null ? desc = Object.getOwnPropertyDescriptor(target, key) : desc, d3;
    if (typeof Reflect === "object" && typeof Reflect.decorate === "function") r6 = Reflect.decorate(decorators, target, key, desc);
    else for (var i8 = decorators.length - 1; i8 >= 0; i8--) if (d3 = decorators[i8]) r6 = (c6 < 3 ? d3(r6) : c6 > 3 ? d3(target, key, r6) : d3(target, key)) || r6;
    return c6 > 3 && r6 && Object.defineProperty(target, key, r6), r6;
  };
  var NLDDNumberField = class NLDDNumberField2 extends i4 {
    constructor() {
      super(...arguments);
      this._internals = this.attachInternals();
      this._initialValue = 0;
      this.value = 0;
      this.min = -Infinity;
      this.max = Infinity;
      this.step = 1;
      this.size = "md";
      this.disabled = false;
      this.name = "";
      this.width = "";
      this.hideSpinButtons = false;
      this.accessibleLabel = "";
      this.translations = {};
      this._lastValidValue = 0;
    }
    firstUpdated() {
      if (import_meta9.env?.DEV && !this.accessibleLabel) {
        console.warn("<nldd-number-field>: No accessible-label provided. Add an accessible-label attribute so screen readers can announce the input's purpose.");
      }
      this._lastValidValue = this._clamp(this.value);
      this._initialValue = this._lastValidValue;
    }
    updated(changedProperties) {
      if (changedProperties.has("width")) {
        const w2 = this.width;
        if (w2 && w2 !== "full" && CSS.supports("width", w2)) {
          this.style.setProperty("--_width", w2);
        } else {
          this.style.removeProperty("--_width");
        }
      }
      if (changedProperties.has("value")) {
        this._internals.setFormValue(String(this.value));
      }
    }
    formResetCallback() {
      this.value = this._initialValue;
    }
    formDisabledCallback(disabled) {
      this.disabled = disabled;
    }
    formStateRestoreCallback(state) {
      if (typeof state !== "string")
        return;
      const parsed = parseFloat(state);
      if (!isNaN(parsed))
        this.value = this._clamp(parsed);
    }
    // — i18n —————————————————————————————————————————————————————————————————
    _t(key) {
      return this.translations[key] ?? nlddNumberFieldTranslations[key];
    }
    // — Actions ——————————————————————————————————————————————————————————————
    _handleDecrease() {
      if (this.disabled)
        return;
      this._commitValue(this.value - this.step);
    }
    _handleIncrease() {
      if (this.disabled)
        return;
      this._commitValue(this.value + this.step);
    }
    /** Fires while the user types. The value may be outside [min, max]; clamping happens on change. */
    _handleInput(e9) {
      if (this.disabled)
        return;
      const input = e9.target;
      const parsed = parseFloat(input.value);
      if (isNaN(parsed))
        return;
      this.value = parsed;
      this.dispatchEvent(new CustomEvent("input", {
        detail: { value: this.value },
        bubbles: true,
        composed: true
      }));
    }
    /** Fires on blur or Enter. Clamps the value and falls back to the last valid value when empty. */
    _handleChange(e9) {
      if (this.disabled)
        return;
      const input = e9.target;
      const parsed = parseFloat(input.value);
      const finalValue = isNaN(parsed) ? this._lastValidValue : this._clamp(parsed);
      this._commitValue(finalValue, input);
    }
    _commitValue(newValue, input) {
      const clampedValue = this._clamp(newValue);
      const changed = clampedValue !== this.value;
      this.value = clampedValue;
      this._lastValidValue = clampedValue;
      if (input)
        input.value = String(clampedValue);
      if (!changed)
        return;
      this.dispatchEvent(new CustomEvent("input", {
        detail: { value: this.value },
        bubbles: true,
        composed: true
      }));
      this.dispatchEvent(new CustomEvent("change", {
        detail: { value: this.value },
        bubbles: true,
        composed: true
      }));
    }
    _clamp(value) {
      return Math.max(this.min, Math.min(this.max, value));
    }
    /**
     * Delegates focus to the inner native `<input>`, so consumers can call
     * `numberFieldEl.focus()` without reaching into shadow DOM.
     */
    focus(options) {
      this.shadowRoot?.querySelector(".number-field__input")?.focus(options);
    }
    render() {
      return numberFieldTemplate(this);
    }
  };
  NLDDNumberField.formAssociated = true;
  NLDDNumberField.styles = numberFieldStyles;
  __decorate30([
    n4({ type: Number })
  ], NLDDNumberField.prototype, "value", void 0);
  __decorate30([
    n4({ type: Number })
  ], NLDDNumberField.prototype, "min", void 0);
  __decorate30([
    n4({ type: Number })
  ], NLDDNumberField.prototype, "max", void 0);
  __decorate30([
    n4({ type: Number })
  ], NLDDNumberField.prototype, "step", void 0);
  __decorate30([
    n4({ type: String, reflect: true })
  ], NLDDNumberField.prototype, "size", void 0);
  __decorate30([
    n4({ type: Boolean, reflect: true })
  ], NLDDNumberField.prototype, "disabled", void 0);
  __decorate30([
    n4({ type: String, reflect: true })
  ], NLDDNumberField.prototype, "name", void 0);
  __decorate30([
    n4({ type: String, reflect: true })
  ], NLDDNumberField.prototype, "width", void 0);
  __decorate30([
    n4({ type: Boolean, reflect: true, attribute: "hide-spin-buttons" })
  ], NLDDNumberField.prototype, "hideSpinButtons", void 0);
  __decorate30([
    n4({ type: String, attribute: "accessible-label" })
  ], NLDDNumberField.prototype, "accessibleLabel", void 0);
  __decorate30([
    n4({ type: Object })
  ], NLDDNumberField.prototype, "translations", void 0);
  NLDDNumberField = __decorate30([
    t3("nldd-number-field")
  ], NLDDNumberField);

  // node_modules/@nldd/design-system/dist/components/inputs/dropdown/dropdown.js
  init_lit();
  init_decorators();

  // node_modules/@nldd/design-system/dist/components/inputs/dropdown/dropdown.styles.js
  init_lit();
  init_slotted_reset();
  var dropdownStyles = i`


	/* # Host */

	:host {
		--_width: 100%;
		--_corner-radius: var(--semantics-controls-md-corner-radius);
		--_min-size: var(--semantics-controls-md-min-size);
		--_inline-padding: var(--semantics-controls-md-inline-padding);
		--_text-font: var(--semantics-input-fields-md-text-font);
		--_validation-icon-area-padding-right: var(--primitives-space-4);
		--_validation-icon-size: var(--semantics-input-fields-md-validation-icon-size);
		--_picker-icon-size: var(--primitives-space-24);
		--_background-color: var(--semantics-buttons-neutral-tinted-background-color);
		--_content-color: var(--semantics-buttons-neutral-tinted-primary-content-color);
		--_is-hovered-background-color: var(--semantics-buttons-neutral-tinted-is-hovered-background-color);
		--_is-hovered-content-color: var(--semantics-buttons-neutral-tinted-is-hovered-primary-content-color);
		--_is-active-background-color: var(--semantics-buttons-neutral-tinted-is-active-background-color);
		--_is-active-content-color: var(--semantics-buttons-neutral-tinted-is-active-primary-content-color);
		--_highlight-border-color: var(--semantics-buttons-neutral-tinted-highlight-border-color);
		--_is-hovered-highlight-border-color: var(--semantics-buttons-neutral-tinted-is-hovered-highlight-border-color);
		--_is-active-highlight-border-color: var(--semantics-buttons-neutral-tinted-is-active-highlight-border-color);
		--_is-expanded-highlight-border-color: var(--semantics-buttons-neutral-tinted-is-expanded-highlight-border-color);

		${inheritedTextReset}
		display: block;
		width: var(--_width);
		max-width: 100%;
		user-select: none;
		-webkit-tap-highlight-color: transparent;
	}

	:host([hidden]) {
		display: none;
	}

	:host([size="xs"]) {
		--_corner-radius: var(--semantics-controls-xs-corner-radius);
		--_min-size: var(--semantics-controls-xs-min-size);
		--_inline-padding: var(--semantics-controls-xs-inline-padding);
		--_text-font: var(--semantics-input-fields-xs-text-font);
		--_validation-icon-area-padding-right: var(--primitives-space-0);
		--_validation-icon-size: var(--semantics-input-fields-xs-validation-icon-size);
		--_picker-icon-size: var(--primitives-space-16);
	}

	:host([size="sm"]) {
		--_corner-radius: var(--semantics-controls-sm-corner-radius);
		--_min-size: var(--semantics-controls-sm-min-size);
		--_inline-padding: var(--semantics-controls-sm-inline-padding);
		--_text-font: var(--semantics-input-fields-sm-text-font);
		--_validation-icon-area-padding-right: var(--primitives-space-2);
		--_validation-icon-size: var(--semantics-input-fields-sm-validation-icon-size);
		--_picker-icon-size: var(--primitives-space-20);
	}

	:host([expanded]) {
		--_background-color: var(--semantics-buttons-neutral-tinted-is-expanded-background-color);
		--_content-color: var(--semantics-buttons-neutral-tinted-is-expanded-primary-content-color);
		--_is-hovered-background-color: var(--semantics-buttons-neutral-tinted-is-expanded-is-hovered-background-color);
		--_is-hovered-content-color: var(--semantics-buttons-neutral-tinted-is-expanded-is-hovered-primary-content-color);
		--_is-active-background-color: var(--semantics-buttons-neutral-tinted-is-expanded-is-active-background-color);
		--_is-active-content-color: var(--semantics-buttons-neutral-tinted-is-expanded-is-active-primary-content-color);
		--_highlight-border-color: var(--_is-expanded-highlight-border-color);
	}

	:host([disabled]) {
		opacity: var(--primitives-opacity-disabled);
		pointer-events: none;
	}


	/* # Block */

	.dropdown {
		box-sizing: border-box;
		display: flex;
		position: relative;
		border-radius: var(--_corner-radius);
		background-color: var(--_background-color);
		box-shadow: inset 0 0 0 var(--primitives-border-width-thin) var(--_highlight-border-color);
		width: 100%;
		min-height: var(--_min-size);
		flex-direction: row;
		align-items: center;
		color: var(--_content-color);
		transition:
			background-color var(--primitives-transition-duration-fast) var(--primitives-transition-easing-default),
			color var(--primitives-transition-duration-fast) var(--primitives-transition-easing-default)
		;
	}

	.dropdown:hover {
		@media (hover: hover) {
			background-color: var(--_is-hovered-background-color);
			color: var(--_is-hovered-content-color);
		}
	}

	@media (hover: hover) {
		.dropdown:hover {
			--_highlight-border-color: var(--_is-hovered-highlight-border-color);
		}
	}

	.dropdown:active {
		background-color: var(--_is-active-background-color);
		color: var(--_is-active-content-color);
		--_highlight-border-color: var(--_is-active-highlight-border-color);
	}

	.dropdown:focus-within {
		outline: var(--semantics-focus-ring-outline);
		outline-offset: var(--semantics-focus-ring-outline-offset);
		box-shadow: var(--semantics-focus-ring-box-shadow), inset 0 0 0 var(--primitives-border-width-thin) var(--_highlight-border-color);
	}

	:host([is-pointer-focus]) .dropdown:focus-within {
		outline: none;
		box-shadow: inset 0 0 0 var(--primitives-border-width-thin) var(--_highlight-border-color);
	}

	@media (prefers-reduced-motion: reduce) {
		.dropdown {
			transition: none;
		}
	}


	/* # Elements */

	::slotted(select) {
		${slottedReset}
		box-sizing: border-box !important;
		position: absolute !important;
		inset: 0 !important;
		opacity: 0 !important;
		margin: 0 !important;
		outline: none !important;
		border: none !important;
		background: transparent !important;
		width: 100% !important;
		height: 100% !important;
		padding: 0 !important;
		font: var(--semantics-input-fields-native-select-font) !important;
		appearance: none !important;
	}

	.dropdown__value {
		min-width: 0;
		overflow: hidden;
		padding: 0 var(--_inline-padding);
		flex-grow: 1;
		color: inherit;
		font: var(--_text-font);
		white-space: nowrap;
		text-overflow: ellipsis;
	}

	.dropdown__validation-icon-area {
		display: flex;
		height: 100%;
		padding-right: var(--_validation-icon-area-padding-right);
		flex-shrink: 0;
		align-items: center;
		justify-content: center;
	}

	:host([valid]) .dropdown__validation-icon-area {
		color: var(--semantics-input-fields-is-valid-icon-color);
	}

	:host([invalid]) .dropdown__validation-icon-area {
		color: var(--semantics-input-fields-is-invalid-icon-color);
	}

	.dropdown__validation-icon {
		width: var(--_validation-icon-size);
		height: var(--_validation-icon-size);
	}

	.dropdown__picker-icon {
		display: flex;
		width: var(--_picker-icon-size);
		height: var(--_picker-icon-size);
		padding-right: calc((var(--_min-size) - var(--_picker-icon-size)) / 2);
		flex-shrink: 0;
		align-items: center;
		justify-content: center;
		color: inherit;
	}
`;

  // node_modules/@nldd/design-system/dist/components/inputs/dropdown/dropdown.template.js
  init_lit();
  init_icon();
  function renderValidationIcon4(component) {
    if (component.invalid) {
      return b2`
			<div class="dropdown__validation-icon-area">
				<nldd-icon class="dropdown__validation-icon"
					name="invalid"
					aria-hidden="true"
				></nldd-icon>
			</div>
		`;
    }
    if (component.valid) {
      return b2`
			<div class="dropdown__validation-icon-area">
				<nldd-icon class="dropdown__validation-icon"
					name="valid"
					aria-hidden="true"
				></nldd-icon>
			</div>
		`;
    }
    return A;
  }
  function dropdownTemplate(component) {
    return b2`
		<div class="dropdown">
			<slot @slotchange=${component._onSlotChange}></slot>
			<span class="dropdown__value">${component._displayValue}</span>
			${renderValidationIcon4(component)}
			<div class="dropdown__picker-icon">
				<nldd-icon name="chevron-up-chevron-down"></nldd-icon>
			</div>
		</div>
	`;
  }

  // node_modules/@nldd/design-system/dist/components/inputs/dropdown/dropdown.js
  init_input_modality();
  init_icon();
  var import_meta10 = {};
  var __decorate31 = function(decorators, target, key, desc) {
    var c6 = arguments.length, r6 = c6 < 3 ? target : desc === null ? desc = Object.getOwnPropertyDescriptor(target, key) : desc, d3;
    if (typeof Reflect === "object" && typeof Reflect.decorate === "function") r6 = Reflect.decorate(decorators, target, key, desc);
    else for (var i8 = decorators.length - 1; i8 >= 0; i8--) if (d3 = decorators[i8]) r6 = (c6 < 3 ? d3(r6) : c6 > 3 ? d3(target, key, r6) : d3(target, key)) || r6;
    return c6 > 3 && r6 && Object.defineProperty(target, key, r6), r6;
  };
  var NLDDDropdown = class NLDDDropdown2 extends i4 {
    constructor() {
      super(...arguments);
      this.size = "md";
      this.valid = false;
      this.invalid = false;
      this.disabled = false;
      this.expanded = false;
      this.width = "";
      this._displayValue = "";
      this._select = null;
      this._handleSelectChange = (e9) => {
        e9.stopPropagation();
        this._syncDisplayValue();
        this.dispatchEvent(new CustomEvent("change", {
          detail: { value: this._select?.value ?? "" },
          bubbles: true,
          composed: true
        }));
      };
      this._handleSelectFocus = () => {
        this.toggleAttribute("is-pointer-focus", isPointerMode());
      };
      this._handleSelectBlur = () => {
        this.toggleAttribute("is-pointer-focus", false);
        this.expanded = false;
      };
      this._handleSelectKeydown = () => {
        this.toggleAttribute("is-pointer-focus", false);
      };
      this._handleSelectToggle = (e9) => {
        this.expanded = e9.newState === "open";
      };
    }
    // — Lifecycle ——————————————————————————————————————————————————————————————
    updated(changedProperties) {
      if (changedProperties.has("disabled")) {
        this._syncDisabled();
      }
      if (changedProperties.has("invalid")) {
        this._syncAriaInvalid();
      }
      if (changedProperties.has("width")) {
        const w2 = this.width;
        if (w2 && w2 !== "full" && CSS.supports("width", w2)) {
          this.style.setProperty("--_width", w2);
        } else {
          this.style.removeProperty("--_width");
        }
      }
    }
    // — Slot ——————————————————————————————————————————————————————————————————
    _onSlotChange() {
      const slot = this.shadowRoot?.querySelector("slot");
      const select = slot?.assignedElements({ flatten: true }).find((el) => el.tagName === "SELECT") ?? null;
      if (this._select && this._select !== select) {
        this._select.removeEventListener("change", this._handleSelectChange);
        this._select.removeEventListener("focus", this._handleSelectFocus);
        this._select.removeEventListener("blur", this._handleSelectBlur);
        this._select.removeEventListener("keydown", this._handleSelectKeydown);
        this._select.removeEventListener("toggle", this._handleSelectToggle);
      }
      this._select = select;
      if (!select) {
        this._displayValue = "";
        this.expanded = false;
        return;
      }
      if (import_meta10.env?.DEV && !select.hasAttribute("aria-label") && !select.hasAttribute("aria-labelledby") && !select.labels?.length) {
        console.warn("<nldd-dropdown>: The slotted <select> has no accessible name. Add an aria-label or aria-labelledby attribute to the <select> element.");
      }
      select.addEventListener("change", this._handleSelectChange);
      select.addEventListener("focus", this._handleSelectFocus);
      select.addEventListener("blur", this._handleSelectBlur);
      select.addEventListener("keydown", this._handleSelectKeydown);
      select.addEventListener("toggle", this._handleSelectToggle);
      this._syncDisabled();
      this._syncAriaInvalid();
      this._syncDisplayValue();
    }
    // — Internal helpers ——————————————————————————————————————————————————————
    _syncDisabled() {
      if (!this._select)
        return;
      this._select.disabled = this.disabled;
    }
    _syncAriaInvalid() {
      if (!this._select)
        return;
      if (this.invalid) {
        this._select.setAttribute("aria-invalid", "true");
      } else {
        this._select.removeAttribute("aria-invalid");
      }
    }
    _syncDisplayValue() {
      if (!this._select)
        return;
      this._displayValue = this._select.selectedOptions[0]?.text ?? "";
    }
    render() {
      return dropdownTemplate(this);
    }
  };
  NLDDDropdown.styles = dropdownStyles;
  __decorate31([
    n4({ type: String, reflect: true })
  ], NLDDDropdown.prototype, "size", void 0);
  __decorate31([
    n4({ type: Boolean, reflect: true })
  ], NLDDDropdown.prototype, "valid", void 0);
  __decorate31([
    n4({ type: Boolean, reflect: true })
  ], NLDDDropdown.prototype, "invalid", void 0);
  __decorate31([
    n4({ type: Boolean, reflect: true })
  ], NLDDDropdown.prototype, "disabled", void 0);
  __decorate31([
    n4({ type: Boolean, reflect: true })
  ], NLDDDropdown.prototype, "expanded", void 0);
  __decorate31([
    n4({ type: String, reflect: true })
  ], NLDDDropdown.prototype, "width", void 0);
  __decorate31([
    r5()
  ], NLDDDropdown.prototype, "_displayValue", void 0);
  NLDDDropdown = __decorate31([
    t3("nldd-dropdown")
  ], NLDDDropdown);

  // node_modules/@nldd/design-system/dist/components/inputs/combo-box/combo-box.js
  init_lit();
  init_decorators();

  // node_modules/@nldd/design-system/dist/components/inputs/combo-box/combo-box.styles.js
  init_lit();
  init_slotted_reset();
  var comboBoxStyles = i`


	/* # Host */

	:host {
		--_width: 100%;
		--_background-color: var(--semantics-input-fields-background-color);
		--_corner-radius: var(--semantics-controls-md-corner-radius);
		--_min-size: var(--semantics-controls-md-min-size);
		--_inline-padding: calc(var(--semantics-controls-md-inline-padding) - var(--semantics-input-fields-border-thickness));
		--_text-font: var(--semantics-input-fields-md-text-font);
		--_end-padding-right: calc((var(--_min-size) - var(--semantics-controls-sm-min-size)) / 2 - var(--semantics-input-fields-border-thickness));
		--_z-index-button-focus: 1;
		--_validation-icon-size: var(--semantics-input-fields-md-validation-icon-size);

		${inheritedTextReset}
		display: block;
		width: var(--_width);
		max-width: 100%;
		-webkit-tap-highlight-color: transparent;
	}

	:host([hidden]) {
		display: none;
	}

	:host([size="sm"]) {
		--_corner-radius: var(--semantics-controls-sm-corner-radius);
		--_min-size: var(--semantics-controls-sm-min-size);
		--_inline-padding: calc(var(--semantics-controls-sm-inline-padding) - var(--semantics-input-fields-border-thickness));
		--_text-font: var(--semantics-input-fields-sm-text-font);
		--_end-padding-right: calc((var(--_min-size) - var(--semantics-controls-xs-min-size)) / 2 - var(--semantics-input-fields-border-thickness));
		--_validation-icon-size: var(--semantics-input-fields-sm-validation-icon-size);
	}

	:host([disabled]) {
		opacity: var(--primitives-opacity-disabled);
		pointer-events: none;
	}


	/* # Block */

	.combo-box {
		box-sizing: border-box;
		display: flex;
		border: var(--semantics-input-fields-border);
		border-radius: var(--_corner-radius);
		background-color: var(--_background-color);
		width: 100%;
		min-height: var(--_min-size);
		flex-direction: row;
		align-items: center;
	}

	:host([valid]) .combo-box {
		border-color: var(--semantics-input-fields-is-valid-border-color);
	}

	:host([invalid]) .combo-box {
		border-color: var(--semantics-input-fields-is-invalid-border-color);
	}

	.combo-box:has(input:-webkit-autofill),
	.combo-box:has(input:autofill) {
		--_background-color: var(--semantics-input-fields-is-autofill-background-color);
	}

	.combo-box:has(.combo-box__input:focus-visible) {
		outline: var(--semantics-focus-ring-outline);
		outline-offset: var(--semantics-focus-ring-outline-offset);
		box-shadow: var(--semantics-focus-ring-box-shadow);
	}


	/* # Elements */

	.combo-box__input {
		box-sizing: border-box;
		margin: 0;
		outline: none;
		border: none;
		background: transparent;
		min-width: 0;
		width: 100%;
		padding-left: var(--_inline-padding);
		flex-grow: 1;
		flex-shrink: 1;
		flex-basis: 0;
		align-self: stretch;
		color: var(--semantics-content-color);
		font: var(--_text-font);
		appearance: none;
	}

	.combo-box__input::placeholder {
		color: var(--semantics-input-fields-placeholder-color);
	}

	.combo-box__input:-webkit-autofill,
	.combo-box__input:autofill,
	.combo-box__input:-webkit-autofill:disabled,
	.combo-box__input:autofill:disabled {
		box-shadow: 0 0 0 999px var(--_background-color) inset;
		-webkit-text-fill-color: var(--semantics-input-fields-is-autofill-content-color);
	}

	.combo-box__input-fade {
		position: relative;
		width: 0;
		flex-shrink: 0;
		align-self: stretch;
	}

	.combo-box__input-fade::after {
		content: '';
		position: absolute;
		top: 0;
		right: 0;
		bottom: 0;
		background: linear-gradient(90deg, color-mix(in oklch, var(--_background-color) 0%, transparent) 0%, var(--_background-color) 100%);
		pointer-events: none;
		width: var(--primitives-space-8);
	}

	.combo-box__end {
		display: flex;
		padding-right: var(--_end-padding-right);
		flex-shrink: 0;
		align-items: center;
	}

	.combo-box__clear-button:focus-within {
		position: relative;
		z-index: var(--_z-index-button-focus);
	}

	.combo-box__validation-icon-area {
		display: flex;
		height: 100%;
		flex-shrink: 0;
		align-items: center;
		justify-content: center;
	}

	:host([valid]) .combo-box__validation-icon-area {
		color: var(--semantics-input-fields-is-valid-icon-color);
	}

	:host([invalid]) .combo-box__validation-icon-area {
		color: var(--semantics-input-fields-is-invalid-icon-color);
	}

	.combo-box__validation-icon {
		width: var(--_validation-icon-size);
		height: var(--_validation-icon-size);
	}

	.combo-box__picker-button {
		margin-left: var(--primitives-space-6);
	}

	.combo-box__picker-button:focus-within {
		position: relative;
		z-index: var(--_z-index-button-focus);
	}
`;

  // node_modules/@nldd/design-system/dist/components/inputs/combo-box/combo-box.template.js
  init_lit();
  init_icon_button();
  init_icon();
  function renderValidationIcon5(component) {
    if (component.invalid) {
      return b2`
			<div class="combo-box__validation-icon-area">
				<nldd-icon class="combo-box__validation-icon"
					name="invalid"
					aria-hidden="true"
				></nldd-icon>
			</div>
		`;
    }
    if (component.valid) {
      return b2`
			<div class="combo-box__validation-icon-area">
				<nldd-icon class="combo-box__validation-icon"
					name="valid"
					aria-hidden="true"
				></nldd-icon>
			</div>
		`;
    }
    return A;
  }
  function comboBoxTemplate(component) {
    const iconButtonSize = component.size === "sm" ? "xs" : "sm";
    return b2`
		<div class="combo-box">
			<input class="combo-box__input"
				type="text"
				role="combobox"
				aria-label=${component.accessibleLabel || A}
				aria-expanded=${component._isOpen ? "true" : "false"}
				aria-controls=${component._menuId}
				aria-autocomplete="list"
				aria-haspopup="listbox"
				aria-activedescendant=${component._highlightedId || A}
				aria-invalid=${component.invalid ? "true" : A}
				.value=${component.text}
				placeholder=${component.placeholder || A}
				?disabled=${component.disabled}
				name=${component.name || A}
				autocomplete=${component.autocomplete || A}
				spellcheck=${component.noSpellcheck ? "false" : "true"}
				@input=${component._handleInput}
				@keydown=${component._handleKeydown}
				@blur=${component._handleBlur}
			>
			<div class="combo-box__input-fade"></div>
			<div class="combo-box__end">
				${component.text ? b2`
					<div class="combo-box__clear-button">
						<nldd-icon-button
							variant="neutral-transparent"
							size=${iconButtonSize}
							icon="dismiss"
							text=${component._t("components.combo-box.clear-action")}
							tooltip-timing="never"
							?disabled=${component.disabled}
							@click=${component._handleClear}
						></nldd-icon-button>
					</div>
				` : A}
				${renderValidationIcon5(component)}
				<div class="combo-box__picker-button">
					<nldd-icon-button
						variant="neutral-tinted"
						size=${iconButtonSize}
						icon="chevron-down"
						text=${component._t("components.combo-box.open-menu-action")}
						tooltip-timing="never"
						?disabled=${component.disabled}
						?expanded=${component._isOpen}
						popup-type="listbox"
						@pointerdown=${component._handlePickerPointerdown}
						@click=${component._toggleMenu}
					></nldd-icon-button>
				</div>
			</div>
		</div>
		<slot @slotchange=${component._onSlotChange}></slot>
	`;
  }

  // node_modules/@nldd/design-system/dist/components/inputs/combo-box/combo-box.i18n.js
  var nlddComboBoxTranslations = {
    "components.combo-box.open-menu-action": "Toon opties",
    "components.combo-box.clear-action": "Wis invoer"
  };

  // node_modules/@nldd/design-system/dist/components/inputs/combo-box/combo-box.js
  init_icon_button();
  init_icon();
  var import_meta11 = {};
  var __decorate32 = function(decorators, target, key, desc) {
    var c6 = arguments.length, r6 = c6 < 3 ? target : desc === null ? desc = Object.getOwnPropertyDescriptor(target, key) : desc, d3;
    if (typeof Reflect === "object" && typeof Reflect.decorate === "function") r6 = Reflect.decorate(decorators, target, key, desc);
    else for (var i8 = decorators.length - 1; i8 >= 0; i8--) if (d3 = decorators[i8]) r6 = (c6 < 3 ? d3(r6) : c6 > 3 ? d3(target, key, r6) : d3(target, key)) || r6;
    return c6 > 3 && r6 && Object.defineProperty(target, key, r6), r6;
  };
  var NLDDComboBox_1;
  var NLDDComboBox = NLDDComboBox_1 = class NLDDComboBox2 extends i4 {
    constructor() {
      super(...arguments);
      this._internals = this.attachInternals();
      this._initialValue = "";
      this._initialText = "";
      this.value = "";
      this.text = "";
      this.placeholder = "";
      this.size = "md";
      this.valid = false;
      this.invalid = false;
      this.disabled = false;
      this.name = "";
      this.autocomplete = "off";
      this.maxItems = 8;
      this.accessibleLabel = "";
      this.translations = {};
      this.noSpellcheck = false;
      this.width = "";
      this._isOpen = false;
      this._highlightedId = "";
      this._menuId = `nldd-combo-box-menu-${NLDDComboBox_1._idCounter++}`;
      this._menu = null;
      this._resizeObserver = null;
      this._handleScrollOrResize = () => {
        if (!this._isOpen)
          return;
        this._updateMenuWidth();
        this._menu?.reposition();
      };
      this._handleMenuToggle = (e9) => {
        this._isOpen = e9.newState === "open";
        if (!this._isOpen) {
          this._highlightedId = "";
        } else {
          requestAnimationFrame(() => {
            this._highlightedId = this._menu?.getHighlightedId() ?? "";
          });
        }
      };
      this._handleMenuSelect = (e9) => {
        const item = e9.target;
        this.text = item.text;
        this.value = item.value || item.text;
        this._highlightedId = "";
        this._closeMenu();
        this._menu?.filter("");
        this.dispatchEvent(new CustomEvent("change", {
          detail: { value: this.value },
          bubbles: true,
          composed: true
        }));
        this._input?.focus();
      };
      this._pickerPointerdownWhileOpen = false;
      this._handleMenuKeydown = (e9) => {
        if (e9.key !== "Tab")
          return;
        e9.preventDefault();
        this._closeMenu();
        this._input?.focus();
      };
    }
    // — i18n ——————————————————————————————————————————————————————————————————
    _t(key) {
      return this.translations[key] ?? nlddComboBoxTranslations[key];
    }
    // — Lifecycle ————————————————————————————————————————————————————————————
    firstUpdated() {
      if (import_meta11.env?.DEV && !this.accessibleLabel) {
        console.warn("<nldd-combo-box>: No accessible-label provided. Add an accessible-label attribute for screen reader accessibility.");
      }
      this._initialValue = this.value;
      this._initialText = this.text;
    }
    willUpdate(changedProperties) {
      if (changedProperties.has("value") && !changedProperties.has("text")) {
        this._deriveTextFromMenu();
      }
    }
    updated(changedProperties) {
      if (changedProperties.has("maxItems") && this._menu) {
        this._menu.maxItems = this.maxItems;
      }
      if (changedProperties.has("width")) {
        const w2 = this.width;
        if (w2 && w2 !== "full" && CSS.supports("width", w2)) {
          this.style.setProperty("--_width", w2);
        } else {
          this.style.removeProperty("--_width");
        }
      }
      if (changedProperties.has("value") || changedProperties.has("text")) {
        const state = new FormData();
        state.append("value", this.value);
        state.append("display", this.text);
        this._internals.setFormValue(this.value, state);
      }
    }
    formResetCallback() {
      this.value = this._initialValue;
      this.text = this._initialText;
    }
    formDisabledCallback(disabled) {
      this.disabled = disabled;
    }
    formStateRestoreCallback(state) {
      if (state instanceof FormData) {
        this.value = String(state.get("value") ?? "");
        this.text = String(state.get("display") ?? "");
      } else if (typeof state === "string") {
        this.value = state;
        this.text = state;
      }
    }
    connectedCallback() {
      super.connectedCallback();
      this._resizeObserver = new ResizeObserver(() => this._updateMenuWidth());
      this._resizeObserver.observe(this);
      window.addEventListener("scroll", this._handleScrollOrResize, true);
      window.addEventListener("resize", this._handleScrollOrResize);
    }
    disconnectedCallback() {
      super.disconnectedCallback();
      this._resizeObserver?.disconnect();
      this._resizeObserver = null;
      window.removeEventListener("scroll", this._handleScrollOrResize, true);
      window.removeEventListener("resize", this._handleScrollOrResize);
      if (this._menu) {
        this._menu.removeEventListener("toggle", this._handleMenuToggle);
        this._menu.removeEventListener("select", this._handleMenuSelect);
        this._menu.removeEventListener("keydown", this._handleMenuKeydown);
        this._menu = null;
      }
    }
    // — Slot —————————————————————————————————————————————————————————————————
    _onSlotChange() {
      const slot = this.shadowRoot?.querySelector("slot");
      const menu = slot?.assignedElements({ flatten: true }).find((el) => el.tagName.toLowerCase() === "nldd-menu");
      if (!menu || menu === this._menu)
        return;
      this._menu = menu;
      menu.id = this._menuId;
      menu.anchorElement = this;
      menu.placement = "bottom-start";
      menu.maxItems = this.maxItems;
      menu.variant = "listbox";
      menu.addEventListener("toggle", this._handleMenuToggle);
      menu.addEventListener("select", this._handleMenuSelect);
      menu.addEventListener("keydown", this._handleMenuKeydown);
      this._updateMenuWidth();
      if (this.value && !this.text) {
        this._deriveTextFromMenu();
      }
    }
    /**
     * Walk slotted menu items and set `text` from the first item whose `value`
     * (or `text`, when value is unset) matches the current `value`. No-op when
     * the menu isn't wired yet or no item matches.
     */
    _deriveTextFromMenu() {
      if (!this._menu || !this.value)
        return;
      const items = Array.from(this._menu.querySelectorAll("nldd-menu-item")).filter((item) => item.closest("nldd-menu") === this._menu);
      for (const item of items) {
        if ((item.value || item.text) === this.value) {
          this.text = item.text;
          return;
        }
      }
    }
    // — Menu width & position ————————————————————————————————————————————————
    _updateMenuWidth() {
      if (!this._menu)
        return;
      const rect = this.getBoundingClientRect();
      this._menu.width = `${rect.width}px`;
    }
    _updateActiveDescendant() {
      this._highlightedId = this._menu?.getHighlightedId() ?? "";
    }
    /**
     * Open the menu. Focus always stays on the input — highlight moves via
     * aria-activedescendant rather than by moving focus to items.
     */
    _openMenu() {
      if (!this._menu || this._isOpen)
        return;
      if (!("showPopover" in this._menu)) {
        console.warn("<nldd-combo-box>: Popover API is not supported in this browser. The dropdown will not open.");
        return;
      }
      this._updateMenuWidth();
      this._menu.showPopover();
    }
    _closeMenu() {
      if (!this._menu || !this._isOpen)
        return;
      this._menu.hidePopover();
    }
    _toggleMenu() {
      if (this._pickerPointerdownWhileOpen) {
        this._pickerPointerdownWhileOpen = false;
        return;
      }
      if (this._isOpen) {
        this._closeMenu();
        this._input?.focus();
      } else {
        this._openMenu();
        this._input?.focus();
      }
    }
    _handlePickerPointerdown() {
      if (this._isOpen) {
        this._pickerPointerdownWhileOpen = true;
      }
    }
    _handleInput(e9) {
      const input = e9.target;
      this.text = input.value;
      this._menu?.filter(this.text);
      this._updateActiveDescendant();
      if (!this._isOpen)
        this._openMenu();
      this.dispatchEvent(new CustomEvent("input", {
        detail: { value: this.text },
        bubbles: true,
        composed: true
      }));
    }
    _handleClear() {
      this.text = "";
      this.value = "";
      this._menu?.filter("");
      this._closeMenu();
      this.dispatchEvent(new CustomEvent("input", {
        detail: { value: "" },
        bubbles: true,
        composed: true
      }));
      this.dispatchEvent(new CustomEvent("change", {
        detail: { value: "" },
        bubbles: true,
        composed: true
      }));
      this._input?.focus();
    }
    /** Accept a custom typed value and close the menu when focus leaves the input. */
    _handleBlur(e9) {
      const relatedTarget = e9.relatedTarget;
      if (!relatedTarget || !this._menu?.contains(relatedTarget)) {
        this._closeMenu();
      }
      if (this.text !== "" && this.text !== this.value) {
        this.value = this.text;
        this.dispatchEvent(new CustomEvent("change", {
          detail: { value: this.value },
          bubbles: true,
          composed: true
        }));
      }
    }
    _handleKeydown(e9) {
      if (!this._isOpen) {
        if (e9.key === "ArrowDown") {
          e9.preventDefault();
          this._openMenu();
        }
        return;
      }
      switch (e9.key) {
        case "ArrowDown":
          e9.preventDefault();
          this._menu?.moveHighlight("next");
          this._updateActiveDescendant();
          break;
        case "ArrowUp":
          e9.preventDefault();
          this._menu?.moveHighlight("prev");
          this._updateActiveDescendant();
          break;
        case "Enter": {
          e9.preventDefault();
          const highlighted = this._menu?.getHighlighted();
          if (highlighted) {
            highlighted.select();
          } else {
            this.value = this.text;
            this._closeMenu();
            this.dispatchEvent(new CustomEvent("change", {
              detail: { value: this.value },
              bubbles: true,
              composed: true
            }));
          }
          break;
        }
        case "Escape":
          e9.preventDefault();
          this._closeMenu();
          break;
      }
    }
    /**
     * Delegates focus to the inner native `<input>`, so consumers can call
     * `comboBoxEl.focus()` without reaching into shadow DOM.
     */
    focus(options) {
      this._input?.focus(options);
    }
    render() {
      return comboBoxTemplate(this);
    }
  };
  NLDDComboBox.formAssociated = true;
  NLDDComboBox.styles = comboBoxStyles;
  NLDDComboBox._idCounter = 0;
  __decorate32([
    n4({ type: String })
  ], NLDDComboBox.prototype, "value", void 0);
  __decorate32([
    n4({ type: String })
  ], NLDDComboBox.prototype, "text", void 0);
  __decorate32([
    n4({ type: String })
  ], NLDDComboBox.prototype, "placeholder", void 0);
  __decorate32([
    n4({ type: String, reflect: true })
  ], NLDDComboBox.prototype, "size", void 0);
  __decorate32([
    n4({ type: Boolean, reflect: true })
  ], NLDDComboBox.prototype, "valid", void 0);
  __decorate32([
    n4({ type: Boolean, reflect: true })
  ], NLDDComboBox.prototype, "invalid", void 0);
  __decorate32([
    n4({ type: Boolean, reflect: true })
  ], NLDDComboBox.prototype, "disabled", void 0);
  __decorate32([
    n4({ type: String, reflect: true })
  ], NLDDComboBox.prototype, "name", void 0);
  __decorate32([
    n4({ type: String })
  ], NLDDComboBox.prototype, "autocomplete", void 0);
  __decorate32([
    n4({ type: Number, attribute: "max-items" })
  ], NLDDComboBox.prototype, "maxItems", void 0);
  __decorate32([
    n4({ type: String, attribute: "accessible-label" })
  ], NLDDComboBox.prototype, "accessibleLabel", void 0);
  __decorate32([
    n4({ type: Object })
  ], NLDDComboBox.prototype, "translations", void 0);
  __decorate32([
    n4({ type: Boolean, reflect: true, attribute: "no-spellcheck" })
  ], NLDDComboBox.prototype, "noSpellcheck", void 0);
  __decorate32([
    n4({ type: String, reflect: true })
  ], NLDDComboBox.prototype, "width", void 0);
  __decorate32([
    r5()
  ], NLDDComboBox.prototype, "_isOpen", void 0);
  __decorate32([
    r5()
  ], NLDDComboBox.prototype, "_highlightedId", void 0);
  __decorate32([
    e5(".combo-box__input")
  ], NLDDComboBox.prototype, "_input", void 0);
  NLDDComboBox = NLDDComboBox_1 = __decorate32([
    t3("nldd-combo-box")
  ], NLDDComboBox);

  // node_modules/@nldd/design-system/dist/components/inputs/stepper/stepper.js
  init_lit();
  init_decorators();

  // node_modules/@nldd/design-system/dist/components/inputs/stepper/stepper.styles.js
  init_lit();
  var stepperStyles = i`


	/* # Host */

	:host {
		--_corner-radius: var(--semantics-controls-md-corner-radius);
		--_divider-length: var(--semantics-buttons-md-divider-length);
		--_highlight-border-color: var(--semantics-buttons-neutral-tinted-highlight-border-color);

		display: inline-flex;
		isolation: isolate;
		user-select: none;
		-webkit-tap-highlight-color: transparent;
	}

	:host([hidden]) {
		display: none;
	}

	:host([size="xs"]) {
		--_corner-radius: var(--semantics-controls-xs-corner-radius);
		--_divider-length: var(--semantics-buttons-xs-divider-length);
	}

	:host([size="sm"]) {
		--_corner-radius: var(--semantics-controls-sm-corner-radius);
		--_divider-length: var(--semantics-buttons-sm-divider-length);
	}

	:host([disabled]) {
		opacity: var(--primitives-opacity-disabled);
		pointer-events: none;
	}

	:host([disabled]) nldd-icon-button {
		opacity: 1;
	}


	/* # Block */

	.stepper {
		display: inline-flex;
		position: relative;
		border-radius: var(--_corner-radius);
		background-color: var(--semantics-buttons-neutral-tinted-background-color);
		flex-direction: row;
		align-items: center;
	}

	.stepper::after {
		content: '';
		position: absolute;
		inset: 0;
		border-radius: inherit;
		box-shadow: inset 0 0 0 var(--primitives-border-width-thin) var(--_highlight-border-color);
		pointer-events: none;
	}

	.stepper:focus-visible {
		outline: var(--semantics-focus-ring-outline);
		outline-offset: var(--semantics-focus-ring-outline-offset);
		box-shadow: var(--semantics-focus-ring-box-shadow);
	}


	/* # Elements */

	.stepper__divider {
		background-color: var(--semantics-buttons-neutral-tinted-divider-color);
		width: var(--semantics-dividers-thickness);
		height: var(--_divider-length);
		flex-shrink: 0;
	}

	nldd-icon-button:focus-within {
		position: relative;
		z-index: 1;
	}
`;

  // node_modules/@nldd/design-system/dist/components/inputs/stepper/stepper.template.js
  init_lit();
  init_icon_button();
  function stepperTemplate(component) {
    const atMin = component.value <= component.min;
    const atMax = component.value >= component.max;
    return b2`
		<div class="stepper"
			role="spinbutton"
			tabindex=${component.disabled ? A : "0"}
			aria-valuenow=${component.value}
			aria-valuemin=${isFinite(component.min) ? component.min : A}
			aria-valuemax=${isFinite(component.max) ? component.max : A}
			aria-label=${component._t("components.stepper.to-adjust-value-action")}
			aria-disabled=${component.disabled ? "true" : A}
			@keydown=${component._handleKeydown}
		>
			<nldd-icon-button
				variant="neutral-tinted"
				size=${component.size}
				icon="minus"
				text=${component._t("components.stepper.decrement-action")}
				tooltip-timing="never"
				no-highlight-border
				?disabled=${component.disabled || atMin}
				aria-hidden="true"
				tabindex="-1"
				@click=${component._decrement}
			></nldd-icon-button>
			<div class="stepper__divider"
				aria-hidden="true"
			></div>
			<nldd-icon-button
				variant="neutral-tinted"
				size=${component.size}
				icon="plus"
				text=${component._t("components.stepper.increment-action")}
				tooltip-timing="never"
				no-highlight-border
				?disabled=${component.disabled || atMax}
				aria-hidden="true"
				tabindex="-1"
				@click=${component._increment}
			></nldd-icon-button>
		</div>
	`;
  }

  // node_modules/@nldd/design-system/dist/components/inputs/stepper/stepper.i18n.js
  var nlddStepperTranslations = {
    "components.stepper.decrement-action": "Verlaag",
    "components.stepper.increment-action": "Verhoog",
    "components.stepper.to-adjust-value-action": "Aantal aanpassen"
  };

  // node_modules/@nldd/design-system/dist/components/inputs/stepper/stepper.js
  init_icon_button();
  init_icon();
  var __decorate33 = function(decorators, target, key, desc) {
    var c6 = arguments.length, r6 = c6 < 3 ? target : desc === null ? desc = Object.getOwnPropertyDescriptor(target, key) : desc, d3;
    if (typeof Reflect === "object" && typeof Reflect.decorate === "function") r6 = Reflect.decorate(decorators, target, key, desc);
    else for (var i8 = decorators.length - 1; i8 >= 0; i8--) if (d3 = decorators[i8]) r6 = (c6 < 3 ? d3(r6) : c6 > 3 ? d3(target, key, r6) : d3(target, key)) || r6;
    return c6 > 3 && r6 && Object.defineProperty(target, key, r6), r6;
  };
  var NLDDStepper = class NLDDStepper2 extends i4 {
    constructor() {
      super(...arguments);
      this._internals = this.attachInternals();
      this._initialValue = 0;
      this.value = 0;
      this.min = 0;
      this.max = Infinity;
      this.step = 1;
      this.disabled = false;
      this.size = "md";
      this.name = "";
      this.translations = {};
    }
    firstUpdated() {
      this._initialValue = this.value;
    }
    updated(changed) {
      if (changed.has("value")) {
        this._internals.setFormValue(String(this.value));
      }
    }
    formResetCallback() {
      this.value = this._initialValue;
    }
    formDisabledCallback(disabled) {
      this.disabled = disabled;
    }
    formStateRestoreCallback(state) {
      if (typeof state !== "string")
        return;
      const parsed = parseFloat(state);
      if (!isNaN(parsed))
        this.value = Math.max(this.min, Math.min(this.max, parsed));
    }
    // — i18n —————————————————————————————————————————————————————————————————
    _t(key) {
      return this.translations[key] ?? nlddStepperTranslations[key];
    }
    // — Actions ——————————————————————————————————————————————————————————————
    _decrement() {
      if (this.disabled)
        return;
      const newValue = Math.max(this.min, this.value - this.step);
      if (newValue !== this.value) {
        this.value = newValue;
        this._dispatchChange();
      }
    }
    _increment() {
      if (this.disabled)
        return;
      const newValue = Math.min(this.max, this.value + this.step);
      if (newValue !== this.value) {
        this.value = newValue;
        this._dispatchChange();
      }
    }
    _handleKeydown(e9) {
      switch (e9.key) {
        case "ArrowUp":
        case "ArrowRight":
          e9.preventDefault();
          this._increment();
          break;
        case "ArrowDown":
        case "ArrowLeft":
          e9.preventDefault();
          this._decrement();
          break;
        case "Home":
          e9.preventDefault();
          if (isFinite(this.min)) {
            this.value = this.min;
            this._dispatchChange();
          }
          break;
        case "End":
          e9.preventDefault();
          if (isFinite(this.max)) {
            this.value = this.max;
            this._dispatchChange();
          }
          break;
      }
    }
    _dispatchChange() {
      this.dispatchEvent(new CustomEvent("change", {
        detail: { value: this.value },
        bubbles: true,
        composed: true
      }));
    }
    render() {
      return stepperTemplate(this);
    }
  };
  NLDDStepper.formAssociated = true;
  NLDDStepper.styles = stepperStyles;
  __decorate33([
    n4({ type: Number })
  ], NLDDStepper.prototype, "value", void 0);
  __decorate33([
    n4({ type: Number })
  ], NLDDStepper.prototype, "min", void 0);
  __decorate33([
    n4({ type: Number })
  ], NLDDStepper.prototype, "max", void 0);
  __decorate33([
    n4({ type: Number })
  ], NLDDStepper.prototype, "step", void 0);
  __decorate33([
    n4({ type: Boolean, reflect: true })
  ], NLDDStepper.prototype, "disabled", void 0);
  __decorate33([
    n4({ type: String, reflect: true })
  ], NLDDStepper.prototype, "size", void 0);
  __decorate33([
    n4({ type: String, reflect: true })
  ], NLDDStepper.prototype, "name", void 0);
  __decorate33([
    n4({ type: Object })
  ], NLDDStepper.prototype, "translations", void 0);
  NLDDStepper = __decorate33([
    t3("nldd-stepper")
  ], NLDDStepper);

  // node_modules/@nldd/design-system/dist/components/inputs/checkbox/checkbox.js
  init_lit();
  init_decorators();

  // node_modules/@nldd/design-system/dist/components/inputs/checkbox/checkbox.styles.js
  init_lit();
  var checkboxStyles = i`


	/* # Host */

	:host {
		display: inline-flex;
		position: relative;
		width: var(--semantics-controls-xs-min-size);
		height: var(--semantics-controls-xs-min-size);
		align-items: center;
		justify-content: center;
		user-select: none;
		-webkit-tap-highlight-color: transparent;
	}

	:host([hidden]) {
		display: none;
	}


	/* # Elements */

	.checkbox__input {
		position: absolute;
		inset: 0;
		opacity: 0;
		z-index: 1;
		margin: 0;
		width: 100%;
		height: 100%;
	}

	.checkbox__box {
		box-sizing: border-box;
		position: relative;
		border: var(--components-checkbox-border-thickness) solid var(--components-checkbox-border-color);
		border-radius: var(--semantics-controls-xs-corner-radius);
		background-color: var(--components-checkbox-background-color);
		width: var(--semantics-controls-xs-min-size);
		height: var(--semantics-controls-xs-min-size);
		color: transparent;
	}

	.checkbox__input:checked ~ .checkbox__box,
	.checkbox__input:indeterminate ~ .checkbox__box {
		border-color: var(--components-checkbox-is-selected-border-color);
		background-color: var(--components-checkbox-is-selected-background-color);
		color: var(--components-checkbox-is-selected-icon-color);
	}

	@media (hover: hover) {
		.checkbox__input:hover:not(:disabled) ~ .checkbox__box {
			border-color: var(--components-checkbox-is-hovered-border-color);
		}

		.checkbox__input:checked:hover:not(:disabled) ~ .checkbox__box,
		.checkbox__input:indeterminate:hover:not(:disabled) ~ .checkbox__box {
			border-color: var(--components-checkbox-is-selected-is-hovered-border-color);
			background-color: var(--components-checkbox-is-selected-is-hovered-background-color);
			color: var(--components-checkbox-is-selected-is-hovered-icon-color);
		}
	}

	.checkbox__input:active:not(:disabled) ~ .checkbox__box {
		border-color: var(--components-checkbox-is-active-border-color);
	}

	.checkbox__input:checked:active:not(:disabled) ~ .checkbox__box,
	.checkbox__input:indeterminate:active:not(:disabled) ~ .checkbox__box {
		border-color: var(--components-checkbox-is-selected-is-active-border-color);
		background-color: var(--components-checkbox-is-selected-is-active-background-color);
		color: var(--components-checkbox-is-selected-is-active-icon-color);
	}

	.checkbox__input:focus-visible ~ .checkbox__box {
		outline: var(--semantics-focus-ring-outline);
		outline-offset: var(--semantics-focus-ring-outline-offset);
		box-shadow: var(--semantics-focus-ring-box-shadow);
	}

	.checkbox__input:disabled ~ .checkbox__box {
		opacity: var(--primitives-opacity-disabled);
	}

	.checkbox__check-icon,
	.checkbox__indeterminate-icon {
		display: none;
		position: absolute;
		top: 50%;
		left: 50%;
		width: var(--primitives-space-24);
		height: var(--primitives-space-24);
		transform: translate(-50%, -50%);
	}

	.checkbox__input:checked ~ .checkbox__box .checkbox__check-icon {
		display: block;
	}

	.checkbox__input:checked:indeterminate ~ .checkbox__box .checkbox__check-icon {
		display: none;
	}

	.checkbox__input:indeterminate ~ .checkbox__box .checkbox__indeterminate-icon {
		display: block;
	}
`;

  // node_modules/@nldd/design-system/dist/components/inputs/checkbox/checkbox.template.js
  init_lit();
  init_icon();
  function checkboxTemplate(component) {
    return b2`
		<input class="checkbox__input"
			type="checkbox"
			.checked=${component.checked}
			.indeterminate=${component.indeterminate}
			?disabled=${component.disabled}
			name=${component.name || ""}
			value=${component.value}
			aria-label=${component.accessibleLabel || A}
			@change=${component._handleChange}
		>
		<div class="checkbox__box" aria-hidden="true">
			<nldd-icon class="checkbox__check-icon" name="check-mark-small"></nldd-icon>
			<nldd-icon class="checkbox__indeterminate-icon" name="minus-extra-small"></nldd-icon>
		</div>
	`;
  }

  // node_modules/@nldd/design-system/dist/components/inputs/checkbox/checkbox.js
  var __decorate34 = function(decorators, target, key, desc) {
    var c6 = arguments.length, r6 = c6 < 3 ? target : desc === null ? desc = Object.getOwnPropertyDescriptor(target, key) : desc, d3;
    if (typeof Reflect === "object" && typeof Reflect.decorate === "function") r6 = Reflect.decorate(decorators, target, key, desc);
    else for (var i8 = decorators.length - 1; i8 >= 0; i8--) if (d3 = decorators[i8]) r6 = (c6 < 3 ? d3(r6) : c6 > 3 ? d3(target, key, r6) : d3(target, key)) || r6;
    return c6 > 3 && r6 && Object.defineProperty(target, key, r6), r6;
  };
  var NLDDCheckbox = class NLDDCheckbox2 extends i4 {
    constructor() {
      super(...arguments);
      this._internals = this.attachInternals();
      this._initialChecked = false;
      this.checked = false;
      this.disabled = false;
      this.indeterminate = false;
      this.value = "on";
      this.name = "";
      this.accessibleLabel = "";
    }
    firstUpdated() {
      this._initialChecked = this.checked;
    }
    updated(changed) {
      if (changed.has("checked") || changed.has("value")) {
        this._internals.setFormValue(this.checked ? this.value : null);
      }
    }
    formResetCallback() {
      this.checked = this._initialChecked;
      this.indeterminate = false;
    }
    formDisabledCallback(disabled) {
      this.disabled = disabled;
    }
    formStateRestoreCallback(state) {
      this.checked = state !== null;
    }
    toggle() {
      if (this.disabled)
        return;
      this.checked = !this.checked;
      this.indeterminate = false;
      this.dispatchEvent(new CustomEvent("change", {
        detail: { checked: this.checked, value: this.value },
        bubbles: true,
        composed: true
      }));
    }
    _handleChange(e9) {
      const input = e9.target;
      this.checked = input.checked;
      this.indeterminate = input.indeterminate;
      this.dispatchEvent(new CustomEvent("change", {
        detail: { checked: this.checked, value: this.value },
        bubbles: true,
        composed: true
      }));
    }
    render() {
      return checkboxTemplate(this);
    }
  };
  NLDDCheckbox.formAssociated = true;
  NLDDCheckbox.styles = checkboxStyles;
  __decorate34([
    n4({ type: Boolean, reflect: true })
  ], NLDDCheckbox.prototype, "checked", void 0);
  __decorate34([
    n4({ type: Boolean, reflect: true })
  ], NLDDCheckbox.prototype, "disabled", void 0);
  __decorate34([
    n4({ type: Boolean, reflect: true })
  ], NLDDCheckbox.prototype, "indeterminate", void 0);
  __decorate34([
    n4({ type: String })
  ], NLDDCheckbox.prototype, "value", void 0);
  __decorate34([
    n4({ type: String, reflect: true })
  ], NLDDCheckbox.prototype, "name", void 0);
  __decorate34([
    n4({ type: String, attribute: "accessible-label" })
  ], NLDDCheckbox.prototype, "accessibleLabel", void 0);
  NLDDCheckbox = __decorate34([
    t3("nldd-checkbox")
  ], NLDDCheckbox);

  // node_modules/@nldd/design-system/dist/components/inputs/checkbox-field/checkbox-field.js
  init_lit();
  init_decorators();

  // node_modules/@nldd/design-system/dist/components/inputs/checkbox-field/checkbox-field.styles.js
  init_lit();
  init_slotted_reset();
  var checkboxFieldStyles = i`


	/* # Host */

	:host {
		${inheritedTextReset}
		display: block;
	}

	:host([hidden]) {
		display: none;
	}


	/* # Block */

	.checkbox-field {
		display: flex;
		min-height: var(--semantics-controls-md-min-size);
		flex-direction: row;
		gap: var(--primitives-space-8);
		align-items: flex-start;
	}


	/* # Elements */

	.checkbox-field__control {
		display: flex;
		min-height: var(--semantics-controls-md-min-size);
		flex-shrink: 0;
		align-items: center;
	}

	.checkbox-field__label {
		display: flex;
		cursor: default;
		padding-top: calc((var(--semantics-controls-md-min-size) - 1em * var(--primitives-line-height-snug)) / 2);
		flex-grow: 1;
		color: var(--semantics-content-color);
		font: var(--primitives-font-body-md-regular-snug);
	}

	:host([disabled]) .checkbox-field__label {
		opacity: var(--primitives-opacity-disabled);
	}
`;

  // node_modules/@nldd/design-system/dist/components/inputs/checkbox-field/checkbox-field.template.js
  init_lit();
  function checkboxFieldTemplate(component) {
    return b2`
		<div class="checkbox-field"
			@click=${component._handleLabelClick}
		>
			<div class="checkbox-field__control">
				<nldd-checkbox
					?checked=${component.checked}
					?indeterminate=${component.indeterminate}
					?disabled=${component.disabled}
					name=${component.name || A}
					value=${component.value}
					accessible-label=${component.label || A}
					@change=${component._handleChange}
				></nldd-checkbox>
			</div>
			<span class="checkbox-field__label">${component.label}</span>
		</div>
	`;
  }

  // node_modules/@nldd/design-system/dist/components/inputs/checkbox-field/checkbox-field.js
  var __decorate35 = function(decorators, target, key, desc) {
    var c6 = arguments.length, r6 = c6 < 3 ? target : desc === null ? desc = Object.getOwnPropertyDescriptor(target, key) : desc, d3;
    if (typeof Reflect === "object" && typeof Reflect.decorate === "function") r6 = Reflect.decorate(decorators, target, key, desc);
    else for (var i8 = decorators.length - 1; i8 >= 0; i8--) if (d3 = decorators[i8]) r6 = (c6 < 3 ? d3(r6) : c6 > 3 ? d3(target, key, r6) : d3(target, key)) || r6;
    return c6 > 3 && r6 && Object.defineProperty(target, key, r6), r6;
  };
  var NLDDCheckboxField = class NLDDCheckboxField2 extends i4 {
    constructor() {
      super(...arguments);
      this.checked = false;
      this.indeterminate = false;
      this.disabled = false;
      this.value = "on";
      this.name = "";
      this.label = "";
    }
    _handleLabelClick(e9) {
      if (this.disabled)
        return;
      if (e9.target.closest?.("nldd-checkbox"))
        return;
      const checkbox = this.shadowRoot?.querySelector("nldd-checkbox");
      checkbox?.toggle();
    }
    _handleChange(e9) {
      const { checked } = e9.detail;
      this.checked = checked;
      this.indeterminate = false;
      this.dispatchEvent(new CustomEvent("change", {
        detail: { checked: this.checked, value: this.value },
        bubbles: true,
        composed: true
      }));
    }
    render() {
      return checkboxFieldTemplate(this);
    }
  };
  NLDDCheckboxField.styles = checkboxFieldStyles;
  __decorate35([
    n4({ type: Boolean, reflect: true })
  ], NLDDCheckboxField.prototype, "checked", void 0);
  __decorate35([
    n4({ type: Boolean, reflect: true })
  ], NLDDCheckboxField.prototype, "indeterminate", void 0);
  __decorate35([
    n4({ type: Boolean, reflect: true })
  ], NLDDCheckboxField.prototype, "disabled", void 0);
  __decorate35([
    n4({ type: String })
  ], NLDDCheckboxField.prototype, "value", void 0);
  __decorate35([
    n4({ type: String })
  ], NLDDCheckboxField.prototype, "name", void 0);
  __decorate35([
    n4({ type: String })
  ], NLDDCheckboxField.prototype, "label", void 0);
  NLDDCheckboxField = __decorate35([
    t3("nldd-checkbox-field")
  ], NLDDCheckboxField);

  // node_modules/@nldd/design-system/dist/components/inputs/radio-button/radio-button.js
  init_lit();
  init_decorators();

  // node_modules/@nldd/design-system/dist/components/inputs/radio-button/radio-button.styles.js
  init_lit();
  var radioButtonStyles = i`


	/* # Host */

	:host {
		display: inline-flex;
		position: relative;
		width: var(--semantics-controls-xs-min-size);
		height: var(--semantics-controls-xs-min-size);
		align-items: center;
		justify-content: center;
		user-select: none;
		-webkit-tap-highlight-color: transparent;
	}

	:host([hidden]) {
		display: none;
	}


	/* # Elements */

	.radio-button__input {
		position: absolute;
		inset: 0;
		opacity: 0;
		z-index: 1;
		margin: 0;
		width: 100%;
		height: 100%;
	}

	.radio-button__outer-shape {
		box-sizing: border-box;
		position: relative;
		border: var(--components-radio-button-border-thickness) solid var(--components-radio-button-border-color);
		border-radius: 50%;
		background-color: var(--components-radio-button-background-color);
		width: var(--semantics-controls-xs-min-size);
		height: var(--semantics-controls-xs-min-size);
	}

	.radio-button__inner-shape {
		box-sizing: border-box;
		position: absolute;
		top: 50%;
		left: 50%;
		border: var(--components-radio-button-is-selected-inner-shape-border-thickness) solid var(--components-radio-button-is-selected-inner-shape-border-color);
		border-radius: 50%;
		width: var(--primitives-space-20);
		height: var(--primitives-space-20);
		transform: translate(-50%, -50%) scale(0);
	}

	.radio-button__input:checked ~ .radio-button__outer-shape {
		border-color: var(--components-radio-button-is-selected-border-color);
		background-color: var(--components-radio-button-is-selected-background-color);
	}

	.radio-button__input:checked ~ .radio-button__outer-shape .radio-button__inner-shape {
		transform: translate(-50%, -50%) scale(1);
	}

	@media (hover: hover) {
		.radio-button__input:hover:not(:disabled) ~ .radio-button__outer-shape {
			border-color: var(--components-radio-button-is-hovered-border-color);
		}

		.radio-button__input:checked:hover:not(:disabled) ~ .radio-button__outer-shape {
			border-color: var(--components-radio-button-is-selected-is-hovered-border-color);
			background-color: var(--components-radio-button-is-selected-is-hovered-background-color);
		}

		.radio-button__input:checked:hover:not(:disabled) ~ .radio-button__outer-shape .radio-button__inner-shape {
			border-color: var(--components-radio-button-is-selected-is-hovered-inner-shape-border-color);
		}
	}

	.radio-button__input:active:not(:disabled) ~ .radio-button__outer-shape {
		border-color: var(--components-radio-button-is-active-border-color);
	}

	.radio-button__input:checked:active:not(:disabled) ~ .radio-button__outer-shape {
		border-color: var(--components-radio-button-is-selected-is-active-border-color);
		background-color: var(--components-radio-button-is-selected-is-active-background-color);
	}

	.radio-button__input:checked:active:not(:disabled) ~ .radio-button__outer-shape .radio-button__inner-shape {
		border-color: var(--components-radio-button-is-selected-is-active-inner-shape-border-color);
	}

	.radio-button__input:focus-visible ~ .radio-button__outer-shape {
		outline: var(--semantics-focus-ring-outline);
		outline-offset: var(--semantics-focus-ring-outline-offset);
		box-shadow: var(--semantics-focus-ring-box-shadow);
	}

	.radio-button__input:disabled ~ .radio-button__outer-shape {
		opacity: var(--primitives-opacity-disabled);
	}
`;

  // node_modules/@nldd/design-system/dist/components/inputs/radio-button/radio-button.template.js
  init_lit();
  function radioButtonTemplate(component) {
    return b2`
		<input class="radio-button__input"
			type="radio"
			.checked=${component.checked}
			?disabled=${component.disabled}
			?required=${component.required}
			name=${component.name || ""}
			value=${component.value}
			aria-label=${component.accessibleLabel || A}
			@change=${component._handleChange}
		>
		<div class="radio-button__outer-shape" aria-hidden="true">
			<div class="radio-button__inner-shape"></div>
		</div>
	`;
  }

  // node_modules/@nldd/design-system/dist/components/inputs/radio-button/radio-button.js
  var __decorate36 = function(decorators, target, key, desc) {
    var c6 = arguments.length, r6 = c6 < 3 ? target : desc === null ? desc = Object.getOwnPropertyDescriptor(target, key) : desc, d3;
    if (typeof Reflect === "object" && typeof Reflect.decorate === "function") r6 = Reflect.decorate(decorators, target, key, desc);
    else for (var i8 = decorators.length - 1; i8 >= 0; i8--) if (d3 = decorators[i8]) r6 = (c6 < 3 ? d3(r6) : c6 > 3 ? d3(target, key, r6) : d3(target, key)) || r6;
    return c6 > 3 && r6 && Object.defineProperty(target, key, r6), r6;
  };
  var NLDDRadioButton = class NLDDRadioButton2 extends i4 {
    constructor() {
      super(...arguments);
      this._internals = this.attachInternals();
      this._initialChecked = false;
      this.checked = false;
      this.disabled = false;
      this.required = false;
      this.name = "";
      this.value = "";
      this.accessibleLabel = "";
    }
    firstUpdated() {
      this._initialChecked = this.checked;
    }
    updated(changed) {
      if (changed.has("checked") || changed.has("value")) {
        this._internals.setFormValue(this.checked ? this.value : null);
      }
    }
    formResetCallback() {
      this.checked = this._initialChecked;
    }
    formDisabledCallback(disabled) {
      this.disabled = disabled;
    }
    formStateRestoreCallback(state) {
      this.checked = state !== null;
    }
    select() {
      if (this.disabled || this.checked)
        return;
      const input = this.shadowRoot?.querySelector("input");
      if (!input)
        return;
      input.checked = true;
      input.dispatchEvent(new Event("change", { bubbles: true }));
    }
    _handleChange(e9) {
      const input = e9.target;
      this.checked = input.checked;
      this.dispatchEvent(new CustomEvent("change", {
        detail: { checked: this.checked, value: this.value, name: this.name },
        bubbles: true,
        composed: true
      }));
    }
    render() {
      return radioButtonTemplate(this);
    }
  };
  NLDDRadioButton.formAssociated = true;
  NLDDRadioButton.styles = radioButtonStyles;
  __decorate36([
    n4({ type: Boolean, reflect: true })
  ], NLDDRadioButton.prototype, "checked", void 0);
  __decorate36([
    n4({ type: Boolean, reflect: true })
  ], NLDDRadioButton.prototype, "disabled", void 0);
  __decorate36([
    n4({ type: Boolean, reflect: true })
  ], NLDDRadioButton.prototype, "required", void 0);
  __decorate36([
    n4({ type: String, reflect: true })
  ], NLDDRadioButton.prototype, "name", void 0);
  __decorate36([
    n4({ type: String })
  ], NLDDRadioButton.prototype, "value", void 0);
  __decorate36([
    n4({ type: String, attribute: "accessible-label" })
  ], NLDDRadioButton.prototype, "accessibleLabel", void 0);
  NLDDRadioButton = __decorate36([
    t3("nldd-radio-button")
  ], NLDDRadioButton);

  // node_modules/@nldd/design-system/dist/components/inputs/radio-button-field/radio-button-field.js
  init_lit();
  init_decorators();

  // node_modules/@nldd/design-system/dist/components/inputs/radio-button-field/radio-button-field.styles.js
  init_lit();
  init_slotted_reset();
  var radioButtonFieldStyles = i`


	/* # Host */

	:host {
		${inheritedTextReset}
		display: block;
	}

	:host([hidden]) {
		display: none;
	}


	/* # Block */

	.radio-button-field {
		display: flex;
		min-height: var(--semantics-controls-md-min-size);
		flex-direction: row;
		gap: var(--primitives-space-8);
		align-items: flex-start;
	}


	/* # Elements */

	.radio-button-field__control {
		display: flex;
		min-height: var(--semantics-controls-md-min-size);
		flex-shrink: 0;
		align-items: center;
	}

	.radio-button-field__label {
		display: flex;
		cursor: default;
		padding-top: calc((var(--semantics-controls-md-min-size) - 1em * var(--primitives-line-height-snug)) / 2);
		flex-grow: 1;
		color: var(--semantics-content-color);
		font: var(--primitives-font-body-md-regular-snug);
	}

	:host([disabled]) .radio-button-field__label {
		opacity: var(--primitives-opacity-disabled);
	}
`;

  // node_modules/@nldd/design-system/dist/components/inputs/radio-button-field/radio-button-field.template.js
  init_lit();
  function radioButtonFieldTemplate(component) {
    return b2`
		<div class="radio-button-field"
			@click=${component._handleLabelClick}
		>
			<div class="radio-button-field__control">
				<nldd-radio-button
					?checked=${component.checked}
					?disabled=${component.disabled}
					?required=${component.required}
					name=${component.name || ""}
					value=${component.value}
					accessible-label=${component.label || A}
					@change=${component._handleChange}
				></nldd-radio-button>
			</div>
			<span class="radio-button-field__label">${component.label}</span>
		</div>
	`;
  }

  // node_modules/@nldd/design-system/dist/components/inputs/radio-button-field/radio-button-field.js
  var __decorate37 = function(decorators, target, key, desc) {
    var c6 = arguments.length, r6 = c6 < 3 ? target : desc === null ? desc = Object.getOwnPropertyDescriptor(target, key) : desc, d3;
    if (typeof Reflect === "object" && typeof Reflect.decorate === "function") r6 = Reflect.decorate(decorators, target, key, desc);
    else for (var i8 = decorators.length - 1; i8 >= 0; i8--) if (d3 = decorators[i8]) r6 = (c6 < 3 ? d3(r6) : c6 > 3 ? d3(target, key, r6) : d3(target, key)) || r6;
    return c6 > 3 && r6 && Object.defineProperty(target, key, r6), r6;
  };
  var NLDDRadioButtonField = class NLDDRadioButtonField2 extends i4 {
    constructor() {
      super(...arguments);
      this.checked = false;
      this.disabled = false;
      this.value = "";
      this.name = "";
      this.required = false;
      this.label = "";
    }
    _handleLabelClick(e9) {
      if (this.disabled)
        return;
      if (e9.target.closest?.("nldd-radio-button"))
        return;
      const radioButton = this.shadowRoot?.querySelector("nldd-radio-button");
      radioButton?.select();
    }
    _handleChange(e9) {
      this.checked = e9.detail.checked;
      this.dispatchEvent(new CustomEvent("change", {
        detail: { checked: this.checked, value: this.value },
        bubbles: true,
        composed: true
      }));
    }
    render() {
      return radioButtonFieldTemplate(this);
    }
  };
  NLDDRadioButtonField.styles = radioButtonFieldStyles;
  __decorate37([
    n4({ type: Boolean, reflect: true })
  ], NLDDRadioButtonField.prototype, "checked", void 0);
  __decorate37([
    n4({ type: Boolean, reflect: true })
  ], NLDDRadioButtonField.prototype, "disabled", void 0);
  __decorate37([
    n4({ type: String })
  ], NLDDRadioButtonField.prototype, "value", void 0);
  __decorate37([
    n4({ type: String })
  ], NLDDRadioButtonField.prototype, "name", void 0);
  __decorate37([
    n4({ type: Boolean, reflect: true })
  ], NLDDRadioButtonField.prototype, "required", void 0);
  __decorate37([
    n4({ type: String })
  ], NLDDRadioButtonField.prototype, "label", void 0);
  NLDDRadioButtonField = __decorate37([
    t3("nldd-radio-button-field")
  ], NLDDRadioButtonField);

  // node_modules/@nldd/design-system/dist/components/inputs/radio-button-group/radio-button-group.js
  init_lit();
  init_decorators();

  // node_modules/@nldd/design-system/dist/components/inputs/radio-button-group/radio-button-group.styles.js
  init_lit();
  init_slotted_reset();
  var radioButtonGroupStyles = i`


	/* # Host */

	:host {
		${inheritedTextReset}
		display: block;
	}

	:host([hidden]) {
		display: none;
	}


	/* # Block */

	.radio-button-group {
		display: flex;
		flex-direction: column;
	}
`;

  // node_modules/@nldd/design-system/dist/components/inputs/radio-button-group/radio-button-group.template.js
  init_lit();
  function radioButtonGroupTemplate(component) {
    return b2`
		<div class="radio-button-group">
			<slot @slotchange=${component._onSlotChange}></slot>
		</div>
	`;
  }

  // node_modules/@nldd/design-system/dist/components/inputs/radio-button-group/radio-button-group.js
  var __decorate38 = function(decorators, target, key, desc) {
    var c6 = arguments.length, r6 = c6 < 3 ? target : desc === null ? desc = Object.getOwnPropertyDescriptor(target, key) : desc, d3;
    if (typeof Reflect === "object" && typeof Reflect.decorate === "function") r6 = Reflect.decorate(decorators, target, key, desc);
    else for (var i8 = decorators.length - 1; i8 >= 0; i8--) if (d3 = decorators[i8]) r6 = (c6 < 3 ? d3(r6) : c6 > 3 ? d3(target, key, r6) : d3(target, key)) || r6;
    return c6 > 3 && r6 && Object.defineProperty(target, key, r6), r6;
  };
  var NLDDRadioButtonGroup = class NLDDRadioButtonGroup2 extends i4 {
    constructor() {
      super(...arguments);
      this.name = "";
      this.disabled = false;
      this.required = false;
      this.accessibleLabelledBy = "";
      this._handleChange = (e9) => {
        const changedField = e9.target;
        if (!changedField.checked)
          return;
        this._getFields().forEach((field) => {
          if (field !== changedField)
            field.checked = false;
        });
      };
      this._handleKeyDown = (e9) => {
        if (e9.key !== "ArrowDown" && e9.key !== "ArrowUp" && e9.key !== "ArrowRight" && e9.key !== "ArrowLeft")
          return;
        const fields = this._getEnabledFields();
        if (fields.length === 0)
          return;
        const activeField = fields.find((f3) => f3.checked) ?? fields[0];
        const currentIndex = fields.indexOf(activeField);
        const isNext = e9.key === "ArrowDown" || e9.key === "ArrowRight";
        const nextIndex = isNext ? (currentIndex + 1) % fields.length : (currentIndex - 1 + fields.length) % fields.length;
        const nextField = fields[nextIndex];
        if (!nextField)
          return;
        e9.preventDefault();
        activeField.checked = false;
        nextField.checked = true;
        const input = nextField.shadowRoot?.querySelector("nldd-radio-button")?.shadowRoot?.querySelector("input");
        input?.focus();
        nextField.dispatchEvent(new CustomEvent("change", {
          detail: { checked: true, value: nextField.value },
          bubbles: true,
          composed: true
        }));
      };
      this._onSlotChange = () => {
        this._syncFields();
      };
    }
    connectedCallback() {
      super.connectedCallback();
      this.setAttribute("role", "radiogroup");
      this.addEventListener("keydown", this._handleKeyDown);
      this.addEventListener("change", this._handleChange);
    }
    disconnectedCallback() {
      super.disconnectedCallback();
      this.removeEventListener("keydown", this._handleKeyDown);
      this.removeEventListener("change", this._handleChange);
    }
    updated(changed) {
      if (changed.has("name") || changed.has("disabled") || changed.has("required")) {
        this._syncFields();
      }
      if (changed.has("accessibleLabelledBy")) {
        if (this.accessibleLabelledBy) {
          this.setAttribute("aria-labelledby", this.accessibleLabelledBy);
        } else {
          this.removeAttribute("aria-labelledby");
        }
      }
      if (changed.has("required")) {
        if (this.required) {
          this.setAttribute("aria-required", "true");
        } else {
          this.removeAttribute("aria-required");
        }
      }
    }
    _getFields() {
      return Array.from(this.querySelectorAll("nldd-radio-button-field"));
    }
    _getEnabledFields() {
      return this._getFields().filter((f3) => !f3.disabled);
    }
    _syncFields() {
      this._getFields().forEach((field) => {
        field.name = this.name;
        field.required = this.required;
        if (this.disabled) {
          if (!field.hasAttribute("disabled")) {
            field.setAttribute("group-disabled", "");
            field.disabled = true;
          }
        } else if (field.hasAttribute("group-disabled")) {
          field.removeAttribute("group-disabled");
          field.disabled = false;
        }
      });
    }
    render() {
      return radioButtonGroupTemplate(this);
    }
  };
  NLDDRadioButtonGroup.styles = radioButtonGroupStyles;
  __decorate38([
    n4({ type: String })
  ], NLDDRadioButtonGroup.prototype, "name", void 0);
  __decorate38([
    n4({ type: Boolean, reflect: true })
  ], NLDDRadioButtonGroup.prototype, "disabled", void 0);
  __decorate38([
    n4({ type: Boolean, reflect: true })
  ], NLDDRadioButtonGroup.prototype, "required", void 0);
  __decorate38([
    n4({ type: String, attribute: "accessible-labelledby" })
  ], NLDDRadioButtonGroup.prototype, "accessibleLabelledBy", void 0);
  NLDDRadioButtonGroup = __decorate38([
    t3("nldd-radio-button-group")
  ], NLDDRadioButtonGroup);

  // node_modules/@nldd/design-system/dist/components/inputs/switch/switch.js
  init_lit();
  init_decorators();

  // node_modules/@nldd/design-system/dist/components/inputs/switch/switch.styles.js
  init_lit();
  var switchStyles = i`


	/* # Host */

	:host {
		--_track-width: var(--semantics-controls-lg-min-size);
		--_track-height: var(--semantics-controls-sm-min-size);
		--_padding: var(--primitives-space-2);
		--_transition-duration: var(--primitives-transition-duration-fast);
		--_thumb-size: calc(var(--_track-height) - var(--_padding) * 2 - var(--components-switch-thumb-border-thickness) * 2);

		display: inline-block;
		position: relative;
		width: var(--_track-width);
		height: var(--_track-height);
		flex-shrink: 0;
		user-select: none;
		-webkit-tap-highlight-color: transparent;
	}

	:host([hidden]) {
		display: none;
	}

	:host([size="xs"]) {
		--_track-width: var(--semantics-controls-md-min-size);
		--_track-height: var(--semantics-controls-xs-min-size);
	}

	:host([disabled]) {
		opacity: var(--primitives-opacity-disabled);
	}


	/* # Elements */

	.switch__input {
		position: absolute;
		inset: 0;
		opacity: 0;
		z-index: 1;
		margin: 0;
		width: 100%;
		height: 100%;
	}

	.switch__track {
		box-sizing: border-box;
		display: flex;
		position: relative;
		border: var(--components-switch-border-thickness) solid var(--components-switch-border-color);
		border-radius: calc(var(--_track-height) / 2);
		background-color: var(--components-switch-background-color);
		width: 100%;
		height: 100%;
		padding: var(--_padding);
		align-items: center;
		transition: background-color var(--_transition-duration) ease, border-color var(--_transition-duration) ease;
	}

	.switch__input:checked ~ .switch__track {
		border-color: var(--components-switch-is-selected-background-color);
		background-color: var(--components-switch-is-selected-background-color);
	}

	.switch__input:focus-visible ~ .switch__track {
		outline: var(--semantics-focus-ring-outline);
		outline-offset: var(--semantics-focus-ring-outline-offset);
		box-shadow: var(--semantics-focus-ring-box-shadow);
	}

	.switch__thumb {
		box-sizing: border-box;
		position: absolute;
		left: var(--_padding);
		border: var(--components-switch-thumb-border-thickness) solid var(--components-switch-thumb-border-color);
		border-radius: 50%;
		background-color: var(--components-switch-thumb-background-color);
		width: var(--_thumb-size);
		height: var(--_thumb-size);
		transition: width var(--_transition-duration) ease, height var(--_transition-duration) ease, left var(--_transition-duration) ease, background-color var(--_transition-duration) ease, border-color var(--_transition-duration) ease;
		will-change: width, height, left;
	}

	.switch__input:checked ~ .switch__track .switch__thumb {
		left: calc(var(--_track-width) - var(--components-switch-thumb-border-thickness) * 2 - var(--_thumb-size) - var(--_padding) * 2);
		border-color: var(--components-switch-is-selected-thumb-background-color);
		background-color: var(--components-switch-is-selected-thumb-background-color);
		width: calc(var(--_thumb-size) + var(--_padding) * 2);
		height: calc(var(--_thumb-size) + var(--_padding) * 2);
	}

	.switch__check {
		display: flex;
		position: absolute;
		top: 50%;
		left: 50%;
		opacity: 0;
		pointer-events: none;
		width: calc(100% + var(--components-switch-thumb-border-thickness) * 2);
		height: calc(100% + var(--components-switch-thumb-border-thickness) * 2);
		align-items: center;
		justify-content: center;
		color: var(--components-switch-is-selected-background-color);
		transform: translate(-50%, -50%);
		transition: opacity var(--_transition-duration) ease;
	}

	.switch__input:checked ~ .switch__track .switch__check {
		opacity: 1;
	}
`;

  // node_modules/@nldd/design-system/dist/components/inputs/switch/switch.template.js
  init_lit();
  init_icon();
  function switchTemplate(component) {
    return b2`
		<input class="switch__input"
			type="checkbox"
			role="switch"
			.checked=${component.checked}
			aria-checked=${component.checked}
			?disabled=${component.disabled}
			value=${component.value}
			aria-label=${component.accessibleLabel || A}
			@change=${component._handleChange}
			@pointerdown=${component._handlePointerDown}
			@pointermove=${component._handlePointerMove}
			@pointerup=${component._handlePointerUp}
			@pointercancel=${component._handlePointerCancel}
			@click=${component._handleClick}
		>
		<div class="switch__track"
			aria-hidden="true"
		>
			<div class="switch__thumb">
				<div class="switch__check">
					<nldd-icon name="check-mark-small"></nldd-icon>
				</div>
			</div>
		</div>
	`;
  }

  // node_modules/@nldd/design-system/dist/components/inputs/switch/switch.js
  var import_meta12 = {};
  var __decorate39 = function(decorators, target, key, desc) {
    var c6 = arguments.length, r6 = c6 < 3 ? target : desc === null ? desc = Object.getOwnPropertyDescriptor(target, key) : desc, d3;
    if (typeof Reflect === "object" && typeof Reflect.decorate === "function") r6 = Reflect.decorate(decorators, target, key, desc);
    else for (var i8 = decorators.length - 1; i8 >= 0; i8--) if (d3 = decorators[i8]) r6 = (c6 < 3 ? d3(r6) : c6 > 3 ? d3(target, key, r6) : d3(target, key)) || r6;
    return c6 > 3 && r6 && Object.defineProperty(target, key, r6), r6;
  };
  var NLDDSwitch_1;
  var NLDDSwitch = NLDDSwitch_1 = class NLDDSwitch2 extends i4 {
    constructor() {
      super(...arguments);
      this._internals = this.attachInternals();
      this.name = "";
      this.checked = false;
      this.disabled = false;
      this.size = "sm";
      this.accessibleLabel = "";
      this.value = "on";
      this._initialChecked = false;
      this._pointerStartX = null;
      this._swiped = false;
    }
    firstUpdated() {
      if (import_meta12.env?.DEV && !this.accessibleLabel) {
        console.warn("<nldd-switch>: No accessible-label provided. Use nldd-switch-field for labeled usage, or provide an accessible-label attribute for screen reader accessibility.");
      }
      this._initialChecked = this.checked;
    }
    updated(changed) {
      if (changed.has("checked") || changed.has("value")) {
        this._internals.setFormValue(this.checked ? this.value : null);
      }
    }
    formResetCallback() {
      this.checked = this._initialChecked;
    }
    formDisabledCallback(disabled) {
      this.disabled = disabled;
    }
    formStateRestoreCallback(state) {
      this.checked = state !== null;
    }
    _handlePointerDown(e9) {
      this._pointerStartX = e9.clientX;
      this._swiped = false;
      try {
        e9.target.setPointerCapture(e9.pointerId);
      } catch {
      }
    }
    _handlePointerMove(e9) {
      if (this._pointerStartX === null)
        return;
      const dx = e9.clientX - this._pointerStartX;
      if (Math.abs(dx) >= NLDDSwitch_1.SWIPE_THRESHOLD) {
        this._swiped = true;
      }
    }
    _handlePointerUp(e9) {
      if (this._pointerStartX === null)
        return;
      const dx = e9.clientX - this._pointerStartX;
      this._pointerStartX = null;
      if (!this._swiped)
        return;
      e9.preventDefault();
      const isRtl = this.closest("[dir]")?.getAttribute("dir") === "rtl";
      const shouldCheck = isRtl ? dx < 0 : dx > 0;
      if (shouldCheck !== this.checked) {
        this.toggle();
      }
    }
    _handlePointerCancel() {
      this._pointerStartX = null;
      this._swiped = false;
    }
    _handleClick(e9) {
      if (this._swiped) {
        e9.preventDefault();
        this._swiped = false;
      }
    }
    // ## Public API
    toggle() {
      if (this.disabled)
        return;
      this.checked = !this.checked;
      this.dispatchEvent(new CustomEvent("change", {
        detail: { checked: this.checked, value: this.value },
        bubbles: true,
        composed: true
      }));
    }
    _handleChange(e9) {
      if (this.disabled)
        return;
      const input = e9.target;
      this.checked = input.checked;
      this.dispatchEvent(new CustomEvent("change", {
        detail: { checked: this.checked, value: this.value },
        bubbles: true,
        composed: true
      }));
    }
    /**
     * Delegates focus to the inner native checkbox `<input>`, so consumers can
     * call `switchEl.focus()` without reaching into shadow DOM.
     */
    focus(options) {
      this.shadowRoot?.querySelector(".switch__input")?.focus(options);
    }
    render() {
      return switchTemplate(this);
    }
  };
  NLDDSwitch.formAssociated = true;
  NLDDSwitch.styles = switchStyles;
  NLDDSwitch.SWIPE_THRESHOLD = 10;
  __decorate39([
    n4({ type: String, reflect: true })
  ], NLDDSwitch.prototype, "name", void 0);
  __decorate39([
    n4({ type: Boolean, reflect: true })
  ], NLDDSwitch.prototype, "checked", void 0);
  __decorate39([
    n4({ type: Boolean, reflect: true })
  ], NLDDSwitch.prototype, "disabled", void 0);
  __decorate39([
    n4({ type: String, reflect: true })
  ], NLDDSwitch.prototype, "size", void 0);
  __decorate39([
    n4({ type: String, attribute: "accessible-label" })
  ], NLDDSwitch.prototype, "accessibleLabel", void 0);
  __decorate39([
    n4({ type: String })
  ], NLDDSwitch.prototype, "value", void 0);
  NLDDSwitch = NLDDSwitch_1 = __decorate39([
    t3("nldd-switch")
  ], NLDDSwitch);

  // node_modules/@nldd/design-system/dist/components/inputs/switch-field/switch-field.js
  init_lit();
  init_decorators();

  // node_modules/@nldd/design-system/dist/components/inputs/switch-field/switch-field.styles.js
  init_lit();
  init_slotted_reset();
  var switchFieldStyles = i`


	/* # Host */

	:host {
		${inheritedTextReset}
		display: block;
	}

	:host([hidden]) {
		display: none;
	}


	/* # Block */

	.switch-field {
		display: flex;
		min-height: var(--semantics-controls-md-min-size);
		flex-direction: row;
		gap: var(--primitives-space-8);
		align-items: flex-start;
	}


	/* # Elements */

	.switch-field__control {
		display: flex;
		min-height: var(--semantics-controls-md-min-size);
		flex-shrink: 0;
		align-items: center;
	}

	.switch-field__label {
		display: flex;
		cursor: default;
		padding-top: calc((var(--semantics-controls-md-min-size) - 1em * var(--primitives-line-height-snug)) / 2);
		flex-grow: 1;
		color: var(--semantics-content-color);
		font: var(--primitives-font-body-md-regular-snug);
	}

	:host([disabled]) .switch-field__label {
		opacity: var(--primitives-opacity-disabled);
	}
`;

  // node_modules/@nldd/design-system/dist/components/inputs/switch-field/switch-field.template.js
  init_lit();
  function switchFieldTemplate(component) {
    return b2`
		<div class="switch-field"
			@click=${component._handleLabelClick}
		>
			<div class="switch-field__control">
				<nldd-switch
					size="sm"
					name=${component.name || A}
					value=${component.value}
					?checked=${component.checked}
					?disabled=${component.disabled}
					accessible-label=${component.label || A}
					@change=${component._handleChange}
				></nldd-switch>
			</div>
			<span class="switch-field__label">${component.label}</span>
		</div>
	`;
  }

  // node_modules/@nldd/design-system/dist/components/inputs/switch-field/switch-field.js
  var __decorate40 = function(decorators, target, key, desc) {
    var c6 = arguments.length, r6 = c6 < 3 ? target : desc === null ? desc = Object.getOwnPropertyDescriptor(target, key) : desc, d3;
    if (typeof Reflect === "object" && typeof Reflect.decorate === "function") r6 = Reflect.decorate(decorators, target, key, desc);
    else for (var i8 = decorators.length - 1; i8 >= 0; i8--) if (d3 = decorators[i8]) r6 = (c6 < 3 ? d3(r6) : c6 > 3 ? d3(target, key, r6) : d3(target, key)) || r6;
    return c6 > 3 && r6 && Object.defineProperty(target, key, r6), r6;
  };
  var NLDDSwitchField = class NLDDSwitchField2 extends i4 {
    constructor() {
      super(...arguments);
      this.checked = false;
      this.disabled = false;
      this.value = "on";
      this.name = "";
      this.label = "";
    }
    _handleLabelClick(e9) {
      if (this.disabled)
        return;
      if (e9.target.closest?.("nldd-switch"))
        return;
      const switchEl = this.shadowRoot?.querySelector("nldd-switch");
      switchEl?.toggle();
    }
    _handleChange(e9) {
      this.checked = e9.detail.checked;
      this.dispatchEvent(new CustomEvent("change", {
        detail: { checked: this.checked, value: this.value },
        bubbles: true,
        composed: true
      }));
    }
    /**
     * Delegates focus to the inner `<nldd-switch>`, which in turn focuses its
     * native checkbox input. Lets consumers call `switchFieldEl.focus()` without
     * reaching into shadow DOM.
     */
    focus(options) {
      this.shadowRoot?.querySelector("nldd-switch")?.focus(options);
    }
    render() {
      return switchFieldTemplate(this);
    }
  };
  NLDDSwitchField.styles = switchFieldStyles;
  __decorate40([
    n4({ type: Boolean, reflect: true })
  ], NLDDSwitchField.prototype, "checked", void 0);
  __decorate40([
    n4({ type: Boolean, reflect: true })
  ], NLDDSwitchField.prototype, "disabled", void 0);
  __decorate40([
    n4({ type: String })
  ], NLDDSwitchField.prototype, "value", void 0);
  __decorate40([
    n4({ type: String })
  ], NLDDSwitchField.prototype, "name", void 0);
  __decorate40([
    n4({ type: String })
  ], NLDDSwitchField.prototype, "label", void 0);
  NLDDSwitchField = __decorate40([
    t3("nldd-switch-field")
  ], NLDDSwitchField);

  // node_modules/@nldd/design-system/dist/components/inputs/segmented-control/segmented-control.js
  init_lit();
  init_decorators();

  // node_modules/@nldd/design-system/dist/components/inputs/segmented-control/segmented-control.styles.js
  init_lit();
  init_slotted_reset();
  var segmentedControlStyles = i`


	/* # Host */

	:host {
		--_corner-radius: var(--semantics-controls-md-corner-radius);
		--_width: auto;
		--_gap: var(--primitives-space-1);
		--_z-index-selected: 1;
		--_z-index-focus: 2;

		${inheritedTextReset}
		display: inline-grid;
		position: relative;
		border-radius: var(--_corner-radius);
		background-color: var(--semantics-buttons-neutral-tinted-background-color);
		width: var(--_width);
		max-width: 100%;
		grid-auto-columns: 1fr;
		grid-auto-flow: column;
		gap: var(--_gap);
		isolation: isolate;
		user-select: none;
		-webkit-tap-highlight-color: transparent;
	}

	:host::after {
		content: '';
		position: absolute;
		inset: 0;
		border-radius: inherit;
		box-shadow: inset 0 0 0 var(--primitives-border-width-thin) var(--semantics-buttons-neutral-tinted-highlight-border-color);
		pointer-events: none;
	}

	:host([hidden]) {
		display: none;
	}

	:host([size="sm"]) {
		--_corner-radius: var(--semantics-controls-sm-corner-radius);
	}

	:host([size="lg"]) {
		--_corner-radius: var(--semantics-controls-lg-corner-radius);
	}

	:host([width="full"]) {
		--_width: 100%;

		display: grid;
	}

	:host([width="fit-content"]) {
		grid-auto-columns: auto;
	}

	:host([disabled]) {
		opacity: var(--primitives-opacity-disabled);
		pointer-events: none;
	}


	/* # Elements */

	::slotted(nldd-segmented-control-item[selected]) {
		position: relative;
		z-index: var(--_z-index-selected);
	}

	::slotted(nldd-segmented-control-item:focus-within) {
		position: relative;
		z-index: var(--_z-index-focus);
	}
`;
  var segmentedControlItemStyles = i`


	/* # Host */

	:host {
		--_corner-radius: var(--semantics-controls-md-corner-radius);
		--_min-size: var(--semantics-controls-md-min-size);
		--_padding-inline: var(--primitives-space-12);
		--_gap: var(--semantics-buttons-md-gap);
		--_font: var(--semantics-buttons-md-primary-text-font);
		--_icon-size: var(--semantics-buttons-md-icon-size);
		--_icon-only-icon-size: var(--semantics-buttons-md-icon-only-icon-size);
		--_highlight-border-color: transparent;

		${inheritedTextReset}
		display: flex;
		position: relative;
		min-width: 0;
		-webkit-tap-highlight-color: transparent;
	}

	:host([hidden]) {
		display: none;
	}

	:host([size="sm"]) {
		--_corner-radius: var(--semantics-controls-sm-corner-radius);
		--_min-size: var(--semantics-controls-sm-min-size);
		--_padding-inline: var(--primitives-space-8);
		--_gap: var(--semantics-buttons-sm-gap);
		--_font: var(--semantics-buttons-sm-primary-text-font);
		--_icon-size: var(--semantics-buttons-sm-icon-size);
		--_icon-only-icon-size: var(--semantics-buttons-sm-icon-only-icon-size);
	}

	:host([size="lg"]) {
		--_corner-radius: var(--semantics-controls-lg-corner-radius);
		--_min-size: var(--semantics-controls-lg-min-size);
		--_padding-inline: var(--primitives-space-16);
		--_gap: var(--semantics-buttons-lg-gap);
		--_font: var(--semantics-buttons-lg-primary-text-font);
		--_icon-size: var(--semantics-buttons-lg-icon-size);
		--_icon-only-icon-size: var(--primitives-space-28);
	}

	:host([size="lg"][variant="icon-and-text"]) .segmented-control__item {
		flex-direction: column;
		gap: var(--primitives-space-2);
		padding-inline: var(--primitives-space-8);
		padding-block: var(--primitives-space-8);
	}

	:host([size="lg"][variant="icon-and-text"]) .segmented-control__item-text {
		font: var(--primitives-font-body-xxs-medium-flat);
	}


	/* # Block */

	.segmented-control__item {
		box-sizing: border-box;
		display: flex;
		position: relative;
		cursor: default;
		border-radius: var(--_corner-radius);
		box-shadow: inset 0 0 0 var(--primitives-border-width-thin) var(--_highlight-border-color);
		width: 100%;
		min-width: var(--_min-size);
		height: var(--_min-size);
		padding-inline: var(--_padding-inline);
		gap: var(--_gap);
		align-items: center;
		justify-content: center;
		color: var(--semantics-buttons-neutral-tinted-primary-content-color);
		font: var(--_font);
	}

	:host([variant="icon"]) .segmented-control__item {
		padding: 0;
	}

	:host([selected]) .segmented-control__item {
		--_highlight-border-color: var(--semantics-buttons-neutral-tinted-is-selected-highlight-border-color);
		background-color: var(--semantics-buttons-neutral-tinted-is-selected-background-color);
		color: var(--semantics-buttons-neutral-tinted-is-selected-primary-content-color);
	}

	@media (hover: hover) {
		:host(:not([selected])) .segmented-control__item:hover {
			background-color: var(--semantics-buttons-neutral-tinted-is-hovered-background-color);
		}

		:host([selected]) .segmented-control__item:hover {
			--_highlight-border-color: var(--semantics-buttons-neutral-tinted-is-selected-is-hovered-highlight-border-color);
		}
	}

	:host(:not([selected])) .segmented-control__item:active {
		background-color: var(--semantics-buttons-neutral-tinted-is-active-background-color);
		color: var(--semantics-buttons-neutral-tinted-is-active-primary-content-color);
	}

	:host([selected]) .segmented-control__item:active {
		--_highlight-border-color: var(--semantics-buttons-neutral-tinted-is-selected-is-active-highlight-border-color);
		background-color: var(--semantics-buttons-neutral-tinted-is-selected-is-active-background-color);
		color: var(--semantics-buttons-neutral-tinted-is-selected-is-active-primary-content-color);
	}

	:host([disabled]) .segmented-control__item {
		opacity: var(--primitives-opacity-disabled);
	}

	.segmented-control__item:has(:focus-visible) {
		outline: var(--semantics-focus-ring-outline);
		outline-offset: var(--semantics-focus-ring-outline-offset);
		box-shadow: var(--semantics-focus-ring-box-shadow), inset 0 0 0 var(--primitives-border-width-thin) var(--_highlight-border-color);
	}


	/* # Elements */

	.segmented-control__item-input {
		position: absolute;
		inset: 0;
		opacity: 0;
		z-index: 1;
		margin: 0;
		width: 100%;
		height: 100%;
	}

	.segmented-control__item-text {
		pointer-events: none;
		overflow: hidden;
		white-space: nowrap;
		text-overflow: ellipsis;
	}

	:host([variant="icon"]) .segmented-control__item-text {
		display: none;
	}

	.segmented-control__item-icon {
		display: flex;
		pointer-events: none;
		width: var(--_icon-size);
		height: var(--_icon-size);
		flex-shrink: 0;
		align-items: center;
		justify-content: center;
	}

	:host([variant="icon"]) .segmented-control__item-icon {
		width: var(--_icon-only-icon-size);
		height: var(--_icon-only-icon-size);
	}

	:host([variant="text"]) .segmented-control__item-icon {
		display: none;
	}

	::slotted(nldd-icon) {
		display: block;
		width: 100%;
		height: 100%;
	}
`;

  // node_modules/@nldd/design-system/dist/components/inputs/segmented-control/segmented-control.template.js
  init_lit();
  init_tooltip();
  function segmentedControlTemplate(component) {
    return b2`<slot @slotchange=${component._onSlotChange}></slot>`;
  }
  function segmentedControlItemTemplate(component) {
    const isIcon = component.variant === "icon";
    const showPlaceholder = isIcon || component.variant === "icon-and-text";
    const labelText = component.text || A;
    const label = b2`
		<label class="segmented-control__item">
			<input class="segmented-control__item-input"
				type=${component.inputType}
				name=${component.groupName || A}
				value=${component.value}
				.checked=${component.selected}
				?disabled=${component.disabled}
				aria-label=${isIcon ? labelText : A}
				@change=${component._handleChange}
			>
			<span class="segmented-control__item-icon"
				aria-hidden=${isIcon ? A : "true"}
			>
				${component.icon ? b2`<nldd-icon name=${component.icon}></nldd-icon>` : b2`<slot name="icon">${showPlaceholder ? b2`<nldd-icon name="icon-placeholder"></nldd-icon>` : A}</slot>`}
			</span>
			<span class="segmented-control__item-text"
				aria-hidden=${isIcon ? "true" : A}
			>
				${component.text}
			</span>
		</label>`;
    if (isIcon && labelText) {
      return b2`<nldd-tooltip text=${labelText}>${label}</nldd-tooltip>`;
    }
    return label;
  }

  // node_modules/@nldd/design-system/dist/components/inputs/segmented-control/segmented-control.js
  init_icon();
  var import_meta13 = {};
  var __decorate41 = function(decorators, target, key, desc) {
    var c6 = arguments.length, r6 = c6 < 3 ? target : desc === null ? desc = Object.getOwnPropertyDescriptor(target, key) : desc, d3;
    if (typeof Reflect === "object" && typeof Reflect.decorate === "function") r6 = Reflect.decorate(decorators, target, key, desc);
    else for (var i8 = decorators.length - 1; i8 >= 0; i8--) if (d3 = decorators[i8]) r6 = (c6 < 3 ? d3(r6) : c6 > 3 ? d3(target, key, r6) : d3(target, key)) || r6;
    return c6 > 3 && r6 && Object.defineProperty(target, key, r6), r6;
  };
  var NLDDSegmentedControl_1;
  var NLDDSegmentedControlItem = class NLDDSegmentedControlItem2 extends i4 {
    constructor() {
      super(...arguments);
      this.value = "";
      this.selected = false;
      this.disabled = false;
      this.size = "md";
      this.variant = "text";
      this.inputType = "radio";
      this.groupName = "";
      this.text = "";
      this.icon = "";
    }
    _handleChange(e9) {
      const input = e9.target;
      this.dispatchEvent(new CustomEvent("item-change", {
        detail: { value: this.value, checked: input.checked },
        bubbles: true,
        composed: true
      }));
    }
    render() {
      return segmentedControlItemTemplate(this);
    }
  };
  NLDDSegmentedControlItem.styles = segmentedControlItemStyles;
  __decorate41([
    n4({ type: String })
  ], NLDDSegmentedControlItem.prototype, "value", void 0);
  __decorate41([
    n4({ type: Boolean, reflect: true })
  ], NLDDSegmentedControlItem.prototype, "selected", void 0);
  __decorate41([
    n4({ type: Boolean, reflect: true })
  ], NLDDSegmentedControlItem.prototype, "disabled", void 0);
  __decorate41([
    n4({ type: String, reflect: true })
  ], NLDDSegmentedControlItem.prototype, "size", void 0);
  __decorate41([
    n4({ type: String, reflect: true, attribute: "variant" })
  ], NLDDSegmentedControlItem.prototype, "variant", void 0);
  __decorate41([
    n4({ type: String, reflect: true, attribute: "input-type" })
  ], NLDDSegmentedControlItem.prototype, "inputType", void 0);
  __decorate41([
    n4({ type: String })
  ], NLDDSegmentedControlItem.prototype, "groupName", void 0);
  __decorate41([
    n4({ type: String })
  ], NLDDSegmentedControlItem.prototype, "text", void 0);
  __decorate41([
    n4({ type: String })
  ], NLDDSegmentedControlItem.prototype, "icon", void 0);
  NLDDSegmentedControlItem = __decorate41([
    t3("nldd-segmented-control-item")
  ], NLDDSegmentedControlItem);
  var NLDDSegmentedControl = NLDDSegmentedControl_1 = class NLDDSegmentedControl2 extends i4 {
    constructor() {
      super(...arguments);
      this._internals = this.attachInternals();
      this._initialValue = "";
      this._initialValues = [];
      this.value = "";
      this.values = [];
      this.size = "md";
      this.type = "radio";
      this.variant = "text";
      this.disabled = false;
      this.width = "";
      this.name = "";
      this.accessibleLabel = "";
      this.accessibleLabelledBy = "";
      this._generatedName = "";
      this._handleItemChange = (e9) => {
        e9.stopPropagation();
        if (this.disabled)
          return;
        if (this.type === "checkbox") {
          const current = this.values;
          const updated = e9.detail.checked ? [...current, e9.detail.value] : current.filter((v3) => v3 !== e9.detail.value);
          this.values = updated;
          this._syncItems();
          this.dispatchEvent(new CustomEvent("change", {
            detail: { values: updated },
            bubbles: true,
            composed: true
          }));
        } else {
          if (e9.detail.value === this.value)
            return;
          this.value = e9.detail.value;
          this._syncItems();
          this.dispatchEvent(new CustomEvent("change", {
            detail: { value: this.value },
            bubbles: true,
            composed: true
          }));
        }
      };
      this._handleKeydown = (e9) => {
        if (this.disabled || this.type === "checkbox")
          return;
        const items = this._getItems().filter((item) => !item.disabled);
        if (items.length === 0)
          return;
        const currentIndex = items.findIndex((item) => item.selected);
        let nextIndex = currentIndex;
        switch (e9.key) {
          case "ArrowLeft":
          case "ArrowUp":
            e9.preventDefault();
            nextIndex = currentIndex <= 0 ? items.length - 1 : currentIndex - 1;
            break;
          case "ArrowRight":
          case "ArrowDown":
            e9.preventDefault();
            nextIndex = currentIndex >= items.length - 1 ? 0 : currentIndex + 1;
            break;
          case "Home":
            e9.preventDefault();
            nextIndex = 0;
            break;
          case "End":
            e9.preventDefault();
            nextIndex = items.length - 1;
            break;
          default:
            return;
        }
        if (nextIndex !== currentIndex && items[nextIndex]) {
          this.value = items[nextIndex].value;
          this._syncItems();
          items[nextIndex].shadowRoot?.querySelector("input")?.focus();
          this.dispatchEvent(new CustomEvent("change", {
            detail: { value: this.value },
            bubbles: true,
            composed: true
          }));
        }
      };
    }
    // — Lifecycle ——————————————————————————————————————————————————————————————
    connectedCallback() {
      super.connectedCallback();
      this.setAttribute("role", this.type === "checkbox" ? "group" : "radiogroup");
      if (this.accessibleLabel)
        this.setAttribute("aria-label", this.accessibleLabel);
      if (this.accessibleLabelledBy)
        this.setAttribute("aria-labelledby", this.accessibleLabelledBy);
      this.addEventListener("item-change", this._handleItemChange);
      this.addEventListener("keydown", this._handleKeydown);
    }
    disconnectedCallback() {
      super.disconnectedCallback();
      this.removeEventListener("item-change", this._handleItemChange);
      this.removeEventListener("keydown", this._handleKeydown);
    }
    firstUpdated() {
      this._syncItems();
      this._initialValue = this.value;
      this._initialValues = [...this.values];
      if (import_meta13.env?.DEV && !this.accessibleLabel && !this.accessibleLabelledBy) {
        console.warn("<nldd-segmented-control>: No accessible name provided. Add an accessible-label or accessible-labelledby attribute for screen reader accessibility.");
      }
    }
    _syncFormValue() {
      if (this.type === "checkbox") {
        if (this.values.length === 0 || !this.name) {
          this._internals.setFormValue(null);
          return;
        }
        const data = new FormData();
        for (const v3 of this.values)
          data.append(this.name, v3);
        this._internals.setFormValue(data);
      } else {
        this._internals.setFormValue(this.value || null);
      }
    }
    formResetCallback() {
      this.value = this._initialValue;
      this.values = [...this._initialValues];
    }
    formDisabledCallback(disabled) {
      this.disabled = disabled;
    }
    formStateRestoreCallback(state) {
      if (state === null)
        return;
      if (this.type === "checkbox" && state instanceof FormData && this.name) {
        this.values = state.getAll(this.name).map(String);
      } else if (typeof state === "string") {
        this.value = state;
      }
    }
    updated(changedProperties) {
      if (changedProperties.has("width")) {
        const w2 = this.width;
        const isKeyword = w2 === "full" || w2 === "fit-content";
        if (w2 && !isKeyword && CSS.supports("width", w2)) {
          this.style.setProperty("--_width", w2);
        } else {
          this.style.removeProperty("--_width");
        }
      }
      if (changedProperties.has("value") || changedProperties.has("values") || changedProperties.has("size") || changedProperties.has("disabled") || changedProperties.has("type") || changedProperties.has("variant") || changedProperties.has("name")) {
        this._syncItems();
      }
      if (changedProperties.has("value") || changedProperties.has("values") || changedProperties.has("type") || changedProperties.has("name")) {
        this._syncFormValue();
      }
      if (changedProperties.has("type")) {
        this.setAttribute("role", this.type === "checkbox" ? "group" : "radiogroup");
      }
      if (changedProperties.has("accessibleLabel")) {
        if (this.accessibleLabel) {
          this.setAttribute("aria-label", this.accessibleLabel);
        } else {
          this.removeAttribute("aria-label");
        }
      }
      if (changedProperties.has("accessibleLabelledBy")) {
        if (this.accessibleLabelledBy) {
          this.setAttribute("aria-labelledby", this.accessibleLabelledBy);
        } else {
          this.removeAttribute("aria-labelledby");
        }
      }
    }
    // — Items ——————————————————————————————————————————————————————————————————
    _getItems() {
      const slot = this.shadowRoot?.querySelector("slot");
      if (!slot)
        return [];
      return slot.assignedElements().filter((el) => el.tagName.toLowerCase() === "nldd-segmented-control-item");
    }
    _getSelectedValues() {
      return this.type === "checkbox" ? this.values : this.value ? [this.value] : [];
    }
    _syncItems() {
      const items = this._getItems();
      const selectedValues = this._getSelectedValues();
      if (this.disabled) {
        items.forEach((item) => {
          if (!item.hasAttribute("disabled")) {
            item.setAttribute("group-disabled", "");
            item.disabled = true;
          }
        });
      } else {
        items.forEach((item) => {
          if (item.hasAttribute("group-disabled")) {
            item.removeAttribute("group-disabled");
            item.disabled = false;
          }
        });
      }
      items.forEach((item) => {
        item.size = this.size;
        item.inputType = this.type;
        item.variant = this.variant;
        item.groupName = this.name || this._autoName;
        item.selected = this.type === "checkbox" ? selectedValues.includes(item.value) : item.value === this.value;
      });
    }
    get _autoName() {
      if (!this._generatedName) {
        this._generatedName = `nldd-segmented-${NLDDSegmentedControl_1._idCounter++}`;
      }
      return this._generatedName;
    }
    _onSlotChange() {
      this._syncItems();
    }
    render() {
      return segmentedControlTemplate(this);
    }
  };
  NLDDSegmentedControl.formAssociated = true;
  NLDDSegmentedControl.styles = segmentedControlStyles;
  NLDDSegmentedControl._idCounter = 0;
  __decorate41([
    n4({ type: String, reflect: true })
  ], NLDDSegmentedControl.prototype, "value", void 0);
  __decorate41([
    n4({ type: Array, attribute: false })
  ], NLDDSegmentedControl.prototype, "values", void 0);
  __decorate41([
    n4({ type: String, reflect: true })
  ], NLDDSegmentedControl.prototype, "size", void 0);
  __decorate41([
    n4({ type: String, reflect: true })
  ], NLDDSegmentedControl.prototype, "type", void 0);
  __decorate41([
    n4({ type: String, reflect: true, attribute: "variant" })
  ], NLDDSegmentedControl.prototype, "variant", void 0);
  __decorate41([
    n4({ type: Boolean, reflect: true })
  ], NLDDSegmentedControl.prototype, "disabled", void 0);
  __decorate41([
    n4({ type: String, reflect: true })
  ], NLDDSegmentedControl.prototype, "width", void 0);
  __decorate41([
    n4({ type: String, reflect: true })
  ], NLDDSegmentedControl.prototype, "name", void 0);
  __decorate41([
    n4({ type: String, attribute: "accessible-label" })
  ], NLDDSegmentedControl.prototype, "accessibleLabel", void 0);
  __decorate41([
    n4({ type: String, attribute: "accessible-labelledby" })
  ], NLDDSegmentedControl.prototype, "accessibleLabelledBy", void 0);
  NLDDSegmentedControl = NLDDSegmentedControl_1 = __decorate41([
    t3("nldd-segmented-control")
  ], NLDDSegmentedControl);

  // node_modules/@nldd/design-system/dist/components/inputs/toggle-button/toggle-button.js
  init_lit();
  init_decorators();

  // node_modules/@nldd/design-system/dist/components/inputs/toggle-button/toggle-button.styles.js
  init_lit();
  init_slotted_reset();
  var toggleButtonStyles = i`


	/* # Host */

	:host {
		--_corner-radius: var(--semantics-controls-md-corner-radius);
		--_min-size: var(--semantics-controls-md-min-size);
		--_padding: var(--semantics-controls-md-block-padding) var(--semantics-controls-md-inline-padding);
		--_gap: var(--semantics-buttons-md-gap);
		--_font: var(--semantics-buttons-md-primary-text-font);
		--_icon-size: var(--semantics-buttons-md-icon-size);
		--_icon-only-icon-size: var(--semantics-buttons-md-icon-only-icon-size);
		--_highlight-border-color: var(--semantics-buttons-neutral-tinted-highlight-border-color);
		--_is-hovered-highlight-border-color: var(--semantics-buttons-neutral-tinted-is-hovered-highlight-border-color);
		--_is-active-highlight-border-color: var(--semantics-buttons-neutral-tinted-is-active-highlight-border-color);
		--_is-selected-highlight-border-color: var(--semantics-buttons-neutral-tinted-is-selected-highlight-border-color);
		--_is-selected-is-hovered-highlight-border-color: var(--semantics-buttons-neutral-tinted-is-selected-is-hovered-highlight-border-color);
		--_is-selected-is-active-highlight-border-color: var(--semantics-buttons-neutral-tinted-is-selected-is-active-highlight-border-color);

		${inheritedTextReset}
		display: inline-block;
		user-select: none;
		-webkit-tap-highlight-color: transparent;
	}

	:host([hidden]) {
		display: none;
	}

	:host([size="xs"]) {
		--_corner-radius: var(--semantics-controls-xs-corner-radius);
		--_min-size: var(--semantics-controls-xs-min-size);
		--_padding: var(--semantics-controls-xs-block-padding) var(--semantics-controls-xs-inline-padding);
		--_gap: var(--semantics-buttons-xs-gap);
		--_font: var(--semantics-buttons-xs-primary-text-font);
		--_icon-size: var(--semantics-buttons-xs-icon-size);
		--_icon-only-icon-size: var(--semantics-buttons-xs-icon-only-icon-size);
	}

	:host([size="sm"]) {
		--_corner-radius: var(--semantics-controls-sm-corner-radius);
		--_min-size: var(--semantics-controls-sm-min-size);
		--_padding: var(--semantics-controls-sm-block-padding) var(--semantics-controls-sm-inline-padding);
		--_gap: var(--semantics-buttons-sm-gap);
		--_font: var(--semantics-buttons-sm-primary-text-font);
		--_icon-size: var(--semantics-buttons-sm-icon-size);
		--_icon-only-icon-size: var(--semantics-buttons-sm-icon-only-icon-size);
	}

	:host([size="lg"]) {
		--_corner-radius: var(--semantics-controls-lg-corner-radius);
		--_min-size: var(--semantics-controls-lg-min-size);
		--_padding: var(--semantics-controls-lg-block-padding) var(--semantics-controls-lg-inline-padding);
		--_gap: var(--semantics-buttons-lg-gap);
		--_font: var(--semantics-buttons-lg-primary-text-font);
		--_icon-size: var(--semantics-buttons-lg-icon-size);
		--_icon-only-icon-size: var(--primitives-space-28);
		--_stacked-text-font: var(--primitives-font-body-xxs-medium-flat);
	}

	:host([disabled]) {
		opacity: var(--primitives-opacity-disabled);
		pointer-events: none;
	}


	/* # Block */

	.toggle-button {
		box-sizing: border-box;
		display: inline-flex;
		position: relative;
		margin: 0;
		border: none;
		border-radius: var(--_corner-radius);
		background: none;
		background-color: var(--semantics-buttons-neutral-tinted-background-color);
		box-shadow: inset 0 0 0 var(--primitives-border-width-thin) var(--_highlight-border-color);
		width: var(--_min-size);
		min-height: var(--_min-size);
		padding: 0;
		gap: var(--_gap);
		align-items: center;
		justify-content: center;
		color: var(--semantics-buttons-neutral-tinted-primary-content-color);
		font: var(--_font);
		white-space: nowrap;
		text-decoration: none;
		appearance: none;
	}

	.toggle-button:has(.toggle-button__text) {
		width: auto;
		padding: var(--_padding);
	}

	@media (hover: hover) {
		.toggle-button:hover,
		.toggle-button:has(.toggle-button__input:hover) {
			--_highlight-border-color: var(--_is-hovered-highlight-border-color);
			background-color: var(--semantics-buttons-neutral-tinted-is-hovered-background-color);
			color: var(--semantics-buttons-neutral-tinted-is-hovered-primary-content-color);
		}
	}

	.toggle-button:active,
	.toggle-button:has(.toggle-button__input:active) {
		--_highlight-border-color: var(--_is-active-highlight-border-color);
		background-color: var(--semantics-buttons-neutral-tinted-is-active-background-color);
		color: var(--semantics-buttons-neutral-tinted-is-active-primary-content-color);
	}

	:host([selected]) .toggle-button {
		--_highlight-border-color: var(--_is-selected-highlight-border-color);
		background-color: var(--semantics-buttons-neutral-tinted-is-selected-background-color);
		color: var(--semantics-buttons-neutral-tinted-is-selected-primary-content-color);
	}

	@media (hover: hover) {
		:host([selected]) .toggle-button:hover,
		:host([selected]) .toggle-button:has(.toggle-button__input:hover) {
			--_highlight-border-color: var(--_is-selected-is-hovered-highlight-border-color);
			background-color: var(--semantics-buttons-neutral-tinted-is-selected-is-hovered-background-color);
			color: var(--semantics-buttons-neutral-tinted-is-selected-is-hovered-primary-content-color);
		}
	}

	:host([selected]) .toggle-button:active,
	:host([selected]) .toggle-button:has(.toggle-button__input:active) {
		--_highlight-border-color: var(--_is-selected-is-active-highlight-border-color);
		background-color: var(--semantics-buttons-neutral-tinted-is-selected-is-active-background-color);
		color: var(--semantics-buttons-neutral-tinted-is-selected-is-active-primary-content-color);
	}

	.toggle-button:focus-visible,
	.toggle-button:has(.toggle-button__input:focus-visible) {
		outline: var(--semantics-focus-ring-outline);
		outline-offset: var(--semantics-focus-ring-outline-offset);
		box-shadow: var(--semantics-focus-ring-box-shadow), inset 0 0 0 var(--primitives-border-width-thin) var(--_highlight-border-color);
	}

	.toggle-button:focus:not(:focus-visible) {
		outline: none;
	}


	/* # Elements */

	::slotted(nldd-icon) {
		display: none;
	}

	.toggle-button__icon,
	::slotted([slot="icon"]) {
		display: block;
		width: var(--_icon-only-icon-size);
		height: var(--_icon-only-icon-size);
		flex-shrink: 0;
	}

	.toggle-button:has(.toggle-button__text) .toggle-button__icon,
	.toggle-button:has(.toggle-button__text) ::slotted([slot="icon"]) {
		width: var(--_icon-size);
		height: var(--_icon-size);
	}

	:host([size="lg"][variant="icon-and-text"]) .toggle-button,
	:host([size="lg"][variant=""]) .toggle-button:has(.toggle-button__text):has(.toggle-button__icon) {
		width: auto;
		padding: var(--primitives-space-8);
		gap: var(--primitives-space-2);
		flex-direction: column;
	}

	:host([size="lg"][variant="icon-and-text"]) .toggle-button__text,
	:host([size="lg"][variant=""]) .toggle-button:has(.toggle-button__icon) .toggle-button__text {
		font: var(--_stacked-text-font);
	}

	/* variant="text" keeps the icon slot in shadow DOM (so slotchange still
	   fires after a future variant change) but hides any rendered icon. */
	:host([variant="text"]) .toggle-button__icon,
	:host([variant="text"]) ::slotted([slot="icon"]) {
		display: none;
	}

	.toggle-button__input {
		position: absolute;
		inset: 0;
		opacity: 0;
		z-index: 1;
		margin: 0;
		width: 100%;
		height: 100%;
	}
`;

  // node_modules/@nldd/design-system/dist/components/inputs/toggle-button/toggle-button.template.js
  init_lit();
  init_tooltip();
  function toggleButtonTemplate(component) {
    const showText = component.variant !== "icon" && !!component.text;
    const showIcon = component.variant !== "text" && component._hasIcon;
    const showPlaceholder = component.variant === "icon" || component.variant === "icon-and-text";
    const ariaLabel = component.accessibleLabel || (component.variant === "icon" && component.text ? component.text : "") || A;
    const isIconOnlyDisplay = showIcon && !showText;
    const tooltipText = isIconOnlyDisplay ? component.accessibleLabel || component.text || "" : "";
    const icon = component.icon ? b2`<nldd-icon class="toggle-button__icon" name=${component.icon}></nldd-icon>` : b2`<slot name="icon" @slotchange=${component.requestUpdate}>${showPlaceholder ? b2`<nldd-icon class="toggle-button__icon" name="icon-placeholder"></nldd-icon>` : A}</slot>`;
    const textContent = showText ? b2`<span class="toggle-button__text">${component.text}</span>` : A;
    let result;
    if (component.type === "checkbox" || component.type === "radio") {
      result = b2`
			<label class="toggle-button">
				<input class="toggle-button__input"
					type=${component.type}
					.checked=${component.selected}
					?disabled=${component.disabled}
					name=${component.name || A}
					value=${component.value}
					aria-label=${ariaLabel}
					@change=${component._handleInputChange}
				>
				${icon}
				${textContent}
			</label>
		`;
    } else {
      result = b2`
			<button class="toggle-button"
				type="button"
				aria-pressed=${component.selected}
				?disabled=${component.disabled}
				aria-label=${ariaLabel}
				@click=${component._handleButtonClick}
			>
				${icon}
				${textContent}
			</button>
		`;
    }
    if (tooltipText) {
      return b2`<nldd-tooltip text=${tooltipText}>${result}</nldd-tooltip>`;
    }
    return result;
  }

  // node_modules/@nldd/design-system/dist/components/inputs/toggle-button/toggle-button.js
  init_icon();
  var import_meta14 = {};
  var __decorate42 = function(decorators, target, key, desc) {
    var c6 = arguments.length, r6 = c6 < 3 ? target : desc === null ? desc = Object.getOwnPropertyDescriptor(target, key) : desc, d3;
    if (typeof Reflect === "object" && typeof Reflect.decorate === "function") r6 = Reflect.decorate(decorators, target, key, desc);
    else for (var i8 = decorators.length - 1; i8 >= 0; i8--) if (d3 = decorators[i8]) r6 = (c6 < 3 ? d3(r6) : c6 > 3 ? d3(target, key, r6) : d3(target, key)) || r6;
    return c6 > 3 && r6 && Object.defineProperty(target, key, r6), r6;
  };
  var NLDDToggleButton = class NLDDToggleButton2 extends i4 {
    constructor() {
      super(...arguments);
      this._internals = this.attachInternals();
      this._initialSelected = false;
      this.type = "button";
      this.size = "md";
      this.selected = false;
      this.disabled = false;
      this.value = "on";
      this.name = "";
      this.text = "";
      this.icon = "";
      this.variant = "";
      this.accessibleLabel = "";
      this._warnedA11y = false;
      this._warnedEmptyIcon = false;
    }
    /** Whether an icon is present via attribute or slot. */
    get _hasIcon() {
      if (this.icon)
        return true;
      const slot = this.shadowRoot?.querySelector('slot[name="icon"]');
      if (slot && slot.assignedElements().length > 0)
        return true;
      return this.querySelector(':scope > [slot="icon"]') !== null;
    }
    firstUpdated() {
      this._initialSelected = this.selected;
    }
    updated(changed) {
      const inaccessible = !this.text && !this.accessibleLabel;
      if (import_meta14.env?.DEV && inaccessible && !this._warnedA11y) {
        this._warnedA11y = true;
        console.warn("<nldd-toggle-button>: Provide a text or accessible-label attribute for accessibility.");
      } else if (!inaccessible) {
        this._warnedA11y = false;
      }
      if (import_meta14.env?.DEV && this.variant === "icon" && !this._hasIcon && !this._warnedEmptyIcon) {
        this._warnedEmptyIcon = true;
        console.warn('<nldd-toggle-button variant="icon">: No icon attribute or slot="icon" content provided. A placeholder icon is shown.');
      } else if (this._hasIcon || this.variant !== "icon") {
        this._warnedEmptyIcon = false;
      }
      if (changed.has("selected") || changed.has("value") || changed.has("type")) {
        const submits = this.type === "checkbox" || this.type === "radio";
        this._internals.setFormValue(submits && this.selected ? this.value : null);
      }
    }
    formResetCallback() {
      this.selected = this._initialSelected;
    }
    formDisabledCallback(disabled) {
      this.disabled = disabled;
    }
    formStateRestoreCallback(state) {
      this.selected = state !== null;
    }
    _handleButtonClick() {
      if (this.disabled)
        return;
      this._toggle();
    }
    _handleInputChange(e9) {
      const input = e9.target;
      this.selected = input.checked;
      this._dispatchChange();
    }
    _toggle() {
      this.selected = !this.selected;
      this._dispatchChange();
    }
    _dispatchChange() {
      this.dispatchEvent(new CustomEvent("change", {
        detail: { selected: this.selected, value: this.value },
        bubbles: true,
        composed: true
      }));
    }
    /**
     * Toggle selected state programmatically.
     * For type="radio", the button can only be selected, never deselected (native behavior).
     */
    toggle() {
      if (this.disabled)
        return;
      if (this.type === "radio" && this.selected)
        return;
      this._toggle();
    }
    render() {
      return toggleButtonTemplate(this);
    }
  };
  NLDDToggleButton.formAssociated = true;
  NLDDToggleButton.styles = toggleButtonStyles;
  __decorate42([
    n4({ type: String, reflect: true })
  ], NLDDToggleButton.prototype, "type", void 0);
  __decorate42([
    n4({ type: String, reflect: true })
  ], NLDDToggleButton.prototype, "size", void 0);
  __decorate42([
    n4({ type: Boolean, reflect: true })
  ], NLDDToggleButton.prototype, "selected", void 0);
  __decorate42([
    n4({ type: Boolean, reflect: true })
  ], NLDDToggleButton.prototype, "disabled", void 0);
  __decorate42([
    n4({ type: String })
  ], NLDDToggleButton.prototype, "value", void 0);
  __decorate42([
    n4({ type: String, reflect: true })
  ], NLDDToggleButton.prototype, "name", void 0);
  __decorate42([
    n4({ type: String })
  ], NLDDToggleButton.prototype, "text", void 0);
  __decorate42([
    n4({ type: String })
  ], NLDDToggleButton.prototype, "icon", void 0);
  __decorate42([
    n4({ type: String, reflect: true })
  ], NLDDToggleButton.prototype, "variant", void 0);
  __decorate42([
    n4({ type: String, attribute: "accessible-label" })
  ], NLDDToggleButton.prototype, "accessibleLabel", void 0);
  NLDDToggleButton = __decorate42([
    t3("nldd-toggle-button")
  ], NLDDToggleButton);

  // node_modules/@nldd/design-system/dist/components/inputs/toggle-button-group/toggle-button-group.js
  init_lit();
  init_decorators();

  // node_modules/@nldd/design-system/dist/components/inputs/toggle-button-group/toggle-button-group.styles.js
  init_lit();
  var toggleButtonGroupStyles = i`


	/* # Host */

	:host {
		display: block;
		isolation: isolate;
	}

	:host([hidden]) {
		display: none;
	}


	/* # Block */

	.toggle-button-group {
		display: flex;
		flex-wrap: wrap;
		gap: var(--components-toggle-button-group-md-gap);
	}

	:host([size="sm"]) .toggle-button-group {
		gap: var(--components-toggle-button-group-sm-gap);
	}

	:host([size="xs"]) .toggle-button-group {
		gap: var(--components-toggle-button-group-xs-gap);
	}
`;

  // node_modules/@nldd/design-system/dist/components/inputs/toggle-button-group/toggle-button-group.template.js
  init_lit();
  function toggleButtonGroupTemplate(component) {
    return b2`
		<div class="toggle-button-group">
			<slot @slotchange=${component._onSlotChange}></slot>
		</div>
	`;
  }

  // node_modules/@nldd/design-system/dist/components/inputs/toggle-button-group/toggle-button-group.js
  var import_meta15 = {};
  var __decorate43 = function(decorators, target, key, desc) {
    var c6 = arguments.length, r6 = c6 < 3 ? target : desc === null ? desc = Object.getOwnPropertyDescriptor(target, key) : desc, d3;
    if (typeof Reflect === "object" && typeof Reflect.decorate === "function") r6 = Reflect.decorate(decorators, target, key, desc);
    else for (var i8 = decorators.length - 1; i8 >= 0; i8--) if (d3 = decorators[i8]) r6 = (c6 < 3 ? d3(r6) : c6 > 3 ? d3(target, key, r6) : d3(target, key)) || r6;
    return c6 > 3 && r6 && Object.defineProperty(target, key, r6), r6;
  };
  var NLDDToggleButtonGroup = class NLDDToggleButtonGroup2 extends i4 {
    constructor() {
      super(...arguments);
      this.type = "checkbox";
      this.name = "";
      this.size = "md";
      this.disabled = false;
      this.accessibleLabel = "";
      this.accessibleLabelledBy = "";
      this._handleChange = (e9) => {
        if (this.type !== "radio")
          return;
        const changedButton = e9.target;
        if (!changedButton.selected)
          return;
        this._getButtons().forEach((button) => {
          if (button !== changedButton)
            button.selected = false;
        });
      };
      this._handleKeyDown = (e9) => {
        if (this.type !== "radio")
          return;
        if (!["ArrowDown", "ArrowUp", "ArrowRight", "ArrowLeft"].includes(e9.key))
          return;
        const buttons = this._getEnabledButtons();
        if (buttons.length === 0)
          return;
        const activeButton = buttons.find((b3) => b3.selected);
        const currentIndex = activeButton ? buttons.indexOf(activeButton) : -1;
        const isNext = e9.key === "ArrowDown" || e9.key === "ArrowRight";
        const nextIndex = isNext ? (currentIndex + 1) % buttons.length : currentIndex <= 0 ? buttons.length - 1 : currentIndex - 1;
        const nextButton = buttons[nextIndex];
        if (!nextButton)
          return;
        e9.preventDefault();
        if (activeButton)
          activeButton.selected = false;
        nextButton.selected = true;
        const input = nextButton.shadowRoot?.querySelector(".toggle-button__input");
        input?.focus();
        nextButton.dispatchEvent(new CustomEvent("change", {
          detail: { selected: true, value: nextButton.value },
          bubbles: true,
          composed: true
        }));
      };
      this._onSlotChange = () => {
        this._syncButtons();
      };
    }
    connectedCallback() {
      super.connectedCallback();
      this._syncRole();
      this.addEventListener("change", this._handleChange);
      this.addEventListener("keydown", this._handleKeyDown);
    }
    disconnectedCallback() {
      super.disconnectedCallback();
      this.removeEventListener("change", this._handleChange);
      this.removeEventListener("keydown", this._handleKeyDown);
    }
    _syncRole() {
      this.setAttribute("role", this.type === "radio" ? "radiogroup" : "group");
    }
    firstUpdated() {
      import_meta15.env?.DEV && !this.accessibleLabel && !this.accessibleLabelledBy && console.warn("<nldd-toggle-button-group>: No accessible name provided. Add an accessible-label or accessible-labelledby attribute for screen reader accessibility.");
    }
    updated(changed) {
      if (changed.has("type") || changed.has("name") || changed.has("size") || changed.has("disabled")) {
        this._syncButtons();
      }
      if (changed.has("type")) {
        this._syncRole();
      }
      if (changed.has("accessibleLabel")) {
        if (this.accessibleLabel) {
          this.setAttribute("aria-label", this.accessibleLabel);
        } else {
          this.removeAttribute("aria-label");
        }
      }
      if (changed.has("accessibleLabelledBy")) {
        if (this.accessibleLabelledBy) {
          this.setAttribute("aria-labelledby", this.accessibleLabelledBy);
        } else {
          this.removeAttribute("aria-labelledby");
        }
      }
    }
    _getButtons() {
      return Array.from(this.querySelectorAll("nldd-toggle-button"));
    }
    _getEnabledButtons() {
      return this._getButtons().filter((b3) => !b3.disabled);
    }
    _syncButtons() {
      this._getButtons().forEach((button) => {
        button.type = this.type;
        button.size = this.size;
        if (this.type !== "button") {
          button.name = this.name;
        }
        if (this.disabled) {
          if (!button.hasAttribute("disabled")) {
            button.setAttribute("group-disabled", "");
            button.disabled = true;
          }
        } else if (button.hasAttribute("group-disabled")) {
          button.removeAttribute("group-disabled");
          button.disabled = false;
        }
      });
    }
    render() {
      return toggleButtonGroupTemplate(this);
    }
  };
  NLDDToggleButtonGroup.styles = toggleButtonGroupStyles;
  __decorate43([
    n4({ type: String, reflect: true })
  ], NLDDToggleButtonGroup.prototype, "type", void 0);
  __decorate43([
    n4({ type: String })
  ], NLDDToggleButtonGroup.prototype, "name", void 0);
  __decorate43([
    n4({ type: String, reflect: true })
  ], NLDDToggleButtonGroup.prototype, "size", void 0);
  __decorate43([
    n4({ type: Boolean, reflect: true })
  ], NLDDToggleButtonGroup.prototype, "disabled", void 0);
  __decorate43([
    n4({ type: String, attribute: "accessible-label" })
  ], NLDDToggleButtonGroup.prototype, "accessibleLabel", void 0);
  __decorate43([
    n4({ type: String, attribute: "accessible-labelledby" })
  ], NLDDToggleButtonGroup.prototype, "accessibleLabelledBy", void 0);
  NLDDToggleButtonGroup = __decorate43([
    t3("nldd-toggle-button-group")
  ], NLDDToggleButtonGroup);

  // node_modules/@nldd/design-system/dist/components/inputs/token/token.js
  init_lit();
  init_decorators();

  // node_modules/@nldd/design-system/dist/components/inputs/token/token.styles.js
  init_lit();
  init_slotted_reset();
  var tokenStyles = i`


	/* # Host */

	:host {
		--_highlight-border-color: var(--semantics-buttons-neutral-tinted-highlight-border-color);

		${inheritedTextReset}
		display: inline-block;
		user-select: none;
		-webkit-tap-highlight-color: transparent;
	}

	:host([hidden]) {
		display: none;
	}

	:host([disabled]) {
		opacity: var(--primitives-opacity-disabled);
		pointer-events: none;
	}


	/* # Block */

	.token {
		box-sizing: border-box;
		display: inline-flex;
		margin: 0;
		border: none;
		border-radius: var(--semantics-controls-sm-corner-radius);
		background: none;
		background-color: var(--semantics-buttons-neutral-tinted-background-color);
		position: relative;
		height: var(--semantics-controls-sm-min-size);
		padding: 0 var(--primitives-space-6);
		align-items: center;
		color: var(--semantics-buttons-neutral-tinted-primary-content-color);
		font: var(--semantics-buttons-sm-primary-text-font);
		transition: background-color var(--primitives-transition-duration-fast) var(--primitives-transition-easing-default);
		appearance: none;
	}

	.token::after {
		content: '';
		position: absolute;
		inset: 0;
		border-radius: inherit;
		box-shadow: inset 0 0 0 var(--primitives-border-width-thin) var(--_highlight-border-color);
		pointer-events: none;
	}

	@media (prefers-reduced-motion: reduce) {
		.token {
			transition: none;
		}
	}

	@media (hover: hover) {
		:host([control="menu"]) .token:hover:not(:disabled) {
			--_highlight-border-color: var(--semantics-buttons-neutral-tinted-is-hovered-highlight-border-color);
			background-color: var(--semantics-buttons-neutral-tinted-is-hovered-background-color);
			color: var(--semantics-buttons-neutral-tinted-is-hovered-primary-content-color);
		}
	}

	:host([control="menu"]) .token:active:not(:disabled) {
		--_highlight-border-color: var(--semantics-buttons-neutral-tinted-is-active-highlight-border-color);
		background-color: var(--semantics-buttons-neutral-tinted-is-active-background-color);
		color: var(--semantics-buttons-neutral-tinted-is-active-primary-content-color);
	}

	:host([expanded]) .token {
		--_highlight-border-color: var(--semantics-buttons-neutral-tinted-is-expanded-highlight-border-color);
		background-color: var(--semantics-buttons-neutral-tinted-is-expanded-background-color);
		color: var(--semantics-buttons-neutral-tinted-is-expanded-primary-content-color);
	}

	@media (hover: hover) {
		:host([expanded]) .token:hover:not(:disabled) {
			--_highlight-border-color: var(--semantics-buttons-neutral-tinted-is-expanded-is-hovered-highlight-border-color);
			background-color: var(--semantics-buttons-neutral-tinted-is-expanded-is-hovered-background-color);
			color: var(--semantics-buttons-neutral-tinted-is-expanded-is-hovered-primary-content-color);
		}
	}

	:host([expanded]) .token:active:not(:disabled) {
		--_highlight-border-color: var(--semantics-buttons-neutral-tinted-is-expanded-is-active-highlight-border-color);
		background-color: var(--semantics-buttons-neutral-tinted-is-expanded-is-active-background-color);
		color: var(--semantics-buttons-neutral-tinted-is-expanded-is-active-primary-content-color);
	}

	:host([control="menu"]) .token:focus-visible {
		outline: var(--semantics-focus-ring-outline);
		outline-offset: var(--semantics-focus-ring-outline-offset);
		box-shadow: var(--semantics-focus-ring-box-shadow);
	}

	:host([control="menu"]) .token:focus:not(:focus-visible) {
		outline: none;
	}

	/* Remove right padding so the nldd-icon-button flush-fits the token edge */
	:host([control="dismiss"]) .token {
		padding-right: 0;
	}

	@media (forced-colors: active) {
		.token {
			border: 1px solid CanvasText;
		}

		:host([control="menu"]) .token:focus-visible,
		.token__dismiss:focus-visible {
			outline: 2px solid CanvasText;
			outline-offset: 2px;
		}
	}


	/* # Elements */

	.token__text {
		display: flex;
		padding: 0 var(--primitives-space-2);
		align-items: center;
	}

	.token__icon {
		display: block;
		width: var(--primitives-space-16);
		height: var(--primitives-space-16);
		flex-shrink: 0;
	}
`;

  // node_modules/@nldd/design-system/dist/components/inputs/token/token.template.js
  init_lit();
  function tokenTemplate(component) {
    if (component.control === "menu") {
      return b2`
			<button
				class="token"
				type="button"
				?disabled=${component.disabled}
				aria-expanded=${component.expanded}
				aria-controls=${component.controls || A}
				@click=${component._handleMenuClick}
			>
				<span class="token__text"><slot></slot></span>
				<nldd-icon class="token__icon" name="chevron-down-small"></nldd-icon>
			</button>
		`;
    }
    return b2`
		<div class="token">
			<span class="token__text"><slot></slot></span>
			${component.control === "dismiss" ? b2`
				<div class="token__dismiss-action">
					<nldd-icon-button
						size="sm"
						variant="neutral-tinted"
						icon="dismiss-small"
						text=${component.dismissText}
						accessible-label=${component.dismissText}
						tooltip-timing="never"
						no-highlight-border
						?disabled=${component.disabled}
						@click=${component._handleDismiss}
					></nldd-icon-button>
				</div>
			` : A}
		</div>
	`;
  }

  // node_modules/@nldd/design-system/dist/components/inputs/token/token.js
  init_icon();
  init_icon_button();
  var __decorate44 = function(decorators, target, key, desc) {
    var c6 = arguments.length, r6 = c6 < 3 ? target : desc === null ? desc = Object.getOwnPropertyDescriptor(target, key) : desc, d3;
    if (typeof Reflect === "object" && typeof Reflect.decorate === "function") r6 = Reflect.decorate(decorators, target, key, desc);
    else for (var i8 = decorators.length - 1; i8 >= 0; i8--) if (d3 = decorators[i8]) r6 = (c6 < 3 ? d3(r6) : c6 > 3 ? d3(target, key, r6) : d3(target, key)) || r6;
    return c6 > 3 && r6 && Object.defineProperty(target, key, r6), r6;
  };
  var NLDDToken = class NLDDToken2 extends i4 {
    constructor() {
      super(...arguments);
      this.control = "none";
      this.expanded = false;
      this.disabled = false;
      this.dismissText = "Verwijder";
      this.controls = "";
    }
    _handleDismiss(e9) {
      e9.stopPropagation();
      if (this.disabled)
        return;
      this.dispatchEvent(new CustomEvent("dismiss", {
        bubbles: true,
        composed: true
      }));
    }
    _handleMenuClick() {
      if (this.disabled)
        return;
      this.expanded = !this.expanded;
      this.dispatchEvent(new CustomEvent("toggle", {
        detail: { expanded: this.expanded },
        bubbles: true,
        composed: true
      }));
    }
    render() {
      return tokenTemplate(this);
    }
  };
  NLDDToken.styles = tokenStyles;
  __decorate44([
    n4({ type: String, reflect: true })
  ], NLDDToken.prototype, "control", void 0);
  __decorate44([
    n4({ type: Boolean, reflect: true })
  ], NLDDToken.prototype, "expanded", void 0);
  __decorate44([
    n4({ type: Boolean, reflect: true })
  ], NLDDToken.prototype, "disabled", void 0);
  __decorate44([
    n4({ type: String, attribute: "dismiss-text" })
  ], NLDDToken.prototype, "dismissText", void 0);
  __decorate44([
    n4({ type: String, reflect: true })
  ], NLDDToken.prototype, "controls", void 0);
  NLDDToken = __decorate44([
    t3("nldd-token")
  ], NLDDToken);

  // node_modules/@nldd/design-system/dist/components/layout/app-view/app-view.js
  init_lit();
  init_decorators();

  // node_modules/@nldd/design-system/dist/components/layout/app-view/app-view.styles.js
  init_lit();
  var appViewStyles = i`


	/* # Host */

	:host {
		--context-parent-background-color: var(--semantics-surfaces-base-background-color);
		--_background-color: var(--context-parent-background-color);

		display: flex;
		background-color: var(--_background-color);
		width: 100%;
		height: 100%;
	}

	:host([hidden]) {
		display: none;
	}

	:host([background="tinted"]) {
		--context-parent-background-color: var(--semantics-surfaces-tinted-background-color);
		--_background-color: var(--semantics-surfaces-tinted-background-color);
	}


	/* # Block */

	.app-view {
		display: flex;
		min-width: 0;
		min-height: 0;
		overflow: hidden;
		flex-direction: column;
		flex-grow: 1;
		flex-shrink: 1;
		flex-basis: 0;
	}


	/* # Elements */

	::slotted(*) {
		min-height: 0;
		flex-grow: 1;
		flex-shrink: 1;
		flex-basis: 0;
	}
`;

  // node_modules/@nldd/design-system/dist/components/layout/app-view/app-view.template.js
  init_lit();
  function appViewTemplate(_component) {
    return b2`
		<div class="app-view">
			<slot></slot>
		</div>
	`;
  }

  // node_modules/@nldd/design-system/dist/components/layout/app-view/app-view.js
  var __decorate45 = function(decorators, target, key, desc) {
    var c6 = arguments.length, r6 = c6 < 3 ? target : desc === null ? desc = Object.getOwnPropertyDescriptor(target, key) : desc, d3;
    if (typeof Reflect === "object" && typeof Reflect.decorate === "function") r6 = Reflect.decorate(decorators, target, key, desc);
    else for (var i8 = decorators.length - 1; i8 >= 0; i8--) if (d3 = decorators[i8]) r6 = (c6 < 3 ? d3(r6) : c6 > 3 ? d3(target, key, r6) : d3(target, key)) || r6;
    return c6 > 3 && r6 && Object.defineProperty(target, key, r6), r6;
  };
  var _connectedStack = [];
  function _currentOwner() {
    for (let i8 = _connectedStack.length - 1; i8 >= 0; i8--) {
      if (_connectedStack[i8].isConnected)
        return _connectedStack[i8];
    }
    return null;
  }
  var NLDDAppView = class NLDDAppView2 extends i4 {
    constructor() {
      super(...arguments);
      this.background = "base";
    }
    connectedCallback() {
      super.connectedCallback();
      _connectedStack.push(this);
      this._writeBodyBackground();
      document.documentElement.style.overscrollBehavior = "none";
      document.body.style.overscrollBehavior = "none";
    }
    disconnectedCallback() {
      super.disconnectedCallback();
      const idx = _connectedStack.indexOf(this);
      if (idx >= 0)
        _connectedStack.splice(idx, 1);
      const owner = _currentOwner();
      if (owner) {
        owner._writeBodyBackground();
      } else {
        document.body.style.removeProperty("background-color");
        document.documentElement.style.removeProperty("overscroll-behavior");
        document.body.style.removeProperty("overscroll-behavior");
      }
    }
    updated(changed) {
      if (changed.has("background") && _currentOwner() === this) {
        this._writeBodyBackground();
      }
    }
    _writeBodyBackground() {
      const token = this.background === "tinted" ? "--semantics-surfaces-tinted-background-color" : "--semantics-surfaces-base-background-color";
      document.body.style.backgroundColor = `var(${token})`;
    }
    render() {
      return appViewTemplate(this);
    }
  };
  NLDDAppView.styles = appViewStyles;
  __decorate45([
    n4({ type: String, reflect: true })
  ], NLDDAppView.prototype, "background", void 0);
  NLDDAppView = __decorate45([
    t3("nldd-app-view")
  ], NLDDAppView);

  // node_modules/@nldd/design-system/dist/components/layout/split-views/bar-split-view/bar-split-view.js
  init_lit();
  init_decorators();

  // node_modules/@nldd/design-system/dist/components/layout/split-views/bar-split-view/bar-split-view.styles.js
  init_lit();
  var barSplitViewStyles = i`


	/* # Host */

	:host {
		--_background-color: var(--context-parent-background-color, var(--semantics-surfaces-base-background-color));

		display: flex;
		background-color: var(--_background-color);
		width: 100%;
		height: 100%;
		flex-direction: column;
	}

	:host([hidden]) {
		display: none;
	}

	:host([background="base"]) {
		--context-parent-background-color: var(--semantics-surfaces-base-background-color);
		--_background-color: var(--context-parent-background-color);
	}

	:host([background="tinted"]) {
		--context-parent-background-color: var(--semantics-surfaces-tinted-background-color);
		--_background-color: var(--context-parent-background-color);
	}


	/* # Block */

	.bar-split-view {
		display: flex;
		min-width: 0;
		min-height: 0;
		flex-direction: column;
		flex-grow: 1;
		flex-shrink: 1;
		flex-basis: 0;
	}


	/* # Elements */

	.bar-split-view__bar {
		display: flex;
		min-width: 0;
		overflow: hidden;
		flex-direction: column;
		flex-shrink: 0;
	}

	.bar-split-view__divider {
		flex-shrink: 0;
	}

	.bar-split-view__main {
		display: flex;
		min-width: 0;
		min-height: 0;
		overflow: hidden;
		flex-direction: column;
		flex-grow: 1;
		flex-shrink: 1;
		flex-basis: 0;
	}

	/* Only the main pane grows to fill remaining space; bar panes size to
	 * their content. Without this scoping a bar's slotted content gets
	 * flex-basis: 0 and collapses (the old 'flex: 1' shorthand expanded
	 * to 'flex: 1 1 0%', where % resolved to 'auto' inside the
	 * height-indeterminate bar wrapper — the longhand conversion lost
	 * that nuance, hence this rule). */
	::slotted([slot="main"]) {
		min-height: 0;
		flex-grow: 1;
		flex-shrink: 1;
		flex-basis: 0;
	}
`;

  // node_modules/@nldd/design-system/dist/components/layout/split-views/bar-split-view/bar-split-view.template.js
  init_lit();
  init_class_map2();

  // node_modules/@nldd/design-system/dist/components/layout/split-views/split-view-divider/split-view-divider.js
  init_lit();
  init_decorators();

  // node_modules/@nldd/design-system/dist/components/layout/split-views/split-view-divider/split-view-divider.styles.js
  init_lit();
  var splitViewDividerStyles = i`


	/* # Host */

	:host {
		--_thickness: var(--semantics-dividers-thickness);

		display: flex;
		flex-shrink: 0;
		align-self: stretch;
	}

	:host([hidden]) {
		display: none;
	}

	:host([has-drag-handle]) {
		--_thickness: var(--primitives-space-12);
	}


	/* # Block */

	.split-view-divider {
		display: flex;
		position: relative;
		background-color: var(--semantics-dividers-color);
		width: 100%;
		height: var(--_thickness);
		align-items: center;
		justify-content: center;
	}

	:host([orientation="vertical"]) .split-view-divider {
		width: var(--_thickness);
		height: 100%;
	}

	@media (forced-colors: active) {
		.split-view-divider {
			background-color: CanvasText;
		}
	}


	/* # Elements */

	.split-view-divider__drag-handle {
		position: absolute;
		border-radius: var(--primitives-corner-radius-full);
		background-color: var(--semantics-content-secondary-color);
		width: var(--primitives-space-40);
		height: var(--primitives-space-4);
	}

	:host([orientation="vertical"]) .split-view-divider__drag-handle {
		width: var(--primitives-space-4);
		height: var(--primitives-space-40);
	}
`;

  // node_modules/@nldd/design-system/dist/components/layout/split-views/split-view-divider/split-view-divider.template.js
  init_lit();
  function splitViewDividerTemplate(component) {
    return b2`
		<div
			class="split-view-divider"
			role="separator"
			aria-orientation=${component.orientation}
		>
			${component.hasDragHandle ? b2`<div class="split-view-divider__drag-handle"></div>` : A}
		</div>
	`;
  }

  // node_modules/@nldd/design-system/dist/components/layout/split-views/split-view-divider/split-view-divider.js
  var __decorate46 = function(decorators, target, key, desc) {
    var c6 = arguments.length, r6 = c6 < 3 ? target : desc === null ? desc = Object.getOwnPropertyDescriptor(target, key) : desc, d3;
    if (typeof Reflect === "object" && typeof Reflect.decorate === "function") r6 = Reflect.decorate(decorators, target, key, desc);
    else for (var i8 = decorators.length - 1; i8 >= 0; i8--) if (d3 = decorators[i8]) r6 = (c6 < 3 ? d3(r6) : c6 > 3 ? d3(target, key, r6) : d3(target, key)) || r6;
    return c6 > 3 && r6 && Object.defineProperty(target, key, r6), r6;
  };
  var NLDDSplitViewDivider = class NLDDSplitViewDivider2 extends i4 {
    constructor() {
      super(...arguments);
      this.orientation = "vertical";
      this.hasDragHandle = false;
    }
    render() {
      return splitViewDividerTemplate(this);
    }
  };
  NLDDSplitViewDivider.styles = splitViewDividerStyles;
  __decorate46([
    n4({ type: String, reflect: true })
  ], NLDDSplitViewDivider.prototype, "orientation", void 0);
  __decorate46([
    n4({ type: Boolean, reflect: true, attribute: "has-drag-handle" })
  ], NLDDSplitViewDivider.prototype, "hasDragHandle", void 0);
  NLDDSplitViewDivider = __decorate46([
    t3("nldd-split-view-divider")
  ], NLDDSplitViewDivider);

  // node_modules/@nldd/design-system/dist/components/layout/split-views/bar-split-view/bar-split-view.template.js
  function barSplitViewTemplate(component) {
    const sorted = component._getSortedChildren();
    const isSm = component._currentBreakpoint === "sm";
    return b2`
		<div class="bar-split-view">
			${sorted.map((el, index) => {
      const isMain = el.slot === "main";
      const isLast = index === sorted.length - 1;
      const next = sorted[index + 1];
      const showDivider = !isSm && !isLast && !el.hasAttribute("no-divider") && !next?.hasAttribute("no-divider");
      return b2`
					<div class=${e8({ "bar-split-view__main": isMain, "bar-split-view__bar": !isMain })}>
						<slot name=${el.slot}></slot>
					</div>
					${showDivider ? b2`
						<div class="bar-split-view__divider">
							<nldd-split-view-divider orientation="horizontal"></nldd-split-view-divider>
						</div>
					` : A}
				`;
    })}
			${!sorted.some((el) => el.slot === "main") ? b2`
				<div class="bar-split-view__main">
					<slot name="main"></slot>
				</div>
			` : A}
		</div>
	`;
  }

  // node_modules/@nldd/design-system/dist/components/layout/split-views/bar-split-view/bar-split-view.js
  init_breakpoints();
  var import_meta16 = {};
  var __decorate47 = function(decorators, target, key, desc) {
    var c6 = arguments.length, r6 = c6 < 3 ? target : desc === null ? desc = Object.getOwnPropertyDescriptor(target, key) : desc, d3;
    if (typeof Reflect === "object" && typeof Reflect.decorate === "function") r6 = Reflect.decorate(decorators, target, key, desc);
    else for (var i8 = decorators.length - 1; i8 >= 0; i8--) if (d3 = decorators[i8]) r6 = (c6 < 3 ? d3(r6) : c6 > 3 ? d3(target, key, r6) : d3(target, key)) || r6;
    return c6 > 3 && r6 && Object.defineProperty(target, key, r6), r6;
  };
  var smMaxPx = parseInt(breakpoints.smMax);
  var mdMaxPx = parseInt(breakpoints.mdMax);
  var NLDDBarSplitView = class NLDDBarSplitView2 extends i4 {
    constructor() {
      super(...arguments);
      this.background = "inherit";
      this._currentBreakpoint = null;
      this._observer = null;
      this._resizeObserver = null;
    }
    connectedCallback() {
      super.connectedCallback();
      this._currentBreakpoint = this._getBreakpoint(this.getBoundingClientRect().width);
      this._observer = new MutationObserver(() => this.requestUpdate());
      this._observer.observe(this, { childList: true, attributes: true, attributeFilter: ["above", "below", "only", "sm-order", "md-order", "lg-order", "no-divider"], subtree: false });
      this._resizeObserver = new ResizeObserver(() => {
        const bp = this._getBreakpoint(this.getBoundingClientRect().width);
        if (bp !== this._currentBreakpoint) {
          this._currentBreakpoint = bp;
          this.requestUpdate();
        }
      });
      this._resizeObserver.observe(this);
    }
    disconnectedCallback() {
      super.disconnectedCallback();
      this._observer?.disconnect();
      this._observer = null;
      this._resizeObserver?.disconnect();
      this._resizeObserver = null;
    }
    _getBreakpoint(width) {
      if (width <= smMaxPx)
        return "sm";
      if (width <= mdMaxPx)
        return "md";
      return "lg";
    }
    _isChildVisible(el) {
      if (this._currentBreakpoint === null)
        return true;
      const bp = this._currentBreakpoint;
      const order = ["sm", "md", "lg"];
      const bpIndex = order.indexOf(bp);
      const only = el.getAttribute("only");
      if (only)
        return bp === only;
      const above = el.getAttribute("above");
      if (above)
        return bpIndex >= order.indexOf(above);
      const below = el.getAttribute("below");
      if (below)
        return bpIndex <= order.indexOf(below);
      return true;
    }
    _getSortedChildren() {
      const all = Array.from(this.children).filter((el) => {
        if (!el.slot) {
          if (import_meta16.env?.DEV) {
            console.warn('<nldd-bar-split-view>: every child must have a slot attribute (e.g. slot="toolbar", slot="status-bar", or slot="bar-1" if no meaningful name applies). Child without slot attribute is ignored:', el);
          }
          return false;
        }
        return this._isChildVisible(el);
      });
      if (this._currentBreakpoint === null)
        return all;
      const attr = `${this._currentBreakpoint}-order`;
      return [...all].sort((a4, b3) => {
        const aVal = a4.hasAttribute(attr) ? parseInt(a4.getAttribute(attr)) : all.indexOf(a4);
        const bVal = b3.hasAttribute(attr) ? parseInt(b3.getAttribute(attr)) : all.indexOf(b3);
        return aVal - bVal;
      });
    }
    render() {
      return barSplitViewTemplate(this);
    }
  };
  NLDDBarSplitView.styles = barSplitViewStyles;
  __decorate47([
    n4({ type: String, reflect: true })
  ], NLDDBarSplitView.prototype, "background", void 0);
  __decorate47([
    r5()
  ], NLDDBarSplitView.prototype, "_currentBreakpoint", void 0);
  NLDDBarSplitView = __decorate47([
    t3("nldd-bar-split-view")
  ], NLDDBarSplitView);

  // node_modules/@nldd/design-system/dist/components/layout/split-views/navigation-split-view/navigation-split-view.js
  init_lit();
  init_decorators();
  init_input_modality();

  // node_modules/@nldd/design-system/dist/components/layout/split-views/navigation-split-view/navigation-split-view.styles.js
  init_lit();
  init_breakpoints();
  var smMax3 = r(breakpoints.smMax);
  var mdMin4 = r(breakpoints.mdMin);
  var mdMax2 = r(breakpoints.mdMax);
  var lgMin2 = r(breakpoints.lgMin);
  var navigationSplitViewStyles = i`


	/* # Host */

	:host {
		--_background-color: var(--context-parent-background-color, var(--semantics-surfaces-base-background-color));
		/* Pane min-widths — read by JS via getComputedStyle in firstUpdated */
		--_sidebar-min-width: var(--primitives-area-320);
		--_secondary-sidebar-min-width: var(--primitives-area-320);
		--_main-min-width: var(--primitives-area-480);
		--_inspector-min-width: var(--primitives-area-320);

		display: flex;
		background-color: var(--_background-color);
		width: 100%;
		height: 100%;
	}

	:host([background="base"]) {
		--context-parent-background-color: var(--semantics-surfaces-base-background-color);
		--_background-color: var(--context-parent-background-color);
	}

	:host([background="tinted"]) {
		--context-parent-background-color: var(--semantics-surfaces-tinted-background-color);
		--_background-color: var(--context-parent-background-color);
	}


	/* # Keyframes */

	@keyframes navigation-split-view-inspector-slide-in {
		from { transform: translateX(100%); }
		to { transform: translateX(0); }
	}

	@keyframes navigation-split-view-inspector-slide-out {
		from { transform: translateX(0); }
		to { transform: translateX(100%); }
	}

	@keyframes navigation-split-view-sidebar-slide-in {
		from { transform: translateX(-100%); }
		to { transform: translateX(0); }
	}

	@keyframes navigation-split-view-sidebar-slide-out {
		from { transform: translateX(0); }
		to { transform: translateX(-100%); }
	}

	@keyframes navigation-split-view-slide-in-bottom {
		from { transform: translateY(100%); }
		to { transform: translateY(0); }
	}

	@keyframes navigation-split-view-slide-out-bottom {
		from { transform: translateY(0); }
		to { transform: translateY(100%); }
	}


	/* # Block */

	.navigation-split-view {
		display: flex;
		min-width: 0;
		min-height: 0;
		overflow: hidden;
		flex-direction: row;
		flex-grow: 1;
		flex-shrink: 1;
		flex-basis: 0;
	}


	/* # Elements */

	.navigation-split-view__sidebar-pane {
		display: flex;
		min-width: var(--_sidebar-min-width);
		min-height: 0;
		overflow: hidden;
		flex-direction: column;
		flex-shrink: 0;
	}

	.navigation-split-view__secondary-sidebar-pane {
		display: flex;
		min-width: var(--_secondary-sidebar-min-width);
		min-height: 0;
		overflow: hidden;
		flex-direction: column;
		flex-shrink: 0;
	}

	.navigation-split-view__main-pane {
		display: flex;
		min-width: var(--_main-min-width);
		min-height: 0;
		overflow: hidden;
		flex-direction: column;
		flex-grow: 1;
		flex-shrink: 1;
		flex-basis: 0;
	}

	/* Full-stack: the single visible pane fills the space, no minimum */
	:host(.full-stack) .navigation-split-view__sidebar-pane,
	:host(.full-stack) .navigation-split-view__secondary-sidebar-pane,
	:host(.full-stack) .navigation-split-view__main-pane {
		min-width: 0;
		flex-grow: 1;
		flex-shrink: 1;
		flex-basis: 0;
	}

	/* Inline sidebar panes suppress their dismiss button */
	.navigation-split-view__sidebar-pane,
	.navigation-split-view__secondary-sidebar-pane {
		--context-dismiss-button-display: none;
	}

	.navigation-split-view__inspector-pane {
		/* Inspector is always dismissable as a sheet, not inline */
		--context-dismiss-button-display: none;

		display: flex;
		min-width: var(--_inspector-min-width);
		min-height: 0;
		overflow: hidden;
		flex-direction: column;
		flex-shrink: 0;
	}

	.navigation-split-view__inspector-sheet {
		display: flex;
		position: fixed;
		margin: 0;
		outline: none;
		border: none;
		box-shadow: var(--semantics-overlays-box-shadow);
		background: var(--semantics-surfaces-base-background-color);
		overflow: hidden;
		padding: 0;
		flex-direction: column;

		@media (max-width: ${smMax3}) {
			inset: auto 0 0 0;
			border-radius: var(--semantics-overlays-corner-radius) var(--semantics-overlays-corner-radius) 0 0;
			width: 100%;
			max-width: 100%;
			height: auto;
			max-height: calc(100dvh - var(--semantics-sheets-bottom-top-inset));
		}

		@media (min-width: ${mdMin4}) and (max-width: ${mdMax2}) {
			inset: var(--semantics-overlays-inset) var(--semantics-overlays-inset) var(--semantics-overlays-inset) auto;
			border-radius: var(--semantics-overlays-corner-radius);
			width: var(--semantics-sheets-side-md-width);
			height: calc(100dvh - var(--semantics-overlays-inset) * 2);
		}

		@media (min-width: ${lgMin2}) {
			inset: var(--semantics-overlays-inset) var(--semantics-overlays-inset) var(--semantics-overlays-inset) auto;
			border-radius: var(--semantics-overlays-corner-radius);
			width: var(--semantics-sheets-side-lg-width);
			height: calc(100dvh - var(--semantics-overlays-inset) * 2);
		}

		&:focus-visible:not(.is-pointer-focus) {
			outline: var(--semantics-focus-ring-outline);
			outline-offset: var(--semantics-focus-ring-outline-offset);
			box-shadow: var(--semantics-focus-ring-box-shadow), var(--semantics-overlays-box-shadow);
		}

		&:not([open]) {
			display: none;
		}

		&::backdrop {
			background: var(--semantics-overlays-backdrop-color);
		}

		&[open] {
			@media (max-width: ${smMax3}) {
				animation: navigation-split-view-slide-in-bottom var(--semantics-sheets-bottom-animation-duration) var(--primitives-transition-easing-default) both;
			}

			@media (min-width: ${mdMin4}) {
				animation: navigation-split-view-inspector-slide-in var(--semantics-sheets-side-animation-duration) var(--primitives-transition-easing-default) both;
			}
		}

		&.is-closing {
			@media (max-width: ${smMax3}) {
				animation: navigation-split-view-slide-out-bottom var(--semantics-sheets-bottom-animation-duration) var(--primitives-transition-easing-default) both;
			}

			@media (min-width: ${mdMin4}) {
				animation: navigation-split-view-inspector-slide-out var(--semantics-sheets-side-animation-duration) var(--primitives-transition-easing-default) both;
			}
		}
	}

	.navigation-split-view__inspector-sheet-body {
		display: flex;
		width: 100%;
		min-height: 0;
		flex-direction: column;
		flex-grow: 1;
	}

	.navigation-split-view__sidebar-sheet {
		display: flex;
		position: fixed;
		margin: 0;
		outline: none;
		border: none;
		box-shadow: var(--semantics-overlays-box-shadow);
		background: var(--semantics-surfaces-base-background-color);
		overflow: hidden;
		padding: 0;
		flex-direction: column;

		@media (max-width: ${smMax3}) {
			inset: auto 0 0 0;
			border-radius: var(--semantics-overlays-corner-radius) var(--semantics-overlays-corner-radius) 0 0;
			width: 100%;
			max-width: 100%;
			height: auto;
			max-height: calc(100dvh - var(--semantics-sheets-bottom-top-inset));
		}

		@media (min-width: ${mdMin4}) and (max-width: ${mdMax2}) {
			inset: var(--semantics-overlays-inset) auto var(--semantics-overlays-inset) var(--semantics-overlays-inset);
			border-radius: var(--semantics-overlays-corner-radius);
			width: var(--semantics-sheets-side-md-width);
			height: calc(100dvh - var(--semantics-overlays-inset) * 2);
		}

		@media (min-width: ${lgMin2}) {
			inset: var(--semantics-overlays-inset) auto var(--semantics-overlays-inset) var(--semantics-overlays-inset);
			border-radius: var(--semantics-overlays-corner-radius);
			width: var(--semantics-sheets-side-lg-width);
			height: calc(100dvh - var(--semantics-overlays-inset) * 2);
		}

		&:focus-visible:not(.is-pointer-focus) {
			outline: var(--semantics-focus-ring-outline);
			outline-offset: var(--semantics-focus-ring-outline-offset);
			box-shadow: var(--semantics-focus-ring-box-shadow), var(--semantics-overlays-box-shadow);
		}

		&:not([open]) {
			display: none;
		}

		&::backdrop {
			background: var(--semantics-overlays-backdrop-color);
		}

		&[open] {
			@media (max-width: ${smMax3}) {
				animation: navigation-split-view-slide-in-bottom var(--semantics-sheets-bottom-animation-duration) var(--primitives-transition-easing-default) both;
			}

			@media (min-width: ${mdMin4}) {
				animation: navigation-split-view-sidebar-slide-in var(--semantics-sheets-side-animation-duration) var(--primitives-transition-easing-default) both;
			}
		}

		&.is-closing {
			@media (max-width: ${smMax3}) {
				animation: navigation-split-view-slide-out-bottom var(--semantics-sheets-bottom-animation-duration) var(--primitives-transition-easing-default) both;
			}

			@media (min-width: ${mdMin4}) {
				animation: navigation-split-view-sidebar-slide-out var(--semantics-sheets-side-animation-duration) var(--primitives-transition-easing-default) both;
			}
		}
	}

	.navigation-split-view__sidebar-sheet-body {
		/* Show dismiss button inside sidebar sheet */
		--context-dismiss-button-display: block;

		display: flex;
		width: 100%;
		min-height: 0;
		flex-direction: column;
		flex-grow: 1;
	}

	@media (prefers-reduced-motion: reduce) {
		.navigation-split-view__inspector-sheet[open],
		.navigation-split-view__inspector-sheet.is-closing,
		.navigation-split-view__sidebar-sheet[open],
		.navigation-split-view__sidebar-sheet.is-closing {
			animation: none;
		}
	}

	::slotted(*) {
		min-height: 0;
		flex-grow: 1;
		flex-shrink: 1;
		flex-basis: 0;
	}
`;

  // node_modules/@nldd/design-system/dist/components/layout/split-views/navigation-split-view/navigation-split-view.template.js
  init_lit();
  function navigationSplitViewTemplate(component) {
    return b2`
		<div class="navigation-split-view">
			${component._showSidebar ? b2`
				<div class="navigation-split-view__sidebar-pane">
					<slot name="sidebar"></slot>
				</div>
				<nldd-split-view-divider orientation="vertical"></nldd-split-view-divider>
			` : A}
			${component._showSecondarySidebar ? b2`
				<div class="navigation-split-view__secondary-sidebar-pane">
					<slot name="secondary-sidebar"></slot>
				</div>
				<nldd-split-view-divider orientation="vertical"></nldd-split-view-divider>
			` : A}
			${component._showMain ? b2`
				<div class="navigation-split-view__main-pane">
					<slot name="main"></slot>
				</div>
			` : A}
			${component._showInspector ? b2`
				<nldd-split-view-divider orientation="vertical"></nldd-split-view-divider>
				<div class="navigation-split-view__inspector-pane">
					<slot name="inspector"></slot>
				</div>
			` : component.inspectorAutoHidden || component.inspectorAsSheet ? b2`
				<dialog class="navigation-split-view__inspector-sheet"
					aria-label=${component.inspectorAccessibleLabel}
					aria-modal="true"
					@click=${component._handleInspectorSheetClick}
					@cancel=${component._handleInspectorSheetCancel}
				>
					<div class="navigation-split-view__inspector-sheet-body">
						<slot name="inspector"></slot>
					</div>
				</dialog>
			` : A}
			${component.sidebarAsSheet ? b2`
				<dialog class="navigation-split-view__sidebar-sheet"
					aria-label=${component.sidebarAccessibleLabel}
					aria-modal="true"
					@click=${component._handleSidebarSheetClick}
					@cancel=${component._handleSidebarSheetCancel}
				>
					<div class="navigation-split-view__sidebar-sheet-body">
						${component._hasSecondarySidebar && component._paneHasContent("secondary-sidebar") ? b2`
							<slot name="secondary-sidebar"></slot>
						` : b2`
							<slot name="sidebar"></slot>
						`}
					</div>
				</dialog>
			` : A}
		</div>
	`;
  }

  // node_modules/@nldd/design-system/dist/components/layout/split-views/navigation-split-view/navigation-split-view.js
  var __decorate48 = function(decorators, target, key, desc) {
    var c6 = arguments.length, r6 = c6 < 3 ? target : desc === null ? desc = Object.getOwnPropertyDescriptor(target, key) : desc, d3;
    if (typeof Reflect === "object" && typeof Reflect.decorate === "function") r6 = Reflect.decorate(decorators, target, key, desc);
    else for (var i8 = decorators.length - 1; i8 >= 0; i8--) if (d3 = decorators[i8]) r6 = (c6 < 3 ? d3(r6) : c6 > 3 ? d3(target, key, r6) : d3(target, key)) || r6;
    return c6 > 3 && r6 && Object.defineProperty(target, key, r6), r6;
  };
  var NLDDNavigationSplitView = class NLDDNavigationSplitView2 extends i4 {
    constructor() {
      super(...arguments);
      this.inspectorAutoHidden = false;
      this.inspectorAsSheet = false;
      this.sidebarAsSheet = false;
      this.inspectorAccessibleLabel = "Details";
      this.sidebarAccessibleLabel = "Navigatie";
      this._showSidebar = false;
      this._showSecondarySidebar = false;
      this._showMain = true;
      this._showInspector = true;
      this._mode = "spatial";
      this._resizeObserver = null;
      this._paneObserver = null;
      this._hostObserver = null;
      this._paneMinWidths = { sidebar: 320, secondarySidebar: 320, main: 480, inspector: 320 };
      this._handleDismiss = (e9) => {
        const path = e9.composedPath();
        if (path.some((el) => el === this._sidebarSheet)) {
          this.hideSidebarSheet();
        } else if (this.inspectorAutoHidden || this.inspectorAsSheet) {
          this.hideInspectorSheet();
        }
      };
    }
    get _inspectorSheet() {
      return this.shadowRoot?.querySelector(".navigation-split-view__inspector-sheet") ?? null;
    }
    get _sidebarSheet() {
      return this.shadowRoot?.querySelector(".navigation-split-view__sidebar-sheet") ?? null;
    }
    get _hasSidebar() {
      return this.querySelector(':scope > [slot="sidebar"]') !== null;
    }
    /** @internal */
    get _hasSecondarySidebar() {
      return this.querySelector(':scope > [slot="secondary-sidebar"]') !== null;
    }
    get _hasInspector() {
      return this.querySelector(':scope > [slot="inspector"]') !== null;
    }
    _paneHasContent(slot) {
      return this.querySelector(`:scope > nldd-split-view-pane[slot="${slot}"]`)?.hasAttribute("has-content") ?? false;
    }
    connectedCallback() {
      super.connectedCallback();
      this._resizeObserver = new ResizeObserver(() => this._updateLayout());
      this._resizeObserver.observe(this);
      this.addEventListener("dismiss", this._handleDismiss);
      this._paneObserver = new MutationObserver(() => this._updateLayout());
      this._hostObserver = new MutationObserver(() => {
        this._observePanes();
        this._updateLayout();
      });
      this._hostObserver.observe(this, { childList: true });
      this._observePanes();
    }
    disconnectedCallback() {
      super.disconnectedCallback();
      this._resizeObserver?.disconnect();
      this._resizeObserver = null;
      this._paneObserver?.disconnect();
      this._paneObserver = null;
      this._hostObserver?.disconnect();
      this._hostObserver = null;
      this.removeEventListener("dismiss", this._handleDismiss);
    }
    _observePanes() {
      this._paneObserver?.disconnect();
      this.querySelectorAll(":scope > nldd-split-view-pane").forEach((pane) => {
        this._paneObserver.observe(pane, {
          attributes: true,
          attributeFilter: ["has-content"]
        });
      });
    }
    firstUpdated() {
      const style = getComputedStyle(this);
      const read = (prop) => parseFloat(style.getPropertyValue(prop));
      this._paneMinWidths = {
        sidebar: read("--_sidebar-min-width") || this._paneMinWidths.sidebar,
        secondarySidebar: read("--_secondary-sidebar-min-width") || this._paneMinWidths.secondarySidebar,
        main: read("--_main-min-width") || this._paneMinWidths.main,
        inspector: read("--_inspector-min-width") || this._paneMinWidths.inspector
      };
      this._updateLayout();
    }
    updated(changed) {
      if (changed.has("inspectorAsSheet") || changed.has("sidebarAsSheet")) {
        this._updateLayout();
      }
      if (changed.has("inspectorAutoHidden") && !this.inspectorAutoHidden && !this.inspectorAsSheet) {
        this._closeSheetImmediate(this._inspectorSheet);
      }
    }
    _updateLayout() {
      const width = this.getBoundingClientRect().width;
      const { sidebar: sidebarMin, secondarySidebar: secondarySidebarMin, main: mainMin, inspector: inspectorMin } = this._paneMinWidths;
      let sidebar = this.sidebarAsSheet ? false : this._hasSidebar;
      let secondarySidebar = this.sidebarAsSheet ? false : this._hasSecondarySidebar;
      let main = true;
      let inspector = this._hasInspector;
      const requestedWidth = () => [
        sidebar && sidebarMin,
        secondarySidebar && secondarySidebarMin,
        main && mainMin,
        inspector && inspectorMin
      ].reduce((sum, v3) => sum + (v3 || 0), 0);
      if (inspector && requestedWidth() > width)
        inspector = false;
      if (sidebar && secondarySidebar && requestedWidth() > width)
        sidebar = false;
      const sidebarCollapsed = this._hasSidebar && this._hasSecondarySidebar && !sidebar && secondarySidebar;
      const fits = requestedWidth() <= width;
      let mode;
      if (sidebarCollapsed && fits) {
        mode = "sidebar-stack";
      } else if (!sidebarCollapsed && fits) {
        mode = "spatial";
      } else {
        mode = "full-stack";
      }
      if (mode === "full-stack") {
        sidebar = false;
        secondarySidebar = false;
        main = false;
        inspector = false;
        if (this._paneHasContent("main")) {
          main = true;
        } else if (this._hasSecondarySidebar && this._paneHasContent("secondary-sidebar")) {
          secondarySidebar = true;
        } else if (this._hasSidebar && this._paneHasContent("sidebar")) {
          sidebar = true;
        } else {
          main = true;
        }
      }
      if (!sidebar && !secondarySidebar && inspector && !this._paneHasContent("main")) {
        inspector = false;
      }
      this._showSidebar = sidebar;
      this._showSecondarySidebar = secondarySidebar;
      this._showMain = main;
      this._showInspector = inspector && !this.inspectorAsSheet;
      this.inspectorAutoHidden = this._hasInspector && !inspector;
      this._mode = mode;
      this.classList.toggle("full-stack", mode === "full-stack");
      this._updatePaneBackButtons();
    }
    _updatePaneBackButtons() {
      const panes = {
        sidebar: this.querySelector(':scope > nldd-split-view-pane[slot="sidebar"]'),
        secondarySidebar: this.querySelector(':scope > nldd-split-view-pane[slot="secondary-sidebar"]'),
        main: this.querySelector(':scope > nldd-split-view-pane[slot="main"]')
      };
      if (this.sidebarAsSheet) {
        panes.secondarySidebar?.removeAttribute("hide-back");
        panes.main?.setAttribute("hide-back", "");
        return;
      }
      if (this._mode === "spatial") {
        panes.secondarySidebar?.setAttribute("hide-back", "");
        panes.main?.setAttribute("hide-back", "");
        return;
      }
      if (this._mode === "sidebar-stack") {
        panes.secondarySidebar?.removeAttribute("hide-back");
        panes.main?.setAttribute("hide-back", "");
        return;
      }
      if (this._mode === "full-stack") {
        panes.secondarySidebar?.removeAttribute("hide-back");
        panes.main?.removeAttribute("hide-back");
        return;
      }
    }
    // ----------------------------------------------------------------
    // Sidebar sheet
    // ----------------------------------------------------------------
    /** Opens the sidebar as a sheet. Awaitable — resolves once the dialog is open. */
    async showSidebarSheet() {
      if (!this.sidebarAsSheet)
        return;
      await this.updateComplete;
      this._sidebarSheet?.showModal();
      this._manageSidebarSheetFocus();
    }
    hideSidebarSheet() {
      this._hideSheet(this._sidebarSheet);
    }
    _manageSidebarSheetFocus() {
      const activeSlotName = this._hasSecondarySidebar && this._paneHasContent("secondary-sidebar") ? "secondary-sidebar" : "sidebar";
      const slot = this.shadowRoot?.querySelector(`slot[name="${activeSlotName}"]`);
      const assigned = slot?.assignedElements({ flatten: true }) ?? [];
      this._manageFocusForSlot(assigned, this._sidebarSheet);
    }
    _handleSidebarSheetClick(e9) {
      if (e9.target === this._sidebarSheet)
        this.hideSidebarSheet();
    }
    _handleSidebarSheetCancel(e9) {
      e9.preventDefault();
      this.hideSidebarSheet();
    }
    // ----------------------------------------------------------------
    // Inspector sheet
    // ----------------------------------------------------------------
    /** Opens the inspector as a sheet. Awaitable — resolves once the dialog is open. */
    async showInspectorSheet() {
      if (!this.inspectorAutoHidden && !this.inspectorAsSheet)
        return;
      await this.updateComplete;
      this._inspectorSheet?.showModal();
      this._manageInspectorSheetFocus();
    }
    hideInspectorSheet() {
      this._hideSheet(this._inspectorSheet);
    }
    _manageInspectorSheetFocus() {
      const slot = this.shadowRoot?.querySelector('slot[name="inspector"]');
      const assigned = slot?.assignedElements({ flatten: true }) ?? [];
      this._manageFocusForSlot(assigned, this._inspectorSheet);
    }
    _handleInspectorSheetClick(e9) {
      if (e9.target === this._inspectorSheet)
        this.hideInspectorSheet();
    }
    _handleInspectorSheetCancel(e9) {
      e9.preventDefault();
      this.hideInspectorSheet();
    }
    // ----------------------------------------------------------------
    // Shared focus helper
    // ----------------------------------------------------------------
    _manageFocusForSlot(assigned, dialog) {
      if (assigned.some((el) => el.querySelector("[autofocus]")))
        return;
      if (!dialog)
        return;
      dialog.classList.toggle("is-pointer-focus", isPointerMode());
      dialog.focus();
    }
    _hideSheet(dialog) {
      if (!dialog?.open)
        return;
      if (dialog.dataset["closing"] === "true")
        return;
      dialog.dataset["closing"] = "true";
      dialog.classList.add("is-closing");
      dialog.addEventListener("animationend", () => {
        dialog.classList.remove("is-closing");
        delete dialog.dataset["closing"];
        dialog.close();
      }, { once: true });
      requestAnimationFrame(() => {
        if (dialog.dataset["closing"] === "true" && getComputedStyle(dialog).animationName === "none") {
          dialog.classList.remove("is-closing");
          delete dialog.dataset["closing"];
          dialog.close();
        }
      });
    }
    _closeSheetImmediate(dialog) {
      if (!dialog?.open)
        return;
      dialog.classList.remove("is-closing");
      dialog.close();
    }
    render() {
      return navigationSplitViewTemplate(this);
    }
  };
  NLDDNavigationSplitView.styles = navigationSplitViewStyles;
  __decorate48([
    n4({ type: Boolean, reflect: true, attribute: "inspector-auto-hidden" })
  ], NLDDNavigationSplitView.prototype, "inspectorAutoHidden", void 0);
  __decorate48([
    n4({ type: Boolean, reflect: true, attribute: "inspector-as-sheet" })
  ], NLDDNavigationSplitView.prototype, "inspectorAsSheet", void 0);
  __decorate48([
    n4({ type: Boolean, reflect: true, attribute: "sidebar-as-sheet" })
  ], NLDDNavigationSplitView.prototype, "sidebarAsSheet", void 0);
  __decorate48([
    n4({ type: String, attribute: "inspector-accessible-label" })
  ], NLDDNavigationSplitView.prototype, "inspectorAccessibleLabel", void 0);
  __decorate48([
    n4({ type: String, attribute: "sidebar-accessible-label" })
  ], NLDDNavigationSplitView.prototype, "sidebarAccessibleLabel", void 0);
  __decorate48([
    r5()
  ], NLDDNavigationSplitView.prototype, "_showSidebar", void 0);
  __decorate48([
    r5()
  ], NLDDNavigationSplitView.prototype, "_showSecondarySidebar", void 0);
  __decorate48([
    r5()
  ], NLDDNavigationSplitView.prototype, "_showMain", void 0);
  __decorate48([
    r5()
  ], NLDDNavigationSplitView.prototype, "_showInspector", void 0);
  NLDDNavigationSplitView = __decorate48([
    t3("nldd-navigation-split-view")
  ], NLDDNavigationSplitView);

  // node_modules/@nldd/design-system/dist/components/layout/split-views/side-by-side-split-view/side-by-side-split-view.js
  init_lit();
  init_decorators();

  // node_modules/@nldd/design-system/dist/components/layout/split-views/side-by-side-split-view/side-by-side-split-view.styles.js
  init_lit();
  var sideBySideSplitViewStyles = i`


	/* # Host */

	:host {
		--_background-color: var(--context-parent-background-color, var(--semantics-surfaces-base-background-color));
		/* Pane min-width — read by JS via getComputedStyle in firstUpdated */
		--_pane-min-width: var(--primitives-area-320);

		display: flex;
		background-color: var(--_background-color);
		width: 100%;
		height: 100%;
	}

	:host([hidden]) {
		display: none;
	}

	:host([background="base"]) {
		--context-parent-background-color: var(--semantics-surfaces-base-background-color);
		--_background-color: var(--context-parent-background-color);
	}

	:host([background="tinted"]) {
		--context-parent-background-color: var(--semantics-surfaces-tinted-background-color);
		--_background-color: var(--context-parent-background-color);
	}


	/* # Block */

	.side-by-side-split-view {
		display: flex;
		min-width: 0;
		min-height: 0;
		overflow: hidden;
		flex-direction: row;
		flex-grow: 1;
		flex-shrink: 1;
		flex-basis: 0;
	}


	/* # Elements */

	.side-by-side-split-view__pane {
		display: flex;
		min-width: var(--_pane-min-width);
		min-height: 0;
		overflow: hidden;
		flex-direction: column;
		flex-grow: 1;
		flex-shrink: 1;
		flex-basis: 0;
	}

	.side-by-side-split-view__pane[hidden] {
		display: none;
	}

	::slotted(*) {
		min-height: 0;
		flex-grow: 1;
		flex-shrink: 1;
		flex-basis: 0;
	}
`;

  // node_modules/@nldd/design-system/dist/components/layout/split-views/side-by-side-split-view/side-by-side-split-view.template.js
  init_lit();
  function sideBySideSplitViewTemplate(component) {
    const panes = Array.from({ length: component.panes }, (_2, i8) => i8 + 1);
    return b2`
		<div class="side-by-side-split-view">
			${panes.map((n7, i8) => b2`
				${i8 > 0 && i8 < component._visiblePanes ? b2`
					<nldd-split-view-divider orientation="vertical"></nldd-split-view-divider>
				` : A}
				<div
					class="side-by-side-split-view__pane"
					?hidden=${i8 >= component._visiblePanes}
				>
					<slot name="pane-${n7}"></slot>
				</div>
			`)}
		</div>
	`;
  }

  // node_modules/@nldd/design-system/dist/components/layout/split-views/side-by-side-split-view/side-by-side-split-view.js
  var __decorate49 = function(decorators, target, key, desc) {
    var c6 = arguments.length, r6 = c6 < 3 ? target : desc === null ? desc = Object.getOwnPropertyDescriptor(target, key) : desc, d3;
    if (typeof Reflect === "object" && typeof Reflect.decorate === "function") r6 = Reflect.decorate(decorators, target, key, desc);
    else for (var i8 = decorators.length - 1; i8 >= 0; i8--) if (d3 = decorators[i8]) r6 = (c6 < 3 ? d3(r6) : c6 > 3 ? d3(target, key, r6) : d3(target, key)) || r6;
    return c6 > 3 && r6 && Object.defineProperty(target, key, r6), r6;
  };
  var NLDDSideBySideSplitView = class NLDDSideBySideSplitView2 extends i4 {
    constructor() {
      super(...arguments);
      this.background = "inherit";
      this.panes = 2;
      this._visiblePanes = Infinity;
      this._paneMinWidth = 0;
      this._resizeObserver = null;
    }
    connectedCallback() {
      super.connectedCallback();
      this._resizeObserver = new ResizeObserver(() => this._updateVisiblePanes());
      this._resizeObserver.observe(this);
    }
    disconnectedCallback() {
      super.disconnectedCallback();
      this._resizeObserver?.disconnect();
      this._resizeObserver = null;
    }
    firstUpdated() {
      this._paneMinWidth = parseFloat(getComputedStyle(this).getPropertyValue("--_pane-min-width"));
      this._updateVisiblePanes();
    }
    updated(changed) {
      if (changed.has("panes")) {
        this._updateVisiblePanes();
      }
    }
    _updateVisiblePanes() {
      if (!this._paneMinWidth)
        return;
      const width = this.getBoundingClientRect().width;
      const fitting = Math.floor(width / this._paneMinWidth);
      this._visiblePanes = Math.min(this.panes, Math.max(1, fitting));
    }
    render() {
      return sideBySideSplitViewTemplate(this);
    }
  };
  NLDDSideBySideSplitView.styles = sideBySideSplitViewStyles;
  __decorate49([
    n4({ type: String, reflect: true })
  ], NLDDSideBySideSplitView.prototype, "background", void 0);
  __decorate49([
    n4({ type: Number, reflect: true })
  ], NLDDSideBySideSplitView.prototype, "panes", void 0);
  __decorate49([
    r5()
  ], NLDDSideBySideSplitView.prototype, "_visiblePanes", void 0);
  NLDDSideBySideSplitView = __decorate49([
    t3("nldd-side-by-side-split-view")
  ], NLDDSideBySideSplitView);

  // node_modules/@nldd/design-system/dist/components/layout/split-views/stacked-split-view/stacked-split-view.js
  init_lit();
  init_decorators();

  // node_modules/@nldd/design-system/dist/components/layout/split-views/stacked-split-view/stacked-split-view.styles.js
  init_lit();
  var stackedSplitViewStyles = i`


	/* # Host */

	:host {
		--_background-color: var(--context-parent-background-color, var(--semantics-surfaces-base-background-color));
		/* Pane min-height — read by JS via getComputedStyle in firstUpdated */
		--_pane-min-height: var(--primitives-area-200);

		display: flex;
		background-color: var(--_background-color);
		width: 100%;
		height: 100%;
		flex-direction: column;
	}

	:host([hidden]) {
		display: none;
	}

	:host([background="base"]) {
		--context-parent-background-color: var(--semantics-surfaces-base-background-color);
		--_background-color: var(--context-parent-background-color);
	}

	:host([background="tinted"]) {
		--context-parent-background-color: var(--semantics-surfaces-tinted-background-color);
		--_background-color: var(--context-parent-background-color);
	}


	/* # Block */

	.stacked-split-view {
		display: flex;
		min-width: 0;
		min-height: 0;
		overflow: hidden;
		flex-direction: column;
		flex-grow: 1;
		flex-shrink: 1;
		flex-basis: 0;
	}


	/* # Elements */

	.stacked-split-view__pane {
		display: flex;
		min-width: 0;
		min-height: var(--_pane-min-height);
		overflow: hidden;
		flex-direction: column;
		flex-grow: 1;
		flex-shrink: 1;
		flex-basis: 0;
	}

	.stacked-split-view__pane[hidden] {
		display: none;
	}

	::slotted(*) {
		min-height: 0;
		flex-grow: 1;
		flex-shrink: 1;
		flex-basis: 0;
	}
`;

  // node_modules/@nldd/design-system/dist/components/layout/split-views/stacked-split-view/stacked-split-view.template.js
  init_lit();
  function stackedSplitViewTemplate(component) {
    const panes = Array.from({ length: component.panes }, (_2, i8) => i8 + 1);
    return b2`
		<div class="stacked-split-view">
			${panes.map((n7, i8) => b2`
				${i8 > 0 && i8 < component._visiblePanes ? b2`
					<nldd-split-view-divider orientation="horizontal"></nldd-split-view-divider>
				` : A}
				<div
					class="stacked-split-view__pane"
					?hidden=${i8 >= component._visiblePanes}
				>
					<slot name="pane-${n7}"></slot>
				</div>
			`)}
		</div>
	`;
  }

  // node_modules/@nldd/design-system/dist/components/layout/split-views/stacked-split-view/stacked-split-view.js
  var __decorate50 = function(decorators, target, key, desc) {
    var c6 = arguments.length, r6 = c6 < 3 ? target : desc === null ? desc = Object.getOwnPropertyDescriptor(target, key) : desc, d3;
    if (typeof Reflect === "object" && typeof Reflect.decorate === "function") r6 = Reflect.decorate(decorators, target, key, desc);
    else for (var i8 = decorators.length - 1; i8 >= 0; i8--) if (d3 = decorators[i8]) r6 = (c6 < 3 ? d3(r6) : c6 > 3 ? d3(target, key, r6) : d3(target, key)) || r6;
    return c6 > 3 && r6 && Object.defineProperty(target, key, r6), r6;
  };
  var NLDDStackedSplitView = class NLDDStackedSplitView2 extends i4 {
    constructor() {
      super(...arguments);
      this.background = "inherit";
      this.panes = 2;
      this._visiblePanes = Infinity;
      this._paneMinHeight = 0;
      this._resizeObserver = null;
    }
    connectedCallback() {
      super.connectedCallback();
      this._resizeObserver = new ResizeObserver(() => this._updateVisiblePanes());
      this._resizeObserver.observe(this);
    }
    disconnectedCallback() {
      super.disconnectedCallback();
      this._resizeObserver?.disconnect();
      this._resizeObserver = null;
    }
    firstUpdated() {
      this._paneMinHeight = parseFloat(getComputedStyle(this).getPropertyValue("--_pane-min-height"));
      this._updateVisiblePanes();
    }
    updated(changed) {
      if (changed.has("panes")) {
        this._updateVisiblePanes();
      }
    }
    _updateVisiblePanes() {
      if (!this._paneMinHeight)
        return;
      const height = this.getBoundingClientRect().height;
      const fitting = Math.floor(height / this._paneMinHeight);
      this._visiblePanes = Math.min(this.panes, Math.max(1, fitting));
    }
    render() {
      return stackedSplitViewTemplate(this);
    }
  };
  NLDDStackedSplitView.styles = stackedSplitViewStyles;
  __decorate50([
    n4({ type: String, reflect: true })
  ], NLDDStackedSplitView.prototype, "background", void 0);
  __decorate50([
    n4({ type: Number, reflect: true })
  ], NLDDStackedSplitView.prototype, "panes", void 0);
  __decorate50([
    r5()
  ], NLDDStackedSplitView.prototype, "_visiblePanes", void 0);
  NLDDStackedSplitView = __decorate50([
    t3("nldd-stacked-split-view")
  ], NLDDStackedSplitView);

  // node_modules/@nldd/design-system/dist/components/layout/split-views/split-view-pane/split-view-pane.js
  init_lit();
  init_decorators();

  // node_modules/@nldd/design-system/dist/components/layout/split-views/split-view-pane/split-view-pane.styles.js
  init_lit();
  init_breakpoints();
  var mdMin5 = r(breakpoints.mdMin);
  var splitViewPaneStyles = i`


	/* # Host */

	:host {
		--_background-color: var(--context-parent-background-color, var(--semantics-surfaces-base-background-color));

		display: flex;
		width: 100%;
		height: 100%;

		@media (min-width: ${mdMin5}) {
			background-color: var(--_background-color);
		}
	}

	:host([hidden]) {
		display: none;
	}

	:host([background="base"]) {
		--context-parent-background-color: var(--semantics-surfaces-base-background-color);
		--_background-color: var(--context-parent-background-color);
	}

	:host([background="tinted"]) {
		--context-parent-background-color: var(--semantics-surfaces-tinted-background-color);
		--_background-color: var(--context-parent-background-color);
	}

	:host([hide-back]) {
		--context-back-button-display: none;
	}


	/* # Block */

	/* flex-basis: auto (not 0) so the pane works in both determinate
	 * parents (main column — grows to fill via flex-grow) AND
	 * indeterminate parents (a bar — auto = content size, no growth
	 * space, stays compact). The old 'flex: 1' shorthand expanded to
	 * 'flex-basis: 0%' which the spec resolves to 'auto' in indeterminate
	 * parents; the longhand conversion lost that nuance and collapsed
	 * panes inside bars. */
	.split-view-pane {
		display: flex;
		min-width: 0;
		min-height: 0;
		overflow: hidden;
		flex-direction: column;
		flex-grow: 1;
		flex-shrink: 1;
		flex-basis: auto;
	}


	/* # Elements */

	::slotted(*) {
		min-height: 0;
		flex-grow: 1;
		flex-shrink: 1;
		flex-basis: auto;
	}
`;

  // node_modules/@nldd/design-system/dist/components/layout/split-views/split-view-pane/split-view-pane.template.js
  init_lit();
  function splitViewPaneTemplate(_component) {
    return b2`
		<div class="split-view-pane">
			<slot></slot>
		</div>
	`;
  }

  // node_modules/@nldd/design-system/dist/components/layout/split-views/split-view-pane/split-view-pane.js
  var __decorate51 = function(decorators, target, key, desc) {
    var c6 = arguments.length, r6 = c6 < 3 ? target : desc === null ? desc = Object.getOwnPropertyDescriptor(target, key) : desc, d3;
    if (typeof Reflect === "object" && typeof Reflect.decorate === "function") r6 = Reflect.decorate(decorators, target, key, desc);
    else for (var i8 = decorators.length - 1; i8 >= 0; i8--) if (d3 = decorators[i8]) r6 = (c6 < 3 ? d3(r6) : c6 > 3 ? d3(target, key, r6) : d3(target, key)) || r6;
    return c6 > 3 && r6 && Object.defineProperty(target, key, r6), r6;
  };
  var NLDDSplitViewPane = class NLDDSplitViewPane2 extends i4 {
    constructor() {
      super(...arguments);
      this.hasContent = false;
      this.hideBack = false;
      this.background = "inherit";
    }
    connectedCallback() {
      super.connectedCallback();
      this.style.containerType = "inline-size";
      this.style.containerName = "layout-container";
    }
    render() {
      return splitViewPaneTemplate(this);
    }
  };
  NLDDSplitViewPane.styles = splitViewPaneStyles;
  __decorate51([
    n4({ type: Boolean, reflect: true, attribute: "has-content" })
  ], NLDDSplitViewPane.prototype, "hasContent", void 0);
  __decorate51([
    n4({ type: Boolean, reflect: true, attribute: "hide-back" })
  ], NLDDSplitViewPane.prototype, "hideBack", void 0);
  __decorate51([
    n4({ type: String, reflect: true })
  ], NLDDSplitViewPane.prototype, "background", void 0);
  NLDDSplitViewPane = __decorate51([
    t3("nldd-split-view-pane")
  ], NLDDSplitViewPane);

  // node_modules/@nldd/design-system/dist/components/index.js
  init_page();
  init_simple_section();

  // node_modules/@nldd/design-system/dist/components/layout/page-sections/full-bleed-section/full-bleed-section.js
  init_lit();
  init_decorators();
  init_page_section_mixin();

  // node_modules/@nldd/design-system/dist/components/layout/page-sections/full-bleed-section/full-bleed-section.styles.js
  init_lit();
  init_breakpoints();
  var smMax5 = r(breakpoints.smMax);
  var mdMin7 = r(breakpoints.mdMin);
  var mdMax4 = r(breakpoints.mdMax);
  var lgMin4 = r(breakpoints.lgMin);
  var fullBleedSectionStyles = i`


	/* # Host */

	:host {
		container-type: inline-size;
		/* Block-padding overrides from PageSectionMixin; 'initial' lets the
		   block fall back to the responsive default until the mixin sets one. */
		--_padding-top: initial;
		--_padding-bottom: initial;
		--_sm-padding-top: initial;
		--_sm-padding-bottom: initial;
		--_md-padding-top: initial;
		--_md-padding-bottom: initial;
		--_lg-padding-top: initial;
		--_lg-padding-bottom: initial;
		--_max-width: var(--semantics-page-sections-body-max-width);

		display: flex;
		width: 100%;
		flex-direction: column;
		align-items: center;
	}

	:host([hidden]) {
		display: none;
	}

	:host([width="full"]) {
		--_max-width: none;
	}


	/* # Block */

	.full-bleed-section {
		box-sizing: border-box;
		display: flex;
		width: 100%;
		flex-direction: column;
		flex-grow: 1;
		align-items: center;


		@container (max-width: ${smMax5}) {
			padding-top: var(--_sm-padding-top, var(--_padding-top, var(--semantics-page-sections-sm-margin-block)));
			padding-bottom: var(--_sm-padding-bottom, var(--_padding-bottom, var(--semantics-page-sections-sm-margin-block)));
		}

		@container (min-width: ${mdMin7}) and (max-width: ${mdMax4}) {
			padding-top: var(--_md-padding-top, var(--_padding-top, var(--semantics-page-sections-md-margin-block)));
			padding-bottom: var(--_md-padding-bottom, var(--_padding-bottom, var(--semantics-page-sections-md-margin-block)));
		}

		@container (min-width: ${lgMin4}) {
			padding-top: var(--_lg-padding-top, var(--_padding-top, var(--semantics-page-sections-lg-margin-block)));
			padding-bottom: var(--_lg-padding-bottom, var(--_padding-bottom, var(--semantics-page-sections-lg-margin-block)));
		}
	}


	/* # Elements */

	.full-bleed-section__body {
		display: flex;
		width: 100%;
		max-width: var(--_max-width);
		flex-direction: column;
		flex-grow: 1;

		@container (max-width: ${smMax5}) {
			gap: var(--semantics-page-sections-sm-gap);
		}

		@container (min-width: ${mdMin7}) and (max-width: ${mdMax4}) {
			gap: var(--semantics-page-sections-md-gap);
		}

		@container (min-width: ${lgMin4}) {
			gap: var(--semantics-page-sections-lg-gap);
		}
	}

	.full-bleed-section__header[hidden] {
		display: none;
	}

	.full-bleed-section__main {
		display: flex;
		flex-direction: column;
		flex-grow: 1;
	}

	.full-bleed-section__footer[hidden] {
		display: none;
	}
`;

  // node_modules/@nldd/design-system/dist/components/layout/page-sections/full-bleed-section/full-bleed-section.template.js
  init_lit();
  function fullBleedSectionTemplate(component) {
    return b2`
		<section class="full-bleed-section">
			<div class="full-bleed-section__body">
				<header class="full-bleed-section__header" hidden>
					<slot name="header" @slotchange=${component._onSlotChange}></slot>
				</header>
				<div class="full-bleed-section__main">
					<slot></slot>
				</div>
				<footer class="full-bleed-section__footer" hidden>
					<slot name="footer" @slotchange=${component._onSlotChange}></slot>
				</footer>
			</div>
		</section>
	`;
  }

  // node_modules/@nldd/design-system/dist/components/layout/page-sections/full-bleed-section/full-bleed-section.js
  var __decorate55 = function(decorators, target, key, desc) {
    var c6 = arguments.length, r6 = c6 < 3 ? target : desc === null ? desc = Object.getOwnPropertyDescriptor(target, key) : desc, d3;
    if (typeof Reflect === "object" && typeof Reflect.decorate === "function") r6 = Reflect.decorate(decorators, target, key, desc);
    else for (var i8 = decorators.length - 1; i8 >= 0; i8--) if (d3 = decorators[i8]) r6 = (c6 < 3 ? d3(r6) : c6 > 3 ? d3(target, key, r6) : d3(target, key)) || r6;
    return c6 > 3 && r6 && Object.defineProperty(target, key, r6), r6;
  };
  var NLDDFullBleedSection = class NLDDFullBleedSection2 extends PageSectionMixin(i4) {
    constructor() {
      super(...arguments);
      this.width = "";
    }
    updated(changedProperties) {
      super.updated(changedProperties);
      if (changedProperties.has("width")) {
        const w2 = this.width;
        if (w2 && w2 !== "full" && CSS.supports("max-width", w2)) {
          this.style.setProperty("--_max-width", w2);
        } else {
          this.style.removeProperty("--_max-width");
        }
      }
    }
    render() {
      return fullBleedSectionTemplate(this);
    }
  };
  NLDDFullBleedSection.styles = fullBleedSectionStyles;
  __decorate55([
    n4({ type: String, reflect: true })
  ], NLDDFullBleedSection.prototype, "width", void 0);
  NLDDFullBleedSection = __decorate55([
    t3("nldd-full-bleed-section")
  ], NLDDFullBleedSection);

  // node_modules/@nldd/design-system/dist/components/layout/page-sections/one-third-two-thirds-section/one-third-two-thirds-section.js
  init_lit();
  init_decorators();
  init_page_section_mixin();

  // node_modules/@nldd/design-system/dist/components/layout/page-sections/one-third-two-thirds-section/one-third-two-thirds-section.styles.js
  init_lit();
  init_breakpoints();
  var smMax6 = r(breakpoints.smMax);
  var mdMin8 = r(breakpoints.mdMin);
  var mdMax5 = r(breakpoints.mdMax);
  var lgMin5 = r(breakpoints.lgMin);
  var oneThirdTwoThirdsSectionStyles = i`


	/* # Host */

	:host {
		container-type: inline-size;
		/* Block-padding overrides from PageSectionMixin; 'initial' lets the
		   block fall back to the responsive default until the mixin sets one. */
		--_padding-top: initial;
		--_padding-bottom: initial;
		--_sm-padding-top: initial;
		--_sm-padding-bottom: initial;
		--_md-padding-top: initial;
		--_md-padding-bottom: initial;
		--_lg-padding-top: initial;
		--_lg-padding-bottom: initial;
		--_max-width: var(--semantics-page-sections-body-max-width);

		display: flex;
		width: 100%;
		flex-direction: column;
		align-items: center;
	}

	:host([hidden]) {
		display: none;
	}

	:host([width="full"]) {
		--_max-width: none;
	}


	/* # Block */

	.one-third-two-thirds-section {
		box-sizing: border-box;
		display: flex;
		width: 100%;
		flex-direction: column;
		align-items: center;


		@container (max-width: ${smMax6}) {
			padding-inline: var(--semantics-page-sections-sm-margin-inline);
			padding-top: var(--_sm-padding-top, var(--_padding-top, var(--semantics-page-sections-sm-margin-block)));
			padding-bottom: var(--_sm-padding-bottom, var(--_padding-bottom, var(--semantics-page-sections-sm-margin-block)));
		}

		@container (min-width: ${mdMin8}) and (max-width: ${mdMax5}) {
			padding-inline: var(--semantics-page-sections-md-margin-inline);
			padding-top: var(--_md-padding-top, var(--_padding-top, var(--semantics-page-sections-md-margin-block)));
			padding-bottom: var(--_md-padding-bottom, var(--_padding-bottom, var(--semantics-page-sections-md-margin-block)));
		}

		@container (min-width: ${lgMin5}) {
			padding-inline: var(--semantics-page-sections-lg-margin-inline);
			padding-top: var(--_lg-padding-top, var(--_padding-top, var(--semantics-page-sections-lg-margin-block)));
			padding-bottom: var(--_lg-padding-bottom, var(--_padding-bottom, var(--semantics-page-sections-lg-margin-block)));
		}
	}


	/* # Elements */

	.one-third-two-thirds-section__body {
		display: flex;
		width: 100%;
		max-width: var(--_max-width);
		flex-direction: column;

		@container (max-width: ${smMax6}) {
			gap: var(--semantics-page-sections-sm-gap);
		}

		@container (min-width: ${mdMin8}) and (max-width: ${mdMax5}) {
			gap: var(--semantics-page-sections-md-gap);
		}

		@container (min-width: ${lgMin5}) {
			gap: var(--semantics-page-sections-lg-gap);
		}
	}

	.one-third-two-thirds-section__header[hidden],
	.one-third-two-thirds-section__footer[hidden] {
		display: none;
	}

	.one-third-two-thirds-section__columns {
		display: flex;
		flex-wrap: wrap;

		/* Below ~768px the 2/3 column would shrink under 400px and read as
		   two near-equal columns; stack to a single column instead. nowrap
		   stops the column-direction wrap container from stretching the
		   shorter column to fill height. */
		@container (max-width: 768px) {
			flex-direction: column;
			flex-wrap: nowrap;
		}

		@container (max-width: ${smMax6}) {
			gap: var(--semantics-page-sections-sm-gap);
		}

		@container (min-width: ${mdMin8}) and (max-width: ${mdMax5}) {
			gap: var(--semantics-page-sections-md-gap);
		}

		@container (min-width: ${lgMin5}) {
			gap: var(--semantics-page-sections-lg-gap);
		}
	}

	.one-third-two-thirds-section__left-column {
		min-width: var(--primitives-area-280);
		flex-grow: 1;
		flex-shrink: 1;
		flex-basis: 0;
	}

	.one-third-two-thirds-section__right-column {
		min-width: var(--primitives-area-280);
		flex-grow: 2;
		flex-shrink: 1;
		flex-basis: 0;
	}
`;

  // node_modules/@nldd/design-system/dist/components/layout/page-sections/one-third-two-thirds-section/one-third-two-thirds-section.template.js
  init_lit();
  function oneThirdTwoThirdsSectionTemplate(component) {
    return b2`
		<section class="one-third-two-thirds-section">
			<div class="one-third-two-thirds-section__body">
				<header class="one-third-two-thirds-section__header" hidden>
					<slot name="header" @slotchange=${component._onSlotChange}></slot>
				</header>
				<div class="one-third-two-thirds-section__columns">
					<div class="one-third-two-thirds-section__left-column">
						<slot name="left"></slot>
					</div>
					<div class="one-third-two-thirds-section__right-column">
						<slot></slot>
						<slot name="right"></slot>
					</div>
				</div>
				<footer class="one-third-two-thirds-section__footer" hidden>
					<slot name="footer" @slotchange=${component._onSlotChange}></slot>
				</footer>
			</div>
		</section>
	`;
  }

  // node_modules/@nldd/design-system/dist/components/layout/page-sections/one-third-two-thirds-section/one-third-two-thirds-section.js
  var __decorate56 = function(decorators, target, key, desc) {
    var c6 = arguments.length, r6 = c6 < 3 ? target : desc === null ? desc = Object.getOwnPropertyDescriptor(target, key) : desc, d3;
    if (typeof Reflect === "object" && typeof Reflect.decorate === "function") r6 = Reflect.decorate(decorators, target, key, desc);
    else for (var i8 = decorators.length - 1; i8 >= 0; i8--) if (d3 = decorators[i8]) r6 = (c6 < 3 ? d3(r6) : c6 > 3 ? d3(target, key, r6) : d3(target, key)) || r6;
    return c6 > 3 && r6 && Object.defineProperty(target, key, r6), r6;
  };
  var NLDDOneThirdTwoThirdsSection = class NLDDOneThirdTwoThirdsSection2 extends PageSectionMixin(i4) {
    constructor() {
      super(...arguments);
      this.width = "";
    }
    updated(changedProperties) {
      super.updated(changedProperties);
      if (changedProperties.has("width")) {
        const w2 = this.width;
        if (w2 && w2 !== "full" && CSS.supports("max-width", w2)) {
          this.style.setProperty("--_max-width", w2);
        } else {
          this.style.removeProperty("--_max-width");
        }
      }
    }
    render() {
      return oneThirdTwoThirdsSectionTemplate(this);
    }
  };
  NLDDOneThirdTwoThirdsSection.styles = oneThirdTwoThirdsSectionStyles;
  __decorate56([
    n4({ type: String, reflect: true })
  ], NLDDOneThirdTwoThirdsSection.prototype, "width", void 0);
  NLDDOneThirdTwoThirdsSection = __decorate56([
    t3("nldd-one-third-two-thirds-section")
  ], NLDDOneThirdTwoThirdsSection);

  // node_modules/@nldd/design-system/dist/components/layout/page-sections/two-thirds-one-third-section/two-thirds-one-third-section.js
  init_lit();
  init_decorators();
  init_page_section_mixin();

  // node_modules/@nldd/design-system/dist/components/layout/page-sections/two-thirds-one-third-section/two-thirds-one-third-section.styles.js
  init_lit();
  init_breakpoints();
  var smMax7 = r(breakpoints.smMax);
  var mdMin9 = r(breakpoints.mdMin);
  var mdMax6 = r(breakpoints.mdMax);
  var lgMin6 = r(breakpoints.lgMin);
  var twoThirdsOneThirdSectionStyles = i`


	/* # Host */

	:host {
		container-type: inline-size;
		/* Block-padding overrides from PageSectionMixin; 'initial' lets the
		   block fall back to the responsive default until the mixin sets one. */
		--_padding-top: initial;
		--_padding-bottom: initial;
		--_sm-padding-top: initial;
		--_sm-padding-bottom: initial;
		--_md-padding-top: initial;
		--_md-padding-bottom: initial;
		--_lg-padding-top: initial;
		--_lg-padding-bottom: initial;
		--_max-width: var(--semantics-page-sections-body-max-width);

		display: flex;
		width: 100%;
		flex-direction: column;
		align-items: center;
	}

	:host([hidden]) {
		display: none;
	}

	:host([width="full"]) {
		--_max-width: none;
	}


	/* # Block */

	.two-thirds-one-third-section {
		box-sizing: border-box;
		display: flex;
		width: 100%;
		flex-direction: column;
		align-items: center;


		@container (max-width: ${smMax7}) {
			padding-inline: var(--semantics-page-sections-sm-margin-inline);
			padding-top: var(--_sm-padding-top, var(--_padding-top, var(--semantics-page-sections-sm-margin-block)));
			padding-bottom: var(--_sm-padding-bottom, var(--_padding-bottom, var(--semantics-page-sections-sm-margin-block)));
		}

		@container (min-width: ${mdMin9}) and (max-width: ${mdMax6}) {
			padding-inline: var(--semantics-page-sections-md-margin-inline);
			padding-top: var(--_md-padding-top, var(--_padding-top, var(--semantics-page-sections-md-margin-block)));
			padding-bottom: var(--_md-padding-bottom, var(--_padding-bottom, var(--semantics-page-sections-md-margin-block)));
		}

		@container (min-width: ${lgMin6}) {
			padding-inline: var(--semantics-page-sections-lg-margin-inline);
			padding-top: var(--_lg-padding-top, var(--_padding-top, var(--semantics-page-sections-lg-margin-block)));
			padding-bottom: var(--_lg-padding-bottom, var(--_padding-bottom, var(--semantics-page-sections-lg-margin-block)));
		}
	}


	/* # Elements */

	.two-thirds-one-third-section__body {
		display: flex;
		width: 100%;
		max-width: var(--_max-width);
		flex-direction: column;

		@container (max-width: ${smMax7}) {
			gap: var(--semantics-page-sections-sm-gap);
		}

		@container (min-width: ${mdMin9}) and (max-width: ${mdMax6}) {
			gap: var(--semantics-page-sections-md-gap);
		}

		@container (min-width: ${lgMin6}) {
			gap: var(--semantics-page-sections-lg-gap);
		}
	}

	.two-thirds-one-third-section__header[hidden],
	.two-thirds-one-third-section__footer[hidden] {
		display: none;
	}

	.two-thirds-one-third-section__columns {
		display: flex;
		flex-wrap: wrap;

		/* Below ~768px the 2/3 column would shrink under 400px and read as
		   two near-equal columns; stack to a single column instead. nowrap
		   stops the column-direction wrap container from stretching the
		   shorter column to fill height. */
		@container (max-width: 768px) {
			flex-direction: column;
			flex-wrap: nowrap;
		}

		@container (max-width: ${smMax7}) {
			gap: var(--semantics-page-sections-sm-gap);
		}

		@container (min-width: ${mdMin9}) and (max-width: ${mdMax6}) {
			gap: var(--semantics-page-sections-md-gap);
		}

		@container (min-width: ${lgMin6}) {
			gap: var(--semantics-page-sections-lg-gap);
		}
	}

	.two-thirds-one-third-section__left-column {
		min-width: var(--primitives-area-280);
		flex-grow: 2;
		flex-shrink: 1;
		flex-basis: 0;
	}

	.two-thirds-one-third-section__right-column {
		min-width: var(--primitives-area-280);
		flex-grow: 1;
		flex-shrink: 1;
		flex-basis: 0;
	}
`;

  // node_modules/@nldd/design-system/dist/components/layout/page-sections/two-thirds-one-third-section/two-thirds-one-third-section.template.js
  init_lit();
  function twoThirdsOneThirdSectionTemplate(component) {
    return b2`
		<section class="two-thirds-one-third-section">
			<div class="two-thirds-one-third-section__body">
				<header class="two-thirds-one-third-section__header" hidden>
					<slot name="header" @slotchange=${component._onSlotChange}></slot>
				</header>
				<div class="two-thirds-one-third-section__columns">
					<div class="two-thirds-one-third-section__left-column">
						<slot></slot>
						<slot name="left"></slot>
					</div>
					<div class="two-thirds-one-third-section__right-column">
						<slot name="right"></slot>
					</div>
				</div>
				<footer class="two-thirds-one-third-section__footer" hidden>
					<slot name="footer" @slotchange=${component._onSlotChange}></slot>
				</footer>
			</div>
		</section>
	`;
  }

  // node_modules/@nldd/design-system/dist/components/layout/page-sections/two-thirds-one-third-section/two-thirds-one-third-section.js
  var __decorate57 = function(decorators, target, key, desc) {
    var c6 = arguments.length, r6 = c6 < 3 ? target : desc === null ? desc = Object.getOwnPropertyDescriptor(target, key) : desc, d3;
    if (typeof Reflect === "object" && typeof Reflect.decorate === "function") r6 = Reflect.decorate(decorators, target, key, desc);
    else for (var i8 = decorators.length - 1; i8 >= 0; i8--) if (d3 = decorators[i8]) r6 = (c6 < 3 ? d3(r6) : c6 > 3 ? d3(target, key, r6) : d3(target, key)) || r6;
    return c6 > 3 && r6 && Object.defineProperty(target, key, r6), r6;
  };
  var NLDDTwoThirdsOneThirdSection = class NLDDTwoThirdsOneThirdSection2 extends PageSectionMixin(i4) {
    constructor() {
      super(...arguments);
      this.width = "";
    }
    updated(changedProperties) {
      super.updated(changedProperties);
      if (changedProperties.has("width")) {
        const w2 = this.width;
        if (w2 && w2 !== "full" && CSS.supports("max-width", w2)) {
          this.style.setProperty("--_max-width", w2);
        } else {
          this.style.removeProperty("--_max-width");
        }
      }
    }
    render() {
      return twoThirdsOneThirdSectionTemplate(this);
    }
  };
  NLDDTwoThirdsOneThirdSection.styles = twoThirdsOneThirdSectionStyles;
  __decorate57([
    n4({ type: String, reflect: true })
  ], NLDDTwoThirdsOneThirdSection.prototype, "width", void 0);
  NLDDTwoThirdsOneThirdSection = __decorate57([
    t3("nldd-two-thirds-one-third-section")
  ], NLDDTwoThirdsOneThirdSection);

  // node_modules/@nldd/design-system/dist/components/layout/page-sections/one-half-one-half-section/one-half-one-half-section.js
  init_lit();
  init_decorators();
  init_page_section_mixin();

  // node_modules/@nldd/design-system/dist/components/layout/page-sections/one-half-one-half-section/one-half-one-half-section.styles.js
  init_lit();
  init_breakpoints();
  var smMax8 = r(breakpoints.smMax);
  var mdMin10 = r(breakpoints.mdMin);
  var mdMax7 = r(breakpoints.mdMax);
  var lgMin7 = r(breakpoints.lgMin);
  var oneHalfOneHalfSectionStyles = i`


	/* # Host */

	:host {
		container-type: inline-size;
		/* Block-padding overrides from PageSectionMixin; 'initial' lets the
		   block fall back to the responsive default until the mixin sets one. */
		--_padding-top: initial;
		--_padding-bottom: initial;
		--_sm-padding-top: initial;
		--_sm-padding-bottom: initial;
		--_md-padding-top: initial;
		--_md-padding-bottom: initial;
		--_lg-padding-top: initial;
		--_lg-padding-bottom: initial;
		--_max-width: var(--semantics-page-sections-body-max-width);

		display: flex;
		width: 100%;
		flex-direction: column;
		align-items: center;
	}

	:host([hidden]) {
		display: none;
	}

	:host([width="full"]) {
		--_max-width: none;
	}


	/* # Block */

	.one-half-one-half-section {
		box-sizing: border-box;
		display: flex;
		width: 100%;
		flex-direction: column;
		align-items: center;


		@container (max-width: ${smMax8}) {
			padding-inline: var(--semantics-page-sections-sm-margin-inline);
			padding-top: var(--_sm-padding-top, var(--_padding-top, var(--semantics-page-sections-sm-margin-block)));
			padding-bottom: var(--_sm-padding-bottom, var(--_padding-bottom, var(--semantics-page-sections-sm-margin-block)));
		}

		@container (min-width: ${mdMin10}) and (max-width: ${mdMax7}) {
			padding-inline: var(--semantics-page-sections-md-margin-inline);
			padding-top: var(--_md-padding-top, var(--_padding-top, var(--semantics-page-sections-md-margin-block)));
			padding-bottom: var(--_md-padding-bottom, var(--_padding-bottom, var(--semantics-page-sections-md-margin-block)));
		}

		@container (min-width: ${lgMin7}) {
			padding-inline: var(--semantics-page-sections-lg-margin-inline);
			padding-top: var(--_lg-padding-top, var(--_padding-top, var(--semantics-page-sections-lg-margin-block)));
			padding-bottom: var(--_lg-padding-bottom, var(--_padding-bottom, var(--semantics-page-sections-lg-margin-block)));
		}
	}


	/* # Elements */

	.one-half-one-half-section__body {
		display: flex;
		width: 100%;
		max-width: var(--_max-width);
		flex-direction: column;

		@container (max-width: ${smMax8}) {
			gap: var(--semantics-page-sections-sm-gap);
		}

		@container (min-width: ${mdMin10}) and (max-width: ${mdMax7}) {
			gap: var(--semantics-page-sections-md-gap);
		}

		@container (min-width: ${lgMin7}) {
			gap: var(--semantics-page-sections-lg-gap);
		}
	}

	.one-half-one-half-section__header[hidden],
	.one-half-one-half-section__footer[hidden] {
		display: none;
	}

	.one-half-one-half-section__columns {
		display: flex;
		flex-wrap: wrap;

		@container (max-width: ${smMax8}) {
			gap: var(--semantics-page-sections-sm-gap);
		}

		@container (min-width: ${mdMin10}) and (max-width: ${mdMax7}) {
			gap: var(--semantics-page-sections-md-gap);
		}

		@container (min-width: ${lgMin7}) {
			gap: var(--semantics-page-sections-lg-gap);
		}
	}

	.one-half-one-half-section__left-column {
		min-width: var(--primitives-area-280);
		flex-grow: 1;
		flex-shrink: 1;
		flex-basis: 0;
	}

	.one-half-one-half-section__right-column {
		min-width: var(--primitives-area-280);
		flex-grow: 1;
		flex-shrink: 1;
		flex-basis: 0;
	}
`;

  // node_modules/@nldd/design-system/dist/components/layout/page-sections/one-half-one-half-section/one-half-one-half-section.template.js
  init_lit();
  function oneHalfOneHalfSectionTemplate(component) {
    return b2`
		<section class="one-half-one-half-section">
			<div class="one-half-one-half-section__body">
				<header class="one-half-one-half-section__header" hidden>
					<slot name="header" @slotchange=${component._onSlotChange}></slot>
				</header>
				<div class="one-half-one-half-section__columns">
					<div class="one-half-one-half-section__left-column">
						<slot></slot>
						<slot name="left"></slot>
					</div>
					<div class="one-half-one-half-section__right-column">
						<slot name="right"></slot>
					</div>
				</div>
				<footer class="one-half-one-half-section__footer" hidden>
					<slot name="footer" @slotchange=${component._onSlotChange}></slot>
				</footer>
			</div>
		</section>
	`;
  }

  // node_modules/@nldd/design-system/dist/components/layout/page-sections/one-half-one-half-section/one-half-one-half-section.js
  var __decorate58 = function(decorators, target, key, desc) {
    var c6 = arguments.length, r6 = c6 < 3 ? target : desc === null ? desc = Object.getOwnPropertyDescriptor(target, key) : desc, d3;
    if (typeof Reflect === "object" && typeof Reflect.decorate === "function") r6 = Reflect.decorate(decorators, target, key, desc);
    else for (var i8 = decorators.length - 1; i8 >= 0; i8--) if (d3 = decorators[i8]) r6 = (c6 < 3 ? d3(r6) : c6 > 3 ? d3(target, key, r6) : d3(target, key)) || r6;
    return c6 > 3 && r6 && Object.defineProperty(target, key, r6), r6;
  };
  var NLDDOneHalfOneHalfSection = class NLDDOneHalfOneHalfSection2 extends PageSectionMixin(i4) {
    constructor() {
      super(...arguments);
      this.width = "";
    }
    updated(changedProperties) {
      super.updated(changedProperties);
      if (changedProperties.has("width")) {
        const w2 = this.width;
        if (w2 && w2 !== "full" && CSS.supports("max-width", w2)) {
          this.style.setProperty("--_max-width", w2);
        } else {
          this.style.removeProperty("--_max-width");
        }
      }
    }
    render() {
      return oneHalfOneHalfSectionTemplate(this);
    }
  };
  NLDDOneHalfOneHalfSection.styles = oneHalfOneHalfSectionStyles;
  __decorate58([
    n4({ type: String, reflect: true })
  ], NLDDOneHalfOneHalfSection.prototype, "width", void 0);
  NLDDOneHalfOneHalfSection = __decorate58([
    t3("nldd-one-half-one-half-section")
  ], NLDDOneHalfOneHalfSection);

  // node_modules/@nldd/design-system/dist/components/layout/box/box.js
  init_lit();
  init_decorators();

  // node_modules/@nldd/design-system/dist/components/layout/box/box.styles.js
  init_lit();
  var boxStyles = i`


	/* # Host */

	:host {
		--_background-color: var(--components-box-background-color);
		--_border-color: var(--components-box-border-color);

		display: block;
	}

	:host([hidden]) {
		display: none;
	}


	/* ## Background variants
	   Default is tinted (matches :host above). base = card-on-tinted, the
	   border ring picks the +2-step semantic so the frame still reads
	   against an already-tinted parent. */

	:host([background="tinted"]) {
		--_background-color: var(--components-box-background-color);
		--_border-color: var(--components-box-border-color);
	}

	:host([background="base"]) {
		--_background-color: var(--components-box-base-background-color);
		--_border-color: var(--components-box-base-border-color);
	}


	/* # Block */

	.box {
		box-sizing: border-box;
		border-radius: var(--components-box-corner-radius);
		/* Inner box-shadow paints a 1px border ring inside the radius
		   without taking layout space. The color is one to two palette
		   steps above the surface bg depending on the variant, giving the
		   box a defined edge that reads as "subtle dimensional lift"
		   rather than a hard border. */
		box-shadow: inset 0 0 0 1px var(--_border-color);
		background-color: var(--_background-color);
		padding: var(--components-box-padding);
	}


	/* # Accessibility
	   box-shadow is dropped by Windows High Contrast / forced-colors mode,
	   so the border ring would disappear and the box would lose its
	   defined edge against the system background. Restore the edge with a
	   real border in that mode. CanvasText is the system foreground color
	   so the border always meets contrast. */

	@media (forced-colors: active) {
		.box {
			border: 1px solid CanvasText;
		}
	}
`;

  // node_modules/@nldd/design-system/dist/components/layout/box/box.template.js
  init_lit();
  function boxTemplate(_component) {
    return b2`
		<div class="box">
			<slot></slot>
		</div>
	`;
  }

  // node_modules/@nldd/design-system/dist/components/layout/box/box.js
  var __decorate59 = function(decorators, target, key, desc) {
    var c6 = arguments.length, r6 = c6 < 3 ? target : desc === null ? desc = Object.getOwnPropertyDescriptor(target, key) : desc, d3;
    if (typeof Reflect === "object" && typeof Reflect.decorate === "function") r6 = Reflect.decorate(decorators, target, key, desc);
    else for (var i8 = decorators.length - 1; i8 >= 0; i8--) if (d3 = decorators[i8]) r6 = (c6 < 3 ? d3(r6) : c6 > 3 ? d3(target, key, r6) : d3(target, key)) || r6;
    return c6 > 3 && r6 && Object.defineProperty(target, key, r6), r6;
  };
  var NLDDBox = class NLDDBox2 extends i4 {
    constructor() {
      super(...arguments);
      this.background = "tinted";
    }
    render() {
      return boxTemplate(this);
    }
  };
  NLDDBox.styles = boxStyles;
  __decorate59([
    n4({ reflect: true })
  ], NLDDBox.prototype, "background", void 0);
  NLDDBox = __decorate59([
    t3("nldd-box")
  ], NLDDBox);

  // node_modules/@nldd/design-system/dist/components/layout/card/card.js
  init_lit();
  init_decorators();

  // node_modules/@nldd/design-system/dist/components/layout/card/card.styles.js
  init_lit();
  var cardStyles = i`


	/* # Host */

	:host {
		--_highlight-border: inset 0 0 0 var(--components-card-highlight-border-width) var(--components-card-highlight-border-color);

		display: flex;
		flex-direction: column;
	}

	:host([hidden]) {
		display: none;
	}


	/* # Block */

	.card {
		display: flex;
		position: relative;
		border-radius: var(--components-card-corner-radius);
		box-shadow: var(--components-card-box-shadow);
		background-color: var(--components-card-background-color);
		overflow: hidden;
		flex-direction: column;
		flex-grow: 1;
	}

	.card::after {
		content: '';
		position: absolute;
		inset: 0;
		border-radius: inherit;
		box-shadow: var(--_highlight-border);
		pointer-events: none;
	}


	/* # Elements */

	.card__header {
		flex-shrink: 0;
	}

	.card__header[hidden] {
		display: none;
	}

	.card__main {
		display: flex;
		min-height: 0;
		flex-direction: column;
		flex-grow: 1;
	}

	.card__footer {
		flex-shrink: 0;
	}

	.card__footer[hidden] {
		display: none;
	}
`;

  // node_modules/@nldd/design-system/dist/components/layout/card/card.template.js
  init_lit();
  function cardTemplate(component) {
    return b2`
		<article class="card"
			aria-label=${component.accessibleLabel ?? A}
		>
			<header class="card__header"
				hidden
			>
				<slot name="header"
					@slotchange=${component._onSlotChange}
				></slot>
			</header>
			<div class="card__main">
				<slot></slot>
			</div>
			<footer class="card__footer"
				hidden
			>
				<slot name="footer"
					@slotchange=${component._onSlotChange}
				></slot>
			</footer>
		</article>
	`;
  }

  // node_modules/@nldd/design-system/dist/components/layout/card/card.js
  var __decorate60 = function(decorators, target, key, desc) {
    var c6 = arguments.length, r6 = c6 < 3 ? target : desc === null ? desc = Object.getOwnPropertyDescriptor(target, key) : desc, d3;
    if (typeof Reflect === "object" && typeof Reflect.decorate === "function") r6 = Reflect.decorate(decorators, target, key, desc);
    else for (var i8 = decorators.length - 1; i8 >= 0; i8--) if (d3 = decorators[i8]) r6 = (c6 < 3 ? d3(r6) : c6 > 3 ? d3(target, key, r6) : d3(target, key)) || r6;
    return c6 > 3 && r6 && Object.defineProperty(target, key, r6), r6;
  };
  var NLDDCard = class NLDDCard2 extends i4 {
    constructor() {
      super(...arguments);
      this._onSlotChange = (e9) => {
        const slot = e9.target;
        const wrapper = slot.parentElement;
        wrapper.hidden = slot.assignedElements().length === 0;
      };
    }
    connectedCallback() {
      super.connectedCallback();
      this.style.containerType = "inline-size";
      this.style.containerName = "layout-container";
    }
    render() {
      return cardTemplate(this);
    }
  };
  NLDDCard.styles = cardStyles;
  __decorate60([
    n4({ type: String, attribute: "accessible-label" })
  ], NLDDCard.prototype, "accessibleLabel", void 0);
  NLDDCard = __decorate60([
    t3("nldd-card")
  ], NLDDCard);

  // node_modules/@nldd/design-system/dist/components/layout/collection/collection.js
  init_lit();
  init_decorators();

  // node_modules/@nldd/design-system/dist/components/layout/collection/collection.styles.js
  init_lit();
  init_breakpoints();
  var mdMin11 = r(breakpoints.mdMin);
  var lgMin8 = r(breakpoints.lgMin);
  var collectionStyles = i`


	/* # Host */

	:host {
		--_item-width: var(--primitives-area-280);
		--_focus-ring-z-index: 1;

		display: flex;
		width: 100%;
		min-width: 0;
		flex-direction: column;
		gap: var(--components-collection-sm-gap);

		@media (min-width: ${mdMin11}) {
			gap: var(--components-collection-md-gap);
		}

		@media (min-width: ${lgMin8}) {
			gap: var(--components-collection-lg-gap);
		}
	}

	:host([hidden]) {
		display: none;
	}


	/* # Items */

	.collection__items {
		display: flex;
		width: 100%;
		gap: var(--components-collection-sm-gap);

		@media (min-width: ${mdMin11}) {
			gap: var(--components-collection-md-gap);
		}

		@media (min-width: ${lgMin8}) {
			gap: var(--components-collection-lg-gap);
		}
	}

	/* ## Grid */

	:host([layout="grid"]) .collection__items,
	:host(:not([layout])) .collection__items {
		/* min(item-width, 100%) clamps the track min to the container width so
		   a single column never forces horizontal overflow on narrow screens. */
		display: grid;
		grid-template-columns: repeat(auto-fill, minmax(min(var(--_item-width), 100%), 1fr));
	}

	/* ## List */

	:host([layout="list"]) .collection__items {
		flex-direction: column;
	}

	/* ## Horizontal scroll */

	:host([layout="horizontal-scroll"]) .collection__items {
		margin-inline-start: calc(var(--primitives-space-16) * -1);
		margin-block: calc(var(--primitives-space-16) * -1);
		overflow-x: auto;
		padding-inline-start: var(--primitives-space-16);
		padding-block: var(--primitives-space-16);
		flex-direction: row;
		flex-wrap: nowrap;
		scroll-snap-type: x mandatory;
		scroll-behavior: smooth;
		-webkit-overflow-scrolling: touch;
		scrollbar-width: none;
		scroll-padding-inline-start: var(--primitives-space-16);
		mask-image: linear-gradient(
			to right,
			transparent 0,
			black var(--primitives-space-16),
			black calc(100% - var(--primitives-space-48)),
			transparent 100%
		);
	}

	:host([layout="horizontal-scroll"]) .collection__items::after {
		content: '';
		flex-grow: 0;
		flex-shrink: 0;
		flex-basis: var(--primitives-space-48);
	}

	:host([layout="horizontal-scroll"]) .collection__items::-webkit-scrollbar {
		display: none;
	}

	:host([layout="horizontal-scroll"]) .collection__items ::slotted(*) {
		flex-grow: 1;
		flex-shrink: 0;
		flex-basis: var(--_item-width);
		scroll-snap-align: start;
	}

	/* The horizontal-scroll items has a mask-image that fades the left
	 * and right edges, which would clip an outline drawn directly on it.
	 * The .collection__scroll-area wrapper has no mask and is the layout
	 * box that matches the visible content area; its ::after sits above
	 * slotted cards via z-index, so card box-shadows can't overlap the
	 * ring. :has() works here because both elements are in the same
	 * shadow tree (unlike :host(:has()), which doesn't pierce). */
	.collection__scroll-area {
		position: relative;
	}

	.collection__items:focus-visible {
		outline: none;
	}

	.collection__scroll-area:has(.collection__items:focus-visible)::after {
		content: '';
		position: absolute;
		inset: 0;
		z-index: var(--_focus-ring-z-index);
		outline: var(--semantics-focus-ring-outline);
		outline-offset: var(--semantics-focus-ring-outline-offset);
		box-shadow: var(--semantics-focus-ring-box-shadow);
		pointer-events: none;
	}


	/* # Footer */

	.collection__footer {
		display: flex;
		width: 100%;
	}

	.collection__footer[hidden] {
		display: none;
	}

	/* ## Scroll navigation — horizontal scroll */

	:host([layout="horizontal-scroll"]) .collection__footer {
		justify-content: flex-end;
		gap: var(--primitives-space-16);
	}
`;

  // node_modules/@nldd/design-system/dist/components/layout/collection/collection.template.js
  init_lit();
  init_if_defined2();
  function collectionTemplate(component) {
    const isHorizontal = component.layout === "horizontal-scroll";
    const showLoadMore = !isHorizontal && component.showLoadMore && component._hasMore;
    const showFooter = isHorizontal || showLoadMore || component._hasFooterSlot;
    const scrollable = isHorizontal && component._isScrollable;
    return b2`
		<div class="collection__scroll-area">
			<div class="collection__items"
				tabindex=${o7(scrollable ? "0" : void 0)}
				role=${o7(scrollable ? "region" : void 0)}
				aria-label=${o7(scrollable ? component._t("components.collection.region-label") : void 0)}
			>
				<slot @slotchange=${(e9) => component._onSlotChange(e9)}></slot>
			</div>
		</div>
		<footer class="collection__footer" ?hidden=${!showFooter}>
			<slot name="footer" @slotchange=${component._onFooterSlotChange}>
				${isHorizontal ? b2`
					<nldd-button-bar>
						<nldd-icon-button
							icon="chevron-left"
							text=${component._t("components.collection.previous-action")}
							tooltip-timing="never"
							.disabled=${component._atStart}
							@click=${() => component._scrollBy(-1)}
						></nldd-icon-button>
						<nldd-button-bar-divider></nldd-button-bar-divider>
						<nldd-icon-button
							icon="chevron-right"
							text=${component._t("components.collection.next-action")}
							tooltip-timing="never"
							.disabled=${component._atEnd}
							@click=${() => component._scrollBy(1)}
						></nldd-icon-button>
					</nldd-button-bar>
				` : A}
				${showLoadMore ? b2`
					<nldd-button
						variant="neutral-tinted"
						text=${component._t("components.collection.load-more-action")}
						width="full"
						@click=${() => component._loadMore()}
					></nldd-button>
				` : A}
			</slot>
		</footer>
	`;
  }

  // node_modules/@nldd/design-system/dist/components/layout/collection/collection.i18n.js
  var nlddCollectionTranslations = {
    "components.collection.previous-action": "Vorige",
    "components.collection.next-action": "Volgende",
    "components.collection.load-more-action": "Toon meer",
    "components.collection.region-label": "Collectie"
  };

  // node_modules/@nldd/design-system/dist/components/layout/collection/collection.js
  init_button();
  init_icon_button();
  init_icon();
  var __decorate61 = function(decorators, target, key, desc) {
    var c6 = arguments.length, r6 = c6 < 3 ? target : desc === null ? desc = Object.getOwnPropertyDescriptor(target, key) : desc, d3;
    if (typeof Reflect === "object" && typeof Reflect.decorate === "function") r6 = Reflect.decorate(decorators, target, key, desc);
    else for (var i8 = decorators.length - 1; i8 >= 0; i8--) if (d3 = decorators[i8]) r6 = (c6 < 3 ? d3(r6) : c6 > 3 ? d3(target, key, r6) : d3(target, key)) || r6;
    return c6 > 3 && r6 && Object.defineProperty(target, key, r6), r6;
  };
  var NLDDCollection = class NLDDCollection2 extends i4 {
    constructor() {
      super(...arguments);
      this.layout = "grid";
      this.showLoadMore = false;
      this.maxItems = 24;
      this.lazyLoad = false;
      this.translations = {};
      this._visibleCount = 0;
      this._totalCount = 0;
      this._atStart = true;
      this._atEnd = false;
      this._isScrollable = false;
      this._hasFooterSlot = false;
      this._onFooterSlotChange = (e9) => {
        const slot = e9.target;
        this._hasFooterSlot = slot.assignedElements().length > 0;
      };
      this._scrollListener = () => {
        const el = this._itemsEl;
        if (!el)
          return;
        this._atStart = el.scrollLeft < 1;
        this._atEnd = el.scrollLeft + el.clientWidth >= el.scrollWidth - 1;
        this._isScrollable = el.scrollWidth > el.clientWidth;
      };
      this._scrollListenerAttached = false;
    }
    // — i18n —————————————————————————————————————————————————————————————————
    _t(key) {
      return this.translations[key] ?? nlddCollectionTranslations[key];
    }
    connectedCallback() {
      super.connectedCallback();
      this._visibleCount = this.maxItems;
    }
    disconnectedCallback() {
      super.disconnectedCallback();
      this._intersectionObserver?.disconnect();
      this._teardownScrollListeners();
    }
    updated(changedProperties) {
      if (changedProperties.has("layout")) {
        this._setupScrollListeners();
      }
      if (changedProperties.has("itemWidth")) {
        if (this.itemWidth) {
          this.style.setProperty("--_item-width", this.itemWidth);
        } else {
          this.style.removeProperty("--_item-width");
        }
      }
      if (this.lazyLoad && this._loadMoreBtn && !this._intersectionObserver) {
        this._intersectionObserver = new IntersectionObserver(([entry]) => {
          if (entry.isIntersecting)
            this._loadMore();
        }, { threshold: 0.1 });
        this._intersectionObserver.observe(this._loadMoreBtn);
      } else if (!this._loadMoreBtn) {
        this._intersectionObserver?.disconnect();
        this._intersectionObserver = void 0;
      }
    }
    _setupScrollListeners() {
      this._teardownScrollListeners();
      if (this.layout === "horizontal-scroll" && this._itemsEl) {
        this._itemsEl.addEventListener("scroll", this._scrollListener, { passive: true });
        this._resizeObserver = new ResizeObserver(() => this._scrollListener());
        this._resizeObserver.observe(this._itemsEl);
        this._scrollListenerAttached = true;
        this._scrollListener();
      }
    }
    _teardownScrollListeners() {
      this._itemsEl?.removeEventListener("scroll", this._scrollListener);
      this._resizeObserver?.disconnect();
      this._resizeObserver = void 0;
      this._scrollListenerAttached = false;
      this._isScrollable = false;
    }
    _onSlotChange(e9) {
      const slot = e9.target;
      const items = slot.assignedElements();
      this._totalCount = items.length;
      if (this.layout !== "horizontal-scroll") {
        this._applyVisibility(items);
      }
    }
    _applyVisibility(items) {
      const slot = this._itemsEl?.querySelector("slot");
      const elements = items ?? (slot?.assignedElements() ?? []);
      elements.forEach((el, i8) => {
        el.hidden = i8 >= this._visibleCount;
      });
    }
    _loadMore() {
      this._visibleCount = Math.min(this._visibleCount + this.maxItems, this._totalCount);
      this._applyVisibility();
      this.dispatchEvent(new CustomEvent("load-more", { bubbles: true, composed: true }));
    }
    get _hasMore() {
      return this._visibleCount < this._totalCount;
    }
    _scrollBy(direction) {
      const slot = this._itemsEl?.querySelector("slot");
      const firstItem = slot?.assignedElements()[0];
      const itemWidth = firstItem?.offsetWidth ?? 280;
      this._itemsEl?.scrollBy({ left: direction * (itemWidth + 16), behavior: "smooth" });
    }
    render() {
      return collectionTemplate(this);
    }
  };
  NLDDCollection.styles = collectionStyles;
  __decorate61([
    n4({ type: String, reflect: true })
  ], NLDDCollection.prototype, "layout", void 0);
  __decorate61([
    n4({ type: Boolean, reflect: true, attribute: "show-load-more" })
  ], NLDDCollection.prototype, "showLoadMore", void 0);
  __decorate61([
    n4({ type: Number, attribute: "max-items" })
  ], NLDDCollection.prototype, "maxItems", void 0);
  __decorate61([
    n4({ type: Boolean, reflect: true, attribute: "lazy-load" })
  ], NLDDCollection.prototype, "lazyLoad", void 0);
  __decorate61([
    n4({ type: String, reflect: true, attribute: "item-width" })
  ], NLDDCollection.prototype, "itemWidth", void 0);
  __decorate61([
    n4({ type: Object })
  ], NLDDCollection.prototype, "translations", void 0);
  __decorate61([
    r5()
  ], NLDDCollection.prototype, "_visibleCount", void 0);
  __decorate61([
    r5()
  ], NLDDCollection.prototype, "_totalCount", void 0);
  __decorate61([
    r5()
  ], NLDDCollection.prototype, "_atStart", void 0);
  __decorate61([
    r5()
  ], NLDDCollection.prototype, "_atEnd", void 0);
  __decorate61([
    r5()
  ], NLDDCollection.prototype, "_isScrollable", void 0);
  __decorate61([
    r5()
  ], NLDDCollection.prototype, "_hasFooterSlot", void 0);
  __decorate61([
    e5(".collection__items")
  ], NLDDCollection.prototype, "_itemsEl", void 0);
  __decorate61([
    e5("nldd-button.load-more")
  ], NLDDCollection.prototype, "_loadMoreBtn", void 0);
  NLDDCollection = __decorate61([
    t3("nldd-collection")
  ], NLDDCollection);

  // node_modules/@nldd/design-system/dist/components/layout/spacer/spacer.js
  init_lit();
  init_decorators();

  // node_modules/@nldd/design-system/dist/components/layout/spacer/spacer.styles.js
  init_lit();
  init_breakpoints();
  var smMax9 = r(breakpoints.smMax);
  var mdMin12 = r(breakpoints.mdMin);
  var mdMax8 = r(breakpoints.mdMax);
  var lgMin9 = r(breakpoints.lgMin);
  var spacerStyles = i`


	/* # Host */

	:host {
		--_size: var(--primitives-space-16);

		display: block;
		width: var(--_size);
		height: var(--_size);
		flex-shrink: 0;
	}

	:host([hidden]) {
		display: none;
	}


	/* # Direction */

	:host([direction="horizontal"]) {
		height: auto;
	}

	:host([direction="vertical"]) {
		width: auto;
	}


	/* # Size — applies at every breakpoint */

	:host([size="2"]) { --_size: var(--primitives-space-2); }
	:host([size="4"]) { --_size: var(--primitives-space-4); }
	:host([size="6"]) { --_size: var(--primitives-space-6); }
	:host([size="8"]) { --_size: var(--primitives-space-8); }
	:host([size="10"]) { --_size: var(--primitives-space-10); }
	:host([size="12"]) { --_size: var(--primitives-space-12); }
	:host([size="16"]) { --_size: var(--primitives-space-16); }
	:host([size="20"]) { --_size: var(--primitives-space-20); }
	:host([size="24"]) { --_size: var(--primitives-space-24); }
	:host([size="28"]) { --_size: var(--primitives-space-28); }
	:host([size="32"]) { --_size: var(--primitives-space-32); }
	:host([size="40"]) { --_size: var(--primitives-space-40); }
	:host([size="44"]) { --_size: var(--primitives-space-44); }
	:host([size="48"]) { --_size: var(--primitives-space-48); }
	:host([size="56"]) { --_size: var(--primitives-space-56); }
	:host([size="64"]) { --_size: var(--primitives-space-64); }
	:host([size="80"]) { --_size: var(--primitives-space-80); }
	:host([size="96"]) { --_size: var(--primitives-space-96); }

	/* ## Size: flexible — takes all remaining space in the flex parent */

	:host([size="flexible"]) {
		min-width: 0;
		min-height: 0;
		width: auto;
		height: auto;
		flex-grow: 1;
		flex-shrink: 1;
		flex-basis: 0;
	}

	:host([size="flexible"][direction="horizontal"]) {
		min-height: auto;
	}

	:host([size="flexible"][direction="vertical"]) {
		min-width: auto;
	}


	/* # Per-viewport size overrides
	 *
	 * Each per-breakpoint attribute (sm-size / md-size / lg-size) sets the
	 * same --_size local var inside its respective @media query, so it wins
	 * over the base [size] selector at that breakpoint via cascade source
	 * order. If only [size] is set, --_size keeps the base value across all
	 * breakpoints.
	 */

	/* ## sm: max-width 640px */

	@media (max-width: ${smMax9}) {
		:host([sm-size="2"]) { --_size: var(--primitives-space-2); }
		:host([sm-size="4"]) { --_size: var(--primitives-space-4); }
		:host([sm-size="6"]) { --_size: var(--primitives-space-6); }
		:host([sm-size="8"]) { --_size: var(--primitives-space-8); }
		:host([sm-size="10"]) { --_size: var(--primitives-space-10); }
		:host([sm-size="12"]) { --_size: var(--primitives-space-12); }
		:host([sm-size="16"]) { --_size: var(--primitives-space-16); }
		:host([sm-size="20"]) { --_size: var(--primitives-space-20); }
		:host([sm-size="24"]) { --_size: var(--primitives-space-24); }
		:host([sm-size="28"]) { --_size: var(--primitives-space-28); }
		:host([sm-size="32"]) { --_size: var(--primitives-space-32); }
		:host([sm-size="40"]) { --_size: var(--primitives-space-40); }
		:host([sm-size="44"]) { --_size: var(--primitives-space-44); }
		:host([sm-size="48"]) { --_size: var(--primitives-space-48); }
		:host([sm-size="56"]) { --_size: var(--primitives-space-56); }
		:host([sm-size="64"]) { --_size: var(--primitives-space-64); }
		:host([sm-size="80"]) { --_size: var(--primitives-space-80); }
		:host([sm-size="96"]) { --_size: var(--primitives-space-96); }

		:host([sm-size="flexible"]) {
			min-width: 0;
			min-height: 0;
			width: auto;
			height: auto;
			flex-grow: 1;
			flex-shrink: 1;
			flex-basis: 0;
		}

		:host([sm-size="flexible"][direction="horizontal"]) {
			min-height: auto;
		}

		:host([sm-size="flexible"][direction="vertical"]) {
			min-width: auto;
		}
	}

	/* ## md: 641px–1007px */

	@media (min-width: ${mdMin12}) and (max-width: ${mdMax8}) {
		:host([md-size="2"]) { --_size: var(--primitives-space-2); }
		:host([md-size="4"]) { --_size: var(--primitives-space-4); }
		:host([md-size="6"]) { --_size: var(--primitives-space-6); }
		:host([md-size="8"]) { --_size: var(--primitives-space-8); }
		:host([md-size="10"]) { --_size: var(--primitives-space-10); }
		:host([md-size="12"]) { --_size: var(--primitives-space-12); }
		:host([md-size="16"]) { --_size: var(--primitives-space-16); }
		:host([md-size="20"]) { --_size: var(--primitives-space-20); }
		:host([md-size="24"]) { --_size: var(--primitives-space-24); }
		:host([md-size="28"]) { --_size: var(--primitives-space-28); }
		:host([md-size="32"]) { --_size: var(--primitives-space-32); }
		:host([md-size="40"]) { --_size: var(--primitives-space-40); }
		:host([md-size="44"]) { --_size: var(--primitives-space-44); }
		:host([md-size="48"]) { --_size: var(--primitives-space-48); }
		:host([md-size="56"]) { --_size: var(--primitives-space-56); }
		:host([md-size="64"]) { --_size: var(--primitives-space-64); }
		:host([md-size="80"]) { --_size: var(--primitives-space-80); }
		:host([md-size="96"]) { --_size: var(--primitives-space-96); }

		:host([md-size="flexible"]) {
			min-width: 0;
			min-height: 0;
			width: auto;
			height: auto;
			flex-grow: 1;
			flex-shrink: 1;
			flex-basis: 0;
		}

		:host([md-size="flexible"][direction="horizontal"]) {
			min-height: auto;
		}

		:host([md-size="flexible"][direction="vertical"]) {
			min-width: auto;
		}
	}

	/* ## lg: min-width 1008px */

	@media (min-width: ${lgMin9}) {
		:host([lg-size="2"]) { --_size: var(--primitives-space-2); }
		:host([lg-size="4"]) { --_size: var(--primitives-space-4); }
		:host([lg-size="6"]) { --_size: var(--primitives-space-6); }
		:host([lg-size="8"]) { --_size: var(--primitives-space-8); }
		:host([lg-size="10"]) { --_size: var(--primitives-space-10); }
		:host([lg-size="12"]) { --_size: var(--primitives-space-12); }
		:host([lg-size="16"]) { --_size: var(--primitives-space-16); }
		:host([lg-size="20"]) { --_size: var(--primitives-space-20); }
		:host([lg-size="24"]) { --_size: var(--primitives-space-24); }
		:host([lg-size="28"]) { --_size: var(--primitives-space-28); }
		:host([lg-size="32"]) { --_size: var(--primitives-space-32); }
		:host([lg-size="40"]) { --_size: var(--primitives-space-40); }
		:host([lg-size="44"]) { --_size: var(--primitives-space-44); }
		:host([lg-size="48"]) { --_size: var(--primitives-space-48); }
		:host([lg-size="56"]) { --_size: var(--primitives-space-56); }
		:host([lg-size="64"]) { --_size: var(--primitives-space-64); }
		:host([lg-size="80"]) { --_size: var(--primitives-space-80); }
		:host([lg-size="96"]) { --_size: var(--primitives-space-96); }

		:host([lg-size="flexible"]) {
			min-width: 0;
			min-height: 0;
			width: auto;
			height: auto;
			flex-grow: 1;
			flex-shrink: 1;
			flex-basis: 0;
		}

		:host([lg-size="flexible"][direction="horizontal"]) {
			min-height: auto;
		}

		:host([lg-size="flexible"][direction="vertical"]) {
			min-width: auto;
		}
	}
`;

  // node_modules/@nldd/design-system/dist/components/layout/spacer/spacer.js
  var __decorate62 = function(decorators, target, key, desc) {
    var c6 = arguments.length, r6 = c6 < 3 ? target : desc === null ? desc = Object.getOwnPropertyDescriptor(target, key) : desc, d3;
    if (typeof Reflect === "object" && typeof Reflect.decorate === "function") r6 = Reflect.decorate(decorators, target, key, desc);
    else for (var i8 = decorators.length - 1; i8 >= 0; i8--) if (d3 = decorators[i8]) r6 = (c6 < 3 ? d3(r6) : c6 > 3 ? d3(target, key, r6) : d3(target, key)) || r6;
    return c6 > 3 && r6 && Object.defineProperty(target, key, r6), r6;
  };
  var NLDDSpacer = class NLDDSpacer2 extends i4 {
    constructor() {
      super(...arguments);
      this.size = "16";
      this.direction = "both";
    }
  };
  NLDDSpacer.styles = spacerStyles;
  __decorate62([
    n4({ type: String, reflect: true })
  ], NLDDSpacer.prototype, "size", void 0);
  __decorate62([
    n4({ type: String, reflect: true, attribute: "sm-size" })
  ], NLDDSpacer.prototype, "smSize", void 0);
  __decorate62([
    n4({ type: String, reflect: true, attribute: "md-size" })
  ], NLDDSpacer.prototype, "mdSize", void 0);
  __decorate62([
    n4({ type: String, reflect: true, attribute: "lg-size" })
  ], NLDDSpacer.prototype, "lgSize", void 0);
  __decorate62([
    n4({ type: String, reflect: true })
  ], NLDDSpacer.prototype, "direction", void 0);
  NLDDSpacer = __decorate62([
    t3("nldd-spacer")
  ], NLDDSpacer);

  // node_modules/@nldd/design-system/dist/components/layout/container/container.js
  init_lit();
  init_decorators();

  // node_modules/@nldd/design-system/dist/components/layout/container/container.styles.js
  init_lit();
  init_breakpoints();
  var smMax10 = r(breakpoints.smMax);
  var mdMin13 = r(breakpoints.mdMin);
  var mdMax9 = r(breakpoints.mdMax);
  var lgMin10 = r(breakpoints.lgMin);
  var containerStyles = i`


	/* # Host — outer chrome (padding + scheme + query container) */

	:host {
		--_min-column-width: var(--primitives-area-280);
		--_justify-content: initial;
		--_justify-items: initial;
		--_align-items: initial;
		--_gap: 0;
		--_sm-gap: var(--_gap);
		--_md-gap: var(--_gap);
		--_lg-gap: var(--_gap);
		--_padding-top: 0;
		--_padding-right: 0;
		--_padding-bottom: 0;
		--_padding-left: 0;
		--_sm-padding-top: var(--_padding-top);
		--_sm-padding-right: var(--_padding-right);
		--_sm-padding-bottom: var(--_padding-bottom);
		--_sm-padding-left: var(--_padding-left);
		--_md-padding-top: var(--_padding-top);
		--_md-padding-right: var(--_padding-right);
		--_md-padding-bottom: var(--_padding-bottom);
		--_md-padding-left: var(--_padding-left);
		--_lg-padding-top: var(--_padding-top);
		--_lg-padding-right: var(--_padding-right);
		--_lg-padding-bottom: var(--_padding-bottom);
		--_lg-padding-left: var(--_padding-left);
		--_slot-order: 0;
		--_slot-sm-order: var(--_slot-order);
		--_slot-md-order: var(--_slot-order);
		--_slot-lg-order: var(--_slot-order);

		container-type: inline-size;
		display: block;
		height: auto;
		padding-top: var(--_padding-top);
		padding-right: var(--_padding-right);
		padding-bottom: var(--_padding-bottom);
		padding-left: var(--_padding-left);

		@media (max-width: ${smMax10}) {
			padding-top: var(--_sm-padding-top);
			padding-right: var(--_sm-padding-right);
			padding-bottom: var(--_sm-padding-bottom);
			padding-left: var(--_sm-padding-left);
		}

		@media (min-width: ${mdMin13}) and (max-width: ${mdMax9}) {
			padding-top: var(--_md-padding-top);
			padding-right: var(--_md-padding-right);
			padding-bottom: var(--_md-padding-bottom);
			padding-left: var(--_md-padding-left);
		}

		@media (min-width: ${lgMin10}) {
			padding-top: var(--_lg-padding-top);
			padding-right: var(--_lg-padding-right);
			padding-bottom: var(--_lg-padding-bottom);
			padding-left: var(--_lg-padding-left);
		}

		@container layout-container (max-width: ${smMax10}) {
			padding-top: var(--_sm-padding-top);
			padding-right: var(--_sm-padding-right);
			padding-bottom: var(--_sm-padding-bottom);
			padding-left: var(--_sm-padding-left);
		}

		@container layout-container (min-width: ${mdMin13}) and (max-width: ${mdMax9}) {
			padding-top: var(--_md-padding-top);
			padding-right: var(--_md-padding-right);
			padding-bottom: var(--_md-padding-bottom);
			padding-left: var(--_md-padding-left);
		}

		@container layout-container (min-width: ${lgMin10}) {
			padding-top: var(--_lg-padding-top);
			padding-right: var(--_lg-padding-right);
			padding-bottom: var(--_lg-padding-bottom);
			padding-left: var(--_lg-padding-left);
		}
	}

	:host([hidden]) {
		display: none;
	}


	/* # Inner — actual layout */

	.container {
		display: flex;
		flex-direction: column;
		flex-wrap: nowrap;
		justify-content: var(--_justify-content);
		justify-items: var(--_justify-items);
		align-items: var(--_align-items);
		gap: var(--_gap);

		@media (max-width: ${smMax10}) { gap: var(--_sm-gap); }
		@media (min-width: ${mdMin13}) and (max-width: ${mdMax9}) { gap: var(--_md-gap); }
		@media (min-width: ${lgMin10}) { gap: var(--_lg-gap); }

		@container layout-container (max-width: ${smMax10}) { gap: var(--_sm-gap); }
		@container layout-container (min-width: ${mdMin13}) and (max-width: ${mdMax9}) { gap: var(--_md-gap); }
		@container layout-container (min-width: ${lgMin10}) { gap: var(--_lg-gap); }
	}

	:host([layout="row"]) .container {
		flex-direction: row;
	}

	:host([layout="wrap"]) .container {
		flex-direction: row;
		flex-wrap: wrap;
	}

	:host([layout="grid"]) .container {
		display: grid;
		grid-template-columns: repeat(
			var(--_column-count, auto-fit),
			minmax(var(--_track-min, var(--_min-column-width)), 1fr)
		);
	}

	:host([layout="columns"]) .container {
		display: block;
		columns: var(--_min-column-width);
		column-gap: var(--_gap);
	}

	:host([layout="columns"]) ::slotted(*) {
		break-inside: avoid;
	}

	:host([layout="columns"][column-count]) .container,
	:host([layout="columns"][sm-column-count]) .container,
	:host([layout="columns"][md-column-count]) .container,
	:host([layout="columns"][lg-column-count]) .container {
		column-count: var(--_column-count);
		column-width: auto;
	}


	/* # Column count — base scope */

	:host([column-count="1"]) .container { --_column-count: 1; }
	:host([column-count="2"]) .container { --_column-count: 2; }
	:host([column-count="3"]) .container { --_column-count: 3; }
	:host([column-count="4"]) .container { --_column-count: 4; }
	:host([column-count="5"]) .container { --_column-count: 5; }
	:host([column-count="6"]) .container { --_column-count: 6; }
	:host([column-count="7"]) .container { --_column-count: 7; }
	:host([column-count="8"]) .container { --_column-count: 8; }
	:host([column-count]) .container { --_track-min: 0; }


	/* # Column count — sm scope (queries :host own width) */

	@container (max-width: ${smMax10}) {
		:host([sm-column-count="1"]) .container { --_column-count: 1; }
		:host([sm-column-count="2"]) .container { --_column-count: 2; }
		:host([sm-column-count="3"]) .container { --_column-count: 3; }
		:host([sm-column-count="4"]) .container { --_column-count: 4; }
		:host([sm-column-count="5"]) .container { --_column-count: 5; }
		:host([sm-column-count="6"]) .container { --_column-count: 6; }
		:host([sm-column-count="7"]) .container { --_column-count: 7; }
		:host([sm-column-count="8"]) .container { --_column-count: 8; }
		:host([sm-column-count]) .container { --_track-min: 0; }
	}


	/* # Column count — md scope */

	@container (min-width: ${mdMin13}) and (max-width: ${mdMax9}) {
		:host([md-column-count="1"]) .container { --_column-count: 1; }
		:host([md-column-count="2"]) .container { --_column-count: 2; }
		:host([md-column-count="3"]) .container { --_column-count: 3; }
		:host([md-column-count="4"]) .container { --_column-count: 4; }
		:host([md-column-count="5"]) .container { --_column-count: 5; }
		:host([md-column-count="6"]) .container { --_column-count: 6; }
		:host([md-column-count="7"]) .container { --_column-count: 7; }
		:host([md-column-count="8"]) .container { --_column-count: 8; }
		:host([md-column-count]) .container { --_track-min: 0; }
	}


	/* # Column count — lg scope */

	@container (min-width: ${lgMin10}) {
		:host([lg-column-count="1"]) .container { --_column-count: 1; }
		:host([lg-column-count="2"]) .container { --_column-count: 2; }
		:host([lg-column-count="3"]) .container { --_column-count: 3; }
		:host([lg-column-count="4"]) .container { --_column-count: 4; }
		:host([lg-column-count="5"]) .container { --_column-count: 5; }
		:host([lg-column-count="6"]) .container { --_column-count: 6; }
		:host([lg-column-count="7"]) .container { --_column-count: 7; }
		:host([lg-column-count="8"]) .container { --_column-count: 8; }
		:host([lg-column-count]) .container { --_track-min: 0; }
	}


	/* # Slot order — per-child via order / sm-order / md-order / lg-order
	   attributes on slotted children. Container JS bridges those to
	   --_slot-{attr} inline custom props on the child; the queries below
	   pick the right value per breakpoint with var() cascading
	   sm/md/lg-order → order → 0. No-op for layout="columns" (multicol). */

	::slotted(*) {
		order: var(--_slot-order, 0);
	}

	@container (max-width: ${smMax10}) {
		::slotted(*) { order: var(--_slot-sm-order, var(--_slot-order, 0)); }
	}

	@container (min-width: ${mdMin13}) and (max-width: ${mdMax9}) {
		::slotted(*) { order: var(--_slot-md-order, var(--_slot-order, 0)); }
	}

	@container (min-width: ${lgMin10}) {
		::slotted(*) { order: var(--_slot-lg-order, var(--_slot-order, 0)); }
	}
`;

  // node_modules/@nldd/design-system/dist/components/layout/container/container.template.js
  init_lit();
  function containerTemplate(component) {
    return b2`<div class="container"><slot @slotchange=${component._onSlotChange}></slot></div>`;
  }

  // node_modules/@nldd/design-system/dist/components/layout/container/container.js
  var __decorate63 = function(decorators, target, key, desc) {
    var c6 = arguments.length, r6 = c6 < 3 ? target : desc === null ? desc = Object.getOwnPropertyDescriptor(target, key) : desc, d3;
    if (typeof Reflect === "object" && typeof Reflect.decorate === "function") r6 = Reflect.decorate(decorators, target, key, desc);
    else for (var i8 = decorators.length - 1; i8 >= 0; i8--) if (d3 = decorators[i8]) r6 = (c6 < 3 ? d3(r6) : c6 > 3 ? d3(target, key, r6) : d3(target, key)) || r6;
    return c6 > 3 && r6 && Object.defineProperty(target, key, r6), r6;
  };
  var HORIZONTAL_TO_FLEX = {
    left: "flex-start",
    center: "center",
    right: "flex-end"
  };
  var VERTICAL_TO_FLEX = {
    top: "flex-start",
    center: "center",
    bottom: "flex-end"
  };
  var ORDER_ATTRS = ["order", "sm-order", "md-order", "lg-order"];
  function sizeToValue(size3) {
    if (size3 === void 0)
      return null;
    if (size3 === "0")
      return "0";
    return `var(--primitives-space-${size3})`;
  }
  var NLDDContainer = class NLDDContainer2 extends i4 {
    constructor() {
      super(...arguments);
      this.horizontalAlignment = void 0;
      this.verticalAlignment = void 0;
      this.gap = void 0;
      this.smGap = void 0;
      this.mdGap = void 0;
      this.lgGap = void 0;
      this.padding = void 0;
      this.paddingInline = void 0;
      this.paddingBlock = void 0;
      this.paddingTop = void 0;
      this.paddingRight = void 0;
      this.paddingBottom = void 0;
      this.paddingLeft = void 0;
      this.smPadding = void 0;
      this.smPaddingInline = void 0;
      this.smPaddingBlock = void 0;
      this.smPaddingTop = void 0;
      this.smPaddingRight = void 0;
      this.smPaddingBottom = void 0;
      this.smPaddingLeft = void 0;
      this.mdPadding = void 0;
      this.mdPaddingInline = void 0;
      this.mdPaddingBlock = void 0;
      this.mdPaddingTop = void 0;
      this.mdPaddingRight = void 0;
      this.mdPaddingBottom = void 0;
      this.mdPaddingLeft = void 0;
      this.lgPadding = void 0;
      this.lgPaddingInline = void 0;
      this.lgPaddingBlock = void 0;
      this.lgPaddingTop = void 0;
      this.lgPaddingRight = void 0;
      this.lgPaddingBottom = void 0;
      this.lgPaddingLeft = void 0;
      this._onSlotChange = (e9) => {
        const slot = e9.target;
        this._childObserver?.disconnect();
        this._childObserver = new MutationObserver((muts) => {
          for (const m3 of muts) {
            if (m3.target instanceof HTMLElement)
              this._applyOrderProps(m3.target);
          }
        });
        for (const el of slot.assignedElements()) {
          if (!(el instanceof HTMLElement))
            continue;
          this._applyOrderProps(el);
          this._childObserver.observe(el, { attributes: true, attributeFilter: [...ORDER_ATTRS] });
        }
      };
    }
    updated(_changed) {
      this.writeCustomProperties();
    }
    writeCustomProperties() {
      const setProp = (name, value) => {
        if (value === null)
          this.style.removeProperty(name);
        else
          this.style.setProperty(name, value);
      };
      const horizontal = this.horizontalAlignment ? HORIZONTAL_TO_FLEX[this.horizontalAlignment] : null;
      const vertical = this.verticalAlignment ? VERTICAL_TO_FLEX[this.verticalAlignment] : null;
      const isFlexRow = this.layout === "row" || this.layout === "wrap";
      const isGrid = this.layout === "grid";
      if (isGrid) {
        setProp("--_justify-items", horizontal);
        setProp("--_justify-content", horizontal);
        setProp("--_align-items", vertical);
      } else if (isFlexRow) {
        setProp("--_justify-content", horizontal);
        setProp("--_align-items", vertical);
        setProp("--_justify-items", null);
      } else {
        setProp("--_justify-content", vertical);
        setProp("--_align-items", horizontal);
        setProp("--_justify-items", null);
      }
      setProp("--_gap", sizeToValue(this.gap));
      setProp("--_sm-gap", sizeToValue(this.smGap));
      setProp("--_md-gap", sizeToValue(this.mdGap));
      setProp("--_lg-gap", sizeToValue(this.lgGap));
      for (const scope of ["", "sm", "md", "lg"]) {
        const [top, right, bottom, left] = this.resolvePadding(scope);
        const prefix = scope ? `${scope}-` : "";
        setProp(`--_${prefix}padding-top`, sizeToValue(top));
        setProp(`--_${prefix}padding-right`, sizeToValue(right));
        setProp(`--_${prefix}padding-bottom`, sizeToValue(bottom));
        setProp(`--_${prefix}padding-left`, sizeToValue(left));
      }
    }
    resolvePadding(scope) {
      const get = (key) => {
        const prop = scope ? `${scope}${key}` : key.charAt(0).toLowerCase() + key.slice(1);
        return this[prop];
      };
      const all = get("Padding");
      const inline2 = get("PaddingInline");
      const block = get("PaddingBlock");
      const top = get("PaddingTop") ?? block ?? all;
      const right = get("PaddingRight") ?? inline2 ?? all;
      const bottom = get("PaddingBottom") ?? block ?? all;
      const left = get("PaddingLeft") ?? inline2 ?? all;
      return [top, right, bottom, left];
    }
    disconnectedCallback() {
      super.disconnectedCallback();
      this._childObserver?.disconnect();
      this._childObserver = void 0;
    }
    _applyOrderProps(el) {
      for (const attr of ORDER_ATTRS) {
        const v3 = el.getAttribute(attr);
        const prop = `--_slot-${attr}`;
        if (v3 !== null)
          el.style.setProperty(prop, v3);
        else
          el.style.removeProperty(prop);
      }
    }
    render() {
      return containerTemplate(this);
    }
  };
  NLDDContainer.styles = containerStyles;
  __decorate63([
    n4({ type: String, reflect: true })
  ], NLDDContainer.prototype, "layout", void 0);
  __decorate63([
    n4({ type: Number, reflect: true, attribute: "column-count" })
  ], NLDDContainer.prototype, "columnCount", void 0);
  __decorate63([
    n4({ type: Number, reflect: true, attribute: "sm-column-count" })
  ], NLDDContainer.prototype, "smColumnCount", void 0);
  __decorate63([
    n4({ type: Number, reflect: true, attribute: "md-column-count" })
  ], NLDDContainer.prototype, "mdColumnCount", void 0);
  __decorate63([
    n4({ type: Number, reflect: true, attribute: "lg-column-count" })
  ], NLDDContainer.prototype, "lgColumnCount", void 0);
  __decorate63([
    n4({ type: String, reflect: true, attribute: "horizontal-alignment" })
  ], NLDDContainer.prototype, "horizontalAlignment", void 0);
  __decorate63([
    n4({ type: String, reflect: true, attribute: "vertical-alignment" })
  ], NLDDContainer.prototype, "verticalAlignment", void 0);
  __decorate63([
    n4({ type: String, reflect: true })
  ], NLDDContainer.prototype, "gap", void 0);
  __decorate63([
    n4({ type: String, reflect: true, attribute: "sm-gap" })
  ], NLDDContainer.prototype, "smGap", void 0);
  __decorate63([
    n4({ type: String, reflect: true, attribute: "md-gap" })
  ], NLDDContainer.prototype, "mdGap", void 0);
  __decorate63([
    n4({ type: String, reflect: true, attribute: "lg-gap" })
  ], NLDDContainer.prototype, "lgGap", void 0);
  __decorate63([
    n4({ type: String, reflect: true })
  ], NLDDContainer.prototype, "padding", void 0);
  __decorate63([
    n4({ type: String, reflect: true, attribute: "padding-inline" })
  ], NLDDContainer.prototype, "paddingInline", void 0);
  __decorate63([
    n4({ type: String, reflect: true, attribute: "padding-block" })
  ], NLDDContainer.prototype, "paddingBlock", void 0);
  __decorate63([
    n4({ type: String, reflect: true, attribute: "padding-top" })
  ], NLDDContainer.prototype, "paddingTop", void 0);
  __decorate63([
    n4({ type: String, reflect: true, attribute: "padding-right" })
  ], NLDDContainer.prototype, "paddingRight", void 0);
  __decorate63([
    n4({ type: String, reflect: true, attribute: "padding-bottom" })
  ], NLDDContainer.prototype, "paddingBottom", void 0);
  __decorate63([
    n4({ type: String, reflect: true, attribute: "padding-left" })
  ], NLDDContainer.prototype, "paddingLeft", void 0);
  __decorate63([
    n4({ type: String, reflect: true, attribute: "sm-padding" })
  ], NLDDContainer.prototype, "smPadding", void 0);
  __decorate63([
    n4({ type: String, reflect: true, attribute: "sm-padding-inline" })
  ], NLDDContainer.prototype, "smPaddingInline", void 0);
  __decorate63([
    n4({ type: String, reflect: true, attribute: "sm-padding-block" })
  ], NLDDContainer.prototype, "smPaddingBlock", void 0);
  __decorate63([
    n4({ type: String, reflect: true, attribute: "sm-padding-top" })
  ], NLDDContainer.prototype, "smPaddingTop", void 0);
  __decorate63([
    n4({ type: String, reflect: true, attribute: "sm-padding-right" })
  ], NLDDContainer.prototype, "smPaddingRight", void 0);
  __decorate63([
    n4({ type: String, reflect: true, attribute: "sm-padding-bottom" })
  ], NLDDContainer.prototype, "smPaddingBottom", void 0);
  __decorate63([
    n4({ type: String, reflect: true, attribute: "sm-padding-left" })
  ], NLDDContainer.prototype, "smPaddingLeft", void 0);
  __decorate63([
    n4({ type: String, reflect: true, attribute: "md-padding" })
  ], NLDDContainer.prototype, "mdPadding", void 0);
  __decorate63([
    n4({ type: String, reflect: true, attribute: "md-padding-inline" })
  ], NLDDContainer.prototype, "mdPaddingInline", void 0);
  __decorate63([
    n4({ type: String, reflect: true, attribute: "md-padding-block" })
  ], NLDDContainer.prototype, "mdPaddingBlock", void 0);
  __decorate63([
    n4({ type: String, reflect: true, attribute: "md-padding-top" })
  ], NLDDContainer.prototype, "mdPaddingTop", void 0);
  __decorate63([
    n4({ type: String, reflect: true, attribute: "md-padding-right" })
  ], NLDDContainer.prototype, "mdPaddingRight", void 0);
  __decorate63([
    n4({ type: String, reflect: true, attribute: "md-padding-bottom" })
  ], NLDDContainer.prototype, "mdPaddingBottom", void 0);
  __decorate63([
    n4({ type: String, reflect: true, attribute: "md-padding-left" })
  ], NLDDContainer.prototype, "mdPaddingLeft", void 0);
  __decorate63([
    n4({ type: String, reflect: true, attribute: "lg-padding" })
  ], NLDDContainer.prototype, "lgPadding", void 0);
  __decorate63([
    n4({ type: String, reflect: true, attribute: "lg-padding-inline" })
  ], NLDDContainer.prototype, "lgPaddingInline", void 0);
  __decorate63([
    n4({ type: String, reflect: true, attribute: "lg-padding-block" })
  ], NLDDContainer.prototype, "lgPaddingBlock", void 0);
  __decorate63([
    n4({ type: String, reflect: true, attribute: "lg-padding-top" })
  ], NLDDContainer.prototype, "lgPaddingTop", void 0);
  __decorate63([
    n4({ type: String, reflect: true, attribute: "lg-padding-right" })
  ], NLDDContainer.prototype, "lgPaddingRight", void 0);
  __decorate63([
    n4({ type: String, reflect: true, attribute: "lg-padding-bottom" })
  ], NLDDContainer.prototype, "lgPaddingBottom", void 0);
  __decorate63([
    n4({ type: String, reflect: true, attribute: "lg-padding-left" })
  ], NLDDContainer.prototype, "lgPaddingLeft", void 0);
  NLDDContainer = __decorate63([
    t3("nldd-container")
  ], NLDDContainer);

  // node_modules/@nldd/design-system/dist/components/layout/divider/divider.js
  init_lit();
  init_decorators();

  // node_modules/@nldd/design-system/dist/components/layout/divider/divider.styles.js
  init_lit();
  var dividerStyles = i`


	/* # Host */

	:host {
		display: block;
		flex-shrink: 0;
	}

	:host([hidden]) {
		display: none;
	}


	/* # Block */

	.divider {
		display: block;
		margin: 0;
		border: none;
		background-color: var(--semantics-dividers-color);
		width: 100%;
		height: var(--semantics-dividers-thickness);
	}

	@media (forced-colors: active) {
		.divider {
			background-color: CanvasText;
		}
	}
`;

  // node_modules/@nldd/design-system/dist/components/layout/divider/divider.template.js
  init_lit();
  function dividerTemplate(_component) {
    return b2`
		<hr class="divider">
	`;
  }

  // node_modules/@nldd/design-system/dist/components/layout/divider/divider.js
  var __decorate64 = function(decorators, target, key, desc) {
    var c6 = arguments.length, r6 = c6 < 3 ? target : desc === null ? desc = Object.getOwnPropertyDescriptor(target, key) : desc, d3;
    if (typeof Reflect === "object" && typeof Reflect.decorate === "function") r6 = Reflect.decorate(decorators, target, key, desc);
    else for (var i8 = decorators.length - 1; i8 >= 0; i8--) if (d3 = decorators[i8]) r6 = (c6 < 3 ? d3(r6) : c6 > 3 ? d3(target, key, r6) : d3(target, key)) || r6;
    return c6 > 3 && r6 && Object.defineProperty(target, key, r6), r6;
  };
  var NLDDDivider = class NLDDDivider2 extends i4 {
    render() {
      return dividerTemplate(this);
    }
  };
  NLDDDivider.styles = dividerStyles;
  NLDDDivider = __decorate64([
    t3("nldd-divider")
  ], NLDDDivider);

  // node_modules/@nldd/design-system/dist/components/layout/popover/popover.js
  init_lit();
  init_decorators();
  init_floating_ui_dom();

  // node_modules/@nldd/design-system/dist/components/layout/popover/popover.styles.js
  init_lit();
  init_breakpoints();
  var smMax11 = r(breakpoints.smMax);
  var mdMin14 = r(breakpoints.mdMin);
  var popoverStyles = i`


	/* # Host */

	:host {
		--_max-height: calc(100vh - var(--semantics-overlays-inset) * 2);

		margin: 0;
		outline: none;
		border: none;
		box-shadow: var(--semantics-overlays-box-shadow);
		background-color: var(--semantics-surfaces-base-background-color);
		overflow: auto;
		padding: 0;
		isolation: isolate;

		@media (max-width: ${smMax11}) {
			position: fixed;
			inset: auto 0 0 0;
			border-radius: var(--semantics-overlays-corner-radius) var(--semantics-overlays-corner-radius) 0 0;
			width: 100%;
			max-width: 100%;
			max-height: calc(100dvh - var(--semantics-sheets-bottom-top-inset));
			transform: translateY(100%);
			transition:
				transform var(--semantics-sheets-bottom-animation-duration) var(--primitives-transition-easing-default),
				display var(--semantics-sheets-bottom-animation-duration) allow-discrete,
				overlay var(--semantics-sheets-bottom-animation-duration) allow-discrete;
		}

		@media (min-width: ${mdMin14}) {
			position: absolute;
			inset: unset;
			border-radius: var(--semantics-overlays-corner-radius);
			width: var(--components-popover-default-width);
			max-width: calc(100vw - var(--semantics-overlays-inset) * 2);
			max-height: var(--_max-height);
		}
	}

	:host([hidden]) {
		display: none;
	}

	:host(:not(:popover-open)) {
		display: none;
	}

	:host(:focus-visible:not(.is-pointer-focus)) {
		outline: var(--semantics-focus-ring-outline);
		outline-offset: var(--semantics-focus-ring-outline-offset);
		box-shadow: var(--semantics-focus-ring-box-shadow), var(--semantics-overlays-box-shadow);
	}

	:host(:popover-open) {
		@media (max-width: ${smMax11}) {
			transform: translateY(0);

			@starting-style {
				transform: translateY(100%);
			}
		}
	}

	:host::backdrop {
		@media (max-width: ${smMax11}) {
			background-color: var(--semantics-overlays-backdrop-color);
		}
	}

	:host([sm-full-height]) {
		@media (max-width: ${smMax11}) {
			height: calc(100dvh - var(--semantics-sheets-bottom-top-inset));
		}
	}

	/* Top-level safety net: transitions currently only exist in the sm
	   bottom-sheet rule, but this guard also disables any future
	   desktop-viewport animation for prefers-reduced-motion users. */
	@media (prefers-reduced-motion: reduce) {
		:host {
			transition: none;
		}
	}
`;

  // node_modules/@nldd/design-system/dist/components/layout/popover/popover.template.js
  init_lit();
  function popoverTemplate() {
    return b2`<slot></slot>`;
  }

  // node_modules/@nldd/design-system/dist/components/layout/popover/popover.i18n.js
  var nlddPopoverTranslations = {
    "components.popover.accessible-label": "Popover"
  };

  // node_modules/@nldd/design-system/dist/components/layout/popover/popover.js
  init_input_modality();
  init_breakpoints();
  var import_meta17 = {};
  var __decorate65 = function(decorators, target, key, desc) {
    var c6 = arguments.length, r6 = c6 < 3 ? target : desc === null ? desc = Object.getOwnPropertyDescriptor(target, key) : desc, d3;
    if (typeof Reflect === "object" && typeof Reflect.decorate === "function") r6 = Reflect.decorate(decorators, target, key, desc);
    else for (var i8 = decorators.length - 1; i8 >= 0; i8--) if (d3 = decorators[i8]) r6 = (c6 < 3 ? d3(r6) : c6 > 3 ? d3(target, key, r6) : d3(target, key)) || r6;
    return c6 > 3 && r6 && Object.defineProperty(target, key, r6), r6;
  };
  var NLDDPopover = class NLDDPopover2 extends i4 {
    constructor() {
      super(...arguments);
      this.anchor = "";
      this.anchorElement = null;
      this.placement = "bottom-start";
      this.top = void 0;
      this.left = void 0;
      this.right = void 0;
      this.bottom = void 0;
      this.centered = false;
      this.smFullHeight = false;
      this.accessibleLabel = "";
      this.translations = {};
      this._isOpen = false;
      this._hasWarnedLabel = false;
      this._previousFocus = null;
      this._pointerdownOnAnchorWhileOpen = false;
      this._cleanupAutoUpdate = null;
      this._smQuery = null;
      this._wasOnSm = false;
      this._previousAnchorEl = null;
      this._handleViewportChange = () => {
        if (this._isOpen) {
          this.reposition();
        } else {
          this._wasOnSm = this._smQuery?.matches ?? false;
        }
      };
      this._handleDocumentPointerdown = (event) => {
        if (!this._isOpen)
          return;
        const anchorEl = this._getAnchorEl();
        if (!anchorEl)
          return;
        if (!event.composedPath().includes(anchorEl))
          return;
        this._pointerdownOnAnchorWhileOpen = true;
      };
      this._handleDocumentClick = (event) => {
        const anchorEl = this._getAnchorEl();
        if (!anchorEl)
          return;
        const path = event.composedPath();
        if (!path.includes(anchorEl))
          return;
        const popovertarget = anchorEl.getAttribute("popovertarget");
        if (popovertarget && this.id && popovertarget === this.id)
          return;
        if (this._pointerdownOnAnchorWhileOpen) {
          this._pointerdownOnAnchorWhileOpen = false;
          return;
        }
        if (this._isOpen) {
          this.hidePopover();
        } else {
          this.showPopover();
        }
      };
      this._handleToggle = async (event) => {
        const toggleEvent = event;
        this._isOpen = toggleEvent.newState === "open";
        this._updateAnchorAria(this._isOpen);
        if (toggleEvent.newState !== "open") {
          this._cleanupAutoUpdate?.();
          this._cleanupAutoUpdate = null;
          this._returnFocus();
          this.dispatchEvent(new CustomEvent("close", { bubbles: true, composed: true }));
          return;
        }
        this._previousFocus = document.activeElement ?? this._getAnchorEl();
        await this.reposition();
        const anchorEl = this._getAnchorEl();
        if (anchorEl) {
          this._cleanupAutoUpdate = autoUpdate(anchorEl, this, () => this.reposition());
        }
        await this.updateComplete;
        this._manageFocus();
        this.dispatchEvent(new CustomEvent("open", { bubbles: true, composed: true }));
      };
      this._handleKeydown = (event) => {
        if (event.key !== "Tab")
          return;
        const focusables = this._getFocusables();
        const focused = event.composedPath()[0] ?? document.activeElement;
        const idx = focused ? focusables.indexOf(focused) : -1;
        const tabsOut = focusables.length === 0 || (event.shiftKey ? idx === 0 : idx === focusables.length - 1);
        if (tabsOut) {
          event.preventDefault();
          this.hide();
        }
      };
    }
    get open() {
      return this._isOpen;
    }
    connectedCallback() {
      super.connectedCallback();
      this.style.containerType = "inline-size";
      this.style.containerName = "layout-container";
      if (!this.hasAttribute("popover"))
        this.setAttribute("popover", "");
      if (!this.hasAttribute("tabindex"))
        this.setAttribute("tabindex", "-1");
      if (!this.hasAttribute("role"))
        this.setAttribute("role", "dialog");
      this.addEventListener("toggle", this._handleToggle);
      this.addEventListener("keydown", this._handleKeydown);
      document.addEventListener("pointerdown", this._handleDocumentPointerdown, true);
      document.addEventListener("click", this._handleDocumentClick);
      this._smQuery = window.matchMedia(`(max-width: ${breakpoints.smMax})`);
      this._wasOnSm = this._smQuery.matches;
      this._smQuery.addEventListener("change", this._handleViewportChange);
      Promise.resolve().then(() => {
        this._updateAnchorAria(false);
        this._warnIfMissingLabel();
      });
    }
    disconnectedCallback() {
      super.disconnectedCallback();
      this.removeEventListener("toggle", this._handleToggle);
      this.removeEventListener("keydown", this._handleKeydown);
      document.removeEventListener("pointerdown", this._handleDocumentPointerdown, true);
      document.removeEventListener("click", this._handleDocumentClick);
      this._smQuery?.removeEventListener("change", this._handleViewportChange);
      this._smQuery = null;
      this._cleanupAutoUpdate?.();
      this._cleanupAutoUpdate = null;
      const anchorEl = this._getAnchorEl();
      if (anchorEl) {
        anchorEl.removeAttribute("aria-expanded");
        anchorEl.removeAttribute("aria-haspopup");
        if (this.id && anchorEl.getAttribute("aria-controls") === this.id) {
          anchorEl.removeAttribute("aria-controls");
        }
      }
      this._previousAnchorEl = null;
    }
    updated(changed) {
      if (changed.has("width")) {
        if (this.width) {
          this.style.setProperty("--components-popover-default-width", this.width);
        } else {
          this.style.removeProperty("--components-popover-default-width");
        }
      }
      if (changed.has("accessibleLabel") || changed.has("translations")) {
        this.setAttribute("aria-label", this._resolvedAccessibleLabel);
      }
      if (changed.has("anchor") || changed.has("anchorElement")) {
        this._updateAnchorAria(this._isOpen);
      }
      if (changed.has("top") || changed.has("left") || changed.has("right") || changed.has("bottom") || changed.has("centered")) {
        if (!this.top && !this.centered)
          this.style.removeProperty("top");
        if (!this.left && !this.centered)
          this.style.removeProperty("left");
        if (!this.right)
          this.style.removeProperty("right");
        if (!this.bottom)
          this.style.removeProperty("bottom");
        if (this._isOpen)
          this.reposition();
      }
    }
    // — i18n ——————————————————————————————————————————————————————————————————
    _t(key) {
      return this.translations[key] ?? nlddPopoverTranslations[key];
    }
    get _resolvedAccessibleLabel() {
      return this.accessibleLabel || this._t("components.popover.accessible-label");
    }
    // — Public API ————————————————————————————————————————————————————————————
    show() {
      this._warnIfMissingLabel();
      const anchorEl = this._getAnchorEl();
      if (!anchorEl) {
        if (import_meta17.env?.DEV)
          console.warn("<nldd-popover>: anchor element not found. Set anchor=ID or anchorElement before calling show().");
        return;
      }
      this.showPopover();
    }
    _warnIfMissingLabel() {
      if (!import_meta17.env?.DEV)
        return;
      if (this.accessibleLabel || this._hasWarnedLabel)
        return;
      this._hasWarnedLabel = true;
      console.warn(`<nldd-popover>: No accessible-label provided. Screen readers will announce this popover as "${this._t("components.popover.accessible-label")}". Set accessible-label to a unique, descriptive name.`);
    }
    hide() {
      if (!this._isOpen)
        return;
      this.hidePopover();
    }
    toggle() {
      if (this._isOpen)
        this.hide();
      else
        this.show();
    }
    /**
     * Restore the inline transition after a breakpoint-suppressed tick. Force
     * a layout flush first (`void offsetHeight`) so the just-applied position
     * styles commit before transitions resume — otherwise the cleared
     * `transition: ''` would re-enable animations on those exact properties
     * and you'd still see the slide we were trying to suppress.
     */
    _restoreTransition() {
      void this.offsetHeight;
      this.style.transition = "";
    }
    /** Herberekent positie t.o.v. anchor. Wordt automatisch aangeroepen bij openen. */
    async reposition() {
      if (!this._isOpen)
        return;
      const isSm = this._smQuery?.matches ?? false;
      const crossedBreakpoint = isSm !== this._wasOnSm;
      this._wasOnSm = isSm;
      if (crossedBreakpoint)
        this.style.transition = "none";
      if (isSm) {
        this.style.removeProperty("top");
        this.style.removeProperty("left");
        this.style.removeProperty("right");
        this.style.removeProperty("bottom");
        this.style.removeProperty("transform");
        this.style.removeProperty("--_max-height");
        if (crossedBreakpoint)
          this._restoreTransition();
        return;
      }
      const hasOverride = this.top || this.left || this.right || this.bottom || this.centered;
      if (hasOverride) {
        const yCenter = this.centered && !this.top && !this.bottom;
        const xCenter = this.centered && !this.left && !this.right;
        if (this.top)
          this.style.setProperty("top", this.top);
        else if (yCenter)
          this.style.setProperty("top", "50%");
        if (this.bottom)
          this.style.setProperty("bottom", this.bottom);
        if (this.left)
          this.style.setProperty("left", this.left);
        else if (xCenter)
          this.style.setProperty("left", "50%");
        if (this.right)
          this.style.setProperty("right", this.right);
        if (xCenter || yCenter) {
          this.style.setProperty("transform", `translate(${xCenter ? "-50%" : "0"}, ${yCenter ? "-50%" : "0"})`);
        } else {
          this.style.removeProperty("transform");
        }
        if (crossedBreakpoint)
          this._restoreTransition();
        return;
      }
      const anchorEl = this._getAnchorEl();
      if (!anchorEl)
        return;
      const inset = parseFloat(getComputedStyle(this).getPropertyValue("--semantics-overlays-inset"));
      const { x: x2, y: y3 } = await computePosition2(anchorEl, this, {
        placement: this.placement,
        middleware: [
          flip2({ padding: inset }),
          shift2({ padding: inset }),
          size2({
            padding: inset,
            apply: ({ availableHeight }) => {
              this.style.setProperty("--_max-height", `${availableHeight}px`);
            }
          })
        ]
      });
      Object.assign(this.style, {
        left: `${x2}px`,
        top: `${y3}px`
      });
      if (crossedBreakpoint)
        this._restoreTransition();
    }
    // — Anchor ————————————————————————————————————————————————————————————————
    _getAnchorEl() {
      if (this.anchorElement)
        return this.anchorElement;
      if (this.anchor)
        return document.getElementById(this.anchor);
      return null;
    }
    _updateAnchorAria(open) {
      const anchorEl = this._getAnchorEl();
      if (this._previousAnchorEl && this._previousAnchorEl !== anchorEl) {
        this._previousAnchorEl.removeAttribute("aria-expanded");
        if (this._previousAnchorEl.getAttribute("aria-controls") === this.id) {
          this._previousAnchorEl.removeAttribute("aria-controls");
        }
      }
      if (!anchorEl) {
        this._previousAnchorEl = null;
        return;
      }
      anchorEl.setAttribute("aria-expanded", open ? "true" : "false");
      if (!anchorEl.hasAttribute("aria-haspopup")) {
        anchorEl.setAttribute("aria-haspopup", "dialog");
      }
      if (this.id && !anchorEl.hasAttribute("aria-controls")) {
        anchorEl.setAttribute("aria-controls", this.id);
      }
      this._previousAnchorEl = anchorEl;
    }
    // — Focus ————————————————————————————————————————————————————————————————
    _manageFocus() {
      this.classList.toggle("is-pointer-focus", isPointerMode());
      const autofocusEl = this.querySelector("[autofocus]");
      if (autofocusEl) {
        autofocusEl.focus();
        return;
      }
      this.focus();
    }
    _returnFocus() {
      const target = this._previousFocus ?? this._getAnchorEl();
      this._previousFocus = null;
      target?.focus();
    }
    _getFocusables() {
      const selector = [
        "a[href]",
        "button:not([disabled])",
        "input:not([disabled])",
        "select:not([disabled])",
        "textarea:not([disabled])",
        '[tabindex]:not([tabindex="-1"])',
        '[contenteditable=""]',
        '[contenteditable="true"]',
        "details > summary:first-of-type"
      ].join(",");
      const result = [];
      const visit = (root) => {
        for (const child of Array.from(root.children)) {
          const el = child;
          if (el.matches?.(selector) && !el.hasAttribute("disabled")) {
            if (el.getClientRects().length > 0)
              result.push(el);
          }
          if (el.shadowRoot)
            visit(el.shadowRoot);
          visit(el);
        }
      };
      visit(this);
      return result;
    }
    render() {
      return popoverTemplate();
    }
  };
  NLDDPopover.styles = popoverStyles;
  __decorate65([
    n4({ type: String, reflect: true })
  ], NLDDPopover.prototype, "anchor", void 0);
  __decorate65([
    n4({ attribute: false })
  ], NLDDPopover.prototype, "anchorElement", void 0);
  __decorate65([
    n4({ type: String, reflect: true })
  ], NLDDPopover.prototype, "placement", void 0);
  __decorate65([
    n4({ type: String, reflect: true })
  ], NLDDPopover.prototype, "width", void 0);
  __decorate65([
    n4({ type: String, reflect: true })
  ], NLDDPopover.prototype, "top", void 0);
  __decorate65([
    n4({ type: String, reflect: true })
  ], NLDDPopover.prototype, "left", void 0);
  __decorate65([
    n4({ type: String, reflect: true })
  ], NLDDPopover.prototype, "right", void 0);
  __decorate65([
    n4({ type: String, reflect: true })
  ], NLDDPopover.prototype, "bottom", void 0);
  __decorate65([
    n4({ type: Boolean, reflect: true })
  ], NLDDPopover.prototype, "centered", void 0);
  __decorate65([
    n4({ type: Boolean, reflect: true, attribute: "sm-full-height" })
  ], NLDDPopover.prototype, "smFullHeight", void 0);
  __decorate65([
    n4({ type: String, attribute: "accessible-label" })
  ], NLDDPopover.prototype, "accessibleLabel", void 0);
  __decorate65([
    n4({ type: Object })
  ], NLDDPopover.prototype, "translations", void 0);
  NLDDPopover = __decorate65([
    t3("nldd-popover")
  ], NLDDPopover);

  // node_modules/@nldd/design-system/dist/components/index.js
  init_sheet();

  // node_modules/@nldd/design-system/dist/components/layout/window/window.js
  init_lit();
  init_decorators();

  // node_modules/@nldd/design-system/dist/components/layout/window/window.styles.js
  init_lit();
  init_breakpoints();
  init_slotted_reset();
  var smMax13 = r(breakpoints.smMax);
  var windowStyles = i`


	/* # Host */

	:host {
		${inheritedTextReset}
		display: block;
	}

	:host([hidden]) {
		display: none;
	}


	/* # Block */

	.window {
		display: flex;
		position: fixed;
		margin: auto;
		outline: none;
		border: none;
		border-radius: var(--semantics-overlays-corner-radius);
		box-shadow: var(--semantics-overlays-box-shadow);
		background-color: var(--semantics-surfaces-base-background-color);
		width: var(--components-window-default-width);
		max-width: calc(100vw - var(--semantics-overlays-inset) * 2);
		max-height: calc(100dvh - var(--semantics-overlays-inset) * 2);
		overflow: hidden;
		padding: 0;
		flex-direction: column;
	}

	.window:not([open]) {
		display: none;
	}

	.window:focus-visible:not(.is-pointer-focus) {
		outline: var(--semantics-focus-ring-outline);
		outline-offset: var(--semantics-focus-ring-outline-offset);
		box-shadow: var(--semantics-focus-ring-box-shadow), var(--semantics-overlays-box-shadow);
	}

	.window::backdrop {
		background-color: var(--semantics-overlays-backdrop-color);
	}


	/* # Elements */

	.window__body {
		display: flex;
		min-height: 0;
		width: 100%;
		flex-direction: column;
		flex-grow: 1;
	}

	:host([movable]:not([has-drag-handle])) .window__body {
		cursor: grab;
	}

	:host([movable]:not([has-drag-handle])) .window__body:active {
		cursor: grabbing;
	}

	/* Drag handle cursor is set via JS in _detectDragHandle because
	   ::slotted cannot reach nested elements (e.g. nldd-top-title-bar
	   inside nldd-page). */

	::slotted(*) {
		min-height: 0;
	}

	/* ## Responsive: sm — dragging disabled */

	@media (max-width: ${smMax13}) {
		:host([movable]:not([has-drag-handle])) .window__body {
			cursor: default;
		}

		:host([movable]:not([has-drag-handle])) .window__body:active {
			cursor: default;
		}
	}
`;

  // node_modules/@nldd/design-system/dist/components/layout/window/window.template.js
  init_lit();
  function windowTemplate(component) {
    return b2`
		<dialog class="window"
			aria-label=${component._resolvedAccessibleLabel}
			aria-modal=${component.modeless ? A : "true"}
			@click=${component._handleDialogClick}
			@cancel=${component._handleCancel}
			@pointerdown=${component._handlePointerDown}
		>
			<div class="window__body">
				<slot @slotchange=${component._detectDragHandle}></slot>
			</div>
		</dialog>
	`;
  }

  // node_modules/@nldd/design-system/dist/components/layout/window/window.i18n.js
  var nlddWindowTranslations = {
    "components.window.accessible-label": "Venster"
  };

  // node_modules/@nldd/design-system/dist/components/layout/window/window.js
  init_input_modality();
  init_breakpoints();
  var import_meta19 = {};
  var __decorate67 = function(decorators, target, key, desc) {
    var c6 = arguments.length, r6 = c6 < 3 ? target : desc === null ? desc = Object.getOwnPropertyDescriptor(target, key) : desc, d3;
    if (typeof Reflect === "object" && typeof Reflect.decorate === "function") r6 = Reflect.decorate(decorators, target, key, desc);
    else for (var i8 = decorators.length - 1; i8 >= 0; i8--) if (d3 = decorators[i8]) r6 = (c6 < 3 ? d3(r6) : c6 > 3 ? d3(target, key, r6) : d3(target, key)) || r6;
    return c6 > 3 && r6 && Object.defineProperty(target, key, r6), r6;
  };
  var NLDDWindow = class NLDDWindow2 extends i4 {
    constructor() {
      super(...arguments);
      this.modeless = false;
      this.movable = false;
      this.accessibleLabel = "";
      this.translations = {};
      this.centered = false;
      this.scheme = "inherit";
      this._closing = false;
      this._dragging = false;
      this._dragOffsetX = 0;
      this._dragOffsetY = 0;
      this._didDrag = false;
      this._dragHandle = null;
      this._dragPointerId = -1;
      this._hasWarnedLabel = false;
      this._handleDialogClick = (e9) => {
        if (this._didDrag) {
          this._didDrag = false;
          return;
        }
        if (this.modeless)
          return;
        const dialog = this._dialog;
        if (!dialog)
          return;
        const rect = dialog.getBoundingClientRect();
        const outside = e9.clientX < rect.left || e9.clientX > rect.right || e9.clientY < rect.top || e9.clientY > rect.bottom;
        if (outside) {
          this.hide();
        }
      };
      this._handleCancel = (e9) => {
        e9.preventDefault();
        this.hide();
      };
      this._handlePointerDown = (e9) => {
        if (!this._isMovable)
          return;
        if (this._dragging || this._dragHandle)
          return;
        const target = e9.composedPath()[0];
        if (target?.closest('button, a, input, select, textarea, [role="button"]'))
          return;
        const dialog = this._dialog;
        if (dialog) {
          const rect = dialog.getBoundingClientRect();
          const onBackdrop = e9.clientX < rect.left || e9.clientX > rect.right || e9.clientY < rect.top || e9.clientY > rect.bottom;
          if (onBackdrop)
            return;
        }
        const handle = this._findDragHandle(e9);
        if (!handle)
          return;
        this._dragHandle = handle;
        this._dragPointerId = e9.pointerId;
        handle.addEventListener("pointermove", this._handlePointerMove);
        handle.addEventListener("pointerup", this._handlePointerUp);
        handle.addEventListener("pointercancel", this._handlePointerUp);
      };
      this._handlePointerMove = (e9) => {
        if (!this._dragging && this._dragHandle) {
          this._dragging = true;
          e9.preventDefault();
          try {
            this._dragHandle.setPointerCapture(this._dragPointerId);
          } catch {
          }
          if (this._dragHandle instanceof HTMLElement) {
            this._dragHandle.style.cursor = "grabbing";
          }
          const dialog = this._dialog;
          if (dialog) {
            const rect = dialog.getBoundingClientRect();
            this._dragOffsetX = e9.clientX - rect.left;
            this._dragOffsetY = e9.clientY - rect.top;
          }
        }
        this._didDrag = true;
        const newLeft = e9.clientX - this._dragOffsetX;
        const newTop = e9.clientY - this._dragOffsetY;
        const clamped = this._clampPosition(newLeft, newTop);
        this.left = `${clamped.left}px`;
        this.top = `${clamped.top}px`;
        this.right = void 0;
        this.bottom = void 0;
      };
      this._handlePointerUp = (e9) => {
        const handle = this._dragHandle;
        this._dragging = false;
        this._dragHandle = null;
        if (handle) {
          try {
            handle.releasePointerCapture(e9.pointerId);
          } catch {
          }
          if (handle instanceof HTMLElement) {
            handle.style.cursor = this._isMovable ? "grab" : "";
          }
          handle.removeEventListener("pointermove", this._handlePointerMove);
          handle.removeEventListener("pointerup", this._handlePointerUp);
          handle.removeEventListener("pointercancel", this._handlePointerUp);
        }
      };
      this._detectDragHandle = () => {
        const handle = this.querySelector("[window-drag-handle]");
        this.toggleAttribute("has-drag-handle", handle !== null);
        if (handle) {
          handle.style.cursor = this._isMovable ? "grab" : "";
        }
      };
      this._handleResize = () => {
        this._applyPositionStyles();
        this._clampToViewport();
        this._detectDragHandle();
      };
      this._handleDismiss = () => {
        this.hide();
      };
    }
    get _dialog() {
      return this.shadowRoot?.querySelector("dialog") ?? null;
    }
    get _isMovable() {
      if (!this.movable)
        return false;
      return window.matchMedia(`(min-width: ${breakpoints.mdMin})`).matches;
    }
    connectedCallback() {
      super.connectedCallback();
      this.style.containerType = "inline-size";
      this.style.containerName = "layout-container";
      this.addEventListener("dismiss", this._handleDismiss);
      this._detectDragHandle();
      window.addEventListener("resize", this._handleResize);
    }
    disconnectedCallback() {
      super.disconnectedCallback();
      this.removeEventListener("dismiss", this._handleDismiss);
      this._removeDragListeners();
      window.removeEventListener("resize", this._handleResize);
    }
    firstUpdated() {
      this._applyScheme();
    }
    updated(changed) {
      this._applyPositionStyles();
      if (changed.has("movable")) {
        this._detectDragHandle();
      }
      if (changed.has("scheme")) {
        this._applyScheme();
      }
    }
    _applyScheme() {
      const dialog = this._dialog;
      if (this.scheme === "light" || this.scheme === "dark") {
        this.style.colorScheme = this.scheme;
        if (dialog)
          dialog.style.colorScheme = this.scheme;
      } else {
        this.style.removeProperty("color-scheme");
        if (dialog)
          dialog.style.removeProperty("color-scheme");
      }
    }
    // — i18n ——————————————————————————————————————————————————————————————————
    _t(key) {
      return this.translations[key] ?? nlddWindowTranslations[key];
    }
    get _resolvedAccessibleLabel() {
      return this.accessibleLabel || this._t("components.window.accessible-label");
    }
    show() {
      const dialog = this._dialog;
      if (!dialog)
        return;
      if (import_meta19.env?.DEV && !this.accessibleLabel && !this._hasWarnedLabel) {
        this._hasWarnedLabel = true;
        console.warn(`<nldd-window>: No accessible-label provided. Screen readers will announce this window as "${this._t("components.window.accessible-label")}". Set accessible-label to a unique, descriptive name.`);
      }
      if (this.modeless) {
        dialog.show();
      } else {
        dialog.showModal();
      }
      this._applyPositionStyles();
      this._manageFocus();
      this.dispatchEvent(new CustomEvent("open", { bubbles: true, composed: true }));
    }
    hide() {
      const dialog = this._dialog;
      if (!dialog || !dialog.open || this._closing)
        return;
      this._closing = true;
      this._didDrag = false;
      this._removeDragListeners();
      dialog.close();
      this._closing = false;
      this.dispatchEvent(new CustomEvent("close", { bubbles: true, composed: true }));
    }
    _manageFocus() {
      if (this.querySelector("[autofocus]"))
        return;
      const dialog = this._dialog;
      if (!dialog)
        return;
      dialog.classList.toggle("is-pointer-focus", isPointerMode());
      dialog.focus();
    }
    _applyPositionStyles() {
      const dialog = this._dialog;
      if (!dialog)
        return;
      const hasEdge = this.top !== void 0 || this.left !== void 0 || this.right !== void 0 || this.bottom !== void 0;
      const hasOverride = hasEdge || this.centered;
      const yCenter = this.centered && this.top === void 0 && this.bottom === void 0;
      const xCenter = this.centered && this.left === void 0 && this.right === void 0;
      dialog.style.top = this.top ?? (yCenter ? "50%" : this.bottom !== void 0 ? "auto" : "");
      dialog.style.bottom = this.bottom ?? (this.top !== void 0 ? "auto" : "");
      dialog.style.left = this.left ?? (xCenter ? "50%" : this.right !== void 0 ? "auto" : "");
      dialog.style.right = this.right ?? (this.left !== void 0 ? "auto" : "");
      dialog.style.width = this.width ?? "";
      dialog.style.height = this.height ?? "";
      dialog.style.transform = xCenter || yCenter ? `translate(${xCenter ? "-50%" : "0"}, ${yCenter ? "-50%" : "0"})` : "";
      dialog.style.margin = hasOverride ? "0" : "";
    }
    _findDragHandle(e9) {
      const handle = this.querySelector("[window-drag-handle]");
      if (handle) {
        const path = e9.composedPath();
        if (path.includes(handle))
          return handle;
        return null;
      }
      return this._dialog;
    }
    _removeDragListeners() {
      this._dragging = false;
      if (this._dragHandle) {
        if (this._dragHandle instanceof HTMLElement) {
          this._dragHandle.style.cursor = this._isMovable ? "grab" : "";
        }
        this._dragHandle.removeEventListener("pointermove", this._handlePointerMove);
        this._dragHandle.removeEventListener("pointerup", this._handlePointerUp);
        this._dragHandle.removeEventListener("pointercancel", this._handlePointerUp);
        this._dragHandle = null;
      }
    }
    _clampToViewport() {
      if (this.left === void 0 || this.top === void 0)
        return;
      const dialog = this._dialog;
      if (!dialog || !dialog.open)
        return;
      const dialogRect = dialog.getBoundingClientRect();
      const clamped = this._clampPosition(dialogRect.left, dialogRect.top);
      if (clamped.left !== dialogRect.left || clamped.top !== dialogRect.top) {
        this.left = `${clamped.left}px`;
        this.top = `${clamped.top}px`;
      }
    }
    _clampPosition(dialogLeft, dialogTop) {
      const minVisible = 44;
      const dialog = this._dialog;
      const dialogRect = dialog?.getBoundingClientRect();
      const handle = this._dragHandle ?? this.querySelector("[window-drag-handle]");
      const handleRect = handle?.getBoundingClientRect();
      const grabOffsetX = handleRect && dialogRect ? handleRect.left - dialogRect.left : 0;
      const grabOffsetY = handleRect && dialogRect ? handleRect.top - dialogRect.top : 0;
      const grabWidth = handleRect?.width ?? dialogRect?.width ?? 0;
      const minLeft = -(grabOffsetX + grabWidth - minVisible);
      const maxLeft = window.innerWidth - grabOffsetX - minVisible;
      const minTop = -grabOffsetY;
      const maxTop = window.innerHeight - grabOffsetY - minVisible;
      return {
        left: Math.max(minLeft, Math.min(dialogLeft, maxLeft)),
        top: Math.max(minTop, Math.min(dialogTop, maxTop))
      };
    }
    render() {
      return windowTemplate(this);
    }
  };
  NLDDWindow.styles = windowStyles;
  __decorate67([
    n4({ type: Boolean, reflect: true })
  ], NLDDWindow.prototype, "modeless", void 0);
  __decorate67([
    n4({ type: Boolean, reflect: true, attribute: "movable" })
  ], NLDDWindow.prototype, "movable", void 0);
  __decorate67([
    n4({ type: String, attribute: "accessible-label" })
  ], NLDDWindow.prototype, "accessibleLabel", void 0);
  __decorate67([
    n4({ type: Object })
  ], NLDDWindow.prototype, "translations", void 0);
  __decorate67([
    n4({ type: String, reflect: true })
  ], NLDDWindow.prototype, "top", void 0);
  __decorate67([
    n4({ type: String, reflect: true })
  ], NLDDWindow.prototype, "left", void 0);
  __decorate67([
    n4({ type: String, reflect: true })
  ], NLDDWindow.prototype, "right", void 0);
  __decorate67([
    n4({ type: String, reflect: true })
  ], NLDDWindow.prototype, "bottom", void 0);
  __decorate67([
    n4({ type: Boolean, reflect: true })
  ], NLDDWindow.prototype, "centered", void 0);
  __decorate67([
    n4({ type: String, reflect: true })
  ], NLDDWindow.prototype, "width", void 0);
  __decorate67([
    n4({ type: String, reflect: true })
  ], NLDDWindow.prototype, "height", void 0);
  __decorate67([
    n4({ type: String, reflect: true })
  ], NLDDWindow.prototype, "scheme", void 0);
  NLDDWindow = __decorate67([
    t3("nldd-window")
  ], NLDDWindow);

  // node_modules/@nldd/design-system/dist/components/layout/page-footer/page-footer.js
  init_lit();
  init_decorators();

  // node_modules/@nldd/design-system/dist/components/layout/page-footer/page-footer.styles.js
  init_lit();
  init_breakpoints();
  init_slotted_reset();
  var smMax14 = r(breakpoints.smMax);
  var mdMin16 = r(breakpoints.mdMin);
  var mdMax10 = r(breakpoints.mdMax);
  var lgMin12 = r(breakpoints.lgMin);
  var pageFooterStyles = i`
	:host {
		--_max-width: var(--semantics-page-sections-body-max-width);
		/* Official Rijksoverheid lintje colour — identical across all
		   .rijks.app / .overheid.nl sites by visual-identity policy, so it
		   intentionally lives outside the semantic token system. Local
		   --_ var keeps the value discoverable in one place. */
		--_lintje-color: #154273;
		--context-parent-background-color: var(--components-page-footer-background-color);

		container-type: inline-size;
		display: block;
		background-color: var(--components-page-footer-background-color);
	}

	:host([hidden]) {
		display: none;
	}

	.page-footer {
		--_lintje-height: calc(var(--_lintje-width) / 2);

		box-sizing: border-box;
		display: flex;
		position: relative;
		width: 100%;
		flex-direction: column;
		align-items: center;

		@container (max-width: ${smMax14}) {
			padding-inline: var(--semantics-page-sections-sm-margin-inline);
			--_lintje-width: var(--primitives-space-40);
		}

		@container (min-width: ${mdMin16}) and (max-width: ${mdMax10}) {
			padding-inline: var(--semantics-page-sections-md-margin-inline);
			--_lintje-width: var(--primitives-space-44);
		}

		@container (min-width: ${lgMin12}) {
			padding-inline: var(--semantics-page-sections-lg-margin-inline);
			--_lintje-width: var(--primitives-space-48);
		}
	}

	.page-footer::after {
		content: '';
		position: absolute;
		bottom: 0;
		left: 50%;
		background-color: var(--_lintje-color);
		width: var(--_lintje-width);
		height: var(--_lintje-height);
		transform: translateX(-50%);
	}

	@media (forced-colors: active) {
		.page-footer::after {
			background-color: ButtonText;
		}
	}

	.page-footer__body {
		display: flex;
		width: 100%;
		max-width: var(--_max-width);
		flex-direction: column;
	}

	.page-footer__breadcrumbs,
	.page-footer__legal-bar {
		@container (max-width: ${smMax14}) {
			padding-block: var(--primitives-space-16);
		}

		@container (min-width: ${mdMin16}) and (max-width: ${mdMax10}) {
			padding-block: var(--primitives-space-24);
		}

		@container (min-width: ${lgMin12}) {
			padding-block: var(--primitives-space-24);
		}
	}

	.page-footer__main {
		@container (max-width: ${smMax14}) {
			padding-block: var(--primitives-space-24);
		}

		@container (min-width: ${mdMin16}) and (max-width: ${mdMax10}) {
			padding-block: var(--primitives-space-32);
		}

		@container (min-width: ${lgMin12}) {
			padding-block: var(--primitives-space-48);
		}
	}

	.page-footer__body > div:not([hidden]):not(:has(~ div:not([hidden]))) {
		padding-bottom: calc(var(--primitives-space-16) + var(--_lintje-height));
	}

	:host([single-slot]) .page-footer__body > div:not([hidden]) {
		padding-top: calc(var(--primitives-space-16) + var(--_lintje-height));
	}

	.page-footer__breadcrumbs[hidden],
	.page-footer__main[hidden],
	.page-footer__legal-bar[hidden],
	.page-footer__divider[hidden] {
		display: none;
	}

	.page-footer__divider {
		margin: 0;
		border: none;
		background-color: var(--components-page-footer-divider-color);
		width: 100%;
		height: var(--semantics-dividers-thickness);
	}

	@media (forced-colors: active) {
		.page-footer__divider {
			background-color: CanvasText;
		}
	}
`;
  var pageFooterLegalBarStyles = i`
	:host {
		display: block;
	}

	:host([hidden]) {
		display: none;
	}

	.page-footer__legal-bar {
		display: flex;
		flex-wrap: wrap;
		row-gap: var(--primitives-space-4);
		column-gap: var(--primitives-space-16);
		align-items: flex-start;
		justify-content: space-between;
	}

	.page-footer__legal-bar-start {
		display: flex;
		flex-wrap: wrap;
		gap: var(--primitives-space-12);
		order: 0;
	}

	.page-footer__legal-bar-end {
		display: flex;
		flex-wrap: wrap;
		gap: var(--primitives-space-12);
		order: 1;
	}

	.page-footer__legal-bar-start[hidden],
	.page-footer__legal-bar-end[hidden] {
		display: none;
	}
`;
  var pageFooterLegalBarItemStyles = i`
	:host {
		${inheritedTextReset}
		display: inline-flex;
	}

	:host([hidden]) {
		display: none;
	}

	.page-footer__legal-bar-item {
		display: inline-flex;
		color: var(--components-page-footer-legal-bar-item-color);
		font: var(--primitives-font-body-sm-regular-tight);
	}

	.page-footer__legal-bar-item-link {
		color: var(--components-page-footer-legal-bar-item-color);
		text-decoration: underline;
	}

	.page-footer__legal-bar-item-link:focus-visible {
		outline: var(--semantics-focus-ring-outline);
		outline-offset: var(--semantics-focus-ring-outline-offset);
		border-radius: var(--primitives-corner-radius-xs);
		box-shadow: var(--semantics-focus-ring-box-shadow);
	}
`;

  // node_modules/@nldd/design-system/dist/components/layout/page-footer/page-footer.template.js
  init_lit();
  function pageFooterTemplate(component) {
    const showDividerAboveMain = component._hasBreadcrumbs && component._hasMain;
    const showDividerAboveLegal = (component._hasBreadcrumbs || component._hasMain) && component._hasLegalBar;
    return b2`
		<div class="page-footer">
			<div class="page-footer__body">
				<div class="page-footer__breadcrumbs"
					?hidden=${!component._hasBreadcrumbs}
				>
					<slot name="breadcrumbs" @slotchange=${component._onSlotChange}></slot>
				</div>
				<hr class="page-footer__divider"
					?hidden=${!showDividerAboveMain}
				>
				<div class="page-footer__main"
					?hidden=${!component._hasMain}
				>
					<slot @slotchange=${component._onSlotChange}></slot>
				</div>
				<hr class="page-footer__divider"
					?hidden=${!showDividerAboveLegal}
				>
				<div class="page-footer__legal-bar"
					?hidden=${!component._hasLegalBar}
				>
					<slot name="legal-bar" @slotchange=${component._onSlotChange}></slot>
				</div>
			</div>
		</div>
	`;
  }
  function pageFooterLegalBarTemplate(component) {
    const label = component._t("components.page-footer.legal-bar-accessible-label");
    return b2`
		<nav class="page-footer__legal-bar"
			aria-label=${label || A}
			?hidden=${!component._hasStart && !component._hasEnd}
		>
			<div class="page-footer__legal-bar-start"
				?hidden=${!component._hasStart}
			>
				<slot name="start" @slotchange=${component._onSlotChange}></slot>
			</div>
			<div class="page-footer__legal-bar-end"
				?hidden=${!component._hasEnd}
			>
				<slot name="end" @slotchange=${component._onSlotChange}></slot>
			</div>
		</nav>
	`;
  }
  function pageFooterLegalBarItemTemplate(component) {
    const label = component.text || b2`<slot></slot>`;
    if (component.href) {
      return b2`
			<span class="page-footer__legal-bar-item">
				<a class="page-footer__legal-bar-item-link"
					href=${component.href}
				>${label}</a>
			</span>
		`;
    }
    return b2`<span class="page-footer__legal-bar-item">${label}</span>`;
  }

  // node_modules/@nldd/design-system/dist/components/layout/page-footer/page-footer.i18n.js
  var nlddPageFooterTranslations = {
    "components.page-footer.legal-bar-accessible-label": "Juridische links"
  };

  // node_modules/@nldd/design-system/dist/components/layout/page-footer/page-footer.js
  var import_meta20 = {};
  var __decorate68 = function(decorators, target, key, desc) {
    var c6 = arguments.length, r6 = c6 < 3 ? target : desc === null ? desc = Object.getOwnPropertyDescriptor(target, key) : desc, d3;
    if (typeof Reflect === "object" && typeof Reflect.decorate === "function") r6 = Reflect.decorate(decorators, target, key, desc);
    else for (var i8 = decorators.length - 1; i8 >= 0; i8--) if (d3 = decorators[i8]) r6 = (c6 < 3 ? d3(r6) : c6 > 3 ? d3(target, key, r6) : d3(target, key)) || r6;
    return c6 > 3 && r6 && Object.defineProperty(target, key, r6), r6;
  };
  var NLDDPageFooterLegalBarItem = class extends i4 {
    constructor() {
      super(...arguments);
      this.text = "";
    }
    render() {
      return pageFooterLegalBarItemTemplate(this);
    }
  };
  NLDDPageFooterLegalBarItem.styles = pageFooterLegalBarItemStyles;
  __decorate68([
    n4({ type: String, reflect: true })
  ], NLDDPageFooterLegalBarItem.prototype, "href", void 0);
  __decorate68([
    n4({ type: String, reflect: true })
  ], NLDDPageFooterLegalBarItem.prototype, "text", void 0);
  if (!customElements.get("nldd-page-footer-legal-bar-item")) {
    customElements.define("nldd-page-footer-legal-bar-item", NLDDPageFooterLegalBarItem);
  }
  var NLDDPageFooterLegalBar = class extends i4 {
    constructor() {
      super(...arguments);
      this.accessibleLabel = "";
      this.translations = {};
      this._hasStart = false;
      this._hasEnd = false;
      this._mergedTranslations = { ...nlddPageFooterTranslations };
      this._onSlotChange = (e9) => {
        const slot = e9.target;
        const hasContent = slot.assignedElements().length > 0;
        const name = slot.getAttribute("name") ?? "";
        if (name === "start")
          this._hasStart = hasContent;
        else if (name === "end")
          this._hasEnd = hasContent;
      };
    }
    willUpdate(changed) {
      if (changed.has("translations") || changed.has("accessibleLabel")) {
        this._mergedTranslations = {
          ...nlddPageFooterTranslations,
          ...this.translations
        };
        if (this.accessibleLabel) {
          this._mergedTranslations["components.page-footer.legal-bar-accessible-label"] = this.accessibleLabel;
        }
      }
    }
    _t(key) {
      const value = this._mergedTranslations[key];
      if (value === void 0 && import_meta20.env?.DEV) {
        console.warn(`<nldd-page-footer-legal-bar>: missing translation for "${key}"`);
      }
      return value ?? "";
    }
    render() {
      return pageFooterLegalBarTemplate(this);
    }
  };
  NLDDPageFooterLegalBar.styles = pageFooterLegalBarStyles;
  __decorate68([
    n4({ type: String, attribute: "accessible-label" })
  ], NLDDPageFooterLegalBar.prototype, "accessibleLabel", void 0);
  __decorate68([
    n4({ type: Object })
  ], NLDDPageFooterLegalBar.prototype, "translations", void 0);
  __decorate68([
    r5()
  ], NLDDPageFooterLegalBar.prototype, "_hasStart", void 0);
  __decorate68([
    r5()
  ], NLDDPageFooterLegalBar.prototype, "_hasEnd", void 0);
  if (!customElements.get("nldd-page-footer-legal-bar")) {
    customElements.define("nldd-page-footer-legal-bar", NLDDPageFooterLegalBar);
  }
  var NLDDPageFooter = class NLDDPageFooter2 extends i4 {
    constructor() {
      super(...arguments);
      this._hasBreadcrumbs = false;
      this._hasMain = false;
      this._hasLegalBar = false;
      this._onSlotChange = (e9) => {
        const slot = e9.target;
        const hasContent = this._hasMeaningfulContent(slot);
        const name = slot.getAttribute("name") ?? "";
        if (name === "breadcrumbs")
          this._hasBreadcrumbs = hasContent;
        else if (name === "legal-bar")
          this._hasLegalBar = hasContent;
        else
          this._hasMain = hasContent;
        const visibleCount = (this._hasBreadcrumbs ? 1 : 0) + (this._hasMain ? 1 : 0) + (this._hasLegalBar ? 1 : 0);
        this.toggleAttribute("single-slot", visibleCount === 1);
      };
    }
    connectedCallback() {
      super.connectedCallback();
      if (!this.hasAttribute("role")) {
        this.setAttribute("role", "contentinfo");
      }
      if (!this.id) {
        if (import_meta20.env?.DEV && document.getElementById("page-footer")) {
          console.warn('<nldd-page-footer>: another element already has id="page-footer"; set a unique id on this instance so skip-links target the right one.');
        }
        this.id = "page-footer";
      }
    }
    _hasMeaningfulContent(slot) {
      const nodes = slot?.assignedNodes({ flatten: true }) ?? [];
      return nodes.some((n7) => n7.nodeType === Node.ELEMENT_NODE || n7.nodeType === Node.TEXT_NODE && (n7.textContent?.trim() ?? "") !== "");
    }
    render() {
      return pageFooterTemplate(this);
    }
  };
  NLDDPageFooter.styles = pageFooterStyles;
  __decorate68([
    r5()
  ], NLDDPageFooter.prototype, "_hasBreadcrumbs", void 0);
  __decorate68([
    r5()
  ], NLDDPageFooter.prototype, "_hasMain", void 0);
  __decorate68([
    r5()
  ], NLDDPageFooter.prototype, "_hasLegalBar", void 0);
  NLDDPageFooter = __decorate68([
    t3("nldd-page-footer")
  ], NLDDPageFooter);

  // node_modules/@nldd/design-system/dist/components/index.js
  init_list();
  init_list_item();

  // node_modules/@nldd/design-system/dist/components/lists-and-tables/table/table.js
  init_lit();
  init_decorators();

  // node_modules/@nldd/design-system/dist/components/lists-and-tables/table/table.styles.js
  init_lit();
  init_slotted_reset();
  var tableStyles = i`
	:host {
		--_columns: none;
		--_corner-radius: var(--semantics-tables-corner-radius);
		--_column-gap: var(--semantics-tables-column-gap);
		--_background-color: var(--semantics-surfaces-base-background-color);
		--_border-color: var(--semantics-surfaces-base-border-color);
		/* Shared so the focus-visible rule can compose the focus ring over the
		   border ring with var(...) instead of repeating the inset literal. */
		--_border-shadow: inset 0 0 0 var(--semantics-tables-border-width) var(--_border-color);

		${inheritedTextReset}
		box-sizing: border-box;
		display: grid;
		grid-template-columns: var(--_columns);
		width: 100%;
		/* The table is always a boxed surface and its own horizontal scroll
		   container: the columns scroll when they don't fit and the table is made
		   focusable in JS while it scrolls. overflow-y stays hidden so the rounded
		   corners keep clipping the rows (and their full-bleed dividers). */
		border-radius: var(--_corner-radius);
		box-shadow: var(--_border-shadow);
		background-color: var(--_background-color);
		overflow-x: auto;
		overflow-y: hidden;
		column-gap: var(--_column-gap);
		row-gap: 0;
	}

	:host([hidden]) {
		display: none;
	}

	/* Default surface is base; tinted is opt-in. */
	:host([background="tinted"]) {
		--_background-color: var(--semantics-surfaces-tinted-background-color);
		--_border-color: var(--semantics-surfaces-tinted-border-color);
	}

	/* Keyboard focus while the table scrolls (it gains tabindex in JS): the
	   project focus ring, layered over the border ring (mirrors nldd-code-viewer).
	   Focus first (outer), border second (inner). */
	:host(:focus-visible) {
		outline: var(--semantics-focus-ring-outline);
		outline-offset: var(--semantics-focus-ring-outline-offset);
		box-shadow: var(--semantics-focus-ring-box-shadow), var(--_border-shadow);
	}

	:host(:focus:not(:focus-visible)) {
		outline: none;
	}

	slot {
		display: contents;
	}

	/* Empty state spans all columns below the rows; its own padding gives the
	   message room. The header is hidden in the empty state (only the message). */
	.table__empty {
		grid-column: 1 / -1;
	}

	.table__empty[hidden] {
		display: none;
	}

	.table__empty-cell {
		padding: var(--primitives-space-16);
	}

	/* Empty state: collapse the column tracks to a single box-wide column so the
	   message spans the surface instead of the (wider) data columns — otherwise
	   the empty box scrolls horizontally. */
	:host(.is-empty) {
		grid-template-columns: minmax(0, 1fr);
	}

	:host(.is-empty) slot[name="header"] {
		display: none;
	}
`;
  var tableRowStyles = i`
	:host {
		--_min-height: var(--semantics-tables-row-min-height);
		--_padding-block: var(--semantics-tables-row-padding-block);
		--_padding-inline: var(--semantics-tables-row-padding-inline);
		--_divider-color: var(--semantics-tables-border-color);
		--_divider-thickness: var(--semantics-tables-border-width);

		${inheritedTextReset}
		box-sizing: border-box;
		display: grid;
		grid-column: 1 / -1;
		grid-template-columns: subgrid;
		min-height: var(--_min-height);
		border-bottom: var(--_divider-thickness) solid var(--_divider-color);
		padding-block: var(--_padding-block);
		padding-inline: var(--_padding-inline);
		align-items: center;
	}

	:host([hidden]) {
		display: none;
	}

	/* Selection: a full-bleed row tint (reaches the box edges, corners clipped by
	   the table) plus the selected content color, from the shared table tokens. */
	:host([selected]) {
		background-color: var(--semantics-tables-row-is-selected-background-color);
		--context-cell-content-color: var(--semantics-tables-row-is-selected-content-color);
	}

	/* The box's border ring is the table's outer frame, so the last row drops its
	   divider (no doubling on the ring). Inner dividers run full-bleed to the
	   sides — the padding only insets the cell content, not the row's border. */
	:host(:last-child) {
		border-bottom: none;
	}

	/* display:contents so the cells become grid items of this subgrid. */
	slot {
		display: contents;
	}
`;

  // node_modules/@nldd/design-system/dist/components/lists-and-tables/table/table.template.js
  init_lit();
  function tableTemplate(emptyText, emptySupportingText) {
    return b2`
		<slot name="header"></slot>
		<slot></slot>
		<div class="table__empty" role="row" hidden>
			<div class="table__empty-cell" role="cell">
				<slot name="empty">
					<nldd-inline-dialog
						text=${emptyText}
						supporting-text=${emptySupportingText || A}
					></nldd-inline-dialog>
				</slot>
			</div>
		</div>
	`;
  }
  function tableRowTemplate(onSlotChange) {
    return b2`<slot @slotchange=${onSlotChange}></slot>`;
  }

  // node_modules/@nldd/design-system/dist/components/lists-and-tables/table/table.i18n.js
  var nlddTableTranslations = {
    "components.table.accessible-label": "Tabel",
    "components.table.empty-text": "Geen items"
  };

  // node_modules/@nldd/design-system/dist/components/lists-and-tables/table/table.js
  init_breakpoints();
  init_inline_dialog();
  var import_meta23 = {};
  var __decorate71 = function(decorators, target, key, desc) {
    var c6 = arguments.length, r6 = c6 < 3 ? target : desc === null ? desc = Object.getOwnPropertyDescriptor(target, key) : desc, d3;
    if (typeof Reflect === "object" && typeof Reflect.decorate === "function") r6 = Reflect.decorate(decorators, target, key, desc);
    else for (var i8 = decorators.length - 1; i8 >= 0; i8--) if (d3 = decorators[i8]) r6 = (c6 < 3 ? d3(r6) : c6 > 3 ? d3(target, key, r6) : d3(target, key)) || r6;
    return c6 > 3 && r6 && Object.defineProperty(target, key, r6), r6;
  };
  var SM_MAX = parseInt(breakpoints.smMax, 10);
  var MD_MAX = parseInt(breakpoints.mdMax, 10);
  var NLDDTable = class NLDDTable2 extends i4 {
    constructor() {
      super(...arguments);
      this.background = "base";
      this.columns = "";
      this.smColumns = "";
      this.mdColumns = "";
      this.lgColumns = "";
      this.accessibleLabel = "";
      this.selectable = false;
      this.emptyText = "";
      this.emptySupportingText = "";
      this.translations = {};
      this._warnedColumns = false;
      this._warnedLabel = false;
      this._width = 0;
    }
    _t(key) {
      return this.translations[key] ?? nlddTableTranslations[key];
    }
    connectedCallback() {
      super.connectedCallback();
      this.setAttribute("role", "table");
      this.style.containerType = "inline-size";
      this.style.containerName = "cells-container";
      this._resizeObserver = new ResizeObserver((entries) => {
        this._width = entries[0].contentRect.width;
        this._applyColumns();
        this._syncHostA11y();
      });
      this._resizeObserver.observe(this);
    }
    disconnectedCallback() {
      super.disconnectedCallback();
      this._resizeObserver?.disconnect();
      this._resizeObserver = void 0;
      this._rowsObserver?.disconnect();
      this._rowsObserver = void 0;
    }
    firstUpdated() {
      const slot = this.shadowRoot?.querySelector("slot:not([name])");
      slot?.addEventListener("slotchange", () => this._updateEmpty());
      this._rowsObserver = new MutationObserver((mutations) => {
        const relevant = mutations.some((m3) => {
          if (m3.type === "childList")
            return m3.target === this;
          if (m3.type === "attributes" && m3.attributeName === "hidden") {
            return m3.target instanceof Element && m3.target.parentElement === this;
          }
          return false;
        });
        if (relevant)
          this._updateEmpty();
      });
      this._rowsObserver.observe(this, {
        childList: true,
        subtree: true,
        attributes: true,
        attributeFilter: ["hidden"]
      });
      this._updateEmpty();
    }
    /** Show the empty-state slot when there are no visible body rows (header
     *  excluded). Toggled imperatively (like the tabindex affordance) so it
     *  doesn't schedule a reactive re-render out of firstUpdated. The `is-empty`
     *  host class drives hiding the header (CSS) so only the message shows. */
    _updateEmpty() {
      const empty = this.shadowRoot?.querySelector(".table__empty");
      if (!empty)
        return;
      const slot = this.shadowRoot?.querySelector("slot:not([name])");
      const rows = (slot?.assignedElements() ?? []).filter((el) => el.localName === "nldd-table-row");
      const isEmpty = rows.length === 0 || rows.every((row) => row.hasAttribute("hidden"));
      empty.toggleAttribute("hidden", !isEmpty);
      this.classList.toggle("is-empty", isEmpty);
    }
    /** The track list for the current width: a breakpoint override when set,
     *  otherwise the base `columns`. */
    _activeColumns() {
      if (this._width <= SM_MAX)
        return this.smColumns || this.columns;
      if (this._width <= MD_MAX)
        return this.mdColumns || this.columns;
      return this.lgColumns || this.columns;
    }
    _applyColumns() {
      const cols = this._activeColumns();
      if (cols) {
        this.style.setProperty("--_columns", cols);
      } else {
        this.style.removeProperty("--_columns");
      }
    }
    /** Make the table focusable while it scrolls horizontally so keyboard users
     *  can pan it (mirrors nldd-code-viewer / nldd-rich-text), and keep its
     *  accessible name in sync. The table is its own scroll container
     *  (overflow-x: auto); the overflow-x check (not width alone) is defensive.
     *  accessible-label is the name (role="table" needs one); when it is absent a
     *  generic fallback keeps the table (and any focusable region) named. */
    _syncHostA11y() {
      const overflowX = getComputedStyle(this).overflowX;
      const scrollableOverflow = overflowX === "auto" || overflowX === "scroll";
      const scrollable = scrollableOverflow && this.scrollWidth > this.clientWidth;
      if (scrollable) {
        this.setAttribute("tabindex", "0");
      } else {
        this.removeAttribute("tabindex");
      }
      if (this.accessibleLabel) {
        this.setAttribute("aria-label", this.accessibleLabel);
      } else {
        this.setAttribute("aria-label", this._t("components.table.accessible-label"));
      }
    }
    updated(changed) {
      if (changed.has("columns") || changed.has("smColumns") || changed.has("mdColumns") || changed.has("lgColumns")) {
        this._applyColumns();
      }
      if (changed.has("accessibleLabel")) {
        this._syncHostA11y();
      }
      if (changed.has("selectable")) {
        for (const row of this.querySelectorAll("nldd-table-row")) {
          row._syncAriaSelected();
        }
      }
      if (import_meta23.env?.DEV && !this.columns && !this._warnedColumns) {
        this._warnedColumns = true;
        console.warn('<nldd-table>: no `columns` set. Define a CSS grid track list (e.g. columns="200px 1fr 80px") so cells align into columns.');
      } else if (this.columns) {
        this._warnedColumns = false;
      }
      if (import_meta23.env?.DEV && !this.accessibleLabel && !this._warnedLabel) {
        this._warnedLabel = true;
        console.warn('<nldd-table>: no `accessible-label` set. role="table" needs an accessible name \u2014 add accessible-label="\u2026".');
      } else if (this.accessibleLabel) {
        this._warnedLabel = false;
      }
    }
    render() {
      return tableTemplate(this.emptyText || this._t("components.table.empty-text"), this.emptySupportingText);
    }
  };
  NLDDTable.styles = tableStyles;
  __decorate71([
    n4({ type: String, reflect: true })
  ], NLDDTable.prototype, "background", void 0);
  __decorate71([
    n4({ type: String, reflect: true })
  ], NLDDTable.prototype, "columns", void 0);
  __decorate71([
    n4({ type: String, reflect: true, attribute: "sm-columns" })
  ], NLDDTable.prototype, "smColumns", void 0);
  __decorate71([
    n4({ type: String, reflect: true, attribute: "md-columns" })
  ], NLDDTable.prototype, "mdColumns", void 0);
  __decorate71([
    n4({ type: String, reflect: true, attribute: "lg-columns" })
  ], NLDDTable.prototype, "lgColumns", void 0);
  __decorate71([
    n4({ type: String, reflect: true, attribute: "accessible-label" })
  ], NLDDTable.prototype, "accessibleLabel", void 0);
  __decorate71([
    n4({ type: Boolean, reflect: true })
  ], NLDDTable.prototype, "selectable", void 0);
  __decorate71([
    n4({ type: String, attribute: "empty-text" })
  ], NLDDTable.prototype, "emptyText", void 0);
  __decorate71([
    n4({ type: String, attribute: "empty-supporting-text" })
  ], NLDDTable.prototype, "emptySupportingText", void 0);
  __decorate71([
    n4({ type: Object })
  ], NLDDTable.prototype, "translations", void 0);
  NLDDTable = __decorate71([
    t3("nldd-table")
  ], NLDDTable);
  var NLDDTableRow = class NLDDTableRow2 extends i4 {
    constructor() {
      super(...arguments);
      this.selected = false;
      this._onSlotChangeBound = this._onSlotChange.bind(this);
    }
    connectedCallback() {
      super.connectedCallback();
      this.setAttribute("role", "row");
      this._syncAriaSelected();
    }
    updated(changed) {
      if (changed.has("selected")) {
        this._syncAriaSelected();
      }
    }
    /** A header row lives in the table's `header` slot. */
    get _isHeader() {
      return this.getAttribute("slot") === "header";
    }
    /** Reflect `selected` to aria-selected — but only when the parent table is
     *  `selectable`, so a non-selectable table doesn't announce a false
     *  selection affordance (ARIA 1.2 §6.6.5). Header rows are never selectable.
     *  @internal Re-invoked by the parent table when its `selectable` changes. */
    _syncAriaSelected() {
      const selectable = !this._isHeader && this.closest("nldd-table")?.hasAttribute("selectable") === true;
      if (selectable) {
        this.setAttribute("aria-selected", String(this.selected));
      } else {
        this.removeAttribute("aria-selected");
      }
    }
    _onSlotChange(e9) {
      const slot = e9.target;
      const cells = slot.assignedElements();
      const cellRole = this._isHeader ? "columnheader" : "cell";
      for (const cell of cells) {
        cell.setAttribute("role", cellRole);
        if (cell.localName === "nldd-cell") {
          const w2 = cell.getAttribute("width");
          if (w2 === null || w2 === "" || w2 === "fit-content") {
            cell.setAttribute("width", "full");
          }
        }
      }
    }
    render() {
      return tableRowTemplate(this._onSlotChangeBound);
    }
  };
  NLDDTableRow.styles = tableRowStyles;
  __decorate71([
    n4({ type: Boolean, reflect: true })
  ], NLDDTableRow.prototype, "selected", void 0);
  NLDDTableRow = __decorate71([
    t3("nldd-table-row")
  ], NLDDTableRow);

  // node_modules/@nldd/design-system/dist/components/lists-and-tables/cells/cell/cell.js
  init_lit();
  init_decorators();

  // node_modules/@nldd/design-system/dist/components/lists-and-tables/cells/cell/cell.styles.js
  init_lit();
  init_slotted_reset();
  var cellStyles = i`


	/* # Host */

	:host {
		--_width: auto;
		--_min-width: 0;
		--_max-width: none;
		--_min-height: 0;

		${inheritedTextReset}
		display: flex;
		width: var(--_width);
		min-width: var(--_min-width);
		max-width: var(--_max-width);
		min-height: var(--_min-height);
		flex-direction: column;
		flex-shrink: 0;
		align-items: flex-start;
		justify-content: center;
	}

	:host([hidden]) {
		display: none;
	}


	/* # Width */

	:host([width="full"]) {
		flex-grow: 1;
		flex-shrink: 1;
		flex-basis: 0;
	}

	:host([width="fit-content"]),
	:host(:not([width])),
	:host([width=""]) {
		width: fit-content;
		flex-grow: 0;
		flex-basis: auto;
	}

	:host([width]:not([width="full"]):not([width="fit-content"]):not([width=""])) {
		flex-shrink: 0;
	}

	:host([max-width]) {
		flex-basis: var(--_max-width);
	}


	/* # Vertical alignment */

	:host([vertical-alignment="center"]),
	:host(:not([vertical-alignment])) {
		align-self: stretch;
	}

	:host([vertical-alignment="top"]) {
		align-self: flex-start;
	}

	:host([vertical-alignment="bottom"]) {
		align-self: flex-end;
	}


	/* # Horizontal alignment */

	:host([horizontal-alignment="left"]),
	:host(:not([horizontal-alignment])) {
		align-items: flex-start;
	}

	:host([horizontal-alignment="center"]) {
		align-items: center;
	}

	:host([horizontal-alignment="right"]) {
		align-items: flex-end;
	}


	/* # Slotted content */

	::slotted(*) {
		flex-shrink: 0;
	}
`;

  // node_modules/@nldd/design-system/dist/components/lists-and-tables/cells/cell/cell.template.js
  init_lit();
  var template15 = () => b2`<slot></slot>`;

  // node_modules/@nldd/design-system/dist/components/lists-and-tables/cells/cell/cell.js
  init_visibility_mixin();
  var __decorate72 = function(decorators, target, key, desc) {
    var c6 = arguments.length, r6 = c6 < 3 ? target : desc === null ? desc = Object.getOwnPropertyDescriptor(target, key) : desc, d3;
    if (typeof Reflect === "object" && typeof Reflect.decorate === "function") r6 = Reflect.decorate(decorators, target, key, desc);
    else for (var i8 = decorators.length - 1; i8 >= 0; i8--) if (d3 = decorators[i8]) r6 = (c6 < 3 ? d3(r6) : c6 > 3 ? d3(target, key, r6) : d3(target, key)) || r6;
    return c6 > 3 && r6 && Object.defineProperty(target, key, r6), r6;
  };
  var NLDDCell = class NLDDCell2 extends VisibilityMixin(i4, "cells-container") {
    constructor() {
      super(...arguments);
      this.width = "fit-content";
      this.verticalAlignment = "center";
      this.horizontalAlignment = "left";
    }
    updated(changed) {
      super.updated(changed);
      if (changed.has("width") || changed.has("minWidth") || changed.has("maxWidth") || changed.has("minHeight")) {
        this._applyDimensionStyles();
      }
    }
    _applyDimensionStyles() {
      const w2 = this.width;
      const widthIsKeyword = w2 === "full" || w2 === "fit-content";
      const widthIsValidLength = !!w2 && !widthIsKeyword && CSS.supports("width", w2);
      if (widthIsValidLength) {
        this.style.setProperty("--_width", w2);
      } else {
        this.style.removeProperty("--_width");
      }
      if (w2 && !widthIsKeyword && !widthIsValidLength) {
        this.width = "";
      }
      if (this.minWidth) {
        this.style.setProperty("--_min-width", this.minWidth);
      } else {
        this.style.removeProperty("--_min-width");
      }
      if (this.maxWidth) {
        this.style.setProperty("--_max-width", this.maxWidth);
      } else {
        this.style.removeProperty("--_max-width");
      }
      if (this.minHeight) {
        this.style.setProperty("--_min-height", this.minHeight);
      } else {
        this.style.removeProperty("--_min-height");
      }
    }
    render() {
      return template15();
    }
  };
  NLDDCell.styles = [cellStyles];
  __decorate72([
    n4({ type: String, reflect: true })
  ], NLDDCell.prototype, "width", void 0);
  __decorate72([
    n4({ type: String, reflect: true, attribute: "min-width" })
  ], NLDDCell.prototype, "minWidth", void 0);
  __decorate72([
    n4({ type: String, reflect: true, attribute: "max-width" })
  ], NLDDCell.prototype, "maxWidth", void 0);
  __decorate72([
    n4({ type: String, reflect: true, attribute: "min-height" })
  ], NLDDCell.prototype, "minHeight", void 0);
  __decorate72([
    n4({ reflect: true, attribute: "vertical-alignment" })
  ], NLDDCell.prototype, "verticalAlignment", void 0);
  __decorate72([
    n4({ reflect: true, attribute: "horizontal-alignment" })
  ], NLDDCell.prototype, "horizontalAlignment", void 0);
  NLDDCell = __decorate72([
    t3("nldd-cell")
  ], NLDDCell);

  // node_modules/@nldd/design-system/dist/components/index.js
  init_spacer_cell();

  // node_modules/@nldd/design-system/dist/components/lists-and-tables/cells/title-cell/title-cell.js
  init_lit();
  init_decorators();

  // node_modules/@nldd/design-system/dist/components/lists-and-tables/cells/title-cell/title-cell.styles.js
  init_lit();
  init_slotted_reset();
  var titleCellStyles = i`


	/* # Host */

	:host {
		--_width: auto;
		--_min-width: 0;
		--_max-width: none;
		--_min-height: 0;
		--_text-align: start;
		--_secondary-color: var(--context-cell-content-color, var(--semantics-content-secondary-color));
		--_title-color: var(--context-cell-content-color, var(--semantics-content-color));
		--_title-font: var(--primitives-font-display-5-sm);

		${inheritedTextReset}
		display: flex;
		width: var(--_width);
		min-width: var(--_min-width);
		max-width: var(--_max-width);
		min-height: var(--_min-height);
		flex-direction: column;
		justify-content: center;
	}

	:host([hidden]) {
		display: none;
	}


	/* # Width */

	:host([width="full"]),
	:host(:not([width])),
	:host([width=""]) {
		flex-grow: 1;
		flex-shrink: 1;
		flex-basis: 0;
	}

	:host([width="fit-content"]) {
		width: fit-content;
		flex-grow: 0;
		flex-shrink: 0;
		flex-basis: auto;
	}

	:host([width]:not([width="full"]):not([width="fit-content"]):not([width=""])) {
		flex-shrink: 0;
	}

	:host([max-width]) {
		flex-basis: var(--_max-width);
	}


	/* # Vertical alignment */

	/* "center" (default) stretches to the full row height then centers content;
	   use vertical-alignment="top" for strict top without a minimum height */

	:host([vertical-alignment="center"]),
	:host(:not([vertical-alignment])) {
		align-self: stretch;
	}

	:host([vertical-alignment="top"]) {
		align-self: flex-start;
	}

	:host([vertical-alignment="bottom"]) {
		align-self: flex-end;
	}


	/* # Horizontal alignment */

	:host([horizontal-alignment="left"]),
	:host(:not([horizontal-alignment])) {
		align-items: flex-start;
	}

	:host([horizontal-alignment="center"]) {
		--_text-align: center;
		align-items: center;
	}

	:host([horizontal-alignment="right"]) {
		--_text-align: right;
		align-items: flex-end;
	}


	/* # Size */

	:host([size="1"]) {
		--_title-font: var(--primitives-font-display-1-sm);
	}

	:host([size="2"]) {
		--_title-font: var(--primitives-font-display-2-sm);
	}

	:host([size="3"]) {
		--_title-font: var(--primitives-font-display-3-sm);
	}

	:host([size="4"]) {
		--_title-font: var(--primitives-font-display-4-sm);
	}

	:host([size="6"]) {
		--_title-font: var(--primitives-font-display-6-sm);
	}


	/* # Color */

	:host([color="secondary"]) {
		--_title-color: var(--context-cell-content-secondary-color, var(--semantics-content-secondary-color));
	}

	:host([color="accent"]) {
		--_secondary-color: var(--context-cell-content-accent-color, var(--semantics-content-accent-color));
		--_title-color: var(--context-cell-content-accent-color, var(--semantics-content-accent-color));
	}

	:host([color="success"]) {
		--_secondary-color: var(--context-cell-content-success-color, var(--semantics-content-success-color));
		--_title-color: var(--context-cell-content-success-color, var(--semantics-content-success-color));
	}

	:host([color="warning"]) {
		--_secondary-color: var(--context-cell-content-warning-color, var(--semantics-content-warning-color));
		--_title-color: var(--context-cell-content-warning-color, var(--semantics-content-warning-color));
	}

	:host([color="critical"]) {
		--_secondary-color: var(--context-cell-content-critical-color, var(--semantics-content-critical-color));
		--_title-color: var(--context-cell-content-critical-color, var(--semantics-content-critical-color));
	}


	/* # Elements */

	.title-cell__overline {
		margin: 0;
		min-width: 0;
		align-self: stretch;
		text-align: var(--_text-align);
		color: var(--_secondary-color);
		font: var(--primitives-font-body-xs-regular-tight);
		overflow-wrap: anywhere;
	}

	.title-cell__title {
		margin: 0;
		min-width: 0;
		align-self: stretch;
		text-align: var(--_text-align);
		color: var(--_title-color);
		font: var(--_title-font);
		overflow-wrap: break-word;
		text-wrap: pretty;
	}

	@media (forced-colors: active) {
		.title-cell__title {
			forced-color-adjust: none;
		}
	}

	.title-cell__supporting-text {
		margin: 0;
		min-width: 0;
		align-self: stretch;
		text-align: var(--_text-align);
		color: var(--_secondary-color);
		font: var(--primitives-font-body-sm-regular-tight);
		overflow-wrap: anywhere;
	}
`;

  // node_modules/@nldd/design-system/dist/components/lists-and-tables/cells/title-cell/title-cell.template.js
  init_lit();

  // node_modules/lit-html/static.js
  init_lit_html();
  var a3 = /* @__PURE__ */ Symbol.for("");
  var o9 = (t6) => {
    if (t6?.r === a3) return t6?._$litStatic$;
  };
  var s5 = (t6) => ({ _$litStatic$: t6, r: a3 });
  var l3 = /* @__PURE__ */ new Map();
  var n6 = (t6) => (r6, ...e9) => {
    const a4 = e9.length;
    let s6, i8;
    const n7 = [], u6 = [];
    let c6, $3 = 0, f3 = false;
    for (; $3 < a4; ) {
      for (c6 = r6[$3]; $3 < a4 && void 0 !== (i8 = e9[$3], s6 = o9(i8)); ) c6 += s6 + r6[++$3], f3 = true;
      $3 !== a4 && u6.push(i8), n7.push(c6), $3++;
    }
    if ($3 === a4 && n7.push(r6[a4]), f3) {
      const t7 = n7.join("$$lit$$");
      void 0 === (r6 = l3.get(t7)) && (n7.raw = n7, l3.set(t7, r6 = n7)), e9 = u6;
    }
    return t6(r6, ...e9);
  };
  var u5 = n6(b2);
  var c5 = n6(w);
  var $2 = n6(T);

  // node_modules/@nldd/design-system/dist/components/lists-and-tables/cells/title-cell/title-cell.template.js
  init_render_marked();
  var HEADING_TAGS = {
    1: s5("h1"),
    2: s5("h2"),
    3: s5("h3"),
    4: s5("h4"),
    5: s5("h5"),
    6: s5("h6")
  };
  function renderTitle(component) {
    const fallback = component.text && !component._hasDefaultSlotted ? renderQueryMark(component.text, component.query, component.queryMarkMode) : A;
    const isHidden = !component.text && !component._hasDefaultSlotted;
    const tag = HEADING_TAGS[component.headingLevel];
    if (tag) {
      return u5`<${tag}
			class="title-cell__title"
			?hidden=${isHidden}
		>${fallback}<slot @slotchange=${component._onSlotChange}></slot></${tag}>`;
    }
    return b2`<p
		class="title-cell__title"
		?hidden=${isHidden}
	>${fallback}<slot @slotchange=${component._onSlotChange}></slot></p>`;
  }
  var template16 = function() {
    return b2`
		<p
			class="title-cell__overline"
			?hidden=${!this.overline && !this._hasOverlineSlotted}
		>
			${this.overline && !this._hasOverlineSlotted ? renderQueryMark(this.overline, this.query, this.queryMarkMode) : A}
			<slot name="overline" @slotchange=${this._onSlotChange}></slot>
		</p>
		${renderTitle(this)}
		<p
			class="title-cell__supporting-text"
			?hidden=${!this.supportingText && !this._hasSupportingTextSlotted}
		>
			${this.supportingText && !this._hasSupportingTextSlotted ? renderQueryMark(this.supportingText, this.query, this.queryMarkMode) : A}
			<slot name="supporting-text" @slotchange=${this._onSlotChange}></slot>
		</p>
	`;
  };

  // node_modules/@nldd/design-system/dist/components/lists-and-tables/cells/title-cell/title-cell.js
  init_visibility_mixin();
  var __decorate73 = function(decorators, target, key, desc) {
    var c6 = arguments.length, r6 = c6 < 3 ? target : desc === null ? desc = Object.getOwnPropertyDescriptor(target, key) : desc, d3;
    if (typeof Reflect === "object" && typeof Reflect.decorate === "function") r6 = Reflect.decorate(decorators, target, key, desc);
    else for (var i8 = decorators.length - 1; i8 >= 0; i8--) if (d3 = decorators[i8]) r6 = (c6 < 3 ? d3(r6) : c6 > 3 ? d3(target, key, r6) : d3(target, key)) || r6;
    return c6 > 3 && r6 && Object.defineProperty(target, key, r6), r6;
  };
  var NLDDTitleCell = class NLDDTitleCell2 extends VisibilityMixin(i4, "cells-container") {
    constructor() {
      super(...arguments);
      this.size = 5;
      this.color = "default";
      this.width = "full";
      this.horizontalAlignment = "left";
      this.verticalAlignment = "center";
      this.text = "";
      this.overline = "";
      this.supportingText = "";
      this.headingLevel = void 0;
      this.query = "";
      this.queryMarkMode = "predictive";
      this._hasOverlineSlotted = false;
      this._hasDefaultSlotted = false;
      this._hasSupportingTextSlotted = false;
      this._onSlotChange = (e9) => {
        const slot = e9.target;
        const hasContent = slot.assignedNodes().some((node) => {
          if (node.nodeType === Node.TEXT_NODE) {
            return (node.textContent ?? "").trim().length > 0;
          }
          return node.nodeType === Node.ELEMENT_NODE;
        });
        switch (slot.name) {
          case "overline":
            this._hasOverlineSlotted = hasContent;
            break;
          case "supporting-text":
            this._hasSupportingTextSlotted = hasContent;
            break;
          default:
            this._hasDefaultSlotted = hasContent;
            break;
        }
      };
    }
    updated(changed) {
      super.updated(changed);
      if (changed.has("width") || changed.has("minWidth") || changed.has("maxWidth") || changed.has("minHeight")) {
        this._applyDimensionStyles();
      }
    }
    _applyDimensionStyles() {
      const w2 = this.width;
      const widthIsKeyword = w2 === "full" || w2 === "fit-content";
      const widthIsValidLength = !!w2 && !widthIsKeyword && CSS.supports("width", w2);
      if (widthIsValidLength) {
        this.style.setProperty("--_width", w2);
      } else {
        this.style.removeProperty("--_width");
      }
      if (w2 && !widthIsKeyword && !widthIsValidLength) {
        this.width = "";
      }
      if (this.minWidth) {
        this.style.setProperty("--_min-width", this.minWidth);
      } else {
        this.style.removeProperty("--_min-width");
      }
      if (this.maxWidth) {
        this.style.setProperty("--_max-width", this.maxWidth);
      } else {
        this.style.removeProperty("--_max-width");
      }
      if (this.minHeight) {
        this.style.setProperty("--_min-height", this.minHeight);
      } else {
        this.style.removeProperty("--_min-height");
      }
    }
    render() {
      return template16.call(this);
    }
  };
  NLDDTitleCell.styles = [titleCellStyles];
  __decorate73([
    n4({ type: Number, reflect: true })
  ], NLDDTitleCell.prototype, "size", void 0);
  __decorate73([
    n4({ type: String, reflect: true })
  ], NLDDTitleCell.prototype, "color", void 0);
  __decorate73([
    n4({ type: String, reflect: true })
  ], NLDDTitleCell.prototype, "width", void 0);
  __decorate73([
    n4({ type: String, reflect: true, attribute: "min-width" })
  ], NLDDTitleCell.prototype, "minWidth", void 0);
  __decorate73([
    n4({ type: String, reflect: true, attribute: "max-width" })
  ], NLDDTitleCell.prototype, "maxWidth", void 0);
  __decorate73([
    n4({ type: String, reflect: true, attribute: "min-height" })
  ], NLDDTitleCell.prototype, "minHeight", void 0);
  __decorate73([
    n4({ type: String, reflect: true, attribute: "horizontal-alignment" })
  ], NLDDTitleCell.prototype, "horizontalAlignment", void 0);
  __decorate73([
    n4({ type: String, reflect: true, attribute: "vertical-alignment" })
  ], NLDDTitleCell.prototype, "verticalAlignment", void 0);
  __decorate73([
    n4({ type: String })
  ], NLDDTitleCell.prototype, "text", void 0);
  __decorate73([
    n4({ type: String })
  ], NLDDTitleCell.prototype, "overline", void 0);
  __decorate73([
    n4({ type: String, attribute: "supporting-text" })
  ], NLDDTitleCell.prototype, "supportingText", void 0);
  __decorate73([
    n4({ type: Number, attribute: "heading-level" })
  ], NLDDTitleCell.prototype, "headingLevel", void 0);
  __decorate73([
    n4({ type: String })
  ], NLDDTitleCell.prototype, "query", void 0);
  __decorate73([
    n4({ type: String, attribute: "query-mark-mode" })
  ], NLDDTitleCell.prototype, "queryMarkMode", void 0);
  __decorate73([
    r5()
  ], NLDDTitleCell.prototype, "_hasOverlineSlotted", void 0);
  __decorate73([
    r5()
  ], NLDDTitleCell.prototype, "_hasDefaultSlotted", void 0);
  __decorate73([
    r5()
  ], NLDDTitleCell.prototype, "_hasSupportingTextSlotted", void 0);
  NLDDTitleCell = __decorate73([
    t3("nldd-title-cell")
  ], NLDDTitleCell);

  // node_modules/@nldd/design-system/dist/components/index.js
  init_text_cell();

  // node_modules/@nldd/design-system/dist/components/lists-and-tables/cells/description-cell/description-cell.js
  init_lit();
  init_decorators();

  // node_modules/@nldd/design-system/dist/components/lists-and-tables/cells/description-cell/description-cell.styles.js
  init_lit();
  init_slotted_reset();
  var descriptionCellStyles = i`


	/* # Host */

	:host {
		--_width: auto;
		--_min-width: 0;
		--_max-width: none;
		--_min-height: 0;

		${inheritedTextReset}
		display: flex;
		width: var(--_width);
		min-width: var(--_min-width);
		max-width: var(--_max-width);
		min-height: var(--_min-height);
		flex-direction: column;
		justify-content: center;
	}

	:host([hidden]) {
		display: none;
	}


	/* # Width */

	:host([width="full"]),
	:host(:not([width])),
	:host([width=""]) {
		flex-grow: 1;
		flex-shrink: 1;
		flex-basis: 0;
	}

	:host([width="fit-content"]) {
		width: fit-content;
		flex-grow: 0;
		flex-shrink: 0;
		flex-basis: auto;
	}

	:host([width]:not([width="full"]):not([width="fit-content"]):not([width=""])) {
		flex-shrink: 0;
	}

	:host([max-width]) {
		flex-basis: var(--_max-width);
	}


	/* # Vertical alignment */

	/* "center" (default) stretches to the full row height then centers content;
	   use vertical-alignment="top" for strict top without a minimum height */

	:host([vertical-alignment="center"]),
	:host(:not([vertical-alignment])) {
		align-self: stretch;
	}

	:host([vertical-alignment="top"]) {
		align-self: flex-start;
	}

	:host([vertical-alignment="bottom"]) {
		align-self: flex-end;
	}


	/* # Elements */

	::slotted([slot="title"]) {
		${slottedReset}
		${inheritedTextReset}
		margin: 0 !important;
		min-width: 0 !important;
		align-self: stretch !important;
		color: var(--context-cell-content-color, var(--semantics-content-secondary-color)) !important;
		font: var(--primitives-font-body-sm-regular-flat) !important;
	}

	@media (forced-colors: active) {
		::slotted([slot="title"]) {
			forced-color-adjust: none !important;
		}
	}

	::slotted([slot="description"]) {
		${slottedReset}
		${inheritedTextReset}
		margin: 0 !important;
		min-width: 0 !important;
		align-self: stretch !important;
		color: var(--context-cell-content-color, var(--semantics-content-color)) !important;
		font: var(--primitives-font-body-md-regular-tight) !important;
	}

	@media (forced-colors: active) {
		::slotted([slot="description"]) {
			forced-color-adjust: none !important;
		}
	}
`;

  // node_modules/@nldd/design-system/dist/components/lists-and-tables/cells/description-cell/description-cell.template.js
  init_lit();
  var template17 = () => b2`
	<slot name="title"></slot>
	<slot name="description"></slot>
`;

  // node_modules/@nldd/design-system/dist/components/lists-and-tables/cells/description-cell/description-cell.js
  init_visibility_mixin();
  var __decorate74 = function(decorators, target, key, desc) {
    var c6 = arguments.length, r6 = c6 < 3 ? target : desc === null ? desc = Object.getOwnPropertyDescriptor(target, key) : desc, d3;
    if (typeof Reflect === "object" && typeof Reflect.decorate === "function") r6 = Reflect.decorate(decorators, target, key, desc);
    else for (var i8 = decorators.length - 1; i8 >= 0; i8--) if (d3 = decorators[i8]) r6 = (c6 < 3 ? d3(r6) : c6 > 3 ? d3(target, key, r6) : d3(target, key)) || r6;
    return c6 > 3 && r6 && Object.defineProperty(target, key, r6), r6;
  };
  var NLDDDescriptionCell = class NLDDDescriptionCell2 extends VisibilityMixin(i4, "cells-container") {
    constructor() {
      super(...arguments);
      this.width = "full";
      this.verticalAlignment = "center";
    }
    updated(changed) {
      super.updated(changed);
      if (changed.has("width") || changed.has("minWidth") || changed.has("maxWidth") || changed.has("minHeight")) {
        this._applyDimensionStyles();
      }
    }
    _applyDimensionStyles() {
      const w2 = this.width;
      const widthIsKeyword = w2 === "full" || w2 === "fit-content";
      const widthIsValidLength = !!w2 && !widthIsKeyword && CSS.supports("width", w2);
      if (widthIsValidLength) {
        this.style.setProperty("--_width", w2);
      } else {
        this.style.removeProperty("--_width");
      }
      if (w2 && !widthIsKeyword && !widthIsValidLength) {
        this.width = "";
      }
      if (this.minWidth) {
        this.style.setProperty("--_min-width", this.minWidth);
      } else {
        this.style.removeProperty("--_min-width");
      }
      if (this.maxWidth) {
        this.style.setProperty("--_max-width", this.maxWidth);
      } else {
        this.style.removeProperty("--_max-width");
      }
      if (this.minHeight) {
        this.style.setProperty("--_min-height", this.minHeight);
      } else {
        this.style.removeProperty("--_min-height");
      }
    }
    render() {
      return template17();
    }
  };
  NLDDDescriptionCell.styles = [descriptionCellStyles];
  __decorate74([
    n4({ type: String, reflect: true })
  ], NLDDDescriptionCell.prototype, "width", void 0);
  __decorate74([
    n4({ type: String, reflect: true, attribute: "min-width" })
  ], NLDDDescriptionCell.prototype, "minWidth", void 0);
  __decorate74([
    n4({ type: String, reflect: true, attribute: "max-width" })
  ], NLDDDescriptionCell.prototype, "maxWidth", void 0);
  __decorate74([
    n4({ type: String, reflect: true, attribute: "min-height" })
  ], NLDDDescriptionCell.prototype, "minHeight", void 0);
  __decorate74([
    n4({ reflect: true, attribute: "vertical-alignment" })
  ], NLDDDescriptionCell.prototype, "verticalAlignment", void 0);
  NLDDDescriptionCell = __decorate74([
    t3("nldd-description-cell")
  ], NLDDDescriptionCell);

  // node_modules/@nldd/design-system/dist/components/lists-and-tables/cells/drag-handle-cell/drag-handle-cell.js
  init_lit();
  init_decorators();

  // node_modules/@nldd/design-system/dist/components/lists-and-tables/cells/drag-handle-cell/drag-handle-cell.styles.js
  init_lit();
  var dragHandleCellStyles = i`


	/* # Host */

	:host {
		--_corner-radius: var(--semantics-controls-md-corner-radius);
		--_width: var(--semantics-controls-sm-min-size);
		--_height: var(--semantics-controls-md-min-size);

		display: inline-flex;
		cursor: grab;
		width: fit-content;
		align-items: center;
		justify-content: center;
	}

	:host([hidden]) {
		display: none;
	}

	:host(:active) {
		cursor: grabbing;
	}

	:host([size="sm"]) {
		--_corner-radius: var(--semantics-controls-sm-corner-radius);
		--_width: var(--semantics-controls-xs-min-size);
		--_height: var(--semantics-controls-sm-min-size);
	}


	/* # Block */

	.drag-handle-cell__control {
		display: flex;
		margin: 0;
		border: none;
		border-radius: var(--_corner-radius);
		background-color: var(--semantics-grab-handles-background-color);
		cursor: inherit;
		width: var(--_width);
		height: var(--_height);
		padding: 0;
		align-items: center;
		justify-content: center;
		-webkit-appearance: none;
		appearance: none;
	}

	.drag-handle-cell__control:focus-visible {
		outline: var(--semantics-focus-ring-outline);
		outline-offset: var(--semantics-focus-ring-outline-offset);
		box-shadow: var(--semantics-focus-ring-box-shadow);
	}


	/* # Elements */

	.drag-handle-cell__control-grip {
		display: block;
		color: var(--semantics-grab-handles-grip-color);
	}
`;

  // node_modules/@nldd/design-system/dist/components/lists-and-tables/cells/drag-handle-cell/drag-handle-cell.template.js
  init_lit();
  var gripMd = w`
	<svg aria-hidden="true" class="drag-handle-cell__control-grip"
		width="10"
		height="22"
		viewBox="0 0 10 22"
		xmlns="http://www.w3.org/2000/svg"
	>
		<circle cx="2" cy="2"  r="2" fill="currentColor"/>
		<circle cx="8" cy="2"  r="2" fill="currentColor"/>
		<circle cx="2" cy="8"  r="2" fill="currentColor"/>
		<circle cx="8" cy="8"  r="2" fill="currentColor"/>
		<circle cx="2" cy="14" r="2" fill="currentColor"/>
		<circle cx="8" cy="14" r="2" fill="currentColor"/>
		<circle cx="2" cy="20" r="2" fill="currentColor"/>
		<circle cx="8" cy="20" r="2" fill="currentColor"/>
	</svg>
`;
  var gripSm = w`
	<svg aria-hidden="true" class="drag-handle-cell__control-grip"
		width="10"
		height="16"
		viewBox="0 0 10 16"
		xmlns="http://www.w3.org/2000/svg"
	>
		<circle cx="2" cy="2"  r="2" fill="currentColor"/>
		<circle cx="8" cy="2"  r="2" fill="currentColor"/>
		<circle cx="2" cy="8"  r="2" fill="currentColor"/>
		<circle cx="8" cy="8"  r="2" fill="currentColor"/>
		<circle cx="2" cy="14" r="2" fill="currentColor"/>
		<circle cx="8" cy="14" r="2" fill="currentColor"/>
	</svg>
`;
  function template18(size3, label) {
    return b2`
		<button class="drag-handle-cell__control"
			type="button"
			aria-label=${label}
		>
			${size3 === "sm" ? gripSm : gripMd}
		</button>
	`;
  }

  // node_modules/@nldd/design-system/dist/components/lists-and-tables/cells/drag-handle-cell/drag-handle-cell.i18n.js
  var nlddDragHandleCellTranslations = {
    "components.drag-handle-cell.label-text": "Sleepgreep, druk spatie of enter om te verplaatsen"
  };

  // node_modules/@nldd/design-system/dist/components/lists-and-tables/cells/drag-handle-cell/drag-handle-cell.js
  var __decorate75 = function(decorators, target, key, desc) {
    var c6 = arguments.length, r6 = c6 < 3 ? target : desc === null ? desc = Object.getOwnPropertyDescriptor(target, key) : desc, d3;
    if (typeof Reflect === "object" && typeof Reflect.decorate === "function") r6 = Reflect.decorate(decorators, target, key, desc);
    else for (var i8 = decorators.length - 1; i8 >= 0; i8--) if (d3 = decorators[i8]) r6 = (c6 < 3 ? d3(r6) : c6 > 3 ? d3(target, key, r6) : d3(target, key)) || r6;
    return c6 > 3 && r6 && Object.defineProperty(target, key, r6), r6;
  };
  var NLDDDragHandleCell = class NLDDDragHandleCell2 extends i4 {
    constructor() {
      super(...arguments);
      this.size = "md";
      this.translations = {};
    }
    // — i18n ————————————————————————————————————————————————————————————————
    _t(key) {
      return this.translations[key] ?? nlddDragHandleCellTranslations[key];
    }
    render() {
      const label = this._t("components.drag-handle-cell.label-text");
      return template18(this.size, label);
    }
  };
  NLDDDragHandleCell.styles = dragHandleCellStyles;
  __decorate75([
    n4({ type: String, reflect: true })
  ], NLDDDragHandleCell.prototype, "size", void 0);
  __decorate75([
    n4({ type: Object })
  ], NLDDDragHandleCell.prototype, "translations", void 0);
  NLDDDragHandleCell = __decorate75([
    t3("nldd-drag-handle-cell")
  ], NLDDDragHandleCell);

  // node_modules/@nldd/design-system/dist/components/lists-and-tables/cells/timeline-track-cell/timeline-track-cell.js
  init_lit();
  init_decorators();

  // node_modules/@nldd/design-system/dist/components/lists-and-tables/cells/timeline-track-cell/timeline-track-cell.styles.js
  init_lit();
  var timelineTrackCellStyles = i`


	/* # Host */

	:host {
		--_size: var(--primitives-space-16);
		--_line-width: var(--primitives-space-2);
		--_z-index-dot: 1;
		--_color: var(--components-timeline-track-cell-color);
		--_future-background-color: var(--components-timeline-track-cell-future-background-color);

		display: flex;
		width: var(--_size);
		flex-direction: column;
		align-self: stretch;
		align-items: center;
	}

	:host([hidden]) {
		display: none;
	}


	/* # Block */

	.timeline-track-cell {
		position: relative;
		width: var(--_size);
		height: 100%;
		min-height: var(--primitives-space-48);
	}


	/* # Elements */

	.timeline-track-cell__line-top,
	.timeline-track-cell__line-bottom,
	.timeline-track-cell__line-full {
		position: absolute;
		left: 50%;
		margin-left: calc(var(--_line-width) / -2);
		background-color: var(--_color);
		width: var(--_line-width);
	}

	.timeline-track-cell__line-top {
		bottom: 50%;
		height: calc(50% + var(--context-list-item-padding-block, 0px));
	}

	.timeline-track-cell__line-bottom {
		top: 50%;
		height: calc(50% + var(--context-list-item-padding-block, 0px));
	}

	.timeline-track-cell__line-full {
		top: calc(-1 * var(--context-list-item-padding-block, 0px));
		bottom: calc(-1 * var(--context-list-item-padding-block, 0px));
	}

	.timeline-track-cell__dot {
		box-sizing: border-box;
		position: absolute;
		top: 50%;
		left: 0;
		z-index: var(--_z-index-dot);
		margin-top: calc(var(--_size) / -2);
		border: var(--_line-width) solid var(--_color);
		border-radius: var(--primitives-corner-radius-full);
		width: var(--_size);
		height: var(--_size);
	}

	:host([step="past"]) .timeline-track-cell__dot,
	:host(:not([step])) .timeline-track-cell__dot {
		background-color: var(--_color);
	}

	:host([step="future"]) .timeline-track-cell__dot {
		background-color: var(--_future-background-color);
	}

	@media (forced-colors: active) {
		.timeline-track-cell__dot,
		.timeline-track-cell__line-top,
		.timeline-track-cell__line-bottom,
		.timeline-track-cell__line-full {
			forced-color-adjust: none;
		}
	}
`;

  // node_modules/@nldd/design-system/dist/components/lists-and-tables/cells/timeline-track-cell/timeline-track-cell.template.js
  init_lit();
  function timelineTrackCellTemplate(component) {
    if (component.step === "none") {
      return b2`
			<div class="timeline-track-cell">
				<div class="timeline-track-cell__line-full"></div>
			</div>
		`;
    }
    const showTopLine = component.child === "between" || component.child === "last";
    const showBottomLine = component.child === "between" || component.child === "first";
    return b2`
		<div class="timeline-track-cell">
			${showTopLine ? b2`<div class="timeline-track-cell__line-top"></div>` : A}
			<div class="timeline-track-cell__dot"></div>
			${showBottomLine ? b2`<div class="timeline-track-cell__line-bottom"></div>` : A}
		</div>
	`;
  }

  // node_modules/@nldd/design-system/dist/components/lists-and-tables/cells/timeline-track-cell/timeline-track-cell.js
  var __decorate76 = function(decorators, target, key, desc) {
    var c6 = arguments.length, r6 = c6 < 3 ? target : desc === null ? desc = Object.getOwnPropertyDescriptor(target, key) : desc, d3;
    if (typeof Reflect === "object" && typeof Reflect.decorate === "function") r6 = Reflect.decorate(decorators, target, key, desc);
    else for (var i8 = decorators.length - 1; i8 >= 0; i8--) if (d3 = decorators[i8]) r6 = (c6 < 3 ? d3(r6) : c6 > 3 ? d3(target, key, r6) : d3(target, key)) || r6;
    return c6 > 3 && r6 && Object.defineProperty(target, key, r6), r6;
  };
  var NLDDTimelineTrackCell = class NLDDTimelineTrackCell2 extends i4 {
    constructor() {
      super(...arguments);
      this.step = "past";
      this.child = "between";
    }
    render() {
      return timelineTrackCellTemplate(this);
    }
  };
  NLDDTimelineTrackCell.styles = timelineTrackCellStyles;
  __decorate76([
    n4({ type: String, reflect: true })
  ], NLDDTimelineTrackCell.prototype, "step", void 0);
  __decorate76([
    n4({ type: String, reflect: true })
  ], NLDDTimelineTrackCell.prototype, "child", void 0);
  NLDDTimelineTrackCell = __decorate76([
    t3("nldd-timeline-track-cell")
  ], NLDDTimelineTrackCell);

  // node_modules/@nldd/design-system/dist/components/navigation/breadcrumbs/breadcrumbs.js
  init_lit();
  init_decorators();

  // node_modules/@nldd/design-system/dist/components/navigation/breadcrumbs/breadcrumbs.styles.js
  init_lit();
  init_slotted_reset();
  var breadcrumbsStyles = i`
	:host {
		${inheritedTextReset}
		display: block;
	}

	:host([hidden]) {
		display: none;
	}

	.breadcrumbs {
		display: flex;
	}

	.breadcrumbs__items {
		display: flex;
		flex-wrap: wrap;
		row-gap: var(--primitives-space-4);
	}
`;
  var breadcrumbsItemStyles = i`
	:host {
		/* Small downward offset so the chevron-right-small icon sits closer
		   to the text's optical centerline (the icon's bbox renders slightly
		   above the visual baseline). */
		--_separator-vertical-offset: 0.05em;

		${inheritedTextReset}
		display: inline-flex;
		align-items: center;
	}

	:host([hidden]) {
		display: none;
	}

	.breadcrumbs__item {
		display: inline-flex;
		align-items: center;
		color: var(--semantics-content-color);
		font: var(--primitives-font-body-md-regular-tight);
	}

	.breadcrumbs__item-link {
		color: var(--semantics-links-color);
		text-decoration: underline;
	}

	.breadcrumbs__item-link:hover {
		color: var(--semantics-links-is-hovered-color);
	}

	.breadcrumbs__item-link:active {
		color: var(--semantics-links-is-active-color);
	}

	.breadcrumbs__item-link:focus-visible {
		outline: var(--semantics-focus-ring-outline);
		outline-offset: var(--semantics-focus-ring-outline-offset);
		border-radius: var(--primitives-corner-radius-xs);
		box-shadow: var(--semantics-focus-ring-box-shadow);
	}

	.breadcrumbs__separator {
		display: inline-flex;
		position: relative;
		top: var(--_separator-vertical-offset);
		margin-inline: var(--primitives-space-2);
		color: var(--semantics-content-secondary-color);
		width: var(--primitives-space-16);
		height: var(--primitives-space-16);
		flex-shrink: 0;
	}

	:host(:last-of-type) .breadcrumbs__separator {
		display: none;
	}
`;

  // node_modules/@nldd/design-system/dist/components/navigation/breadcrumbs/breadcrumbs.template.js
  init_lit();
  init_icon();
  function breadcrumbsTemplate(component) {
    const label = component._t("components.breadcrumbs.accessible-label");
    return b2`
		<nav class="breadcrumbs"
			aria-label=${label || A}
		>
			<div class="breadcrumbs__items"
				role="list"
			>
				<slot></slot>
			</div>
		</nav>
	`;
  }
  function breadcrumbsItemTemplate(component) {
    const label = component.text || b2`<slot></slot>`;
    const separator = b2`<span class="breadcrumbs__separator"
		aria-hidden="true"
	><nldd-icon name="chevron-right-small"></nldd-icon></span>`;
    if (!component.current && component.href) {
      return b2`
			<span class="breadcrumbs__item">
				<a class="breadcrumbs__item-link"
					href=${component.href}
				>${label}</a>
			</span>${separator}
		`;
    }
    return b2`<span class="breadcrumbs__item"
		aria-current=${component.current ? "page" : A}
	>${label}</span>${separator}`;
  }

  // node_modules/@nldd/design-system/dist/components/navigation/breadcrumbs/breadcrumbs.i18n.js
  var nlddBreadcrumbsTranslations = {
    "components.breadcrumbs.accessible-label": "Kruimelpad"
  };

  // node_modules/@nldd/design-system/dist/components/navigation/breadcrumbs/breadcrumbs.js
  var import_meta24 = {};
  var __decorate77 = function(decorators, target, key, desc) {
    var c6 = arguments.length, r6 = c6 < 3 ? target : desc === null ? desc = Object.getOwnPropertyDescriptor(target, key) : desc, d3;
    if (typeof Reflect === "object" && typeof Reflect.decorate === "function") r6 = Reflect.decorate(decorators, target, key, desc);
    else for (var i8 = decorators.length - 1; i8 >= 0; i8--) if (d3 = decorators[i8]) r6 = (c6 < 3 ? d3(r6) : c6 > 3 ? d3(target, key, r6) : d3(target, key)) || r6;
    return c6 > 3 && r6 && Object.defineProperty(target, key, r6), r6;
  };
  var NLDDBreadcrumbsItem = class extends i4 {
    constructor() {
      super(...arguments);
      this.current = false;
      this.text = "";
    }
    connectedCallback() {
      super.connectedCallback();
      if (!this.hasAttribute("role")) {
        this.setAttribute("role", "listitem");
      }
    }
    updated(changed) {
      if (changed.has("current")) {
        if (this.current) {
          this.setAttribute("aria-current", "page");
        } else {
          this.removeAttribute("aria-current");
        }
      }
    }
    render() {
      return breadcrumbsItemTemplate(this);
    }
  };
  NLDDBreadcrumbsItem.styles = breadcrumbsItemStyles;
  __decorate77([
    n4({ type: String, reflect: true })
  ], NLDDBreadcrumbsItem.prototype, "href", void 0);
  __decorate77([
    n4({ type: Boolean, reflect: true })
  ], NLDDBreadcrumbsItem.prototype, "current", void 0);
  __decorate77([
    n4({ type: String, reflect: true })
  ], NLDDBreadcrumbsItem.prototype, "text", void 0);
  if (!customElements.get("nldd-breadcrumbs-item")) {
    customElements.define("nldd-breadcrumbs-item", NLDDBreadcrumbsItem);
  }
  var NLDDBreadcrumbs = class NLDDBreadcrumbs2 extends i4 {
    constructor() {
      super(...arguments);
      this.accessibleLabel = "";
      this.translations = {};
      this._mergedTranslations = { ...nlddBreadcrumbsTranslations };
    }
    willUpdate(changed) {
      if (changed.has("translations") || changed.has("accessibleLabel")) {
        this._mergedTranslations = {
          ...nlddBreadcrumbsTranslations,
          ...this.translations
        };
        if (this.accessibleLabel) {
          this._mergedTranslations["components.breadcrumbs.accessible-label"] = this.accessibleLabel;
        }
      }
    }
    _t(key) {
      const value = this._mergedTranslations[key];
      if (value === void 0 && import_meta24.env?.DEV) {
        console.warn(`<nldd-breadcrumbs>: missing translation for "${key}"`);
      }
      return value ?? "";
    }
    render() {
      return breadcrumbsTemplate(this);
    }
  };
  NLDDBreadcrumbs.styles = breadcrumbsStyles;
  __decorate77([
    n4({ type: String, attribute: "accessible-label" })
  ], NLDDBreadcrumbs.prototype, "accessibleLabel", void 0);
  __decorate77([
    n4({ type: Object })
  ], NLDDBreadcrumbs.prototype, "translations", void 0);
  NLDDBreadcrumbs = __decorate77([
    t3("nldd-breadcrumbs")
  ], NLDDBreadcrumbs);

  // node_modules/@nldd/design-system/dist/components/navigation/menu-bar/menu-bar.js
  init_lit();
  init_decorators();

  // node_modules/@nldd/design-system/dist/components/navigation/menu-bar/menu-bar.styles.js
  init_lit();
  init_slotted_reset();
  var menuBarStyles = i`


	/* # Host */

	:host {
		${inheritedTextReset}
		display: flex;
		min-width: 0;
		flex-grow: 1;
		flex-shrink: 1;
		user-select: none;
	}

	:host([hidden]),
	:host([empty]) {
		display: none;
	}


	/* # Block */

	.menu-bar {
		display: flex;
		min-width: 0;
		flex-direction: row;
		flex-grow: 1;
		flex-shrink: 1;
		align-items: center;
	}


	/* # Elements */

	.menu-bar__overflow-button {
		display: none;
	}
`;

  // node_modules/@nldd/design-system/dist/components/navigation/menu-bar/menu-bar.template.js
  init_lit();
  function template19(component) {
    return b2`
		<nav class="menu-bar"
			aria-label=${component.accessibleLabel || A}
		>
			<slot></slot>
			<div class="menu-bar__overflow-button">
				<nldd-menu-bar-item
					text="${component._overflowText}"
					icon="ellipsis"
					icon-only
					haspopup="menu"
					?expanded=${component._menuOpen}
					@pointerdown=${component._handleOverflowButtonPointerdown}
					@click=${component._toggleOverflowMenu}
				>
					<!-- The overflow popover is opened/closed explicitly by
						 _toggleOverflowMenu (same mechanism as an expandable
						 nldd-menu-bar-item's own submenu): anchored to this
						 always-visible trigger + a reopen guard. aria-controls
						 omitted: ARIA IDREF attributes cannot cross shadow DOM
						 boundaries and the menu is reparented to document.body;
						 haspopup + expanded forward to the inner button and give
						 sufficient AT context for WCAG 2.1 AA. -->
				</nldd-menu-bar-item>
			</div>
		</nav>
	`;
  }

  // node_modules/@nldd/design-system/dist/utilities/with-translations.js
  init_decorators();
  var __decorate78 = function(decorators, target, key, desc) {
    var c6 = arguments.length, r6 = c6 < 3 ? target : desc === null ? desc = Object.getOwnPropertyDescriptor(target, key) : desc, d3;
    if (typeof Reflect === "object" && typeof Reflect.decorate === "function") r6 = Reflect.decorate(decorators, target, key, desc);
    else for (var i8 = decorators.length - 1; i8 >= 0; i8--) if (d3 = decorators[i8]) r6 = (c6 < 3 ? d3(r6) : c6 > 3 ? d3(target, key, r6) : d3(target, key)) || r6;
    return c6 > 3 && r6 && Object.defineProperty(target, key, r6), r6;
  };
  function withTranslations(Base, defaults) {
    class TranslationsMixin extends Base {
      constructor() {
        super(...arguments);
        this.translations = {};
        this._mergedTranslations = { ...defaults };
      }
      /** @internal */
      _t(key, vars) {
        let str = this._mergedTranslations[key] ?? String(key);
        if (vars) {
          for (const [k2, v3] of Object.entries(vars)) {
            str = str.split(`{${k2}}`).join(String(v3));
          }
        }
        return str;
      }
      willUpdate(changed) {
        super.willUpdate(changed);
        if (changed.has("translations")) {
          this._mergedTranslations = { ...defaults, ...this.translations };
        }
      }
    }
    __decorate78([
      n4({ type: Object })
    ], TranslationsMixin.prototype, "translations", void 0);
    return TranslationsMixin;
  }

  // node_modules/@nldd/design-system/dist/components/navigation/menu-bar/menu-bar.i18n.js
  var nlddMenuBarTranslations = {
    "components.menu-bar.overflow-action": "Meer opties"
  };

  // node_modules/@nldd/design-system/dist/components/navigation/menu-bar-item/menu-bar-item.js
  init_lit();
  init_decorators();

  // node_modules/@nldd/design-system/dist/components/navigation/menu-bar-item/menu-bar-item.styles.js
  init_lit();
  init_slotted_reset();
  var menuBarItemStyles = i`


	/* # Host */

	:host {
		--_indicator-z-index: 0;
		--_content-z-index: 1;
		--_focus-z-index: 1;

		${inheritedTextReset}
		display: inline-block;
		position: relative;
		flex-grow: 0;
		flex-shrink: 0;
		flex-basis: auto;
		isolation: isolate;
		user-select: none;
		-webkit-tap-highlight-color: transparent;
	}

	:host([hidden]) {
		display: none;
	}


	/* # Block */

	.menu-bar-item {
		box-sizing: border-box;
		display: flex;
		position: relative;
		margin: 0;
		border: none;
		background: none;
		min-width: var(--semantics-controls-md-min-size);
		height: var(--semantics-controls-md-min-size);
		padding: 0 var(--components-menu-bar-item-inline-padding);
		gap: var(--primitives-space-4);
		align-items: center;
		justify-content: center;
		text-align: center;
		color: var(--components-menu-bar-item-content-color);
		font: var(--components-menu-bar-item-font);
		text-decoration: none;
		white-space: nowrap;
		appearance: none;
	}

	a.menu-bar-item {
		cursor: var(--semantics-controls-link-cursor);
	}

	/* ## Hover indicator (::before) */

	.menu-bar-item::before {
		content: '';
		position: absolute;
		top: var(--primitives-space-6);
		right: 0;
		bottom: var(--primitives-space-6);
		left: 0;
		z-index: var(--_indicator-z-index);
		border-radius: var(--semantics-controls-sm-corner-radius);
		pointer-events: none;
	}

	@media (hover: hover) {
		.menu-bar-item:hover::before {
			background-color: var(--components-menu-bar-item-is-hovered-indicator-background-color);
		}
	}

	:host([expanded]) .menu-bar-item::before {
		background-color: var(--components-menu-bar-item-is-expanded-indicator-background-color);
	}

	@media (hover: hover) {
		:host([expanded]) .menu-bar-item:hover::before {
			background-color: var(--components-menu-bar-item-is-hovered-indicator-background-color);
		}
	}

	/* ## Current indicator (::after) */

	:host([current]) .menu-bar-item::after {
		content: '';
		position: absolute;
		right: var(--primitives-space-8);
		bottom: 0;
		left: var(--primitives-space-8);
		z-index: var(--_indicator-z-index);
		background-color: var(--components-menu-bar-item-is-current-indicator-background-color);
		height: var(--components-menu-bar-item-is-current-indicator-height);
		pointer-events: none;
	}

	/* ## Text */

	.menu-bar-item__text {
		position: relative;
		z-index: var(--_content-z-index);
	}

	/* ## Icon */

	.menu-bar-item__icon {
		z-index: var(--_content-z-index);
		width: var(--primitives-space-20);
		height: var(--primitives-space-20);
		flex-shrink: 0;
	}

	/* ## Disclosure icon */

	.menu-bar-item__disclosure-icon {
		z-index: var(--_content-z-index);
		width: var(--primitives-space-16);
		height: var(--primitives-space-16);
	}


	/* # Focus */

	:host(:focus-within) {
		z-index: var(--_focus-z-index);
	}

	.menu-bar-item:focus-visible {
		outline: none;
	}

	.menu-bar-item:focus-visible::before {
		outline: var(--semantics-focus-ring-outline);
		outline-offset: var(--semantics-focus-ring-outline-offset);
		box-shadow: var(--semantics-focus-ring-box-shadow);
	}


	/* # Slotted */

	::slotted(nldd-menu-item),
	::slotted(nldd-menu-divider) {
		display: none;
	}


	/* # Disabled */

	:host([disabled]) .menu-bar-item {
		opacity: var(--primitives-opacity-disabled);
		pointer-events: none;
	}


	/* # Icon-only */

	:host([icon-only]) .menu-bar-item,
	:host([content-priority="icon"][compact][icon]:not([icon=""])) .menu-bar-item {
		padding: var(--primitives-space-8);
	}

	:host([icon-only]) .menu-bar-item__text,
	:host([content-priority="icon"][compact][icon]:not([icon=""])) .menu-bar-item__text {
		position: absolute;
		margin: -1px;
		border: 0;
		width: 1px;
		height: 1px;
		overflow: hidden;
		padding: 0;
		white-space: nowrap;
		clip-path: inset(50%);
	}

	:host([content-priority="text"][compact]) .menu-bar-item__icon {
		display: none;
	}
`;

  // node_modules/@nldd/design-system/dist/components/navigation/menu-bar-item/menu-bar-item.template.js
  init_lit();

  // node_modules/@nldd/design-system/dist/utilities/sanitize-url.js
  function sanitizeUrl(url) {
    if (!url)
      return null;
    const trimmed = url.replace(/^[\s\u00A0\u200B\u2028\u2029]+|[\s\u00A0\u200B\u2028\u2029]+$/g, "").replace(/[\0\t\n\r]/g, "");
    const lower = trimmed.toLowerCase();
    if (lower.startsWith("javascript:") || lower.startsWith("data:") || lower.startsWith("vbscript:") || lower.startsWith("blob:")) {
      return null;
    }
    return trimmed;
  }

  // node_modules/@nldd/design-system/dist/components/navigation/menu-bar-item/menu-bar-item.template.js
  function template20(component) {
    const safeHref = sanitizeUrl(component.href);
    const isLink = Boolean(safeHref);
    const isIconOnly = Boolean((component.iconOnly || component.contentPriority === "icon" && component.compact) && component.text);
    const ariaLabel = component.accessibleLabel || (isIconOnly ? component.text : A);
    const ariaCurrent = component.current ? component.currentType : A;
    const ariaHaspopup = component.expandable ? "menu" : component.haspopup || A;
    const ariaExpanded = component.expandable || component.haspopup ? String(component.expanded) : A;
    if (isLink) {
      return b2`
			<a class="menu-bar-item"
				href=${safeHref}
				aria-disabled=${component.disabled || A}
				tabindex=${component.disabled ? "-1" : A}
				aria-current=${ariaCurrent}
				aria-label=${ariaLabel}
				aria-haspopup=${ariaHaspopup}
				aria-expanded=${ariaExpanded}
			>
				${component.icon ? b2`
					<span class="menu-bar-item__icon">
						<nldd-icon name=${component.icon}></nldd-icon>
					</span>
				` : A}
				<span class="menu-bar-item__text">
					${component.text}
				</span>
				${component.expandable ? b2`
					<span class="menu-bar-item__disclosure-icon">
						<nldd-icon name="chevron-down-small"></nldd-icon>
					</span>
				` : A}
			</a>
			<slot @slotchange=${component._onSlotChange}></slot>
		`;
    }
    return b2`
		<button class="menu-bar-item"
			type="button"
			?disabled=${component.disabled}
			aria-current=${ariaCurrent}
			aria-label=${ariaLabel}
			aria-haspopup=${ariaHaspopup}
			aria-expanded=${ariaExpanded}
			.popoverTargetElement=${component.popoverTargetElement}
			.popoverTargetAction=${component.popoverTargetAction}
		>
			${component.icon ? b2`
				<span class="menu-bar-item__icon">
					<nldd-icon name=${component.icon}></nldd-icon>
				</span>
			` : A}
			<span class="menu-bar-item__text">
				${component.text}
			</span>
			${component.expandable ? b2`
				<span class="menu-bar-item__disclosure-icon">
					<nldd-icon name="chevron-down-small"></nldd-icon>
				</span>
			` : A}
		</button>
		<slot @slotchange=${component._onSlotChange}></slot>
	`;
  }

  // node_modules/@nldd/design-system/dist/components/navigation/menu-bar-item/menu-bar-item.js
  init_icon();
  var __decorate79 = function(decorators, target, key, desc) {
    var c6 = arguments.length, r6 = c6 < 3 ? target : desc === null ? desc = Object.getOwnPropertyDescriptor(target, key) : desc, d3;
    if (typeof Reflect === "object" && typeof Reflect.decorate === "function") r6 = Reflect.decorate(decorators, target, key, desc);
    else for (var i8 = decorators.length - 1; i8 >= 0; i8--) if (d3 = decorators[i8]) r6 = (c6 < 3 ? d3(r6) : c6 > 3 ? d3(target, key, r6) : d3(target, key)) || r6;
    return c6 > 3 && r6 && Object.defineProperty(target, key, r6), r6;
  };
  var NLDDMenuBarItem = class NLDDMenuBarItem2 extends i4 {
    constructor() {
      super(...arguments);
      this.text = "";
      this.current = false;
      this.currentType = "page";
      this.href = "";
      this.icon = "";
      this.expandable = false;
      this.iconOnly = false;
      this.contentPriority = "";
      this.compact = false;
      this.disabled = false;
      this.accessibleLabel = "";
      this.haspopup = "";
      this.expanded = false;
      this.popoverTargetElement = null;
      this.popoverTargetAction = "toggle";
      this._menu = null;
      this._menuOpen = false;
      this._pointerdownWhileMenuOpen = false;
      this._menuToggleHandler = null;
      this._onSlotChange = () => {
        this._wireMenu();
      };
      this._handlePointerdown = () => {
        if (this._menuOpen) {
          this._pointerdownWhileMenuOpen = true;
        }
      };
      this._handleClick = (event) => {
        if (this.disabled) {
          event.preventDefault();
          event.stopPropagation();
          return;
        }
        if (this.expandable && this._findMenu()) {
          event.preventDefault();
          this._toggleMenu();
          return;
        }
        if (!this.href) {
          event.preventDefault();
          this.dispatchEvent(new CustomEvent("select", {
            bubbles: true,
            composed: true,
            detail: { item: this }
          }));
        }
      };
    }
    // ## Lifecycle
    connectedCallback() {
      super.connectedCallback();
      this.addEventListener("pointerdown", this._handlePointerdown);
      this.addEventListener("click", this._handleClick);
      this._wireMenu();
    }
    firstUpdated() {
      this._wireMenu();
    }
    disconnectedCallback() {
      super.disconnectedCallback();
      this.removeEventListener("pointerdown", this._handlePointerdown);
      this.removeEventListener("click", this._handleClick);
      this._unwireMenu();
    }
    focus(options) {
      const focusable = this.shadowRoot?.querySelector("button, a");
      focusable?.focus(options);
    }
    // ## Helpers
    _findMenu() {
      return this.querySelector("nldd-menu");
    }
    _wireMenu() {
      const menu = this._findMenu();
      if (menu === this._menu)
        return;
      this._unwireMenu();
      if (!menu)
        return;
      this._menu = menu;
      this._menuToggleHandler = (event) => {
        const isOpen = event.newState === "open";
        this._menuOpen = isOpen;
        this.expanded = isOpen;
      };
      menu.addEventListener("toggle", this._menuToggleHandler);
    }
    _unwireMenu() {
      if (this._menu && this._menuToggleHandler) {
        this._menu.removeEventListener("toggle", this._menuToggleHandler);
      }
      this._menu = null;
      this._menuToggleHandler = null;
      this._menuOpen = false;
    }
    // ## Menu popover
    _toggleMenu() {
      if (this._pointerdownWhileMenuOpen) {
        this._pointerdownWhileMenuOpen = false;
        return;
      }
      if (!this._menu)
        return;
      this._menu.anchorElement = this;
      if (this._menuOpen) {
        this._menu.hidePopover();
      } else {
        this._menu.showPopover();
      }
    }
    render() {
      return template20(this);
    }
  };
  NLDDMenuBarItem.styles = menuBarItemStyles;
  __decorate79([
    n4({ type: String, reflect: true })
  ], NLDDMenuBarItem.prototype, "text", void 0);
  __decorate79([
    n4({ type: Boolean, reflect: true })
  ], NLDDMenuBarItem.prototype, "current", void 0);
  __decorate79([
    n4({ type: String, attribute: "current-type", reflect: true })
  ], NLDDMenuBarItem.prototype, "currentType", void 0);
  __decorate79([
    n4({ type: String })
  ], NLDDMenuBarItem.prototype, "href", void 0);
  __decorate79([
    n4({ type: String, reflect: true })
  ], NLDDMenuBarItem.prototype, "icon", void 0);
  __decorate79([
    n4({ type: Boolean, reflect: true })
  ], NLDDMenuBarItem.prototype, "expandable", void 0);
  __decorate79([
    n4({ type: Boolean, attribute: "icon-only", reflect: true })
  ], NLDDMenuBarItem.prototype, "iconOnly", void 0);
  __decorate79([
    n4({ type: String, attribute: "content-priority", reflect: true })
  ], NLDDMenuBarItem.prototype, "contentPriority", void 0);
  __decorate79([
    n4({ type: Boolean, reflect: true })
  ], NLDDMenuBarItem.prototype, "compact", void 0);
  __decorate79([
    n4({ type: Boolean, reflect: true })
  ], NLDDMenuBarItem.prototype, "disabled", void 0);
  __decorate79([
    n4({ type: String, attribute: "accessible-label" })
  ], NLDDMenuBarItem.prototype, "accessibleLabel", void 0);
  __decorate79([
    n4({ type: String })
  ], NLDDMenuBarItem.prototype, "haspopup", void 0);
  __decorate79([
    n4({ type: Boolean, reflect: true })
  ], NLDDMenuBarItem.prototype, "expanded", void 0);
  __decorate79([
    n4({ attribute: false })
  ], NLDDMenuBarItem.prototype, "popoverTargetElement", void 0);
  __decorate79([
    n4({ attribute: false })
  ], NLDDMenuBarItem.prototype, "popoverTargetAction", void 0);
  NLDDMenuBarItem = __decorate79([
    t3("nldd-menu-bar-item")
  ], NLDDMenuBarItem);

  // node_modules/@nldd/design-system/dist/components/navigation/menu-bar/menu-bar.js
  var import_meta25 = {};
  var __decorate80 = function(decorators, target, key, desc) {
    var c6 = arguments.length, r6 = c6 < 3 ? target : desc === null ? desc = Object.getOwnPropertyDescriptor(target, key) : desc, d3;
    if (typeof Reflect === "object" && typeof Reflect.decorate === "function") r6 = Reflect.decorate(decorators, target, key, desc);
    else for (var i8 = decorators.length - 1; i8 >= 0; i8--) if (d3 = decorators[i8]) r6 = (c6 < 3 ? d3(r6) : c6 > 3 ? d3(target, key, r6) : d3(target, key)) || r6;
    return c6 > 3 && r6 && Object.defineProperty(target, key, r6), r6;
  };
  var NLDDMenuBar = class NLDDMenuBar2 extends withTranslations(i4, nlddMenuBarTranslations) {
    constructor() {
      super(...arguments);
      this.overflowText = "";
      this.accessibleLabel = "";
      this.compact = false;
      this._menuOpen = false;
      this._overflowMenu = null;
      this._overflowPointerdownWhileOpen = false;
      this._overflowUpdatePending = false;
      this._resizeObserver = null;
      this._overflowRAF = null;
      this._setupRAF = null;
      this._onSlotChange = () => {
        this._syncCompactAttribute();
        this._scheduleOverflowUpdate();
        this._syncEmpty();
      };
      this._scheduleOverflowUpdate = () => {
        if (this._overflowRAF)
          cancelAnimationFrame(this._overflowRAF);
        this._overflowRAF = requestAnimationFrame(() => {
          this._overflowRAF = null;
          this._updateOverflow();
        });
      };
      this._toggleOverflowMenu = () => {
        if (this._overflowPointerdownWhileOpen) {
          this._overflowPointerdownWhileOpen = false;
          return;
        }
        this._createOverflowMenu();
        if (!this._overflowMenu)
          return;
        const trigger = this._overflowButton?.querySelector("nldd-menu-bar-item");
        this._overflowMenu.anchorElement = trigger ?? this._overflowButton;
        if (this._menuOpen) {
          this._overflowMenu.hidePopover();
        } else {
          this._populateOverflowMenu();
          this._overflowMenu.showPopover();
        }
      };
      this._handleOverflowButtonPointerdown = () => {
        if (this._menuOpen) {
          this._overflowPointerdownWhileOpen = true;
        }
      };
    }
    willUpdate(changed) {
      super.willUpdate(changed);
      if (changed.has("compact")) {
        this._syncCompactAttribute();
      }
    }
    // ## Computed properties
    /** @internal Used by template */
    get _overflowText() {
      return this.overflowText || this._t("components.menu-bar.overflow-action");
    }
    // ## Lifecycle
    disconnectedCallback() {
      super.disconnectedCallback();
      this._cleanupOverflowDetection();
      this._overflowMenu?.remove();
      this._overflowMenu = null;
    }
    firstUpdated() {
      this._setupOverflowDetection();
      this._syncCompactAttribute();
      this._syncEmpty();
      if (import_meta25.env?.DEV) {
        setTimeout(() => {
          if (this.isConnected && !this.accessibleLabel && !this.hasAttribute("empty")) {
            console.warn("nldd-menu-bar: accessible-label is niet gezet. Pagina's met meerdere nav landmarks moeten elke nav een uniek label geven (WCAG 1.3.1).");
          }
        }, 0);
      }
    }
    /** Request a recalculation of overflow state. Call from parent when layout changes. */
    requestOverflowUpdate() {
      this._scheduleOverflowUpdate();
    }
    /** Hide the component when no items are slotted to avoid empty nav landmarks. */
    _syncEmpty() {
      const items = this._defaultSlot?.assignedElements({ flatten: true }) ?? [];
      const hasItems = items.some((el) => el.tagName === "NLDD-MENU-BAR-ITEM");
      this.toggleAttribute("empty", !hasItems);
    }
    /** Propagate compact attribute to slotted items. */
    _syncCompactAttribute() {
      const items = this._defaultSlot?.assignedElements({ flatten: true }) ?? [];
      for (const item of items) {
        item.toggleAttribute("compact", this.compact);
      }
    }
    // ## Overflow detection
    _setupOverflowDetection() {
      this._cleanupOverflowDetection();
      this._setupRAF = requestAnimationFrame(() => {
        this._setupRAF = null;
        if (!this.isConnected)
          return;
        this._resizeObserver = new ResizeObserver(() => {
          this._scheduleOverflowUpdate();
        });
        this._resizeObserver.observe(this);
        if (this._defaultSlot) {
          this._defaultSlot.addEventListener("slotchange", this._onSlotChange);
        }
        this._scheduleOverflowUpdate();
      });
    }
    _cleanupOverflowDetection() {
      if (this._setupRAF) {
        cancelAnimationFrame(this._setupRAF);
        this._setupRAF = null;
      }
      if (this._overflowRAF) {
        cancelAnimationFrame(this._overflowRAF);
        this._overflowRAF = null;
      }
      if (this._resizeObserver) {
        this._resizeObserver.disconnect();
        this._resizeObserver = null;
      }
      if (this._defaultSlot) {
        this._defaultSlot.removeEventListener("slotchange", this._onSlotChange);
      }
    }
    /**
     * Calculate which slotted items overflow and hide them behind an overflow button.
     * Note: not unit-tested — JSDOM lacks layout support (offsetWidth, clientWidth).
     * Covered by visual/E2E testing via Storybook stories.
     */
    _updateOverflow() {
      const overflowButton = this._overflowButton;
      if (!overflowButton)
        return;
      if (this._menuOpen) {
        this._overflowUpdatePending = true;
        return;
      }
      const slottedElements = this._defaultSlot?.assignedElements({ flatten: true }) ?? [];
      const items = slottedElements.filter((el) => el.tagName === "NLDD-MENU-BAR-ITEM");
      if (items.length === 0) {
        overflowButton.style.display = "none";
        return;
      }
      items.forEach((item) => {
        item.style.display = "";
        item.removeAttribute("data-overflow");
      });
      overflowButton.style.display = "inline-block";
      const containerWidth = this.clientWidth;
      const totalItemsWidth = items.reduce((sum, item) => sum + item.offsetWidth, 0);
      if (totalItemsWidth <= containerWidth) {
        overflowButton.style.display = "none";
        return;
      }
      const overflowButtonWidth = overflowButton.offsetWidth;
      const availableWidth = containerWidth - overflowButtonWidth;
      let usedWidth = 0;
      let overflowStartIndex = -1;
      for (let i8 = 0; i8 < items.length; i8++) {
        const itemWidth = items[i8].offsetWidth;
        if (usedWidth + itemWidth > availableWidth) {
          overflowStartIndex = i8;
          break;
        }
        usedWidth += itemWidth;
      }
      if (overflowStartIndex >= 0) {
        for (let i8 = overflowStartIndex; i8 < items.length; i8++) {
          items[i8].style.display = "none";
          items[i8].setAttribute("data-overflow", "true");
        }
      } else {
        overflowButton.style.display = "none";
      }
    }
    // ## Overflow popover menu
    _createOverflowMenu() {
      if (this._overflowMenu)
        return;
      if (typeof document === "undefined")
        return;
      const menu = document.createElement("nldd-menu");
      menu.setAttribute("placement", "bottom-end");
      menu.addEventListener("toggle", (event) => {
        const isOpen = event.newState === "open";
        this._menuOpen = isOpen;
        if (!isOpen) {
          if (this._overflowUpdatePending) {
            this._overflowUpdatePending = false;
            this._scheduleOverflowUpdate();
          }
        }
      });
      document.body.appendChild(menu);
      this._overflowMenu = menu;
    }
    _populateOverflowMenu() {
      if (!this._overflowMenu)
        return;
      this._overflowMenu.replaceChildren();
      const slottedElements = this._defaultSlot?.assignedElements({ flatten: true }) ?? [];
      const overflowItems = slottedElements.filter((el) => el.tagName === "NLDD-MENU-BAR-ITEM" && el.hasAttribute("data-overflow"));
      for (const item of overflowItems) {
        const menuItem = document.createElement("nldd-menu-item");
        menuItem.setAttribute("text", item.text);
        if (item.icon)
          menuItem.setAttribute("icon", item.icon);
        if (item.current)
          menuItem.setAttribute("selected", "");
        if (item.disabled)
          menuItem.setAttribute("disabled", "");
        if (item.expandable) {
          const children = item.querySelectorAll("nldd-menu-item, nldd-menu-divider");
          if (children.length > 0) {
            const submenu = document.createElement("nldd-menu");
            children.forEach((child) => {
              const clone = child.cloneNode(true);
              const original = child;
              if (typeof original.select === "function") {
                clone.addEventListener("click", () => original.select());
              }
              submenu.appendChild(clone);
            });
            menuItem.appendChild(submenu);
            this._overflowMenu.appendChild(menuItem);
            continue;
          }
        }
        menuItem.addEventListener("click", () => {
          item.click();
        });
        this._overflowMenu.appendChild(menuItem);
      }
    }
    // ## Render
    render() {
      return template19(this);
    }
  };
  NLDDMenuBar.styles = menuBarStyles;
  __decorate80([
    n4({ type: String, attribute: "overflow-text" })
  ], NLDDMenuBar.prototype, "overflowText", void 0);
  __decorate80([
    n4({ type: String, attribute: "accessible-label" })
  ], NLDDMenuBar.prototype, "accessibleLabel", void 0);
  __decorate80([
    n4({ type: Boolean, reflect: true })
  ], NLDDMenuBar.prototype, "compact", void 0);
  __decorate80([
    e5("slot:not([name])")
  ], NLDDMenuBar.prototype, "_defaultSlot", void 0);
  __decorate80([
    e5(".menu-bar__overflow-button")
  ], NLDDMenuBar.prototype, "_overflowButton", void 0);
  __decorate80([
    r5()
  ], NLDDMenuBar.prototype, "_menuOpen", void 0);
  NLDDMenuBar = __decorate80([
    t3("nldd-menu-bar")
  ], NLDDMenuBar);

  // node_modules/@nldd/design-system/dist/components/navigation/link/link.js
  init_lit();
  init_decorators();

  // node_modules/@nldd/design-system/dist/components/navigation/link/link.styles.js
  init_lit();
  init_slotted_reset();
  var linkStyles = i`


	/* # Host */

	/* Two modes: no [size] or [size="inherit"] → display:inline (wraps in
	   running text, inherits font). Sized xs/sm/md/lg → inline-flex (start/end
	   icons baseline-aligned with explicit gap). */

	:host {
		${inheritedTextReset}
		-webkit-tap-highlight-color: transparent;
	}

	:host(:not([size])),
	:host([size="inherit"]) {
		display: inline;
	}

	:host([size="xs"]),
	:host([size="sm"]),
	:host([size="md"]),
	:host([size="lg"]) {
		display: inline-flex;
	}

	:host([hidden]) {
		display: none;
	}

	:host([disabled]) {
		opacity: var(--primitives-opacity-disabled);
		pointer-events: none;
	}


	/* # Block */

	.link {
		border-radius: var(--primitives-corner-radius-xs);
		background: none;
		color: var(--semantics-links-color);
		text-decoration: none;
		transition: color var(--primitives-transition-duration-fast) var(--primitives-transition-easing-default);
		appearance: none;
	}

	@media (prefers-reduced-motion: reduce) {
		.link {
			transition: none;
		}
	}

	:host(:not([size])) .link,
	:host([size="inherit"]) .link {
		display: inline;
	}

	:host([size="xs"]) .link {
		display: inline-flex;
		gap: var(--primitives-space-4);
		align-items: center;
		font: var(--primitives-font-body-xs-regular-flat);
	}

	:host([size="sm"]) .link {
		display: inline-flex;
		gap: var(--primitives-space-4);
		align-items: center;
		font: var(--primitives-font-body-sm-regular-flat);
	}

	:host([size="md"]) .link {
		display: inline-flex;
		gap: var(--primitives-space-6);
		align-items: center;
		font: var(--primitives-font-body-md-regular-flat);
	}

	:host([size="lg"]) .link {
		display: inline-flex;
		gap: var(--primitives-space-6);
		align-items: center;
		font: var(--primitives-font-body-lg-regular-flat);
	}

	@media (hover: hover) {
		.link:hover {
			color: var(--semantics-links-is-hovered-color);
		}
	}

	.link:active {
		color: var(--semantics-links-is-active-color);
	}

	.link:focus-visible {
		outline: var(--semantics-focus-ring-outline);
		outline-offset: var(--semantics-focus-ring-outline-offset);
		box-shadow: var(--semantics-focus-ring-box-shadow);
	}

	@media (forced-colors: active) {
		.link:focus-visible {
			outline: 2px solid CanvasText;
		}
	}

	.link:focus:not(:focus-visible) {
		outline: none;
	}


	/* # Elements */

	.link__label {
		text-decoration: underline;
		text-underline-offset: 0.15em;
	}

	.link:focus-visible .link__label {
		text-decoration: none;
	}

	.link__start-icon,
	.link__end-icon {
		display: inline-flex;
		position: relative;
		width: 1em;
		height: 1em;
		flex-shrink: 0;
		align-items: center;
	}

	:host(:not([size])) .link__start-icon,
	:host([size="inherit"]) .link__start-icon,
	:host(:not([size])) .link__end-icon,
	:host([size="inherit"]) .link__end-icon {
		top: 0.2em;
	}
`;

  // node_modules/@nldd/design-system/dist/components/navigation/link/link.template.js
  init_lit();
  function template21(helpers) {
    const resolvedRel = this._resolvedRel();
    return b2`
		<a class="link"
			href=${this.disabled ? A : this.href || A}
			role=${this.disabled ? "link" : A}
			tabindex=${this.disabled ? "0" : A}
			target=${this.disabled ? A : this.target || A}
			rel=${this.disabled ? A : resolvedRel || A}
			aria-disabled=${this.disabled ? "true" : A}
			aria-label=${this.accessibleLabel || A}
			@click=${helpers.handleClick}
		>
			${this.startIcon ? b2`
				<span class="link__start-icon"><nldd-icon name=${this.startIcon}></nldd-icon></span>
			` : b2`<slot name="start-icon"></slot>`}
			<span class="link__label">${this.text ? this.text : b2`<slot></slot>`}</span>
			${this.endIcon ? b2`
				<span class="link__end-icon"><nldd-icon name=${this.endIcon}></nldd-icon></span>
			` : b2`<slot name="end-icon"></slot>`}
		</a>
	`;
  }

  // node_modules/@nldd/design-system/dist/components/navigation/link/link.js
  init_icon();
  var __decorate81 = function(decorators, target, key, desc) {
    var c6 = arguments.length, r6 = c6 < 3 ? target : desc === null ? desc = Object.getOwnPropertyDescriptor(target, key) : desc, d3;
    if (typeof Reflect === "object" && typeof Reflect.decorate === "function") r6 = Reflect.decorate(decorators, target, key, desc);
    else for (var i8 = decorators.length - 1; i8 >= 0; i8--) if (d3 = decorators[i8]) r6 = (c6 < 3 ? d3(r6) : c6 > 3 ? d3(target, key, r6) : d3(target, key)) || r6;
    return c6 > 3 && r6 && Object.defineProperty(target, key, r6), r6;
  };
  var NLDDLink = class NLDDLink2 extends i4 {
    constructor() {
      super(...arguments);
      this.href = void 0;
      this.target = void 0;
      this.rel = void 0;
      this.text = "";
      this.startIcon = "";
      this.endIcon = "";
      this.accessibleLabel = "";
      this.disabled = false;
    }
    _handleClick(e9) {
      if (this.disabled) {
        e9.preventDefault();
        e9.stopPropagation();
      }
    }
    /** @internal */
    _resolvedRel() {
      const base = this.rel ?? "";
      if (this.target !== "_blank")
        return base;
      const parts = new Set(base.split(/\s+/).filter(Boolean));
      parts.add("noopener");
      parts.add("noreferrer");
      return [...parts].join(" ");
    }
    render() {
      return template21.call(this, {
        handleClick: this._handleClick.bind(this)
      });
    }
  };
  NLDDLink.styles = linkStyles;
  __decorate81([
    n4({ type: String, reflect: true })
  ], NLDDLink.prototype, "href", void 0);
  __decorate81([
    n4({ type: String })
  ], NLDDLink.prototype, "target", void 0);
  __decorate81([
    n4({ type: String })
  ], NLDDLink.prototype, "rel", void 0);
  __decorate81([
    n4({ type: String, reflect: true })
  ], NLDDLink.prototype, "size", void 0);
  __decorate81([
    n4({ type: String })
  ], NLDDLink.prototype, "text", void 0);
  __decorate81([
    n4({ type: String, attribute: "start-icon" })
  ], NLDDLink.prototype, "startIcon", void 0);
  __decorate81([
    n4({ type: String, attribute: "end-icon" })
  ], NLDDLink.prototype, "endIcon", void 0);
  __decorate81([
    n4({ type: String, attribute: "accessible-label" })
  ], NLDDLink.prototype, "accessibleLabel", void 0);
  __decorate81([
    n4({ type: Boolean, reflect: true })
  ], NLDDLink.prototype, "disabled", void 0);
  NLDDLink = __decorate81([
    t3("nldd-link")
  ], NLDDLink);

  // node_modules/@nldd/design-system/dist/components/navigation/skip-link/skip-link.js
  init_lit();
  init_decorators();

  // node_modules/@nldd/design-system/dist/components/navigation/skip-link/skip-link.styles.js
  init_lit();
  init_slotted_reset();
  var skipLinkStyles = i`


	/* # Host */

	:host {
		--_z-index: 1000;
		--_box-shadow: var(--primitives-box-shadows-level-3);
		--_focus-box-shadow: inset var(--semantics-focus-ring-box-shadow);
		--_focus-outline-offset: -6px;

		${inheritedTextReset}
		display: block;
		position: relative;
	}

	:host([hidden]) {
		display: none;
	}


	/* # Block */

	.skip-link {
		display: flex;
		position: absolute;
		top: 0;
		left: 0;
		opacity: 0;
		z-index: var(--_z-index);
		border-radius: var(--semantics-controls-md-corner-radius);
		box-shadow: var(--_box-shadow);
		background-color: var(--semantics-surfaces-base-background-color);
		pointer-events: none;
		justify-content: center;
	}

	.skip-link:has(:focus-visible) {
		opacity: 1;
		pointer-events: auto;
	}


	/* # Elements */

	.skip-link__control {
		display: inline-flex;
		border: none;
		border-radius: var(--semantics-controls-sm-corner-radius);
		background: none;
		min-height: var(--semantics-controls-md-min-size);
		padding: var(--primitives-space-4) var(--primitives-space-16);
		align-items: center;
		color: var(--semantics-links-color);
		font: var(--primitives-font-body-md-medium-flat);
		text-decoration: underline;
		white-space: nowrap;
		appearance: none;
	}

	.skip-link__control:focus-visible {
		outline: var(--semantics-focus-ring-outline);
		/* negative: keep the focus halo inside the small skip-link, not past the viewport */
		outline-offset: var(--_focus-outline-offset);
		box-shadow: var(--_focus-box-shadow);
	}
`;

  // node_modules/@nldd/design-system/dist/components/navigation/skip-link/skip-link.template.js
  init_lit();
  function template22(component) {
    const safeHref = sanitizeUrl(component.href);
    return b2`
		<div class="skip-link">
			${safeHref ? b2`
				<a class="skip-link__control"
					href=${safeHref}
				>
					${component._text}
				</a>
			` : b2`
				<button class="skip-link__control"
					type="button"
					@click=${component._handleClick}
				>
					${component._text}
				</button>
			`}
		</div>
		<slot></slot>
	`;
  }

  // node_modules/@nldd/design-system/dist/components/navigation/skip-link/skip-link.i18n.js
  var nlddSkipLinkTranslations = {
    "components.skip-link.action": "Sla over"
  };

  // node_modules/@nldd/design-system/dist/components/navigation/skip-link/skip-link.js
  var __decorate82 = function(decorators, target, key, desc) {
    var c6 = arguments.length, r6 = c6 < 3 ? target : desc === null ? desc = Object.getOwnPropertyDescriptor(target, key) : desc, d3;
    if (typeof Reflect === "object" && typeof Reflect.decorate === "function") r6 = Reflect.decorate(decorators, target, key, desc);
    else for (var i8 = decorators.length - 1; i8 >= 0; i8--) if (d3 = decorators[i8]) r6 = (c6 < 3 ? d3(r6) : c6 > 3 ? d3(target, key, r6) : d3(target, key)) || r6;
    return c6 > 3 && r6 && Object.defineProperty(target, key, r6), r6;
  };
  var NLDDSkipLink = class NLDDSkipLink2 extends withTranslations(i4, nlddSkipLinkTranslations) {
    constructor() {
      super(...arguments);
      this.text = "";
      this.href = "";
      this._handleClick = () => {
        const next = this.nextElementSibling;
        if (next) {
          const hadTabindex = next.hasAttribute("tabindex");
          if (!hadTabindex) {
            next.setAttribute("tabindex", "-1");
            next.addEventListener("blur", () => {
              next.removeAttribute("tabindex");
            }, { once: true });
          }
          next.focus();
        }
      };
    }
    get _text() {
      return this.text || this._t("components.skip-link.action");
    }
    render() {
      return template22(this);
    }
  };
  NLDDSkipLink.styles = skipLinkStyles;
  __decorate82([
    n4({ type: String })
  ], NLDDSkipLink.prototype, "text", void 0);
  __decorate82([
    n4({ type: String })
  ], NLDDSkipLink.prototype, "href", void 0);
  NLDDSkipLink = __decorate82([
    t3("nldd-skip-link")
  ], NLDDSkipLink);

  // node_modules/@nldd/design-system/dist/components/navigation/top-navigation-bar/top-navigation-bar.js
  init_lit();
  init_decorators();

  // node_modules/@nldd/design-system/dist/components/navigation/top-navigation-bar/top-navigation-bar.styles.js
  init_lit();
  init_breakpoints();
  init_slotted_reset();
  var smMax15 = r(breakpoints.smMax);
  var mdMin17 = r(breakpoints.mdMin);
  var mdMax11 = r(breakpoints.mdMax);
  var lgMin13 = r(breakpoints.lgMin);
  var topNavigationBarStyles = i`


	/* # Host */

	:host {
		--_logo-width: var(--primitives-space-40);
		--_wordmark-content-color: light-dark(var(--primitives-color-reference-lintblauw), var(--primitives-color-neutral-1000));

		${inheritedTextReset}
		container-type: inline-size;
		display: block;
		width: 100%;
	}

	:host([hidden]) {
		display: none;
	}


	/* # Block */

	.top-navigation-bar {
		box-sizing: border-box;
		display: flex;
		margin: 0 auto;
		width: 100%;
		flex-direction: column;
	}


	/* # Logo bar */

	.top-navigation-bar__logo-bar {
		display: grid;
		grid-template-columns: 1fr auto 1fr;
		gap: var(--primitives-space-8);
		align-items: center;

		@container (max-width: ${smMax15}) {
			padding-inline: var(--semantics-page-sections-sm-margin-inline);
		}

		@container (min-width: ${mdMin17}) and (max-width: ${mdMax11}) {
			padding-inline: var(--semantics-page-sections-md-margin-inline);
		}

		@container (min-width: ${lgMin13}) {
			padding-inline: var(--semantics-page-sections-lg-margin-inline);
		}
	}

	/* ## Logo */

	.top-navigation-bar__logo {
		display: flex;
		width: var(--_logo-width);
		height: calc(var(--_logo-width) * 2);
		grid-column: 2;
		align-self: start;
		align-items: center;
		justify-content: center;

		@container (max-width: ${smMax15}) {
			--_logo-width: var(--primitives-space-40);
		}

		@container (min-width: ${mdMin17}) and (max-width: ${mdMax11}) {
			--_logo-width: var(--primitives-space-44);
		}

		@container (min-width: ${lgMin13}) {
			--_logo-width: var(--primitives-space-48);
		}
	}

	.top-navigation-bar__logo svg {
		width: 100%;
		height: 100%;
	}

	a.top-navigation-bar__logo {
		color: inherit;
		text-decoration: none;
	}

	a.top-navigation-bar__logo:focus-visible {
		outline: var(--semantics-focus-ring-outline);
		outline-offset: var(--semantics-focus-ring-outline-offset);
		box-shadow: var(--semantics-focus-ring-box-shadow);
	}

	/* ## Logo and wordmark */

	.top-navigation-bar__logo-and-wordmark {
		display: grid;
		grid-column: 2 / 4;
		grid-template-columns: subgrid;
		align-items: center;
	}

	.top-navigation-bar__logo-and-wordmark > .top-navigation-bar__logo {
		grid-column: 1;
	}

	.top-navigation-bar__logo-and-wordmark > .top-navigation-bar__wordmark {
		grid-column: 2;
	}

	a.top-navigation-bar__logo-and-wordmark {
		text-decoration: none;
	}

	a.top-navigation-bar__logo-and-wordmark:focus-visible {
		outline: var(--semantics-focus-ring-outline);
		outline-offset: var(--semantics-focus-ring-outline-offset);
		box-shadow: var(--semantics-focus-ring-box-shadow);
	}

	/* ## Wordmark */

	.top-navigation-bar__wordmark {
		display: flex;
		min-height: calc(var(--_logo-width) * 2);
		grid-column: 3;
		flex-direction: column;
		color: var(--_wordmark-content-color);
	}

	.top-navigation-bar__wordmark-spacer {
		height: var(--_logo-width);
		flex-grow: 0;
		flex-shrink: 0;
	}

	.top-navigation-bar__wordmark-content {
		display: flex;
		flex-direction: column;
		flex-grow: 1;
		flex-shrink: 1;
		flex-basis: 50%;
	}

	.top-navigation-bar__wordmark-title {
		margin: 0;
		font: var(--primitives-font-body-sm-medium-flat);
		text-wrap: pretty;
	}

	.top-navigation-bar__wordmark-subtitle {
		margin: 0;
		font: var(--primitives-font-body-xs-regular-flat);
		text-wrap: pretty;
	}

	.top-navigation-bar__wordmark-supporting-text {
		margin: 0;
		font: var(--primitives-font-body-xxs-regular-flat);
		text-wrap: pretty;
	}


	/* # Main bar */

	.top-navigation-bar__main-bar {
		display: flex;

		@container (max-width: ${smMax15}) {
			padding-inline: calc(var(--semantics-page-sections-sm-margin-inline) - var(--components-menu-bar-item-inline-padding));
			flex-direction: column;
		}

		@container (min-width: ${mdMin17}) and (max-width: ${mdMax11}) {
			padding-inline: calc(var(--semantics-page-sections-md-margin-inline) - var(--components-menu-bar-item-inline-padding));
			flex-direction: row;
			align-items: center;
		}

		@container (min-width: ${lgMin13}) {
			padding-inline: calc(var(--semantics-page-sections-lg-margin-inline) - var(--components-menu-bar-item-inline-padding));
			flex-direction: row;
			align-items: center;
		}
	}

	/* ## Title bar */

	.top-navigation-bar__website-title-bar {
		display: flex;
		padding-inline: calc(var(--primitives-space-8) + var(--components-menu-bar-item-inline-padding));
		align-items: center;
		justify-content: center;

		@container (max-width: ${smMax15}) {
			padding-top: var(--primitives-space-4);
		}

		@container (min-width: ${mdMin17}) {
			padding: 0;
			justify-content: flex-start;
		}
	}

	:host([no-logo]) .top-navigation-bar__website-title-bar {
		@container (max-width: ${smMax15}) {
			padding-top: var(--primitives-space-8);
		}
	}

	/* ## Title */

	.top-navigation-bar__website-title {
		font: var(--components-top-navigation-bar-title-sm-font);
		color: var(--semantics-content-color);
		white-space: nowrap;

		@container (min-width: ${mdMin17}) {
			font: var(--components-top-navigation-bar-title-md-font);
			margin-inline: var(--components-menu-bar-item-inline-padding);
		}

		@container (min-width: ${lgMin13}) {
			font: var(--components-top-navigation-bar-title-lg-font);
		}
	}

	a.top-navigation-bar__website-title {
		border-radius: var(--primitives-corner-radius-xxs);
		text-decoration: none;
	}

	a.top-navigation-bar__website-title:focus-visible {
		outline: var(--semantics-focus-ring-outline);
		outline-offset: var(--semantics-focus-ring-outline-offset);
		box-shadow: var(--semantics-focus-ring-box-shadow);
	}

	/* ## Menu bar */

	.top-navigation-bar__menu-bar {
		display: flex;
		min-width: 0;
		align-items: center;
		gap: var(--primitives-space-12);
		flex-grow: 1;
	}

	/* ## Menu bar start */

	.top-navigation-bar__menu-bar-start {
		display: flex;
		min-width: 0;
		align-items: center;

		@container (max-width: ${mdMax11}) {
			flex-grow: 0;
			flex-shrink: 0;
		}

		@container (min-width: ${lgMin13}) {
			flex-grow: 1;
			flex-shrink: 1;
		}
	}

	/* ## Menu bar end */

	.top-navigation-bar__menu-bar-end {
		display: flex;
		min-width: 0;
		align-items: center;

		@container (max-width: ${mdMax11}) {
			flex-grow: 1;
			flex-shrink: 1;
		}

		@container (min-width: ${lgMin13}) {
			flex-grow: 0;
			flex-shrink: 0;
		}
	}

	/* ## Global bar */

	.top-navigation-bar__global-menu-bar {
		display: none;
		min-width: 0;
		flex-grow: 1;
		flex-shrink: 1;
		@container (min-width: ${lgMin13}) {
			:host(.has-global-items) & {
				display: flex;
			}
		}
	}

	/* ## Menu button */

	.top-navigation-bar__menu-button {
		display: none;

		@container (max-width: ${mdMax11}) {
			:host(.has-global-items) & {
				display: inline-block;
			}
		}
	}

	/* ## Utility menu bar */

	.top-navigation-bar__utility-menu-bar {
		display: flex;
		min-width: 0;
		flex-grow: 1;
		flex-shrink: 1;
		justify-content: flex-end;
	}

	slot[name="utility"]::slotted(nldd-menu-bar) {
		flex-grow: 0;
	}
`;

  // node_modules/@nldd/design-system/dist/components/navigation/top-navigation-bar/top-navigation-bar.template.js
  init_lit();
  init_unsafe_html2();

  // node_modules/@nldd/design-system/dist/components/navigation/top-navigation-bar/logo.js
  var logo_default = '<svg xmlns="http://www.w3.org/2000/svg" viewBox="0 0 50 100" aria-hidden="true" focusable="false"><path d="M0 0h50v100H0z" fill="#154273"/><path d="M25.9 77.36h-.87v-2.41h.87zm3.93-10.64h-.87v-2.41h.87zM13.7 62.56c-.06 0-.51 0-.51.1 0 .09.2.04.51.1.7.11.97.46 1.46.46.45 0 .9-.3.72-.94q-.06-.13-.1-.01c-.07.29-.42.45-.8.45-.46 0-.55-.16-1.28-.16m-4.79 2.41c.08.11.9 1.17-.36 1.58q-.1.04 0 .11c.94.39 1.39-.33 1.39-.33.47 1.08-.25 1.67-.25 1.67.1.2.54.52 1.22.67.26-.14.62-.4.77-.8 0 0 .18.46.01.91 1.9.17 2.58-1.35 4.04-1.07q.06 0 .06-.07c0-.94-.57-2.65-2.38-3s-1.33-1.09-1.33-1.09c.77-.03 1.2.3 1.2.3.15-.23-.05-.48-.05-.48.07-.08.11-.4.11-.4-.86 0-1.08-.18-1.08-.18-.13-.5.22-1.23 1.5-.46.32-.28.3-.72.3-.72.09-.04.15-.2.15-.26 0-.2-1.02-.78-1.28-.85-.07-.3-.46-.75-1.66-.6-.57.08-.98-.07-.79-.4.03-.06-.07-.07-.12-.01-.13.13-.31.5.18.75.55.28.59.83.6.9 0 .12-.14.14-.17.02-.25-.93-1.53-1.13-2.28-.64.04.5.58.83.87.83.17 0 .4-.17.4-.4 0-.11.06-.1.1-.01.12.33-.37 1.01-1.1.6-.74.74-.44 1.65-.37 1.99.14.6-.16 1.2-.7.92q-.05-.05-.06.04c0 .38.5.81 1.08.48M13 61.24c-.34.38-.9.3-1.04-.34.7-.04 1.01.03 1.04.34m22.07 1.48c-.37 0-.72-.16-.79-.45-.01-.07-.08-.1-.1 0-.18.65.27.95.72.95.48 0 .76-.35 1.45-.47.31-.05.52 0 .52-.1 0-.09-.45-.09-.52-.09-.72 0-.82.16-1.28.16m7.16 1.77q0-.09-.06-.04c-.54.27-.84-.31-.7-.92.07-.34.36-1.25-.38-2-.73.42-1.22-.26-1.1-.6.04-.08.1-.1.1.02 0 .23.23.4.4.4.3 0 .83-.33.88-.83-.76-.5-2.03-.3-2.28.64-.03.12-.17.1-.16-.01 0-.08.03-.63.58-.91.5-.25.32-.62.19-.75-.06-.06-.16-.05-.13 0 .2.34-.21.49-.78.42-1.2-.16-1.6.28-1.66.6-.26.06-1.28.63-1.28.84 0 .07.06.22.16.26 0 0-.03.44.29.72 1.27-.77 1.63-.05 1.5.46 0 0-.23.18-1.09.19 0 0 .04.3.12.4 0 0-.2.24-.06.47 0 0 .44-.33 1.2-.3 0 0 .49.74-1.32 1.1-1.81.34-2.39 2.05-2.39 3q0 .06.06.06c1.47-.28 2.14 1.24 4.04 1.07-.16-.45.02-.91.02-.91.15.4.51.66.77.8.67-.15 1.12-.48 1.22-.67 0 0-.72-.6-.25-1.67 0 0 .45.72 1.38.33q.11-.07.01-.1c-1.26-.42-.44-1.48-.36-1.59.58.33 1.08-.1 1.08-.48m-4.12-3.6c-.14.64-.7.73-1.05.35.03-.31.34-.38 1.05-.34M24.7 50.22c0 .16.07.3.12.38.12.16.1.25.04.3-.05.05-.14.07-.3-.04a1 1 0 0 0-.37-.12c-.28 0-.41.18-.41.33s.13.33.4.33q.26-.01.38-.12.23-.15.3-.04c.06.05-.02.6-.08 1.04-.46.1-.67.45-.67.67 0 .54.58.86.92 1.04.35-.18.93-.5.93-1.04 0-.22-.22-.58-.67-.67a4 4 0 0 1-.09-1.04c.05-.05.14-.07.3.04q.13.1.38.12c.28 0 .41-.18.41-.33s-.13-.33-.41-.33q-.25.01-.38.12-.23.15-.3.04c-.05-.05-.07-.14.04-.3a1 1 0 0 0 .13-.38c0-.27-.18-.4-.34-.4-.15 0-.33.13-.33.4m.33 4.06a.67.67 0 1 0 0 1.33.67.67 0 0 0 0-1.33m0 6.41c2.67 0 4.18.51 4.18.51.01-1.34 0-2.1.78-1.98-.43-1.14 1.4-1.58 1.4-3.06 0-.97-.7-1.08-.9-1.08-.6 0-.5.4-1.02.4-.06 0-.13-.02-.13.02 0 .17.14.46.38.46.32 0 .32-.31.56-.31.1 0 .24.08.24.4 0 .63-.4 1.33-.9 1.87a.6.6 0 0 0-.44-.25c-.28 0-.54.3-.54.67q0 .18.08.35a1 1 0 0 1-.42.14c-.22 0-.55-.09-.55-.57 0-1.07 1.44-2.01 1.44-3.37 0-.64-.44-1.29-1.34-1.29-.97 0-.79.89-1.63.89q-.06-.01-.07.04c0 .06.15.55.58.55.47 0 .6-.8 1-.8.16 0 .4.09.4.63 0 .42-.22 1.1-.52 1.76a.6.6 0 0 0-.42-.19c-.36 0-.62.35-.62.77q0 .4.28.68c-.2.27-.42.49-.72.49-.58 0-.83-.33-.83-.69 0-.38.7-.53.7-1.13 0-.41-.26-.64-.52-.64-.28 0-.42.18-.45.18s-.17-.18-.44-.18c-.26 0-.53.23-.53.64 0 .6.7.75.7 1.13 0 .36-.25.69-.83.69-.29 0-.5-.22-.72-.5a1 1 0 0 0 .28-.67c0-.42-.25-.77-.61-.77a.6.6 0 0 0-.43.2 5 5 0 0 1-.5-1.77c0-.54.22-.63.38-.63.4 0 .54.8 1 .8.44 0 .58-.5.58-.55q0-.04-.07-.04c-.84 0-.65-.89-1.62-.89-.9 0-1.34.65-1.34 1.3 0 1.35 1.43 2.3 1.43 3.36 0 .48-.33.57-.54.57a1 1 0 0 1-.42-.14 1 1 0 0 0 .07-.35c0-.37-.25-.67-.54-.67q-.27.01-.44.25c-.49-.54-.9-1.24-.9-1.87 0-.32.15-.4.25-.4.23 0 .24.31.56.31.23 0 .38-.29.38-.46 0-.04-.07-.03-.13-.03-.52 0-.42-.39-1.03-.39-.2 0-.89.11-.89 1.08 0 1.48 1.82 1.92 1.4 3.06.77-.12.76.64.78 1.98 0 0 1.5-.5 4.17-.5m-4.89 2q-1.11.24-1.48.38c.01-.33.13-.62.42-.69.04 0 .05-.06.01-.08-.65-.43-1 .4-1 .42-.23-.02-.58.05-.58.37 0 .92-2.12 1.44-2.61 1.51.86.34 1.22 1.43 1.22 1.43a5 5 0 0 1 1.53-1.18c.17 0 .23.2.24.33q.02.06.07.02c.33-.5.17-.6.15-.75-.03-.11 0-.54.49-.5v6.2l-.25.05c-.11.02-1.02-.75-2.05-1.47-1.04-.72-1.6-.29-2.78.35-1.54.84-3.1.08-3.1.08-1.34.95-1.8 3.7-1.8 3.7-.26.12-.7.24-1.12.24-1.01 0-1.2-.62-1.2-.99 0-1.57 1.97-2.24 1.97-3.79 0-.37-.18-1.9-2.07-1.9H4.51c-.7 0-.88-.51-.98-.7-.04-.08-.11-.04-.08.02.05.13-.09.33-.09.72 0 .63.37 1 1.06 1 .31 0 .6-.08.7-.14.07-.04.1.02.07.06-.3.35-.32 1-.2 1.16q.05.05.08 0a1.7 1.7 0 0 1 1.62-1.3c1.03 0 1.02.88 1.02 1.1 0 1.25-2.21 2.18-2.21 3.9 0 1.77 1.85 2.14 2.93 1.86-.06 1.76-2.08 2.36-2.14 1.1q-.02-.1-.08 0c-.25.63-.15 1.18.63 1.35q.06.03-.02.08c-.78.5-.36 2.43-.32 3 .06.82-.76.72-.82.7q-.08-.02-.02.07c.58.71 1.3.06 1.3.06.54.17.2.63-.16.87q-.08.07.01.08c.08 0 .92.08 1.07-.59.37.44.92.28 1.03.22.16-.08.95-.36 1.02.52 0 .06.05.02.07-.01.6-.75-.04-1.2-.4-1.26a.6.6 0 0 1 .94.11q.04.08.06-.03c.05-.95-.72-1.03-1.22-.62-.02-.1-.19-.63.56-.61.02 0 .06-.03.02-.06-.58-.62-1.06-.06-1.18.07-.26.31-.9.19-.96.18.2-3.29 3.1-2.12 3.5-3.72.01-.08.05-.05.06-.02.1.39 1.1.44 1.27-.32q.02-.1-.03-.05c-.84.83-2.73-1.78-.15-3.91a3.74 3.74 0 0 1 4.92-.2c.04.26-.12.34-.22.37q-.05.03.01.07c.21.08.5.05.65-.14.47.33.07.74-.13.86q-.05.05.02.07c.43.08.79-.37.8-.62l.08.04v1.87c0 2.66 3.07 3.03 6.43 5.28 3.37-2.25 6.44-2.62 6.44-5.28v-1.87l.07-.04c.02.25.38.7.81.62q.07-.01.01-.07c-.19-.12-.6-.53-.12-.86.14.2.43.22.64.14q.08-.04.01-.07c-.1-.03-.25-.11-.21-.38.75-.6 2.72-1.6 4.92.2 2.57 2.14.68 4.75-.15 3.92q-.05-.04-.03.05c.18.76 1.16.7 1.27.32 0-.03.04-.06.06.02.4 1.6 3.3.43 3.5 3.72-.06 0-.7.13-.97-.18-.1-.13-.59-.7-1.17-.07-.04.03 0 .06.01.06.76-.02.59.51.57.61-.5-.41-1.27-.33-1.22.62q0 .1.06.03a.6.6 0 0 1 .93-.11c-.35.05-.99.51-.4 1.26.03.03.07.07.07 0 .08-.87.86-.59 1.03-.5.11.05.66.21 1.03-.23.15.67.98.6 1.06.6q.1-.02.01-.09c-.36-.24-.7-.7-.15-.87 0 0 .72.65 1.3-.06.05-.07.01-.08-.02-.07-.07.02-.89.12-.83-.7.04-.57.46-2.5-.31-3q-.1-.06-.02-.08c.78-.17.88-.72.63-1.34q-.07-.11-.09 0c-.06 1.25-2.07.65-2.13-1.1 1.07.27 2.93-.1 2.93-1.86 0-1.73-2.21-2.66-2.21-3.92 0-.2-.01-1.08 1.02-1.08.83 0 1.46.63 1.62 1.3 0 .02.05.03.07 0 .13-.17.1-.82-.2-1.17-.03-.04 0-.1.07-.06.1.06.4.15.7.15.7 0 1.06-.38 1.06-1.01 0-.4-.13-.59-.08-.72.03-.06-.05-.1-.08-.03-.1.2-.28.7-.99.7h-1.69c-1.89 0-2.06 1.54-2.06 1.91 0 1.55 1.97 2.22 1.97 3.8 0 .36-.19.98-1.2.98-.42 0-.87-.12-1.13-.23 0 0-.45-2.76-1.79-3.71 0 0-1.56.76-3.1-.08-1.19-.64-1.74-1.07-2.78-.35s-1.94 1.49-2.05 1.47l-.25-.05v-6.2c.48-.04.51.39.5.5-.04.15-.2.26.14.75q.05.04.06-.02c.02-.12.08-.33.25-.33s.9.55 1.52 1.18c0 0 .36-1.1 1.23-1.43-.5-.07-2.61-.59-2.61-1.51 0-.32-.35-.39-.58-.37-.01-.03-.36-.85-1-.42a.04.04 0 0 0 0 .08c.3.07.42.36.43.69-.62-.2-3.25-.93-6.38-.93-1.51 0-2.9.17-4.02.37v2.15h-.87zm-5.76 21.98c-.23-.1-.38-.24-.38-.55v-1.33c-.38-.13-1.43-.53-3.5-.53-1.74 0-2.28.39-2.3.53l-.47 2.53s.7-.64 3.21-.47c3.02.19 6.16 1.97 6.16.07v-1.12c-2.37 0-2.72.76-2.72.87m18.6-.87v1.12c0 1.9 3.12.12 6.14-.07 2.51-.17 3.21.47 3.21.47l-.46-2.53c-.02-.14-.56-.53-2.3-.53-2.07 0-3.12.4-3.5.53v1.33q-.02.43-.38.55c0-.11-.35-.87-2.72-.87m-7.95-1.15c4.87 0 9.81.26 10.46.99.05.06.07 0 .07-.05V81c0-.76-5.1-1.2-10.53-1.2s-10.52.44-10.52 1.2v2.57c0 .05.02.1.07.05.65-.73 5.59-.99 10.45-.99M20.7 70.47l-.49-.12-.04.06-.57-.25h.51q.07-.02.03-.06l-.44-.43.56.21q.06.01.04-.04l-.38-.55.55.37q.05.02.05-.03l-.27-.65.5.48q.05.02.05-.02l-.14-.65.37.54q.04.04.05-.01l.01-.6.24.55q.02.06.06-.01l.2-.46.01.62-.08.01-.1.74c.73-.17 1.65-1.43 2.35-1.43.84 0 1.14.98 2.68.49.4.57.53 1.08.68 1.77.6.56 1.43.37 1.43-.22 0-.8-.87-.95-.87-1.79 0-.37.26-.9.98-.9.37 0 .93.16 1.24.16.38 0 .46-.28.5-.39.04-.08.14-.06.13 0-.01.09.04.2.04.38 0 .52-.76.6-.86.53-.04-.03-.07.01-.04.03.12.1.09.41-.05.61q-.02.03-.04 0c-.08-.38-.53-.85-.92-.85-.14 0-.43.09-.43.42 0 .47.96.9.96 1.9 0 1.01-1.02 1.26-2.04 1 .04.97 1.48 1.55 1.52.86 0-.05.03-.02.04 0 .22.3.12.52-.16.63.26.12.3.62.38.93.1.45.59.2.61.19q.05-.02.03.03c-.19.47-.67.3-.67.3-.26.17.05.33.28.4q.06.01 0 .04c-.16.07-.53.18-.7-.12-.13.31-.4.3-.5.3-.2 0-.32.2-.27.45q0 .03-.03.01c-.45-.3-.2-.64-.01-.72-.03-.02-.34-.15-.47.2q-.01.06-.04.01c-.19-.51.23-.66.54-.53-.02-.08-.18-.33-.58-.25q-.03 0-.02-.03c.06-.15.26-.39.63-.15.18.11.5-.05.53-.07-.65-1.29-1.9-.2-2.18-1.22 0-.02-.03-.04-.05.02-.07.21-.62.23-.72-.2q0-.04.03-.01c.08.08.25.15.48-.18.15-.21.17-.5.17-.7 0-.78-.78-2.1-2-2.1-.78 0-1.6.3-2.22.75l.57.14a.9.9 0 0 1-1.12.77l.09-.66a1 1 0 0 0-.44.28l-.03-.01c-.1-.38.2-.57.3-.61q.03-.02 0-.02a.6.6 0 0 0-.48.26q-.01.02-.02 0c-.03-.07-.08-.26.05-.4m1.7 4.48c-.21.27-.4.01-.7.01-.27 0-.31.23-.32.32q0 .03-.02 0c-.3-.41.04-.64.24-.67-.03-.03-.33-.23-.53.04q-.02.02-.03 0c0-.53.4-.57.68-.34.05-.2-.08-.33-.3-.34q-.04 0-.02-.03.14-.14.27-.14c.29 0 .33.36.51.36.29 0 .69-.38.73-.42 0-.02-.28-.75-.23-1.2q0-.02-.02 0c-.13.07-.48.07-.48-.36q0-.06.03-.01c.15.2.4-.03.52-.17.13-.15.85-1.19 1.8-1.19.21 0 .48.12.7.27-.33.4 0 .78.24.74q.05 0 0 .03c-1.25.47-1.82.66-1.82 1.71 0 .55.2.86.52.79q.04 0 .01.02a.5.5 0 0 1-.36.19c-.27 0-.31-.15-.45-.15s-.62.28-.62.51q-.01.14.22.31v.02c-.1.02-.48.05-.56-.3m3.32-10.25c-.4-.03-.64.06-.63.19.19.2.44.18.63-.2m-1.45.97c.28-.07.58-.04.62-.03s.04.09-.01.1c-.58.07-.6.33-.98.33-.24 0-.5-.24-.4-.6q.02-.08.05 0c.04.15.27.32.72.2m-7.53 12.86c.38.5.75.03 1.26.03.24 0 .54.08.6.58q0 .04.05 0c.55-.76-.08-1.18-.44-1.23.08-.07.61-.4.96.08q.03.04.05-.01c0-.95-.71-1.02-1.23-.6-.1-.38.19-.62.55-.62q.06 0 .03-.06c-.64-.68-1.11.18-1.22.31-.25.34-1.16-.3-1.52-.67 0 0 .8-.84.8-1.61v-.14q0-.03.03-.02.12.08.3.08c.17 0 .52-.06.52-.71q0-.07-.04-.02-.16.19-.28.17c-.65 0-2-2.16-3.6-2.16-.42 0-1.2.3-1.6.73.19.11.22.35.2.52-.03.27-.24.4-.47.47q-.06.02 0 .06c.81.3 2.28.86 2.61 1.15.4.34.32.98.09 1.87-.22.85-.7.73-.87.7q-.03 0-.02.04c.21.26.47.35.68.35.42 0 .58-.26.8-.26.36 0 1.12.52 1.12.92 0 .22-.17.42-.4.57q-.01.02.01.03c.18.03.9.1 1.03-.55m17.63.55q.04 0 .01-.03c-.22-.15-.4-.35-.4-.57 0-.4.77-.92 1.13-.92.22 0 .38.26.8.26q.34.02.67-.35.02-.04-.02-.03c-.17.02-.64.14-.86-.71-.23-.89-.3-1.53.09-1.87.33-.3 1.8-.85 2.6-1.15q.07-.04 0-.06c-.22-.06-.44-.2-.47-.47-.02-.17.02-.4.21-.52a2.7 2.7 0 0 0-1.6-.73c-1.6 0-2.95 2.16-3.6 2.16q-.13.02-.28-.17-.04-.05-.04.02c0 .65.34.7.53.7q.16.01.3-.07.02-.01.02.02v.14c0 .77.8 1.6.8 1.6-.36.39-1.27 1.02-1.52.68-.11-.13-.59-.99-1.22-.3q-.04.04.03.05c.36 0 .64.24.54.61-.51-.41-1.22-.34-1.22.6q.01.06.05.02c.34-.48.88-.15.96-.08-.36.05-.99.47-.44 1.23q.03.04.05 0c.05-.5.36-.58.6-.58.5 0 .87.46 1.26-.03.14.64.85.58 1.02.55M21.45 66.61c-.05.07-.22 0-.28-.03h-.02c.04.1.2.37.45.37.1 0 .26-.07.3-.14l1.41.43q-.27.37-.31.85c-.38-.23-1.88-1.04-1.88-1.04l-.46.64c-.08.1-.45-.17-.37-.27l.35-.49c-.12-.12-.4-.05-.48-.02q-.02 0 0-.02c.04-.1.22-.4.66-.26 0-.27-.4-.18-.5-.15q-.03 0-.01-.02c.1-.12.43-.38.8-.16l.16-.22-.33-.34.2-.28.23.3 1.73-2.37c.05-.05.39-.15.52-.18.01.13.03.49-.02.55l-1.7 2.39.36.12-.2.27-.42-.2zm6.07-.39c0 .22.13.54.42.44q.03 0 .03.02c-.05.24-.48.46-.68.17-.17.37-.13.72.25.93q.03.01 0 .05a.5.5 0 0 1-.74-.19c-.31.24-.27.73-.08.9 0 0-.86.52-1.64.15-.52-.25-.94-.56-1.6-.51.09-.94.56-1.18 1.42-1.32.68-.1.77-.42.67-.66-.23-.07-.66.15-.66.15-.08-.13.04-.26.04-.26-.05-.06-.07-.23-.07-.23.48 0 .57-.1.57-.1.08-.5-.43-.56-.81-.29-.18-.16-.16-.37-.16-.37q-.08-.06-.08-.15c0-.11.57-.43.71-.47.04-.17.25-.4.85-.34.07-.16-.04-.75-.12-.9q-.02-.08.07-.05c.2.14.42.63.56.64 0 0 .68-.36.72-.34s.12.78.12.78c.1.11.62 0 .85.1q.08.06 0 .08c-.17.03-.72.26-.8.41.3.48.16 1.17.16 1.36" fill="#fff"/></svg>';

  // node_modules/@nldd/design-system/dist/components/navigation/top-navigation-bar/top-navigation-bar.template.js
  function wordmarkContent(component) {
    return b2`
		<div class="top-navigation-bar__wordmark">
			<div class="top-navigation-bar__wordmark-spacer"></div>
			<div class="top-navigation-bar__wordmark-content">
				<p class="top-navigation-bar__wordmark-title">
					${component.logoTitle}
				</p>
				${component.logoSubtitle ? b2`
					<p class="top-navigation-bar__wordmark-subtitle">
						${component.logoSubtitle}
					</p>
				` : A}
				${component.logoSupportingText1 ? b2`
					<p class="top-navigation-bar__wordmark-supporting-text">
						${component.logoSupportingText1}
					</p>
				` : A}
				${component.logoSupportingText2 ? b2`
					<p class="top-navigation-bar__wordmark-supporting-text">
						${component.logoSupportingText2}
					</p>
				` : A}
			</div>
		</div>
	`;
  }
  function template23(component) {
    const safeLogoHref = sanitizeUrl(component.logoHref);
    const safeWebsiteHref = sanitizeUrl(component.websiteHref);
    return b2`
		<div class="top-navigation-bar">
			${!component.noLogo ? b2`<div class="top-navigation-bar__logo-bar">
				${component.logoTitle && safeLogoHref ? b2`
					<a class="top-navigation-bar__logo-and-wordmark"
						href="${safeLogoHref}"
					>
						<div class="top-navigation-bar__logo"
							aria-hidden="true"
						>
							${o6(logo_default)}
						</div>
						${wordmarkContent(component)}
					</a>
				` : safeLogoHref ? b2`
					<a class="top-navigation-bar__logo"
						href="${safeLogoHref}"
						aria-label="${component._t("components.top-navigation-bar.logo-label")}"
					>
						<span aria-hidden="true">${o6(logo_default)}</span>
					</a>
				` : b2`
					<div class="top-navigation-bar__logo"
						role="img"
						aria-label="${component._t("components.top-navigation-bar.logo-label")}"
					>
						${o6(logo_default)}
					</div>
					${component.logoTitle ? wordmarkContent(component) : A}
				`}
			</div>` : A}
			<div class="top-navigation-bar__main-bar">
				${component.websiteTitle ? b2`
					<div class="top-navigation-bar__website-title-bar">
						${safeWebsiteHref ? b2`
							<a class="top-navigation-bar__website-title"
								href="${safeWebsiteHref}"
							>
								${component.websiteTitle}
							</a>
						` : b2`
							<span class="top-navigation-bar__website-title">
								${component.websiteTitle}
							</span>
						`}
					</div>
				` : A}
				<div class="top-navigation-bar__menu-bar">
					<div class="top-navigation-bar__menu-bar-start">
						${component._hasBackButton ? b2`
							<div class="top-navigation-bar__back-button">
								<nldd-menu-bar-item
									icon="chevron-left"
									text="${component._backText}"
									href=${component.backHref || A}
									accessible-label="${component._backText}"
									@click=${component._handleBackClick}
								></nldd-menu-bar-item>
							</div>
						` : A}
						<div class="top-navigation-bar__menu-button">
							<nldd-menu-bar-item
								icon="menu"
								text="${component._menuText}"
								haspopup="dialog"
								@click=${component._onMenuButtonClick}
							></nldd-menu-bar-item>
						</div>
						<div class="top-navigation-bar__global-menu-bar">
							<slot name="global"
								@slotchange=${component._onGlobalSlotChange}
							></slot>
						</div>
					</div>
					<div class="top-navigation-bar__menu-bar-end">
						<div class="top-navigation-bar__utility-menu-bar">
							<slot name="utility"
								@slotchange=${component._onUtilitySlotChange}
							></slot>
						</div>
					</div>
				</div>
			</div>
		</div>
	`;
  }

  // node_modules/@nldd/design-system/dist/components/navigation/top-navigation-bar/top-navigation-bar.i18n.js
  var nlddTopNavigationBarTranslations = {
    "components.top-navigation-bar.global-menu-bar-label": "Hoofdnavigatie",
    "components.top-navigation-bar.back-action": "Terug",
    "components.top-navigation-bar.menu-action": "Menu",
    "components.top-navigation-bar.logo-label": "Rijkswapen - Rijksoverheid",
    "components.top-navigation-bar.utility-menu-bar-label": "Hulplinks",
    "components.top-navigation-bar.menu-sheet-dismiss-action": "Sluit"
  };

  // node_modules/@nldd/design-system/dist/components/navigation/top-navigation-bar/top-navigation-bar.js
  init_icon();
  init_breakpoints();
  var import_meta26 = {};
  var __decorate84 = function(decorators, target, key, desc) {
    var c6 = arguments.length, r6 = c6 < 3 ? target : desc === null ? desc = Object.getOwnPropertyDescriptor(target, key) : desc, d3;
    if (typeof Reflect === "object" && typeof Reflect.decorate === "function") r6 = Reflect.decorate(decorators, target, key, desc);
    else for (var i8 = decorators.length - 1; i8 >= 0; i8--) if (d3 = decorators[i8]) r6 = (c6 < 3 ? d3(r6) : c6 > 3 ? d3(target, key, r6) : d3(target, key)) || r6;
    return c6 > 3 && r6 && Object.defineProperty(target, key, r6), r6;
  };
  var NLDDTopNavigationBar = class NLDDTopNavigationBar2 extends withTranslations(i4, nlddTopNavigationBarTranslations) {
    constructor() {
      super(...arguments);
      this.websiteTitle = "";
      this.noLogo = false;
      this.logoTitle = "";
      this.logoSubtitle = "";
      this.logoSupportingText1 = "";
      this.logoSupportingText2 = "";
      this.logoHref = "";
      this.websiteHref = "";
      this.backHref = "";
      this.backText = "";
      this._globalMenuSheet = null;
      this._globalMenuSheetList = null;
      this._resizeObserver = null;
      this._compactRAF = null;
      this._setupRAF = null;
      this._appliedMenuBarLabels = /* @__PURE__ */ new WeakMap();
      this._handleItemSelect = (event) => {
        const detail = event.detail;
        if (!detail?.item)
          return;
        const globalItems = this._getSlottedItems(this._globalSlot);
        if (!globalItems.includes(detail.item))
          return;
        const selectEvent = new CustomEvent("itemselect", {
          bubbles: true,
          composed: true,
          cancelable: true,
          detail
        });
        this.dispatchEvent(selectEvent);
        if (!selectEvent.defaultPrevented) {
          detail.item.current = true;
          globalItems.forEach((item) => {
            if (item !== detail.item) {
              item.removeAttribute("current");
            }
          });
        }
      };
      this._onGlobalSlotChange = () => {
        this._syncHasGlobalItems();
        this._syncSlottedMenuBarLabels();
        this._syncCompactAttribute();
      };
      this._onUtilitySlotChange = () => {
        this._syncSlottedMenuBarLabels();
        this._syncCompactAttribute();
      };
      this._scheduleCompactUpdate = () => {
        if (this._compactRAF)
          cancelAnimationFrame(this._compactRAF);
        this._compactRAF = requestAnimationFrame(() => {
          this._syncCompactAttribute();
        });
      };
      this._onMenuButtonClick = async () => {
        if (!this._globalMenuSheet) {
          try {
            await this._loadGlobalMenuSheetDependencies();
          } catch (error) {
            if (import_meta26.env?.DEV) {
              console.error("nldd-top-navigation-bar: failed to load menu sheet dependencies", error);
            }
            return;
          }
          if (!this.isConnected)
            return;
          if (this._globalMenuSheet)
            return;
          this._globalMenuSheet = this._createGlobalMenuSheet();
          const menuButtonItem = this._menuButton?.querySelector("nldd-menu-bar-item");
          this._globalMenuSheet.addEventListener("open", () => {
            if (menuButtonItem)
              menuButtonItem.expanded = true;
          });
          this._globalMenuSheet.addEventListener("close", () => {
            if (menuButtonItem)
              menuButtonItem.expanded = false;
          });
        }
        this._syncGlobalMenuSheetItems();
        requestAnimationFrame(() => {
          this._globalMenuSheet?.show();
        });
      };
      this._handleBackClick = (e9) => {
        if (!this.backHref) {
          e9.preventDefault();
          this.dispatchEvent(new CustomEvent("back-click", {
            bubbles: true,
            composed: true
          }));
        }
      };
    }
    willUpdate(changed) {
      super.willUpdate(changed);
      if (changed.has("translations")) {
        this._globalMenuSheet?.setAttribute("accessible-label", this._t("components.top-navigation-bar.menu-action"));
        this._globalMenuSheet?.querySelector("nldd-top-title-bar")?.setAttribute("text", this._menuText);
        this._globalMenuSheet?.querySelector("nldd-top-title-bar")?.setAttribute("dismiss-text", this._t("components.top-navigation-bar.menu-sheet-dismiss-action"));
        this._syncSlottedMenuBarLabels();
      }
    }
    // ## Slotted menu-bars
    /** Items live inside the consumer-supplied <nldd-menu-bar> in the given slot. */
    _getSlottedMenuBar(slot) {
      const assigned = slot?.assignedElements({ flatten: true }) ?? [];
      return assigned.find((el) => el.tagName === "NLDD-MENU-BAR") ?? null;
    }
    _getSlottedItems(slot) {
      const menuBar = this._getSlottedMenuBar(slot);
      if (!menuBar)
        return [];
      return Array.from(menuBar.querySelectorAll(":scope > nldd-menu-bar-item"));
    }
    _syncSlottedMenuBarLabels() {
      const setDefault = (menuBar, labelKey) => {
        if (!menuBar)
          return;
        const current = menuBar.getAttribute("accessible-label");
        const ourLast = this._appliedMenuBarLabels.get(menuBar);
        if (current !== null && current !== ourLast) {
          this._appliedMenuBarLabels.delete(menuBar);
          return;
        }
        const next = this._t(labelKey);
        menuBar.setAttribute("accessible-label", next);
        this._appliedMenuBarLabels.set(menuBar, next);
      };
      setDefault(this._getSlottedMenuBar(this._globalSlot), "components.top-navigation-bar.global-menu-bar-label");
      setDefault(this._getSlottedMenuBar(this._utilitySlot), "components.top-navigation-bar.utility-menu-bar-label");
    }
    // ## Computed properties
    /** @internal Used by template */
    get _hasBackButton() {
      return Boolean(this.backHref || this.backText);
    }
    /** @internal Used by template */
    get _backText() {
      return this.backText || this._t("components.top-navigation-bar.back-action");
    }
    /** @internal Used by template */
    get _menuText() {
      return this._t("components.top-navigation-bar.menu-action");
    }
    // ## Lifecycle
    connectedCallback() {
      super.connectedCallback();
      this.addEventListener("select", this._handleItemSelect);
    }
    disconnectedCallback() {
      super.disconnectedCallback();
      this.removeEventListener("select", this._handleItemSelect);
      this._cleanupCompactDetection();
      this._globalMenuSheet?.remove();
      this._globalMenuSheet = null;
      this._globalMenuSheetList = null;
    }
    firstUpdated() {
      this._syncHasGlobalItems();
      this._setupCompactDetection();
    }
    // ## Compact attribute propagation
    _setupCompactDetection() {
      this._cleanupCompactDetection();
      this._setupRAF = requestAnimationFrame(() => {
        this._setupRAF = null;
        if (!this.isConnected)
          return;
        this._resizeObserver = new ResizeObserver(() => {
          this._scheduleCompactUpdate();
        });
        this._resizeObserver.observe(this);
        this._scheduleCompactUpdate();
      });
    }
    _cleanupCompactDetection() {
      if (this._setupRAF) {
        cancelAnimationFrame(this._setupRAF);
        this._setupRAF = null;
      }
      if (this._compactRAF) {
        cancelAnimationFrame(this._compactRAF);
        this._compactRAF = null;
      }
      if (this._resizeObserver) {
        this._resizeObserver.disconnect();
        this._resizeObserver = null;
      }
    }
    /** Update has-global-items class on host based on global slot content. */
    _syncHasGlobalItems() {
      this.classList.toggle("has-global-items", this._getSlottedItems(this._globalSlot).length > 0);
    }
    /** Propagate compact to menu-bars and internal items when container is sm. */
    _syncCompactAttribute() {
      const isCompact = this._isSmBreakpoint();
      const slottedMenuBars = [
        this._getSlottedMenuBar(this._globalSlot),
        this._getSlottedMenuBar(this._utilitySlot)
      ].filter((mb) => mb !== null);
      for (const menuBar of slottedMenuBars) {
        menuBar.toggleAttribute("compact", isCompact);
        menuBar.requestOverflowUpdate();
      }
      const internalItems = this.shadowRoot?.querySelectorAll("nldd-menu-bar-item") ?? [];
      for (const item of internalItems) {
        item.toggleAttribute("compact", isCompact);
      }
      this._syncHasGlobalItems();
    }
    /** Check if the container is at the sm breakpoint (<= smMax). */
    _isSmBreakpoint() {
      const container = this.shadowRoot?.querySelector(".top-navigation-bar");
      if (!container)
        return false;
      return container.clientWidth <= parseInt(breakpoints.smMax);
    }
    // ## Menu sheet
    async _loadGlobalMenuSheetDependencies() {
      await Promise.all([
        Promise.resolve().then(() => (init_sheet(), sheet_exports)),
        Promise.resolve().then(() => (init_page(), page_exports)),
        Promise.resolve().then(() => (init_simple_section(), simple_section_exports)),
        Promise.resolve().then(() => (init_top_title_bar(), top_title_bar_exports)),
        Promise.resolve().then(() => (init_list(), list_exports)),
        Promise.resolve().then(() => (init_list_item(), list_item_exports)),
        Promise.resolve().then(() => (init_text_cell(), text_cell_exports))
      ]);
    }
    _createGlobalMenuSheet() {
      const sheet = document.createElement("nldd-sheet");
      sheet.setAttribute("placement", "left");
      sheet.setAttribute("accessible-label", this._t("components.top-navigation-bar.menu-action"));
      const page = document.createElement("nldd-page");
      page.setAttribute("sticky-header", "");
      const titleBar = document.createElement("nldd-top-title-bar");
      titleBar.setAttribute("slot", "header");
      titleBar.setAttribute("text", this._menuText);
      titleBar.setAttribute("dismiss-text", this._t("components.top-navigation-bar.menu-sheet-dismiss-action"));
      page.appendChild(titleBar);
      const section = document.createElement("nldd-simple-section");
      this._globalMenuSheetList = document.createElement("nldd-list");
      this._globalMenuSheetList.setAttribute("variant", "simple");
      this._globalMenuSheetList.setAttribute("no-dividers", "");
      section.appendChild(this._globalMenuSheetList);
      page.appendChild(section);
      sheet.appendChild(page);
      document.body.appendChild(sheet);
      return sheet;
    }
    _syncGlobalMenuSheetItems() {
      if (!this._globalMenuSheetList)
        return;
      this._globalMenuSheetList.replaceChildren();
      const items = this._getSlottedItems(this._globalSlot);
      for (const item of items) {
        const listItem = document.createElement("nldd-list-item");
        const safeHref = sanitizeUrl(item.href);
        if (safeHref) {
          listItem.setAttribute("type", "link");
          listItem.setAttribute("href", safeHref);
        } else {
          listItem.setAttribute("type", "button");
        }
        if (item.current)
          listItem.setAttribute("selected", "");
        const textCell = document.createElement("nldd-text-cell");
        textCell.setAttribute("text", item.text);
        listItem.appendChild(textCell);
        listItem.addEventListener("click", () => {
          if (!safeHref)
            item.click();
          this._globalMenuSheet?.hide();
        });
        this._globalMenuSheetList.appendChild(listItem);
      }
    }
    // ## Render
    render() {
      return template23(this);
    }
  };
  NLDDTopNavigationBar.styles = topNavigationBarStyles;
  __decorate84([
    n4({ type: String, attribute: "website-title" })
  ], NLDDTopNavigationBar.prototype, "websiteTitle", void 0);
  __decorate84([
    n4({ type: Boolean, attribute: "no-logo", reflect: true })
  ], NLDDTopNavigationBar.prototype, "noLogo", void 0);
  __decorate84([
    n4({ type: String, attribute: "logo-title" })
  ], NLDDTopNavigationBar.prototype, "logoTitle", void 0);
  __decorate84([
    n4({ type: String, attribute: "logo-subtitle" })
  ], NLDDTopNavigationBar.prototype, "logoSubtitle", void 0);
  __decorate84([
    n4({ type: String, attribute: "logo-supporting-text-1" })
  ], NLDDTopNavigationBar.prototype, "logoSupportingText1", void 0);
  __decorate84([
    n4({ type: String, attribute: "logo-supporting-text-2" })
  ], NLDDTopNavigationBar.prototype, "logoSupportingText2", void 0);
  __decorate84([
    n4({ type: String, attribute: "logo-href" })
  ], NLDDTopNavigationBar.prototype, "logoHref", void 0);
  __decorate84([
    n4({ type: String, attribute: "website-href" })
  ], NLDDTopNavigationBar.prototype, "websiteHref", void 0);
  __decorate84([
    n4({ type: String, attribute: "back-href" })
  ], NLDDTopNavigationBar.prototype, "backHref", void 0);
  __decorate84([
    n4({ type: String, attribute: "back-text" })
  ], NLDDTopNavigationBar.prototype, "backText", void 0);
  __decorate84([
    e5(".top-navigation-bar__menu-button")
  ], NLDDTopNavigationBar.prototype, "_menuButton", void 0);
  __decorate84([
    e5('slot[name="global"]')
  ], NLDDTopNavigationBar.prototype, "_globalSlot", void 0);
  __decorate84([
    e5('slot[name="utility"]')
  ], NLDDTopNavigationBar.prototype, "_utilitySlot", void 0);
  NLDDTopNavigationBar = __decorate84([
    t3("nldd-top-navigation-bar")
  ], NLDDTopNavigationBar);

  // node_modules/@nldd/design-system/dist/components/index.js
  init_top_title_bar();

  // node_modules/@nldd/design-system/dist/components/navigation/tab-bar/tab-bar.js
  init_lit();
  init_decorators();

  // node_modules/@nldd/design-system/dist/components/navigation/tab-bar/tab-bar.styles.js
  init_lit();
  init_slotted_reset();
  var tabBarStyles = i`


	/* # Host */

	:host {
		--_gap: var(--primitives-space-1);
		--_z-index-selected: 1;
		--_z-index-focus: 2;

		${inheritedTextReset}
		display: inline-block;
		position: relative;
		isolation: isolate;
		user-select: none;
		-webkit-tap-highlight-color: transparent;
	}

	:host([hidden]) {
		display: none;
	}

	:host([centered]) {
		display: block;
		width: 100%;
	}


	/* # Block */

	.tab-bar {
		display: flex;
		flex-direction: row;
		align-items: center;
		justify-content: flex-start;
	}

	:host([centered]) .tab-bar {
		justify-content: center;
	}

	.tab-bar__items {
		display: flex;
		position: relative;
		border-radius: var(--semantics-controls-md-corner-radius);
		background-color: var(--semantics-buttons-neutral-tinted-background-color);
		flex-direction: row;
		align-items: center;
		justify-content: center;
		gap: var(--_gap);
	}

	:host([size="lg"]) .tab-bar__items {
		border-radius: var(--semantics-controls-lg-corner-radius);
	}

	.tab-bar__items::after {
		content: '';
		position: absolute;
		inset: 0;
		border-radius: inherit;
		box-shadow: inset 0 0 0 var(--primitives-border-width-thin) var(--semantics-buttons-neutral-tinted-highlight-border-color);
		pointer-events: none;
	}


	/* # Focus */

	::slotted(nldd-tab-bar-item[selected]) {
		position: relative;
		z-index: var(--_z-index-selected);
	}

	::slotted(nldd-tab-bar-item:focus-within) {
		position: relative;
		z-index: var(--_z-index-focus);
	}
`;
  var tabBarItemStyles = i`


	/* # Host */

	:host {
		--_lg-item-padding: var(--primitives-space-8);
		--_highlight-border-color: transparent;
		--_z-index-content: 1;

		${inheritedTextReset}
		display: inline-block;
		position: relative;
		-webkit-tap-highlight-color: transparent;
	}

	:host([hidden]) {
		display: none;
	}


	/* # Block */

	.tab-bar__item {
		box-sizing: border-box;
		display: flex;
		position: relative;
		margin: 0;
		border: none;
		border-radius: var(--semantics-controls-md-corner-radius);
		background: none;
		padding: 0;
		align-items: center;
		justify-content: center;
		color: var(--semantics-buttons-neutral-tinted-primary-content-color);
		font: var(--semantics-buttons-md-primary-text-font);
		text-decoration: none;
		appearance: none;
	}

	a.tab-bar__item {
		cursor: var(--semantics-controls-link-cursor);
	}

	:host([variant="icon-and-text"]:not([size="lg"])) .tab-bar__item {
		height: var(--semantics-controls-md-min-size);
		padding: var(--primitives-space-8) var(--primitives-space-12);
		gap: var(--semantics-buttons-md-gap);
	}

	:host([variant="text"]:not([size="lg"])) .tab-bar__item {
		height: var(--semantics-controls-md-min-size);
		padding: var(--primitives-space-8) var(--primitives-space-12);
	}

	:host([variant="icon"]:not([size="lg"])) .tab-bar__item {
		width: var(--semantics-controls-md-min-size);
		height: var(--semantics-controls-md-min-size);
		padding: 0;
	}

	:host([variant="icon-and-text"][size="lg"]) .tab-bar__item {
		height: var(--semantics-controls-lg-min-size);
		padding: var(--_lg-item-padding);
		flex-direction: column;
	}

	:host([variant="text"][size="lg"]) .tab-bar__item {
		min-height: var(--semantics-controls-lg-min-size);
		padding: var(--semantics-controls-lg-block-padding) var(--primitives-space-16);
		font: var(--semantics-buttons-lg-primary-text-font);
	}

	:host([variant="icon"][size="lg"]) .tab-bar__item {
		width: var(--semantics-controls-lg-min-size);
		height: var(--semantics-controls-lg-min-size);
		padding: var(--_lg-item-padding);
	}

	@media (hover: hover) {
		.tab-bar__item:hover {
			background-color: var(--semantics-buttons-neutral-tinted-is-hovered-background-color);
		}

		:host([selected]) .tab-bar__item:hover {
			--_highlight-border-color: var(--semantics-buttons-neutral-tinted-is-selected-is-hovered-highlight-border-color);

			background-color: var(--semantics-buttons-neutral-tinted-is-selected-is-hovered-background-color);
			color: var(--semantics-buttons-neutral-tinted-is-selected-is-hovered-primary-content-color);
		}
	}

	.tab-bar__item:active {
		background-color: var(--semantics-buttons-neutral-tinted-is-active-background-color);
		color: var(--semantics-buttons-neutral-tinted-is-active-primary-content-color);
	}

	:host([selected]) .tab-bar__item {
		--_highlight-border-color: var(--semantics-buttons-neutral-tinted-is-selected-highlight-border-color);

		background-color: var(--semantics-buttons-neutral-tinted-is-selected-background-color);
		color: var(--semantics-buttons-neutral-tinted-is-selected-primary-content-color);
		box-shadow: inset 0 0 0 var(--primitives-border-width-thin) var(--_highlight-border-color);
	}

	:host([selected]) .tab-bar__item:active {
		--_highlight-border-color: var(--semantics-buttons-neutral-tinted-is-selected-is-active-highlight-border-color);

		background-color: var(--semantics-buttons-neutral-tinted-is-selected-is-active-background-color);
		color: var(--semantics-buttons-neutral-tinted-is-selected-is-active-primary-content-color);
	}

	@media (forced-colors: active) {
		:host([selected]) .tab-bar__item {
			background-color: Highlight;
		}
	}

	.tab-bar__item:focus-visible {
		outline: var(--semantics-focus-ring-outline);
		outline-offset: var(--semantics-focus-ring-outline-offset);
		box-shadow: var(--semantics-focus-ring-box-shadow);
	}

	:host([selected]) .tab-bar__item:focus-visible {
		box-shadow: var(--semantics-focus-ring-box-shadow), inset 0 0 0 var(--primitives-border-width-thin) var(--_highlight-border-color);
	}


	/* # Elements */

	.tab-bar__item-icon {
		display: flex;
		position: relative;
		z-index: var(--_z-index-content);
		width: var(--semantics-buttons-md-icon-size);
		height: var(--semantics-buttons-md-icon-size);
		flex-shrink: 0;
		align-items: center;
		justify-content: center;
	}

	:host([variant="icon"]:not([size="lg"])) .tab-bar__item-icon {
		width: var(--semantics-buttons-md-icon-only-icon-size);
		height: var(--semantics-buttons-md-icon-only-icon-size);
	}

	:host([variant="icon-and-text"][size="lg"]) .tab-bar__item-icon {
		width: var(--semantics-buttons-md-icon-only-icon-size);
		height: var(--semantics-buttons-md-icon-only-icon-size);
	}

	:host([variant="icon"][size="lg"]) .tab-bar__item-icon {
		width: var(--semantics-buttons-lg-icon-only-icon-size);
		height: var(--semantics-buttons-lg-icon-only-icon-size);
	}

	:host([variant="text"]) .tab-bar__item-icon {
		display: none;
	}

	::slotted([slot="icon"]) {
		display: block;
		width: 100%;
		height: 100%;
	}

	.tab-bar__item-text {
		position: relative;
		z-index: var(--_z-index-content);
	}

	:host([variant="icon-and-text"][size="lg"]) .tab-bar__item-text {
		font: var(--primitives-font-body-xxs-medium-flat);
	}

	:host([variant="icon"]) .tab-bar__item-text {
		display: none;
	}
`;

  // node_modules/@nldd/design-system/dist/components/navigation/tab-bar/tab-bar.template.js
  init_lit();
  init_tooltip();
  init_icon();
  function tabBarTemplate(component) {
    const label = component.accessibleLabel || "Tabs";
    const isNavigation = component.navigation;
    const itemsContainer = b2`
		<div class="tab-bar__items"
			role=${isNavigation ? A : "tablist"}
			aria-label=${isNavigation ? A : label}
		>
			<slot @slotchange=${component._onSlotChange}></slot>
		</div>
	`;
    if (isNavigation) {
      return b2`
			<nav class="tab-bar" aria-label=${label}>
				${itemsContainer}
			</nav>
		`;
    }
    return b2`
		<div class="tab-bar">
			${itemsContainer}
		</div>
	`;
  }
  function tabBarItemTemplate(component) {
    const safeHref = sanitizeUrl(component.href);
    const isLink = Boolean(safeHref);
    const isNavigation = component._navigation;
    const tabindex = component.selected || component._isFallbackFocusable ? "0" : "-1";
    const isIconVariant = component._effectiveVariant === "icon";
    const iconLabel = isIconVariant ? component.text || A : A;
    const showPlaceholder = isIconVariant || component._effectiveVariant === "icon-and-text";
    const content = b2`
		<span class="tab-bar__item-icon" aria-hidden="true">
			${component.icon ? b2`<nldd-icon name=${component.icon}></nldd-icon>` : b2`<slot name="icon" @slotchange=${component._onIconSlotChange}>${showPlaceholder ? b2`<nldd-icon name="icon-placeholder"></nldd-icon>` : A}</slot>`}
		</span>
		<span class="tab-bar__item-text">
			${component.text}
		</span>
	`;
    let result;
    if (isLink) {
      result = b2`
			<a class="tab-bar__item"
				href=${safeHref}
				role=${isNavigation ? A : "tab"}
				aria-current=${isNavigation && component.selected ? "page" : A}
				aria-selected=${!isNavigation ? component.selected ? "true" : "false" : A}
				aria-label=${iconLabel}
				tabindex=${tabindex}
				@click=${component._handleClick}
			>${content}</a>
		`;
    } else {
      result = b2`
			<button class="tab-bar__item"
				type="button"
				role="tab"
				aria-selected=${component.selected ? "true" : "false"}
				aria-label=${iconLabel}
				tabindex=${tabindex}
				@click=${component._handleClick}
			>${content}</button>
		`;
    }
    if (isIconVariant && component.text) {
      return b2`<nldd-tooltip text=${component.text}>${result}</nldd-tooltip>`;
    }
    return result;
  }

  // node_modules/@nldd/design-system/dist/components/navigation/tab-bar/tab-bar.js
  var import_meta27 = {};
  var __decorate85 = function(decorators, target, key, desc) {
    var c6 = arguments.length, r6 = c6 < 3 ? target : desc === null ? desc = Object.getOwnPropertyDescriptor(target, key) : desc, d3;
    if (typeof Reflect === "object" && typeof Reflect.decorate === "function") r6 = Reflect.decorate(decorators, target, key, desc);
    else for (var i8 = decorators.length - 1; i8 >= 0; i8--) if (d3 = decorators[i8]) r6 = (c6 < 3 ? d3(r6) : c6 > 3 ? d3(target, key, r6) : d3(target, key)) || r6;
    return c6 > 3 && r6 && Object.defineProperty(target, key, r6), r6;
  };
  var NLDDTabBarItem = class NLDDTabBarItem2 extends i4 {
    constructor() {
      super(...arguments);
      this.selected = false;
      this.href = "";
      this._groupVariant = "";
      this._authorVariant = "";
      this.text = "";
      this.icon = "";
      this.size = "md";
      this._hasIconSlot = false;
      this._navigation = false;
      this._isFallbackFocusable = false;
    }
    get _effectiveVariant() {
      if (this._authorVariant)
        return this._authorVariant;
      if (this._groupVariant)
        return this._groupVariant;
      const hasIcon = Boolean(this.icon) || this._hasIconSlot;
      if (this.text && hasIcon)
        return "icon-and-text";
      if (this.text)
        return "text";
      return "icon";
    }
    connectedCallback() {
      super.connectedCallback();
      this.setAttribute("role", "none");
      const attr = this.getAttribute("variant");
      if (attr === "text" || attr === "icon" || attr === "icon-and-text") {
        this._authorVariant = attr;
      }
    }
    updated() {
      this.setAttribute("variant", this._effectiveVariant);
    }
    focus(options) {
      this.shadowRoot?.querySelector(".tab-bar__item")?.focus(options);
    }
    _onIconSlotChange(e9) {
      const slot = e9.target;
      this._hasIconSlot = slot.assignedElements({ flatten: true }).length > 0;
    }
    _handleClick(event) {
      if (!sanitizeUrl(this.href)) {
        event.preventDefault();
      }
      this.dispatchEvent(new CustomEvent("select", {
        bubbles: true,
        composed: true,
        detail: { item: this }
      }));
    }
    render() {
      return tabBarItemTemplate(this);
    }
  };
  NLDDTabBarItem.styles = tabBarItemStyles;
  __decorate85([
    n4({ type: Boolean, reflect: true })
  ], NLDDTabBarItem.prototype, "selected", void 0);
  __decorate85([
    n4({ type: String })
  ], NLDDTabBarItem.prototype, "href", void 0);
  __decorate85([
    n4({ type: String })
  ], NLDDTabBarItem.prototype, "_groupVariant", void 0);
  __decorate85([
    n4({ type: String })
  ], NLDDTabBarItem.prototype, "text", void 0);
  __decorate85([
    n4({ type: String, reflect: true })
  ], NLDDTabBarItem.prototype, "icon", void 0);
  __decorate85([
    n4({ type: String, reflect: true })
  ], NLDDTabBarItem.prototype, "size", void 0);
  __decorate85([
    r5()
  ], NLDDTabBarItem.prototype, "_hasIconSlot", void 0);
  __decorate85([
    r5()
  ], NLDDTabBarItem.prototype, "_navigation", void 0);
  __decorate85([
    r5()
  ], NLDDTabBarItem.prototype, "_isFallbackFocusable", void 0);
  NLDDTabBarItem = __decorate85([
    t3("nldd-tab-bar-item")
  ], NLDDTabBarItem);
  var NLDDTabBar = class NLDDTabBar2 extends i4 {
    constructor() {
      super(...arguments);
      this.centered = false;
      this.variant = "";
      this.size = "md";
      this.navigation = false;
      this.accessibleLabel = "";
      this._hasCustomLabel = false;
      this._handleItemSelect = (event) => {
        event.stopPropagation();
        const items = this._getItems();
        items.forEach((item) => {
          item.selected = item === event.detail.item;
        });
        this.dispatchEvent(new CustomEvent("tabchange", {
          bubbles: true,
          composed: true,
          detail: event.detail
        }));
      };
      this._handleKeyDown = (event) => {
        const items = this._getItems();
        if (items.length === 0)
          return;
        const currentIndex = items.findIndex((item) => item === event.target || item.contains(event.target));
        let newIndex = -1;
        switch (event.key) {
          case "ArrowLeft":
            event.preventDefault();
            newIndex = currentIndex > 0 ? currentIndex - 1 : items.length - 1;
            break;
          case "ArrowRight":
            event.preventDefault();
            newIndex = currentIndex < items.length - 1 ? currentIndex + 1 : 0;
            break;
          case "Home":
            event.preventDefault();
            newIndex = 0;
            break;
          case "End":
            event.preventDefault();
            newIndex = items.length - 1;
            break;
          default:
            return;
        }
        if (newIndex >= 0 && newIndex < items.length) {
          items[newIndex].focus();
          if (!this.navigation) {
            items.forEach((item) => {
              item.selected = item === items[newIndex];
            });
            this.dispatchEvent(new CustomEvent("tabchange", {
              bubbles: true,
              composed: true,
              detail: { item: items[newIndex] }
            }));
          }
        }
      };
    }
    connectedCallback() {
      super.connectedCallback();
      this.addEventListener("select", this._handleItemSelect);
      this.addEventListener("keydown", this._handleKeyDown);
    }
    disconnectedCallback() {
      super.disconnectedCallback();
      this.removeEventListener("select", this._handleItemSelect);
      this.removeEventListener("keydown", this._handleKeyDown);
    }
    firstUpdated() {
      this._hasCustomLabel = Boolean(this.accessibleLabel);
      import_meta27.env?.DEV && !this._hasCustomLabel && console.warn('<nldd-tab-bar>: No accessible-label provided. Add an accessible-label attribute for a meaningful navigation landmark name. Falling back to "Tabs".');
      this._syncItems();
    }
    updated(changedProperties) {
      if (changedProperties.has("variant") || changedProperties.has("size") || changedProperties.has("navigation")) {
        this._syncItems();
      }
    }
    _getItems() {
      const slot = this.shadowRoot?.querySelector("slot");
      if (!slot)
        return [];
      return slot.assignedElements().filter((el) => el.tagName.toLowerCase() === "nldd-tab-bar-item");
    }
    _syncItems() {
      const items = this._getItems();
      items.forEach((item) => {
        item._groupVariant = this.variant;
        item.size = this.size;
        item._navigation = this.navigation;
      });
      const hasSelected = items.some((item) => item.selected);
      const firstItem = items[0] ?? null;
      items.forEach((item) => {
        item._isFallbackFocusable = !hasSelected && item === firstItem;
      });
    }
    _onSlotChange() {
      this._syncItems();
    }
    render() {
      return tabBarTemplate(this);
    }
  };
  NLDDTabBar.styles = tabBarStyles;
  __decorate85([
    n4({ type: Boolean, reflect: true })
  ], NLDDTabBar.prototype, "centered", void 0);
  __decorate85([
    n4({ type: String, reflect: true })
  ], NLDDTabBar.prototype, "variant", void 0);
  __decorate85([
    n4({ type: String, reflect: true })
  ], NLDDTabBar.prototype, "size", void 0);
  __decorate85([
    n4({ type: Boolean, reflect: true })
  ], NLDDTabBar.prototype, "navigation", void 0);
  __decorate85([
    n4({ type: String, attribute: "accessible-label" })
  ], NLDDTabBar.prototype, "accessibleLabel", void 0);
  NLDDTabBar = __decorate85([
    t3("nldd-tab-bar")
  ], NLDDTabBar);

  // node_modules/@nldd/design-system/dist/components/navigation/document-tab-bar/document-tab-bar.js
  init_lit();
  init_decorators();

  // node_modules/@nldd/design-system/dist/components/navigation/document-tab-bar/document-tab-bar.styles.js
  init_lit();
  init_slotted_reset();
  var documentTabBarStyles = i`


	/* # Host */

	:host {
		--_drag-clone-top: 0px;
		--_drag-clone-left: 0px;
		--_drag-clone-width: 0px;
		--_drag-clone-height: 0px;
		--_drag-clone-opacity: 0.95;
		--_drag-clone-z-index: 100;
		--_short-text-threshold: 200px;
		--_item-min-width: 100px;
		--_overflow-button-reserve: 52px; /* Used for overflowButtonReserve. Overflow button width + spacing */
		--_dismiss-button-is-hovered-background-color: light-dark(var(--primitives-color-neutral-150), var(--primitives-color-neutral-250));
		--_dismiss-button-is-active-background-color: light-dark(var(--primitives-color-neutral-200), var(--primitives-color-neutral-300));
		--_dismiss-button-is-selected-is-hovered-background-color: light-dark(var(--primitives-color-accent-650), var(--primitives-color-accent-600));
		--_dismiss-button-is-selected-is-active-background-color: light-dark(var(--primitives-color-accent-600), var(--primitives-color-accent-550));

		${inheritedTextReset}
		display: block;
		position: relative;
		isolation: isolate;
		user-select: none;
		-webkit-tap-highlight-color: transparent;
	}

	:host([hidden]) {
		display: none;
	}


	/* # Block */

	.document-tab-bar {
		display: flex;
		flex-direction: row;
		align-items: center;
		gap: var(--primitives-space-8);
	}

	.document-tab-bar__items {
		display: flex;
		min-width: 0;
		flex-direction: row;
		align-items: center;
		gap: var(--primitives-space-8);
		flex-grow: 1;
		flex-shrink: 1;
		flex-basis: 0;
	}

	::slotted(nldd-document-tab-bar-item) {
		min-width: var(--_item-min-width);
		flex-grow: 1;
		flex-shrink: 1;
		flex-basis: 0;
	}

	::slotted(nldd-document-tab-bar-item[hidden]) {
		display: none;
	}

	.document-tab-bar__items.is-measuring slot {
		flex-grow: 0;
	}

	.document-tab-bar__overflow {
		flex-grow: 0;
		flex-shrink: 0;
	}

	.document-tab-bar__overflow.is-hidden {
		display: none;
	}

	.document-tab-bar__end {
		display: flex;
		flex-direction: row;
		align-items: center;
		gap: var(--primitives-space-8);
		flex-grow: 0;
		flex-shrink: 0;
	}

	.document-tab-bar__end[hidden] {
		display: none;
	}


	/* # Focus */

	::slotted(nldd-document-tab-bar-item:focus-within) {
		position: relative;
		z-index: 4;
	}


	/* # Drag states */

	::slotted(nldd-document-tab-bar-item.is-dragging) {
		display: none;
	}


	/* # Drag placeholder */

	::slotted(.nldd-document-tab-bar-drag-placeholder) {
		box-sizing: border-box;
		opacity: var(--semantics-controls-is-dragging-opacity);
		border-radius: var(--semantics-controls-md-corner-radius);
		background-color: var(--semantics-buttons-neutral-tinted-background-color);
		pointer-events: none;
		height: var(--semantics-controls-md-min-size);
		min-width: var(--_item-min-width);
		flex-grow: 1;
		flex-shrink: 1;
		flex-basis: 0;
	}


	/* # Drag clone */

	.document-tab-bar__drag-clone {
		position: absolute;
		top: var(--_drag-clone-top);
		left: var(--_drag-clone-left);
		display: flex;
		opacity: var(--_drag-clone-opacity);
		z-index: var(--_drag-clone-z-index);
		border-radius: var(--semantics-controls-md-corner-radius);
		background: var(--semantics-buttons-neutral-tinted-background-color);
		pointer-events: none;
		cursor: grabbing;
		width: var(--_drag-clone-width);
		height: var(--_drag-clone-height);
		overflow: hidden;
		flex-direction: row;
		align-items: stretch;
	}

	.document-tab-bar__drag-clone .document-tab-bar__item {
		box-sizing: border-box;
		position: relative;
		width: 100%;
		height: 100%;
	}

	.document-tab-bar__drag-clone .document-tab-bar__item-tab {
		display: flex;
		box-sizing: border-box;
		border-radius: var(--semantics-controls-md-corner-radius);
		width: 100%;
		height: 100%;
		min-width: 0;
		overflow: hidden;
		padding-block: var(--primitives-space-6);
		padding-inline-start: var(--primitives-space-10);
		padding-inline-end: calc(var(--semantics-controls-sm-min-size) + var(--primitives-space-6) * 2);
		flex-direction: column;
		justify-content: center;
	}

	a.document-tab-bar__item-tab {
		cursor: var(--semantics-controls-link-cursor);
	}

	.document-tab-bar__drag-clone .document-tab-bar__item-text {
		overflow: hidden;
		font: var(--components-document-tab-bar-tab-title-font);
		color: var(--semantics-buttons-neutral-tinted-primary-content-color);
		text-overflow: ellipsis;
		white-space: nowrap;
	}

	.document-tab-bar__drag-clone.is-selected .document-tab-bar__item-tab {
		background-color: var(--semantics-buttons-neutral-tinted-is-selected-background-color);
	}

	.document-tab-bar__drag-clone.is-selected .document-tab-bar__item-text,
	.document-tab-bar__drag-clone.is-selected .document-tab-bar__item-supporting-text {
		color: var(--semantics-buttons-neutral-tinted-is-selected-primary-content-color);
	}

	.document-tab-bar__drag-clone .document-tab-bar__item-supporting-text {
		overflow: hidden;
		font: var(--primitives-font-body-xs-regular-flat);
		color: var(--semantics-content-secondary-color);
		text-overflow: ellipsis;
		white-space: nowrap;
	}


	/* # Announcers */

	.document-tab-bar__polite-announcer,
	.document-tab-bar__assertive-announcer {
		position: absolute;
		margin: -1px;
		border: 0;
		width: 1px;
		height: 1px;
		overflow: hidden;
		padding: 0;
		white-space: nowrap;
		clip-path: inset(50%);
	}

`;
  var documentTabBarItemStyles = i`


	/* # Host */

	:host {
		--_highlight-border-color: var(--semantics-buttons-neutral-tinted-highlight-border-color);
		--_is-hovered-highlight-border-color: var(--semantics-buttons-neutral-tinted-is-hovered-highlight-border-color);
		--_is-active-highlight-border-color: var(--semantics-buttons-neutral-tinted-is-active-highlight-border-color);
		--_is-selected-highlight-border-color: var(--semantics-buttons-neutral-tinted-is-selected-highlight-border-color);
		--_is-selected-is-hovered-highlight-border-color: var(--semantics-buttons-neutral-tinted-is-selected-is-hovered-highlight-border-color);
		--_is-selected-is-active-highlight-border-color: var(--semantics-buttons-neutral-tinted-is-selected-is-active-highlight-border-color);

		--_secondary-content-color: var(--semantics-buttons-neutral-tinted-secondary-content-color);

		${inheritedTextReset}
		container-type: inline-size;
		display: block;
		min-width: 0;
		touch-action: pan-y;
		-webkit-tap-highlight-color: transparent;
	}

	:host([hidden]) {
		display: none;
	}


	/* # Item */

	.document-tab-bar__item {
		position: relative;
		box-sizing: border-box;
		height: var(--semantics-controls-md-min-size);
		width: 100%;
	}


	/* # Item tab */

	.document-tab-bar__item-tab {
		display: flex;
		box-sizing: border-box;
		position: relative;
		margin: 0;
		border: none;
		border-radius: var(--semantics-controls-md-corner-radius);
		background-color: var(--semantics-buttons-neutral-tinted-background-color);
		box-shadow: inset 0 0 0 var(--primitives-border-width-thin) var(--_highlight-border-color);
		width: 100%;
		height: 100%;
		min-width: 0;
		overflow: hidden;
		padding-block: var(--primitives-space-6);
		padding-inline-start: var(--primitives-space-10);
		padding-inline-end: calc(var(--semantics-controls-sm-min-size) + var(--primitives-space-6));
		text-align: left;
		flex-direction: column;
		justify-content: center;
		text-decoration: none;
		appearance: none;
	}

	@media (hover: hover) {
		.document-tab-bar__item-tab:hover {
			background-color: var(--semantics-buttons-neutral-tinted-is-hovered-background-color);
			--_highlight-border-color: var(--_is-hovered-highlight-border-color);
			--_secondary-content-color: var(--semantics-buttons-neutral-tinted-is-hovered-secondary-content-color);
		}
	}

	.document-tab-bar__item-tab:active {
		background-color: var(--semantics-buttons-neutral-tinted-is-active-background-color);
		--_highlight-border-color: var(--_is-active-highlight-border-color);
		--_secondary-content-color: var(--semantics-buttons-neutral-tinted-is-active-secondary-content-color);
	}

	:host([selected]) .document-tab-bar__item-tab {
		background-color: var(--semantics-buttons-neutral-tinted-is-selected-background-color);
		--_highlight-border-color: var(--_is-selected-highlight-border-color);
		--_secondary-content-color: var(--semantics-buttons-neutral-tinted-is-selected-secondary-content-color);
	}

	@media (hover: hover) {
		:host([selected]) .document-tab-bar__item-tab:hover {
			background-color: var(--semantics-buttons-neutral-tinted-is-selected-background-color);
			--_highlight-border-color: var(--_is-selected-is-hovered-highlight-border-color);
			--_secondary-content-color: var(--semantics-buttons-neutral-tinted-is-selected-is-hovered-secondary-content-color);
		}
	}

	:host([selected]) .document-tab-bar__item-tab:active {
		--_highlight-border-color: var(--_is-selected-is-active-highlight-border-color);
		--_secondary-content-color: var(--semantics-buttons-neutral-tinted-is-selected-is-active-secondary-content-color);
	}

	/* ## Focus */

	.document-tab-bar__item-tab:focus-visible {
		outline: var(--semantics-focus-ring-outline);
		outline-offset: var(--semantics-focus-ring-outline-offset);
		box-shadow: var(--semantics-focus-ring-box-shadow), inset 0 0 0 var(--primitives-border-width-thin) var(--_highlight-border-color);
	}


	/* # Item text wrappers */

	.document-tab-bar__item-normal {
		display: contents;

		@container (max-width: 200px) {
			display: none;
		}
	}

	.document-tab-bar__item-short {
		display: none;

		@container (max-width: 200px) {
			display: contents;
		}
	}


	/* # Item label */

	.document-tab-bar__item-text {
		overflow: hidden;
		padding-inline-end: var(--primitives-space-6);
		font: var(--components-document-tab-bar-tab-title-font);
		color: var(--semantics-buttons-neutral-tinted-primary-content-color);
		text-overflow: ellipsis;
		white-space: nowrap;
	}

	:host([selected]) .document-tab-bar__item-text {
		color: var(--semantics-buttons-neutral-tinted-is-selected-primary-content-color);
	}

	.document-tab-bar__item-short-text {
		overflow: hidden;
		padding-inline-end: var(--primitives-space-6);
		font: var(--components-document-tab-bar-tab-title-font);
		color: var(--semantics-buttons-neutral-tinted-primary-content-color);
		text-overflow: ellipsis;
		white-space: nowrap;
	}

	:host([selected]) .document-tab-bar__item-short-text {
		color: var(--semantics-buttons-neutral-tinted-is-selected-primary-content-color);
	}


	/* # Item supporting label */

	.document-tab-bar__item-supporting-text {
		overflow: hidden;
		padding-inline-end: var(--primitives-space-6);
		font: var(--primitives-font-body-xs-regular-flat);
		color: var(--_secondary-content-color);
		text-overflow: ellipsis;
		white-space: nowrap;
	}

	.document-tab-bar__item-short-supporting-text {
		overflow: hidden;
		padding-inline-end: var(--primitives-space-6);
		font: var(--primitives-font-body-xs-regular-flat);
		color: var(--_secondary-content-color);
		text-overflow: ellipsis;
		white-space: nowrap;
	}


	/* # Item dismiss button */

	.document-tab-bar__item-dismiss-button {
		display: flex;
		position: absolute;
		top: 50%;
		right: var(--primitives-space-6);
		margin: 0;
		border: none;
		border-radius: var(--semantics-controls-sm-corner-radius);
		background: none;
		width: var(--semantics-controls-sm-min-size);
		height: var(--semantics-controls-sm-min-size);
		padding: var(--primitives-space-4);
		align-items: center;
		justify-content: center;
		color: var(--semantics-buttons-neutral-tinted-primary-content-color);
		transform: translateY(-50%);
		appearance: none;
	}

	@media (hover: hover) {
		.document-tab-bar__item-dismiss-button:hover {
			background-color: var(--_dismiss-button-is-hovered-background-color);
		}
	}

	.document-tab-bar__item-dismiss-button:active {
		background-color: var(--_dismiss-button-is-active-background-color);
	}

	:host([selected]) .document-tab-bar__item-dismiss-button {
		color: var(--semantics-buttons-neutral-tinted-is-selected-primary-content-color);
	}

	@media (hover: hover) {
		:host([selected]) .document-tab-bar__item-dismiss-button:hover {
			background-color: var(--_dismiss-button-is-selected-is-hovered-background-color);
		}
	}

	:host([selected]) .document-tab-bar__item-dismiss-button:active {
		background-color: var(--_dismiss-button-is-selected-is-active-background-color);
	}

	.document-tab-bar__item-dismiss-button:focus-visible {
		outline: var(--semantics-focus-ring-outline);
		outline-offset: var(--semantics-focus-ring-outline-offset);
		box-shadow: var(--semantics-focus-ring-box-shadow);
	}


	/* # Item dismiss icon */

	.document-tab-bar__item-dismiss-icon {
		display: flex;
		width: var(--primitives-space-16);
		height: var(--primitives-space-16);
	}

`;

  // node_modules/@nldd/design-system/dist/components/navigation/document-tab-bar/document-tab-bar.template.js
  init_lit();
  init_class_map2();
  init_icon_button();
  init_icon();
  init_tooltip();
  function documentTabBarTemplate(component) {
    const hasOverflow = component._overflowCount > 0;
    const label = component.accessibleLabel || "Tabbladen";
    const isNavigation = component.navigation;
    const inner = b2`
		<div class="document-tab-bar__items"
			role=${isNavigation ? A : "tablist"}
			aria-label=${isNavigation ? A : label}
		>
			<slot @slotchange=${component._onSlotChange}></slot>
		</div>
		<div class=${e8({ "document-tab-bar__overflow": true, "is-hidden": !hasOverflow })}>
			<nldd-icon-button
				text=${component._t("components.document-tab-bar.overflow-action")}
				variant="neutral-tinted"
				icon="ellipsis"
				tooltip-timing="never"
				popup-type="menu"
				?expanded=${component._menuOpen}
			></nldd-icon-button>
		</div>
		<div class="document-tab-bar__end" hidden>
			<slot name="end" @slotchange=${component._onEndSlotChange}></slot>
		</div>
	`;
    return b2`
		${isNavigation ? b2`<nav class="document-tab-bar" aria-label=${label}>${inner}</nav>` : b2`<div class="document-tab-bar">${inner}</div>`}
		<div class="document-tab-bar__polite-announcer"
			role="status"
			aria-live="polite"
			aria-atomic="true"
		></div>
		<div class="document-tab-bar__assertive-announcer"
			role="alert"
			aria-live="assertive"
			aria-atomic="true"
		></div>
	`;
  }
  function documentTabBarItemTemplate(component) {
    const shortTextValue = component.shortText || component.text;
    const shortSupportingTextValue = component.shortSupportingText || component.supportingText;
    const isNavigation = component._navigation;
    const safeHref = sanitizeUrl(component.href);
    const isLink = Boolean(safeHref);
    const tabindex = component.selected || component._isFallbackFocusable ? "0" : "-1";
    const tooltipText = component.supportingText ? `${component.text} \xB7 ${component.supportingText}` : component.text;
    const tabContent = b2`
		<span class="document-tab-bar__item-normal" aria-hidden="true">
			<span class="document-tab-bar__item-text">${component.text}</span>
			${component.supportingText ? b2`<span class="document-tab-bar__item-supporting-text">${component.supportingText}</span>` : A}
		</span>
		<span class="document-tab-bar__item-short">
			<span class="document-tab-bar__item-short-text">${shortTextValue}</span>
			${shortSupportingTextValue ? b2`<span class="document-tab-bar__item-short-supporting-text">${shortSupportingTextValue}</span>` : A}
		</span>
	`;
    const tab = isLink ? b2`<a class="document-tab-bar__item-tab"
				href=${safeHref}
				role=${isNavigation ? A : "tab"}
				aria-current=${isNavigation && component.selected ? "page" : A}
				aria-selected=${!isNavigation ? component.selected ? "true" : "false" : A}
				aria-label=${tooltipText}
				tabindex=${tabindex}
				@click=${component._handleClick}
			>${tabContent}</a>` : b2`<button class="document-tab-bar__item-tab"
				type="button"
				role="tab"
				aria-selected=${component.selected ? "true" : "false"}
				aria-label=${tooltipText}
				tabindex=${tabindex}
				@click=${component._handleClick}
			>${tabContent}</button>`;
    return b2`
		<nldd-tooltip text=${tooltipText} timing=${component._isShort ? "default" : "never"}>
			<div class="document-tab-bar__item">
				${tab}
				<button class="document-tab-bar__item-dismiss-button"
					aria-label=${component._dismissButtonAccessibilityLabel}
					tabindex=${component.selected || component._isFallbackFocusable ? "0" : "-1"}
					@click=${component._handleDismiss}
				>
					<span class="document-tab-bar__item-dismiss-icon">
						<nldd-icon name="dismiss"></nldd-icon>
					</span>
				</button>
			</div>
		</nldd-tooltip>
	`;
  }

  // node_modules/@nldd/design-system/dist/components/navigation/document-tab-bar/document-tab-bar.i18n.js
  var nlddDocumentTabBarTranslations = {
    "components.document-tab-bar.dismiss-action": "Sluit",
    "components.document-tab-bar.overflow-action": "Toon meer tabbladen",
    "components.document-tab-bar.reorder-dropped-text": "Tabblad verplaatst naar positie {position}.",
    "components.document-tab-bar.reorder-no-change-text": "Tabblad verplaatst. Positie ongewijzigd.",
    "components.document-tab-bar.reorder-cancelled-text": "Slepen geannuleerd."
  };

  // node_modules/@nldd/design-system/dist/components/navigation/document-tab-bar/document-tab-bar.js
  var import_meta28 = {};
  var __decorate86 = function(decorators, target, key, desc) {
    var c6 = arguments.length, r6 = c6 < 3 ? target : desc === null ? desc = Object.getOwnPropertyDescriptor(target, key) : desc, d3;
    if (typeof Reflect === "object" && typeof Reflect.decorate === "function") r6 = Reflect.decorate(decorators, target, key, desc);
    else for (var i8 = decorators.length - 1; i8 >= 0; i8--) if (d3 = decorators[i8]) r6 = (c6 < 3 ? d3(r6) : c6 > 3 ? d3(target, key, r6) : d3(target, key)) || r6;
    return c6 > 3 && r6 && Object.defineProperty(target, key, r6), r6;
  };
  var NLDDDocumentTabBarItem_1;
  var NLDDDocumentTabBar_1;
  var DRAG_THRESHOLD = 5;
  var NLDDDocumentTabBarItem = NLDDDocumentTabBarItem_1 = class NLDDDocumentTabBarItem2 extends i4 {
    constructor() {
      super(...arguments);
      this._id = `nldd-document-tab-bar-item-${NLDDDocumentTabBarItem_1._idCounter++}`;
      this.selected = false;
      this.text = "";
      this.supportingText = "";
      this.shortText = "";
      this.shortSupportingText = "";
      this.href = "";
      this._isShort = false;
      this._itemResizeObserver = null;
      this._dismissButtonAccessibilityLabel = "Sluit";
      this._isFallbackFocusable = false;
      this._navigation = false;
    }
    connectedCallback() {
      super.connectedCallback();
      this.setAttribute("role", "none");
      this._itemResizeObserver = new ResizeObserver(() => this._updateIsShort());
      this._itemResizeObserver.observe(this);
    }
    disconnectedCallback() {
      super.disconnectedCallback();
      this._itemResizeObserver?.disconnect();
      this._itemResizeObserver = null;
    }
    _updateIsShort() {
      const threshold = parseFloat(getComputedStyle(this).getPropertyValue("--_short-text-threshold"));
      this._isShort = Number.isFinite(threshold) && this.getBoundingClientRect().width < threshold;
    }
    focus(options) {
      this.shadowRoot?.querySelector(".document-tab-bar__item-tab")?.focus(options);
    }
    _handleClick() {
      this.dispatchEvent(new CustomEvent("select", {
        bubbles: true,
        composed: true,
        detail: { item: this }
      }));
    }
    _handleDismiss(event) {
      event.stopPropagation();
      this.dispatchEvent(new CustomEvent("dismiss", {
        bubbles: true,
        composed: true,
        detail: { item: this }
      }));
    }
    render() {
      return documentTabBarItemTemplate(this);
    }
  };
  NLDDDocumentTabBarItem.styles = documentTabBarItemStyles;
  NLDDDocumentTabBarItem._idCounter = 0;
  __decorate86([
    n4({ type: Boolean, reflect: true })
  ], NLDDDocumentTabBarItem.prototype, "selected", void 0);
  __decorate86([
    n4({ type: String, attribute: "text" })
  ], NLDDDocumentTabBarItem.prototype, "text", void 0);
  __decorate86([
    n4({ type: String, attribute: "supporting-text" })
  ], NLDDDocumentTabBarItem.prototype, "supportingText", void 0);
  __decorate86([
    n4({ type: String, attribute: "short-text" })
  ], NLDDDocumentTabBarItem.prototype, "shortText", void 0);
  __decorate86([
    n4({ type: String, attribute: "short-supporting-text" })
  ], NLDDDocumentTabBarItem.prototype, "shortSupportingText", void 0);
  __decorate86([
    n4({ type: String })
  ], NLDDDocumentTabBarItem.prototype, "href", void 0);
  __decorate86([
    r5()
  ], NLDDDocumentTabBarItem.prototype, "_isShort", void 0);
  __decorate86([
    r5()
  ], NLDDDocumentTabBarItem.prototype, "_dismissButtonAccessibilityLabel", void 0);
  __decorate86([
    r5()
  ], NLDDDocumentTabBarItem.prototype, "_isFallbackFocusable", void 0);
  __decorate86([
    r5()
  ], NLDDDocumentTabBarItem.prototype, "_navigation", void 0);
  NLDDDocumentTabBarItem = NLDDDocumentTabBarItem_1 = __decorate86([
    t3("nldd-document-tab-bar-item")
  ], NLDDDocumentTabBarItem);
  var NLDDDocumentTabBar = NLDDDocumentTabBar_1 = class NLDDDocumentTabBar2 extends withTranslations(i4, nlddDocumentTabBarTranslations) {
    constructor() {
      super(...arguments);
      this._id = `nldd-document-tab-bar-${NLDDDocumentTabBar_1._idCounter++}`;
      this.accessibleLabel = "";
      this.navigation = false;
      this._overflowCount = 0;
      this._menuOpen = false;
      this._menu = null;
      this._resizeObserver = null;
      this._hasCustomLabel = false;
      this._draggingEl = null;
      this._placeholder = null;
      this._currentDropIndex = -1;
      this._pointerId = null;
      this._clone = null;
      this._cloneOffsetX = 0;
      this._tabBarRect = null;
      this._pendingDragItem = null;
      this._pendingDragStartX = 0;
      this._pendingPointerId = null;
      this._onPointerDown = (event) => {
        const path = event.composedPath();
        const onDismiss = path.some((el) => el instanceof Element && el.classList?.contains("document-tab-bar__item-dismiss-button"));
        if (onDismiss)
          return;
        const item = path.find((el) => el instanceof Element && el.tagName.toLowerCase() === "nldd-document-tab-bar-item");
        if (!item || item.hidden)
          return;
        this._pendingDragItem = item;
        this._pendingDragStartX = event.clientX;
        this._pendingPointerId = event.pointerId;
        this.addEventListener("pointermove", this._onPointerMovePending);
        this.addEventListener("pointerup", this._onPointerUpPending);
        this.addEventListener("pointercancel", this._onPointerCancelPending);
      };
      this._onPointerMovePending = (event) => {
        if (event.pointerId !== this._pendingPointerId)
          return;
        if (!this._pendingDragItem)
          return;
        event.preventDefault();
        if (Math.abs(event.clientX - this._pendingDragStartX) < DRAG_THRESHOLD)
          return;
        const item = this._pendingDragItem;
        this._clearPendingDrag();
        event.preventDefault();
        this._startDrag(item, event.clientX);
        this._pointerId = event.pointerId;
        this.setPointerCapture(event.pointerId);
        this.addEventListener("pointermove", this._onPointerMove);
        this.addEventListener("pointerup", this._onPointerUp);
        this.addEventListener("pointercancel", this._onPointerCancel);
      };
      this._onPointerUpPending = () => {
        this._clearPendingDrag();
      };
      this._onPointerCancelPending = () => {
        this._clearPendingDrag();
      };
      this._lastPointerX = 0;
      this._onPointerMove = (event) => {
        if (!this._draggingEl || !this._placeholder)
          return;
        if (this._clone) {
          this._tabBarRect = this.getBoundingClientRect();
          this._clone.style.setProperty("--_drag-clone-left", `${event.clientX - this._tabBarRect.left - this._cloneOffsetX}px`);
        }
        const draggingRight = event.clientX >= this._lastPointerX;
        this._lastPointerX = event.clientX;
        const visibleItems = this._getVisibleItems().filter((i8) => i8 !== this._draggingEl);
        const pointerX = event.clientX;
        let toIndex = visibleItems.length;
        for (let i8 = 0; i8 < visibleItems.length; i8++) {
          const inner = visibleItems[i8].shadowRoot?.querySelector(".document-tab-bar__item") ?? visibleItems[i8];
          const rect = inner.getBoundingClientRect();
          const threshold = draggingRight ? rect.left : rect.right;
          if (pointerX < threshold) {
            toIndex = i8;
            break;
          }
        }
        this._setDropIndex(toIndex);
      };
      this._onPointerUp = () => {
        try {
          this._endDrag();
        } finally {
          document.documentElement.style.cursor = "";
        }
      };
      this._onPointerCancel = () => {
        try {
          this._cancelDrag();
        } finally {
          document.documentElement.style.cursor = "";
        }
      };
      this._handleItemSelect = (event) => {
        event.stopPropagation();
        const selectedItem = event.detail.item;
        this._getItems().forEach((item) => {
          item.selected = item === selectedItem;
        });
        this._syncFallbackFocusable();
        this.dispatchEvent(new CustomEvent("tabchange", {
          bubbles: true,
          composed: true,
          detail: event.detail
        }));
      };
      this._handleItemDismiss = (event) => {
        event.stopPropagation();
        const dismissedItem = event.detail.item;
        const items = this._getItems();
        let nextItem = null;
        if (dismissedItem.selected) {
          const index = items.indexOf(dismissedItem);
          for (let i8 = index + 1; i8 < items.length; i8++) {
            if (!items[i8].hidden) {
              nextItem = items[i8];
              break;
            }
          }
          if (!nextItem) {
            for (let i8 = index - 1; i8 >= 0; i8--) {
              if (!items[i8].hidden) {
                nextItem = items[i8];
                break;
              }
            }
          }
          if (nextItem) {
            nextItem.selected = true;
            nextItem.focus();
          }
          dismissedItem.selected = false;
        }
        const isLastItem = items.length === 1;
        this.dispatchEvent(new CustomEvent("tabdismiss", {
          bubbles: true,
          composed: true,
          detail: { item: dismissedItem, nextItem }
        }));
        if (isLastItem) {
          this.dispatchEvent(new CustomEvent("tabempty", { bubbles: true, composed: true }));
        }
        this._syncFallbackFocusable();
      };
      this._handleKeyDown = (event) => {
        const path = event.composedPath();
        const item = path.find((el) => el instanceof Element && el.tagName.toLowerCase() === "nldd-document-tab-bar-item");
        if (!item || item.hidden)
          return;
        const onDismiss = path.some((el) => el instanceof Element && el.classList?.contains("document-tab-bar__item-dismiss-button"));
        if (onDismiss)
          return;
        if (event.shiftKey && (event.key === "ArrowLeft" || event.key === "ArrowRight")) {
          event.preventDefault();
          const visibleItems = this._getVisibleItems();
          const allItems = this._getItems();
          const currentIndex2 = visibleItems.indexOf(item);
          if (currentIndex2 === -1)
            return;
          const targetIndex = event.key === "ArrowLeft" ? currentIndex2 - 1 : currentIndex2 + 1;
          if (targetIndex < 0 || targetIndex >= visibleItems.length)
            return;
          const sibling = visibleItems[targetIndex];
          if (event.key === "ArrowLeft") {
            sibling.before(item);
          } else {
            sibling.after(item);
          }
          const fromIndex = allItems.indexOf(item);
          const newAllItems = this._getItems();
          const toIndex = newAllItems.indexOf(item);
          this.dispatchEvent(new CustomEvent("nldd-reorder", {
            detail: { fromIndex, toIndex },
            bubbles: true,
            composed: true
          }));
          this._announce(this._t("components.document-tab-bar.reorder-dropped-text", { position: toIndex + 1 }));
          requestAnimationFrame(() => {
            item.shadowRoot?.querySelector(".document-tab-bar__item-tab")?.focus();
          });
          return;
        }
        const items = this._getVisibleItems();
        if (items.length === 0)
          return;
        const currentIndex = items.findIndex((i8) => i8 === event.target || i8.contains(event.target));
        let newIndex = -1;
        switch (event.key) {
          case "ArrowLeft":
            event.preventDefault();
            newIndex = currentIndex > 0 ? currentIndex - 1 : items.length - 1;
            break;
          case "ArrowRight":
            event.preventDefault();
            newIndex = currentIndex < items.length - 1 ? currentIndex + 1 : 0;
            break;
          case "Home":
            event.preventDefault();
            newIndex = 0;
            break;
          case "End":
            event.preventDefault();
            newIndex = items.length - 1;
            break;
          default:
            return;
        }
        if (newIndex >= 0) {
          items[newIndex].focus();
          if (!this.navigation) {
            this._getItems().forEach((item2) => {
              item2.selected = item2 === items[newIndex];
            });
            this.dispatchEvent(new CustomEvent("tabchange", {
              bubbles: true,
              composed: true,
              detail: { item: items[newIndex] }
            }));
          }
        }
      };
    }
    connectedCallback() {
      super.connectedCallback();
      this.addEventListener("select", this._handleItemSelect);
      this.addEventListener("dismiss", this._handleItemDismiss);
      this.addEventListener("keydown", this._handleKeyDown);
      this.addEventListener("pointerdown", this._onPointerDown);
      this._createMenu();
    }
    disconnectedCallback() {
      super.disconnectedCallback();
      this.removeEventListener("select", this._handleItemSelect);
      this.removeEventListener("dismiss", this._handleItemDismiss);
      this.removeEventListener("keydown", this._handleKeyDown);
      this.removeEventListener("pointerdown", this._onPointerDown);
      this._menu?.remove();
      this._menu = null;
      this._resizeObserver?.disconnect();
      this._resizeObserver = null;
      this._cancelDrag();
    }
    firstUpdated() {
      this._hasCustomLabel = Boolean(this.accessibleLabel);
      if (!this._hasCustomLabel) {
        import_meta28.env?.DEV && console.warn('<nldd-document-tab-bar>: No accessible-label provided. Add an accessible-label attribute for a meaningful navigation landmark name. Falling back to "Tabbladen".');
      }
      const container = this.shadowRoot?.querySelector(".document-tab-bar__items");
      if (container) {
        this._resizeObserver = new ResizeObserver(() => this._calculateOverflow());
        this._resizeObserver.observe(container);
      }
      this._syncMenuAnchor();
      requestAnimationFrame(() => this._calculateOverflow());
    }
    updated(changedProperties) {
      if (changedProperties.has("_overflowCount")) {
        this._applyItemVisibility();
        this._updateMenu();
      }
      if (changedProperties.has("translations")) {
        this._propagateDismissLabel();
      }
      if (changedProperties.has("navigation")) {
        this._propagateNavigation();
      }
    }
    // — Items ——————————————————————————————————————————————————————————————————
    _getItems() {
      const slot = this.shadowRoot?.querySelector("slot:not([name])");
      if (!slot)
        return [];
      return slot.assignedElements().filter((el) => el.tagName.toLowerCase() === "nldd-document-tab-bar-item" && !el.hasAttribute("data-nldd-placeholder"));
    }
    _getVisibleItems() {
      return this._getItems().filter((item) => !item.hidden);
    }
    _propagateNavigation() {
      this._getItems().forEach((item) => {
        item._navigation = this.navigation;
      });
    }
    _propagateDismissLabel() {
      const label = this._t("components.document-tab-bar.dismiss-action");
      this._getItems().forEach((item) => {
        item._dismissButtonAccessibilityLabel = label;
      });
    }
    _syncFallbackFocusable() {
      const items = this._getVisibleItems();
      const hasSelected = items.some((item) => item.selected);
      const firstEnabled = items.find((item) => !item.hidden) ?? null;
      items.forEach((item) => {
        item._isFallbackFocusable = !hasSelected && item === firstEnabled;
      });
    }
    _onEndSlotChange(e9) {
      const slot = e9.target;
      const wrapper = slot.parentElement;
      wrapper.hidden = slot.assignedElements().length === 0;
    }
    _onSlotChange() {
      this._calculateOverflow();
      this._propagateDismissLabel();
      this._syncFallbackFocusable();
      this._propagateNavigation();
    }
    _applyItemVisibility() {
      const items = this._getItems();
      const visibleCount = items.length - this._overflowCount;
      items.forEach((item, index) => {
        item.hidden = index >= visibleCount;
      });
    }
    _clearPendingDrag() {
      this._pendingDragItem = null;
      this._pendingDragStartX = 0;
      this._pendingPointerId = null;
      this.removeEventListener("pointermove", this._onPointerMovePending);
      this.removeEventListener("pointerup", this._onPointerUpPending);
      this.removeEventListener("pointercancel", this._onPointerCancelPending);
    }
    // — Drag: keyboard ————————————————————————————————————————————————————————
    // — Drag: core ————————————————————————————————————————————————————————————
    _startDrag(item, clientX = 0) {
      const visibleItems = this._getVisibleItems();
      const visibleIndex = visibleItems.indexOf(item);
      if (visibleIndex === -1)
        return;
      this._draggingEl = item;
      this._currentDropIndex = visibleIndex;
      this._lastPointerX = clientX;
      const inner = item.shadowRoot?.querySelector(".document-tab-bar__item-tab") ?? item;
      const rect = inner.getBoundingClientRect();
      this._placeholder = document.createElement("div");
      this._placeholder.className = "nldd-document-tab-bar-drag-placeholder";
      this._placeholder.setAttribute("aria-hidden", "true");
      this._placeholder.setAttribute("data-nldd-placeholder", "");
      item.after(this._placeholder);
      item.classList.add("is-dragging");
      this._tabBarRect = this.getBoundingClientRect();
      this._cloneOffsetX = clientX - rect.left;
      document.documentElement.style.cursor = "grabbing";
      const threshold = parseFloat(getComputedStyle(item).getPropertyValue("--_short-text-threshold"));
      const useShort = rect.width < threshold;
      const displayTitle = useShort ? item.shortText || item.text : item.text;
      const displaySubtitle = useShort ? item.shortSupportingText || item.supportingText : item.supportingText;
      const cloneInner = document.createElement("div");
      cloneInner.className = "document-tab-bar__item";
      const cloneTab = document.createElement("div");
      cloneTab.className = "document-tab-bar__item-tab";
      const titleEl = document.createElement("span");
      titleEl.className = "document-tab-bar__item-text";
      titleEl.textContent = displayTitle;
      cloneTab.appendChild(titleEl);
      if (displaySubtitle) {
        const subtitleEl = document.createElement("span");
        subtitleEl.className = "document-tab-bar__item-supporting-text";
        subtitleEl.textContent = displaySubtitle;
        cloneTab.appendChild(subtitleEl);
      }
      cloneInner.appendChild(cloneTab);
      this._clone = document.createElement("div");
      this._clone.className = `document-tab-bar__drag-clone${item.selected ? " is-selected" : ""}`;
      this._clone.style.setProperty("--_drag-clone-left", `${clientX - this._tabBarRect.left - this._cloneOffsetX}px`);
      this._clone.style.setProperty("--_drag-clone-top", `${rect.top - this._tabBarRect.top}px`);
      this._clone.style.setProperty("--_drag-clone-width", `${rect.width}px`);
      this._clone.style.setProperty("--_drag-clone-height", `${rect.height}px`);
      this._clone.appendChild(cloneInner);
      this.renderRoot.appendChild(this._clone);
    }
    _setDropIndex(toIndex) {
      if (!this._placeholder || !this._draggingEl)
        return;
      const nonDragging = this._getVisibleItems().filter((i8) => i8 !== this._draggingEl);
      const clamped = Math.max(0, Math.min(nonDragging.length, toIndex));
      this._currentDropIndex = clamped;
      this._placeholder.remove();
      if (nonDragging.length === 0) {
        this._draggingEl.after(this._placeholder);
        return;
      }
      if (clamped === 0) {
        nonDragging[0].before(this._placeholder);
      } else {
        nonDragging[clamped - 1].after(this._placeholder);
      }
    }
    _getDropIndex() {
      return this._currentDropIndex;
    }
    _endDrag() {
      if (!this._draggingEl || !this._placeholder)
        return;
      const allItems = this._getItems();
      const movedItem = this._draggingEl;
      const fromIndex = allItems.indexOf(movedItem);
      this._placeholder.replaceWith(movedItem);
      const newAllItems = this._getItems();
      const toIndex = newAllItems.indexOf(movedItem);
      this._cleanupDrag();
      if (fromIndex !== toIndex) {
        this.dispatchEvent(new CustomEvent("nldd-reorder", {
          detail: { fromIndex, toIndex },
          bubbles: true,
          composed: true
        }));
        this._announce(this._t("components.document-tab-bar.reorder-dropped-text", { position: toIndex + 1 }));
        requestAnimationFrame(() => {
          const inner = movedItem.shadowRoot?.querySelector(".document-tab-bar__item-tab");
          inner?.focus();
        });
      } else {
        this._announce(this._t("components.document-tab-bar.reorder-no-change-text"));
      }
    }
    _cancelDrag() {
      if (!this._draggingEl)
        return;
      this._cleanupDrag();
      this._announce(this._t("components.document-tab-bar.reorder-cancelled-text"));
    }
    _cleanupDrag() {
      this._clearPendingDrag();
      this._draggingEl?.classList.remove("is-dragging");
      this._placeholder?.remove();
      this._clone?.remove();
      document.documentElement.style.cursor = "";
      if (this._pointerId !== null) {
        try {
          this.releasePointerCapture(this._pointerId);
        } catch (e9) {
          if (!(e9 instanceof DOMException))
            throw e9;
        }
        this._pointerId = null;
      }
      this.removeEventListener("pointermove", this._onPointerMove);
      this.removeEventListener("pointerup", this._onPointerUp);
      this.removeEventListener("pointercancel", this._onPointerCancel);
      this._draggingEl = null;
      this._placeholder = null;
      this._clone = null;
      this._cloneOffsetX = 0;
      this._tabBarRect = null;
      this._lastPointerX = 0;
      this._currentDropIndex = -1;
    }
    // — Accessibility ——————————————————————————————————————————————————————————
    _announce(message, assertive = false) {
      const selector = assertive ? ".document-tab-bar__assertive-announcer" : ".document-tab-bar__polite-announcer";
      const region = this.shadowRoot?.querySelector(selector);
      if (!region)
        return;
      region.textContent = "";
      requestAnimationFrame(() => requestAnimationFrame(() => {
        region.textContent = message;
      }));
    }
    _calculateOverflow() {
      const items = this._getItems();
      const totalItems = items.length;
      if (totalItems === 0) {
        this._overflowCount = 0;
        return;
      }
      const container = this.shadowRoot?.querySelector(".document-tab-bar__items");
      if (!container)
        return;
      const containerWidth = container.offsetWidth;
      if (containerWidth === 0)
        return;
      const gap = parseFloat(getComputedStyle(container).gap) || 8;
      const firstItem = this._getItems()[0];
      const minItemWidth = firstItem ? parseFloat(getComputedStyle(firstItem).minWidth) || parseFloat(getComputedStyle(this).getPropertyValue("--_item-min-width")) : parseFloat(getComputedStyle(this).getPropertyValue("--_item-min-width"));
      const overflowButtonReserve = parseFloat(getComputedStyle(this).getPropertyValue("--_overflow-button-reserve"));
      const visible = Math.floor((containerWidth - overflowButtonReserve + gap) / (minItemWidth + gap));
      const newOverflowCount = Math.max(0, totalItems - Math.max(1, visible));
      if (newOverflowCount !== this._overflowCount) {
        this._overflowCount = newOverflowCount;
        this._ensureSelectedVisible();
      }
    }
    _ensureSelectedVisible() {
      const items = this._getItems();
      const visibleCount = items.length - this._overflowCount;
      const selectedIndex = items.findIndex((i8) => i8.selected);
      if (selectedIndex >= visibleCount && visibleCount > 0) {
        const lastVisible = items[visibleCount - 1];
        const selected = items[selectedIndex];
        this.insertBefore(selected, lastVisible);
      }
    }
    // — Overflow menu ——————————————————————————————————————————————————————————
    _updateMenu() {
      if (!this._menu)
        return;
      this._menu.innerHTML = "";
      const items = this._getItems();
      const visibleCount = items.length - this._overflowCount;
      items.slice(visibleCount).forEach((item) => {
        const menuItemText = item.supportingText ? `${item.text || "\u2013"} \xB7 ${item.supportingText}` : item.text || "\u2013";
        const menuItem = document.createElement("nldd-menu-item");
        menuItem.setAttribute("text", menuItemText);
        menuItem.addEventListener("click", () => {
          this._selectAndPromote(item);
          this._closeMenu();
        });
        this._menu.appendChild(menuItem);
      });
    }
    _selectAndPromote(targetItem) {
      const items = this._getItems();
      const visibleCount = items.length - this._overflowCount;
      items.forEach((item) => {
        item.selected = item === targetItem;
      });
      if (visibleCount > 0 && visibleCount < items.length) {
        const lastVisible = items[visibleCount - 1];
        this.insertBefore(targetItem, lastVisible);
      }
      this._applyItemVisibility();
      this._updateMenu();
      this.dispatchEvent(new CustomEvent("tabchange", {
        bubbles: true,
        composed: true,
        detail: { item: targetItem }
      }));
    }
    _createMenu() {
      if (this._menu)
        return;
      if (typeof document === "undefined")
        return;
      const menu = document.createElement("nldd-menu");
      menu.setAttribute("placement", "bottom-end");
      menu.id = `${this._id}-menu`;
      menu.addEventListener("toggle", (event) => {
        this._menuOpen = event.newState === "open";
      });
      document.body.appendChild(menu);
      this._menu = menu;
    }
    _syncMenuAnchor() {
      if (!this._menu)
        return;
      const button = this.shadowRoot?.querySelector(".document-tab-bar__overflow nldd-icon-button");
      if (button) {
        this._menu.anchorElement = button;
        if ("popoverTargetElement" in button) {
          button.popoverTargetElement = this._menu;
        }
        if ("popoverTargetAction" in button && button.popoverTargetAction === "toggle") {
          button.popoverTargetAction = "show";
        }
      }
    }
    _closeMenu() {
      this._menu?.hidePopover();
    }
    render() {
      return documentTabBarTemplate(this);
    }
  };
  NLDDDocumentTabBar.styles = documentTabBarStyles;
  NLDDDocumentTabBar._idCounter = 0;
  __decorate86([
    n4({ type: String, attribute: "accessible-label" })
  ], NLDDDocumentTabBar.prototype, "accessibleLabel", void 0);
  __decorate86([
    n4({ type: Boolean, reflect: true })
  ], NLDDDocumentTabBar.prototype, "navigation", void 0);
  __decorate86([
    r5()
  ], NLDDDocumentTabBar.prototype, "_overflowCount", void 0);
  __decorate86([
    r5()
  ], NLDDDocumentTabBar.prototype, "_menuOpen", void 0);
  NLDDDocumentTabBar = NLDDDocumentTabBar_1 = __decorate86([
    t3("nldd-document-tab-bar")
  ], NLDDDocumentTabBar);

  // node_modules/@nldd/design-system/dist/components/navigation/pagination/pagination.js
  init_lit();
  init_decorators();

  // node_modules/@nldd/design-system/dist/components/navigation/pagination/pagination.styles.js
  init_lit();
  init_slotted_reset();
  var paginationStyles = i`


	/* # Host */

	:host {
		--_z-index-selected: 1;
		--_z-index-focus: 2;

		${inheritedTextReset}
		display: block;
		container-type: inline-size;
		isolation: isolate;
		user-select: none;
		-webkit-tap-highlight-color: transparent;
	}

	:host([hidden]) {
		display: none;
	}

	:host([centered]) {
		display: flex;
		justify-content: center;
	}

	:host([disabled]) {
		opacity: var(--primitives-opacity-disabled);
		pointer-events: none;
	}


	/* # Block */

	.pagination {
		display: inline-flex;
		position: relative;
		border-radius: var(--semantics-controls-md-corner-radius);
		background-color: var(--semantics-buttons-neutral-tinted-background-color);
		align-items: center;
	}

	.pagination::after {
		content: '';
		position: absolute;
		inset: 0;
		border-radius: inherit;
		box-shadow: inset 0 0 0 var(--primitives-border-width-thin) var(--semantics-buttons-neutral-tinted-highlight-border-color);
		pointer-events: none;
	}

	@media (forced-colors: active) {
		.pagination {
			border: 1px solid CanvasText;
		}
	}

	.pagination__previous-button:focus-within,
	.pagination__next-button:focus-within {
		position: relative;
		z-index: var(--_z-index-selected);
	}


	/* # Page button */

	.pagination__page-button {
		--_highlight-border-color: transparent;

		box-sizing: border-box;
		display: inline-flex;
		position: relative;
		z-index: var(--_z-index-selected);
		margin: 0;
		border: none;
		border-radius: var(--semantics-controls-md-corner-radius);
		background: transparent;
		box-shadow: inset 0 0 0 var(--primitives-border-width-thin) var(--_highlight-border-color);
		min-width: var(--semantics-controls-md-min-size);
		height: var(--semantics-controls-md-min-size);
		padding-block: var(--primitives-space-8);
		padding-inline: var(--primitives-space-12);
		align-items: center;
		justify-content: center;
		color: inherit;
		font: var(--semantics-buttons-md-primary-text-font);
		appearance: none;
	}

	a.pagination__page-button {
		cursor: var(--semantics-controls-link-cursor);
		text-decoration: none;
	}

	@media (hover: hover) {
		.pagination__page-button:hover:not(.is-current) {
			background-color: var(--semantics-buttons-neutral-tinted-is-hovered-background-color);
		}

		.pagination__page-button.is-current:hover {
			--_highlight-border-color: var(--semantics-buttons-neutral-tinted-is-selected-is-hovered-highlight-border-color);
			background-color: var(--semantics-buttons-neutral-tinted-is-selected-is-hovered-background-color);
			color: var(--semantics-buttons-neutral-tinted-is-selected-is-hovered-primary-content-color);
		}
	}

	.pagination__page-button:active:not(.is-current) {
		background-color: var(--semantics-buttons-neutral-tinted-is-active-background-color);
		color: var(--semantics-buttons-neutral-tinted-is-active-primary-content-color);
	}

	.pagination__page-button.is-current {
		--_highlight-border-color: var(--semantics-buttons-neutral-tinted-is-selected-highlight-border-color);
		background-color: var(--semantics-buttons-neutral-tinted-is-selected-background-color);
		color: var(--semantics-buttons-neutral-tinted-is-selected-primary-content-color);
	}

	.pagination__page-button.is-current:active {
		--_highlight-border-color: var(--semantics-buttons-neutral-tinted-is-selected-is-active-highlight-border-color);
		background-color: var(--semantics-buttons-neutral-tinted-is-selected-is-active-background-color);
		color: var(--semantics-buttons-neutral-tinted-is-selected-is-active-primary-content-color);
	}

	@media (forced-colors: active) {
		.pagination__page-button.is-current {
			background-color: Highlight;
			color: HighlightText;
		}
	}

	.pagination__page-button:focus-visible {
		z-index: var(--_z-index-focus);
		outline: var(--semantics-focus-ring-outline);
		outline-offset: var(--semantics-focus-ring-outline-offset);
		box-shadow: var(--semantics-focus-ring-box-shadow), inset 0 0 0 var(--primitives-border-width-thin) var(--_highlight-border-color);
	}

	@media (forced-colors: active) {
		.pagination__page-button:focus-visible {
			outline: 2px solid CanvasText;
		}
	}


	/* # Ellipsis */

	.pagination__ellipsis {
		box-sizing: border-box;
		display: inline-flex;
		pointer-events: none;
		min-width: var(--semantics-controls-md-min-size);
		height: var(--semantics-controls-md-min-size);
		padding-block: var(--primitives-space-8);
		padding-inline: var(--primitives-space-12);
		align-items: center;
		justify-content: center;
		color: inherit;
		font: var(--semantics-buttons-md-primary-text-font);
	}


	/* # Divider */

	.pagination__divider {
		display: flex;
		height: var(--semantics-controls-md-min-size);
		flex-shrink: 0;
		align-items: center;
		justify-content: center;
	}

	.pagination__divider-line {
		background-color: var(--semantics-buttons-neutral-tinted-divider-color);
		width: var(--semantics-dividers-thickness);
		height: var(--semantics-buttons-md-divider-length);
	}


	/* # Select (compact fallback) */

	.pagination__select-wrapper {
		/* Mirrors the dropdown's neutral-tinted state tokens. The visible
		   <select> sits on top with a transparent background, so the wrapper
		   carries the hover/active/expanded surface behind it. */
		--_background-color: transparent;
		--_content-color: inherit;
		--_is-hovered-background-color: var(--semantics-buttons-neutral-tinted-is-hovered-background-color);
		--_is-hovered-content-color: var(--semantics-buttons-neutral-tinted-is-hovered-primary-content-color);
		--_is-active-background-color: var(--semantics-buttons-neutral-tinted-is-active-background-color);
		--_is-active-content-color: var(--semantics-buttons-neutral-tinted-is-active-primary-content-color);

		display: inline-flex;
		position: relative;
		border-radius: var(--semantics-controls-md-corner-radius);
		background-color: var(--_background-color);
		align-items: center;
		color: var(--_content-color);
		transition:
			background-color var(--primitives-transition-duration-fast) var(--primitives-transition-easing-default),
			color var(--primitives-transition-duration-fast) var(--primitives-transition-easing-default)
		;
	}

	@media (hover: hover) {
		.pagination__select-wrapper:hover {
			background-color: var(--_is-hovered-background-color);
			color: var(--_is-hovered-content-color);
		}
	}

	.pagination__select-wrapper:active {
		background-color: var(--_is-active-background-color);
		color: var(--_is-active-content-color);
	}

	:host([select-expanded]) .pagination__select-wrapper {
		--_background-color: var(--semantics-buttons-neutral-tinted-is-expanded-background-color);
		--_content-color: var(--semantics-buttons-neutral-tinted-is-expanded-primary-content-color);
		--_is-hovered-background-color: var(--semantics-buttons-neutral-tinted-is-expanded-is-hovered-background-color);
		--_is-hovered-content-color: var(--semantics-buttons-neutral-tinted-is-expanded-is-hovered-primary-content-color);
		--_is-active-background-color: var(--semantics-buttons-neutral-tinted-is-expanded-is-active-background-color);
		--_is-active-content-color: var(--semantics-buttons-neutral-tinted-is-expanded-is-active-primary-content-color);
	}

	@media (prefers-reduced-motion: reduce) {
		.pagination__select-wrapper {
			transition: none;
		}
	}

	.pagination__select {
		box-sizing: border-box;
		position: relative;
		z-index: 1;
		margin: 0;
		border: none;
		background: transparent;
		height: var(--semantics-controls-md-min-size);
		padding-block: var(--primitives-space-8);
		padding-inline-start: var(--primitives-space-12);
		padding-inline-end: calc(var(--primitives-space-24) + var(--primitives-space-12));
		color: inherit;
		font: var(--semantics-buttons-md-primary-text-font);
		appearance: none;
	}

	.pagination__select:focus {
		outline: var(--semantics-focus-ring-outline);
		outline-offset: var(--semantics-focus-ring-outline-offset);
		border-radius: calc(var(--semantics-controls-md-corner-radius) - var(--primitives-space-4) / 2);
		box-shadow: var(--semantics-focus-ring-box-shadow);
	}

	:host([is-pointer-focus]) .pagination__select:focus {
		outline: none;
		box-shadow: none;
	}

	.pagination__select-picker-icon {
		position: absolute;
		inset-inline-end: var(--primitives-space-8);
		pointer-events: none;
		width: var(--primitives-space-24);
		height: var(--primitives-space-24);
	}


	/* # Responsive */

	.pagination__page-buttons {
		display: contents;

		@container (max-width: 400px) {
			display: none;
		}
	}

	.pagination__compact {
		display: none;

		@container (max-width: 400px) {
			display: contents;
		}
	}
`;

  // node_modules/@nldd/design-system/dist/components/navigation/pagination/pagination.template.js
  init_lit();
  init_icon_button();
  init_icon();
  function paginationTemplate(component) {
    const pages = component._getVisiblePages();
    const atFirst = component.current <= 1;
    const atLast = component.current >= component.total;
    const t6 = component._t.bind(component);
    const hasHref = !!component.hrefPattern;
    const isDisabled = component.disabled;
    const renderPageButton = (page) => {
      const isCurrent = page === component.current;
      const label = t6("components.pagination.page-action", { page });
      if (hasHref) {
        return b2`
				<a class="pagination__page-button ${isCurrent ? "is-current" : ""}"
					href=${!isDisabled ? component._hrefForPage(page) : A}
					aria-label=${label}
					aria-current=${isCurrent ? "page" : A}
					tabindex=${isDisabled ? -1 : A}
					aria-disabled=${isDisabled ? "true" : A}
					@click=${(e9) => {
          e9.preventDefault();
          component._goToPage(page);
        }}
				>
					<span class="pagination__page-button-text">${page}</span>
				</a>
			`;
      }
      return b2`
			<button class="pagination__page-button ${isCurrent ? "is-current" : ""}"
				type="button"
				aria-label=${label}
				aria-current=${isCurrent ? "page" : A}
				?disabled=${isDisabled}
				@click=${() => component._goToPage(page)}
			>
				<span class="pagination__page-button-text">${page}</span>
			</button>
		`;
    };
    return b2`
		<nav class="pagination"
			aria-label=${t6("components.pagination.accessibility-label")}
		>
			<div class="pagination__previous-button">
				<nldd-icon-button
					icon="chevron-left-small"
					text=${t6("components.pagination.previous-action")}
					variant="neutral-tinted"
					no-highlight-border
					?disabled=${isDisabled || atFirst}
					href=${hasHref && !isDisabled && !atFirst ? component._hrefForPage(component.current - 1) : A}
					@click=${(e9) => {
      if (hasHref)
        e9.preventDefault();
      component._goToPage(component.current - 1);
    }}
				></nldd-icon-button>
			</div>
			<div class="pagination__divider" aria-hidden="true">
				<div class="pagination__divider-line"></div>
			</div>
			<div class="pagination__page-buttons">
				${pages.map((page) => page === "ellipsis" ? b2`<div class="pagination__ellipsis" aria-hidden="true">&hellip;</div>` : renderPageButton(page))}
			</div>
			<div class="pagination__compact">
				<div class="pagination__select-wrapper">
					<select class="pagination__select"
						aria-label=${t6("components.pagination.go-to-page-label")}
						?disabled=${isDisabled}
						@change=${(e9) => component._goToPage(Number(e9.target.value))}
						@focus=${component._handleSelectFocus}
						@blur=${component._handleSelectBlur}
						@keydown=${component._handleSelectKeydown}
						@toggle=${component._handleSelectToggle}
					>
						${Array.from({ length: component.total }, (_2, i8) => i8 + 1).map((page) => b2`
							<option value=${page} ?selected=${page === component.current}>${page} / ${component.total}</option>
						`)}
					</select>
					<div class="pagination__select-picker-icon">
						<nldd-icon name="chevron-up-chevron-down"></nldd-icon>
					</div>
				</div>
			</div>
			<div class="pagination__divider" aria-hidden="true">
				<div class="pagination__divider-line"></div>
			</div>
			<div class="pagination__next-button">
				<nldd-icon-button
					icon="chevron-right-small"
					text=${t6("components.pagination.next-action")}
					variant="neutral-tinted"
					no-highlight-border
					?disabled=${isDisabled || atLast}
					href=${hasHref && !isDisabled && !atLast ? component._hrefForPage(component.current + 1) : A}
					@click=${(e9) => {
      if (hasHref)
        e9.preventDefault();
      component._goToPage(component.current + 1);
    }}
				></nldd-icon-button>
			</div>
		</nav>
	`;
  }

  // node_modules/@nldd/design-system/dist/components/navigation/pagination/pagination.i18n.js
  var nlddPaginationTranslations = {
    "components.pagination.accessibility-label": "Paginering",
    "components.pagination.previous-action": "Ga naar vorige pagina",
    "components.pagination.next-action": "Ga naar volgende pagina",
    "components.pagination.page-action": "Ga naar pagina {page}",
    "components.pagination.go-to-page-label": "Ga naar pagina"
  };

  // node_modules/@nldd/design-system/dist/components/navigation/pagination/pagination.js
  init_input_modality();
  var __decorate87 = function(decorators, target, key, desc) {
    var c6 = arguments.length, r6 = c6 < 3 ? target : desc === null ? desc = Object.getOwnPropertyDescriptor(target, key) : desc, d3;
    if (typeof Reflect === "object" && typeof Reflect.decorate === "function") r6 = Reflect.decorate(decorators, target, key, desc);
    else for (var i8 = decorators.length - 1; i8 >= 0; i8--) if (d3 = decorators[i8]) r6 = (c6 < 3 ? d3(r6) : c6 > 3 ? d3(target, key, r6) : d3(target, key)) || r6;
    return c6 > 3 && r6 && Object.defineProperty(target, key, r6), r6;
  };
  var NLDDPagination = class NLDDPagination2 extends i4 {
    constructor() {
      super(...arguments);
      this.current = 1;
      this.total = 1;
      this.disabled = false;
      this.centered = false;
      this.hrefPattern = "";
      this.translations = {};
      this._mergedTranslations = { ...nlddPaginationTranslations };
      this._handleSelectFocus = () => {
        this.toggleAttribute("is-pointer-focus", isPointerMode());
      };
      this._handleSelectBlur = () => {
        this.toggleAttribute("is-pointer-focus", false);
        this.toggleAttribute("select-expanded", false);
      };
      this._handleSelectKeydown = () => {
        this.toggleAttribute("is-pointer-focus", false);
      };
      this._handleSelectToggle = (e9) => {
        this.toggleAttribute("select-expanded", e9.newState === "open");
      };
    }
    willUpdate(changed) {
      if (changed.has("translations")) {
        this._mergedTranslations = { ...nlddPaginationTranslations, ...this.translations };
      }
    }
    _t(key, params) {
      let text = this._mergedTranslations[key] ?? key;
      if (params) {
        for (const [k2, v3] of Object.entries(params)) {
          text = text.replace(`{${k2}}`, String(v3));
        }
      }
      return text;
    }
    _getVisiblePages() {
      if (this.total <= 7) {
        return Array.from({ length: this.total }, (_2, i8) => i8 + 1);
      }
      if (this.current <= 3) {
        return [1, 2, 3, 4, "ellipsis", this.total - 1, this.total];
      }
      if (this.current === 4) {
        return [1, 2, 3, 4, 5, "ellipsis", this.total];
      }
      if (this.current >= this.total - 3) {
        return [1, "ellipsis", this.total - 4, this.total - 3, this.total - 2, this.total - 1, this.total];
      }
      return [1, "ellipsis", this.current - 1, this.current, this.current + 1, "ellipsis", this.total];
    }
    _hrefForPage(page) {
      const href = this.hrefPattern.replace("{page}", String(page));
      if (/^(https?:\/\/|[^:]+$)/.test(href))
        return href;
      return "";
    }
    _goToPage(page) {
      if (this.disabled || page < 1 || page > this.total || page === this.current) {
        return;
      }
      const detail = { page };
      if (this.hrefPattern) {
        detail.href = this._hrefForPage(page);
      }
      const cancelable = !!this.hrefPattern;
      const event = new CustomEvent("page-change", {
        detail,
        bubbles: true,
        composed: true,
        cancelable
      });
      const proceeded = this.dispatchEvent(event);
      this.current = page;
      if (this.hrefPattern && proceeded && detail.href) {
        window.location.href = detail.href;
      }
    }
    render() {
      return paginationTemplate(this);
    }
  };
  NLDDPagination.styles = paginationStyles;
  __decorate87([
    n4({ type: Number, reflect: true })
  ], NLDDPagination.prototype, "current", void 0);
  __decorate87([
    n4({ type: Number, reflect: true })
  ], NLDDPagination.prototype, "total", void 0);
  __decorate87([
    n4({ type: Boolean, reflect: true })
  ], NLDDPagination.prototype, "disabled", void 0);
  __decorate87([
    n4({ type: Boolean, reflect: true })
  ], NLDDPagination.prototype, "centered", void 0);
  __decorate87([
    n4({ type: String, attribute: "href-pattern" })
  ], NLDDPagination.prototype, "hrefPattern", void 0);
  __decorate87([
    n4({ type: Object })
  ], NLDDPagination.prototype, "translations", void 0);
  NLDDPagination = __decorate87([
    t3("nldd-pagination")
  ], NLDDPagination);

  // node_modules/@nldd/design-system/dist/components/status-and-feedback/modal-dialog/modal-dialog.js
  init_lit();
  init_decorators();

  // node_modules/@nldd/design-system/dist/components/status-and-feedback/modal-dialog/modal-dialog.styles.js
  init_lit();
  init_breakpoints();
  init_slotted_reset();
  var mdMin18 = r(breakpoints.mdMin);
  var modalDialogStyles = i`


	/* # Host */

	:host {
		--_max-height: 90vh;
		--_animation-duration: 150ms;
		--_animation-easing: ease;

		${inheritedTextReset}
		display: contents;
	}

	:host([hidden]) {
		display: none;
	}


	/* # Block */

	.modal-dialog {
		box-sizing: border-box;
		outline: none;
		border: none;
		border-radius: var(--semantics-overlays-corner-radius);
		box-shadow: var(--semantics-overlays-box-shadow);
		background-color: var(--semantics-surfaces-base-background-color);
		width: calc(100% - var(--primitives-space-16) * 2);
		max-width: var(--primitives-area-480);
		max-height: var(--_max-height);
		overflow-y: auto;
		padding: var(--primitives-space-16);

		@media (min-width: ${mdMin18}) {
			padding: var(--primitives-space-24);
		}
	}

	.modal-dialog:not([open]) {
		display: none;
	}

	.modal-dialog:focus-visible:not(.is-pointer-focus) {
		outline: var(--semantics-focus-ring-outline);
		outline-offset: var(--semantics-focus-ring-outline-offset);
		box-shadow: var(--semantics-focus-ring-box-shadow), var(--semantics-overlays-box-shadow);
	}

	.modal-dialog::backdrop {
		background: var(--semantics-overlays-backdrop-color);
	}

	.modal-dialog[open] {
		animation: modal-dialog-in var(--_animation-duration) var(--_animation-easing) both;
	}

	.modal-dialog.is-closing {
		animation: modal-dialog-out var(--_animation-duration) var(--_animation-easing) both;
	}

	@media (prefers-reduced-motion: reduce) {
		.modal-dialog[open],
		.modal-dialog.is-closing {
			animation: none;
		}
	}


	/* # Keyframes */

	@keyframes modal-dialog-in {
		from {
			opacity: 0;
			transform: scale(0.95);
		}
		to {
			opacity: 1;
			transform: scale(1);
		}
	}

	@keyframes modal-dialog-out {
		from {
			opacity: 1;
			transform: scale(1);
		}
		to {
			opacity: 0;
			transform: scale(0.95);
		}
	}
`;

  // node_modules/@nldd/design-system/dist/components/status-and-feedback/modal-dialog/modal-dialog.template.js
  init_lit();
  function modalDialogTemplate(component) {
    return b2`
		<dialog class="modal-dialog"
			role=${component.variant === "alert" ? "alertdialog" : A}
			aria-label=${component.accessibleLabel || component.text || A}
			aria-modal="true"
			@click=${component._handleBackdropClick}
			@cancel=${component._handleCancel}
		>
			<nldd-inline-dialog
				variant=${component.variant || A}
				icon=${component.icon || A}
				text=${component.text || A}
				supporting-text=${component.supportingText || A}
				heading-level="2"
			>
				<slot></slot>
				<slot slot="actions" name="actions"></slot>
			</nldd-inline-dialog>
		</dialog>
	`;
  }

  // node_modules/@nldd/design-system/dist/components/status-and-feedback/modal-dialog/modal-dialog.js
  init_input_modality();
  init_inline_dialog();
  var __decorate88 = function(decorators, target, key, desc) {
    var c6 = arguments.length, r6 = c6 < 3 ? target : desc === null ? desc = Object.getOwnPropertyDescriptor(target, key) : desc, d3;
    if (typeof Reflect === "object" && typeof Reflect.decorate === "function") r6 = Reflect.decorate(decorators, target, key, desc);
    else for (var i8 = decorators.length - 1; i8 >= 0; i8--) if (d3 = decorators[i8]) r6 = (c6 < 3 ? d3(r6) : c6 > 3 ? d3(target, key, r6) : d3(target, key)) || r6;
    return c6 > 3 && r6 && Object.defineProperty(target, key, r6), r6;
  };
  var NLDDModalDialog = class NLDDModalDialog2 extends i4 {
    constructor() {
      super(...arguments);
      this.variant = "";
      this.icon = "";
      this.text = "";
      this.supportingText = "";
      this.accessibleLabel = "";
      this._closing = false;
    }
    get _dialog() {
      return this.shadowRoot?.querySelector("dialog") ?? null;
    }
    show() {
      const dialog = this._dialog;
      if (!dialog || dialog.open)
        return;
      dialog.showModal();
      this._manageFocus();
      this.dispatchEvent(new CustomEvent("open", { bubbles: true, composed: true }));
    }
    _manageFocus() {
      if (this.querySelector("[autofocus]"))
        return;
      const dialog = this._dialog;
      if (!dialog)
        return;
      dialog.classList.toggle("is-pointer-focus", isPointerMode());
      dialog.focus();
    }
    hide() {
      const dialog = this._dialog;
      if (!dialog || !dialog.open || this._closing)
        return;
      this._closing = true;
      dialog.classList.add("is-closing");
      dialog.addEventListener("animationend", () => {
        dialog.classList.remove("is-closing");
        this._closing = false;
        dialog.close();
        this.dispatchEvent(new CustomEvent("close", { bubbles: true, composed: true }));
      }, { once: true });
      requestAnimationFrame(() => {
        if (this._closing && getComputedStyle(dialog).animationName === "none") {
          dialog.classList.remove("is-closing");
          this._closing = false;
          dialog.close();
          this.dispatchEvent(new CustomEvent("close", { bubbles: true, composed: true }));
        }
      });
    }
    _handleBackdropClick(e9) {
      if (e9.target !== this._dialog)
        return;
      const rect = this._dialog.getBoundingClientRect();
      const insideDialog = e9.clientX >= rect.left && e9.clientX <= rect.right && e9.clientY >= rect.top && e9.clientY <= rect.bottom;
      if (!insideDialog)
        this.hide();
    }
    _handleCancel(e9) {
      e9.preventDefault();
      this.hide();
    }
    render() {
      return modalDialogTemplate(this);
    }
  };
  NLDDModalDialog.styles = modalDialogStyles;
  __decorate88([
    n4({ type: String, reflect: true })
  ], NLDDModalDialog.prototype, "variant", void 0);
  __decorate88([
    n4({ type: String, reflect: true })
  ], NLDDModalDialog.prototype, "icon", void 0);
  __decorate88([
    n4({ type: String, reflect: true })
  ], NLDDModalDialog.prototype, "text", void 0);
  __decorate88([
    n4({ type: String, reflect: true, attribute: "supporting-text" })
  ], NLDDModalDialog.prototype, "supportingText", void 0);
  __decorate88([
    n4({ type: String, attribute: "accessible-label" })
  ], NLDDModalDialog.prototype, "accessibleLabel", void 0);
  NLDDModalDialog = __decorate88([
    t3("nldd-modal-dialog")
  ], NLDDModalDialog);

  // node_modules/@nldd/design-system/dist/components/index.js
  init_inline_dialog();

  // node_modules/@nldd/design-system/dist/components/status-and-feedback/banner/banner.js
  init_lit();
  init_decorators();

  // node_modules/@nldd/design-system/dist/components/status-and-feedback/banner/banner.styles.js
  init_lit();
  init_slotted_reset();
  var bannerStyles = i`


	/* # Host */

	:host {
		--_corner-radius: var(--components-banner-corner-radius);
		--_padding: var(--components-banner-padding);
		--_background-color: var(--components-banner-neutral-background-color);
		--_border-color: var(--components-banner-neutral-border-color);
		--_border-width: var(--primitives-border-width-thin);
		--_icon-color: var(--components-banner-neutral-icon-color);
		--_icon-size: var(--primitives-space-32);
		--_content-color: var(--components-banner-content-color);
		--_content-secondary-color: var(--components-banner-content-secondary-color);
		--_text-icon-offset: calc((var(--_icon-size) - var(--primitives-font-size-100) * var(--primitives-line-height-tight)) / 2);
		--_column-gap: var(--primitives-space-8);
		--_dismissible-padding-right: var(--primitives-space-44);
		--_main-gap: var(--primitives-space-8);
		--_actions-gap: var(--primitives-space-4);

		${inheritedTextReset}
		box-sizing: border-box;
		display: grid;
		position: relative;
		/* Inset box-shadow paints the 1px edge inside the radius without
		   taking layout space, matching the highlight pattern nldd-box
		   uses. forced-colors fallback below restores a real border for
		   Windows High Contrast users (box-shadow is dropped there). */
		border-radius: var(--_corner-radius);
		box-shadow: inset 0 0 0 var(--_border-width) var(--_border-color);
		background-color: var(--_background-color);
		width: 100%;
		padding: var(--_padding);
		grid-template-columns: auto 1fr;
		gap: var(--_column-gap);
		color: var(--_content-color);
	}

	:host([dismissible]) {
		padding-right: var(--_dismissible-padding-right);
	}

	:host([hidden]) {
		display: none;
	}

	:host([variant="accent"]) {
		--_background-color: var(--components-banner-accent-background-color);
		--_border-color: var(--components-banner-accent-border-color);
		--_icon-color: var(--components-banner-accent-icon-color);
	}

	:host([variant="success"]) {
		--_background-color: var(--components-banner-success-background-color);
		--_border-color: var(--components-banner-success-border-color);
		--_icon-color: var(--components-banner-success-icon-color);
	}

	:host([variant="warning"]) {
		--_background-color: var(--components-banner-warning-background-color);
		--_border-color: var(--components-banner-warning-border-color);
		--_icon-color: var(--components-banner-warning-icon-color);
	}

	:host([variant="critical"]) {
		--_background-color: var(--components-banner-critical-background-color);
		--_border-color: var(--components-banner-critical-border-color);
		--_icon-color: var(--components-banner-critical-icon-color);
	}


	/* # Icon */

	.banner__icon {
		display: flex;
		grid-column: 1;
		grid-row: 1;
		width: var(--_icon-size);
		align-items: flex-start;
		color: var(--_icon-color);
	}


	/* # Main */

	.banner__main {
		display: flex;
		grid-column: 2;
		grid-row: 1;
		min-width: 0;
		flex-direction: column;
		gap: var(--_main-gap);
	}

	.banner__heading {
		display: flex;
		padding-top: var(--_text-icon-offset);
		flex-direction: column;
	}

	.banner__heading:has(.banner__supporting-text) {
		padding-top: calc(var(--_text-icon-offset) - var(--primitives-space-2));
	}

	.banner__text {
		margin: 0;
		font: var(--primitives-font-body-md-bold-tight);
		text-wrap: pretty;
	}

	.banner__supporting-text {
		margin: 0;
		color: var(--_content-secondary-color);
		font: var(--primitives-font-body-md-regular-tight);
		text-wrap: pretty;
	}

	.banner__content {
		display: contents;
	}

	.banner__content[hidden] {
		display: none;
	}

	.banner__actions {
		display: flex;
		margin-top: var(--_actions-gap);
	}

	.banner__actions[hidden] {
		display: none;
	}


	/* # Dismiss */

	.banner__dismiss-button {
		display: flex;
		position: absolute;
		top: 0;
		right: 0;
	}


	/* # Accessibility
	   forced-colors / Windows High Contrast strips box-shadow, so the inset
	   edge would disappear and the banner would lose its semantic frame
	   against the system background. Restore it with a real border in that
	   mode. CanvasText is the system foreground color so the border always
	   meets contrast. */

	@media (forced-colors: active) {
		:host {
			border: var(--_border-width) solid CanvasText;
		}
	}
`;

  // node_modules/@nldd/design-system/dist/components/status-and-feedback/banner/banner.template.js
  init_lit();
  function bannerTemplate(component) {
    const heading = (text) => {
      switch (component.headingLevel) {
        case 1:
          return b2`<h1 class="banner__text">${text}</h1>`;
        case 2:
          return b2`<h2 class="banner__text">${text}</h2>`;
        case 3:
          return b2`<h3 class="banner__text">${text}</h3>`;
        case 4:
          return b2`<h4 class="banner__text">${text}</h4>`;
        case 5:
          return b2`<h5 class="banner__text">${text}</h5>`;
        case 6:
          return b2`<h6 class="banner__text">${text}</h6>`;
        default:
          return b2`<p class="banner__text">${text}</p>`;
      }
    };
    return b2`
		<div class="banner__icon" aria-hidden="true">
			<nldd-icon name=${component._resolvedIcon}></nldd-icon>
		</div>
		<div class="banner__main">
			${component.text || component.supportingText ? b2`
				<div class="banner__heading">
					${component.text ? heading(component.text) : A}
					${component.supportingText ? b2`
						<p class="banner__supporting-text">${component.supportingText}</p>
					` : A}
				</div>
			` : A}
			<div class="banner__content" ?hidden=${!component._hasContent}>
				<slot></slot>
			</div>
			<div class="banner__actions" ?hidden=${!component._hasActions}>
				<nldd-button-group orientation="horizontal">
					<slot name="actions"></slot>
				</nldd-button-group>
			</div>
		</div>
		${component.dismissible ? b2`
			<div class="banner__dismiss-button">
				<nldd-icon-button
					icon="dismiss-small"
					variant="neutral-transparent"
					size="md"
					accessible-label=${component._t("components.banner.dismiss-action")}
					@click=${component._onDismissClick}
				></nldd-icon-button>
			</div>
		` : A}
	`;
  }

  // node_modules/@nldd/design-system/dist/components/status-and-feedback/banner/banner.i18n.js
  var nlddBannerTranslations = {
    "components.banner.dismiss-action": "Sluit"
  };

  // node_modules/@nldd/design-system/dist/components/status-and-feedback/banner/banner.js
  init_icon();
  init_button_group();
  init_icon_button();
  var __decorate89 = function(decorators, target, key, desc) {
    var c6 = arguments.length, r6 = c6 < 3 ? target : desc === null ? desc = Object.getOwnPropertyDescriptor(target, key) : desc, d3;
    if (typeof Reflect === "object" && typeof Reflect.decorate === "function") r6 = Reflect.decorate(decorators, target, key, desc);
    else for (var i8 = decorators.length - 1; i8 >= 0; i8--) if (d3 = decorators[i8]) r6 = (c6 < 3 ? d3(r6) : c6 > 3 ? d3(target, key, r6) : d3(target, key)) || r6;
    return c6 > 3 && r6 && Object.defineProperty(target, key, r6), r6;
  };
  var DEFAULT_ICONS = {
    neutral: "info-circle-filled",
    accent: "info-circle-filled",
    success: "check-circle-filled",
    warning: "exclamation-triangle-filled",
    critical: "exclamation-circle-filled"
  };
  var NLDDBanner = class NLDDBanner2 extends i4 {
    constructor() {
      super();
      this.variant = "neutral";
      this.icon = "";
      this.text = "";
      this.supportingText = "";
      this.headingLevel = null;
      this.dismissible = false;
      this.translations = {};
      this._hasContent = false;
      this._hasActions = false;
      const initialVariant = this.getAttribute("variant");
      this._applyAriaForVariant(initialVariant ?? "neutral");
    }
    _t(key) {
      return this.translations[key] ?? nlddBannerTranslations[key];
    }
    get _resolvedIcon() {
      return this.icon || DEFAULT_ICONS[this.variant];
    }
    /* The host gets role+aria-live based on variant. Critical interrupts
     * (assertive); other variants are polite announcements. Not exposed
     * for override — consumers who need quieter semantics should pick a
     * different component.
     *
     * Note on dynamic variant changes: AT engines vary on whether they
     * re-announce when a live-region's role / aria-live changes after the
     * element is already in the AT tree. Constructor-time setup covers
     * the common case (variant fixed at insertion). Toggling variant at
     * runtime updates the visual treatment immediately but the new
     * announcement may not fire on every screen reader. */
    updated(changed) {
      if (changed.has("variant"))
        this._applyAriaForVariant(this.variant);
    }
    _applyAriaForVariant(variant) {
      if (variant === "critical") {
        this.setAttribute("role", "alert");
        this.removeAttribute("aria-live");
      } else {
        this.setAttribute("role", "status");
        this.setAttribute("aria-live", "polite");
      }
      this.setAttribute("aria-atomic", "true");
    }
    /** Re-runs every connectedCallback (i.e. also after a move-and-reinsert),
     *  unlike firstUpdated which is one-shot. Without this, slotchange after
     *  a reconnect would never update _hasContent / _hasActions because the
     *  listeners were torn down in disconnectedCallback and never re-attached. */
    _attachSlotListeners() {
      const contentSlot = this.shadowRoot?.querySelector("slot:not([name])");
      const actionsSlot = this.shadowRoot?.querySelector('slot[name="actions"]');
      if (!contentSlot && !actionsSlot)
        return;
      const hasMeaningfulContent = (slot) => {
        const nodes = slot?.assignedNodes({ flatten: true }) ?? [];
        return nodes.some((n7) => n7.nodeType === Node.ELEMENT_NODE || n7.nodeType === Node.TEXT_NODE && (n7.textContent?.trim() ?? "") !== "");
      };
      this._syncContent = () => {
        this._hasContent = hasMeaningfulContent(contentSlot);
      };
      this._syncActions = () => {
        this._hasActions = hasMeaningfulContent(actionsSlot);
      };
      contentSlot?.addEventListener("slotchange", this._syncContent);
      actionsSlot?.addEventListener("slotchange", this._syncActions);
      this._syncContent();
      this._syncActions();
    }
    firstUpdated() {
      this._attachSlotListeners();
    }
    connectedCallback() {
      super.connectedCallback();
      if (this.shadowRoot && !this._syncContent) {
        this._attachSlotListeners();
      }
    }
    disconnectedCallback() {
      const contentSlot = this.shadowRoot?.querySelector("slot:not([name])");
      const actionsSlot = this.shadowRoot?.querySelector('slot[name="actions"]');
      if (this._syncContent)
        contentSlot?.removeEventListener("slotchange", this._syncContent);
      if (this._syncActions)
        actionsSlot?.removeEventListener("slotchange", this._syncActions);
      this._syncContent = void 0;
      this._syncActions = void 0;
      super.disconnectedCallback();
    }
    _onDismissClick() {
      this.dispatchEvent(new CustomEvent("dismiss", { bubbles: true, composed: true }));
    }
    render() {
      return bannerTemplate(this);
    }
  };
  NLDDBanner.styles = bannerStyles;
  __decorate89([
    n4({ type: String, reflect: true })
  ], NLDDBanner.prototype, "variant", void 0);
  __decorate89([
    n4({ type: String, reflect: true })
  ], NLDDBanner.prototype, "icon", void 0);
  __decorate89([
    n4({ type: String })
  ], NLDDBanner.prototype, "text", void 0);
  __decorate89([
    n4({ type: String, attribute: "supporting-text" })
  ], NLDDBanner.prototype, "supportingText", void 0);
  __decorate89([
    n4({ type: Number, reflect: true, attribute: "heading-level" })
  ], NLDDBanner.prototype, "headingLevel", void 0);
  __decorate89([
    n4({ type: Boolean, reflect: true })
  ], NLDDBanner.prototype, "dismissible", void 0);
  __decorate89([
    n4({ type: Object })
  ], NLDDBanner.prototype, "translations", void 0);
  __decorate89([
    r5()
  ], NLDDBanner.prototype, "_hasContent", void 0);
  __decorate89([
    r5()
  ], NLDDBanner.prototype, "_hasActions", void 0);
  NLDDBanner = __decorate89([
    t3("nldd-banner")
  ], NLDDBanner);

  // node_modules/@nldd/design-system/dist/components/status-and-feedback/badge/badge.js
  init_lit();
  init_decorators();

  // node_modules/@nldd/design-system/dist/components/status-and-feedback/badge/badge.styles.js
  init_lit();
  init_slotted_reset();
  var badgeStyles = i`


	/* # Host */

	:host {
		--_background-color: var(--components-badge-critical-background-color);
		--_border-color: var(--components-badge-critical-border-color);
		--_border-width: var(--components-badge-border-width);
		--_height: var(--primitives-space-20);
		--_inline-padding: var(--primitives-space-6);
		--_gap: var(--primitives-space-3);
		--_content-color: var(--components-badge-critical-content-color);
		--_font: var(--primitives-font-body-xs-medium-flat);
		--_dot-size: var(--primitives-space-10);
		--_icon-size: var(--primitives-space-14);
		--_icon-offset-correction: var(--primitives-space-1);

		${inheritedTextReset}
		display: inline-flex;
		vertical-align: middle;
	}

	:host([size="sm"]) {
		--_height: var(--primitives-space-16);
		--_inline-padding: var(--primitives-space-4);
		--_gap: var(--primitives-space-2);
		--_font: var(--primitives-font-body-xxs-medium-flat);
		--_dot-size: var(--primitives-space-6);
		--_icon-size: var(--primitives-space-12);
	}

	/* ## Color */

	:host([color="accent"]) {
		--_background-color: var(--components-badge-accent-background-color);
		--_border-color: var(--components-badge-accent-border-color);
		--_content-color: var(--components-badge-accent-content-color);
	}

	:host([color="neutral"]) {
		--_background-color: var(--components-badge-neutral-background-color);
		--_border-color: var(--components-badge-neutral-border-color);
		--_content-color: var(--components-badge-neutral-content-color);
	}

	:host([color="warning"]) {
		--_background-color: var(--components-badge-warning-background-color);
		--_border-color: var(--components-badge-warning-border-color);
		--_content-color: var(--components-badge-warning-content-color);
	}

	:host([color="success"]) {
		--_background-color: var(--components-badge-success-background-color);
		--_border-color: var(--components-badge-success-border-color);
		--_content-color: var(--components-badge-success-content-color);
	}


	/* ## Color — rijkskleuren */

	:host([color="coolgray"]) {
		--_background-color: var(--components-badge-coolgray-background-color);
		--_border-color: var(--components-badge-coolgray-border-color);
		--_content-color: var(--components-badge-coolgray-content-color);
	}

	:host([color="lintblauw"]) {
		--_background-color: var(--components-badge-lintblauw-background-color);
		--_border-color: var(--components-badge-lintblauw-border-color);
		--_content-color: var(--components-badge-lintblauw-content-color);
	}

	:host([color="donkerblauw"]) {
		--_background-color: var(--components-badge-donkerblauw-background-color);
		--_border-color: var(--components-badge-donkerblauw-border-color);
		--_content-color: var(--components-badge-donkerblauw-content-color);
	}

	:host([color="hemelblauw"]) {
		--_background-color: var(--components-badge-hemelblauw-background-color);
		--_border-color: var(--components-badge-hemelblauw-border-color);
		--_content-color: var(--components-badge-hemelblauw-content-color);
	}

	:host([color="lichtblauw"]) {
		--_background-color: var(--components-badge-lichtblauw-background-color);
		--_border-color: var(--components-badge-lichtblauw-border-color);
		--_content-color: var(--components-badge-lichtblauw-content-color);
	}

	:host([color="paars"]) {
		--_background-color: var(--components-badge-paars-background-color);
		--_border-color: var(--components-badge-paars-border-color);
		--_content-color: var(--components-badge-paars-content-color);
	}

	:host([color="violet"]) {
		--_background-color: var(--components-badge-violet-background-color);
		--_border-color: var(--components-badge-violet-border-color);
		--_content-color: var(--components-badge-violet-content-color);
	}

	:host([color="robijnrood"]) {
		--_background-color: var(--components-badge-robijnrood-background-color);
		--_border-color: var(--components-badge-robijnrood-border-color);
		--_content-color: var(--components-badge-robijnrood-content-color);
	}

	:host([color="roze"]) {
		--_background-color: var(--components-badge-roze-background-color);
		--_border-color: var(--components-badge-roze-border-color);
		--_content-color: var(--components-badge-roze-content-color);
	}

	:host([color="rood"]) {
		--_background-color: var(--components-badge-rood-background-color);
		--_border-color: var(--components-badge-rood-border-color);
		--_content-color: var(--components-badge-rood-content-color);
	}

	:host([color="oranje"]) {
		--_background-color: var(--components-badge-oranje-background-color);
		--_border-color: var(--components-badge-oranje-border-color);
		--_content-color: var(--components-badge-oranje-content-color);
	}

	:host([color="donkergeel"]) {
		--_background-color: var(--components-badge-donkergeel-background-color);
		--_border-color: var(--components-badge-donkergeel-border-color);
		--_content-color: var(--components-badge-donkergeel-content-color);
	}

	:host([color="geel"]) {
		--_background-color: var(--components-badge-geel-background-color);
		--_border-color: var(--components-badge-geel-border-color);
		--_content-color: var(--components-badge-geel-content-color);
	}

	:host([color="donkerbruin"]) {
		--_background-color: var(--components-badge-donkerbruin-background-color);
		--_border-color: var(--components-badge-donkerbruin-border-color);
		--_content-color: var(--components-badge-donkerbruin-content-color);
	}

	:host([color="bruin"]) {
		--_background-color: var(--components-badge-bruin-background-color);
		--_border-color: var(--components-badge-bruin-border-color);
		--_content-color: var(--components-badge-bruin-content-color);
	}

	:host([color="donkergroen"]) {
		--_background-color: var(--components-badge-donkergroen-background-color);
		--_border-color: var(--components-badge-donkergroen-border-color);
		--_content-color: var(--components-badge-donkergroen-content-color);
	}

	:host([color="groen"]) {
		--_background-color: var(--components-badge-groen-background-color);
		--_border-color: var(--components-badge-groen-border-color);
		--_content-color: var(--components-badge-groen-content-color);
	}

	:host([color="mosgroen"]) {
		--_background-color: var(--components-badge-mosgroen-background-color);
		--_border-color: var(--components-badge-mosgroen-border-color);
		--_content-color: var(--components-badge-mosgroen-content-color);
	}

	:host([color="mintgroen"]) {
		--_background-color: var(--components-badge-mintgroen-background-color);
		--_border-color: var(--components-badge-mintgroen-border-color);
		--_content-color: var(--components-badge-mintgroen-content-color);
	}

	:host([hidden]) {
		display: none;
	}


	/* # Block */

	.badge {
		box-sizing: border-box;
		display: inline-flex;
		border-radius: var(--components-badge-corner-radius);
		box-shadow:
			0 0 0 1px var(--context-parent-background-color, var(--semantics-surfaces-base-background-color)),
			inset 0 0 0 var(--_border-width) var(--_border-color);
		background-color: var(--_background-color);
		min-width: var(--_height);
		height: var(--_height);
		padding: 0 var(--_inline-padding);
		gap: var(--_gap);
		align-items: center;
		justify-content: center;
		color: var(--_content-color);
		font: var(--_font);
		white-space: nowrap;
	}

	@media (forced-colors: active) {
		.badge {
			border: 1px solid CanvasText;
			background-color: Canvas;
			color: CanvasText;
		}
	}

	.badge--dot {
		min-width: var(--_dot-size);
		width: var(--_dot-size);
		height: var(--_dot-size);
		padding: 0;
	}

	.badge--icon-only {
		min-width: var(--_height);
		width: var(--_height);
		padding: 0;
	}


	/* # Elements */

	.badge__icon {
		display: inline-flex;
		width: var(--_icon-size);
		height: var(--_icon-size);
		flex-shrink: 0;
		align-items: center;
		justify-content: center;
	}

	.badge__icon:has(+ .badge__text) {
		margin-left: calc((var(--_height) - var(--_icon-size)) / 2 - var(--_inline-padding) + var(--_icon-offset-correction));
	}

	.badge__icon nldd-icon {
		width: 100%;
		height: 100%;
	}

	.badge__text {
		display: inline-block;
	}
`;

  // node_modules/@nldd/design-system/dist/components/status-and-feedback/badge/badge.template.js
  init_lit();
  function template24(component) {
    const modifiers = [];
    if (component._isDot)
      modifiers.push("badge--dot");
    if (component._isIconOnly)
      modifiers.push("badge--icon-only");
    const classes = ["badge", ...modifiers].join(" ");
    const needsAriaImg = component._isDot || component._isIconOnly;
    return b2`
		<span class=${classes}
			role=${needsAriaImg ? "img" : A}
			aria-label=${needsAriaImg ? component._ariaLabel : A}
		>
			${component.icon ? b2`
				<span class="badge__icon">
					<nldd-icon name=${component.icon}></nldd-icon>
				</span>
			` : ""}
			${component._hasText ? b2`<span class="badge__text">${component._displayValue}</span>` : ""}
		</span>
	`;
  }

  // node_modules/@nldd/design-system/dist/components/status-and-feedback/badge/badge.i18n.js
  var nlddBadgeTranslations = {
    "components.badge.notification-text": "Notificatie"
  };

  // node_modules/@nldd/design-system/dist/components/status-and-feedback/badge/badge.js
  init_icon();
  var __decorate90 = function(decorators, target, key, desc) {
    var c6 = arguments.length, r6 = c6 < 3 ? target : desc === null ? desc = Object.getOwnPropertyDescriptor(target, key) : desc, d3;
    if (typeof Reflect === "object" && typeof Reflect.decorate === "function") r6 = Reflect.decorate(decorators, target, key, desc);
    else for (var i8 = decorators.length - 1; i8 >= 0; i8--) if (d3 = decorators[i8]) r6 = (c6 < 3 ? d3(r6) : c6 > 3 ? d3(target, key, r6) : d3(target, key)) || r6;
    return c6 > 3 && r6 && Object.defineProperty(target, key, r6), r6;
  };
  var NLDDBadge = class NLDDBadge2 extends withTranslations(i4, nlddBadgeTranslations) {
    constructor() {
      super(...arguments);
      this.color = "critical";
      this.size = "md";
      this.text = "";
      this.number = void 0;
      this.max = 99;
      this.icon = "";
      this.accessibleLabel = "";
    }
    get _hasText() {
      return !!this.text || typeof this.number === "number";
    }
    get _isDot() {
      return !this._hasText && !this.icon;
    }
    get _isIconOnly() {
      return !!this.icon && !this._hasText;
    }
    get _displayValue() {
      if (this.text)
        return this.text;
      if (typeof this.number === "number") {
        return this.number > this.max ? `${this.max}+` : String(this.number);
      }
      return "";
    }
    get _ariaLabel() {
      if (this.accessibleLabel)
        return this.accessibleLabel;
      if (this._hasText)
        return this._displayValue;
      return this._t("components.badge.notification-text");
    }
    render() {
      return template24(this);
    }
  };
  NLDDBadge.styles = badgeStyles;
  __decorate90([
    n4({ type: String, reflect: true })
  ], NLDDBadge.prototype, "color", void 0);
  __decorate90([
    n4({ type: String, reflect: true })
  ], NLDDBadge.prototype, "size", void 0);
  __decorate90([
    n4({ type: String })
  ], NLDDBadge.prototype, "text", void 0);
  __decorate90([
    n4({ type: Number, reflect: true })
  ], NLDDBadge.prototype, "number", void 0);
  __decorate90([
    n4({ type: Number, reflect: true })
  ], NLDDBadge.prototype, "max", void 0);
  __decorate90([
    n4({ type: String })
  ], NLDDBadge.prototype, "icon", void 0);
  __decorate90([
    n4({ type: String, attribute: "accessible-label" })
  ], NLDDBadge.prototype, "accessibleLabel", void 0);
  NLDDBadge = __decorate90([
    t3("nldd-badge")
  ], NLDDBadge);

  // node_modules/@nldd/design-system/dist/components/status-and-feedback/progress-bar/progress-bar.js
  init_lit();
  init_decorators();

  // node_modules/@nldd/design-system/dist/components/status-and-feedback/progress-bar/progress-bar.styles.js
  init_lit();
  init_slotted_reset();
  var progressBarStyles = i`


	/* # Host */

	:host {
		--_height: var(--components-progress-bar-md-height);
		--_track-background-color: var(--components-progress-bar-track-background-color);
		--_track-border-color: var(--components-progress-bar-track-border-color);
		--_track-border-width: var(--components-progress-bar-track-border-width);
		--_corner-radius: var(--components-progress-bar-corner-radius);
		--_segment-indicator-gap: var(--components-progress-bar-progress-segment-indicator-gap);
		--_caption-gap: var(--components-progress-bar-caption-gap);
		--_text-color: var(--components-progress-bar-text-color);
		--_supporting-text-color: var(--components-progress-bar-supporting-text-color);
		--_text-font: var(--components-progress-bar-text-font);
		--_supporting-text-font: var(--components-progress-bar-supporting-text-font);
		--_indeterminate-background-color: var(--components-progress-bar-accent-background-color);
		--_indeterminate-border-color: var(--components-progress-bar-accent-border-color);
		--_indeterminate-border-width: var(--components-progress-bar-segment-indicator-border-width);
		--_indeterminate-bar-width: 20%;
		--_indeterminate-duration: 800ms;

		${inheritedTextReset}
		box-sizing: border-box;
		display: flex;
		width: 100%;
		flex-direction: column;
		gap: var(--_caption-gap);
	}

	:host([hidden]) {
		display: none;
	}

	:host([size="sm"]) { --_height: var(--components-progress-bar-sm-height); }
	:host([size="md"]) { --_height: var(--components-progress-bar-md-height); }
	:host([size="lg"]) { --_height: var(--components-progress-bar-lg-height); }

	:host([mode="distribution"]) {
		--_segment-indicator-gap: var(--components-progress-bar-distribution-segment-indicator-gap);
		--_corner-radius: var(--components-progress-bar-distribution-corner-radius);
	}


	/* ## Indeterminate fill + border colour follow the variant; default is accent (blue) */

	:host([color="neutral"]) { --_indeterminate-background-color: var(--components-progress-bar-neutral-background-color); --_indeterminate-border-color: var(--components-progress-bar-neutral-border-color); }
	:host([color="accent"]) { --_indeterminate-background-color: var(--components-progress-bar-accent-background-color); --_indeterminate-border-color: var(--components-progress-bar-accent-border-color); }
	:host([color="success"]) { --_indeterminate-background-color: var(--components-progress-bar-success-background-color); --_indeterminate-border-color: var(--components-progress-bar-success-border-color); }
	:host([color="warning"]) { --_indeterminate-background-color: var(--components-progress-bar-warning-background-color); --_indeterminate-border-color: var(--components-progress-bar-warning-border-color); }
	:host([color="critical"]) { --_indeterminate-background-color: var(--components-progress-bar-critical-background-color); --_indeterminate-border-color: var(--components-progress-bar-critical-border-color); }

	:host([color="coolgray"]) { --_indeterminate-background-color: var(--components-progress-bar-coolgray-background-color); --_indeterminate-border-color: var(--components-progress-bar-coolgray-border-color); }
	:host([color="lintblauw"]) { --_indeterminate-background-color: var(--components-progress-bar-lintblauw-background-color); --_indeterminate-border-color: var(--components-progress-bar-lintblauw-border-color); }
	:host([color="donkerblauw"]) { --_indeterminate-background-color: var(--components-progress-bar-donkerblauw-background-color); --_indeterminate-border-color: var(--components-progress-bar-donkerblauw-border-color); }
	:host([color="hemelblauw"]) { --_indeterminate-background-color: var(--components-progress-bar-hemelblauw-background-color); --_indeterminate-border-color: var(--components-progress-bar-hemelblauw-border-color); }
	:host([color="lichtblauw"]) { --_indeterminate-background-color: var(--components-progress-bar-lichtblauw-background-color); --_indeterminate-border-color: var(--components-progress-bar-lichtblauw-border-color); }
	:host([color="paars"]) { --_indeterminate-background-color: var(--components-progress-bar-paars-background-color); --_indeterminate-border-color: var(--components-progress-bar-paars-border-color); }
	:host([color="violet"]) { --_indeterminate-background-color: var(--components-progress-bar-violet-background-color); --_indeterminate-border-color: var(--components-progress-bar-violet-border-color); }
	:host([color="robijnrood"]) { --_indeterminate-background-color: var(--components-progress-bar-robijnrood-background-color); --_indeterminate-border-color: var(--components-progress-bar-robijnrood-border-color); }
	:host([color="roze"]) { --_indeterminate-background-color: var(--components-progress-bar-roze-background-color); --_indeterminate-border-color: var(--components-progress-bar-roze-border-color); }
	:host([color="rood"]) { --_indeterminate-background-color: var(--components-progress-bar-rood-background-color); --_indeterminate-border-color: var(--components-progress-bar-rood-border-color); }
	:host([color="oranje"]) { --_indeterminate-background-color: var(--components-progress-bar-oranje-background-color); --_indeterminate-border-color: var(--components-progress-bar-oranje-border-color); }
	:host([color="donkergeel"]) { --_indeterminate-background-color: var(--components-progress-bar-donkergeel-background-color); --_indeterminate-border-color: var(--components-progress-bar-donkergeel-border-color); }
	:host([color="geel"]) { --_indeterminate-background-color: var(--components-progress-bar-geel-background-color); --_indeterminate-border-color: var(--components-progress-bar-geel-border-color); }
	:host([color="donkerbruin"]) { --_indeterminate-background-color: var(--components-progress-bar-donkerbruin-background-color); --_indeterminate-border-color: var(--components-progress-bar-donkerbruin-border-color); }
	:host([color="bruin"]) { --_indeterminate-background-color: var(--components-progress-bar-bruin-background-color); --_indeterminate-border-color: var(--components-progress-bar-bruin-border-color); }
	:host([color="donkergroen"]) { --_indeterminate-background-color: var(--components-progress-bar-donkergroen-background-color); --_indeterminate-border-color: var(--components-progress-bar-donkergroen-border-color); }
	:host([color="groen"]) { --_indeterminate-background-color: var(--components-progress-bar-groen-background-color); --_indeterminate-border-color: var(--components-progress-bar-groen-border-color); }
	:host([color="mosgroen"]) { --_indeterminate-background-color: var(--components-progress-bar-mosgroen-background-color); --_indeterminate-border-color: var(--components-progress-bar-mosgroen-border-color); }
	:host([color="mintgroen"]) { --_indeterminate-background-color: var(--components-progress-bar-mintgroen-background-color); --_indeterminate-border-color: var(--components-progress-bar-mintgroen-border-color); }


	/* # Caption */

	.progress-bar__caption {
		display: flex;
		gap: var(--_caption-gap);
		justify-content: space-between;
		align-items: baseline;
		color: var(--_text-color);
	}

	.progress-bar__text {
		font: var(--_text-font);
	}

	.progress-bar__supporting-text {
		color: var(--_supporting-text-color);
		font: var(--_supporting-text-font);
		font-variant-numeric: tabular-nums;
	}


	/* # Track */

	.progress-bar__track {
		box-sizing: border-box;
		display: flex;
		position: relative;
		border-radius: var(--_corner-radius);
		box-shadow: inset 0 0 0 var(--_track-border-width) var(--_track-border-color);
		background-color: var(--_track-background-color);
		width: 100%;
		height: var(--_height);
		overflow: hidden;
		gap: var(--_segment-indicator-gap);
		container-type: inline-size;
	}

	slot {
		display: contents;
	}


	/* # Indeterminate
	   Knight Rider style: track filled with a translucent variant tint,
	   a solid bar bounces left-right. ease-in-out + alternate
	   gives the typical "scanner" acceleration at the ends. */

	/* Indicator has no own bg — the track (and its inset border)
	   simply show through. Only the K.I.T.T. bar is visible.
	   On fade-out the bar fades away and track + border remain. */

	.progress-bar__indeterminate-indicator {
		position: absolute;
		inset: 0;
		opacity: 1;
		transition: opacity var(--primitives-transition-duration-slow) ease-out;
	}

	.progress-bar__indeterminate-indicator.is-fading-out {
		opacity: 0;
	}

	.progress-bar__indeterminate-indicator.is-fading-in {
		animation: progress-bar-indicator-fade-in var(--primitives-transition-duration-slow) ease-out;
	}

	@keyframes progress-bar-indicator-fade-in {
		from { opacity: 0; }
		to { opacity: 1; }
	}

	/* K.I.T.T. bar. translateX uses calc(100cqw - 100%): cqw is the
	   container (track) width, 100% is the bar's own width. */

	.progress-bar__indeterminate-indicator::before {
		content: '';
		position: absolute;
		inset-block: 0;
		left: 0;
		border-radius: var(--_corner-radius);
		background: var(--_indeterminate-background-color);
		box-shadow: inset 0 0 0 var(--_indeterminate-border-width) var(--_indeterminate-border-color);
		width: var(--_indeterminate-bar-width);
		transform: translateX(0);
		/* Duration scales with track width via the container queries below.
		   Wider tracks need longer sweeps so the perceived velocity feels
		   constant — a fast 800 ms cycle reads as frantic on a 1000 px bar
		   and as too slow on a 200 px bar at the same wall-clock time. */
		animation: progress-bar-indeterminate var(--_indeterminate-duration) ease-in-out infinite alternate;
	}

	@container (min-width: 401px) and (max-width: 720px) {
		.progress-bar__indeterminate-indicator::before {
			--_indeterminate-duration: 1000ms;
		}
	}

	@container (min-width: 721px) {
		.progress-bar__indeterminate-indicator::before {
			--_indeterminate-duration: 1200ms;
		}
	}

	@keyframes progress-bar-indeterminate {
		0% { transform: translateX(0); }
		100% { transform: translateX(calc(100cqw - 100%)); }
	}


	/* # Accessibility */

	@media (prefers-reduced-motion: reduce) {
		.progress-bar__indeterminate-indicator::before {
			display: none;
		}

		.progress-bar__indeterminate-indicator {
			animation: progress-bar-indeterminate-pulse 2s ease-in-out infinite;
		}

		@keyframes progress-bar-indeterminate-pulse {
			0%, 100% { background-color: color-mix(in srgb, var(--_indeterminate-background-color) 20%, transparent); }
			50% { background-color: color-mix(in srgb, var(--_indeterminate-background-color) 50%, transparent); }
		}
	}

	@media (forced-colors: active) {
		.progress-bar__track {
			border: 1px solid CanvasText;
		}
	}
`;
  var progressBarSegmentIndicatorStyles = i`


	/* # Host */

	:host {
		--_width: var(--context-progress-bar-segment-indicator-width, 0%);
		--_min-width: var(--components-progress-bar-segment-indicator-min-width);
		--_background-color: var(--components-progress-bar-accent-background-color);
		--_border-color: var(--components-progress-bar-accent-border-color);
		--_border-width: var(--components-progress-bar-segment-indicator-border-width);

		box-sizing: border-box;
		display: block;
		position: relative;
		box-shadow: inset 0 0 0 var(--_border-width) var(--_border-color);
		background-color: var(--_background-color);
		width: var(--_width);
		min-width: var(--_min-width);
		height: 100%;
		overflow: hidden;
		transition: width var(--primitives-transition-duration-medium) ease-out;
	}

	:host([value="0"]),
	:host(:not([value])) {
		display: none;
	}


	/* ## Grow / shrink
	   Set by the parent (data-grow / data-shrink) on the transitions between
	   indeterminate and determinate. */

	:host([data-grow]) {
		animation: progress-bar-segment-indicator-grow var(--primitives-transition-duration-slow) ease-out;
	}

	:host([data-shrink]) {
		animation: progress-bar-segment-indicator-shrink var(--primitives-transition-duration-slow) ease-out forwards;
	}

	@keyframes progress-bar-segment-indicator-grow {
		from { width: 0%; }
		to { width: var(--_width); }
	}

	@keyframes progress-bar-segment-indicator-shrink {
		from { width: var(--_width); opacity: 1; }
		to { width: 0%; opacity: 0; }
	}


	/* ## Rounding per mode
	   Progress mode: every segment is its own capsule. Distribution mode:
	   segments use a small radius matching the track's outer corners.
	   data-mode is set by the parent. */

	:host([data-mode="progress"]) {
		border-radius: var(--components-progress-bar-corner-radius);
	}

	:host([data-mode="distribution"]) {
		border-radius: var(--components-progress-bar-distribution-corner-radius);
	}


	/* ## Variants — semantic */

	:host([color="neutral"]) {
		--_background-color: var(--components-progress-bar-neutral-background-color);
		--_border-color: var(--components-progress-bar-neutral-border-color);
	}
	:host([color="accent"]) {
		--_background-color: var(--components-progress-bar-accent-background-color);
		--_border-color: var(--components-progress-bar-accent-border-color);
	}
	:host([color="success"]) {
		--_background-color: var(--components-progress-bar-success-background-color);
		--_border-color: var(--components-progress-bar-success-border-color);
	}
	:host([color="warning"]) {
		--_background-color: var(--components-progress-bar-warning-background-color);
		--_border-color: var(--components-progress-bar-warning-border-color);
	}
	:host([color="critical"]) {
		--_background-color: var(--components-progress-bar-critical-background-color);
		--_border-color: var(--components-progress-bar-critical-border-color);
	}


	/* ## Variants — Rijkskleuren */

	:host([color="coolgray"]) {
		--_background-color: var(--components-progress-bar-coolgray-background-color);
		--_border-color: var(--components-progress-bar-coolgray-border-color);
	}
	:host([color="lintblauw"]) {
		--_background-color: var(--components-progress-bar-lintblauw-background-color);
		--_border-color: var(--components-progress-bar-lintblauw-border-color);
	}
	:host([color="donkerblauw"]) {
		--_background-color: var(--components-progress-bar-donkerblauw-background-color);
		--_border-color: var(--components-progress-bar-donkerblauw-border-color);
	}
	:host([color="hemelblauw"]) {
		--_background-color: var(--components-progress-bar-hemelblauw-background-color);
		--_border-color: var(--components-progress-bar-hemelblauw-border-color);
	}
	:host([color="lichtblauw"]) {
		--_background-color: var(--components-progress-bar-lichtblauw-background-color);
		--_border-color: var(--components-progress-bar-lichtblauw-border-color);
	}
	:host([color="paars"]) {
		--_background-color: var(--components-progress-bar-paars-background-color);
		--_border-color: var(--components-progress-bar-paars-border-color);
	}
	:host([color="violet"]) {
		--_background-color: var(--components-progress-bar-violet-background-color);
		--_border-color: var(--components-progress-bar-violet-border-color);
	}
	:host([color="robijnrood"]) {
		--_background-color: var(--components-progress-bar-robijnrood-background-color);
		--_border-color: var(--components-progress-bar-robijnrood-border-color);
	}
	:host([color="roze"]) {
		--_background-color: var(--components-progress-bar-roze-background-color);
		--_border-color: var(--components-progress-bar-roze-border-color);
	}
	:host([color="rood"]) {
		--_background-color: var(--components-progress-bar-rood-background-color);
		--_border-color: var(--components-progress-bar-rood-border-color);
	}
	:host([color="oranje"]) {
		--_background-color: var(--components-progress-bar-oranje-background-color);
		--_border-color: var(--components-progress-bar-oranje-border-color);
	}
	:host([color="donkergeel"]) {
		--_background-color: var(--components-progress-bar-donkergeel-background-color);
		--_border-color: var(--components-progress-bar-donkergeel-border-color);
	}
	:host([color="geel"]) {
		--_background-color: var(--components-progress-bar-geel-background-color);
		--_border-color: var(--components-progress-bar-geel-border-color);
	}
	:host([color="donkerbruin"]) {
		--_background-color: var(--components-progress-bar-donkerbruin-background-color);
		--_border-color: var(--components-progress-bar-donkerbruin-border-color);
	}
	:host([color="bruin"]) {
		--_background-color: var(--components-progress-bar-bruin-background-color);
		--_border-color: var(--components-progress-bar-bruin-border-color);
	}
	:host([color="donkergroen"]) {
		--_background-color: var(--components-progress-bar-donkergroen-background-color);
		--_border-color: var(--components-progress-bar-donkergroen-border-color);
	}
	:host([color="groen"]) {
		--_background-color: var(--components-progress-bar-groen-background-color);
		--_border-color: var(--components-progress-bar-groen-border-color);
	}
	:host([color="mosgroen"]) {
		--_background-color: var(--components-progress-bar-mosgroen-background-color);
		--_border-color: var(--components-progress-bar-mosgroen-border-color);
	}
	:host([color="mintgroen"]) {
		--_background-color: var(--components-progress-bar-mintgroen-background-color);
		--_border-color: var(--components-progress-bar-mintgroen-border-color);
	}


	/* # Hover area (tooltip trigger)
	   Fills the segment; captures hover/focus for the wrapping nldd-tooltip. */

	.progress-bar-segment-indicator__tooltip-area {
		display: block;
		position: absolute;
		inset: 0;
	}


	/* # Accessibility */

	@media (prefers-reduced-motion: reduce) {
		:host {
			transition: none;
		}
	}

	@media (forced-colors: active) {
		:host {
			background-color: CanvasText;
			border: 1px solid CanvasText;
		}
	}
`;

  // node_modules/@nldd/design-system/dist/components/status-and-feedback/progress-bar/progress-bar.template.js
  init_lit();
  init_if_defined2();
  init_class_map2();
  function progressBarTemplate(component, onSlotChange) {
    const hasCaption = component._hasCaption;
    const isExiting = component._indeterminateExiting;
    const isEntering = component._indeterminateEntering;
    const showIndeterminateIndicator = (component.indeterminate || isExiting) && !component._hasSegmentIndicators;
    const showInternalSegmentIndicator = !component._hasSegmentIndicators && component.value !== null && component.value > 0 && (!component.indeterminate || isEntering);
    const ariaValueNow = component.indeterminate ? void 0 : Math.round(component._totalValue);
    const ariaLabel = component.text || component._t("components.progress-bar.label-text");
    return b2`
		${hasCaption ? b2`
			<div class="progress-bar__caption">
				<span class="progress-bar__text">${component.text}</span>
				${component.valueDisplay === "inline" && component._displayValue ? b2`<span class="progress-bar__supporting-text">${component._displayValue}</span>` : A}
			</div>
		` : A}
		<div class="progress-bar__track"
			role="progressbar"
			aria-label=${ariaLabel}
			aria-valuemin="0"
			aria-valuemax=${component.max}
			aria-valuenow=${o7(ariaValueNow)}
			aria-valuetext=${component._ariaValueText}
		>
			${showInternalSegmentIndicator ? b2`
				<nldd-progress-bar-segment-indicator class="progress-bar__segment-indicator"
					.value=${component.value}
					color=${component.color}
				></nldd-progress-bar-segment-indicator>
			` : A}
			<slot @slotchange=${onSlotChange}></slot>
			${showIndeterminateIndicator ? b2`
				<div class=${e8({
      "progress-bar__indeterminate-indicator": true,
      "is-fading-out": isExiting && !component.indeterminate,
      "is-fading-in": isEntering && component.indeterminate
    })}></div>
			` : A}
		</div>
	`;
  }

  // node_modules/@nldd/design-system/dist/components/status-and-feedback/progress-bar/progress-bar.i18n.js
  var nlddProgressBarTranslations = {
    "components.progress-bar.completed-suffix-text": "voltooid",
    "components.progress-bar.total-prefix-text": "Totaal",
    "components.progress-bar.loading-text": "Aan het laden",
    "components.progress-bar.label-text": "Voortgang"
  };

  // node_modules/@nldd/design-system/dist/components/status-and-feedback/progress-bar/progress-bar.js
  init_tooltip();
  var import_meta29 = {};
  var __decorate91 = function(decorators, target, key, desc) {
    var c6 = arguments.length, r6 = c6 < 3 ? target : desc === null ? desc = Object.getOwnPropertyDescriptor(target, key) : desc, d3;
    if (typeof Reflect === "object" && typeof Reflect.decorate === "function") r6 = Reflect.decorate(decorators, target, key, desc);
    else for (var i8 = decorators.length - 1; i8 >= 0; i8--) if (d3 = decorators[i8]) r6 = (c6 < 3 ? d3(r6) : c6 > 3 ? d3(target, key, r6) : d3(target, key)) || r6;
    return c6 > 3 && r6 && Object.defineProperty(target, key, r6), r6;
  };
  var INDETERMINATE_TRANSITION_MS = 300;
  var NLDDProgressBarSegmentIndicator = class NLDDProgressBarSegmentIndicator2 extends i4 {
    constructor() {
      super(...arguments);
      this.value = 0;
      this.color = "accent";
      this.name = "";
      this.tooltipText = "";
      this._autoTooltipText = "";
    }
    get _effectiveTooltip() {
      return this.tooltipText || this._autoTooltipText || "";
    }
    render() {
      const text = this._effectiveTooltip;
      if (!text) {
        return b2`<span class="progress-bar-segment-indicator__tooltip-area"></span>`;
      }
      return b2`
			<nldd-tooltip text=${text} timing="instant">
				<span class="progress-bar-segment-indicator__tooltip-area"
					tabindex="0"
					role="img"
					aria-label=${text}
				></span>
			</nldd-tooltip>
		`;
    }
  };
  NLDDProgressBarSegmentIndicator.styles = progressBarSegmentIndicatorStyles;
  __decorate91([
    n4({ type: Number, reflect: true })
  ], NLDDProgressBarSegmentIndicator.prototype, "value", void 0);
  __decorate91([
    n4({ type: String, reflect: true })
  ], NLDDProgressBarSegmentIndicator.prototype, "color", void 0);
  __decorate91([
    n4({ type: String, reflect: true })
  ], NLDDProgressBarSegmentIndicator.prototype, "name", void 0);
  __decorate91([
    n4({ type: String, reflect: true, attribute: "tooltip-text" })
  ], NLDDProgressBarSegmentIndicator.prototype, "tooltipText", void 0);
  __decorate91([
    r5()
  ], NLDDProgressBarSegmentIndicator.prototype, "_autoTooltipText", void 0);
  NLDDProgressBarSegmentIndicator = __decorate91([
    t3("nldd-progress-bar-segment-indicator")
  ], NLDDProgressBarSegmentIndicator);
  var NLDDProgressBar = class NLDDProgressBar2 extends i4 {
    constructor() {
      super(...arguments);
      this.mode = "progress";
      this.max = 100;
      this.value = null;
      this.color = "accent";
      this.size = "md";
      this.text = "";
      this.valueFormat = "percentage";
      this.valueText = "";
      this.valueDisplay = "inline";
      this.accessibleLabel = "";
      this.indeterminate = false;
      this.translations = {};
      this._segmentIndicators = [];
      this._indeterminateExiting = false;
      this._indeterminateEntering = false;
      this._onSlotChangeBound = this._onSlotChange.bind(this);
    }
    _t(key) {
      return this.translations[key] ?? nlddProgressBarTranslations[key];
    }
    /** Returns true when the user has provided segment children. */
    get _hasSegmentIndicators() {
      return this._segmentIndicators.length > 0;
    }
    /** Sum of all segment values, or the parent's own value, or 0. */
    get _totalValue() {
      if (this._hasSegmentIndicators) {
        return this._segmentIndicators.reduce((sum, s6) => sum + Math.max(0, s6.value), 0);
      }
      return Math.max(0, this.value ?? 0);
    }
    /** Percentage filled (0–100). */
    get _percentage() {
      if (this.max <= 0)
        return 0;
      return Math.min(100, this._totalValue / this.max * 100);
    }
    /** Formatted value string for the caption. */
    get _displayValue() {
      if (this.valueText)
        return this.valueText;
      if (this.indeterminate)
        return "";
      const total = this._totalValue;
      switch (this.valueFormat) {
        case "absolute":
          return `${total}`;
        case "fraction":
          return `${total} / ${this.max}`;
        case "percentage":
        default:
          return `${Math.round(this._percentage)}%`;
      }
    }
    /** Computed aria-valuetext, unless overridden. */
    get _ariaValueText() {
      if (this.accessibleLabel)
        return this.accessibleLabel;
      if (this.indeterminate)
        return this._t("components.progress-bar.loading-text");
      const completedSuffix = this._t("components.progress-bar.completed-suffix-text");
      const totalPrefix = this._t("components.progress-bar.total-prefix-text");
      if (!this._hasSegmentIndicators) {
        return `${Math.round(this._percentage)}% ${completedSuffix}`;
      }
      const named = this._segmentIndicators.filter((s6) => s6.name);
      const allNamed = named.length === this._segmentIndicators.length && this._segmentIndicators.length > 0;
      const segmentIndicatorDescriptions = this._segmentIndicators.filter((s6) => s6.value > 0).map((s6) => {
        const pct = this.max > 0 ? Math.round(s6.value / this.max * 100) : 0;
        return s6.name ? `${s6.name}: ${pct}%` : `${pct}%`;
      });
      if (this.mode === "distribution") {
        return segmentIndicatorDescriptions.join(", ");
      }
      const totalPct = Math.round(this._percentage);
      if (allNamed) {
        return `${segmentIndicatorDescriptions.join(", ")}. ${totalPrefix} ${totalPct}% ${completedSuffix}.`;
      }
      return `${totalPct}% ${completedSuffix}`;
    }
    /** True when the caption is rendered: a label is set, or there's an inline
     *  value to show (value-display="inline"). */
    get _hasCaption() {
      return !!this.text || this.valueDisplay === "inline" && !!this._displayValue;
    }
    firstUpdated() {
      this._syncSegmentIndicators();
    }
    willUpdate(changed) {
      if (changed.has("indeterminate")) {
        const previous = changed.get("indeterminate");
        if (previous === true && !this.indeterminate) {
          this._beginIndeterminateExit();
        } else if (previous === false && this.indeterminate) {
          this._beginIndeterminateEnter();
        }
      }
    }
    updated() {
      this._syncSegmentIndicators();
    }
    /** Indeterminate indicator fades out, segment-indicator grows from 0 to its target — both 300ms
     *  (matches --primitives-transition-duration-slow and nldd-progress-circle). */
    _beginIndeterminateExit() {
      this._cancelIndeterminateEnter();
      this._indeterminateExiting = true;
      this._exitTimeout = setTimeout(() => {
        this._indeterminateExiting = false;
        this._exitTimeout = void 0;
      }, INDETERMINATE_TRANSITION_MS);
    }
    /** Indeterminate indicator fades in, segment-indicator shrinks from current width to 0 — both 300ms. */
    _beginIndeterminateEnter() {
      this._cancelIndeterminateExit();
      this._indeterminateEntering = true;
      this._enterTimeout = setTimeout(() => {
        this._indeterminateEntering = false;
        this._enterTimeout = void 0;
      }, INDETERMINATE_TRANSITION_MS);
    }
    _cancelIndeterminateExit() {
      if (this._exitTimeout) {
        clearTimeout(this._exitTimeout);
        this._exitTimeout = void 0;
      }
      this._indeterminateExiting = false;
    }
    _cancelIndeterminateEnter() {
      if (this._enterTimeout) {
        clearTimeout(this._enterTimeout);
        this._enterTimeout = void 0;
      }
      this._indeterminateEntering = false;
    }
    /** Computes a segment-indicator's auto-tooltip from the parent's value-format
     *  and the segment name. Consumers override it per segment with the
     *  tooltip-text attribute, which wins over this auto-text. */
    _formatSegmentIndicatorTooltip(seg) {
      if (this.valueDisplay === "none")
        return seg.name ?? "";
      const v3 = Math.max(0, seg.value);
      const pct = this.max > 0 ? Math.round(v3 / this.max * 100) : 0;
      let valuePart;
      switch (this.valueFormat) {
        case "absolute":
          valuePart = `${v3}`;
          break;
        case "fraction":
          valuePart = `${v3} / ${this.max}`;
          break;
        case "percentage":
        default:
          valuePart = `${pct}%`;
          break;
      }
      return seg.name ? `${seg.name}: ${valuePart}` : valuePart;
    }
    disconnectedCallback() {
      super.disconnectedCallback();
      this._cancelIndeterminateExit();
      this._cancelIndeterminateEnter();
      this._attributeObserver?.disconnect();
    }
    _onSlotChange() {
      const slot = this.shadowRoot?.querySelector("slot:not([name])");
      const assigned = slot?.assignedElements({ flatten: true }) ?? [];
      this._segmentIndicators = assigned.filter((el) => el instanceof NLDDProgressBarSegmentIndicator);
      this._attributeObserver?.disconnect();
      this._attributeObserver = new MutationObserver(() => this.requestUpdate());
      for (const seg of this._segmentIndicators) {
        this._attributeObserver.observe(seg, {
          attributes: true,
          attributeFilter: ["value", "color", "name", "tooltip-text"]
        });
      }
    }
    _syncSegmentIndicators() {
      const total = this._totalValue;
      const denominator = total > this.max ? total : this.max;
      if (import_meta29.env?.DEV && total > this.max && this._hasSegmentIndicators) {
        console.warn(`[nldd-progress-bar] Sum of segment values (${total}) exceeds max (${this.max}). Segments are normalized proportionally.`);
      }
      const internalSegmentIndicator = this.shadowRoot?.querySelector(".progress-bar__segment-indicator") ?? null;
      const allSegmentIndicators = internalSegmentIndicator ? [internalSegmentIndicator, ...this._segmentIndicators] : [...this._segmentIndicators];
      const visibleCount = allSegmentIndicators.filter((s6) => Math.max(0, s6.value) > 0).length;
      const suppressAutoTooltip = visibleCount <= 1 && this.valueDisplay !== "tooltip";
      const isExiting = this._indeterminateExiting;
      const isEntering = this._indeterminateEntering;
      for (const seg of allSegmentIndicators) {
        const v3 = Math.max(0, seg.value);
        if (v3 <= 0) {
          seg.hidden = true;
          seg.removeAttribute("data-mode");
          seg._autoTooltipText = "";
          continue;
        }
        seg.hidden = false;
        const pct = denominator > 0 ? v3 / denominator * 100 : 0;
        seg.style.setProperty("--context-progress-bar-segment-indicator-width", `${pct}%`);
        seg.setAttribute("data-mode", this.mode);
        seg._autoTooltipText = suppressAutoTooltip ? "" : this._formatSegmentIndicatorTooltip(seg);
        seg.toggleAttribute("data-grow", isExiting);
        seg.toggleAttribute("data-shrink", isEntering);
      }
    }
    render() {
      return progressBarTemplate(this, this._onSlotChangeBound);
    }
  };
  NLDDProgressBar.styles = progressBarStyles;
  __decorate91([
    n4({ type: String, reflect: true })
  ], NLDDProgressBar.prototype, "mode", void 0);
  __decorate91([
    n4({ type: Number, reflect: true })
  ], NLDDProgressBar.prototype, "max", void 0);
  __decorate91([
    n4({ type: Number, reflect: true })
  ], NLDDProgressBar.prototype, "value", void 0);
  __decorate91([
    n4({ type: String, reflect: true })
  ], NLDDProgressBar.prototype, "color", void 0);
  __decorate91([
    n4({ type: String, reflect: true })
  ], NLDDProgressBar.prototype, "size", void 0);
  __decorate91([
    n4({ type: String, reflect: true })
  ], NLDDProgressBar.prototype, "text", void 0);
  __decorate91([
    n4({ type: String, reflect: true, attribute: "value-format" })
  ], NLDDProgressBar.prototype, "valueFormat", void 0);
  __decorate91([
    n4({ type: String, reflect: true, attribute: "value-text" })
  ], NLDDProgressBar.prototype, "valueText", void 0);
  __decorate91([
    n4({ type: String, reflect: true, attribute: "value-display" })
  ], NLDDProgressBar.prototype, "valueDisplay", void 0);
  __decorate91([
    n4({ type: String, reflect: true, attribute: "accessible-label" })
  ], NLDDProgressBar.prototype, "accessibleLabel", void 0);
  __decorate91([
    n4({ type: Boolean, reflect: true })
  ], NLDDProgressBar.prototype, "indeterminate", void 0);
  __decorate91([
    n4({ type: Object })
  ], NLDDProgressBar.prototype, "translations", void 0);
  __decorate91([
    r5()
  ], NLDDProgressBar.prototype, "_segmentIndicators", void 0);
  __decorate91([
    r5()
  ], NLDDProgressBar.prototype, "_indeterminateExiting", void 0);
  __decorate91([
    r5()
  ], NLDDProgressBar.prototype, "_indeterminateEntering", void 0);
  NLDDProgressBar = __decorate91([
    t3("nldd-progress-bar")
  ], NLDDProgressBar);

  // node_modules/@nldd/design-system/dist/components/status-and-feedback/progress-circle/progress-circle.js
  init_lit();
  init_decorators();

  // node_modules/@nldd/design-system/dist/components/status-and-feedback/progress-circle/progress-circle.styles.js
  init_lit();
  init_slotted_reset();
  var progressCircleStyles = i`


	/* # Host */

	:host {
		--_size: var(--primitives-space-28);
		/* SVG viewBox is 100×100 user units. To get a visual {N}px stroke at any
		   rendered size, the user-unit stroke-width must scale up by 100/size.
		   Per-size overrides below set the appropriate value. Stroke width
		   grows with circle size — must match getStrokeWidthPx() in the
		   template file. */
		--_stroke-width: calc(4px * 100 / 28);
		--_track-background-color: var(--components-progress-circle-track-background-color);
		--_track-border-color: var(--components-progress-circle-track-border-color);
		--_caption-gap: var(--components-progress-circle-caption-gap);
		--_text-color: var(--components-progress-circle-text-color);
		--_text-font: var(--components-progress-circle-text-font);
		--_supporting-text-color: var(--components-progress-circle-supporting-text-color);
		--_supporting-text-font: var(--components-progress-circle-supporting-text-font);

		${inheritedTextReset}
		box-sizing: border-box;
		display: inline-flex;
		flex-direction: column;
		align-items: center;
		gap: var(--_caption-gap);
	}

	:host([hidden]) {
		display: none;
	}

	:host([size="16"]) { --_size: var(--primitives-space-16); --_stroke-width: calc(3px * 100 / 16); }
	:host([size="20"]) { --_size: var(--primitives-space-20); --_stroke-width: calc(4px * 100 / 20); }
	:host([size="24"]) { --_size: var(--primitives-space-24); --_stroke-width: calc(4px * 100 / 24); }
	:host([size="28"]) { --_size: var(--primitives-space-28); --_stroke-width: calc(4px * 100 / 28); }
	:host([size="32"]) { --_size: var(--primitives-space-32); --_stroke-width: calc(5px * 100 / 32); }
	:host([size="40"]) { --_size: var(--primitives-space-40); --_stroke-width: calc(5px * 100 / 40); }
	:host([size="44"]) { --_size: var(--primitives-space-44); --_stroke-width: calc(6px * 100 / 44); }
	:host([size="48"]) { --_size: var(--primitives-space-48); --_stroke-width: calc(6px * 100 / 48); }
	:host([size="56"]) { --_size: var(--primitives-space-56); --_stroke-width: calc(7px * 100 / 56); }
	:host([size="64"]) { --_size: var(--primitives-space-64); --_stroke-width: calc(7px * 100 / 64); }
	:host([size="80"]) { --_size: var(--primitives-space-80); --_stroke-width: calc(8px * 100 / 80); }
	:host([size="96"]) { --_size: var(--primitives-space-96); --_stroke-width: calc(8px * 100 / 96); }


	/* # SVG wrapper */

	.progress-circle__circle {
		display: block;
		position: relative;
		width: var(--_size);
		height: var(--_size);
	}

	.progress-circle__svg {
		display: block;
		width: 100%;
		height: 100%;
		/* Start segments at 12 o'clock instead of the default 3 o'clock. */
		transform: rotate(-90deg);
	}


	/* # Track ring (background)
	   vector-effect: non-scaling-stroke keeps the stroke at its specified
	   CSS pixel width regardless of the viewBox-to-rendered-size ratio,
	   so a 4px stroke is always 4 actual pixels at any circle size. */

	.progress-circle__track {
		stroke: var(--_track-background-color);
		stroke-width: var(--_stroke-width);
	}


	/* # Arc segments
	   Animate value changes smoothly via stroke-dasharray transition.
	   Crossfade with the indeterminate indicator uses opacity on the SVG group. */

	.progress-circle__segment-indicator {
		stroke-width: var(--_stroke-width);
		opacity: 1;
		transition: stroke-dasharray var(--primitives-transition-duration-slow) ease-out, stroke-dashoffset var(--primitives-transition-duration-slow) ease-out, opacity var(--primitives-transition-duration-slow) ease-out;
	}

	.progress-circle__segment-indicator[data-shrink] {
		opacity: 0;
	}

	/* Progress mode: rounded caps for friendly segment ends. The JS gap
	   calculation compensates for the half-stroke-width extension so the
	   visible gap between segments stays consistent.
	   Distribution mode: butt caps keep the segment boundaries crisp. */

	.progress-circle__segment-indicator {
		stroke-linecap: round;
	}

	:host([mode="distribution"]) .progress-circle__segment-indicator {
		stroke-linecap: butt;
	}


	/* ## Arc color per variant */

	.progress-circle__segment-indicator--neutral { stroke: var(--components-progress-circle-neutral-background-color); }
	.progress-circle__segment-indicator--accent { stroke: var(--components-progress-circle-accent-background-color); }
	.progress-circle__segment-indicator--success { stroke: var(--components-progress-circle-success-background-color); }
	.progress-circle__segment-indicator--warning { stroke: var(--components-progress-circle-warning-background-color); }
	.progress-circle__segment-indicator--critical { stroke: var(--components-progress-circle-critical-background-color); }
	.progress-circle__segment-indicator--coolgray { stroke: var(--components-progress-circle-coolgray-background-color); }
	.progress-circle__segment-indicator--lintblauw { stroke: var(--components-progress-circle-lintblauw-background-color); }
	.progress-circle__segment-indicator--donkerblauw { stroke: var(--components-progress-circle-donkerblauw-background-color); }
	.progress-circle__segment-indicator--hemelblauw { stroke: var(--components-progress-circle-hemelblauw-background-color); }
	.progress-circle__segment-indicator--lichtblauw { stroke: var(--components-progress-circle-lichtblauw-background-color); }
	.progress-circle__segment-indicator--paars { stroke: var(--components-progress-circle-paars-background-color); }
	.progress-circle__segment-indicator--violet { stroke: var(--components-progress-circle-violet-background-color); }
	.progress-circle__segment-indicator--robijnrood { stroke: var(--components-progress-circle-robijnrood-background-color); }
	.progress-circle__segment-indicator--roze { stroke: var(--components-progress-circle-roze-background-color); }
	.progress-circle__segment-indicator--rood { stroke: var(--components-progress-circle-rood-background-color); }
	.progress-circle__segment-indicator--oranje { stroke: var(--components-progress-circle-oranje-background-color); }
	.progress-circle__segment-indicator--donkergeel { stroke: var(--components-progress-circle-donkergeel-background-color); }
	.progress-circle__segment-indicator--geel { stroke: var(--components-progress-circle-geel-background-color); }
	.progress-circle__segment-indicator--donkerbruin { stroke: var(--components-progress-circle-donkerbruin-background-color); }
	.progress-circle__segment-indicator--bruin { stroke: var(--components-progress-circle-bruin-background-color); }
	.progress-circle__segment-indicator--donkergroen { stroke: var(--components-progress-circle-donkergroen-background-color); }
	.progress-circle__segment-indicator--groen { stroke: var(--components-progress-circle-groen-background-color); }
	.progress-circle__segment-indicator--mosgroen { stroke: var(--components-progress-circle-mosgroen-background-color); }
	.progress-circle__segment-indicator--mintgroen { stroke: var(--components-progress-circle-mintgroen-background-color); }


	/* # Spinner (indeterminate)
	   GitHub-style: a fixed-width arc (25 / 100 = quarter turn, 90°) that
	   simply rotates around the circle. Calmer than Material's elastic
	   grow + shrink, which at small sizes (default 28 px) reads as visually
	   busy. pathLength="100" on the indicator normalises the dash pattern
	   to percentages of the circumference so the same numbers stay correct
	   at every size. transform-origin centres the rotation. */

	.progress-circle__indeterminate-indicator {
		stroke-width: var(--_stroke-width);
		stroke-dasharray: 25 100;
		stroke-dashoffset: 0;
		stroke: var(--_indeterminate-background-color);
		opacity: 1;
		transform-origin: 50% 50%;
		transition: opacity var(--primitives-transition-duration-slow) ease-out;
		animation: progress-circle-indeterminate-indicator-rotate 1s linear infinite;
	}

	.progress-circle__indeterminate-indicator[data-fade="out"] {
		opacity: 0;
	}

	@keyframes progress-circle-indeterminate-indicator-rotate {
		100% { transform: rotate(360deg); }
	}


	/* # Indeterminate fill + border colour follow the color variant; default accent (blue) */

	:host {
		--_indeterminate-background-color: var(--components-progress-circle-accent-background-color);
		--_indeterminate-border-color: var(--components-progress-circle-accent-border-color);
	}

	:host([color="neutral"]) { --_indeterminate-background-color: var(--components-progress-circle-neutral-background-color); --_indeterminate-border-color: var(--components-progress-circle-neutral-border-color); }
	:host([color="accent"]) { --_indeterminate-background-color: var(--components-progress-circle-accent-background-color); --_indeterminate-border-color: var(--components-progress-circle-accent-border-color); }
	:host([color="success"]) { --_indeterminate-background-color: var(--components-progress-circle-success-background-color); --_indeterminate-border-color: var(--components-progress-circle-success-border-color); }
	:host([color="warning"]) { --_indeterminate-background-color: var(--components-progress-circle-warning-background-color); --_indeterminate-border-color: var(--components-progress-circle-warning-border-color); }
	:host([color="critical"]) { --_indeterminate-background-color: var(--components-progress-circle-critical-background-color); --_indeterminate-border-color: var(--components-progress-circle-critical-border-color); }
	:host([color="coolgray"]) { --_indeterminate-background-color: var(--components-progress-circle-coolgray-background-color); --_indeterminate-border-color: var(--components-progress-circle-coolgray-border-color); }
	:host([color="lintblauw"]) { --_indeterminate-background-color: var(--components-progress-circle-lintblauw-background-color); --_indeterminate-border-color: var(--components-progress-circle-lintblauw-border-color); }
	:host([color="donkerblauw"]) { --_indeterminate-background-color: var(--components-progress-circle-donkerblauw-background-color); --_indeterminate-border-color: var(--components-progress-circle-donkerblauw-border-color); }
	:host([color="hemelblauw"]) { --_indeterminate-background-color: var(--components-progress-circle-hemelblauw-background-color); --_indeterminate-border-color: var(--components-progress-circle-hemelblauw-border-color); }
	:host([color="lichtblauw"]) { --_indeterminate-background-color: var(--components-progress-circle-lichtblauw-background-color); --_indeterminate-border-color: var(--components-progress-circle-lichtblauw-border-color); }
	:host([color="paars"]) { --_indeterminate-background-color: var(--components-progress-circle-paars-background-color); --_indeterminate-border-color: var(--components-progress-circle-paars-border-color); }
	:host([color="violet"]) { --_indeterminate-background-color: var(--components-progress-circle-violet-background-color); --_indeterminate-border-color: var(--components-progress-circle-violet-border-color); }
	:host([color="robijnrood"]) { --_indeterminate-background-color: var(--components-progress-circle-robijnrood-background-color); --_indeterminate-border-color: var(--components-progress-circle-robijnrood-border-color); }
	:host([color="roze"]) { --_indeterminate-background-color: var(--components-progress-circle-roze-background-color); --_indeterminate-border-color: var(--components-progress-circle-roze-border-color); }
	:host([color="rood"]) { --_indeterminate-background-color: var(--components-progress-circle-rood-background-color); --_indeterminate-border-color: var(--components-progress-circle-rood-border-color); }
	:host([color="oranje"]) { --_indeterminate-background-color: var(--components-progress-circle-oranje-background-color); --_indeterminate-border-color: var(--components-progress-circle-oranje-border-color); }
	:host([color="donkergeel"]) { --_indeterminate-background-color: var(--components-progress-circle-donkergeel-background-color); --_indeterminate-border-color: var(--components-progress-circle-donkergeel-border-color); }
	:host([color="geel"]) { --_indeterminate-background-color: var(--components-progress-circle-geel-background-color); --_indeterminate-border-color: var(--components-progress-circle-geel-border-color); }
	:host([color="donkerbruin"]) { --_indeterminate-background-color: var(--components-progress-circle-donkerbruin-background-color); --_indeterminate-border-color: var(--components-progress-circle-donkerbruin-border-color); }
	:host([color="bruin"]) { --_indeterminate-background-color: var(--components-progress-circle-bruin-background-color); --_indeterminate-border-color: var(--components-progress-circle-bruin-border-color); }
	:host([color="donkergroen"]) { --_indeterminate-background-color: var(--components-progress-circle-donkergroen-background-color); --_indeterminate-border-color: var(--components-progress-circle-donkergroen-border-color); }
	:host([color="groen"]) { --_indeterminate-background-color: var(--components-progress-circle-groen-background-color); --_indeterminate-border-color: var(--components-progress-circle-groen-border-color); }
	:host([color="mosgroen"]) { --_indeterminate-background-color: var(--components-progress-circle-mosgroen-background-color); --_indeterminate-border-color: var(--components-progress-circle-mosgroen-border-color); }
	:host([color="mintgroen"]) { --_indeterminate-background-color: var(--components-progress-circle-mintgroen-background-color); --_indeterminate-border-color: var(--components-progress-circle-mintgroen-border-color); }


	/* # Caption (label + value) */

	.progress-circle__caption {
		display: flex;
		flex-direction: column;
		align-items: center;
	}

	.progress-circle__text {
		color: var(--_text-color);
		font: var(--_text-font);
		text-align: center;
	}

	.progress-circle__supporting-text {
		color: var(--_supporting-text-color);
		font: var(--_supporting-text-font);
		text-align: center;
	}


	/* # Slot
	   Segment children are data-only; hide them visually but keep them in
	   the DOM so consumers can mutate values. */

	slot {
		display: none;
	}


	/* # Accessibility */

	@media (prefers-reduced-motion: reduce) {
		.progress-circle__indeterminate-indicator {
			/* Static arc with the same 25 / 100 dash pattern as the full-motion
			   spinner — only the rotation is removed and an opacity pulse
			   takes its place. Keeping the dasharray identical means the
			   placeholder shape doesn't visibly jump when a user toggles
			   reduce-motion at runtime. */
			animation: progress-circle-indeterminate-indicator-pulse 2s ease-in-out infinite;
		}

		@keyframes progress-circle-indeterminate-indicator-pulse {
			0%, 100% { opacity: 0.3; }
			50% { opacity: 0.7; }
		}

		.progress-circle__segment-indicator {
			transition: none;
			animation: none;
		}
	}

	@media (forced-colors: active) {
		.progress-circle__track,
		.progress-circle__segment-indicator,
		.progress-circle__indeterminate-indicator {
			stroke: CanvasText;
		}
	}
`;
  var progressCircleSegmentIndicatorStyles = i`
	:host {
		display: none;
	}
`;

  // node_modules/@nldd/design-system/dist/components/status-and-feedback/progress-circle/progress-circle.template.js
  init_lit();
  init_if_defined2();
  function getStrokeWidthPx(sizeInPixels) {
    if (sizeInPixels <= 16)
      return 3;
    if (sizeInPixels <= 28)
      return 4;
    if (sizeInPixels <= 40)
      return 5;
    if (sizeInPixels <= 48)
      return 6;
    if (sizeInPixels <= 64)
      return 7;
    return 8;
  }
  function getRadius(sizeInPixels) {
    const strokeWidthUserUnits = getStrokeWidthPx(sizeInPixels) * 100 / sizeInPixels;
    return 50 - strokeWidthUserUnits / 2;
  }
  function getCircumference(sizeInPixels) {
    return 2 * Math.PI * getRadius(sizeInPixels);
  }
  function getBorderErodeRadius(sizeInPixels) {
    return 100 / sizeInPixels;
  }
  function progressCircleTemplate(component, onSlotChange) {
    const tooltipText = component._tooltipText;
    const ariaText = component._ariaValueText;
    const ariaValueNow = component.indeterminate ? void 0 : Math.round(component._totalValue);
    const isExiting = component._indeterminateExiting;
    const isIndeterminate = (component.indeterminate || isExiting) && !component._hasSegmentIndicators;
    const showArcs = !component.indeterminate || component._indeterminateEntering;
    const arcs = showArcs ? component._buildArcs() : [];
    const sizeInPixels = Number(component.size) || 28;
    const radius = getRadius(sizeInPixels);
    const circumference = getCircumference(sizeInPixels);
    const borderErodeRadius = getBorderErodeRadius(sizeInPixels).toFixed(3);
    const arcColors = [...new Set(arcs.map((arc) => arc.color))];
    const uid = component._uid;
    const ariaLabel = component.text || component._t("components.progress-circle.label-text");
    const circle = b2`
		<div class="progress-circle__circle"
			role="progressbar"
			aria-label=${ariaLabel}
			aria-valuemin="0"
			aria-valuemax=${component.max}
			aria-valuenow=${o7(ariaValueNow)}
			aria-valuetext=${ariaText}
		>
			<svg class="progress-circle__svg"
				viewBox="0 0 100 100"
				aria-hidden="true"
			>
				<defs>
					<filter id="progress-circle-border-track-${uid}" filterUnits="userSpaceOnUse" x="-10" y="-10" width="120" height="120" color-interpolation-filters="sRGB">
						<feMorphology in="SourceGraphic" operator="erode" radius=${borderErodeRadius} result="eroded"></feMorphology>
						<feComposite operator="out" in="SourceGraphic" in2="eroded" result="edge"></feComposite>
						<feFlood style="flood-color: var(--_track-border-color)" result="flood"></feFlood>
						<feComposite operator="in" in="flood" in2="edge" result="colored"></feComposite>
						<feComposite operator="over" in="colored" in2="SourceGraphic"></feComposite>
					</filter>
					${arcColors.map((color) => w`
						<filter id="progress-circle-border-${color}-${uid}" filterUnits="userSpaceOnUse" x="-10" y="-10" width="120" height="120" color-interpolation-filters="sRGB">
							<feMorphology in="SourceGraphic" operator="erode" radius=${borderErodeRadius} result="eroded"></feMorphology>
							<feComposite operator="out" in="SourceGraphic" in2="eroded" result="edge"></feComposite>
							<feFlood style="flood-color: var(--components-progress-circle-${color}-border-color)" result="flood"></feFlood>
							<feComposite operator="in" in="flood" in2="edge" result="colored"></feComposite>
							<feComposite operator="over" in="colored" in2="SourceGraphic"></feComposite>
						</filter>
					`)}
					${isIndeterminate ? w`
						<filter id="progress-circle-border-indeterminate-${uid}" filterUnits="userSpaceOnUse" x="-10" y="-10" width="120" height="120" color-interpolation-filters="sRGB">
							<feMorphology in="SourceGraphic" operator="erode" radius=${borderErodeRadius} result="eroded"></feMorphology>
							<feComposite operator="out" in="SourceGraphic" in2="eroded" result="edge"></feComposite>
							<feFlood style="flood-color: var(--_indeterminate-border-color)" result="flood"></feFlood>
							<feComposite operator="in" in="flood" in2="edge" result="colored"></feComposite>
							<feComposite operator="over" in="colored" in2="SourceGraphic"></feComposite>
						</filter>
					` : A}
				</defs>
				<circle class="progress-circle__track"
					cx="50"
					cy="50"
					r=${radius}
					fill="none"
					filter="url(#progress-circle-border-track-${uid})"
				></circle>
				${arcs.map((arc) => w`
					<circle class="progress-circle__segment-indicator progress-circle__segment-indicator--${arc.color}"
						cx="50"
						cy="50"
						r=${radius}
						fill="none"
						stroke-dasharray="${arc.length} ${circumference}"
						stroke-dashoffset=${-arc.offset}
						filter="url(#progress-circle-border-${arc.color}-${uid})"
					></circle>
				`)}
				${isIndeterminate ? w`
					<circle class="progress-circle__indeterminate-indicator"
						cx="50"
						cy="50"
						r=${radius}
						fill="none"
						pathLength="100"
						stroke-linecap="round"
						filter="url(#progress-circle-border-indeterminate-${uid})"
					></circle>
				` : A}
			</svg>
		</div>
	`;
    const wrappedCircle = tooltipText ? b2`
			<nldd-tooltip
				text=${tooltipText}
				placement="top"
				timing="instant"
			>${circle}</nldd-tooltip>
		` : circle;
    return b2`
		${wrappedCircle}
		<slot @slotchange=${onSlotChange}></slot>
		${component.text || component.valueDisplay === "inline" && component._displayValue ? b2`
			<div class="progress-circle__caption">
				${component.text ? b2`<span class="progress-circle__text">${component.text}</span>` : A}
				${component.valueDisplay === "inline" && component._displayValue ? b2`<span class="progress-circle__supporting-text">${component._displayValue}</span>` : A}
			</div>
		` : A}
	`;
  }

  // node_modules/@nldd/design-system/dist/components/status-and-feedback/progress-circle/progress-circle.i18n.js
  var nlddProgressCircleTranslations = {
    "components.progress-circle.completed-suffix-text": "voltooid",
    "components.progress-circle.total-prefix-text": "Totaal",
    "components.progress-circle.loading-text": "Aan het laden",
    "components.progress-circle.label-text": "Voortgang"
  };

  // node_modules/@nldd/design-system/dist/components/status-and-feedback/progress-circle/progress-circle.js
  init_tooltip();
  var __decorate92 = function(decorators, target, key, desc) {
    var c6 = arguments.length, r6 = c6 < 3 ? target : desc === null ? desc = Object.getOwnPropertyDescriptor(target, key) : desc, d3;
    if (typeof Reflect === "object" && typeof Reflect.decorate === "function") r6 = Reflect.decorate(decorators, target, key, desc);
    else for (var i8 = decorators.length - 1; i8 >= 0; i8--) if (d3 = decorators[i8]) r6 = (c6 < 3 ? d3(r6) : c6 > 3 ? d3(target, key, r6) : d3(target, key)) || r6;
    return c6 > 3 && r6 && Object.defineProperty(target, key, r6), r6;
  };
  var NLDDProgressCircle_1;
  var INDETERMINATE_TRANSITION_MS2 = 300;
  var NLDDProgressCircleSegmentIndicator = class NLDDProgressCircleSegmentIndicator2 extends i4 {
    constructor() {
      super(...arguments);
      this.value = 0;
      this.color = "accent";
      this.name = "";
    }
    render() {
      return A;
    }
  };
  NLDDProgressCircleSegmentIndicator.styles = progressCircleSegmentIndicatorStyles;
  __decorate92([
    n4({ type: Number, reflect: true })
  ], NLDDProgressCircleSegmentIndicator.prototype, "value", void 0);
  __decorate92([
    n4({ type: String, reflect: true })
  ], NLDDProgressCircleSegmentIndicator.prototype, "color", void 0);
  __decorate92([
    n4({ type: String, reflect: true })
  ], NLDDProgressCircleSegmentIndicator.prototype, "name", void 0);
  NLDDProgressCircleSegmentIndicator = __decorate92([
    t3("nldd-progress-circle-segment-indicator")
  ], NLDDProgressCircleSegmentIndicator);
  var NLDDProgressCircle = NLDDProgressCircle_1 = class NLDDProgressCircle2 extends i4 {
    constructor() {
      super(...arguments);
      this.mode = "progress";
      this.max = 100;
      this.value = null;
      this.color = "accent";
      this.size = "28";
      this.text = "";
      this.valueFormat = "percentage";
      this.valueText = "";
      this.valueDisplay = "tooltip";
      this.accessibleLabel = "";
      this.indeterminate = false;
      this.translations = {};
      this._segmentIndicators = [];
      this._indeterminateExiting = false;
      this._indeterminateEntering = false;
      this._arcsAtZero = false;
      this._uid = `pc-${NLDDProgressCircle_1._idCounter++}`;
      this._onSlotChangeBound = this._onSlotChange.bind(this);
    }
    _t(key) {
      return this.translations[key] ?? nlddProgressCircleTranslations[key];
    }
    get _hasSegmentIndicators() {
      return this._segmentIndicators.length > 0;
    }
    get _totalValue() {
      if (this._hasSegmentIndicators) {
        return this._segmentIndicators.reduce((sum, s6) => sum + Math.max(0, s6.value), 0);
      }
      return Math.max(0, this.value ?? 0);
    }
    get _percentage() {
      if (this.max <= 0)
        return 0;
      return Math.min(100, this._totalValue / this.max * 100);
    }
    /** Formatted value for inline display (value-display="inline"). Mirrors
     *  nldd-progress-bar: valueText wins, empty while indeterminate, otherwise
     *  the total formatted per value-format. */
    get _displayValue() {
      if (this.valueText)
        return this.valueText;
      if (this.indeterminate)
        return "";
      return this._formatValuePart(this._totalValue, Math.round(this._percentage));
    }
    /** Compact text shown in the tooltip — only in value-display="tooltip" mode
     *  (returns '' otherwise, so the template skips the tooltip wrapper). Follows
     *  value-format (matching nldd-progress-bar's per-segment tooltip style) and
     *  omits the "voltooid" suffix that aria-valuetext uses for screen readers.
     *  valueText overrides the whole thing; indeterminate shows the loading text. */
    get _tooltipText() {
      if (this.valueDisplay !== "tooltip")
        return "";
      if (this.indeterminate)
        return this._t("components.progress-circle.loading-text");
      if (this.valueText)
        return this.valueText;
      if (!this._hasSegmentIndicators) {
        const v3 = Math.max(0, this.value ?? 0);
        const pct = this.max > 0 ? Math.round(v3 / this.max * 100) : 0;
        return this._formatValuePart(v3, pct);
      }
      const named = this._segmentIndicators.filter((s6) => s6.name);
      const allNamed = named.length === this._segmentIndicators.length && this._segmentIndicators.length > 0;
      const segmentIndicatorDescriptions = this._segmentIndicators.filter((s6) => s6.value > 0).map((s6) => {
        const pct = this.max > 0 ? Math.round(s6.value / this.max * 100) : 0;
        const valuePart = this._formatValuePart(s6.value, pct);
        return s6.name ? `${s6.name}: ${valuePart}` : valuePart;
      });
      if (this.mode === "distribution") {
        return segmentIndicatorDescriptions.join(", ");
      }
      const totalPct = Math.round(this._percentage);
      const totalValuePart = this._formatValuePart(this._totalValue, totalPct);
      if (allNamed) {
        const totalPrefix = this._t("components.progress-circle.total-prefix-text");
        return `${segmentIndicatorDescriptions.join(", ")}. ${totalPrefix} ${totalValuePart}`;
      }
      return totalValuePart;
    }
    /** Verbose text used for aria-valuetext (screen readers). Includes the
     *  "voltooid" suffix and total-prefix for clearer announcements. */
    get _ariaValueText() {
      if (this.accessibleLabel)
        return this.accessibleLabel;
      if (this.indeterminate)
        return this._t("components.progress-circle.loading-text");
      const completedSuffix = this._t("components.progress-circle.completed-suffix-text");
      const totalPrefix = this._t("components.progress-circle.total-prefix-text");
      if (!this._hasSegmentIndicators) {
        return `${Math.round(this._percentage)}% ${completedSuffix}`;
      }
      const named = this._segmentIndicators.filter((s6) => s6.name);
      const allNamed = named.length === this._segmentIndicators.length && this._segmentIndicators.length > 0;
      const segmentIndicatorDescriptions = this._segmentIndicators.filter((s6) => s6.value > 0).map((s6) => {
        const pct = this.max > 0 ? Math.round(s6.value / this.max * 100) : 0;
        const valuePart = this._formatValuePart(s6.value, pct);
        return s6.name ? `${s6.name}: ${valuePart}` : valuePart;
      });
      if (this.mode === "distribution") {
        return segmentIndicatorDescriptions.join(", ");
      }
      const totalPct = Math.round(this._percentage);
      if (allNamed) {
        return `${segmentIndicatorDescriptions.join(", ")}. ${totalPrefix} ${totalPct}% ${completedSuffix}.`;
      }
      return `${totalPct}% ${completedSuffix}`;
    }
    _formatValuePart(value, pct) {
      switch (this.valueFormat) {
        case "absolute":
          return `${value}`;
        case "fraction":
          return `${value} / ${this.max}`;
        case "percentage":
        default:
          return `${pct}%`;
      }
    }
    willUpdate(changed) {
      if (changed.has("indeterminate")) {
        const previous = changed.get("indeterminate");
        if (previous === true && !this.indeterminate) {
          this._beginIndeterminateExit();
        } else if (previous === false && this.indeterminate) {
          this._beginIndeterminateEnter();
        }
      }
    }
    disconnectedCallback() {
      super.disconnectedCallback();
      this._cancelIndeterminateExit();
      this._cancelIndeterminateEnter();
      this._attributeObserver?.disconnect();
    }
    _beginIndeterminateExit() {
      this._cancelIndeterminateEnter();
      this._indeterminateExiting = true;
      this._arcsAtZero = true;
      this.updateComplete.then(() => {
        this._growRafId = requestAnimationFrame(() => {
          this._growRafId = void 0;
          this._arcsAtZero = false;
        });
      });
      this._exitTimeout = setTimeout(() => {
        this._indeterminateExiting = false;
        this._exitTimeout = void 0;
      }, INDETERMINATE_TRANSITION_MS2);
    }
    _beginIndeterminateEnter() {
      this._cancelIndeterminateExit();
      this._indeterminateEntering = true;
      this._enterTimeout = setTimeout(() => {
        this._indeterminateEntering = false;
        this._enterTimeout = void 0;
      }, INDETERMINATE_TRANSITION_MS2);
    }
    _cancelIndeterminateExit() {
      if (this._exitTimeout) {
        clearTimeout(this._exitTimeout);
        this._exitTimeout = void 0;
      }
      if (this._growRafId !== void 0) {
        cancelAnimationFrame(this._growRafId);
        this._growRafId = void 0;
      }
      this._indeterminateExiting = false;
      this._arcsAtZero = false;
    }
    _cancelIndeterminateEnter() {
      if (this._enterTimeout) {
        clearTimeout(this._enterTimeout);
        this._enterTimeout = void 0;
      }
      this._indeterminateEntering = false;
    }
    _onSlotChange() {
      const slot = this.shadowRoot?.querySelector("slot:not([name])");
      const assigned = slot?.assignedElements({ flatten: true }) ?? [];
      this._segmentIndicators = assigned.filter(
        // instanceof, not tagName === '...': tagName comparison breaks
        // under scoped custom-element registries where the same class
        // can be registered under a different tag.
        (el) => el instanceof NLDDProgressCircleSegmentIndicator
      );
      this._attributeObserver?.disconnect();
      this._attributeObserver = new MutationObserver(() => this.requestUpdate());
      for (const seg of this._segmentIndicators) {
        this._attributeObserver.observe(seg, {
          attributes: true,
          attributeFilter: ["value", "color", "name"]
        });
      }
    }
    /** Build the SVG arc list for the current segments (or a single synthetic
     *  segment when the user provides only `value`). Each arc gets a length
     *  and offset on the shared circle so they line up.
     *
     *  @internal `public` here is required for the render template to call
     *  this method (templates live outside the class). Treat it as private
     *  externally — the leading underscore signals "not API". */
    _buildArcs() {
      const arcs = [];
      const sourceSegmentIndicators = this._hasSegmentIndicators ? this._segmentIndicators.map((s6) => ({ value: Math.max(0, s6.value), color: s6.color })) : this.value !== null ? [{ value: Math.max(0, this.value), color: this.color }] : [];
      const sizeInPixels = Number(this.size) || 28;
      const strokeWidthPx = getStrokeWidthPx(sizeInPixels);
      const isMultiSegmentIndicator = sourceSegmentIndicators.length > 1 || this.mode === "distribution";
      const isProgressMode = this.mode !== "distribution";
      const visibleGapPx = isMultiSegmentIndicator ? isProgressMode ? 1 : 2 : 0;
      const capCompensationPx = isProgressMode && isMultiSegmentIndicator ? strokeWidthPx : 0;
      const gapLength = (visibleGapPx + capCompensationPx) * (100 / sizeInPixels);
      const circumference = getCircumference(sizeInPixels);
      const total = this._totalValue;
      const denominator = total > this.max ? total : this.max;
      let cumulative = 0;
      for (const seg of sourceSegmentIndicators) {
        const fraction = denominator > 0 ? seg.value / denominator : 0;
        const rawLength = fraction * circumference;
        const length = this._arcsAtZero || seg.value <= 0 ? 0 : Math.max(0, rawLength - gapLength);
        arcs.push({ length, offset: cumulative, color: seg.color });
        cumulative += rawLength;
      }
      return arcs;
    }
    render() {
      return progressCircleTemplate(this, this._onSlotChangeBound);
    }
    updated(changed) {
      if (changed.has("_indeterminateEntering") || changed.has("_indeterminateExiting")) {
        this._syncDynamicAttributes();
      }
    }
    /**
     * Toggle the data-shrink / data-fade attributes after Lit's render. These
     * are conditional (presence-or-absence) and Lit's `=${expr}` binding has
     * quirks removing SVG attributes; setAttribute/removeAttribute is reliable.
     * Other dynamic SVG attributes (stroke-dasharray, stroke-dashoffset) are
     * always set and live in the Lit template.
     */
    _syncDynamicAttributes() {
      const segments = this.shadowRoot?.querySelectorAll(".progress-circle__segment-indicator");
      if (segments) {
        for (const el of segments) {
          if (this._indeterminateEntering) {
            el.setAttribute("data-shrink", "");
          } else {
            el.removeAttribute("data-shrink");
          }
        }
      }
      const indicator = this.shadowRoot?.querySelector(".progress-circle__indeterminate-indicator");
      if (indicator) {
        if (this._indeterminateExiting && !this.indeterminate) {
          indicator.setAttribute("data-fade", "out");
        } else {
          indicator.removeAttribute("data-fade");
        }
      }
    }
  };
  NLDDProgressCircle.styles = progressCircleStyles;
  NLDDProgressCircle._idCounter = 0;
  __decorate92([
    n4({ type: String, reflect: true })
  ], NLDDProgressCircle.prototype, "mode", void 0);
  __decorate92([
    n4({ type: Number, reflect: true })
  ], NLDDProgressCircle.prototype, "max", void 0);
  __decorate92([
    n4({ type: Number, reflect: true })
  ], NLDDProgressCircle.prototype, "value", void 0);
  __decorate92([
    n4({ type: String, reflect: true })
  ], NLDDProgressCircle.prototype, "color", void 0);
  __decorate92([
    n4({ type: String, reflect: true })
  ], NLDDProgressCircle.prototype, "size", void 0);
  __decorate92([
    n4({ type: String, reflect: true })
  ], NLDDProgressCircle.prototype, "text", void 0);
  __decorate92([
    n4({ type: String, reflect: true, attribute: "value-format" })
  ], NLDDProgressCircle.prototype, "valueFormat", void 0);
  __decorate92([
    n4({ type: String, reflect: true, attribute: "value-text" })
  ], NLDDProgressCircle.prototype, "valueText", void 0);
  __decorate92([
    n4({ type: String, reflect: true, attribute: "value-display" })
  ], NLDDProgressCircle.prototype, "valueDisplay", void 0);
  __decorate92([
    n4({ type: String, reflect: true, attribute: "accessible-label" })
  ], NLDDProgressCircle.prototype, "accessibleLabel", void 0);
  __decorate92([
    n4({ type: Boolean, reflect: true })
  ], NLDDProgressCircle.prototype, "indeterminate", void 0);
  __decorate92([
    n4({ type: Object })
  ], NLDDProgressCircle.prototype, "translations", void 0);
  __decorate92([
    r5()
  ], NLDDProgressCircle.prototype, "_segmentIndicators", void 0);
  __decorate92([
    r5()
  ], NLDDProgressCircle.prototype, "_indeterminateExiting", void 0);
  __decorate92([
    r5()
  ], NLDDProgressCircle.prototype, "_indeterminateEntering", void 0);
  __decorate92([
    r5()
  ], NLDDProgressCircle.prototype, "_arcsAtZero", void 0);
  NLDDProgressCircle = NLDDProgressCircle_1 = __decorate92([
    t3("nldd-progress-circle")
  ], NLDDProgressCircle);

  // node_modules/@nldd/design-system/dist/components/index.js
  init_activity_indicator();
})();
/*! Bundled license information:

@lit/reactive-element/css-tag.js:
  (**
   * @license
   * Copyright 2019 Google LLC
   * SPDX-License-Identifier: BSD-3-Clause
   *)

@lit/reactive-element/reactive-element.js:
lit-html/lit-html.js:
lit-element/lit-element.js:
@lit/reactive-element/decorators/custom-element.js:
@lit/reactive-element/decorators/property.js:
@lit/reactive-element/decorators/state.js:
@lit/reactive-element/decorators/event-options.js:
@lit/reactive-element/decorators/base.js:
@lit/reactive-element/decorators/query.js:
@lit/reactive-element/decorators/query-all.js:
@lit/reactive-element/decorators/query-async.js:
@lit/reactive-element/decorators/query-assigned-nodes.js:
lit-html/directive.js:
lit-html/directives/unsafe-html.js:
lit-html/directives/repeat.js:
  (**
   * @license
   * Copyright 2017 Google LLC
   * SPDX-License-Identifier: BSD-3-Clause
   *)

lit-html/is-server.js:
  (**
   * @license
   * Copyright 2022 Google LLC
   * SPDX-License-Identifier: BSD-3-Clause
   *)

@lit/reactive-element/decorators/query-assigned-elements.js:
  (**
   * @license
   * Copyright 2021 Google LLC
   * SPDX-License-Identifier: BSD-3-Clause
   *)

lit-html/directives/class-map.js:
lit-html/directives/if-defined.js:
lit-html/directives/style-map.js:
  (**
   * @license
   * Copyright 2018 Google LLC
   * SPDX-License-Identifier: BSD-3-Clause
   *)

prismjs/prism.js:
  (**
   * Prism: Lightweight, robust, elegant syntax highlighting
   *
   * @license MIT <https://opensource.org/licenses/MIT>
   * @author Lea Verou <https://lea.verou.me>
   * @namespace
   * @public
   *)

lit-html/directive-helpers.js:
lit-html/static.js:
  (**
   * @license
   * Copyright 2020 Google LLC
   * SPDX-License-Identifier: BSD-3-Clause
   *)
*/
