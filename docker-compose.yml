services:
  postgres:
    image: postgres:17
    container_name: wies-postgres
    env_file: .env
    volumes:
      - postgres_data:/var/lib/postgresql/data
    ports:
      - "5432:5432"
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U wies"]
      interval: 5s
      timeout: 5s
      retries: 5

  django:
    build:
      context: .
      dockerfile: ./Dockerfile
      target: web
    ports:
      - 8080:8080
    volumes:
      - .:/app:z
    command: python manage.py runserver 0.0.0.0:8080
    env_file: .env
    depends_on:
      postgres:
        condition: service_healthy
  db_worker:
    build:
      context: .
      dockerfile: ./Dockerfile
      target: worker
    volumes:
      - .:/app:z
    command: python manage.py db_worker
    env_file: .env
    depends_on:
      postgres:
        condition: service_healthy
    healthcheck:
      test:
        [
          "CMD",
          "python",
          "-c",
          "import urllib.request; urllib.request.urlopen('http://localhost:8080/')",
        ]
      interval: 10s
      timeout: 5s
      retries: 3
      start_period: 10s

volumes:
  postgres_data:
